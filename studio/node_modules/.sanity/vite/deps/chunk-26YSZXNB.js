import {
  AnimatePresence,
  AvatarSkeleton$1,
  BehaviorSubject,
  Button as Button2,
  COMMENTS_INSPECTOR_NAME,
  CapabilityGate,
  ChangeConnectorRoot,
  ChangeFieldWrapper,
  ChangeIndicatorsTracker,
  ChangeList,
  ChangesError,
  ChannelError,
  ClientError,
  CommandList,
  CommentsEnabledContext,
  CommentsEnabledProvider,
  CommentsProvider,
  ContextMenuButton,
  CopyPasteProvider,
  DEFAULT_STUDIO_CLIENT_OPTIONS,
  Dialog as Dialog2,
  DocumentChangeContext,
  DocumentPaneContext,
  DocumentPreviewPresence,
  DocumentSheetListContext,
  DocumentStatus,
  DocumentStatusIndicator,
  EMPTY,
  EMPTY_ARRAY$B,
  EMPTY_OBJECT,
  ErrorActions,
  ErrorBoundary,
  Event$1,
  EventsProvider,
  FieldActionsProvider,
  FieldActionsResolver,
  Filters,
  FormBuilder,
  GetFormValueProvider,
  GetHookCollectionState,
  Hotkeys,
  InsufficientPermissionsMessage,
  IntentLink,
  IsLastPaneProvider,
  LATEST,
  LegacyLayerProvider,
  LoadingBlock,
  MenuButton,
  MenuGroup,
  MenuItem as MenuItem2,
  NEVER,
  NoChanges,
  PaneContext,
  PaneLayoutContext,
  PaneRouterContext,
  PerspectiveProvider,
  Popover as Popover2,
  PresenceOverlay,
  PreviewCard,
  RELEASES_INTENT,
  ReferenceInputOptionsContext,
  ReferenceInputOptionsProvider,
  ReleaseAvatar,
  ReleasesNav,
  Resizable,
  ReviewChangesContext,
  SanityDefaultPreview,
  ScrollContainer,
  SearchHeader,
  SearchProvider,
  ServerError,
  SourceProvider,
  StateLink,
  StatusButton,
  StructureToolContext,
  Subject,
  TIMELINE_ITEM_I18N_KEY_MAPPING,
  Tab,
  TextWithTone,
  Tooltip,
  TooltipDelayGroupProvider,
  TooltipOfDisabled,
  Translate,
  UserAvatar,
  VersionChip$1,
  VersionInlineBadge,
  VirtualizerScrollInstanceProvider,
  addWeeks,
  asyncScheduler,
  catchError,
  catchWithCount,
  collate,
  concat,
  createHookFromObservableFactory,
  createPatchChannel,
  createSWR,
  createSearch,
  debounce,
  debounceTime,
  decodeJsonParams,
  defer,
  defineEvent,
  defineLocaleResourceBundle,
  definePlugin,
  delay,
  diffInput,
  distinctUntilChanged,
  encodeJsonParams,
  exhaustMapWithTrailing,
  filter,
  firstValueFrom,
  format,
  formatRelativeLocalePublishDate,
  from,
  fromEvent,
  fromMutationPatches,
  fromString,
  generateHelpUrl,
  get,
  getConfigContextFromSource,
  getDocumentVariantType,
  getDraftId,
  getPreviewStateObservable$1,
  getPreviewValueWithFallback,
  getPublishedId,
  getReleaseIdFromReleaseDocumentId,
  getReleaseTone,
  getSanityCreateLinkMetadata,
  getSearchableTypes,
  getVersionFromId,
  getVersionId,
  getVersionInlineBadge,
  isAfter,
  isArray,
  isBefore,
  isCreateDocumentVersionEvent,
  isDeleteDocumentGroupEvent,
  isDeleteDocumentVersionEvent,
  isDeprecatedSchemaType,
  isDev,
  isDocumentSchemaType,
  isDraftId,
  isDraftPerspective,
  isEditDocumentVersionEvent,
  isGoingToUnpublish,
  isHotkey,
  isIndexSegment,
  isKeySegment,
  isNewDocument,
  isObjectSchemaType,
  isPerspectiveWriteable,
  isPublishDocumentVersionEvent,
  isPublishedId,
  isPublishedPerspective,
  isRecord$4,
  isReferenceSchemaType,
  isReleaseDocument,
  isReleaseScheduledOrScheduling,
  isSanityCreateLinked,
  isSanityDefinedAction,
  isSanityDocument,
  isScheduleDocumentVersionEvent,
  isString,
  isSystemBundle,
  isUnpublishDocumentEvent,
  isUnscheduleDocumentVersionEvent,
  isValidationError,
  isValidationErrorMarker,
  isValidationWarning,
  isVersionId,
  map,
  mapTo,
  merge,
  mergeMap,
  motion,
  observableCallback,
  of,
  parse,
  partition,
  require_capitalize,
  require_createCompounder,
  require_find,
  require_fromPairs,
  require_isEqual,
  require_negate,
  require_noop,
  require_omit,
  require_quick_lru,
  require_react_fast_compare,
  require_shallow_equals,
  require_speakingurl,
  require_startCase,
  require_toPairs,
  require_uniq,
  require_uniqBy,
  require_uniqueId,
  resizeObserver,
  resolveKeyedPath,
  route,
  scan,
  share,
  shareReplay,
  startWith,
  switchMap,
  take,
  takeUntil,
  tap,
  throttleTime,
  throwError,
  timer,
  toString,
  useActiveReleases,
  useActiveWorkspace,
  useArchivedReleases,
  useCanvasCompanionDoc,
  useClient,
  useCommentsEnabled,
  useConditionalToast,
  useConfigContextFromSource,
  useCopyPaste,
  useCurrentUser,
  useDateTimeFormat,
  useDocumentForm,
  useDocumentOperation,
  useDocumentOperationEvent,
  useDocumentPairPermissions,
  useDocumentPresence,
  useDocumentPreview,
  useDocumentPreviewStore,
  useDocumentStore,
  useDocumentType,
  useDocumentVersionInfo,
  useDocumentVersions,
  useEditState,
  useEvents,
  useEventsStore,
  useFieldActions,
  useGetI18nText,
  useGlobalCopyPasteElementHandler,
  useI18nText,
  useInitialValue,
  useIntentLink,
  useKeyValueStore,
  useListFormat,
  useManageFavorite,
  useMiddlewareComponents,
  useNavigateToCanvasDoc,
  useObservable,
  useObservableEvent,
  useOnlyHasVersions,
  usePerspective,
  useProjectId$1,
  useReconnectingToast,
  useRelativeTime,
  useRouter,
  useSanityCreateConfig,
  useSchema,
  useSearchMaxFieldDepth,
  useSearchState,
  useSetPerspective,
  useSource,
  useStudioUrl,
  useSyncState,
  useTelemetry,
  useTemplatePermissions,
  useTemplates,
  useTimelineSelector,
  useTimelineStore,
  useTranslation,
  useUnique,
  useUser,
  useValidationStatus,
  useVersionOperations,
  useWorkspace,
  useZIndex,
  v4_default,
  withLatestFrom,
  wrap
} from "./chunk-YEOJDRMZ.js";
import {
  AddIcon,
  ArrowLeftIcon,
  AvatarStack,
  Badge,
  BoundaryElementProvider,
  Box,
  Button,
  Card,
  Checkbox,
  CheckmarkCircleIcon,
  CheckmarkIcon,
  ChevronDownIcon,
  ChevronLeftIcon,
  ChevronRightIcon,
  CloseIcon,
  Code,
  ComposeSparklesIcon,
  Container,
  CopyIcon,
  Dialog,
  DialogProvider,
  DocumentIcon,
  DocumentRemoveIcon,
  DocumentsIcon,
  DoubleChevronLeftIcon,
  DoubleChevronRightIcon,
  EarthAmericasIcon,
  EditIcon,
  EllipsisHorizontalIcon,
  ErrorOutlineIcon,
  Flex,
  FolderIcon,
  Grid,
  Heading,
  InfoOutlineIcon,
  Inline,
  JsonIcon,
  LaunchIcon,
  Layer,
  LayerProvider,
  LinkIcon,
  LockIcon,
  MasterDetailIcon,
  Menu,
  MenuDivider,
  MenuItem,
  Popover,
  PortalProvider,
  PublishIcon,
  ReadOnlyIcon,
  ResetIcon,
  RestoreIcon,
  RevertIcon,
  SearchIcon,
  Select,
  Skeleton,
  SortIcon,
  Spinner,
  SpinnerIcon,
  SplitVerticalIcon,
  Stack,
  StackCompactIcon,
  StackIcon,
  StarFilledIcon,
  StarIcon,
  SyncIcon,
  TabList,
  TabPanel,
  Text,
  TextInput,
  TextSkeleton,
  TransferIcon,
  TrashIcon,
  UnknownIcon,
  UnpublishIcon,
  WarningOutlineIcon,
  _raf2,
  dt,
  focusFirstDescendant,
  getTheme_v2,
  lt,
  mt,
  rem,
  require_react_is,
  rgba,
  useClickOutsideEvent,
  useEffectEvent,
  useElementRect,
  useGlobalKeyDown,
  useLayer,
  usePortal,
  useToast
} from "./chunk-OICLI4NN.js";
import {
  require_dist
} from "./chunk-RY3OW3S3.js";
import {
  require_jsx_runtime
} from "./chunk-DHAWTH44.js";
import {
  require_react
} from "./chunk-SEMNKSU3.js";
import {
  __commonJS,
  __publicField,
  __toESM
} from "./chunk-HFZ37CMS.js";

// ../node_modules/lodash/camelCase.js
var require_camelCase = __commonJS({
  "../node_modules/lodash/camelCase.js"(exports, module) {
    var capitalize = require_capitalize();
    var createCompounder = require_createCompounder();
    var camelCase2 = createCompounder(function(result, word, index) {
      word = word.toLowerCase();
      return result + (index ? capitalize(word) : word);
    });
    module.exports = camelCase2;
  }
});

// ../node_modules/lodash/kebabCase.js
var require_kebabCase = __commonJS({
  "../node_modules/lodash/kebabCase.js"(exports, module) {
    var createCompounder = require_createCompounder();
    var kebabCase2 = createCompounder(function(result, word, index) {
      return result + (index ? "-" : "") + word.toLowerCase();
    });
    module.exports = kebabCase2;
  }
});

// ../node_modules/debounce/index.js
var require_debounce = __commonJS({
  "../node_modules/debounce/index.js"(exports, module) {
    function debounce3(func, wait, immediate) {
      var timeout, args, context, timestamp, result;
      if (null == wait) wait = 100;
      function later() {
        var last = Date.now() - timestamp;
        if (last < wait && last >= 0) {
          timeout = setTimeout(later, wait - last);
        } else {
          timeout = null;
          if (!immediate) {
            result = func.apply(context, args);
            context = args = null;
          }
        }
      }
      ;
      var debounced = function() {
        context = this;
        args = arguments;
        timestamp = Date.now();
        var callNow = immediate && !timeout;
        if (!timeout) timeout = setTimeout(later, wait);
        if (callNow) {
          result = func.apply(context, args);
          context = args = null;
        }
        return result;
      };
      debounced.clear = function() {
        if (timeout) {
          clearTimeout(timeout);
          timeout = null;
        }
      };
      debounced.flush = function() {
        if (timeout) {
          result = func.apply(context, args);
          context = args = null;
          clearTimeout(timeout);
          timeout = null;
        }
      };
      return debounced;
    }
    debounce3.debounce = debounce3;
    module.exports = debounce3;
  }
});

// ../node_modules/md5-o-matic/lib/md5omatic.js
var require_md5omatic = __commonJS({
  "../node_modules/md5-o-matic/lib/md5omatic.js"(exports, module) {
    "use strict";
    module.exports = md5omatic;
    function md5omatic(str) {
      var x = str2blks_MD5(str);
      var a = 1732584193;
      var b = -271733879;
      var c2 = -1732584194;
      var d = 271733878;
      for (var i = 0; i < x.length; i += 16) {
        var olda = a;
        var oldb = b;
        var oldc = c2;
        var oldd = d;
        a = ff(a, b, c2, d, x[i + 0], 7, -680876936);
        d = ff(d, a, b, c2, x[i + 1], 12, -389564586);
        c2 = ff(c2, d, a, b, x[i + 2], 17, 606105819);
        b = ff(b, c2, d, a, x[i + 3], 22, -1044525330);
        a = ff(a, b, c2, d, x[i + 4], 7, -176418897);
        d = ff(d, a, b, c2, x[i + 5], 12, 1200080426);
        c2 = ff(c2, d, a, b, x[i + 6], 17, -1473231341);
        b = ff(b, c2, d, a, x[i + 7], 22, -45705983);
        a = ff(a, b, c2, d, x[i + 8], 7, 1770035416);
        d = ff(d, a, b, c2, x[i + 9], 12, -1958414417);
        c2 = ff(c2, d, a, b, x[i + 10], 17, -42063);
        b = ff(b, c2, d, a, x[i + 11], 22, -1990404162);
        a = ff(a, b, c2, d, x[i + 12], 7, 1804603682);
        d = ff(d, a, b, c2, x[i + 13], 12, -40341101);
        c2 = ff(c2, d, a, b, x[i + 14], 17, -1502002290);
        b = ff(b, c2, d, a, x[i + 15], 22, 1236535329);
        a = gg(a, b, c2, d, x[i + 1], 5, -165796510);
        d = gg(d, a, b, c2, x[i + 6], 9, -1069501632);
        c2 = gg(c2, d, a, b, x[i + 11], 14, 643717713);
        b = gg(b, c2, d, a, x[i + 0], 20, -373897302);
        a = gg(a, b, c2, d, x[i + 5], 5, -701558691);
        d = gg(d, a, b, c2, x[i + 10], 9, 38016083);
        c2 = gg(c2, d, a, b, x[i + 15], 14, -660478335);
        b = gg(b, c2, d, a, x[i + 4], 20, -405537848);
        a = gg(a, b, c2, d, x[i + 9], 5, 568446438);
        d = gg(d, a, b, c2, x[i + 14], 9, -1019803690);
        c2 = gg(c2, d, a, b, x[i + 3], 14, -187363961);
        b = gg(b, c2, d, a, x[i + 8], 20, 1163531501);
        a = gg(a, b, c2, d, x[i + 13], 5, -1444681467);
        d = gg(d, a, b, c2, x[i + 2], 9, -51403784);
        c2 = gg(c2, d, a, b, x[i + 7], 14, 1735328473);
        b = gg(b, c2, d, a, x[i + 12], 20, -1926607734);
        a = hh(a, b, c2, d, x[i + 5], 4, -378558);
        d = hh(d, a, b, c2, x[i + 8], 11, -2022574463);
        c2 = hh(c2, d, a, b, x[i + 11], 16, 1839030562);
        b = hh(b, c2, d, a, x[i + 14], 23, -35309556);
        a = hh(a, b, c2, d, x[i + 1], 4, -1530992060);
        d = hh(d, a, b, c2, x[i + 4], 11, 1272893353);
        c2 = hh(c2, d, a, b, x[i + 7], 16, -155497632);
        b = hh(b, c2, d, a, x[i + 10], 23, -1094730640);
        a = hh(a, b, c2, d, x[i + 13], 4, 681279174);
        d = hh(d, a, b, c2, x[i + 0], 11, -358537222);
        c2 = hh(c2, d, a, b, x[i + 3], 16, -722521979);
        b = hh(b, c2, d, a, x[i + 6], 23, 76029189);
        a = hh(a, b, c2, d, x[i + 9], 4, -640364487);
        d = hh(d, a, b, c2, x[i + 12], 11, -421815835);
        c2 = hh(c2, d, a, b, x[i + 15], 16, 530742520);
        b = hh(b, c2, d, a, x[i + 2], 23, -995338651);
        a = ii(a, b, c2, d, x[i + 0], 6, -198630844);
        d = ii(d, a, b, c2, x[i + 7], 10, 1126891415);
        c2 = ii(c2, d, a, b, x[i + 14], 15, -1416354905);
        b = ii(b, c2, d, a, x[i + 5], 21, -57434055);
        a = ii(a, b, c2, d, x[i + 12], 6, 1700485571);
        d = ii(d, a, b, c2, x[i + 3], 10, -1894986606);
        c2 = ii(c2, d, a, b, x[i + 10], 15, -1051523);
        b = ii(b, c2, d, a, x[i + 1], 21, -2054922799);
        a = ii(a, b, c2, d, x[i + 8], 6, 1873313359);
        d = ii(d, a, b, c2, x[i + 15], 10, -30611744);
        c2 = ii(c2, d, a, b, x[i + 6], 15, -1560198380);
        b = ii(b, c2, d, a, x[i + 13], 21, 1309151649);
        a = ii(a, b, c2, d, x[i + 4], 6, -145523070);
        d = ii(d, a, b, c2, x[i + 11], 10, -1120210379);
        c2 = ii(c2, d, a, b, x[i + 2], 15, 718787259);
        b = ii(b, c2, d, a, x[i + 9], 21, -343485551);
        a = addme(a, olda);
        b = addme(b, oldb);
        c2 = addme(c2, oldc);
        d = addme(d, oldd);
      }
      return rhex(a) + rhex(b) + rhex(c2) + rhex(d);
    }
    var hex_chr = "0123456789abcdef";
    function bitOR(a, b) {
      var lsb = a & 1 | b & 1;
      var msb31 = a >>> 1 | b >>> 1;
      return msb31 << 1 | lsb;
    }
    function bitXOR(a, b) {
      var lsb = a & 1 ^ b & 1;
      var msb31 = a >>> 1 ^ b >>> 1;
      return msb31 << 1 | lsb;
    }
    function bitAND(a, b) {
      var lsb = a & 1 & (b & 1);
      var msb31 = a >>> 1 & b >>> 1;
      return msb31 << 1 | lsb;
    }
    function addme(x, y) {
      var lsw = (x & 65535) + (y & 65535);
      var msw = (x >> 16) + (y >> 16) + (lsw >> 16);
      return msw << 16 | lsw & 65535;
    }
    function rhex(num) {
      var str = "";
      var j;
      for (j = 0; j <= 3; j++)
        str += hex_chr.charAt(num >> j * 8 + 4 & 15) + hex_chr.charAt(num >> j * 8 & 15);
      return str;
    }
    function str2blks_MD5(str) {
      var nblk = (str.length + 8 >> 6) + 1;
      var blks = new Array(nblk * 16);
      var i;
      for (i = 0; i < nblk * 16; i++)
        blks[i] = 0;
      for (i = 0; i < str.length; i++)
        blks[i >> 2] |= str.charCodeAt(i) << (str.length * 8 + i) % 4 * 8;
      blks[i >> 2] |= 128 << (str.length * 8 + i) % 4 * 8;
      var l2 = str.length * 8;
      blks[nblk * 16 - 2] = l2 & 255;
      blks[nblk * 16 - 2] |= (l2 >>> 8 & 255) << 8;
      blks[nblk * 16 - 2] |= (l2 >>> 16 & 255) << 16;
      blks[nblk * 16 - 2] |= (l2 >>> 24 & 255) << 24;
      return blks;
    }
    function rol(num, cnt) {
      return num << cnt | num >>> 32 - cnt;
    }
    function cmn(q, a, b, x, s2, t2) {
      return addme(rol(addme(addme(a, q), addme(x, t2)), s2), b);
    }
    function ff(a, b, c2, d, x, s2, t2) {
      return cmn(bitOR(bitAND(b, c2), bitAND(~b, d)), a, b, x, s2, t2);
    }
    function gg(a, b, c2, d, x, s2, t2) {
      return cmn(bitOR(bitAND(b, d), bitAND(c2, ~d)), a, b, x, s2, t2);
    }
    function hh(a, b, c2, d, x, s2, t2) {
      return cmn(bitXOR(bitXOR(b, c2), d), a, b, x, s2, t2);
    }
    function ii(a, b, c2, d, x, s2, t2) {
      return cmn(bitXOR(c2, bitOR(b, ~d)), a, b, x, s2, t2);
    }
  }
});

// ../node_modules/sanity/lib/_chunks-es/StructureToolProvider.mjs
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var import_react = __toESM(require_react(), 1);
var import_react_is = __toESM(require_react_is(), 1);
var import_camelCase = __toESM(require_camelCase(), 1);
var import_speakingurl = __toESM(require_speakingurl(), 1);
var import_uniqueId = __toESM(require_uniqueId(), 1);
var import_uniq = __toESM(require_uniq(), 1);
var import_kebabCase = __toESM(require_kebabCase(), 1);
var import_find = __toESM(require_find(), 1);
var import_startCase = __toESM(require_startCase(), 1);
var structureLocaleNamespace = "structure";
var structureUsEnglishLocaleBundle = defineLocaleResourceBundle({
  locale: "en-US",
  namespace: structureLocaleNamespace,
  resources: () => import("./resources6-PRNIXHWK.js")
});
var IMPLICIT_SCHEMA_TYPE_FIELDS = ["_id", "_type", "_createdAt", "_updatedAt", "_rev"];
function joinReferences(schemaType, path, strict = false) {
  const [head, ...tail] = path;
  if (!("fields" in schemaType))
    return "";
  const schemaField = schemaType.fields.find((field) => field.name === head);
  if (!schemaField) {
    if (!IMPLICIT_SCHEMA_TYPE_FIELDS.includes(head)) {
      const errorMessage = `The current ordering config targeted the nonexistent field "${head}" on schema type "${schemaType.name}". It should be one of ${schemaType.fields.map((field) => field.name).join(", ")}`;
      if (strict)
        throw new Error(errorMessage);
      console.warn(errorMessage);
    }
    return "";
  }
  if ("to" in schemaField.type && schemaField.type.name === "reference") {
    const refTypes = schemaField.type.to;
    return `${head}->{${refTypes.map((refType) => joinReferences(refType, tail)).join(",")}}`;
  }
  const tailFields = tail.length > 0 && joinReferences(schemaField.type, tail), tailWrapper = tailFields ? `{${tailFields}}` : "";
  return tail.length > 0 ? `${head}${tailWrapper}` : head;
}
function getExtendedProjection(schemaType, orderBy, strict = false) {
  return orderBy.map((ordering) => joinReferences(schemaType, ordering.field.split("."), strict)).join(", ");
}
var SerializeError = class extends Error {
  constructor(message, parentPath, pathSegment, hint) {
    super(message), this.name = "SerializeError";
    const segment = typeof pathSegment > "u" ? "<unknown>" : `${pathSegment}`;
    this.path = (parentPath || []).concat(hint ? `${segment} (${hint})` : segment);
  }
  withHelpUrl(id2) {
    return this.helpId = id2, this;
  }
};
var HELP_URL = {
  ID_REQUIRED: "structure-node-id-required",
  TITLE_REQUIRED: "structure-title-required",
  FILTER_REQUIRED: "structure-filter-required",
  INVALID_LIST_ITEM: "structure-invalid-list-item",
  COMPONENT_REQUIRED: "structure-view-component-required",
  DOCUMENT_ID_REQUIRED: "structure-document-id-required",
  DOCUMENT_TYPE_REQUIRED: "structure-document-type-required",
  SCHEMA_TYPE_REQUIRED: "structure-schema-type-required",
  SCHEMA_TYPE_NOT_FOUND: "structure-schema-type-not-found",
  LIST_ITEMS_MUST_BE_ARRAY: "structure-list-items-must-be-array",
  QUERY_PROVIDED_FOR_FILTER: "structure-query-provided-for-filter",
  ACTION_OR_INTENT_REQUIRED: "structure-action-or-intent-required",
  LIST_ITEM_IDS_MUST_BE_UNIQUE: "structure-list-item-ids-must-be-unique",
  ACTION_AND_INTENT_MUTUALLY_EXCLUSIVE: "structure-action-and-intent-mutually-exclusive",
  API_VERSION_REQUIRED_FOR_CUSTOM_FILTER: "structure-api-version-required-for-custom-filter"
};
var ORDER_BY_UPDATED_AT = {
  title: "Last edited",
  i18n: {
    title: {
      key: "menu-items.sort-by.last-edited",
      ns: structureLocaleNamespace
    }
  },
  name: "lastEditedDesc",
  by: [{
    field: "_updatedAt",
    direction: "desc"
  }]
};
var ORDER_BY_CREATED_AT = {
  title: "Created",
  i18n: {
    title: {
      key: "menu-items.sort-by.created",
      ns: structureLocaleNamespace
    }
  },
  name: "lastCreatedDesc",
  by: [{
    field: "_createdAt",
    direction: "desc"
  }]
};
var DEFAULT_SELECTED_ORDERING_OPTION = ORDER_BY_UPDATED_AT;
var DEFAULT_ORDERING_OPTIONS = [
  ORDER_BY_UPDATED_AT,
  // _updatedAt
  ORDER_BY_CREATED_AT
  // _createdAt
];
function maybeSerializeMenuItem(item, index, path) {
  return item instanceof MenuItemBuilder ? item.serialize({
    path,
    index
  }) : item;
}
var MenuItemBuilder = class _MenuItemBuilder {
  /** menu item option object. See {@link PartialMenuItem} */
  constructor(_context, spec) {
    this._context = _context, this.spec = spec || {};
  }
  /**
   * Set menu item action
   * @param action - menu item action. See {@link MenuItemActionType}
   * @returns menu item builder based on action provided. See {@link MenuItemBuilder}
   */
  action(action) {
    return this.clone({
      action
    });
  }
  /**
   * Get menu item action
   * @returns menu item builder action. See {@link PartialMenuItem}
   */
  getAction() {
    return this.spec.action;
  }
  /**
   * Set menu item intent
   * @param intent - menu item intent. See {@link Intent}
   * @returns menu item builder based on intent provided. See {@link MenuItemBuilder}
   */
  intent(intent) {
    return this.clone({
      intent
    });
  }
  /**
   * Get menu item intent
   * @returns menu item intent. See {@link PartialMenuItem}
   */
  getIntent() {
    return this.spec.intent;
  }
  /**
   * Set menu item title
   * @param title - menu item title
   * @returns menu item builder based on title provided. See {@link MenuItemBuilder}
   */
  title(title) {
    return this.clone({
      title
    });
  }
  /**
   * Get menu item title. Note that the `i18n` configuration will take
   * precedence and this title is left here for compatibility.
   * @returns menu item title
   */
  getTitle() {
    return this.spec.title;
  }
  /**
   * Set the i18n key and namespace used to populate the localized title.
   * @param i18n - object with i18n key and related namespace
   * @returns menu item builder based on i18n config provided. See {@link MenuItemBuilder}
   */
  i18n(i18n) {
    return this.clone({
      i18n
    });
  }
  /**
   * Get the i18n key and namespace used to populate the localized title.
   * @returns the i18n key and namespace used to populate the localized title.
   */
  getI18n() {
    return this.spec.i18n;
  }
  /**
   * Set menu item group
   * @param group - menu item group
   * @returns menu item builder based on group provided. See {@link MenuItemBuilder}
   */
  group(group) {
    return this.clone({
      group
    });
  }
  /**
   * Get menu item group
   * @returns menu item group. See {@link PartialMenuItem}
   */
  getGroup() {
    return this.spec.group;
  }
  /**
   * Set menu item icon
   * @param icon - menu item icon
   * @returns menu item builder based on icon provided. See {@link MenuItemBuilder}
   */
  icon(icon) {
    return this.clone({
      icon
    });
  }
  /**
   * Get menu item icon
   * @returns menu item icon. See {@link PartialMenuItem}
   */
  getIcon() {
    return this.spec.icon;
  }
  /**
   * Set menu item parameters
   * @param params - menu item parameters. See {@link MenuItemParamsType}
   * @returns menu item builder based on parameters provided. See {@link MenuItemBuilder}
   */
  params(params) {
    return this.clone({
      params
    });
  }
  /**
   * Get meny item parameters
   * @returns menu item parameters. See {@link PartialMenuItem}
   */
  getParams() {
    return this.spec.params;
  }
  /**
   * Set menu item to show as action
   * @param showAsAction - determine if menu item should show as action
   * @returns menu item builder based on if it should show as action. See {@link MenuItemBuilder}
   */
  showAsAction(showAsAction = true) {
    return this.clone({
      showAsAction: !!showAsAction
    });
  }
  /**
   * Check if menu item should show as action
   * @returns true if menu item should show as action, false if not. See {@link PartialMenuItem}
   */
  getShowAsAction() {
    return this.spec.showAsAction;
  }
  /** Serialize menu item builder
   * @param options - serialization options. See {@link SerializeOptions}
   * @returns menu item node based on path provided in options. See {@link MenuItem}
   */
  serialize(options = {
    path: []
  }) {
    const {
      title,
      action,
      intent
    } = this.spec;
    if (!title) {
      const hint = typeof action == "string" ? `action: "${action}"` : void 0;
      throw new SerializeError("`title` is required for menu item", options.path, options.index, hint).withHelpUrl(HELP_URL.TITLE_REQUIRED);
    }
    if (!action && !intent)
      throw new SerializeError(`\`action\` or \`intent\` required for menu item with title ${this.spec.title}`, options.path, options.index, `"${title}"`).withHelpUrl(HELP_URL.ACTION_OR_INTENT_REQUIRED);
    if (intent && action)
      throw new SerializeError("cannot set both `action` AND `intent`", options.path, options.index, `"${title}"`).withHelpUrl(HELP_URL.ACTION_AND_INTENT_MUTUALLY_EXCLUSIVE);
    return {
      ...this.spec,
      title
    };
  }
  /** Clone menu item builder
   * @param withSpec - menu item options. See {@link PartialMenuItem}
   * @returns menu item builder based on context and spec provided. See {@link MenuItemBuilder}
   */
  clone(withSpec) {
    const builder = new _MenuItemBuilder(this._context);
    return builder.spec = {
      ...this.spec,
      ...withSpec
    }, builder;
  }
};
function getOrderingMenuItem(context, {
  by,
  title,
  i18n
}, extendedProjection) {
  let builder = new MenuItemBuilder(context).group("sorting").title(context.i18n.t("default-menu-item.fallback-title", {
    // note this lives in the `studio` bundle because that one is loaded by default
    ns: "studio",
    replace: {
      title
    }
    // replaces the `{{title}}` option
  })).icon(SortIcon).action("setSortOrder").params({
    by,
    extendedProjection
  });
  return i18n && (builder = builder.i18n(i18n)), builder;
}
function getOrderingMenuItemsForSchemaType(context, typeName) {
  const {
    schema
  } = context, type2 = typeof typeName == "string" ? schema.get(typeName) : typeName;
  return !type2 || !("orderings" in type2) ? [] : (type2.orderings ? type2.orderings.concat(DEFAULT_ORDERING_OPTIONS) : DEFAULT_ORDERING_OPTIONS).map((ordering) => getOrderingMenuItem(context, ordering, getExtendedProjection(type2, ordering.by)));
}
function maybeSerializeMenuItemGroup(item, index, path) {
  return item instanceof MenuItemGroupBuilder ? item.serialize({
    path,
    index
  }) : item;
}
var MenuItemGroupBuilder = class _MenuItemGroupBuilder {
  /** Menu item group ID */
  /** Menu item group title */
  constructor(_context, spec) {
    this._context = _context, this._id = spec ? spec.id : "", this._title = spec ? spec.title : "", this._i18n = spec ? spec.i18n : void 0;
  }
  /**
   * Set menu item group ID
   * @param id - menu item group ID
   * @returns menu item group builder based on ID provided. See {@link MenuItemGroupBuilder}
   */
  id(id2) {
    return new _MenuItemGroupBuilder(this._context, {
      id: id2,
      title: this._title,
      i18n: this._i18n
    });
  }
  /**
   * Get menu item group ID
   * @returns menu item group ID
   */
  getId() {
    return this._id;
  }
  /**
   * Set menu item group title
   * @param title - menu item group title
   * @returns menu item group builder based on title provided. See {@link MenuItemGroupBuilder}
   */
  title(title) {
    return new _MenuItemGroupBuilder(this._context, {
      title,
      id: this._id,
      i18n: this._i18n
    });
  }
  /**
   * Get menu item group title
   * @returns menu item group title
   */
  getTitle() {
    return this._title;
  }
  /**
   * Set the i18n key and namespace used to populate the localized title.
   * @param i18n - object with i18n key and related namespace
   * @returns menu item group builder based on i18n info provided. See {@link MenuItemGroupBuilder}
   */
  i18n(i18n) {
    return new _MenuItemGroupBuilder(this._context, {
      i18n,
      id: this._id,
      title: this._title
    });
  }
  /**
   * Get the i18n key and namespace used to populate the localized title.
   * @returns the i18n key and namespace used to populate the localized title.
   */
  getI18n() {
    return this._i18n;
  }
  /**
   * Serialize menu item group builder
   * @param options - serialization options (path). See {@link SerializeOptions}
   * @returns menu item group based on path provided in options. See {@link MenuItemGroup}
   */
  serialize(options = {
    path: []
  }) {
    const {
      _id,
      _title,
      _i18n
    } = this;
    if (!_id)
      throw new SerializeError("`id` is required for a menu item group", options.path, options.index, _title).withHelpUrl(HELP_URL.ID_REQUIRED);
    if (!_title)
      throw new SerializeError("`title` is required for a menu item group", options.path, _id).withHelpUrl(HELP_URL.TITLE_REQUIRED);
    return {
      id: _id,
      title: _title,
      i18n: _i18n
    };
  }
};
var disallowedPattern = /([^A-Za-z0-9-_.])/;
function validateId(id2, parentPath, pathSegment) {
  if (typeof id2 != "string")
    throw new SerializeError(`Structure node id must be of type string, got ${typeof id2}`, parentPath, pathSegment);
  const [disallowedChar] = id2.match(disallowedPattern) || [];
  if (disallowedChar)
    throw new SerializeError(`Structure node id cannot contain character "${disallowedChar}"`, parentPath, pathSegment);
  if (id2.startsWith("__edit__"))
    throw new SerializeError("Structure node id cannot start with __edit__", parentPath, pathSegment);
  return id2;
}
function getStructureNodeId(title, id2) {
  if (id2)
    return id2;
  const camelCased = (0, import_camelCase.default)(title);
  return disallowedPattern.test(camelCased) ? (0, import_camelCase.default)((0, import_speakingurl.default)(title)) : camelCased;
}
var ComponentBuilder = class _ComponentBuilder {
  /** component builder option object */
  constructor(spec) {
    this.spec = {
      options: {},
      ...spec || {}
    };
  }
  /** Set Component ID
   * @param id - component ID
   * @returns component builder based on ID provided
   */
  id(id2) {
    return this.clone({
      id: id2
    });
  }
  /** Get ID
   * @returns ID
   */
  getId() {
    return this.spec.id;
  }
  /** Set Component title
   * @param title - component title
   * @returns component builder based on title provided (and ID)
   */
  title(title) {
    return this.clone({
      title,
      id: getStructureNodeId(title, this.spec.id)
    });
  }
  /** Get Component title
   * @returns title
   */
  getTitle() {
    return this.spec.title;
  }
  /** Set the i18n key and namespace used to populate the localized title.
   * @param i18n - the key and namespaced used to populate the localized title.
   * @returns component builder based on i18n key and ns provided
   */
  i18n(i18n) {
    return this.clone({
      i18n
    });
  }
  /** Get i18n key and namespace used to populate the localized title
   * @returns the i18n key and namespace used to populate the localized title
   */
  getI18n() {
    return this.spec.i18n;
  }
  /** Set Component child
   * @param child - child component
   * @returns component builder based on child component provided
   */
  child(child) {
    return this.clone({
      child
    });
  }
  /** Get Component child
   * @returns child component
   */
  getChild() {
    return this.spec.child;
  }
  /** Set component
   * @param component - user built component
   * @returns component builder based on component provided
   */
  component(component2) {
    return this.clone({
      component: component2
    });
  }
  /** Get Component
   * @returns component
   */
  getComponent() {
    return this.spec.component;
  }
  /** Set Component options
   * @param options - component options
   * @returns component builder based on options provided
   */
  options(options) {
    return this.clone({
      options
    });
  }
  /** Get Component options
   * @returns component options
   */
  getOptions() {
    return this.spec.options || {};
  }
  /** Set Component menu items
   * @param menuItems - component menu items
   * @returns component builder based on menuItems provided
   */
  menuItems(menuItems) {
    return this.clone({
      menuItems
    });
  }
  /** Get Component menu items
   * @returns menu items
   */
  getMenuItems() {
    return this.spec.menuItems;
  }
  /** Set Component menu item groups
   * @param menuItemGroups - component menu item groups
   * @returns component builder based on menuItemGroups provided
   */
  menuItemGroups(menuItemGroups) {
    return this.clone({
      menuItemGroups
    });
  }
  /** Get Component menu item groups
   * @returns menu item groups
   */
  getMenuItemGroups() {
    return this.spec.menuItemGroups;
  }
  canHandleIntent(canHandleIntent) {
    return this.clone({
      canHandleIntent
    });
  }
  /** Serialize component
   * @param options - serialization options
   * @returns component object based on path provided in options
   *
   */
  serialize(options = {
    path: []
  }) {
    const {
      id: id2,
      title,
      child,
      options: componentOptions,
      component: component2
    } = this.spec;
    if (!id2)
      throw new SerializeError("`id` is required for `component` structure item", options.path, options.index).withHelpUrl(HELP_URL.ID_REQUIRED);
    if (!component2)
      throw new SerializeError("`component` is required for `component` structure item", options.path, options.index).withHelpUrl(HELP_URL.ID_REQUIRED);
    return {
      id: validateId(id2, options.path, options.index),
      title,
      type: "component",
      child,
      component: component2,
      canHandleIntent: this.spec.canHandleIntent,
      options: componentOptions || {},
      menuItems: (this.spec.menuItems || []).map((item, i) => maybeSerializeMenuItem(item, i, options.path)),
      menuItemGroups: (this.spec.menuItemGroups || []).map((item, i) => maybeSerializeMenuItemGroup(item, i, options.path))
    };
  }
  /** Clone component builder (allows for options overriding)
   * @param withSpec - component builder options
   * @returns cloned builder
   */
  clone(withSpec) {
    const builder = new _ComponentBuilder();
    return builder.spec = {
      ...this.spec,
      ...withSpec
    }, builder;
  }
};
var DividerBuilder = class _DividerBuilder {
  constructor(spec) {
    this.spec = {
      id: (0, import_uniqueId.default)("__divider__"),
      type: "divider",
      ...spec
    };
  }
  /** Set the title of the divider
   * @param title - the title of the divider
   * @returns divider builder based on title provided
   */
  title(title) {
    return this.clone({
      title
    });
  }
  /** Get the title of the divider
   * @returns the title of the divider
   */
  getTitle() {
    return this.spec.title;
  }
  /** Set the i18n key and namespace used to populate the localized title.
   * @param i18n - the key and namespaced used to populate the localized title.
   * @returns divider builder based on i18n key and ns provided
   */
  i18n(i18n) {
    return this.clone({
      i18n
    });
  }
  /** Get i18n key and namespace used to populate the localized title
   * @returns the i18n key and namespace used to populate the localized title
   */
  getI18n() {
    return this.spec.i18n;
  }
  /** Serialize the divider
   * @returns the serialized divider
   */
  serialize() {
    return {
      ...this.spec
    };
  }
  /** Clone divider builder (allows for options overriding)
   * @param withSpec - divider builder options
   * @returns cloned builder
   */
  clone(withSpec) {
    const builder = new _DividerBuilder();
    return builder.spec = {
      ...this.spec,
      ...withSpec
    }, builder;
  }
};
async function resolveTypeForDocument(getClient, id2) {
  return await getClient(DEFAULT_STUDIO_CLIENT_OPTIONS).fetch("*[sanity::versionOf($publishedId)][0]._type", {
    publishedId: getPublishedId(id2)
  }, {
    tag: "structure.resolve-type"
  });
}
var GenericViewBuilder = class {
  constructor() {
    /** Generic view option object */
    __publicField(this, "spec", {});
  }
  /** Set generic view ID
   * @param id - generic view ID
   * @returns generic view builder based on ID provided.
   */
  id(id2) {
    return this.clone({
      id: id2
    });
  }
  /** Get generic view ID
   * @returns generic view ID
   */
  getId() {
    return this.spec.id;
  }
  /** Set generic view title
   * @param title - generic view title
   * @returns generic view builder based on title provided and (if provided) its ID.
   */
  title(title) {
    return this.clone({
      title,
      id: this.spec.id || (0, import_kebabCase.default)(title)
    });
  }
  /** Get generic view title
   * @returns generic view title
   */
  getTitle() {
    return this.spec.title;
  }
  /** Set generic view icon
   * @param icon - generic view icon
   * @returns generic view builder based on icon provided.
   */
  icon(icon) {
    return this.clone({
      icon
    });
  }
  /** Get generic view icon
   * @returns generic view icon
   */
  getIcon() {
    return this.spec.icon;
  }
  /** Serialize generic view
   * @param options - serialization options. See {@link SerializeOptions}
   * @returns generic view object based on path provided in options. See {@link BaseView}
   */
  serialize(options = {
    path: []
  }) {
    const {
      id: id2,
      title,
      icon
    } = this.spec;
    if (!id2)
      throw new SerializeError("`id` is required for view item", options.path, options.index).withHelpUrl(HELP_URL.ID_REQUIRED);
    if (!title)
      throw new SerializeError("`title` is required for view item", options.path, options.index).withHelpUrl(HELP_URL.TITLE_REQUIRED);
    return {
      id: validateId(id2, options.path, options.index),
      title,
      icon
    };
  }
  /** Clone generic view builder (allows for options overriding)
   * @param withSpec - Partial generic view builder options. See {@link BaseView}
   * @returns Generic view builder.
   */
};
function isSerializable(view) {
  return typeof view.serialize == "function";
}
function maybeSerializeView(item, index, path) {
  return isSerializable(item) ? item.serialize({
    path,
    index
  }) : item;
}
var isComponentSpec = (spec) => isRecord$4(spec) && spec.type === "component";
var ComponentViewBuilder = class _ComponentViewBuilder extends GenericViewBuilder {
  /** Partial Component view option object. See {@link ComponentView} */
  constructor(componentOrSpec) {
    const spec = isComponentSpec(componentOrSpec) ? {
      ...componentOrSpec
    } : {
      options: {}
    };
    super(), this.spec = spec;
    const userComponent = typeof componentOrSpec == "function" ? componentOrSpec : this.spec.component;
    userComponent && (this.spec = this.component(userComponent).spec);
  }
  /** Set view Component
   * @param component - component view component. See {@link UserViewComponent}
   * @returns component view builder based on component view provided. See {@link ComponentViewBuilder}
   */
  component(component2) {
    return this.clone({
      component: component2
    });
  }
  /** Get view Component
   * @returns Partial component view. See {@link ComponentView}
   */
  getComponent() {
    return this.spec.component;
  }
  /** Set view Component options
   * @param options - component view options
   * @returns component view builder based on options provided. See {@link ComponentViewBuilder}
   */
  options(options) {
    return this.clone({
      options
    });
  }
  /** Get view Component options
   * @returns component view options. See {@link ComponentView}
   */
  getOptions() {
    return this.spec.options || {};
  }
  /** Serialize view Component
   * @param options - serialization options. See {@link SerializeOptions}
   * @returns component view based on path provided in options. See {@link ComponentView}
   *
   */
  serialize(options = {
    path: []
  }) {
    const base = super.serialize(options), component2 = this.spec.component;
    if (typeof component2 != "function")
      throw new SerializeError("`component` is required and must be a function for `component()` view item", options.path, options.index).withHelpUrl(HELP_URL.COMPONENT_REQUIRED);
    return {
      ...base,
      component: component2,
      options: this.spec.options || {},
      type: "component"
    };
  }
  /** Clone Component view builder (allows for options overriding)
   * @param withSpec - partial for component view option. See {@link ComponentView}
   * @returns component view builder. See {@link ComponentViewBuilder}
   */
  clone(withSpec) {
    const builder = new _ComponentViewBuilder();
    return builder.spec = {
      ...this.spec,
      ...withSpec
    }, builder;
  }
};
var FormViewBuilder = class _FormViewBuilder extends GenericViewBuilder {
  /** Document list options. See {@link FormView} */
  constructor(spec) {
    super(), this.spec = {
      id: "editor",
      title: "Editor",
      ...spec || {}
    };
  }
  /**
   * Serialize Form view builder
   * @param options - Serialize options. See {@link SerializeOptions}
   * @returns form view builder based on path provided in options. See {@link FormView}
   */
  serialize(options = {
    path: []
  }) {
    return {
      ...super.serialize(options),
      type: "form"
    };
  }
  /**
   * Clone Form view builder (allows for options overriding)
   * @param withSpec - Partial form view builder options. See {@link FormView}
   * @returns form view builder. See {@link FormViewBuilder}
   */
  clone(withSpec) {
    const builder = new _FormViewBuilder();
    return builder.spec = {
      ...this.spec,
      ...withSpec
    }, builder;
  }
};
var form = (spec) => new FormViewBuilder(spec);
var component = (componentOrSpec) => new ComponentViewBuilder(componentOrSpec);
var views = Object.freeze({
  __proto__: null,
  ComponentViewBuilder,
  FormViewBuilder,
  GenericViewBuilder,
  component,
  form,
  maybeSerializeView
});
var createDocumentChildResolver = ({
  resolveDocumentNode,
  getClient
}) => async (itemId, {
  params,
  path
}) => {
  let type2 = params.type;
  const parentPath = path.slice(0, path.length - 1), currentSegment = path[path.length - 1];
  if (type2 || (type2 = await resolveTypeForDocument(getClient, itemId)), !type2)
    throw new SerializeError("Failed to resolve document, and no type provided in parameters.", parentPath, currentSegment);
  return resolveDocumentNode({
    documentId: itemId,
    schemaType: type2
  });
};
var DocumentBuilder = class _DocumentBuilder {
  /** Component builder option object See {@link PartialDocumentNode} */
  constructor(_context, spec) {
    this._context = _context, this.spec = spec || {};
  }
  /** Set Document Builder ID
   * @param id - document builder ID
   * @returns document builder based on ID provided. See {@link DocumentBuilder}
   */
  id(id2) {
    return this.clone({
      id: id2
    });
  }
  /** Get Document Builder ID
   * @returns document ID. See {@link PartialDocumentNode}
   */
  getId() {
    return this.spec.id;
  }
  /** Set Document title
   * @param title - document title
   * @returns document builder based on title provided (and ID). See {@link DocumentBuilder}
   */
  title(title) {
    return this.clone({
      title,
      id: getStructureNodeId(title, this.spec.id)
    });
  }
  /** Get Document title
   * @returns document title. See {@link PartialDocumentNode}
   */
  getTitle() {
    return this.spec.title;
  }
  /** Set the i18n key and namespace used to populate the localized title.
   * @param i18n - the key and namespaced used to populate the localized title.
   * @returns component builder based on i18n key and ns provided
   */
  i18n(i18n) {
    return this.clone({
      i18n
    });
  }
  /** Get i18n key and namespace used to populate the localized title
   * @returns the i18n key and namespace used to populate the localized title
   */
  getI18n() {
    return this.spec.i18n;
  }
  /** Set Document child
   * @param child - document child
   * @returns document builder based on child provided. See {@link DocumentBuilder}
   */
  child(child) {
    return this.clone({
      child
    });
  }
  /** Get Document child
   * @returns document child. See {@link PartialDocumentNode}
   */
  getChild() {
    return this.spec.child;
  }
  /** Set Document ID
   * @param documentId - document ID
   * @returns document builder with document based on ID provided. See {@link DocumentBuilder}
   */
  documentId(documentId) {
    const paneId = this.spec.id || documentId;
    return this.clone({
      id: paneId,
      options: {
        ...this.spec.options,
        id: documentId
      }
    });
  }
  /** Get Document ID
   * @returns document ID. See {@link DocumentOptions}
   */
  getDocumentId() {
    var _a;
    return (_a = this.spec.options) == null ? void 0 : _a.id;
  }
  /** Set Document Type
   * @param documentType - document type
   * @returns document builder with document based on type provided. See {@link DocumentBuilder}
   */
  schemaType(documentType) {
    return this.clone({
      options: {
        ...this.spec.options,
        type: typeof documentType == "string" ? documentType : documentType.name
      }
    });
  }
  /** Get Document Type
   * @returns document type. See {@link DocumentOptions}
   */
  getSchemaType() {
    var _a;
    return (_a = this.spec.options) == null ? void 0 : _a.type;
  }
  /** Set Document Template
   * @param templateId - document template ID
   * @param parameters - document template parameters
   * @returns document builder with document based on template provided. See {@link DocumentBuilder}
   */
  initialValueTemplate(templateId, parameters) {
    return this.clone({
      options: {
        ...this.spec.options,
        template: templateId,
        templateParameters: parameters
      }
    });
  }
  /** Get Document Template
   * @returns document template. See {@link DocumentOptions}
   */
  getInitialValueTemplate() {
    var _a;
    return (_a = this.spec.options) == null ? void 0 : _a.template;
  }
  /** Get Document's initial value Template parameters
   * @returns document template parameters. See {@link DocumentOptions}
   */
  getInitialValueTemplateParameters() {
    var _a;
    return (_a = this.spec.options) == null ? void 0 : _a.templateParameters;
  }
  /** Set Document views
   * @param views - document views. See {@link ViewBuilder} and {@link View}
   * @returns document builder with document based on views provided. See {@link DocumentBuilder}
   */
  views(views2) {
    return this.clone({
      views: views2
    });
  }
  /** Get Document views
   * @returns document views. See {@link ViewBuilder} and {@link View}
   */
  getViews() {
    return this.spec.views || [];
  }
  /** Serialize Document builder
   * @param options - serialization options. See {@link SerializeOptions}
   * @returns document node based on path, index and hint provided in options. See {@link DocumentNode}
   */
  serialize({
    path = [],
    index,
    hint
  } = {
    path: []
  }) {
    const urlId = path[index || path.length - 1], id2 = this.spec.id || urlId && `${urlId}` || "", options = {
      id: id2,
      type: void 0,
      template: void 0,
      templateParameters: void 0,
      ...this.spec.options
    };
    if (typeof id2 != "string" || !id2)
      throw new SerializeError("`id` is required for document nodes", path, index, hint).withHelpUrl(HELP_URL.ID_REQUIRED);
    if (!options || !options.id)
      throw new SerializeError("document id (`id`) is required for document nodes", path, id2, hint).withHelpUrl(HELP_URL.DOCUMENT_ID_REQUIRED);
    if (!options || !options.type)
      throw new SerializeError("document type (`schemaType`) is required for document nodes", path, id2, hint);
    const views2 = (this.spec.views && this.spec.views.length > 0 ? this.spec.views : [form()]).map((item, i) => maybeSerializeView(item, i, path)), viewIds = views2.map((view) => view.id), dupes = (0, import_uniq.default)(viewIds.filter((viewId, i) => viewIds.includes(viewId, i + 1)));
    if (dupes.length > 0)
      throw new SerializeError(`document node has views with duplicate IDs: ${dupes.join(",  ")}`, path, id2, hint);
    return {
      ...this.spec,
      child: this.spec.child || createDocumentChildResolver(this._context),
      id: validateId(id2, path, index),
      type: "document",
      options: getDocumentOptions(options),
      views: views2
    };
  }
  /** Clone Document builder
   * @param withSpec - partial document node specification used to extend the cloned builder. See {@link PartialDocumentNode}
   * @returns document builder based on context and spec provided. See {@link DocumentBuilder}
   */
  clone(withSpec = {}) {
    const builder = new _DocumentBuilder(this._context), options = {
      ...this.spec.options,
      ...withSpec.options
    };
    return builder.spec = {
      ...this.spec,
      ...withSpec,
      options
    }, builder;
  }
};
function getDocumentOptions(spec) {
  const opts = {
    id: spec.id || "",
    type: spec.type || "*"
  };
  return spec.template && (opts.template = spec.template), spec.templateParameters && (opts.templateParameters = spec.templateParameters), opts;
}
function documentFromEditor(context, spec) {
  let doc = (spec == null ? void 0 : spec.type) ? (
    // Use user-defined document fragment as base if possible
    context.resolveDocumentNode({
      schemaType: spec.type
    })
  ) : (
    // Fall back to plain old document builder
    new DocumentBuilder(context)
  );
  if (!spec) return doc;
  const {
    id: id2,
    type: type2,
    template,
    templateParameters
  } = spec.options;
  return doc = doc.id(spec.id).documentId(id2), type2 && (doc = doc.schemaType(type2)), template && (doc = doc.initialValueTemplate(template, templateParameters)), spec.child && (doc = doc.child(spec.child)), doc;
}
function documentFromEditorWithInitialValue({
  resolveDocumentNode,
  templates
}, templateId, parameters) {
  const template = templates.find((t2) => t2.id === templateId);
  if (!template)
    throw new Error(`Template with ID "${templateId}" not defined`);
  return resolveDocumentNode({
    schemaType: template.schemaType
  }).initialValueTemplate(templateId, parameters);
}
var InitialValueTemplateItemBuilder = class _InitialValueTemplateItemBuilder {
  /** Initial Value template item option object. See {@link InitialValueTemplateItem} */
  constructor(_context, spec) {
    this._context = _context, this.spec = spec || {};
  }
  /** Set initial value template item builder ID
   * @param id - initial value template item ID
   * @returns initial value template item based on ID provided. See {@link InitialValueTemplateItemBuilder}
   */
  id(id2) {
    return this.clone({
      id: id2
    });
  }
  /** Get initial value template item builder ID
   * @returns initial value template item ID. See {@link InitialValueTemplateItem}
   */
  getId() {
    return this.spec.id;
  }
  /** Set initial value template item title
   * @param title - initial value template item title
   * @returns initial value template item based on title provided. See {@link InitialValueTemplateItemBuilder}
   */
  title(title) {
    return this.clone({
      title
    });
  }
  /** Get initial value template item title
   * @returns initial value template item title. See {@link InitialValueTemplateItem}
   */
  getTitle() {
    return this.spec.title;
  }
  /** Set initial value template item description
   * @param description - initial value template item description
   * @returns initial value template item builder based on description provided. See {@link InitialValueTemplateItemBuilder}
   */
  description(description) {
    return this.clone({
      description
    });
  }
  /** Get initial value template item description
   * @returns initial value template item description. See {@link InitialValueTemplateItem}
   */
  getDescription() {
    return this.spec.description;
  }
  /** Set initial value template ID
   * @param templateId - initial value template item template ID
   * @returns initial value template item based builder on template ID provided. See {@link InitialValueTemplateItemBuilder}
   */
  templateId(templateId) {
    const paneId = this.spec.id || templateId;
    return this.clone({
      id: paneId,
      templateId
    });
  }
  /** Get initial value template item template ID
   * @returns initial value template item ID. See {@link InitialValueTemplateItem}
   */
  getTemplateId() {
    return this.spec.templateId;
  }
  /** Get initial value template item template parameters
   * @param parameters - initial value template item parameters
   * @returns initial value template item builder based on parameters provided. See {@link InitialValueTemplateItemBuilder}
   */
  parameters(parameters) {
    return this.clone({
      parameters
    });
  }
  /** Get initial value template item template parameters
   * @returns initial value template item parameters. See {@link InitialValueTemplateItem}
   */
  getParameters() {
    return this.spec.parameters;
  }
  /** Serialize initial value template item
   * @param options - serialization options. See {@link SerializeOptions}
   * @returns initial value template item object based on the path, index and hint provided in options. See {@link InitialValueTemplateItem}
   */
  serialize({
    path = [],
    index,
    hint
  } = {
    path: []
  }) {
    const {
      spec,
      _context
    } = this, {
      templates
    } = _context;
    if (typeof spec.id != "string" || !spec.id)
      throw new SerializeError("`id` is required for initial value template item nodes", path, index, hint).withHelpUrl(HELP_URL.ID_REQUIRED);
    if (!spec.templateId)
      throw new SerializeError("template id (`templateId`) is required for initial value template item nodes", path, spec.id, hint).withHelpUrl(HELP_URL.ID_REQUIRED);
    const template = templates.find((t2) => t2.id === spec.templateId);
    if (!template)
      throw new SerializeError("template id (`templateId`) is required for initial value template item nodes", path, spec.id, hint).withHelpUrl(HELP_URL.ID_REQUIRED);
    return {
      id: spec.id,
      templateId: spec.id,
      schemaType: template.schemaType,
      type: "initialValueTemplateItem",
      description: spec.description || template.description,
      title: spec.title || template.title,
      subtitle: spec.subtitle,
      icon: spec.icon || template.icon,
      initialDocumentId: spec.initialDocumentId,
      parameters: spec.parameters
    };
  }
  /** Clone generic view builder (allows for options overriding)
   * @param withSpec - initial value template item builder options. See {@link InitialValueTemplateItemBuilder}
   * @returns initial value template item builder based on the context and options provided. See {@link InitialValueTemplateItemBuilder}
   */
  clone(withSpec = {}) {
    const builder = new _InitialValueTemplateItemBuilder(this._context);
    return builder.spec = {
      ...this.spec,
      ...withSpec
    }, builder;
  }
};
function defaultInitialValueTemplateItems(context) {
  const {
    schema,
    getStructureBuilder,
    templates
  } = context, typeNames = schema.getTypeNames();
  return templates.filter((tpl) => {
    var _a;
    return !((_a = tpl.parameters) == null ? void 0 : _a.length);
  }).sort((a, b) => typeNames.indexOf(a.schemaType) - typeNames.indexOf(b.schemaType)).map((tpl) => getStructureBuilder().initialValueTemplateItem(tpl.id));
}
function maybeSerializeInitialValueTemplateItem(item, index, path) {
  return item instanceof InitialValueTemplateItemBuilder ? item.serialize({
    path,
    index
  }) : item;
}
function menuItemsFromInitialValueTemplateItems(context, templateItems) {
  const {
    schema,
    templates
  } = context;
  return templateItems.map((item) => {
    const template = templates.find((t2) => t2.id === item.templateId), title = item.title || (template == null ? void 0 : template.title) || "Create", params = {};
    template && template.schemaType && (params.type = template.schemaType), item.templateId && (params.template = item.templateId);
    const intentParams = item.parameters ? [params, item.parameters] : params, schemaType = template && schema.get(template.schemaType), i18n = item.i18n || (template == null ? void 0 : template.i18n);
    let builder = new MenuItemBuilder(context).title(title).icon(template && template.icon || (schemaType == null ? void 0 : schemaType.icon) || AddIcon).intent({
      type: "create",
      params: intentParams
    });
    return i18n && (builder = builder.i18n(i18n)), builder.serialize();
  });
}
var DEFAULT_INTENT_HANDLER = Symbol("Document type list canHandleIntent");
var defaultIntentChecker = (intentName, params, {
  pane: pane2
}) => {
  var _a, _b;
  const isEdit = intentName === "edit", isCreate = intentName === "create", typedSpec = pane2, paneFilter = ((_a = typedSpec.options) == null ? void 0 : _a.filter) || "", paneParams = ((_b = typedSpec.options) == null ? void 0 : _b.params) || {}, typeNames = typedSpec.schemaTypeName ? [typedSpec.schemaTypeName] : getTypeNamesFromFilter(paneFilter, paneParams), initialValueTemplates = typedSpec.initialValueTemplates || [];
  return isCreate && params.template ? initialValueTemplates.some((tpl) => tpl.templateId === params.template) : isEdit && params.id && typeNames.includes(params.type) || isCreate && typeNames.includes(params.type);
};
defaultIntentChecker.identity = DEFAULT_INTENT_HANDLER;
var layoutOptions = ["default", "card", "media", "detail", "block"];
function noChildResolver() {
}
var shallowIntentChecker = (intentName, params, {
  pane: pane2,
  index
}) => index <= 1 && defaultIntentChecker(intentName, params, {
  pane: pane2
});
var GenericListBuilder = class {
  constructor() {
    /** Check if initial value templates are set */
    __publicField(this, "initialValueTemplatesSpecified", false);
    /** Generic list option object */
    __publicField(this, "spec", {});
  }
  /** Set generic list ID
   * @param id - generic list ID
   * @returns generic list builder based on ID provided.
   */
  id(id2) {
    return this.clone({
      id: id2
    });
  }
  /** Get generic list ID
   * @returns generic list ID
   */
  getId() {
    return this.spec.id;
  }
  /** Set generic list title
   * @param title - generic list title
   * @returns generic list builder based on title and ID provided.
   */
  title(title) {
    return this.clone({
      title,
      id: getStructureNodeId(title, this.spec.id)
    });
  }
  /** Get generic list title
   * @returns generic list title
   */
  getTitle() {
    return this.spec.title;
  }
  /** Set the i18n key and namespace used to populate the localized title.
   * @param i18n - the key and namespaced used to populate the localized title.
   * @returns component builder based on i18n key and ns provided
   */
  i18n(i18n) {
    return this.clone({
      i18n
    });
  }
  /** Get i18n key and namespace used to populate the localized title
   * @returns the i18n key and namespace used to populate the localized title
   */
  getI18n() {
    return this.spec.i18n;
  }
  /** Set generic list layout
   * @param defaultLayout - generic list layout key.
   * @returns generic list builder based on layout provided.
   */
  defaultLayout(defaultLayout) {
    return this.clone({
      defaultLayout
    });
  }
  /** Get generic list layout
   * @returns generic list layout
   */
  getDefaultLayout() {
    return this.spec.defaultLayout;
  }
  /** Set generic list menu items
   * @param menuItems - generic list menu items. See {@link MenuItem} and {@link MenuItemBuilder}
   * @returns generic list builder based on menu items provided.
   */
  menuItems(menuItems) {
    return this.clone({
      menuItems
    });
  }
  /** Get generic list menu items
   * @returns generic list menu items
   */
  getMenuItems() {
    return this.spec.menuItems;
  }
  /** Set generic list menu item groups
   * @param menuItemGroups - generic list menu item groups. See {@link MenuItemGroup} and {@link MenuItemGroupBuilder}
   * @returns generic list builder based on menu item groups provided.
   */
  menuItemGroups(menuItemGroups) {
    return this.clone({
      menuItemGroups
    });
  }
  /** Get generic list menu item groups
   * @returns generic list menu item groups
   */
  getMenuItemGroups() {
    return this.spec.menuItemGroups;
  }
  /** Set generic list child
   * @param child - generic list child. See {@link Child}
   * @returns generic list builder based on child provided (clone).
   */
  child(child) {
    return this.clone({
      child
    });
  }
  /** Get generic list child
   * @returns generic list child
   */
  getChild() {
    return this.spec.child;
  }
  /** Set generic list can handle intent
   * @param canHandleIntent - generic list intent checker. See {@link IntentChecker}
   * @returns generic list builder based on can handle intent provided.
   */
  canHandleIntent(canHandleIntent) {
    return this.clone({
      canHandleIntent
    });
  }
  /** Get generic list can handle intent
   * @returns generic list can handle intent
   */
  getCanHandleIntent() {
    return this.spec.canHandleIntent;
  }
  /** Set generic list display options
   * @param enabled - allow / disallow for showing icons
   * @returns generic list builder based on display options (showIcons) provided.
   */
  showIcons(enabled = true) {
    return this.clone({
      displayOptions: {
        ...this.spec.displayOptions,
        showIcons: enabled
      }
    });
  }
  /** Get generic list display options
   * @returns generic list display options (specifically showIcons)
   */
  getShowIcons() {
    return this.spec.displayOptions ? this.spec.displayOptions.showIcons : void 0;
  }
  /** Set generic list initial value templates
   * @param templates - generic list initial value templates. See {@link InitialValueTemplateItemBuilder}
   * @returns generic list builder based on templates provided.
   */
  initialValueTemplates(templates) {
    return this.initialValueTemplatesSpecified = true, this.clone({
      initialValueTemplates: Array.isArray(templates) ? templates : [templates]
    });
  }
  /** Get generic list initial value templates
   * @returns generic list initial value templates
   */
  getInitialValueTemplates() {
    return this.spec.initialValueTemplates;
  }
  /** Serialize generic list
   * @param options - serialization options. See {@link SerializeOptions}
   * @returns generic list object based on path provided in options. See {@link GenericList}
   */
  serialize(options = {
    path: []
  }) {
    const id2 = this.spec.id || "", path = options.path, defaultLayout = this.spec.defaultLayout;
    if (defaultLayout && !layoutOptions.includes(defaultLayout))
      throw new SerializeError(`\`layout\` must be one of ${layoutOptions.map((item) => `"${item}"`).join(", ")}`, path, id2 || options.index, this.spec.title);
    const initialValueTemplates = (this.spec.initialValueTemplates || []).map((item, i) => maybeSerializeInitialValueTemplateItem(item, i, path));
    return {
      id: validateId(id2, options.path, id2 || options.index),
      title: this.spec.title,
      i18n: this.spec.i18n,
      type: "genericList",
      defaultLayout,
      child: this.spec.child || noChildResolver,
      canHandleIntent: this.spec.canHandleIntent || shallowIntentChecker,
      displayOptions: this.spec.displayOptions,
      initialValueTemplates,
      menuItems: (this.spec.menuItems || []).map((item, i) => maybeSerializeMenuItem(item, i, path)),
      menuItemGroups: (this.spec.menuItemGroups || []).map((item, i) => maybeSerializeMenuItemGroup(item, i, path))
    };
  }
  /** Clone generic list builder (allows for options overriding)
   * @param _withSpec - generic list options.
   * @returns generic list builder.
   */
};
var validateFilter = (spec, options) => {
  var _a;
  const filter2 = ((_a = spec.options) == null ? void 0 : _a.filter.trim()) || "";
  if (["*", "{"].includes(filter2[0]))
    throw new SerializeError(`\`filter\` cannot start with \`${filter2[0]}\` - looks like you are providing a query, not a filter`, options.path, spec.id, spec.title).withHelpUrl(HELP_URL.QUERY_PROVIDED_FOR_FILTER);
  return filter2;
};
var createDocumentChildResolverForItem = (context) => (itemId, options) => {
  var _a;
  const parentItem = options.parent, template = ((_a = options.params) == null ? void 0 : _a.template) ? context.templates.find((tpl) => tpl.id === options.params.template) : void 0, type2 = template ? template.schemaType : parentItem.schemaTypeName || resolveTypeForDocument(context.getClient, itemId);
  return Promise.resolve(type2).then((schemaType) => schemaType ? context.resolveDocumentNode({
    schemaType,
    documentId: itemId
  }) : new DocumentBuilder(context).id("editor").documentId(itemId).schemaType(""));
};
var DocumentListBuilder = class _DocumentListBuilder extends GenericListBuilder {
  /** Document list options. See {@link PartialDocumentList} */
  constructor(_context, spec) {
    super(), this._context = _context, this.spec = spec || {}, this.initialValueTemplatesSpecified = !!(spec == null ? void 0 : spec.initialValueTemplates);
  }
  /** Set API version
   * @param apiVersion - API version
   * @returns document list builder based on the options and API version provided. See {@link DocumentListBuilder}
   */
  apiVersion(apiVersion) {
    return this.clone({
      options: {
        ...this.spec.options || {
          filter: ""
        },
        apiVersion
      }
    });
  }
  /** Get API version
   * @returns API version
   */
  getApiVersion() {
    var _a;
    return (_a = this.spec.options) == null ? void 0 : _a.apiVersion;
  }
  /** Set Document list filter
   * @param filter - GROQ-filter used to determine which documents to display. Do not support joins, since they operate on individual documents, and will ignore order-clauses and projections. See {@link https://www.sanity.io/docs/realtime-updates}
   * @returns document list builder based on the options and filter provided. See {@link DocumentListBuilder}
   */
  filter(filter2) {
    return this.clone({
      options: {
        ...this.spec.options,
        filter: filter2
      }
    });
  }
  /** Get Document list filter
   * @returns filter
   */
  getFilter() {
    var _a;
    return (_a = this.spec.options) == null ? void 0 : _a.filter;
  }
  /** Set Document list schema type name
   * @param type - schema type name.
   * @returns document list builder based on the schema type name provided. See {@link DocumentListBuilder}
   */
  schemaType(type2) {
    const schemaTypeName = typeof type2 == "string" ? type2 : type2.name;
    return this.clone({
      schemaTypeName
    });
  }
  /** Get Document list schema type name
   * @returns schema type name
   */
  getSchemaType() {
    return this.spec.schemaTypeName;
  }
  /** Set Document list options' parameters
   * @param params - parameters
   * @returns document list builder based on the options provided. See {@link DocumentListBuilder}
   */
  params(params) {
    return this.clone({
      options: {
        ...this.spec.options || {
          filter: ""
        },
        params
      }
    });
  }
  /** Get Document list options' parameters
   * @returns options
   */
  getParams() {
    var _a;
    return (_a = this.spec.options) == null ? void 0 : _a.params;
  }
  /** Set Document list default ordering
   * @param ordering - default sort ordering array. See {@link SortOrderingItem}
   * @returns document list builder based on ordering provided. See {@link DocumentListBuilder}
   */
  defaultOrdering(ordering) {
    if (!Array.isArray(ordering))
      throw new Error("`defaultOrdering` must be an array of order clauses");
    return this.clone({
      options: {
        ...this.spec.options || {
          filter: ""
        },
        defaultOrdering: ordering
      }
    });
  }
  /** Get Document list default ordering
   * @returns default ordering. See {@link SortOrderingItem}
   */
  getDefaultOrdering() {
    var _a;
    return (_a = this.spec.options) == null ? void 0 : _a.defaultOrdering;
  }
  /** Serialize Document list
   * @param options - serialization options. See {@link SerializeOptions}
   * @returns document list object based on path provided in options. See {@link DocumentList}
   */
  serialize(options = {
    path: []
  }) {
    var _a;
    if (typeof this.spec.id != "string" || !this.spec.id)
      throw new SerializeError("`id` is required for document lists", options.path, options.index, this.spec.title).withHelpUrl(HELP_URL.ID_REQUIRED);
    if (!this.spec.options || !this.spec.options.filter)
      throw new SerializeError("`filter` is required for document lists", options.path, this.spec.id, this.spec.title).withHelpUrl(HELP_URL.FILTER_REQUIRED);
    return ((_a = this.spec.options) == null ? void 0 : _a.filter) !== "_type == $type" && this.spec.options.filter && !this.spec.options.apiVersion && console.warn(`No apiVersion specified for document type list with custom filter: \`${this.spec.options.filter}\`. This will be required in the future. See %s for more info.`, generateHelpUrl(HELP_URL.API_VERSION_REQUIRED_FOR_CUSTOM_FILTER)), {
      ...super.serialize(options),
      type: "documentList",
      schemaTypeName: this.spec.schemaTypeName,
      child: this.spec.child || createDocumentChildResolverForItem(this._context),
      options: {
        ...this.spec.options,
        // @todo: make specifying .apiVersion required when using custom (non-simple) filters in v4
        apiVersion: this.spec.options.apiVersion || DEFAULT_STUDIO_CLIENT_OPTIONS.apiVersion,
        filter: validateFilter(this.spec, options)
      }
    };
  }
  /** Clone Document list builder (allows for options overriding)
   * @param withSpec - override document list spec. See {@link PartialDocumentList}
   * @returns document list builder. See {@link DocumentListBuilder}
   */
  clone(withSpec) {
    const builder = new _DocumentListBuilder(this._context);
    return builder.spec = {
      ...this.spec,
      ...withSpec
    }, this.initialValueTemplatesSpecified || (builder.spec.initialValueTemplates = inferInitialValueTemplates(this._context, builder.spec)), builder.spec.schemaTypeName || (builder.spec.schemaTypeName = inferTypeName(builder.spec)), builder;
  }
  /** Get Document list spec
   * @returns document list spec. See {@link PartialDocumentList}
   */
  getSpec() {
    return this.spec;
  }
};
function inferInitialValueTemplates(context, spec) {
  const {
    document: document2
  } = context, {
    schemaTypeName,
    options
  } = spec, {
    filter: filter2,
    params
  } = options || {
    filter: "",
    params: {}
  }, typeNames = schemaTypeName ? [schemaTypeName] : Array.from(new Set(getTypeNamesFromFilter(filter2, params)));
  if (typeNames.length !== 0)
    return typeNames.flatMap((schemaType) => document2.resolveNewDocumentOptions({
      type: "structure",
      schemaType
    })).map((option) => ({
      ...option,
      icon: AddIcon
    }));
}
function inferTypeName(spec) {
  const {
    options
  } = spec, {
    filter: filter2,
    params
  } = options || {
    filter: "",
    params: {}
  }, typeNames = getTypeNamesFromFilter(filter2, params);
  return typeNames.length === 1 ? typeNames[0] : void 0;
}
function getTypeNamesFromFilter(filter2, params = {}) {
  let typeNames = getTypeNamesFromEqualityFilter(filter2, params);
  return typeNames.length === 0 && (typeNames = getTypeNamesFromInTypesFilter(filter2, params)), typeNames;
}
function getTypeNamesFromEqualityFilter(filter2, params = {}) {
  const pattern = /\b_type\s*==\s*(['"].*?['"]|\$.*?(?:\s|$))|\B(['"].*?['"]|\$.*?(?:\s|$))\s*==\s*_type/g, matches = [];
  let match;
  for (; (match = pattern.exec(filter2)) !== null; )
    matches.push(match[1] || match[2]);
  return matches.map((candidate) => ((candidate[0] === "$" ? params[candidate.slice(1)] : candidate) || "").trim().replace(/^["']|["']$/g, "")).filter(Boolean);
}
function getTypeNamesFromInTypesFilter(filter2, params = {}) {
  const pattern = /\b_type\s+in\s+\[(.*?)\]/, matches = filter2.match(pattern);
  return matches ? matches[1].split(/,\s*/).map((match) => match.trim().replace(/^["']+|["']+$/g, "")).map((item) => item[0] === "$" ? params[item.slice(1)] : item).filter(Boolean) : [];
}
var getArgType = (thing) => thing instanceof ListBuilder ? "ListBuilder" : isPromise(thing) ? "Promise" : Array.isArray(thing) ? "array" : typeof thing;
var isListItem = (item) => item.type === "listItem";
var defaultCanHandleIntent = (intentName, params, context) => (context.pane.items || []).filter(isDocumentListItem).some((item) => item.schemaType.name === params.type && item._id === params.id) || shallowIntentChecker(intentName, params, context);
var resolveChildForItem = (itemId, options) => {
  const target = (options.parent.items.filter(isListItem).find((item) => item.id === itemId) || {
    child: void 0
  }).child;
  return !target || typeof target != "function" ? target : typeof target == "function" ? target(itemId, options) : target;
};
function maybeSerializeListItem(item, index, path) {
  if (item instanceof ListItemBuilder)
    return item.serialize({
      path,
      index
    });
  if (item instanceof DividerBuilder)
    return item.serialize();
  const listItem = item;
  if (listItem && listItem.type === "divider")
    return item;
  if (!listItem || listItem.type !== "listItem") {
    const gotWhat = listItem && listItem.type || getArgType(listItem), helpText = gotWhat === "array" ? " - did you forget to spread (...moreItems)?" : "";
    throw new SerializeError(`List items must be of type "listItem", got "${gotWhat}"${helpText}`, path, index).withHelpUrl(HELP_URL.INVALID_LIST_ITEM);
  }
  return item;
}
function isPromise(thing) {
  return isRecord$4(thing) && typeof thing.then == "function";
}
var ListBuilder = class _ListBuilder extends GenericListBuilder {
  /** buildable list option object. See {@link BuildableList} */
  constructor(_context, spec) {
    super(), this._context = _context, this.spec = spec || {}, this.initialValueTemplatesSpecified = !!(spec && spec.initialValueTemplates);
  }
  /**
   * Set list builder based on items provided
   * @param items - list items. See {@link ListItemBuilder}, {@link ListItem} and {@link Divider}
   * @returns list builder based on items provided. See {@link ListBuilder}
   */
  items(items2) {
    return this.clone({
      items: items2
    });
  }
  /** Get list builder items
   * @returns list items. See {@link BuildableList}
   */
  getItems() {
    return this.spec.items;
  }
  /** Serialize list builder
   * @param options - serialization options. See {@link SerializeOptions}
   * @returns list based on path in options. See {@link List}
   */
  serialize(options = {
    path: []
  }) {
    const id2 = this.spec.id;
    if (typeof id2 != "string" || !id2)
      throw new SerializeError("`id` is required for lists", options.path, options.index).withHelpUrl(HELP_URL.ID_REQUIRED);
    const items2 = typeof this.spec.items > "u" ? [] : this.spec.items;
    if (!Array.isArray(items2))
      throw new SerializeError("`items` must be an array of items", options.path, options.index).withHelpUrl(HELP_URL.LIST_ITEMS_MUST_BE_ARRAY);
    const path = (options.path || []).concat(id2), serializedItems = items2.map((item, index) => maybeSerializeListItem(item, index, path)), dupes = serializedItems.filter((val, i) => (0, import_find.default)(serializedItems, {
      id: val.id
    }, i + 1));
    if (dupes.length > 0) {
      const dupeIds = dupes.map((item) => item.id).slice(0, 5), dupeDesc = dupes.length > 5 ? `${dupeIds.join(", ")}...` : dupeIds.join(", ");
      throw new SerializeError(`List items with same ID found (${dupeDesc})`, options.path, options.index).withHelpUrl(HELP_URL.LIST_ITEM_IDS_MUST_BE_UNIQUE);
    }
    return {
      ...super.serialize(options),
      type: "list",
      canHandleIntent: this.spec.canHandleIntent || defaultCanHandleIntent,
      child: this.spec.child || resolveChildForItem,
      items: serializedItems
    };
  }
  /**
   * Clone list builder and return new list builder based on context and spec provided
   * @param withSpec - list options. See {@link BuildableList}
   * @returns new list builder based on context and spec provided. See {@link ListBuilder}
   */
  clone(withSpec) {
    const builder = new _ListBuilder(this._context);
    return builder.spec = {
      ...this.spec,
      ...withSpec
    }, builder;
  }
};
var ListItemBuilder = class _ListItemBuilder {
  /** List item option object. See {@link PartialListItem} */
  constructor(_context, spec) {
    this._context = _context, this.spec = spec || {};
  }
  /**
   * Set list item ID
   * @returns list item builder based on ID provided. See {@link ListItemBuilder}
   */
  id(id2) {
    return this.clone({
      id: id2
    });
  }
  /**
   * Get list item ID
   * @returns list item ID. See {@link PartialListItem}
   */
  getId() {
    return this.spec.id;
  }
  /**
   * Set list item title
   * @returns list item builder based on title provided. See {@link ListItemBuilder}
   */
  title(title) {
    return this.clone({
      title,
      id: getStructureNodeId(title, this.spec.id)
    });
  }
  /**
   * Get list item title
   * @returns list item title. See {@link PartialListItem}
   */
  getTitle() {
    return this.spec.title;
  }
  /** Set the i18n key and namespace used to populate the localized title.
   * @param i18n - the key and namespaced used to populate the localized title.
   * @returns component builder based on i18n key and ns provided
   */
  i18n(i18n) {
    return this.clone({
      i18n
    });
  }
  /** Get i18n key and namespace used to populate the localized title
   * @returns the i18n key and namespace used to populate the localized title
   */
  getI18n() {
    return this.spec.i18n;
  }
  /**
   * Set list item icon
   * @returns list item builder based on icon provided. See {@link ListItemBuilder}
   */
  icon(icon) {
    return this.clone({
      icon
    });
  }
  /**
   * Set if list item should show icon
   * @returns list item builder based on showIcon provided. See {@link ListItemBuilder}
   */
  showIcon(enabled = true) {
    return this.clone({
      displayOptions: {
        ...this.spec.displayOptions,
        showIcon: enabled
      }
    });
  }
  /**
   * Check if list item should show icon
   * @returns true if it should show the icon, false if not, undefined if not set
   */
  getShowIcon() {
    return this.spec.displayOptions ? this.spec.displayOptions.showIcon : void 0;
  }
  /**
   *Get list item icon
   * @returns list item icon. See {@link PartialListItem}
   */
  getIcon() {
    return this.spec.icon;
  }
  /**
   * Set list item child
   * @param child - list item child. See {@link UnserializedListItemChild}
   * @returns list item builder based on child provided. See {@link ListItemBuilder}
   */
  child(child) {
    return this.clone({
      child
    });
  }
  /**
   * Get list item child
   * @returns list item child. See {@link PartialListItem}
   */
  getChild() {
    return this.spec.child;
  }
  /**
   * Set list item schema type
   * @param schemaType - list item schema type. See {@link SchemaType}
   * @returns list item builder based on schema type provided. See {@link ListItemBuilder}
   */
  schemaType(schemaType) {
    return this.clone({
      schemaType
    });
  }
  /**
   * Get list item schema type
   * @returns list item schema type. See {@link PartialListItem}
   */
  getSchemaType() {
    const schemaType = this.spec.schemaType;
    return typeof schemaType == "string" ? this._context.schema.get(schemaType) : this.spec.schemaType;
  }
  /** Serialize list item builder
   * @param options - serialization options. See {@link ListItemSerializeOptions}
   * @returns list item node based on path provided in options. See {@link ListItem}
   */
  serialize(options = {
    path: []
  }) {
    const {
      id: id2,
      title,
      child
    } = this.spec;
    if (typeof id2 != "string" || !id2)
      throw new SerializeError("`id` is required for list items", options.path, options.index).withHelpUrl(HELP_URL.ID_REQUIRED);
    if (!options.titleIsOptional && (typeof title != "string" || !title))
      throw new SerializeError("`title` is required for list items", options.path, id2).withHelpUrl(HELP_URL.TITLE_REQUIRED);
    let schemaType = this.spec.schemaType;
    if (typeof schemaType == "string") {
      const type2 = this._context.schema.get(schemaType);
      if (!type2)
        throw new SerializeError(`Could not find type "${schemaType}" in schema`, options.path, id2).withHelpUrl(HELP_URL.SCHEMA_TYPE_NOT_FOUND);
      schemaType = type2;
    }
    const serializeOptions = {
      path: options.path.concat(id2),
      hint: "child"
    };
    let listChild = child instanceof ComponentBuilder || child instanceof DocumentListBuilder || child instanceof DocumentBuilder || child instanceof ListBuilder ? child.serialize(serializeOptions) : child;
    if (typeof listChild == "function") {
      const originalChild = listChild;
      listChild = (itemId, childOptions) => originalChild(itemId, {
        ...childOptions,
        serializeOptions
      });
    }
    return {
      ...this.spec,
      id: validateId(id2, options.path, options.index),
      schemaType,
      child: listChild,
      title,
      type: "listItem"
    };
  }
  /** Clone list item builder
   * @param withSpec - partial list item options. See {@link PartialListItem}
   * @returns list item builder based on context and spec provided. See {@link ListItemBuilder}
   */
  clone(withSpec) {
    const builder = new _ListItemBuilder(this._context);
    return builder.spec = {
      ...this.spec,
      ...withSpec
    }, builder;
  }
};
var createDefaultChildResolver = (context, spec) => (documentId) => {
  const schemaType = spec.schemaType && (typeof spec.schemaType == "string" ? spec.schemaType : spec.schemaType.name);
  return schemaType ? context.resolveDocumentNode({
    schemaType,
    documentId
  }) : new DocumentBuilder(context).id("documentEditor").documentId(documentId);
};
var DocumentListItemBuilder = class _DocumentListItemBuilder extends ListItemBuilder {
  /** Document list options. See {@link PartialDocumentListItem} */
  constructor(_context, spec) {
    super(_context, spec), this._context = _context, this.spec = spec || {};
  }
  /**
   * Serialize document list item
   * @param options - serialization options. See {@link SerializeOptions}
   * @returns document list item object based on path provided in options. See {@link DocumentListItem}
   */
  serialize(options = {
    path: []
  }) {
    const spec = super.serialize({
      ...options,
      titleIsOptional: true
    });
    if (!spec.schemaType)
      throw new SerializeError("`schemaType` is required for document list items", options.path, options.index).withHelpUrl(HELP_URL.SCHEMA_TYPE_REQUIRED);
    const child = spec.child || createDefaultChildResolver(this._context, spec);
    return {
      ...spec,
      child,
      schemaType: spec.schemaType,
      _id: spec.id
    };
  }
  /** Clone Document list item builder (allows for options overriding)
   * @param withSpec - Document list item builder options. See {@link PartialDocumentListItem}
   * @returns document list item builder. See {@link DocumentListItemBuilder}
   */
  clone(withSpec) {
    const builder = new _DocumentListItemBuilder(this._context);
    return builder.spec = {
      ...this.spec,
      ...withSpec
    }, builder;
  }
};
function isDocumentListItem(item) {
  return isRecord$4(item) && typeof item.schemaType < "u" && typeof item._id == "string";
}
var DocumentTypeListBuilder = class _DocumentTypeListBuilder extends DocumentListBuilder {
  /** Document list options. See {@link PartialDocumentList} */
  constructor(_context, spec) {
    super(_context), this._context = _context, this.spec = spec || {};
  }
  /**
   * Set Document type list child
   * @param child - Child component. See {@link Child}
   * @returns document type list builder based on child component provided without default intent handler. See {@link DocumentTypeListBuilder}
   */
  child(child) {
    return this.cloneWithoutDefaultIntentHandler({
      child
    });
  }
  /** Clone Document type list builder (allows for options overriding)
   * @param withSpec - Document type list builder options. See {@link PartialDocumentList}
   * @returns document type list builder. See {@link DocumentTypeListBuilder}
   */
  clone(withSpec) {
    const parent = super.clone(withSpec), builder = new _DocumentTypeListBuilder(this._context);
    return builder.spec = {
      ...this.spec,
      ...parent.getSpec(),
      ...withSpec
    }, builder;
  }
  /** Clone Document type list builder (allows for options overriding) and remove default intent handler
   * @param withSpec - Document type list builder options. See {@link PartialDocumentList}
   * @returns document type list builder without default intent handler. See {@link DocumentTypeListBuilder}
   */
  cloneWithoutDefaultIntentHandler(withSpec) {
    const parent = super.clone(withSpec), builder = new _DocumentTypeListBuilder(this._context), canHandleIntent = this.spec.canHandleIntent, override = canHandleIntent && canHandleIntent.identity === DEFAULT_INTENT_HANDLER ? {
      canHandleIntent: void 0
    } : {};
    return builder.spec = {
      ...parent.getSpec(),
      ...this.spec,
      ...withSpec,
      ...override
    }, builder;
  }
};
var BUNDLED_DOC_TYPES = ["sanity.imageAsset", "sanity.fileAsset"];
function isBundledDocType(typeName) {
  return BUNDLED_DOC_TYPES.includes(typeName);
}
function isDocumentType(schemaType) {
  var _a;
  return ((_a = schemaType.type) == null ? void 0 : _a.name) === "document";
}
function isList(collection) {
  return collection.type === "list";
}
function getDocumentTypes({
  schema
}) {
  return schema.getTypeNames().filter((n2) => {
    const schemaType = schema.get(n2);
    return schemaType && isDocumentType(schemaType);
  }).filter((n2) => !isBundledDocType(n2));
}
function getDocumentTypeListItems(context) {
  return getDocumentTypes(context).map((typeName) => getDocumentTypeListItem(context, typeName));
}
function getDocumentTypeListItem(context, typeName) {
  const {
    schema
  } = context, type2 = schema.get(typeName);
  if (!type2)
    throw new Error(`Schema type with name "${typeName}" not found`);
  const title = type2.title || (0, import_startCase.default)(typeName);
  return new ListItemBuilder(context).id(typeName).title(title).schemaType(type2).child((id2, childContext) => {
    const parent = childContext.parent, parentItem = isList(parent) ? parent.items.find((item) => item.id === id2) : null;
    let list = getDocumentTypeList(context, typeName);
    return parentItem && parentItem.title && (list = list.title(parentItem.title)), list;
  });
}
function getDocumentTypeList(context, typeNameOrSpec) {
  const {
    schema,
    resolveDocumentNode
  } = context, schemaType = typeof typeNameOrSpec == "string" ? typeNameOrSpec : typeNameOrSpec.schemaType, typeName = typeof schemaType == "string" ? schemaType : schemaType.name, spec = typeof typeNameOrSpec == "string" ? {} : typeNameOrSpec, type2 = schema.get(typeName);
  if (!type2)
    throw new Error(`Schema type with name "${typeName}" not found`);
  const title = type2.title || (0, import_startCase.default)(typeName);
  return new DocumentTypeListBuilder(context).id(spec.id || typeName).title(spec.title || title).filter("_type == $type").params({
    type: typeName
  }).schemaType(type2).defaultOrdering(DEFAULT_SELECTED_ORDERING_OPTION.by).menuItemGroups(spec.menuItemGroups || [{
    id: "sorting",
    title: "Sort",
    i18n: {
      title: {
        key: "menu-item-groups.actions-group",
        ns: structureLocaleNamespace
      }
    }
  }, {
    id: "layout",
    title: "Layout",
    i18n: {
      title: {
        key: "menu-item-groups.layout-group",
        ns: structureLocaleNamespace
      }
    }
  }, {
    id: "actions",
    title: "Actions",
    i18n: {
      title: {
        key: "menu-item-groups.sorting-group",
        ns: structureLocaleNamespace
      }
    }
  }]).child(spec.child || ((documentId) => resolveDocumentNode({
    schemaType: typeName,
    documentId
  }))).canHandleIntent(spec.canHandleIntent || defaultIntentChecker).menuItems(spec.menuItems || [
    // Create new (from action button) will be added in serialization step of GenericList
    // Sort by <Y>
    ...getOrderingMenuItemsForSchemaType(context, type2),
    // Display as <Z>
    new MenuItemBuilder(context).group("layout").i18n({
      title: {
        key: "menu-items.layout.compact-view",
        ns: structureLocaleNamespace
      }
    }).title("Compact view").icon(StackCompactIcon).action("setLayout").params({
      layout: "default"
    }),
    new MenuItemBuilder(context).group("layout").i18n({
      title: {
        key: "menu-items.layout.detailed-view",
        ns: structureLocaleNamespace
      }
    }).title("Detailed view").icon(StackIcon).action("setLayout").params({
      layout: "detail"
    })
    // Create new (from menu) will be added in serialization step of GenericList
  ]);
}
function hasIcon(schemaType) {
  return !schemaType || typeof schemaType == "string" ? false : !!schemaType.icon;
}
function getDefaultStructure(context) {
  const items2 = getDocumentTypeListItems(context);
  return new ListBuilder(context).id("__root__").title("Content").i18n({
    title: {
      key: "default-definition.content-title",
      ns: structureLocaleNamespace
    }
  }).items(items2).showIcons(items2.some((item) => hasIcon(item.getSchemaType())));
}
function createStructureBuilder({
  defaultDocumentNode,
  source,
  perspectiveStack
}) {
  const configContext = getConfigContextFromSource(source), context = {
    ...source,
    getStructureBuilder: () => structureBuilder,
    resolveDocumentNode: (options) => {
      let builder = (defaultDocumentNode == null ? void 0 : defaultDocumentNode(structureBuilder, {
        ...options,
        ...configContext
      })) || new DocumentBuilder(context);
      return builder.getId() || (builder = builder.id("documentEditor")), options.documentId && (builder = builder.documentId(getPublishedId(options.documentId))), builder.schemaType(options.schemaType);
    },
    perspectiveStack
  }, structureBuilder = {
    defaults: () => getDefaultStructure(context),
    documentTypeList: (...args) => getDocumentTypeList(context, ...args),
    documentTypeListItem: (...args) => getDocumentTypeListItem(context, ...args),
    documentTypeListItems: (...args) => getDocumentTypeListItems(context, ...args),
    document: (...args) => new DocumentBuilder(context, ...args),
    documentWithInitialValueTemplate: (...args) => documentFromEditorWithInitialValue(context, ...args),
    defaultDocument: context.resolveDocumentNode,
    list: (...args) => new ListBuilder(context, ...args),
    listItem: (...args) => new ListItemBuilder(context, ...args),
    menuItem: (...args) => new MenuItemBuilder(context, ...args),
    menuItemGroup: (...args) => new MenuItemGroupBuilder(context, ...args),
    menuItemsFromInitialValueTemplateItems: (...args) => menuItemsFromInitialValueTemplateItems(context, ...args),
    documentList: (...args) => new DocumentListBuilder(context, ...args),
    documentListItem: (...args) => new DocumentListItemBuilder(context, ...args),
    orderingMenuItem: (...args) => getOrderingMenuItem(context, ...args),
    orderingMenuItemsForType: (...args) => getOrderingMenuItemsForSchemaType(context, ...args),
    editor: (...args) => documentFromEditor(context, ...args),
    defaultInitialValueTemplateItems: (...args) => defaultInitialValueTemplateItems(context, ...args),
    initialValueTemplateItem: (templateId, parameters) => new InitialValueTemplateItemBuilder(context, {
      id: templateId,
      parameters,
      templateId
    }),
    component: (spec) => (0, import_react_is.isValidElementType)(spec) ? new ComponentBuilder().component(spec) : new ComponentBuilder(spec),
    divider: (spec) => new DividerBuilder(spec),
    view: views,
    context
  };
  return structureBuilder;
}
function StructureToolProvider({
  defaultDocumentNode,
  structure: resolveStructure,
  children
}) {
  const [layoutCollapsed, setLayoutCollapsed] = (0, import_react.useState)(false), source = useSource(), configContext = useConfigContextFromSource(source), documentStore = useDocumentStore(), {
    perspectiveStack
  } = usePerspective(), S = (0, import_react.useMemo)(() => createStructureBuilder({
    defaultDocumentNode,
    source,
    perspectiveStack
  }), [defaultDocumentNode, source, perspectiveStack]), rootPaneNode = (0, import_react.useMemo)(() => resolveStructure ? resolveStructure(S, {
    ...configContext,
    documentStore,
    perspectiveStack
  }) : S.defaults(), [resolveStructure, S, configContext, documentStore, perspectiveStack]), features = (0, import_react.useMemo)(() => ({
    backButton: layoutCollapsed,
    resizablePanes: !layoutCollapsed,
    reviewChanges: !layoutCollapsed,
    splitPanes: !layoutCollapsed,
    splitViews: !layoutCollapsed
  }), [layoutCollapsed]), structureTool2 = (0, import_react.useMemo)(() => ({
    features,
    layoutCollapsed,
    setLayoutCollapsed,
    rootPaneNode,
    structureContext: S.context
  }), [features, layoutCollapsed, rootPaneNode, S.context]);
  return (0, import_jsx_runtime.jsx)(StructureToolContext.Provider, { value: structureTool2, children });
}

// ../node_modules/sanity/lib/_chunks-es/pane.mjs
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var import_react_compiler_runtime = __toESM(require_dist(), 1);
var import_react3 = __toESM(require_react(), 1);
var import_omit = __toESM(require_omit(), 1);
var import_fromPairs = __toESM(require_fromPairs(), 1);
var import_toPairs = __toESM(require_toPairs(), 1);

// ../node_modules/scrollmirror/dist/ScrollMirror.modern.js
var e = () => new Promise((e2) => {
  requestAnimationFrame(() => e2());
});
var t = ({ clientWidth: e2, clientHeight: t2, scrollWidth: o2, scrollHeight: r2 }) => r2 > t2 || o2 > e2;
var o = (e2) => {
  const t2 = window.getComputedStyle(e2).overflow;
  return t2.includes("auto") || t2.includes("scroll");
};
var r = (e2) => {
  if (null == e2) return { x: 0, y: 0 };
  const { scrollTop: t2, scrollHeight: o2, clientHeight: r2, scrollLeft: n2, scrollWidth: l2, clientWidth: s2 } = e2, i = o2 - r2;
  return { x: n2 ? n2 / Math.max(1e-5, l2 - s2) : 0, y: t2 ? t2 / Math.max(1e-5, i) : 0 };
};
function n() {
  return n = Object.assign ? Object.assign.bind() : function(e2) {
    for (var t2 = 1; t2 < arguments.length; t2++) {
      var o2 = arguments[t2];
      for (var r2 in o2) ({}).hasOwnProperty.call(o2, r2) && (e2[r2] = o2[r2]);
    }
    return e2;
  }, n.apply(null, arguments);
}
function l(e2) {
  return e2.matches("body *") ? e2 : window;
}
var s = class {
  constructor(l2, s2 = {}) {
    var i, c2 = this;
    this.elements = void 0, this.defaults = { vertical: true, horizontal: true, debug: true }, this.options = void 0, this.paused = false, this.logger = void 0, this.handleScroll = async function(t2) {
      if (c2.paused) return;
      if (!t2.currentTarget) return;
      const o2 = c2.getScrollContainer(t2.currentTarget);
      await e(), c2.mirrorScrollPositions(r(o2), o2);
    }, this.elements = [...l2].filter(Boolean).map((e2) => this.getScrollContainer(e2)), this.elements = [...new Set(this.elements)], this.options = n({}, this.defaults, s2), this.options.debug && (this.logger = (i = "[scroll-mirror]", { log: (...e2) => console.log(i, ...e2), warn: (...e2) => console.warn(i, ...e2), error: (...e2) => console.error(i, ...e2) }), (function(e2, r2) {
      if (e2.length < 1) null == r2 || r2.warn("No elements provided.");
      else {
        e2.length < 2 && (null == r2 || r2.warn("Only one element provided.", e2)), e2.some((e3) => !e3) && (null == r2 || r2.error("Some elements are not defined.", e2));
        for (const n2 of e2) n2 instanceof HTMLElement && !t(n2) && (null == r2 || r2.warn("Element doesn't have overflow:", n2)), n2 instanceof HTMLElement && n2.matches("body *") && !o(n2) && (null == r2 || r2.warn('No "overflow: auto;" or "overflow: scroll;" set on element:', n2));
      }
    })(this.elements, this.logger)), this.elements.forEach((e2) => this.addScrollHandler(e2)), this.elements.includes(document.documentElement) && this.mirrorScrollPositions(r(document.documentElement), document.documentElement);
  }
  pause() {
    this.paused = true;
  }
  resume() {
    this.paused = false;
  }
  destroy() {
    this.elements.forEach((e2) => this.removeScrollHandler(e2));
  }
  addScrollHandler(e2) {
    this.removeScrollHandler(e2), l(e2).addEventListener("scroll", this.handleScroll, { passive: true });
  }
  removeScrollHandler(e2) {
    l(e2).removeEventListener("scroll", this.handleScroll);
  }
  getScrollContainer(e2) {
    return e2 instanceof HTMLElement && e2.matches("body *") ? e2 : document.documentElement;
  }
  mirrorScrollPositions(e2, t2 = void 0) {
    this.elements.forEach((o2) => {
      t2 !== o2 && (this.removeScrollHandler(o2), this.setScrollPosition(e2, o2), window.requestAnimationFrame(() => {
        this.addScrollHandler(o2);
      }));
    });
  }
  setScrollPosition(e2, t2) {
    const { vertical: o2, horizontal: r2 } = this.options, n2 = t2.scrollWidth - t2.clientWidth, l2 = t2.scrollHeight - t2.clientHeight;
    o2 && l2 && t2.scrollTo({ top: l2 * e2.y, behavior: "instant" }), r2 && n2 && t2.scrollTo({ left: n2 * e2.x, behavior: "instant" });
  }
  get progress() {
    const e2 = this.elements.find((e3) => t(e3));
    return r(e2);
  }
  set progress(e2) {
    "number" == typeof e2 && (e2 = { x: e2, y: e2 });
    const t2 = n({}, this.progress, e2);
    (function(e3, t3) {
      let o2 = true;
      for (const [r2, n2] of Object.entries(e3)) ("number" != typeof n2 || n2 < 0 || n2 > 1) && (null == t3 || t3.error(`progress.${r2} must be a number between 0-1`), o2 = false);
      return o2;
    })(t2, this.logger) && this.mirrorScrollPositions(t2);
  }
};

// ../node_modules/sanity/lib/_chunks-es/pane.mjs
var import_noop = __toESM(require_noop(), 1);
var import_react_fast_compare = __toESM(require_react_fast_compare(), 1);

// ../node_modules/@rexxars/react-json-inspector/dist/index.js
var import_jsx_runtime2 = __toESM(require_jsx_runtime());
var import_react2 = __toESM(require_react());
var import_debounce = __toESM(require_debounce());
var import_md5_o_matic = __toESM(require_md5omatic());
var id = Math.ceil(Math.random() * 10);
var uid = () => ++id;
function type(value) {
  return Object.prototype.toString.call(value).slice(8, -1);
}
function isPrimitive(value) {
  const t2 = type(value);
  return t2 !== "Object" && t2 !== "Array";
}
var Highlighter = class extends import_react2.Component {
  shouldComponentUpdate(p) {
    return p.highlight !== this.props.highlight;
  }
  render() {
    const str = this.props.string || "", highlight = this.props.highlight || "", highlightStart = str.search(highlight);
    if (!highlight || highlightStart === -1)
      return (0, import_jsx_runtime2.jsx)("span", { children: str });
    const highlightLength = highlight.source.length, highlightString = str.slice(
      highlightStart,
      highlightStart + highlightLength
    );
    return (0, import_jsx_runtime2.jsx)("span", { children: str.split(highlight).map(function(part, index) {
      return (0, import_jsx_runtime2.jsxs)("span", { children: [
        index > 0 ? (0, import_jsx_runtime2.jsx)("span", { className: "json-inspector__hl", children: highlightString }) : null,
        part
      ] }, index);
    }) });
  }
};
function isObject(value) {
  return typeof value == "object" && value !== null && !Array.isArray(value);
}
var PATH_PREFIX = ".root.";
var Leaf = class _Leaf extends import_react2.Component {
  constructor(props) {
    super(props);
    __publicField(this, "_onShowOriginalClick", (e2) => {
      var _a, _b;
      this.setState({
        original: (_b = (_a = this.props).getOriginal) == null ? void 0 : _b.call(_a, this.keypath())
      }), e2.stopPropagation();
    });
    this.state = {
      expanded: this._isInitiallyExpanded(this.props)
    };
  }
  render() {
    const { label, data, root: root2, id: inputId } = this.props, id2 = "id_" + uid(), d = {
      path: this.keypath(),
      key: label.toString(),
      value: data
    }, onLabelClick = this._onClick.bind(this, d);
    return (0, import_jsx_runtime2.jsxs)(
      "div",
      {
        "data-testid": root2 ? "leaf-root" : "leaf-child",
        "aria-expanded": this.state.expanded,
        "data-root": root2 || void 0,
        className: this.getClassName(),
        id: "leaf-" + this._rootPath(),
        children: [
          (0, import_jsx_runtime2.jsx)(
            "input",
            {
              className: "json-inspector__radio",
              type: "radio",
              name: id2,
              id: inputId,
              tabIndex: -1
            }
          ),
          (0, import_jsx_runtime2.jsxs)(
            "label",
            {
              className: "json-inspector__line",
              htmlFor: id2,
              onClick: onLabelClick,
              children: [
                (0, import_jsx_runtime2.jsx)("div", { className: "json-inspector__flatpath", children: d.path }),
                (0, import_jsx_runtime2.jsxs)("span", { className: "json-inspector__key", children: [
                  this.format(d.key),
                  ":",
                  this.renderInteractiveLabel(d.key, true)
                ] }),
                this.renderTitle(),
                this.renderShowOriginalButton()
              ]
            }
          ),
          this.renderChildren()
        ]
      }
    );
  }
  renderTitle() {
    const data = this.data(), t2 = type(data);
    if (Array.isArray(data)) {
      const length = data.length;
      return (0, import_jsx_runtime2.jsxs)("span", { className: "json-inspector__value json-inspector__value_helper", children: [
        length > 0 ? "[…] " : "[] ",
        items(length)
      ] });
    }
    if (typeof data == "object" && data !== null) {
      const keys = Object.keys(data).length;
      return (0, import_jsx_runtime2.jsxs)("span", { className: "json-inspector__value json-inspector__value_helper", children: [
        keys > 0 ? "{…} " : "{} ",
        properties(keys)
      ] });
    }
    return (0, import_jsx_runtime2.jsxs)(
      "span",
      {
        className: "json-inspector__value json-inspector__value_" + t2.toLowerCase(),
        children: [
          this.format(String(data)),
          this.renderInteractiveLabel(data, false)
        ]
      }
    );
  }
  renderChildren() {
    const {
      verboseShowOriginal,
      query,
      id: id2,
      isExpanded: isExpanded2,
      interactiveLabel,
      onClick,
      getOriginal
    } = this.props, childPrefix = this._rootPath(), data = this.data();
    return this.state.expanded && (isObject(data) || Array.isArray(data)) ? Object.keys(data).map((key) => {
      const value = data[key], shouldGetOriginal = !this.state.original || (verboseShowOriginal ? query : false);
      return (0, import_jsx_runtime2.jsx)(
        _Leaf,
        {
          data: value,
          label: key,
          prefix: childPrefix,
          onClick,
          id: id2,
          query,
          getOriginal: shouldGetOriginal ? getOriginal : void 0,
          isExpanded: isExpanded2,
          interactiveLabel,
          verboseShowOriginal
        },
        getLeafKey(key, value)
      );
    }) : null;
  }
  renderShowOriginalButton() {
    const { data, getOriginal, query } = this.props;
    return isPrimitive(data) || this.state.original || !getOriginal || !query || query.test(this.keypath()) ? null : (0, import_jsx_runtime2.jsx)(
      "span",
      {
        className: "json-inspector__show-original",
        onClick: this._onShowOriginalClick
      }
    );
  }
  renderInteractiveLabel(originalValue, isKey) {
    const InteractiveLabel = this.props.interactiveLabel;
    return typeof InteractiveLabel == "function" ? (0, import_jsx_runtime2.jsx)(
      InteractiveLabel,
      {
        value: String(originalValue),
        originalValue,
        isKey,
        keypath: this.keypath()
      }
    ) : null;
  }
  static getDerivedStateFromProps(props, state2) {
    return props.query ? {
      expanded: !props.query.test(props.label)
    } : null;
  }
  componentDidUpdate(prevProps) {
    prevProps.query && !this.props.query && this.setState({
      expanded: this._isInitiallyExpanded(this.props)
    });
  }
  _rootPath() {
    return (this.props.prefix || "") + "." + this.props.label;
  }
  keypath() {
    return this._rootPath().slice(PATH_PREFIX.length);
  }
  data() {
    return this.state.original || this.props.data;
  }
  format(str) {
    return (0, import_jsx_runtime2.jsx)(Highlighter, { string: str, highlight: this.props.query });
  }
  getClassName() {
    let cn = "json-inspector__leaf";
    return this.props.root && (cn += " json-inspector__leaf_root"), this.state.expanded && (cn += " json-inspector__leaf_expanded"), isPrimitive(this.props.data) || (cn += " json-inspector__leaf_composite"), cn;
  }
  toggle() {
    this.setState({
      expanded: !this.state.expanded
    });
  }
  _onClick(data, e2) {
    this.toggle(), this.props.onClick && this.props.onClick(data), e2.stopPropagation();
  }
  _isInitiallyExpanded(p) {
    if (p.root)
      return true;
    const keypath = this.keypath();
    return p.query ? !p.query.test(keypath) && typeof p.getOriginal == "function" : p.isExpanded ? p.isExpanded(keypath, p.data) : false;
  }
};
function items(count2) {
  return count2 + (count2 === 1 ? " item" : " items");
}
function properties(count2) {
  return count2 + (count2 === 1 ? " property" : " properties");
}
function getLeafKey(key, value) {
  if (isPrimitive(value)) {
    const hash = (0, import_md5_o_matic.default)(String(value));
    return key + ":" + hash;
  } else
    return key + "[" + type(value) + "]";
}
var noop = (...args) => {
};
var SearchBar = ({ onChange = noop }) => {
  const onSearchChange = (0, import_react2.useCallback)(
    (evt) => onChange(evt.target.value),
    [onChange]
  );
  return (0, import_jsx_runtime2.jsx)(
    "input",
    {
      className: "json-inspector__search",
      type: "search",
      placeholder: "Search",
      onChange: onSearchChange
    }
  );
};
function isEmpty(object) {
  return isObject(object) ? Object.keys(object).length === 0 : Array.isArray(object) ? object.length === 0 : object === null || typeof object != "string" || typeof object != "number" ? true : Object.keys(object).length === 0;
}
var getFilterer = memoize(
  (data, opts) => {
    const options = opts || { cacheResults: true }, cache = {};
    return function(query) {
      if (!options.cacheResults)
        return find2(data, query, options);
      let subquery;
      if (!cache[query]) {
        for (var i = query.length - 1; i > 0; i -= 1)
          if (subquery = query.slice(0, i), cache[subquery]) {
            cache[query] = find2(cache[subquery], query, options);
            break;
          }
      }
      return cache[query] || (cache[query] = find2(data, query, options)), cache[query];
    };
  }
);
function find2(data, query, options) {
  return !isObject(data) && !Array.isArray(data) ? {} : Object.keys(data).reduce(function(acc, key) {
    const value = data[key];
    let matches;
    return value ? typeof value != "object" ? ((contains(query, key, options) || contains(query, value, options)) && (acc[key] = value), acc) : contains(query, key, options) ? (acc[key] = value, acc) : (matches = find2(value, query, options), isEmpty(matches) || Object.assign(acc, pair(key, matches)), acc) : acc;
  }, {});
}
function contains(query, value, options) {
  if (!value)
    return false;
  var haystack = String(value), needle = query;
  return (options == null ? void 0 : options.ignoreCase) && (haystack = haystack.toLowerCase(), needle = needle.toLowerCase()), haystack.indexOf(needle) !== -1;
}
function pair(key, value) {
  return { [key]: value };
}
function memoize(fn) {
  let lastData, lastOptions, lastResult;
  return (data, options) => ((!lastResult || data !== lastData || options !== lastOptions) && (lastData = data, lastOptions = options, lastResult = fn(data, options)), lastResult);
}
var PATH_DELIMITER = ".";
function integer(str) {
  return parseInt(str, 10);
}
function lens(data, path) {
  var p = path.split(PATH_DELIMITER), segment = p.shift();
  if (!segment)
    return data;
  if (Array.isArray(data) && data[integer(segment)])
    return lens(data[integer(segment)], p.join(PATH_DELIMITER));
  if (isObject(data) && segment in data)
    return lens(data[segment], p.join(PATH_DELIMITER));
}
var defaultValidateQuery = (query) => query.length >= 2;
var defaultFilterOptions = { cacheResults: true, ignoreCase: false };
var JsonInspector = class extends import_react2.Component {
  constructor(props) {
    super(props);
    __publicField(this, "search", (query) => {
      this.setState({ query });
    });
    __publicField(this, "createFilterer", (data, options) => {
      this.setState({
        filterer: getFilterer(data, options)
      });
    });
    __publicField(this, "getOriginal", (path) => lens(this.props.data, path));
    this.state = {
      query: "",
      filterer: getFilterer(props.data, props.filterOptions)
    };
  }
  render() {
    const {
      data: rawData,
      className,
      onClick,
      id: id2,
      isExpanded: isExpanded2,
      interactiveLabel,
      verboseShowOriginal,
      filterOptions = defaultFilterOptions,
      validateQuery = defaultValidateQuery
    } = this.props, isQueryValid = this.state.query !== "" && validateQuery(this.state.query), data = isQueryValid ? this.state.filterer(this.state.query) : rawData, isNotFound = isQueryValid && isEmpty(data);
    return (0, import_jsx_runtime2.jsxs)(
      "div",
      {
        "data-testid": "json-inspector",
        className: "json-inspector " + className,
        children: [
          this.renderToolbar(),
          isNotFound ? (0, import_jsx_runtime2.jsx)("div", { className: "json-inspector__not-found", children: "Nothing found" }) : (0, import_jsx_runtime2.jsx)(
            Leaf,
            {
              data,
              onClick,
              id: id2,
              getOriginal: this.getOriginal,
              query: isQueryValid ? new RegExp(
                this.state.query,
                filterOptions.ignoreCase ? "i" : ""
              ) : null,
              label: "root",
              root: true,
              isExpanded: isExpanded2,
              interactiveLabel,
              verboseShowOriginal
            }
          )
        ]
      }
    );
  }
  renderToolbar() {
    var _a;
    const Search2 = this.props.search;
    return Search2 ? (0, import_jsx_runtime2.jsx)("div", { className: "json-inspector__toolbar", children: (0, import_jsx_runtime2.jsx)(
      Search2,
      {
        onChange: (0, import_debounce.default)(
          this.search,
          (_a = this.props.searchOptions) == null ? void 0 : _a.debounceTime
        ),
        data: this.props.data,
        query: this.state.query
      }
    ) }) : null;
  }
  static getDerivedStateFromProps(nextProps, prevState) {
    const filterer = getFilterer(nextProps.data, nextProps.filterOptions);
    return filterer === prevState.filterer ? null : { ...prevState, filterer };
  }
  shouldComponentUpdate(nextProps, prevState) {
    return prevState.query !== this.state.query || nextProps.data !== this.props.data || nextProps.onClick !== this.props.onClick;
  }
};
__publicField(JsonInspector, "defaultProps", {
  data: null,
  search: SearchBar,
  searchOptions: {
    debounceTime: 0
  },
  className: "",
  id: "json-" + Date.now(),
  onClick: noop,
  filterOptions: {
    cacheResults: true,
    ignoreCase: false
  },
  validateQuery: function(query) {
    return query.length >= 2;
  },
  /**
   * Decide whether the leaf node at given `keypath` should be expanded initially.
   *
   * @param keypath - Path to the node
   * @param value - Value of the node
   * @returns True if node should be expanded, false otherwise
   */
  isExpanded: function(keypath, value) {
    return false;
  },
  verboseShowOriginal: false
});

// ../node_modules/sanity/lib/_chunks-es/pane.mjs
var import_quick_lru = __toESM(require_quick_lru(), 1);
var import_isEqual = __toESM(require_isEqual(), 1);
var import_shallow_equals = __toESM(require_shallow_equals(), 1);
var import_uniqBy = __toESM(require_uniqBy(), 1);
var import_negate = __toESM(require_negate(), 1);

// ../node_modules/@tanstack/react-table/build/lib/index.mjs
var React = __toESM(require_react(), 1);

// ../node_modules/@tanstack/table-core/build/lib/index.mjs
function createColumnHelper() {
  return {
    accessor: (accessor, column) => {
      return typeof accessor === "function" ? {
        ...column,
        accessorFn: accessor
      } : {
        ...column,
        accessorKey: accessor
      };
    },
    display: (column) => column,
    group: (column) => column
  };
}
function functionalUpdate(updater, input) {
  return typeof updater === "function" ? updater(input) : updater;
}
function makeStateUpdater(key, instance) {
  return (updater) => {
    instance.setState((old) => {
      return {
        ...old,
        [key]: functionalUpdate(updater, old[key])
      };
    });
  };
}
function isFunction(d) {
  return d instanceof Function;
}
function isNumberArray(d) {
  return Array.isArray(d) && d.every((val) => typeof val === "number");
}
function flattenBy(arr, getChildren) {
  const flat = [];
  const recurse = (subArr) => {
    subArr.forEach((item) => {
      flat.push(item);
      const children = getChildren(item);
      if (children != null && children.length) {
        recurse(children);
      }
    });
  };
  recurse(arr);
  return flat;
}
function memo(getDeps, fn, opts) {
  let deps = [];
  let result;
  return (depArgs) => {
    let depTime;
    if (opts.key && opts.debug) depTime = Date.now();
    const newDeps = getDeps(depArgs);
    const depsChanged = newDeps.length !== deps.length || newDeps.some((dep, index) => deps[index] !== dep);
    if (!depsChanged) {
      return result;
    }
    deps = newDeps;
    let resultTime;
    if (opts.key && opts.debug) resultTime = Date.now();
    result = fn(...newDeps);
    opts == null || opts.onChange == null || opts.onChange(result);
    if (opts.key && opts.debug) {
      if (opts != null && opts.debug()) {
        const depEndTime = Math.round((Date.now() - depTime) * 100) / 100;
        const resultEndTime = Math.round((Date.now() - resultTime) * 100) / 100;
        const resultFpsPercentage = resultEndTime / 16;
        const pad = (str, num) => {
          str = String(str);
          while (str.length < num) {
            str = " " + str;
          }
          return str;
        };
        console.info(`%c⏱ ${pad(resultEndTime, 5)} /${pad(depEndTime, 5)} ms`, `
            font-size: .6rem;
            font-weight: bold;
            color: hsl(${Math.max(0, Math.min(120 - 120 * resultFpsPercentage, 120))}deg 100% 31%);`, opts == null ? void 0 : opts.key);
      }
    }
    return result;
  };
}
function getMemoOptions(tableOptions, debugLevel, key, onChange) {
  return {
    debug: () => {
      var _tableOptions$debugAl;
      return (_tableOptions$debugAl = tableOptions == null ? void 0 : tableOptions.debugAll) != null ? _tableOptions$debugAl : tableOptions[debugLevel];
    },
    key,
    onChange
  };
}
function createCell(table, row, column, columnId) {
  const getRenderValue = () => {
    var _cell$getValue;
    return (_cell$getValue = cell.getValue()) != null ? _cell$getValue : table.options.renderFallbackValue;
  };
  const cell = {
    id: `${row.id}_${column.id}`,
    row,
    column,
    getValue: () => row.getValue(columnId),
    renderValue: getRenderValue,
    getContext: memo(() => [table, column, row, cell], (table2, column2, row2, cell2) => ({
      table: table2,
      column: column2,
      row: row2,
      cell: cell2,
      getValue: cell2.getValue,
      renderValue: cell2.renderValue
    }), getMemoOptions(table.options, "debugCells", "cell.getContext"))
  };
  table._features.forEach((feature) => {
    feature.createCell == null || feature.createCell(cell, column, row, table);
  }, {});
  return cell;
}
function createColumn(table, columnDef, depth, parent) {
  var _ref, _resolvedColumnDef$id;
  const defaultColumn = table._getDefaultColumnDef();
  const resolvedColumnDef = {
    ...defaultColumn,
    ...columnDef
  };
  const accessorKey = resolvedColumnDef.accessorKey;
  let id2 = (_ref = (_resolvedColumnDef$id = resolvedColumnDef.id) != null ? _resolvedColumnDef$id : accessorKey ? typeof String.prototype.replaceAll === "function" ? accessorKey.replaceAll(".", "_") : accessorKey.replace(/\./g, "_") : void 0) != null ? _ref : typeof resolvedColumnDef.header === "string" ? resolvedColumnDef.header : void 0;
  let accessorFn;
  if (resolvedColumnDef.accessorFn) {
    accessorFn = resolvedColumnDef.accessorFn;
  } else if (accessorKey) {
    if (accessorKey.includes(".")) {
      accessorFn = (originalRow) => {
        let result = originalRow;
        for (const key of accessorKey.split(".")) {
          var _result;
          result = (_result = result) == null ? void 0 : _result[key];
          if (result === void 0) {
            console.warn(`"${key}" in deeply nested key "${accessorKey}" returned undefined.`);
          }
        }
        return result;
      };
    } else {
      accessorFn = (originalRow) => originalRow[resolvedColumnDef.accessorKey];
    }
  }
  if (!id2) {
    if (true) {
      throw new Error(resolvedColumnDef.accessorFn ? `Columns require an id when using an accessorFn` : `Columns require an id when using a non-string header`);
    }
    throw new Error();
  }
  let column = {
    id: `${String(id2)}`,
    accessorFn,
    parent,
    depth,
    columnDef: resolvedColumnDef,
    columns: [],
    getFlatColumns: memo(() => [true], () => {
      var _column$columns;
      return [column, ...(_column$columns = column.columns) == null ? void 0 : _column$columns.flatMap((d) => d.getFlatColumns())];
    }, getMemoOptions(table.options, "debugColumns", "column.getFlatColumns")),
    getLeafColumns: memo(() => [table._getOrderColumnsFn()], (orderColumns2) => {
      var _column$columns2;
      if ((_column$columns2 = column.columns) != null && _column$columns2.length) {
        let leafColumns = column.columns.flatMap((column2) => column2.getLeafColumns());
        return orderColumns2(leafColumns);
      }
      return [column];
    }, getMemoOptions(table.options, "debugColumns", "column.getLeafColumns"))
  };
  for (const feature of table._features) {
    feature.createColumn == null || feature.createColumn(column, table);
  }
  return column;
}
var debug = "debugHeaders";
function createHeader(table, column, options) {
  var _options$id;
  const id2 = (_options$id = options.id) != null ? _options$id : column.id;
  let header = {
    id: id2,
    column,
    index: options.index,
    isPlaceholder: !!options.isPlaceholder,
    placeholderId: options.placeholderId,
    depth: options.depth,
    subHeaders: [],
    colSpan: 0,
    rowSpan: 0,
    headerGroup: null,
    getLeafHeaders: () => {
      const leafHeaders = [];
      const recurseHeader = (h) => {
        if (h.subHeaders && h.subHeaders.length) {
          h.subHeaders.map(recurseHeader);
        }
        leafHeaders.push(h);
      };
      recurseHeader(header);
      return leafHeaders;
    },
    getContext: () => ({
      table,
      header,
      column
    })
  };
  table._features.forEach((feature) => {
    feature.createHeader == null || feature.createHeader(header, table);
  });
  return header;
}
var Headers = {
  createTable: (table) => {
    table.getHeaderGroups = memo(() => [table.getAllColumns(), table.getVisibleLeafColumns(), table.getState().columnPinning.left, table.getState().columnPinning.right], (allColumns, leafColumns, left, right) => {
      var _left$map$filter, _right$map$filter;
      const leftColumns = (_left$map$filter = left == null ? void 0 : left.map((columnId) => leafColumns.find((d) => d.id === columnId)).filter(Boolean)) != null ? _left$map$filter : [];
      const rightColumns = (_right$map$filter = right == null ? void 0 : right.map((columnId) => leafColumns.find((d) => d.id === columnId)).filter(Boolean)) != null ? _right$map$filter : [];
      const centerColumns = leafColumns.filter((column) => !(left != null && left.includes(column.id)) && !(right != null && right.includes(column.id)));
      const headerGroups = buildHeaderGroups(allColumns, [...leftColumns, ...centerColumns, ...rightColumns], table);
      return headerGroups;
    }, getMemoOptions(table.options, debug, "getHeaderGroups"));
    table.getCenterHeaderGroups = memo(() => [table.getAllColumns(), table.getVisibleLeafColumns(), table.getState().columnPinning.left, table.getState().columnPinning.right], (allColumns, leafColumns, left, right) => {
      leafColumns = leafColumns.filter((column) => !(left != null && left.includes(column.id)) && !(right != null && right.includes(column.id)));
      return buildHeaderGroups(allColumns, leafColumns, table, "center");
    }, getMemoOptions(table.options, debug, "getCenterHeaderGroups"));
    table.getLeftHeaderGroups = memo(() => [table.getAllColumns(), table.getVisibleLeafColumns(), table.getState().columnPinning.left], (allColumns, leafColumns, left) => {
      var _left$map$filter2;
      const orderedLeafColumns = (_left$map$filter2 = left == null ? void 0 : left.map((columnId) => leafColumns.find((d) => d.id === columnId)).filter(Boolean)) != null ? _left$map$filter2 : [];
      return buildHeaderGroups(allColumns, orderedLeafColumns, table, "left");
    }, getMemoOptions(table.options, debug, "getLeftHeaderGroups"));
    table.getRightHeaderGroups = memo(() => [table.getAllColumns(), table.getVisibleLeafColumns(), table.getState().columnPinning.right], (allColumns, leafColumns, right) => {
      var _right$map$filter2;
      const orderedLeafColumns = (_right$map$filter2 = right == null ? void 0 : right.map((columnId) => leafColumns.find((d) => d.id === columnId)).filter(Boolean)) != null ? _right$map$filter2 : [];
      return buildHeaderGroups(allColumns, orderedLeafColumns, table, "right");
    }, getMemoOptions(table.options, debug, "getRightHeaderGroups"));
    table.getFooterGroups = memo(() => [table.getHeaderGroups()], (headerGroups) => {
      return [...headerGroups].reverse();
    }, getMemoOptions(table.options, debug, "getFooterGroups"));
    table.getLeftFooterGroups = memo(() => [table.getLeftHeaderGroups()], (headerGroups) => {
      return [...headerGroups].reverse();
    }, getMemoOptions(table.options, debug, "getLeftFooterGroups"));
    table.getCenterFooterGroups = memo(() => [table.getCenterHeaderGroups()], (headerGroups) => {
      return [...headerGroups].reverse();
    }, getMemoOptions(table.options, debug, "getCenterFooterGroups"));
    table.getRightFooterGroups = memo(() => [table.getRightHeaderGroups()], (headerGroups) => {
      return [...headerGroups].reverse();
    }, getMemoOptions(table.options, debug, "getRightFooterGroups"));
    table.getFlatHeaders = memo(() => [table.getHeaderGroups()], (headerGroups) => {
      return headerGroups.map((headerGroup) => {
        return headerGroup.headers;
      }).flat();
    }, getMemoOptions(table.options, debug, "getFlatHeaders"));
    table.getLeftFlatHeaders = memo(() => [table.getLeftHeaderGroups()], (left) => {
      return left.map((headerGroup) => {
        return headerGroup.headers;
      }).flat();
    }, getMemoOptions(table.options, debug, "getLeftFlatHeaders"));
    table.getCenterFlatHeaders = memo(() => [table.getCenterHeaderGroups()], (left) => {
      return left.map((headerGroup) => {
        return headerGroup.headers;
      }).flat();
    }, getMemoOptions(table.options, debug, "getCenterFlatHeaders"));
    table.getRightFlatHeaders = memo(() => [table.getRightHeaderGroups()], (left) => {
      return left.map((headerGroup) => {
        return headerGroup.headers;
      }).flat();
    }, getMemoOptions(table.options, debug, "getRightFlatHeaders"));
    table.getCenterLeafHeaders = memo(() => [table.getCenterFlatHeaders()], (flatHeaders) => {
      return flatHeaders.filter((header) => {
        var _header$subHeaders;
        return !((_header$subHeaders = header.subHeaders) != null && _header$subHeaders.length);
      });
    }, getMemoOptions(table.options, debug, "getCenterLeafHeaders"));
    table.getLeftLeafHeaders = memo(() => [table.getLeftFlatHeaders()], (flatHeaders) => {
      return flatHeaders.filter((header) => {
        var _header$subHeaders2;
        return !((_header$subHeaders2 = header.subHeaders) != null && _header$subHeaders2.length);
      });
    }, getMemoOptions(table.options, debug, "getLeftLeafHeaders"));
    table.getRightLeafHeaders = memo(() => [table.getRightFlatHeaders()], (flatHeaders) => {
      return flatHeaders.filter((header) => {
        var _header$subHeaders3;
        return !((_header$subHeaders3 = header.subHeaders) != null && _header$subHeaders3.length);
      });
    }, getMemoOptions(table.options, debug, "getRightLeafHeaders"));
    table.getLeafHeaders = memo(() => [table.getLeftHeaderGroups(), table.getCenterHeaderGroups(), table.getRightHeaderGroups()], (left, center, right) => {
      var _left$0$headers, _left$, _center$0$headers, _center$, _right$0$headers, _right$;
      return [...(_left$0$headers = (_left$ = left[0]) == null ? void 0 : _left$.headers) != null ? _left$0$headers : [], ...(_center$0$headers = (_center$ = center[0]) == null ? void 0 : _center$.headers) != null ? _center$0$headers : [], ...(_right$0$headers = (_right$ = right[0]) == null ? void 0 : _right$.headers) != null ? _right$0$headers : []].map((header) => {
        return header.getLeafHeaders();
      }).flat();
    }, getMemoOptions(table.options, debug, "getLeafHeaders"));
  }
};
function buildHeaderGroups(allColumns, columnsToGroup, table, headerFamily) {
  var _headerGroups$0$heade, _headerGroups$;
  let maxDepth = 0;
  const findMaxDepth = function(columns, depth) {
    if (depth === void 0) {
      depth = 1;
    }
    maxDepth = Math.max(maxDepth, depth);
    columns.filter((column) => column.getIsVisible()).forEach((column) => {
      var _column$columns;
      if ((_column$columns = column.columns) != null && _column$columns.length) {
        findMaxDepth(column.columns, depth + 1);
      }
    }, 0);
  };
  findMaxDepth(allColumns);
  let headerGroups = [];
  const createHeaderGroup = (headersToGroup, depth) => {
    const headerGroup = {
      depth,
      id: [headerFamily, `${depth}`].filter(Boolean).join("_"),
      headers: []
    };
    const pendingParentHeaders = [];
    headersToGroup.forEach((headerToGroup) => {
      const latestPendingParentHeader = [...pendingParentHeaders].reverse()[0];
      const isLeafHeader = headerToGroup.column.depth === headerGroup.depth;
      let column;
      let isPlaceholder = false;
      if (isLeafHeader && headerToGroup.column.parent) {
        column = headerToGroup.column.parent;
      } else {
        column = headerToGroup.column;
        isPlaceholder = true;
      }
      if (latestPendingParentHeader && (latestPendingParentHeader == null ? void 0 : latestPendingParentHeader.column) === column) {
        latestPendingParentHeader.subHeaders.push(headerToGroup);
      } else {
        const header = createHeader(table, column, {
          id: [headerFamily, depth, column.id, headerToGroup == null ? void 0 : headerToGroup.id].filter(Boolean).join("_"),
          isPlaceholder,
          placeholderId: isPlaceholder ? `${pendingParentHeaders.filter((d) => d.column === column).length}` : void 0,
          depth,
          index: pendingParentHeaders.length
        });
        header.subHeaders.push(headerToGroup);
        pendingParentHeaders.push(header);
      }
      headerGroup.headers.push(headerToGroup);
      headerToGroup.headerGroup = headerGroup;
    });
    headerGroups.push(headerGroup);
    if (depth > 0) {
      createHeaderGroup(pendingParentHeaders, depth - 1);
    }
  };
  const bottomHeaders = columnsToGroup.map((column, index) => createHeader(table, column, {
    depth: maxDepth,
    index
  }));
  createHeaderGroup(bottomHeaders, maxDepth - 1);
  headerGroups.reverse();
  const recurseHeadersForSpans = (headers) => {
    const filteredHeaders = headers.filter((header) => header.column.getIsVisible());
    return filteredHeaders.map((header) => {
      let colSpan = 0;
      let rowSpan = 0;
      let childRowSpans = [0];
      if (header.subHeaders && header.subHeaders.length) {
        childRowSpans = [];
        recurseHeadersForSpans(header.subHeaders).forEach((_ref) => {
          let {
            colSpan: childColSpan,
            rowSpan: childRowSpan
          } = _ref;
          colSpan += childColSpan;
          childRowSpans.push(childRowSpan);
        });
      } else {
        colSpan = 1;
      }
      const minChildRowSpan = Math.min(...childRowSpans);
      rowSpan = rowSpan + minChildRowSpan;
      header.colSpan = colSpan;
      header.rowSpan = rowSpan;
      return {
        colSpan,
        rowSpan
      };
    });
  };
  recurseHeadersForSpans((_headerGroups$0$heade = (_headerGroups$ = headerGroups[0]) == null ? void 0 : _headerGroups$.headers) != null ? _headerGroups$0$heade : []);
  return headerGroups;
}
var createRow = (table, id2, original, rowIndex, depth, subRows, parentId) => {
  let row = {
    id: id2,
    index: rowIndex,
    original,
    depth,
    parentId,
    _valuesCache: {},
    _uniqueValuesCache: {},
    getValue: (columnId) => {
      if (row._valuesCache.hasOwnProperty(columnId)) {
        return row._valuesCache[columnId];
      }
      const column = table.getColumn(columnId);
      if (!(column != null && column.accessorFn)) {
        return void 0;
      }
      row._valuesCache[columnId] = column.accessorFn(row.original, rowIndex);
      return row._valuesCache[columnId];
    },
    getUniqueValues: (columnId) => {
      if (row._uniqueValuesCache.hasOwnProperty(columnId)) {
        return row._uniqueValuesCache[columnId];
      }
      const column = table.getColumn(columnId);
      if (!(column != null && column.accessorFn)) {
        return void 0;
      }
      if (!column.columnDef.getUniqueValues) {
        row._uniqueValuesCache[columnId] = [row.getValue(columnId)];
        return row._uniqueValuesCache[columnId];
      }
      row._uniqueValuesCache[columnId] = column.columnDef.getUniqueValues(row.original, rowIndex);
      return row._uniqueValuesCache[columnId];
    },
    renderValue: (columnId) => {
      var _row$getValue;
      return (_row$getValue = row.getValue(columnId)) != null ? _row$getValue : table.options.renderFallbackValue;
    },
    subRows: subRows != null ? subRows : [],
    getLeafRows: () => flattenBy(row.subRows, (d) => d.subRows),
    getParentRow: () => row.parentId ? table.getRow(row.parentId, true) : void 0,
    getParentRows: () => {
      let parentRows = [];
      let currentRow = row;
      while (true) {
        const parentRow = currentRow.getParentRow();
        if (!parentRow) break;
        parentRows.push(parentRow);
        currentRow = parentRow;
      }
      return parentRows.reverse();
    },
    getAllCells: memo(() => [table.getAllLeafColumns()], (leafColumns) => {
      return leafColumns.map((column) => {
        return createCell(table, row, column, column.id);
      });
    }, getMemoOptions(table.options, "debugRows", "getAllCells")),
    _getAllCellsByColumnId: memo(() => [row.getAllCells()], (allCells) => {
      return allCells.reduce((acc, cell) => {
        acc[cell.column.id] = cell;
        return acc;
      }, {});
    }, getMemoOptions(table.options, "debugRows", "getAllCellsByColumnId"))
  };
  for (let i = 0; i < table._features.length; i++) {
    const feature = table._features[i];
    feature == null || feature.createRow == null || feature.createRow(row, table);
  }
  return row;
};
var ColumnFaceting = {
  createColumn: (column, table) => {
    column._getFacetedRowModel = table.options.getFacetedRowModel && table.options.getFacetedRowModel(table, column.id);
    column.getFacetedRowModel = () => {
      if (!column._getFacetedRowModel) {
        return table.getPreFilteredRowModel();
      }
      return column._getFacetedRowModel();
    };
    column._getFacetedUniqueValues = table.options.getFacetedUniqueValues && table.options.getFacetedUniqueValues(table, column.id);
    column.getFacetedUniqueValues = () => {
      if (!column._getFacetedUniqueValues) {
        return /* @__PURE__ */ new Map();
      }
      return column._getFacetedUniqueValues();
    };
    column._getFacetedMinMaxValues = table.options.getFacetedMinMaxValues && table.options.getFacetedMinMaxValues(table, column.id);
    column.getFacetedMinMaxValues = () => {
      if (!column._getFacetedMinMaxValues) {
        return void 0;
      }
      return column._getFacetedMinMaxValues();
    };
  }
};
var includesString = (row, columnId, filterValue) => {
  var _filterValue$toString, _row$getValue;
  const search = filterValue == null || (_filterValue$toString = filterValue.toString()) == null ? void 0 : _filterValue$toString.toLowerCase();
  return Boolean((_row$getValue = row.getValue(columnId)) == null || (_row$getValue = _row$getValue.toString()) == null || (_row$getValue = _row$getValue.toLowerCase()) == null ? void 0 : _row$getValue.includes(search));
};
includesString.autoRemove = (val) => testFalsey(val);
var includesStringSensitive = (row, columnId, filterValue) => {
  var _row$getValue2;
  return Boolean((_row$getValue2 = row.getValue(columnId)) == null || (_row$getValue2 = _row$getValue2.toString()) == null ? void 0 : _row$getValue2.includes(filterValue));
};
includesStringSensitive.autoRemove = (val) => testFalsey(val);
var equalsString = (row, columnId, filterValue) => {
  var _row$getValue3;
  return ((_row$getValue3 = row.getValue(columnId)) == null || (_row$getValue3 = _row$getValue3.toString()) == null ? void 0 : _row$getValue3.toLowerCase()) === (filterValue == null ? void 0 : filterValue.toLowerCase());
};
equalsString.autoRemove = (val) => testFalsey(val);
var arrIncludes = (row, columnId, filterValue) => {
  var _row$getValue4;
  return (_row$getValue4 = row.getValue(columnId)) == null ? void 0 : _row$getValue4.includes(filterValue);
};
arrIncludes.autoRemove = (val) => testFalsey(val);
var arrIncludesAll = (row, columnId, filterValue) => {
  return !filterValue.some((val) => {
    var _row$getValue5;
    return !((_row$getValue5 = row.getValue(columnId)) != null && _row$getValue5.includes(val));
  });
};
arrIncludesAll.autoRemove = (val) => testFalsey(val) || !(val != null && val.length);
var arrIncludesSome = (row, columnId, filterValue) => {
  return filterValue.some((val) => {
    var _row$getValue6;
    return (_row$getValue6 = row.getValue(columnId)) == null ? void 0 : _row$getValue6.includes(val);
  });
};
arrIncludesSome.autoRemove = (val) => testFalsey(val) || !(val != null && val.length);
var equals = (row, columnId, filterValue) => {
  return row.getValue(columnId) === filterValue;
};
equals.autoRemove = (val) => testFalsey(val);
var weakEquals = (row, columnId, filterValue) => {
  return row.getValue(columnId) == filterValue;
};
weakEquals.autoRemove = (val) => testFalsey(val);
var inNumberRange = (row, columnId, filterValue) => {
  let [min2, max2] = filterValue;
  const rowValue = row.getValue(columnId);
  return rowValue >= min2 && rowValue <= max2;
};
inNumberRange.resolveFilterValue = (val) => {
  let [unsafeMin, unsafeMax] = val;
  let parsedMin = typeof unsafeMin !== "number" ? parseFloat(unsafeMin) : unsafeMin;
  let parsedMax = typeof unsafeMax !== "number" ? parseFloat(unsafeMax) : unsafeMax;
  let min2 = unsafeMin === null || Number.isNaN(parsedMin) ? -Infinity : parsedMin;
  let max2 = unsafeMax === null || Number.isNaN(parsedMax) ? Infinity : parsedMax;
  if (min2 > max2) {
    const temp = min2;
    min2 = max2;
    max2 = temp;
  }
  return [min2, max2];
};
inNumberRange.autoRemove = (val) => testFalsey(val) || testFalsey(val[0]) && testFalsey(val[1]);
var filterFns = {
  includesString,
  includesStringSensitive,
  equalsString,
  arrIncludes,
  arrIncludesAll,
  arrIncludesSome,
  equals,
  weakEquals,
  inNumberRange
};
function testFalsey(val) {
  return val === void 0 || val === null || val === "";
}
var ColumnFiltering = {
  getDefaultColumnDef: () => {
    return {
      filterFn: "auto"
    };
  },
  getInitialState: (state2) => {
    return {
      columnFilters: [],
      ...state2
    };
  },
  getDefaultOptions: (table) => {
    return {
      onColumnFiltersChange: makeStateUpdater("columnFilters", table),
      filterFromLeafRows: false,
      maxLeafRowFilterDepth: 100
    };
  },
  createColumn: (column, table) => {
    column.getAutoFilterFn = () => {
      const firstRow = table.getCoreRowModel().flatRows[0];
      const value = firstRow == null ? void 0 : firstRow.getValue(column.id);
      if (typeof value === "string") {
        return filterFns.includesString;
      }
      if (typeof value === "number") {
        return filterFns.inNumberRange;
      }
      if (typeof value === "boolean") {
        return filterFns.equals;
      }
      if (value !== null && typeof value === "object") {
        return filterFns.equals;
      }
      if (Array.isArray(value)) {
        return filterFns.arrIncludes;
      }
      return filterFns.weakEquals;
    };
    column.getFilterFn = () => {
      var _table$options$filter, _table$options$filter2;
      return isFunction(column.columnDef.filterFn) ? column.columnDef.filterFn : column.columnDef.filterFn === "auto" ? column.getAutoFilterFn() : (
        // @ts-ignore
        (_table$options$filter = (_table$options$filter2 = table.options.filterFns) == null ? void 0 : _table$options$filter2[column.columnDef.filterFn]) != null ? _table$options$filter : filterFns[column.columnDef.filterFn]
      );
    };
    column.getCanFilter = () => {
      var _column$columnDef$ena, _table$options$enable, _table$options$enable2;
      return ((_column$columnDef$ena = column.columnDef.enableColumnFilter) != null ? _column$columnDef$ena : true) && ((_table$options$enable = table.options.enableColumnFilters) != null ? _table$options$enable : true) && ((_table$options$enable2 = table.options.enableFilters) != null ? _table$options$enable2 : true) && !!column.accessorFn;
    };
    column.getIsFiltered = () => column.getFilterIndex() > -1;
    column.getFilterValue = () => {
      var _table$getState$colum;
      return (_table$getState$colum = table.getState().columnFilters) == null || (_table$getState$colum = _table$getState$colum.find((d) => d.id === column.id)) == null ? void 0 : _table$getState$colum.value;
    };
    column.getFilterIndex = () => {
      var _table$getState$colum2, _table$getState$colum3;
      return (_table$getState$colum2 = (_table$getState$colum3 = table.getState().columnFilters) == null ? void 0 : _table$getState$colum3.findIndex((d) => d.id === column.id)) != null ? _table$getState$colum2 : -1;
    };
    column.setFilterValue = (value) => {
      table.setColumnFilters((old) => {
        const filterFn = column.getFilterFn();
        const previousFilter = old == null ? void 0 : old.find((d) => d.id === column.id);
        const newFilter = functionalUpdate(value, previousFilter ? previousFilter.value : void 0);
        if (shouldAutoRemoveFilter(filterFn, newFilter, column)) {
          var _old$filter;
          return (_old$filter = old == null ? void 0 : old.filter((d) => d.id !== column.id)) != null ? _old$filter : [];
        }
        const newFilterObj = {
          id: column.id,
          value: newFilter
        };
        if (previousFilter) {
          var _old$map;
          return (_old$map = old == null ? void 0 : old.map((d) => {
            if (d.id === column.id) {
              return newFilterObj;
            }
            return d;
          })) != null ? _old$map : [];
        }
        if (old != null && old.length) {
          return [...old, newFilterObj];
        }
        return [newFilterObj];
      });
    };
  },
  createRow: (row, _table) => {
    row.columnFilters = {};
    row.columnFiltersMeta = {};
  },
  createTable: (table) => {
    table.setColumnFilters = (updater) => {
      const leafColumns = table.getAllLeafColumns();
      const updateFn = (old) => {
        var _functionalUpdate;
        return (_functionalUpdate = functionalUpdate(updater, old)) == null ? void 0 : _functionalUpdate.filter((filter2) => {
          const column = leafColumns.find((d) => d.id === filter2.id);
          if (column) {
            const filterFn = column.getFilterFn();
            if (shouldAutoRemoveFilter(filterFn, filter2.value, column)) {
              return false;
            }
          }
          return true;
        });
      };
      table.options.onColumnFiltersChange == null || table.options.onColumnFiltersChange(updateFn);
    };
    table.resetColumnFilters = (defaultState) => {
      var _table$initialState$c, _table$initialState;
      table.setColumnFilters(defaultState ? [] : (_table$initialState$c = (_table$initialState = table.initialState) == null ? void 0 : _table$initialState.columnFilters) != null ? _table$initialState$c : []);
    };
    table.getPreFilteredRowModel = () => table.getCoreRowModel();
    table.getFilteredRowModel = () => {
      if (!table._getFilteredRowModel && table.options.getFilteredRowModel) {
        table._getFilteredRowModel = table.options.getFilteredRowModel(table);
      }
      if (table.options.manualFiltering || !table._getFilteredRowModel) {
        return table.getPreFilteredRowModel();
      }
      return table._getFilteredRowModel();
    };
  }
};
function shouldAutoRemoveFilter(filterFn, value, column) {
  return (filterFn && filterFn.autoRemove ? filterFn.autoRemove(value, column) : false) || typeof value === "undefined" || typeof value === "string" && !value;
}
var sum = (columnId, _leafRows, childRows) => {
  return childRows.reduce((sum2, next) => {
    const nextValue = next.getValue(columnId);
    return sum2 + (typeof nextValue === "number" ? nextValue : 0);
  }, 0);
};
var min = (columnId, _leafRows, childRows) => {
  let min2;
  childRows.forEach((row) => {
    const value = row.getValue(columnId);
    if (value != null && (min2 > value || min2 === void 0 && value >= value)) {
      min2 = value;
    }
  });
  return min2;
};
var max = (columnId, _leafRows, childRows) => {
  let max2;
  childRows.forEach((row) => {
    const value = row.getValue(columnId);
    if (value != null && (max2 < value || max2 === void 0 && value >= value)) {
      max2 = value;
    }
  });
  return max2;
};
var extent = (columnId, _leafRows, childRows) => {
  let min2;
  let max2;
  childRows.forEach((row) => {
    const value = row.getValue(columnId);
    if (value != null) {
      if (min2 === void 0) {
        if (value >= value) min2 = max2 = value;
      } else {
        if (min2 > value) min2 = value;
        if (max2 < value) max2 = value;
      }
    }
  });
  return [min2, max2];
};
var mean = (columnId, leafRows) => {
  let count2 = 0;
  let sum2 = 0;
  leafRows.forEach((row) => {
    let value = row.getValue(columnId);
    if (value != null && (value = +value) >= value) {
      ++count2, sum2 += value;
    }
  });
  if (count2) return sum2 / count2;
  return;
};
var median = (columnId, leafRows) => {
  if (!leafRows.length) {
    return;
  }
  const values = leafRows.map((row) => row.getValue(columnId));
  if (!isNumberArray(values)) {
    return;
  }
  if (values.length === 1) {
    return values[0];
  }
  const mid = Math.floor(values.length / 2);
  const nums = values.sort((a, b) => a - b);
  return values.length % 2 !== 0 ? nums[mid] : (nums[mid - 1] + nums[mid]) / 2;
};
var unique = (columnId, leafRows) => {
  return Array.from(new Set(leafRows.map((d) => d.getValue(columnId))).values());
};
var uniqueCount = (columnId, leafRows) => {
  return new Set(leafRows.map((d) => d.getValue(columnId))).size;
};
var count = (_columnId, leafRows) => {
  return leafRows.length;
};
var aggregationFns = {
  sum,
  min,
  max,
  extent,
  mean,
  median,
  unique,
  uniqueCount,
  count
};
var ColumnGrouping = {
  getDefaultColumnDef: () => {
    return {
      aggregatedCell: (props) => {
        var _toString, _props$getValue;
        return (_toString = (_props$getValue = props.getValue()) == null || _props$getValue.toString == null ? void 0 : _props$getValue.toString()) != null ? _toString : null;
      },
      aggregationFn: "auto"
    };
  },
  getInitialState: (state2) => {
    return {
      grouping: [],
      ...state2
    };
  },
  getDefaultOptions: (table) => {
    return {
      onGroupingChange: makeStateUpdater("grouping", table),
      groupedColumnMode: "reorder"
    };
  },
  createColumn: (column, table) => {
    column.toggleGrouping = () => {
      table.setGrouping((old) => {
        if (old != null && old.includes(column.id)) {
          return old.filter((d) => d !== column.id);
        }
        return [...old != null ? old : [], column.id];
      });
    };
    column.getCanGroup = () => {
      var _column$columnDef$ena, _table$options$enable;
      return ((_column$columnDef$ena = column.columnDef.enableGrouping) != null ? _column$columnDef$ena : true) && ((_table$options$enable = table.options.enableGrouping) != null ? _table$options$enable : true) && (!!column.accessorFn || !!column.columnDef.getGroupingValue);
    };
    column.getIsGrouped = () => {
      var _table$getState$group;
      return (_table$getState$group = table.getState().grouping) == null ? void 0 : _table$getState$group.includes(column.id);
    };
    column.getGroupedIndex = () => {
      var _table$getState$group2;
      return (_table$getState$group2 = table.getState().grouping) == null ? void 0 : _table$getState$group2.indexOf(column.id);
    };
    column.getToggleGroupingHandler = () => {
      const canGroup = column.getCanGroup();
      return () => {
        if (!canGroup) return;
        column.toggleGrouping();
      };
    };
    column.getAutoAggregationFn = () => {
      const firstRow = table.getCoreRowModel().flatRows[0];
      const value = firstRow == null ? void 0 : firstRow.getValue(column.id);
      if (typeof value === "number") {
        return aggregationFns.sum;
      }
      if (Object.prototype.toString.call(value) === "[object Date]") {
        return aggregationFns.extent;
      }
    };
    column.getAggregationFn = () => {
      var _table$options$aggreg, _table$options$aggreg2;
      if (!column) {
        throw new Error();
      }
      return isFunction(column.columnDef.aggregationFn) ? column.columnDef.aggregationFn : column.columnDef.aggregationFn === "auto" ? column.getAutoAggregationFn() : (_table$options$aggreg = (_table$options$aggreg2 = table.options.aggregationFns) == null ? void 0 : _table$options$aggreg2[column.columnDef.aggregationFn]) != null ? _table$options$aggreg : aggregationFns[column.columnDef.aggregationFn];
    };
  },
  createTable: (table) => {
    table.setGrouping = (updater) => table.options.onGroupingChange == null ? void 0 : table.options.onGroupingChange(updater);
    table.resetGrouping = (defaultState) => {
      var _table$initialState$g, _table$initialState;
      table.setGrouping(defaultState ? [] : (_table$initialState$g = (_table$initialState = table.initialState) == null ? void 0 : _table$initialState.grouping) != null ? _table$initialState$g : []);
    };
    table.getPreGroupedRowModel = () => table.getFilteredRowModel();
    table.getGroupedRowModel = () => {
      if (!table._getGroupedRowModel && table.options.getGroupedRowModel) {
        table._getGroupedRowModel = table.options.getGroupedRowModel(table);
      }
      if (table.options.manualGrouping || !table._getGroupedRowModel) {
        return table.getPreGroupedRowModel();
      }
      return table._getGroupedRowModel();
    };
  },
  createRow: (row, table) => {
    row.getIsGrouped = () => !!row.groupingColumnId;
    row.getGroupingValue = (columnId) => {
      if (row._groupingValuesCache.hasOwnProperty(columnId)) {
        return row._groupingValuesCache[columnId];
      }
      const column = table.getColumn(columnId);
      if (!(column != null && column.columnDef.getGroupingValue)) {
        return row.getValue(columnId);
      }
      row._groupingValuesCache[columnId] = column.columnDef.getGroupingValue(row.original);
      return row._groupingValuesCache[columnId];
    };
    row._groupingValuesCache = {};
  },
  createCell: (cell, column, row, table) => {
    cell.getIsGrouped = () => column.getIsGrouped() && column.id === row.groupingColumnId;
    cell.getIsPlaceholder = () => !cell.getIsGrouped() && column.getIsGrouped();
    cell.getIsAggregated = () => {
      var _row$subRows;
      return !cell.getIsGrouped() && !cell.getIsPlaceholder() && !!((_row$subRows = row.subRows) != null && _row$subRows.length);
    };
  }
};
function orderColumns(leafColumns, grouping, groupedColumnMode) {
  if (!(grouping != null && grouping.length) || !groupedColumnMode) {
    return leafColumns;
  }
  const nonGroupingColumns = leafColumns.filter((col) => !grouping.includes(col.id));
  if (groupedColumnMode === "remove") {
    return nonGroupingColumns;
  }
  const groupingColumns = grouping.map((g) => leafColumns.find((col) => col.id === g)).filter(Boolean);
  return [...groupingColumns, ...nonGroupingColumns];
}
var ColumnOrdering = {
  getInitialState: (state2) => {
    return {
      columnOrder: [],
      ...state2
    };
  },
  getDefaultOptions: (table) => {
    return {
      onColumnOrderChange: makeStateUpdater("columnOrder", table)
    };
  },
  createColumn: (column, table) => {
    column.getIndex = memo((position) => [_getVisibleLeafColumns(table, position)], (columns) => columns.findIndex((d) => d.id === column.id), getMemoOptions(table.options, "debugColumns", "getIndex"));
    column.getIsFirstColumn = (position) => {
      var _columns$;
      const columns = _getVisibleLeafColumns(table, position);
      return ((_columns$ = columns[0]) == null ? void 0 : _columns$.id) === column.id;
    };
    column.getIsLastColumn = (position) => {
      var _columns;
      const columns = _getVisibleLeafColumns(table, position);
      return ((_columns = columns[columns.length - 1]) == null ? void 0 : _columns.id) === column.id;
    };
  },
  createTable: (table) => {
    table.setColumnOrder = (updater) => table.options.onColumnOrderChange == null ? void 0 : table.options.onColumnOrderChange(updater);
    table.resetColumnOrder = (defaultState) => {
      var _table$initialState$c;
      table.setColumnOrder(defaultState ? [] : (_table$initialState$c = table.initialState.columnOrder) != null ? _table$initialState$c : []);
    };
    table._getOrderColumnsFn = memo(() => [table.getState().columnOrder, table.getState().grouping, table.options.groupedColumnMode], (columnOrder, grouping, groupedColumnMode) => (columns) => {
      let orderedColumns = [];
      if (!(columnOrder != null && columnOrder.length)) {
        orderedColumns = columns;
      } else {
        const columnOrderCopy = [...columnOrder];
        const columnsCopy = [...columns];
        while (columnsCopy.length && columnOrderCopy.length) {
          const targetColumnId = columnOrderCopy.shift();
          const foundIndex = columnsCopy.findIndex((d) => d.id === targetColumnId);
          if (foundIndex > -1) {
            orderedColumns.push(columnsCopy.splice(foundIndex, 1)[0]);
          }
        }
        orderedColumns = [...orderedColumns, ...columnsCopy];
      }
      return orderColumns(orderedColumns, grouping, groupedColumnMode);
    }, getMemoOptions(table.options, "debugTable", "_getOrderColumnsFn"));
  }
};
var getDefaultColumnPinningState = () => ({
  left: [],
  right: []
});
var ColumnPinning = {
  getInitialState: (state2) => {
    return {
      columnPinning: getDefaultColumnPinningState(),
      ...state2
    };
  },
  getDefaultOptions: (table) => {
    return {
      onColumnPinningChange: makeStateUpdater("columnPinning", table)
    };
  },
  createColumn: (column, table) => {
    column.pin = (position) => {
      const columnIds = column.getLeafColumns().map((d) => d.id).filter(Boolean);
      table.setColumnPinning((old) => {
        var _old$left3, _old$right3;
        if (position === "right") {
          var _old$left, _old$right;
          return {
            left: ((_old$left = old == null ? void 0 : old.left) != null ? _old$left : []).filter((d) => !(columnIds != null && columnIds.includes(d))),
            right: [...((_old$right = old == null ? void 0 : old.right) != null ? _old$right : []).filter((d) => !(columnIds != null && columnIds.includes(d))), ...columnIds]
          };
        }
        if (position === "left") {
          var _old$left2, _old$right2;
          return {
            left: [...((_old$left2 = old == null ? void 0 : old.left) != null ? _old$left2 : []).filter((d) => !(columnIds != null && columnIds.includes(d))), ...columnIds],
            right: ((_old$right2 = old == null ? void 0 : old.right) != null ? _old$right2 : []).filter((d) => !(columnIds != null && columnIds.includes(d)))
          };
        }
        return {
          left: ((_old$left3 = old == null ? void 0 : old.left) != null ? _old$left3 : []).filter((d) => !(columnIds != null && columnIds.includes(d))),
          right: ((_old$right3 = old == null ? void 0 : old.right) != null ? _old$right3 : []).filter((d) => !(columnIds != null && columnIds.includes(d)))
        };
      });
    };
    column.getCanPin = () => {
      const leafColumns = column.getLeafColumns();
      return leafColumns.some((d) => {
        var _d$columnDef$enablePi, _ref, _table$options$enable;
        return ((_d$columnDef$enablePi = d.columnDef.enablePinning) != null ? _d$columnDef$enablePi : true) && ((_ref = (_table$options$enable = table.options.enableColumnPinning) != null ? _table$options$enable : table.options.enablePinning) != null ? _ref : true);
      });
    };
    column.getIsPinned = () => {
      const leafColumnIds = column.getLeafColumns().map((d) => d.id);
      const {
        left,
        right
      } = table.getState().columnPinning;
      const isLeft = leafColumnIds.some((d) => left == null ? void 0 : left.includes(d));
      const isRight = leafColumnIds.some((d) => right == null ? void 0 : right.includes(d));
      return isLeft ? "left" : isRight ? "right" : false;
    };
    column.getPinnedIndex = () => {
      var _table$getState$colum, _table$getState$colum2;
      const position = column.getIsPinned();
      return position ? (_table$getState$colum = (_table$getState$colum2 = table.getState().columnPinning) == null || (_table$getState$colum2 = _table$getState$colum2[position]) == null ? void 0 : _table$getState$colum2.indexOf(column.id)) != null ? _table$getState$colum : -1 : 0;
    };
  },
  createRow: (row, table) => {
    row.getCenterVisibleCells = memo(() => [row._getAllVisibleCells(), table.getState().columnPinning.left, table.getState().columnPinning.right], (allCells, left, right) => {
      const leftAndRight = [...left != null ? left : [], ...right != null ? right : []];
      return allCells.filter((d) => !leftAndRight.includes(d.column.id));
    }, getMemoOptions(table.options, "debugRows", "getCenterVisibleCells"));
    row.getLeftVisibleCells = memo(() => [row._getAllVisibleCells(), table.getState().columnPinning.left], (allCells, left) => {
      const cells = (left != null ? left : []).map((columnId) => allCells.find((cell) => cell.column.id === columnId)).filter(Boolean).map((d) => ({
        ...d,
        position: "left"
      }));
      return cells;
    }, getMemoOptions(table.options, "debugRows", "getLeftVisibleCells"));
    row.getRightVisibleCells = memo(() => [row._getAllVisibleCells(), table.getState().columnPinning.right], (allCells, right) => {
      const cells = (right != null ? right : []).map((columnId) => allCells.find((cell) => cell.column.id === columnId)).filter(Boolean).map((d) => ({
        ...d,
        position: "right"
      }));
      return cells;
    }, getMemoOptions(table.options, "debugRows", "getRightVisibleCells"));
  },
  createTable: (table) => {
    table.setColumnPinning = (updater) => table.options.onColumnPinningChange == null ? void 0 : table.options.onColumnPinningChange(updater);
    table.resetColumnPinning = (defaultState) => {
      var _table$initialState$c, _table$initialState;
      return table.setColumnPinning(defaultState ? getDefaultColumnPinningState() : (_table$initialState$c = (_table$initialState = table.initialState) == null ? void 0 : _table$initialState.columnPinning) != null ? _table$initialState$c : getDefaultColumnPinningState());
    };
    table.getIsSomeColumnsPinned = (position) => {
      var _pinningState$positio;
      const pinningState = table.getState().columnPinning;
      if (!position) {
        var _pinningState$left, _pinningState$right;
        return Boolean(((_pinningState$left = pinningState.left) == null ? void 0 : _pinningState$left.length) || ((_pinningState$right = pinningState.right) == null ? void 0 : _pinningState$right.length));
      }
      return Boolean((_pinningState$positio = pinningState[position]) == null ? void 0 : _pinningState$positio.length);
    };
    table.getLeftLeafColumns = memo(() => [table.getAllLeafColumns(), table.getState().columnPinning.left], (allColumns, left) => {
      return (left != null ? left : []).map((columnId) => allColumns.find((column) => column.id === columnId)).filter(Boolean);
    }, getMemoOptions(table.options, "debugColumns", "getLeftLeafColumns"));
    table.getRightLeafColumns = memo(() => [table.getAllLeafColumns(), table.getState().columnPinning.right], (allColumns, right) => {
      return (right != null ? right : []).map((columnId) => allColumns.find((column) => column.id === columnId)).filter(Boolean);
    }, getMemoOptions(table.options, "debugColumns", "getRightLeafColumns"));
    table.getCenterLeafColumns = memo(() => [table.getAllLeafColumns(), table.getState().columnPinning.left, table.getState().columnPinning.right], (allColumns, left, right) => {
      const leftAndRight = [...left != null ? left : [], ...right != null ? right : []];
      return allColumns.filter((d) => !leftAndRight.includes(d.id));
    }, getMemoOptions(table.options, "debugColumns", "getCenterLeafColumns"));
  }
};
function safelyAccessDocument(_document) {
  return _document || (typeof document !== "undefined" ? document : null);
}
var defaultColumnSizing = {
  size: 150,
  minSize: 20,
  maxSize: Number.MAX_SAFE_INTEGER
};
var getDefaultColumnSizingInfoState = () => ({
  startOffset: null,
  startSize: null,
  deltaOffset: null,
  deltaPercentage: null,
  isResizingColumn: false,
  columnSizingStart: []
});
var ColumnSizing = {
  getDefaultColumnDef: () => {
    return defaultColumnSizing;
  },
  getInitialState: (state2) => {
    return {
      columnSizing: {},
      columnSizingInfo: getDefaultColumnSizingInfoState(),
      ...state2
    };
  },
  getDefaultOptions: (table) => {
    return {
      columnResizeMode: "onEnd",
      columnResizeDirection: "ltr",
      onColumnSizingChange: makeStateUpdater("columnSizing", table),
      onColumnSizingInfoChange: makeStateUpdater("columnSizingInfo", table)
    };
  },
  createColumn: (column, table) => {
    column.getSize = () => {
      var _column$columnDef$min, _ref, _column$columnDef$max;
      const columnSize = table.getState().columnSizing[column.id];
      return Math.min(Math.max((_column$columnDef$min = column.columnDef.minSize) != null ? _column$columnDef$min : defaultColumnSizing.minSize, (_ref = columnSize != null ? columnSize : column.columnDef.size) != null ? _ref : defaultColumnSizing.size), (_column$columnDef$max = column.columnDef.maxSize) != null ? _column$columnDef$max : defaultColumnSizing.maxSize);
    };
    column.getStart = memo((position) => [position, _getVisibleLeafColumns(table, position), table.getState().columnSizing], (position, columns) => columns.slice(0, column.getIndex(position)).reduce((sum2, column2) => sum2 + column2.getSize(), 0), getMemoOptions(table.options, "debugColumns", "getStart"));
    column.getAfter = memo((position) => [position, _getVisibleLeafColumns(table, position), table.getState().columnSizing], (position, columns) => columns.slice(column.getIndex(position) + 1).reduce((sum2, column2) => sum2 + column2.getSize(), 0), getMemoOptions(table.options, "debugColumns", "getAfter"));
    column.resetSize = () => {
      table.setColumnSizing((_ref2) => {
        let {
          [column.id]: _,
          ...rest
        } = _ref2;
        return rest;
      });
    };
    column.getCanResize = () => {
      var _column$columnDef$ena, _table$options$enable;
      return ((_column$columnDef$ena = column.columnDef.enableResizing) != null ? _column$columnDef$ena : true) && ((_table$options$enable = table.options.enableColumnResizing) != null ? _table$options$enable : true);
    };
    column.getIsResizing = () => {
      return table.getState().columnSizingInfo.isResizingColumn === column.id;
    };
  },
  createHeader: (header, table) => {
    header.getSize = () => {
      let sum2 = 0;
      const recurse = (header2) => {
        if (header2.subHeaders.length) {
          header2.subHeaders.forEach(recurse);
        } else {
          var _header$column$getSiz;
          sum2 += (_header$column$getSiz = header2.column.getSize()) != null ? _header$column$getSiz : 0;
        }
      };
      recurse(header);
      return sum2;
    };
    header.getStart = () => {
      if (header.index > 0) {
        const prevSiblingHeader = header.headerGroup.headers[header.index - 1];
        return prevSiblingHeader.getStart() + prevSiblingHeader.getSize();
      }
      return 0;
    };
    header.getResizeHandler = (_contextDocument) => {
      const column = table.getColumn(header.column.id);
      const canResize = column == null ? void 0 : column.getCanResize();
      return (e2) => {
        if (!column || !canResize) {
          return;
        }
        e2.persist == null || e2.persist();
        if (isTouchStartEvent(e2)) {
          if (e2.touches && e2.touches.length > 1) {
            return;
          }
        }
        const startSize = header.getSize();
        const columnSizingStart = header ? header.getLeafHeaders().map((d) => [d.column.id, d.column.getSize()]) : [[column.id, column.getSize()]];
        const clientX = isTouchStartEvent(e2) ? Math.round(e2.touches[0].clientX) : e2.clientX;
        const newColumnSizing = {};
        const updateOffset = (eventType, clientXPos) => {
          if (typeof clientXPos !== "number") {
            return;
          }
          table.setColumnSizingInfo((old) => {
            var _old$startOffset, _old$startSize;
            const deltaDirection = table.options.columnResizeDirection === "rtl" ? -1 : 1;
            const deltaOffset = (clientXPos - ((_old$startOffset = old == null ? void 0 : old.startOffset) != null ? _old$startOffset : 0)) * deltaDirection;
            const deltaPercentage = Math.max(deltaOffset / ((_old$startSize = old == null ? void 0 : old.startSize) != null ? _old$startSize : 0), -0.999999);
            old.columnSizingStart.forEach((_ref3) => {
              let [columnId, headerSize] = _ref3;
              newColumnSizing[columnId] = Math.round(Math.max(headerSize + headerSize * deltaPercentage, 0) * 100) / 100;
            });
            return {
              ...old,
              deltaOffset,
              deltaPercentage
            };
          });
          if (table.options.columnResizeMode === "onChange" || eventType === "end") {
            table.setColumnSizing((old) => ({
              ...old,
              ...newColumnSizing
            }));
          }
        };
        const onMove = (clientXPos) => updateOffset("move", clientXPos);
        const onEnd = (clientXPos) => {
          updateOffset("end", clientXPos);
          table.setColumnSizingInfo((old) => ({
            ...old,
            isResizingColumn: false,
            startOffset: null,
            startSize: null,
            deltaOffset: null,
            deltaPercentage: null,
            columnSizingStart: []
          }));
        };
        const contextDocument = safelyAccessDocument(_contextDocument);
        const mouseEvents = {
          moveHandler: (e3) => onMove(e3.clientX),
          upHandler: (e3) => {
            contextDocument == null || contextDocument.removeEventListener("mousemove", mouseEvents.moveHandler);
            contextDocument == null || contextDocument.removeEventListener("mouseup", mouseEvents.upHandler);
            onEnd(e3.clientX);
          }
        };
        const touchEvents = {
          moveHandler: (e3) => {
            if (e3.cancelable) {
              e3.preventDefault();
              e3.stopPropagation();
            }
            onMove(e3.touches[0].clientX);
            return false;
          },
          upHandler: (e3) => {
            var _e$touches$;
            contextDocument == null || contextDocument.removeEventListener("touchmove", touchEvents.moveHandler);
            contextDocument == null || contextDocument.removeEventListener("touchend", touchEvents.upHandler);
            if (e3.cancelable) {
              e3.preventDefault();
              e3.stopPropagation();
            }
            onEnd((_e$touches$ = e3.touches[0]) == null ? void 0 : _e$touches$.clientX);
          }
        };
        const passiveIfSupported = passiveEventSupported() ? {
          passive: false
        } : false;
        if (isTouchStartEvent(e2)) {
          contextDocument == null || contextDocument.addEventListener("touchmove", touchEvents.moveHandler, passiveIfSupported);
          contextDocument == null || contextDocument.addEventListener("touchend", touchEvents.upHandler, passiveIfSupported);
        } else {
          contextDocument == null || contextDocument.addEventListener("mousemove", mouseEvents.moveHandler, passiveIfSupported);
          contextDocument == null || contextDocument.addEventListener("mouseup", mouseEvents.upHandler, passiveIfSupported);
        }
        table.setColumnSizingInfo((old) => ({
          ...old,
          startOffset: clientX,
          startSize,
          deltaOffset: 0,
          deltaPercentage: 0,
          columnSizingStart,
          isResizingColumn: column.id
        }));
      };
    };
  },
  createTable: (table) => {
    table.setColumnSizing = (updater) => table.options.onColumnSizingChange == null ? void 0 : table.options.onColumnSizingChange(updater);
    table.setColumnSizingInfo = (updater) => table.options.onColumnSizingInfoChange == null ? void 0 : table.options.onColumnSizingInfoChange(updater);
    table.resetColumnSizing = (defaultState) => {
      var _table$initialState$c;
      table.setColumnSizing(defaultState ? {} : (_table$initialState$c = table.initialState.columnSizing) != null ? _table$initialState$c : {});
    };
    table.resetHeaderSizeInfo = (defaultState) => {
      var _table$initialState$c2;
      table.setColumnSizingInfo(defaultState ? getDefaultColumnSizingInfoState() : (_table$initialState$c2 = table.initialState.columnSizingInfo) != null ? _table$initialState$c2 : getDefaultColumnSizingInfoState());
    };
    table.getTotalSize = () => {
      var _table$getHeaderGroup, _table$getHeaderGroup2;
      return (_table$getHeaderGroup = (_table$getHeaderGroup2 = table.getHeaderGroups()[0]) == null ? void 0 : _table$getHeaderGroup2.headers.reduce((sum2, header) => {
        return sum2 + header.getSize();
      }, 0)) != null ? _table$getHeaderGroup : 0;
    };
    table.getLeftTotalSize = () => {
      var _table$getLeftHeaderG, _table$getLeftHeaderG2;
      return (_table$getLeftHeaderG = (_table$getLeftHeaderG2 = table.getLeftHeaderGroups()[0]) == null ? void 0 : _table$getLeftHeaderG2.headers.reduce((sum2, header) => {
        return sum2 + header.getSize();
      }, 0)) != null ? _table$getLeftHeaderG : 0;
    };
    table.getCenterTotalSize = () => {
      var _table$getCenterHeade, _table$getCenterHeade2;
      return (_table$getCenterHeade = (_table$getCenterHeade2 = table.getCenterHeaderGroups()[0]) == null ? void 0 : _table$getCenterHeade2.headers.reduce((sum2, header) => {
        return sum2 + header.getSize();
      }, 0)) != null ? _table$getCenterHeade : 0;
    };
    table.getRightTotalSize = () => {
      var _table$getRightHeader, _table$getRightHeader2;
      return (_table$getRightHeader = (_table$getRightHeader2 = table.getRightHeaderGroups()[0]) == null ? void 0 : _table$getRightHeader2.headers.reduce((sum2, header) => {
        return sum2 + header.getSize();
      }, 0)) != null ? _table$getRightHeader : 0;
    };
  }
};
var passiveSupported = null;
function passiveEventSupported() {
  if (typeof passiveSupported === "boolean") return passiveSupported;
  let supported = false;
  try {
    const options = {
      get passive() {
        supported = true;
        return false;
      }
    };
    const noop3 = () => {
    };
    window.addEventListener("test", noop3, options);
    window.removeEventListener("test", noop3);
  } catch (err) {
    supported = false;
  }
  passiveSupported = supported;
  return passiveSupported;
}
function isTouchStartEvent(e2) {
  return e2.type === "touchstart";
}
var ColumnVisibility = {
  getInitialState: (state2) => {
    return {
      columnVisibility: {},
      ...state2
    };
  },
  getDefaultOptions: (table) => {
    return {
      onColumnVisibilityChange: makeStateUpdater("columnVisibility", table)
    };
  },
  createColumn: (column, table) => {
    column.toggleVisibility = (value) => {
      if (column.getCanHide()) {
        table.setColumnVisibility((old) => ({
          ...old,
          [column.id]: value != null ? value : !column.getIsVisible()
        }));
      }
    };
    column.getIsVisible = () => {
      var _ref, _table$getState$colum;
      const childColumns = column.columns;
      return (_ref = childColumns.length ? childColumns.some((c2) => c2.getIsVisible()) : (_table$getState$colum = table.getState().columnVisibility) == null ? void 0 : _table$getState$colum[column.id]) != null ? _ref : true;
    };
    column.getCanHide = () => {
      var _column$columnDef$ena, _table$options$enable;
      return ((_column$columnDef$ena = column.columnDef.enableHiding) != null ? _column$columnDef$ena : true) && ((_table$options$enable = table.options.enableHiding) != null ? _table$options$enable : true);
    };
    column.getToggleVisibilityHandler = () => {
      return (e2) => {
        column.toggleVisibility == null || column.toggleVisibility(e2.target.checked);
      };
    };
  },
  createRow: (row, table) => {
    row._getAllVisibleCells = memo(() => [row.getAllCells(), table.getState().columnVisibility], (cells) => {
      return cells.filter((cell) => cell.column.getIsVisible());
    }, getMemoOptions(table.options, "debugRows", "_getAllVisibleCells"));
    row.getVisibleCells = memo(() => [row.getLeftVisibleCells(), row.getCenterVisibleCells(), row.getRightVisibleCells()], (left, center, right) => [...left, ...center, ...right], getMemoOptions(table.options, "debugRows", "getVisibleCells"));
  },
  createTable: (table) => {
    const makeVisibleColumnsMethod = (key, getColumns) => {
      return memo(() => [getColumns(), getColumns().filter((d) => d.getIsVisible()).map((d) => d.id).join("_")], (columns) => {
        return columns.filter((d) => d.getIsVisible == null ? void 0 : d.getIsVisible());
      }, getMemoOptions(table.options, "debugColumns", key));
    };
    table.getVisibleFlatColumns = makeVisibleColumnsMethod("getVisibleFlatColumns", () => table.getAllFlatColumns());
    table.getVisibleLeafColumns = makeVisibleColumnsMethod("getVisibleLeafColumns", () => table.getAllLeafColumns());
    table.getLeftVisibleLeafColumns = makeVisibleColumnsMethod("getLeftVisibleLeafColumns", () => table.getLeftLeafColumns());
    table.getRightVisibleLeafColumns = makeVisibleColumnsMethod("getRightVisibleLeafColumns", () => table.getRightLeafColumns());
    table.getCenterVisibleLeafColumns = makeVisibleColumnsMethod("getCenterVisibleLeafColumns", () => table.getCenterLeafColumns());
    table.setColumnVisibility = (updater) => table.options.onColumnVisibilityChange == null ? void 0 : table.options.onColumnVisibilityChange(updater);
    table.resetColumnVisibility = (defaultState) => {
      var _table$initialState$c;
      table.setColumnVisibility(defaultState ? {} : (_table$initialState$c = table.initialState.columnVisibility) != null ? _table$initialState$c : {});
    };
    table.toggleAllColumnsVisible = (value) => {
      var _value;
      value = (_value = value) != null ? _value : !table.getIsAllColumnsVisible();
      table.setColumnVisibility(table.getAllLeafColumns().reduce((obj, column) => ({
        ...obj,
        [column.id]: !value ? !(column.getCanHide != null && column.getCanHide()) : value
      }), {}));
    };
    table.getIsAllColumnsVisible = () => !table.getAllLeafColumns().some((column) => !(column.getIsVisible != null && column.getIsVisible()));
    table.getIsSomeColumnsVisible = () => table.getAllLeafColumns().some((column) => column.getIsVisible == null ? void 0 : column.getIsVisible());
    table.getToggleAllColumnsVisibilityHandler = () => {
      return (e2) => {
        var _target;
        table.toggleAllColumnsVisible((_target = e2.target) == null ? void 0 : _target.checked);
      };
    };
  }
};
function _getVisibleLeafColumns(table, position) {
  return !position ? table.getVisibleLeafColumns() : position === "center" ? table.getCenterVisibleLeafColumns() : position === "left" ? table.getLeftVisibleLeafColumns() : table.getRightVisibleLeafColumns();
}
var GlobalFaceting = {
  createTable: (table) => {
    table._getGlobalFacetedRowModel = table.options.getFacetedRowModel && table.options.getFacetedRowModel(table, "__global__");
    table.getGlobalFacetedRowModel = () => {
      if (table.options.manualFiltering || !table._getGlobalFacetedRowModel) {
        return table.getPreFilteredRowModel();
      }
      return table._getGlobalFacetedRowModel();
    };
    table._getGlobalFacetedUniqueValues = table.options.getFacetedUniqueValues && table.options.getFacetedUniqueValues(table, "__global__");
    table.getGlobalFacetedUniqueValues = () => {
      if (!table._getGlobalFacetedUniqueValues) {
        return /* @__PURE__ */ new Map();
      }
      return table._getGlobalFacetedUniqueValues();
    };
    table._getGlobalFacetedMinMaxValues = table.options.getFacetedMinMaxValues && table.options.getFacetedMinMaxValues(table, "__global__");
    table.getGlobalFacetedMinMaxValues = () => {
      if (!table._getGlobalFacetedMinMaxValues) {
        return;
      }
      return table._getGlobalFacetedMinMaxValues();
    };
  }
};
var GlobalFiltering = {
  getInitialState: (state2) => {
    return {
      globalFilter: void 0,
      ...state2
    };
  },
  getDefaultOptions: (table) => {
    return {
      onGlobalFilterChange: makeStateUpdater("globalFilter", table),
      globalFilterFn: "auto",
      getColumnCanGlobalFilter: (column) => {
        var _table$getCoreRowMode;
        const value = (_table$getCoreRowMode = table.getCoreRowModel().flatRows[0]) == null || (_table$getCoreRowMode = _table$getCoreRowMode._getAllCellsByColumnId()[column.id]) == null ? void 0 : _table$getCoreRowMode.getValue();
        return typeof value === "string" || typeof value === "number";
      }
    };
  },
  createColumn: (column, table) => {
    column.getCanGlobalFilter = () => {
      var _column$columnDef$ena, _table$options$enable, _table$options$enable2, _table$options$getCol;
      return ((_column$columnDef$ena = column.columnDef.enableGlobalFilter) != null ? _column$columnDef$ena : true) && ((_table$options$enable = table.options.enableGlobalFilter) != null ? _table$options$enable : true) && ((_table$options$enable2 = table.options.enableFilters) != null ? _table$options$enable2 : true) && ((_table$options$getCol = table.options.getColumnCanGlobalFilter == null ? void 0 : table.options.getColumnCanGlobalFilter(column)) != null ? _table$options$getCol : true) && !!column.accessorFn;
    };
  },
  createTable: (table) => {
    table.getGlobalAutoFilterFn = () => {
      return filterFns.includesString;
    };
    table.getGlobalFilterFn = () => {
      var _table$options$filter, _table$options$filter2;
      const {
        globalFilterFn
      } = table.options;
      return isFunction(globalFilterFn) ? globalFilterFn : globalFilterFn === "auto" ? table.getGlobalAutoFilterFn() : (_table$options$filter = (_table$options$filter2 = table.options.filterFns) == null ? void 0 : _table$options$filter2[globalFilterFn]) != null ? _table$options$filter : filterFns[globalFilterFn];
    };
    table.setGlobalFilter = (updater) => {
      table.options.onGlobalFilterChange == null || table.options.onGlobalFilterChange(updater);
    };
    table.resetGlobalFilter = (defaultState) => {
      table.setGlobalFilter(defaultState ? void 0 : table.initialState.globalFilter);
    };
  }
};
var RowExpanding = {
  getInitialState: (state2) => {
    return {
      expanded: {},
      ...state2
    };
  },
  getDefaultOptions: (table) => {
    return {
      onExpandedChange: makeStateUpdater("expanded", table),
      paginateExpandedRows: true
    };
  },
  createTable: (table) => {
    let registered = false;
    let queued = false;
    table._autoResetExpanded = () => {
      var _ref, _table$options$autoRe;
      if (!registered) {
        table._queue(() => {
          registered = true;
        });
        return;
      }
      if ((_ref = (_table$options$autoRe = table.options.autoResetAll) != null ? _table$options$autoRe : table.options.autoResetExpanded) != null ? _ref : !table.options.manualExpanding) {
        if (queued) return;
        queued = true;
        table._queue(() => {
          table.resetExpanded();
          queued = false;
        });
      }
    };
    table.setExpanded = (updater) => table.options.onExpandedChange == null ? void 0 : table.options.onExpandedChange(updater);
    table.toggleAllRowsExpanded = (expanded) => {
      if (expanded != null ? expanded : !table.getIsAllRowsExpanded()) {
        table.setExpanded(true);
      } else {
        table.setExpanded({});
      }
    };
    table.resetExpanded = (defaultState) => {
      var _table$initialState$e, _table$initialState;
      table.setExpanded(defaultState ? {} : (_table$initialState$e = (_table$initialState = table.initialState) == null ? void 0 : _table$initialState.expanded) != null ? _table$initialState$e : {});
    };
    table.getCanSomeRowsExpand = () => {
      return table.getPrePaginationRowModel().flatRows.some((row) => row.getCanExpand());
    };
    table.getToggleAllRowsExpandedHandler = () => {
      return (e2) => {
        e2.persist == null || e2.persist();
        table.toggleAllRowsExpanded();
      };
    };
    table.getIsSomeRowsExpanded = () => {
      const expanded = table.getState().expanded;
      return expanded === true || Object.values(expanded).some(Boolean);
    };
    table.getIsAllRowsExpanded = () => {
      const expanded = table.getState().expanded;
      if (typeof expanded === "boolean") {
        return expanded === true;
      }
      if (!Object.keys(expanded).length) {
        return false;
      }
      if (table.getRowModel().flatRows.some((row) => !row.getIsExpanded())) {
        return false;
      }
      return true;
    };
    table.getExpandedDepth = () => {
      let maxDepth = 0;
      const rowIds = table.getState().expanded === true ? Object.keys(table.getRowModel().rowsById) : Object.keys(table.getState().expanded);
      rowIds.forEach((id2) => {
        const splitId = id2.split(".");
        maxDepth = Math.max(maxDepth, splitId.length);
      });
      return maxDepth;
    };
    table.getPreExpandedRowModel = () => table.getSortedRowModel();
    table.getExpandedRowModel = () => {
      if (!table._getExpandedRowModel && table.options.getExpandedRowModel) {
        table._getExpandedRowModel = table.options.getExpandedRowModel(table);
      }
      if (table.options.manualExpanding || !table._getExpandedRowModel) {
        return table.getPreExpandedRowModel();
      }
      return table._getExpandedRowModel();
    };
  },
  createRow: (row, table) => {
    row.toggleExpanded = (expanded) => {
      table.setExpanded((old) => {
        var _expanded;
        const exists = old === true ? true : !!(old != null && old[row.id]);
        let oldExpanded = {};
        if (old === true) {
          Object.keys(table.getRowModel().rowsById).forEach((rowId) => {
            oldExpanded[rowId] = true;
          });
        } else {
          oldExpanded = old;
        }
        expanded = (_expanded = expanded) != null ? _expanded : !exists;
        if (!exists && expanded) {
          return {
            ...oldExpanded,
            [row.id]: true
          };
        }
        if (exists && !expanded) {
          const {
            [row.id]: _,
            ...rest
          } = oldExpanded;
          return rest;
        }
        return old;
      });
    };
    row.getIsExpanded = () => {
      var _table$options$getIsR;
      const expanded = table.getState().expanded;
      return !!((_table$options$getIsR = table.options.getIsRowExpanded == null ? void 0 : table.options.getIsRowExpanded(row)) != null ? _table$options$getIsR : expanded === true || (expanded == null ? void 0 : expanded[row.id]));
    };
    row.getCanExpand = () => {
      var _table$options$getRow, _table$options$enable, _row$subRows;
      return (_table$options$getRow = table.options.getRowCanExpand == null ? void 0 : table.options.getRowCanExpand(row)) != null ? _table$options$getRow : ((_table$options$enable = table.options.enableExpanding) != null ? _table$options$enable : true) && !!((_row$subRows = row.subRows) != null && _row$subRows.length);
    };
    row.getIsAllParentsExpanded = () => {
      let isFullyExpanded = true;
      let currentRow = row;
      while (isFullyExpanded && currentRow.parentId) {
        currentRow = table.getRow(currentRow.parentId, true);
        isFullyExpanded = currentRow.getIsExpanded();
      }
      return isFullyExpanded;
    };
    row.getToggleExpandedHandler = () => {
      const canExpand = row.getCanExpand();
      return () => {
        if (!canExpand) return;
        row.toggleExpanded();
      };
    };
  }
};
var defaultPageIndex = 0;
var defaultPageSize = 10;
var getDefaultPaginationState = () => ({
  pageIndex: defaultPageIndex,
  pageSize: defaultPageSize
});
var RowPagination = {
  getInitialState: (state2) => {
    return {
      ...state2,
      pagination: {
        ...getDefaultPaginationState(),
        ...state2 == null ? void 0 : state2.pagination
      }
    };
  },
  getDefaultOptions: (table) => {
    return {
      onPaginationChange: makeStateUpdater("pagination", table)
    };
  },
  createTable: (table) => {
    let registered = false;
    let queued = false;
    table._autoResetPageIndex = () => {
      var _ref, _table$options$autoRe;
      if (!registered) {
        table._queue(() => {
          registered = true;
        });
        return;
      }
      if ((_ref = (_table$options$autoRe = table.options.autoResetAll) != null ? _table$options$autoRe : table.options.autoResetPageIndex) != null ? _ref : !table.options.manualPagination) {
        if (queued) return;
        queued = true;
        table._queue(() => {
          table.resetPageIndex();
          queued = false;
        });
      }
    };
    table.setPagination = (updater) => {
      const safeUpdater = (old) => {
        let newState = functionalUpdate(updater, old);
        return newState;
      };
      return table.options.onPaginationChange == null ? void 0 : table.options.onPaginationChange(safeUpdater);
    };
    table.resetPagination = (defaultState) => {
      var _table$initialState$p;
      table.setPagination(defaultState ? getDefaultPaginationState() : (_table$initialState$p = table.initialState.pagination) != null ? _table$initialState$p : getDefaultPaginationState());
    };
    table.setPageIndex = (updater) => {
      table.setPagination((old) => {
        let pageIndex = functionalUpdate(updater, old.pageIndex);
        const maxPageIndex = typeof table.options.pageCount === "undefined" || table.options.pageCount === -1 ? Number.MAX_SAFE_INTEGER : table.options.pageCount - 1;
        pageIndex = Math.max(0, Math.min(pageIndex, maxPageIndex));
        return {
          ...old,
          pageIndex
        };
      });
    };
    table.resetPageIndex = (defaultState) => {
      var _table$initialState$p2, _table$initialState;
      table.setPageIndex(defaultState ? defaultPageIndex : (_table$initialState$p2 = (_table$initialState = table.initialState) == null || (_table$initialState = _table$initialState.pagination) == null ? void 0 : _table$initialState.pageIndex) != null ? _table$initialState$p2 : defaultPageIndex);
    };
    table.resetPageSize = (defaultState) => {
      var _table$initialState$p3, _table$initialState2;
      table.setPageSize(defaultState ? defaultPageSize : (_table$initialState$p3 = (_table$initialState2 = table.initialState) == null || (_table$initialState2 = _table$initialState2.pagination) == null ? void 0 : _table$initialState2.pageSize) != null ? _table$initialState$p3 : defaultPageSize);
    };
    table.setPageSize = (updater) => {
      table.setPagination((old) => {
        const pageSize = Math.max(1, functionalUpdate(updater, old.pageSize));
        const topRowIndex = old.pageSize * old.pageIndex;
        const pageIndex = Math.floor(topRowIndex / pageSize);
        return {
          ...old,
          pageIndex,
          pageSize
        };
      });
    };
    table.setPageCount = (updater) => table.setPagination((old) => {
      var _table$options$pageCo;
      let newPageCount = functionalUpdate(updater, (_table$options$pageCo = table.options.pageCount) != null ? _table$options$pageCo : -1);
      if (typeof newPageCount === "number") {
        newPageCount = Math.max(-1, newPageCount);
      }
      return {
        ...old,
        pageCount: newPageCount
      };
    });
    table.getPageOptions = memo(() => [table.getPageCount()], (pageCount) => {
      let pageOptions = [];
      if (pageCount && pageCount > 0) {
        pageOptions = [...new Array(pageCount)].fill(null).map((_, i) => i);
      }
      return pageOptions;
    }, getMemoOptions(table.options, "debugTable", "getPageOptions"));
    table.getCanPreviousPage = () => table.getState().pagination.pageIndex > 0;
    table.getCanNextPage = () => {
      const {
        pageIndex
      } = table.getState().pagination;
      const pageCount = table.getPageCount();
      if (pageCount === -1) {
        return true;
      }
      if (pageCount === 0) {
        return false;
      }
      return pageIndex < pageCount - 1;
    };
    table.previousPage = () => {
      return table.setPageIndex((old) => old - 1);
    };
    table.nextPage = () => {
      return table.setPageIndex((old) => {
        return old + 1;
      });
    };
    table.firstPage = () => {
      return table.setPageIndex(0);
    };
    table.lastPage = () => {
      return table.setPageIndex(table.getPageCount() - 1);
    };
    table.getPrePaginationRowModel = () => table.getExpandedRowModel();
    table.getPaginationRowModel = () => {
      if (!table._getPaginationRowModel && table.options.getPaginationRowModel) {
        table._getPaginationRowModel = table.options.getPaginationRowModel(table);
      }
      if (table.options.manualPagination || !table._getPaginationRowModel) {
        return table.getPrePaginationRowModel();
      }
      return table._getPaginationRowModel();
    };
    table.getPageCount = () => {
      var _table$options$pageCo2;
      return (_table$options$pageCo2 = table.options.pageCount) != null ? _table$options$pageCo2 : Math.ceil(table.getRowCount() / table.getState().pagination.pageSize);
    };
    table.getRowCount = () => {
      var _table$options$rowCou;
      return (_table$options$rowCou = table.options.rowCount) != null ? _table$options$rowCou : table.getPrePaginationRowModel().rows.length;
    };
  }
};
var getDefaultRowPinningState = () => ({
  top: [],
  bottom: []
});
var RowPinning = {
  getInitialState: (state2) => {
    return {
      rowPinning: getDefaultRowPinningState(),
      ...state2
    };
  },
  getDefaultOptions: (table) => {
    return {
      onRowPinningChange: makeStateUpdater("rowPinning", table)
    };
  },
  createRow: (row, table) => {
    row.pin = (position, includeLeafRows, includeParentRows) => {
      const leafRowIds = includeLeafRows ? row.getLeafRows().map((_ref) => {
        let {
          id: id2
        } = _ref;
        return id2;
      }) : [];
      const parentRowIds = includeParentRows ? row.getParentRows().map((_ref2) => {
        let {
          id: id2
        } = _ref2;
        return id2;
      }) : [];
      const rowIds = /* @__PURE__ */ new Set([...parentRowIds, row.id, ...leafRowIds]);
      table.setRowPinning((old) => {
        var _old$top3, _old$bottom3;
        if (position === "bottom") {
          var _old$top, _old$bottom;
          return {
            top: ((_old$top = old == null ? void 0 : old.top) != null ? _old$top : []).filter((d) => !(rowIds != null && rowIds.has(d))),
            bottom: [...((_old$bottom = old == null ? void 0 : old.bottom) != null ? _old$bottom : []).filter((d) => !(rowIds != null && rowIds.has(d))), ...Array.from(rowIds)]
          };
        }
        if (position === "top") {
          var _old$top2, _old$bottom2;
          return {
            top: [...((_old$top2 = old == null ? void 0 : old.top) != null ? _old$top2 : []).filter((d) => !(rowIds != null && rowIds.has(d))), ...Array.from(rowIds)],
            bottom: ((_old$bottom2 = old == null ? void 0 : old.bottom) != null ? _old$bottom2 : []).filter((d) => !(rowIds != null && rowIds.has(d)))
          };
        }
        return {
          top: ((_old$top3 = old == null ? void 0 : old.top) != null ? _old$top3 : []).filter((d) => !(rowIds != null && rowIds.has(d))),
          bottom: ((_old$bottom3 = old == null ? void 0 : old.bottom) != null ? _old$bottom3 : []).filter((d) => !(rowIds != null && rowIds.has(d)))
        };
      });
    };
    row.getCanPin = () => {
      var _ref3;
      const {
        enableRowPinning,
        enablePinning
      } = table.options;
      if (typeof enableRowPinning === "function") {
        return enableRowPinning(row);
      }
      return (_ref3 = enableRowPinning != null ? enableRowPinning : enablePinning) != null ? _ref3 : true;
    };
    row.getIsPinned = () => {
      const rowIds = [row.id];
      const {
        top,
        bottom
      } = table.getState().rowPinning;
      const isTop = rowIds.some((d) => top == null ? void 0 : top.includes(d));
      const isBottom = rowIds.some((d) => bottom == null ? void 0 : bottom.includes(d));
      return isTop ? "top" : isBottom ? "bottom" : false;
    };
    row.getPinnedIndex = () => {
      var _ref4, _visiblePinnedRowIds$;
      const position = row.getIsPinned();
      if (!position) return -1;
      const visiblePinnedRowIds = (_ref4 = position === "top" ? table.getTopRows() : table.getBottomRows()) == null ? void 0 : _ref4.map((_ref5) => {
        let {
          id: id2
        } = _ref5;
        return id2;
      });
      return (_visiblePinnedRowIds$ = visiblePinnedRowIds == null ? void 0 : visiblePinnedRowIds.indexOf(row.id)) != null ? _visiblePinnedRowIds$ : -1;
    };
  },
  createTable: (table) => {
    table.setRowPinning = (updater) => table.options.onRowPinningChange == null ? void 0 : table.options.onRowPinningChange(updater);
    table.resetRowPinning = (defaultState) => {
      var _table$initialState$r, _table$initialState;
      return table.setRowPinning(defaultState ? getDefaultRowPinningState() : (_table$initialState$r = (_table$initialState = table.initialState) == null ? void 0 : _table$initialState.rowPinning) != null ? _table$initialState$r : getDefaultRowPinningState());
    };
    table.getIsSomeRowsPinned = (position) => {
      var _pinningState$positio;
      const pinningState = table.getState().rowPinning;
      if (!position) {
        var _pinningState$top, _pinningState$bottom;
        return Boolean(((_pinningState$top = pinningState.top) == null ? void 0 : _pinningState$top.length) || ((_pinningState$bottom = pinningState.bottom) == null ? void 0 : _pinningState$bottom.length));
      }
      return Boolean((_pinningState$positio = pinningState[position]) == null ? void 0 : _pinningState$positio.length);
    };
    table._getPinnedRows = (visibleRows, pinnedRowIds, position) => {
      var _table$options$keepPi;
      const rows = ((_table$options$keepPi = table.options.keepPinnedRows) != null ? _table$options$keepPi : true) ? (
        //get all rows that are pinned even if they would not be otherwise visible
        //account for expanded parent rows, but not pagination or filtering
        (pinnedRowIds != null ? pinnedRowIds : []).map((rowId) => {
          const row = table.getRow(rowId, true);
          return row.getIsAllParentsExpanded() ? row : null;
        })
      ) : (
        //else get only visible rows that are pinned
        (pinnedRowIds != null ? pinnedRowIds : []).map((rowId) => visibleRows.find((row) => row.id === rowId))
      );
      return rows.filter(Boolean).map((d) => ({
        ...d,
        position
      }));
    };
    table.getTopRows = memo(() => [table.getRowModel().rows, table.getState().rowPinning.top], (allRows, topPinnedRowIds) => table._getPinnedRows(allRows, topPinnedRowIds, "top"), getMemoOptions(table.options, "debugRows", "getTopRows"));
    table.getBottomRows = memo(() => [table.getRowModel().rows, table.getState().rowPinning.bottom], (allRows, bottomPinnedRowIds) => table._getPinnedRows(allRows, bottomPinnedRowIds, "bottom"), getMemoOptions(table.options, "debugRows", "getBottomRows"));
    table.getCenterRows = memo(() => [table.getRowModel().rows, table.getState().rowPinning.top, table.getState().rowPinning.bottom], (allRows, top, bottom) => {
      const topAndBottom = /* @__PURE__ */ new Set([...top != null ? top : [], ...bottom != null ? bottom : []]);
      return allRows.filter((d) => !topAndBottom.has(d.id));
    }, getMemoOptions(table.options, "debugRows", "getCenterRows"));
  }
};
var RowSelection = {
  getInitialState: (state2) => {
    return {
      rowSelection: {},
      ...state2
    };
  },
  getDefaultOptions: (table) => {
    return {
      onRowSelectionChange: makeStateUpdater("rowSelection", table),
      enableRowSelection: true,
      enableMultiRowSelection: true,
      enableSubRowSelection: true
      // enableGroupingRowSelection: false,
      // isAdditiveSelectEvent: (e: unknown) => !!e.metaKey,
      // isInclusiveSelectEvent: (e: unknown) => !!e.shiftKey,
    };
  },
  createTable: (table) => {
    table.setRowSelection = (updater) => table.options.onRowSelectionChange == null ? void 0 : table.options.onRowSelectionChange(updater);
    table.resetRowSelection = (defaultState) => {
      var _table$initialState$r;
      return table.setRowSelection(defaultState ? {} : (_table$initialState$r = table.initialState.rowSelection) != null ? _table$initialState$r : {});
    };
    table.toggleAllRowsSelected = (value) => {
      table.setRowSelection((old) => {
        value = typeof value !== "undefined" ? value : !table.getIsAllRowsSelected();
        const rowSelection = {
          ...old
        };
        const preGroupedFlatRows = table.getPreGroupedRowModel().flatRows;
        if (value) {
          preGroupedFlatRows.forEach((row) => {
            if (!row.getCanSelect()) {
              return;
            }
            rowSelection[row.id] = true;
          });
        } else {
          preGroupedFlatRows.forEach((row) => {
            delete rowSelection[row.id];
          });
        }
        return rowSelection;
      });
    };
    table.toggleAllPageRowsSelected = (value) => table.setRowSelection((old) => {
      const resolvedValue = typeof value !== "undefined" ? value : !table.getIsAllPageRowsSelected();
      const rowSelection = {
        ...old
      };
      table.getRowModel().rows.forEach((row) => {
        mutateRowIsSelected(rowSelection, row.id, resolvedValue, true, table);
      });
      return rowSelection;
    });
    table.getPreSelectedRowModel = () => table.getCoreRowModel();
    table.getSelectedRowModel = memo(() => [table.getState().rowSelection, table.getCoreRowModel()], (rowSelection, rowModel) => {
      if (!Object.keys(rowSelection).length) {
        return {
          rows: [],
          flatRows: [],
          rowsById: {}
        };
      }
      return selectRowsFn(table, rowModel);
    }, getMemoOptions(table.options, "debugTable", "getSelectedRowModel"));
    table.getFilteredSelectedRowModel = memo(() => [table.getState().rowSelection, table.getFilteredRowModel()], (rowSelection, rowModel) => {
      if (!Object.keys(rowSelection).length) {
        return {
          rows: [],
          flatRows: [],
          rowsById: {}
        };
      }
      return selectRowsFn(table, rowModel);
    }, getMemoOptions(table.options, "debugTable", "getFilteredSelectedRowModel"));
    table.getGroupedSelectedRowModel = memo(() => [table.getState().rowSelection, table.getSortedRowModel()], (rowSelection, rowModel) => {
      if (!Object.keys(rowSelection).length) {
        return {
          rows: [],
          flatRows: [],
          rowsById: {}
        };
      }
      return selectRowsFn(table, rowModel);
    }, getMemoOptions(table.options, "debugTable", "getGroupedSelectedRowModel"));
    table.getIsAllRowsSelected = () => {
      const preGroupedFlatRows = table.getFilteredRowModel().flatRows;
      const {
        rowSelection
      } = table.getState();
      let isAllRowsSelected = Boolean(preGroupedFlatRows.length && Object.keys(rowSelection).length);
      if (isAllRowsSelected) {
        if (preGroupedFlatRows.some((row) => row.getCanSelect() && !rowSelection[row.id])) {
          isAllRowsSelected = false;
        }
      }
      return isAllRowsSelected;
    };
    table.getIsAllPageRowsSelected = () => {
      const paginationFlatRows = table.getPaginationRowModel().flatRows.filter((row) => row.getCanSelect());
      const {
        rowSelection
      } = table.getState();
      let isAllPageRowsSelected = !!paginationFlatRows.length;
      if (isAllPageRowsSelected && paginationFlatRows.some((row) => !rowSelection[row.id])) {
        isAllPageRowsSelected = false;
      }
      return isAllPageRowsSelected;
    };
    table.getIsSomeRowsSelected = () => {
      var _table$getState$rowSe;
      const totalSelected = Object.keys((_table$getState$rowSe = table.getState().rowSelection) != null ? _table$getState$rowSe : {}).length;
      return totalSelected > 0 && totalSelected < table.getFilteredRowModel().flatRows.length;
    };
    table.getIsSomePageRowsSelected = () => {
      const paginationFlatRows = table.getPaginationRowModel().flatRows;
      return table.getIsAllPageRowsSelected() ? false : paginationFlatRows.filter((row) => row.getCanSelect()).some((d) => d.getIsSelected() || d.getIsSomeSelected());
    };
    table.getToggleAllRowsSelectedHandler = () => {
      return (e2) => {
        table.toggleAllRowsSelected(e2.target.checked);
      };
    };
    table.getToggleAllPageRowsSelectedHandler = () => {
      return (e2) => {
        table.toggleAllPageRowsSelected(e2.target.checked);
      };
    };
  },
  createRow: (row, table) => {
    row.toggleSelected = (value, opts) => {
      const isSelected = row.getIsSelected();
      table.setRowSelection((old) => {
        var _opts$selectChildren;
        value = typeof value !== "undefined" ? value : !isSelected;
        if (row.getCanSelect() && isSelected === value) {
          return old;
        }
        const selectedRowIds = {
          ...old
        };
        mutateRowIsSelected(selectedRowIds, row.id, value, (_opts$selectChildren = opts == null ? void 0 : opts.selectChildren) != null ? _opts$selectChildren : true, table);
        return selectedRowIds;
      });
    };
    row.getIsSelected = () => {
      const {
        rowSelection
      } = table.getState();
      return isRowSelected(row, rowSelection);
    };
    row.getIsSomeSelected = () => {
      const {
        rowSelection
      } = table.getState();
      return isSubRowSelected(row, rowSelection) === "some";
    };
    row.getIsAllSubRowsSelected = () => {
      const {
        rowSelection
      } = table.getState();
      return isSubRowSelected(row, rowSelection) === "all";
    };
    row.getCanSelect = () => {
      var _table$options$enable;
      if (typeof table.options.enableRowSelection === "function") {
        return table.options.enableRowSelection(row);
      }
      return (_table$options$enable = table.options.enableRowSelection) != null ? _table$options$enable : true;
    };
    row.getCanSelectSubRows = () => {
      var _table$options$enable2;
      if (typeof table.options.enableSubRowSelection === "function") {
        return table.options.enableSubRowSelection(row);
      }
      return (_table$options$enable2 = table.options.enableSubRowSelection) != null ? _table$options$enable2 : true;
    };
    row.getCanMultiSelect = () => {
      var _table$options$enable3;
      if (typeof table.options.enableMultiRowSelection === "function") {
        return table.options.enableMultiRowSelection(row);
      }
      return (_table$options$enable3 = table.options.enableMultiRowSelection) != null ? _table$options$enable3 : true;
    };
    row.getToggleSelectedHandler = () => {
      const canSelect = row.getCanSelect();
      return (e2) => {
        var _target;
        if (!canSelect) return;
        row.toggleSelected((_target = e2.target) == null ? void 0 : _target.checked);
      };
    };
  }
};
var mutateRowIsSelected = (selectedRowIds, id2, value, includeChildren, table) => {
  var _row$subRows;
  const row = table.getRow(id2, true);
  if (value) {
    if (!row.getCanMultiSelect()) {
      Object.keys(selectedRowIds).forEach((key) => delete selectedRowIds[key]);
    }
    if (row.getCanSelect()) {
      selectedRowIds[id2] = true;
    }
  } else {
    delete selectedRowIds[id2];
  }
  if (includeChildren && (_row$subRows = row.subRows) != null && _row$subRows.length && row.getCanSelectSubRows()) {
    row.subRows.forEach((row2) => mutateRowIsSelected(selectedRowIds, row2.id, value, includeChildren, table));
  }
};
function selectRowsFn(table, rowModel) {
  const rowSelection = table.getState().rowSelection;
  const newSelectedFlatRows = [];
  const newSelectedRowsById = {};
  const recurseRows = function(rows, depth) {
    return rows.map((row) => {
      var _row$subRows2;
      const isSelected = isRowSelected(row, rowSelection);
      if (isSelected) {
        newSelectedFlatRows.push(row);
        newSelectedRowsById[row.id] = row;
      }
      if ((_row$subRows2 = row.subRows) != null && _row$subRows2.length) {
        row = {
          ...row,
          subRows: recurseRows(row.subRows)
        };
      }
      if (isSelected) {
        return row;
      }
    }).filter(Boolean);
  };
  return {
    rows: recurseRows(rowModel.rows),
    flatRows: newSelectedFlatRows,
    rowsById: newSelectedRowsById
  };
}
function isRowSelected(row, selection) {
  var _selection$row$id;
  return (_selection$row$id = selection[row.id]) != null ? _selection$row$id : false;
}
function isSubRowSelected(row, selection, table) {
  var _row$subRows3;
  if (!((_row$subRows3 = row.subRows) != null && _row$subRows3.length)) return false;
  let allChildrenSelected = true;
  let someSelected = false;
  row.subRows.forEach((subRow) => {
    if (someSelected && !allChildrenSelected) {
      return;
    }
    if (subRow.getCanSelect()) {
      if (isRowSelected(subRow, selection)) {
        someSelected = true;
      } else {
        allChildrenSelected = false;
      }
    }
    if (subRow.subRows && subRow.subRows.length) {
      const subRowChildrenSelected = isSubRowSelected(subRow, selection);
      if (subRowChildrenSelected === "all") {
        someSelected = true;
      } else if (subRowChildrenSelected === "some") {
        someSelected = true;
        allChildrenSelected = false;
      } else {
        allChildrenSelected = false;
      }
    }
  });
  return allChildrenSelected ? "all" : someSelected ? "some" : false;
}
var reSplitAlphaNumeric = /([0-9]+)/gm;
var alphanumeric = (rowA, rowB, columnId) => {
  return compareAlphanumeric(toString2(rowA.getValue(columnId)).toLowerCase(), toString2(rowB.getValue(columnId)).toLowerCase());
};
var alphanumericCaseSensitive = (rowA, rowB, columnId) => {
  return compareAlphanumeric(toString2(rowA.getValue(columnId)), toString2(rowB.getValue(columnId)));
};
var text = (rowA, rowB, columnId) => {
  return compareBasic(toString2(rowA.getValue(columnId)).toLowerCase(), toString2(rowB.getValue(columnId)).toLowerCase());
};
var textCaseSensitive = (rowA, rowB, columnId) => {
  return compareBasic(toString2(rowA.getValue(columnId)), toString2(rowB.getValue(columnId)));
};
var datetime = (rowA, rowB, columnId) => {
  const a = rowA.getValue(columnId);
  const b = rowB.getValue(columnId);
  return a > b ? 1 : a < b ? -1 : 0;
};
var basic = (rowA, rowB, columnId) => {
  return compareBasic(rowA.getValue(columnId), rowB.getValue(columnId));
};
function compareBasic(a, b) {
  return a === b ? 0 : a > b ? 1 : -1;
}
function toString2(a) {
  if (typeof a === "number") {
    if (isNaN(a) || a === Infinity || a === -Infinity) {
      return "";
    }
    return String(a);
  }
  if (typeof a === "string") {
    return a;
  }
  return "";
}
function compareAlphanumeric(aStr, bStr) {
  const a = aStr.split(reSplitAlphaNumeric).filter(Boolean);
  const b = bStr.split(reSplitAlphaNumeric).filter(Boolean);
  while (a.length && b.length) {
    const aa = a.shift();
    const bb = b.shift();
    const an = parseInt(aa, 10);
    const bn = parseInt(bb, 10);
    const combo = [an, bn].sort();
    if (isNaN(combo[0])) {
      if (aa > bb) {
        return 1;
      }
      if (bb > aa) {
        return -1;
      }
      continue;
    }
    if (isNaN(combo[1])) {
      return isNaN(an) ? -1 : 1;
    }
    if (an > bn) {
      return 1;
    }
    if (bn > an) {
      return -1;
    }
  }
  return a.length - b.length;
}
var sortingFns = {
  alphanumeric,
  alphanumericCaseSensitive,
  text,
  textCaseSensitive,
  datetime,
  basic
};
var RowSorting = {
  getInitialState: (state2) => {
    return {
      sorting: [],
      ...state2
    };
  },
  getDefaultColumnDef: () => {
    return {
      sortingFn: "auto",
      sortUndefined: 1
    };
  },
  getDefaultOptions: (table) => {
    return {
      onSortingChange: makeStateUpdater("sorting", table),
      isMultiSortEvent: (e2) => {
        return e2.shiftKey;
      }
    };
  },
  createColumn: (column, table) => {
    column.getAutoSortingFn = () => {
      const firstRows = table.getFilteredRowModel().flatRows.slice(10);
      let isString2 = false;
      for (const row of firstRows) {
        const value = row == null ? void 0 : row.getValue(column.id);
        if (Object.prototype.toString.call(value) === "[object Date]") {
          return sortingFns.datetime;
        }
        if (typeof value === "string") {
          isString2 = true;
          if (value.split(reSplitAlphaNumeric).length > 1) {
            return sortingFns.alphanumeric;
          }
        }
      }
      if (isString2) {
        return sortingFns.text;
      }
      return sortingFns.basic;
    };
    column.getAutoSortDir = () => {
      const firstRow = table.getFilteredRowModel().flatRows[0];
      const value = firstRow == null ? void 0 : firstRow.getValue(column.id);
      if (typeof value === "string") {
        return "asc";
      }
      return "desc";
    };
    column.getSortingFn = () => {
      var _table$options$sortin, _table$options$sortin2;
      if (!column) {
        throw new Error();
      }
      return isFunction(column.columnDef.sortingFn) ? column.columnDef.sortingFn : column.columnDef.sortingFn === "auto" ? column.getAutoSortingFn() : (_table$options$sortin = (_table$options$sortin2 = table.options.sortingFns) == null ? void 0 : _table$options$sortin2[column.columnDef.sortingFn]) != null ? _table$options$sortin : sortingFns[column.columnDef.sortingFn];
    };
    column.toggleSorting = (desc, multi) => {
      const nextSortingOrder = column.getNextSortingOrder();
      const hasManualValue = typeof desc !== "undefined" && desc !== null;
      table.setSorting((old) => {
        const existingSorting = old == null ? void 0 : old.find((d) => d.id === column.id);
        const existingIndex = old == null ? void 0 : old.findIndex((d) => d.id === column.id);
        let newSorting = [];
        let sortAction;
        let nextDesc = hasManualValue ? desc : nextSortingOrder === "desc";
        if (old != null && old.length && column.getCanMultiSort() && multi) {
          if (existingSorting) {
            sortAction = "toggle";
          } else {
            sortAction = "add";
          }
        } else {
          if (old != null && old.length && existingIndex !== old.length - 1) {
            sortAction = "replace";
          } else if (existingSorting) {
            sortAction = "toggle";
          } else {
            sortAction = "replace";
          }
        }
        if (sortAction === "toggle") {
          if (!hasManualValue) {
            if (!nextSortingOrder) {
              sortAction = "remove";
            }
          }
        }
        if (sortAction === "add") {
          var _table$options$maxMul;
          newSorting = [...old, {
            id: column.id,
            desc: nextDesc
          }];
          newSorting.splice(0, newSorting.length - ((_table$options$maxMul = table.options.maxMultiSortColCount) != null ? _table$options$maxMul : Number.MAX_SAFE_INTEGER));
        } else if (sortAction === "toggle") {
          newSorting = old.map((d) => {
            if (d.id === column.id) {
              return {
                ...d,
                desc: nextDesc
              };
            }
            return d;
          });
        } else if (sortAction === "remove") {
          newSorting = old.filter((d) => d.id !== column.id);
        } else {
          newSorting = [{
            id: column.id,
            desc: nextDesc
          }];
        }
        return newSorting;
      });
    };
    column.getFirstSortDir = () => {
      var _ref, _column$columnDef$sor;
      const sortDescFirst = (_ref = (_column$columnDef$sor = column.columnDef.sortDescFirst) != null ? _column$columnDef$sor : table.options.sortDescFirst) != null ? _ref : column.getAutoSortDir() === "desc";
      return sortDescFirst ? "desc" : "asc";
    };
    column.getNextSortingOrder = (multi) => {
      var _table$options$enable, _table$options$enable2;
      const firstSortDirection = column.getFirstSortDir();
      const isSorted = column.getIsSorted();
      if (!isSorted) {
        return firstSortDirection;
      }
      if (isSorted !== firstSortDirection && ((_table$options$enable = table.options.enableSortingRemoval) != null ? _table$options$enable : true) && // If enableSortRemove, enable in general
      (multi ? (_table$options$enable2 = table.options.enableMultiRemove) != null ? _table$options$enable2 : true : true)) {
        return false;
      }
      return isSorted === "desc" ? "asc" : "desc";
    };
    column.getCanSort = () => {
      var _column$columnDef$ena, _table$options$enable3;
      return ((_column$columnDef$ena = column.columnDef.enableSorting) != null ? _column$columnDef$ena : true) && ((_table$options$enable3 = table.options.enableSorting) != null ? _table$options$enable3 : true) && !!column.accessorFn;
    };
    column.getCanMultiSort = () => {
      var _ref2, _column$columnDef$ena2;
      return (_ref2 = (_column$columnDef$ena2 = column.columnDef.enableMultiSort) != null ? _column$columnDef$ena2 : table.options.enableMultiSort) != null ? _ref2 : !!column.accessorFn;
    };
    column.getIsSorted = () => {
      var _table$getState$sorti;
      const columnSort = (_table$getState$sorti = table.getState().sorting) == null ? void 0 : _table$getState$sorti.find((d) => d.id === column.id);
      return !columnSort ? false : columnSort.desc ? "desc" : "asc";
    };
    column.getSortIndex = () => {
      var _table$getState$sorti2, _table$getState$sorti3;
      return (_table$getState$sorti2 = (_table$getState$sorti3 = table.getState().sorting) == null ? void 0 : _table$getState$sorti3.findIndex((d) => d.id === column.id)) != null ? _table$getState$sorti2 : -1;
    };
    column.clearSorting = () => {
      table.setSorting((old) => old != null && old.length ? old.filter((d) => d.id !== column.id) : []);
    };
    column.getToggleSortingHandler = () => {
      const canSort = column.getCanSort();
      return (e2) => {
        if (!canSort) return;
        e2.persist == null || e2.persist();
        column.toggleSorting == null || column.toggleSorting(void 0, column.getCanMultiSort() ? table.options.isMultiSortEvent == null ? void 0 : table.options.isMultiSortEvent(e2) : false);
      };
    };
  },
  createTable: (table) => {
    table.setSorting = (updater) => table.options.onSortingChange == null ? void 0 : table.options.onSortingChange(updater);
    table.resetSorting = (defaultState) => {
      var _table$initialState$s, _table$initialState;
      table.setSorting(defaultState ? [] : (_table$initialState$s = (_table$initialState = table.initialState) == null ? void 0 : _table$initialState.sorting) != null ? _table$initialState$s : []);
    };
    table.getPreSortedRowModel = () => table.getGroupedRowModel();
    table.getSortedRowModel = () => {
      if (!table._getSortedRowModel && table.options.getSortedRowModel) {
        table._getSortedRowModel = table.options.getSortedRowModel(table);
      }
      if (table.options.manualSorting || !table._getSortedRowModel) {
        return table.getPreSortedRowModel();
      }
      return table._getSortedRowModel();
    };
  }
};
var builtInFeatures = [
  Headers,
  ColumnVisibility,
  ColumnOrdering,
  ColumnPinning,
  ColumnFaceting,
  ColumnFiltering,
  GlobalFaceting,
  //depends on ColumnFaceting
  GlobalFiltering,
  //depends on ColumnFiltering
  RowSorting,
  ColumnGrouping,
  //depends on RowSorting
  RowExpanding,
  RowPagination,
  RowPinning,
  RowSelection,
  ColumnSizing
];
function createTable(options) {
  var _options$_features, _options$initialState;
  if (options.debugAll || options.debugTable) {
    console.info("Creating Table Instance...");
  }
  const _features = [...builtInFeatures, ...(_options$_features = options._features) != null ? _options$_features : []];
  let table = {
    _features
  };
  const defaultOptions = table._features.reduce((obj, feature) => {
    return Object.assign(obj, feature.getDefaultOptions == null ? void 0 : feature.getDefaultOptions(table));
  }, {});
  const mergeOptions = (options2) => {
    if (table.options.mergeOptions) {
      return table.options.mergeOptions(defaultOptions, options2);
    }
    return {
      ...defaultOptions,
      ...options2
    };
  };
  const coreInitialState = {};
  let initialState = {
    ...coreInitialState,
    ...(_options$initialState = options.initialState) != null ? _options$initialState : {}
  };
  table._features.forEach((feature) => {
    var _feature$getInitialSt;
    initialState = (_feature$getInitialSt = feature.getInitialState == null ? void 0 : feature.getInitialState(initialState)) != null ? _feature$getInitialSt : initialState;
  });
  const queued = [];
  let queuedTimeout = false;
  const coreInstance = {
    _features,
    options: {
      ...defaultOptions,
      ...options
    },
    initialState,
    _queue: (cb) => {
      queued.push(cb);
      if (!queuedTimeout) {
        queuedTimeout = true;
        Promise.resolve().then(() => {
          while (queued.length) {
            queued.shift()();
          }
          queuedTimeout = false;
        }).catch((error) => setTimeout(() => {
          throw error;
        }));
      }
    },
    reset: () => {
      table.setState(table.initialState);
    },
    setOptions: (updater) => {
      const newOptions = functionalUpdate(updater, table.options);
      table.options = mergeOptions(newOptions);
    },
    getState: () => {
      return table.options.state;
    },
    setState: (updater) => {
      table.options.onStateChange == null || table.options.onStateChange(updater);
    },
    _getRowId: (row, index, parent) => {
      var _table$options$getRow;
      return (_table$options$getRow = table.options.getRowId == null ? void 0 : table.options.getRowId(row, index, parent)) != null ? _table$options$getRow : `${parent ? [parent.id, index].join(".") : index}`;
    },
    getCoreRowModel: () => {
      if (!table._getCoreRowModel) {
        table._getCoreRowModel = table.options.getCoreRowModel(table);
      }
      return table._getCoreRowModel();
    },
    // The final calls start at the bottom of the model,
    // expanded rows, which then work their way up
    getRowModel: () => {
      return table.getPaginationRowModel();
    },
    //in next version, we should just pass in the row model as the optional 2nd arg
    getRow: (id2, searchAll) => {
      let row = (searchAll ? table.getPrePaginationRowModel() : table.getRowModel()).rowsById[id2];
      if (!row) {
        row = table.getCoreRowModel().rowsById[id2];
        if (!row) {
          if (true) {
            throw new Error(`getRow could not find row with ID: ${id2}`);
          }
          throw new Error();
        }
      }
      return row;
    },
    _getDefaultColumnDef: memo(() => [table.options.defaultColumn], (defaultColumn) => {
      var _defaultColumn;
      defaultColumn = (_defaultColumn = defaultColumn) != null ? _defaultColumn : {};
      return {
        header: (props) => {
          const resolvedColumnDef = props.header.column.columnDef;
          if (resolvedColumnDef.accessorKey) {
            return resolvedColumnDef.accessorKey;
          }
          if (resolvedColumnDef.accessorFn) {
            return resolvedColumnDef.id;
          }
          return null;
        },
        // footer: props => props.header.column.id,
        cell: (props) => {
          var _props$renderValue$to, _props$renderValue;
          return (_props$renderValue$to = (_props$renderValue = props.renderValue()) == null || _props$renderValue.toString == null ? void 0 : _props$renderValue.toString()) != null ? _props$renderValue$to : null;
        },
        ...table._features.reduce((obj, feature) => {
          return Object.assign(obj, feature.getDefaultColumnDef == null ? void 0 : feature.getDefaultColumnDef());
        }, {}),
        ...defaultColumn
      };
    }, getMemoOptions(options, "debugColumns", "_getDefaultColumnDef")),
    _getColumnDefs: () => table.options.columns,
    getAllColumns: memo(() => [table._getColumnDefs()], (columnDefs) => {
      const recurseColumns = function(columnDefs2, parent, depth) {
        if (depth === void 0) {
          depth = 0;
        }
        return columnDefs2.map((columnDef) => {
          const column = createColumn(table, columnDef, depth, parent);
          const groupingColumnDef = columnDef;
          column.columns = groupingColumnDef.columns ? recurseColumns(groupingColumnDef.columns, column, depth + 1) : [];
          return column;
        });
      };
      return recurseColumns(columnDefs);
    }, getMemoOptions(options, "debugColumns", "getAllColumns")),
    getAllFlatColumns: memo(() => [table.getAllColumns()], (allColumns) => {
      return allColumns.flatMap((column) => {
        return column.getFlatColumns();
      });
    }, getMemoOptions(options, "debugColumns", "getAllFlatColumns")),
    _getAllFlatColumnsById: memo(() => [table.getAllFlatColumns()], (flatColumns2) => {
      return flatColumns2.reduce((acc, column) => {
        acc[column.id] = column;
        return acc;
      }, {});
    }, getMemoOptions(options, "debugColumns", "getAllFlatColumnsById")),
    getAllLeafColumns: memo(() => [table.getAllColumns(), table._getOrderColumnsFn()], (allColumns, orderColumns2) => {
      let leafColumns = allColumns.flatMap((column) => column.getLeafColumns());
      return orderColumns2(leafColumns);
    }, getMemoOptions(options, "debugColumns", "getAllLeafColumns")),
    getColumn: (columnId) => {
      const column = table._getAllFlatColumnsById()[columnId];
      if (!column) {
        console.error(`[Table] Column with id '${columnId}' does not exist.`);
      }
      return column;
    }
  };
  Object.assign(table, coreInstance);
  for (let index = 0; index < table._features.length; index++) {
    const feature = table._features[index];
    feature == null || feature.createTable == null || feature.createTable(table);
  }
  return table;
}
function getCoreRowModel() {
  return (table) => memo(() => [table.options.data], (data) => {
    const rowModel = {
      rows: [],
      flatRows: [],
      rowsById: {}
    };
    const accessRows = function(originalRows, depth, parentRow) {
      if (depth === void 0) {
        depth = 0;
      }
      const rows = [];
      for (let i = 0; i < originalRows.length; i++) {
        const row = createRow(table, table._getRowId(originalRows[i], i, parentRow), originalRows[i], i, depth, void 0, parentRow == null ? void 0 : parentRow.id);
        rowModel.flatRows.push(row);
        rowModel.rowsById[row.id] = row;
        rows.push(row);
        if (table.options.getSubRows) {
          var _row$originalSubRows;
          row.originalSubRows = table.options.getSubRows(originalRows[i], i);
          if ((_row$originalSubRows = row.originalSubRows) != null && _row$originalSubRows.length) {
            row.subRows = accessRows(row.originalSubRows, depth + 1, row);
          }
        }
      }
      return rows;
    };
    rowModel.rows = accessRows(data);
    return rowModel;
  }, getMemoOptions(table.options, "debugTable", "getRowModel", () => table._autoResetPageIndex()));
}
function expandRows(rowModel) {
  const expandedRows = [];
  const handleRow = (row) => {
    var _row$subRows;
    expandedRows.push(row);
    if ((_row$subRows = row.subRows) != null && _row$subRows.length && row.getIsExpanded()) {
      row.subRows.forEach(handleRow);
    }
  };
  rowModel.rows.forEach(handleRow);
  return {
    rows: expandedRows,
    flatRows: rowModel.flatRows,
    rowsById: rowModel.rowsById
  };
}
function filterRows(rows, filterRowImpl, table) {
  if (table.options.filterFromLeafRows) {
    return filterRowModelFromLeafs(rows, filterRowImpl, table);
  }
  return filterRowModelFromRoot(rows, filterRowImpl, table);
}
function filterRowModelFromLeafs(rowsToFilter, filterRow, table) {
  var _table$options$maxLea;
  const newFilteredFlatRows = [];
  const newFilteredRowsById = {};
  const maxDepth = (_table$options$maxLea = table.options.maxLeafRowFilterDepth) != null ? _table$options$maxLea : 100;
  const recurseFilterRows = function(rowsToFilter2, depth) {
    if (depth === void 0) {
      depth = 0;
    }
    const rows = [];
    for (let i = 0; i < rowsToFilter2.length; i++) {
      var _row$subRows;
      let row = rowsToFilter2[i];
      const newRow = createRow(table, row.id, row.original, row.index, row.depth, void 0, row.parentId);
      newRow.columnFilters = row.columnFilters;
      if ((_row$subRows = row.subRows) != null && _row$subRows.length && depth < maxDepth) {
        newRow.subRows = recurseFilterRows(row.subRows, depth + 1);
        row = newRow;
        if (filterRow(row) && !newRow.subRows.length) {
          rows.push(row);
          newFilteredRowsById[row.id] = row;
          newFilteredFlatRows.push(row);
          continue;
        }
        if (filterRow(row) || newRow.subRows.length) {
          rows.push(row);
          newFilteredRowsById[row.id] = row;
          newFilteredFlatRows.push(row);
          continue;
        }
      } else {
        row = newRow;
        if (filterRow(row)) {
          rows.push(row);
          newFilteredRowsById[row.id] = row;
          newFilteredFlatRows.push(row);
        }
      }
    }
    return rows;
  };
  return {
    rows: recurseFilterRows(rowsToFilter),
    flatRows: newFilteredFlatRows,
    rowsById: newFilteredRowsById
  };
}
function filterRowModelFromRoot(rowsToFilter, filterRow, table) {
  var _table$options$maxLea2;
  const newFilteredFlatRows = [];
  const newFilteredRowsById = {};
  const maxDepth = (_table$options$maxLea2 = table.options.maxLeafRowFilterDepth) != null ? _table$options$maxLea2 : 100;
  const recurseFilterRows = function(rowsToFilter2, depth) {
    if (depth === void 0) {
      depth = 0;
    }
    const rows = [];
    for (let i = 0; i < rowsToFilter2.length; i++) {
      let row = rowsToFilter2[i];
      const pass = filterRow(row);
      if (pass) {
        var _row$subRows2;
        if ((_row$subRows2 = row.subRows) != null && _row$subRows2.length && depth < maxDepth) {
          const newRow = createRow(table, row.id, row.original, row.index, row.depth, void 0, row.parentId);
          newRow.subRows = recurseFilterRows(row.subRows, depth + 1);
          row = newRow;
        }
        rows.push(row);
        newFilteredFlatRows.push(row);
        newFilteredRowsById[row.id] = row;
      }
    }
    return rows;
  };
  return {
    rows: recurseFilterRows(rowsToFilter),
    flatRows: newFilteredFlatRows,
    rowsById: newFilteredRowsById
  };
}
function getFilteredRowModel() {
  return (table) => memo(() => [table.getPreFilteredRowModel(), table.getState().columnFilters, table.getState().globalFilter], (rowModel, columnFilters, globalFilter) => {
    if (!rowModel.rows.length || !(columnFilters != null && columnFilters.length) && !globalFilter) {
      for (let i = 0; i < rowModel.flatRows.length; i++) {
        rowModel.flatRows[i].columnFilters = {};
        rowModel.flatRows[i].columnFiltersMeta = {};
      }
      return rowModel;
    }
    const resolvedColumnFilters = [];
    const resolvedGlobalFilters = [];
    (columnFilters != null ? columnFilters : []).forEach((d) => {
      var _filterFn$resolveFilt;
      const column = table.getColumn(d.id);
      if (!column) {
        return;
      }
      const filterFn = column.getFilterFn();
      if (!filterFn) {
        if (true) {
          console.warn(`Could not find a valid 'column.filterFn' for column with the ID: ${column.id}.`);
        }
        return;
      }
      resolvedColumnFilters.push({
        id: d.id,
        filterFn,
        resolvedValue: (_filterFn$resolveFilt = filterFn.resolveFilterValue == null ? void 0 : filterFn.resolveFilterValue(d.value)) != null ? _filterFn$resolveFilt : d.value
      });
    });
    const filterableIds = (columnFilters != null ? columnFilters : []).map((d) => d.id);
    const globalFilterFn = table.getGlobalFilterFn();
    const globallyFilterableColumns = table.getAllLeafColumns().filter((column) => column.getCanGlobalFilter());
    if (globalFilter && globalFilterFn && globallyFilterableColumns.length) {
      filterableIds.push("__global__");
      globallyFilterableColumns.forEach((column) => {
        var _globalFilterFn$resol;
        resolvedGlobalFilters.push({
          id: column.id,
          filterFn: globalFilterFn,
          resolvedValue: (_globalFilterFn$resol = globalFilterFn.resolveFilterValue == null ? void 0 : globalFilterFn.resolveFilterValue(globalFilter)) != null ? _globalFilterFn$resol : globalFilter
        });
      });
    }
    let currentColumnFilter;
    let currentGlobalFilter;
    for (let j = 0; j < rowModel.flatRows.length; j++) {
      const row = rowModel.flatRows[j];
      row.columnFilters = {};
      if (resolvedColumnFilters.length) {
        for (let i = 0; i < resolvedColumnFilters.length; i++) {
          currentColumnFilter = resolvedColumnFilters[i];
          const id2 = currentColumnFilter.id;
          row.columnFilters[id2] = currentColumnFilter.filterFn(row, id2, currentColumnFilter.resolvedValue, (filterMeta) => {
            row.columnFiltersMeta[id2] = filterMeta;
          });
        }
      }
      if (resolvedGlobalFilters.length) {
        for (let i = 0; i < resolvedGlobalFilters.length; i++) {
          currentGlobalFilter = resolvedGlobalFilters[i];
          const id2 = currentGlobalFilter.id;
          if (currentGlobalFilter.filterFn(row, id2, currentGlobalFilter.resolvedValue, (filterMeta) => {
            row.columnFiltersMeta[id2] = filterMeta;
          })) {
            row.columnFilters.__global__ = true;
            break;
          }
        }
        if (row.columnFilters.__global__ !== true) {
          row.columnFilters.__global__ = false;
        }
      }
    }
    const filterRowsImpl = (row) => {
      for (let i = 0; i < filterableIds.length; i++) {
        if (row.columnFilters[filterableIds[i]] === false) {
          return false;
        }
      }
      return true;
    };
    return filterRows(rowModel.rows, filterRowsImpl, table);
  }, getMemoOptions(table.options, "debugTable", "getFilteredRowModel", () => table._autoResetPageIndex()));
}
function getPaginationRowModel(opts) {
  return (table) => memo(() => [table.getState().pagination, table.getPrePaginationRowModel(), table.options.paginateExpandedRows ? void 0 : table.getState().expanded], (pagination, rowModel) => {
    if (!rowModel.rows.length) {
      return rowModel;
    }
    const {
      pageSize,
      pageIndex
    } = pagination;
    let {
      rows,
      flatRows,
      rowsById
    } = rowModel;
    const pageStart = pageSize * pageIndex;
    const pageEnd = pageStart + pageSize;
    rows = rows.slice(pageStart, pageEnd);
    let paginatedRowModel;
    if (!table.options.paginateExpandedRows) {
      paginatedRowModel = expandRows({
        rows,
        flatRows,
        rowsById
      });
    } else {
      paginatedRowModel = {
        rows,
        flatRows,
        rowsById
      };
    }
    paginatedRowModel.flatRows = [];
    const handleRow = (row) => {
      paginatedRowModel.flatRows.push(row);
      if (row.subRows.length) {
        row.subRows.forEach(handleRow);
      }
    };
    paginatedRowModel.rows.forEach(handleRow);
    return paginatedRowModel;
  }, getMemoOptions(table.options, "debugTable", "getPaginationRowModel"));
}

// ../node_modules/@tanstack/react-table/build/lib/index.mjs
function flexRender(Comp, props) {
  return !Comp ? null : isReactComponent(Comp) ? React.createElement(Comp, props) : Comp;
}
function isReactComponent(component2) {
  return isClassComponent(component2) || typeof component2 === "function" || isExoticComponent(component2);
}
function isClassComponent(component2) {
  return typeof component2 === "function" && (() => {
    const proto = Object.getPrototypeOf(component2);
    return proto.prototype && proto.prototype.isReactComponent;
  })();
}
function isExoticComponent(component2) {
  return typeof component2 === "object" && typeof component2.$$typeof === "symbol" && ["react.memo", "react.forward_ref"].includes(component2.$$typeof.description);
}
function useReactTable(options) {
  const resolvedOptions = {
    state: {},
    // Dummy state
    onStateChange: () => {
    },
    // noop
    renderFallbackValue: null,
    ...options
  };
  const [tableRef] = React.useState(() => ({
    current: createTable(resolvedOptions)
  }));
  const [state2, setState] = React.useState(() => tableRef.current.initialState);
  tableRef.current.setOptions((prev) => ({
    ...prev,
    ...options,
    state: {
      ...state2,
      ...options.state
    },
    // Similarly, we'll maintain both our internal state and any user-provided
    // state.
    onStateChange: (updater) => {
      setState(updater);
      options.onStateChange == null || options.onStateChange(updater);
    }
  }));
  return tableRef.current;
}

// ../node_modules/sanity/lib/_chunks-es/pane.mjs
function DocTitle(props) {
  const $ = (0, import_react_compiler_runtime.c)(21), {
    document: documentValue
  } = props, schema = useSchema();
  let t0;
  $[0] !== documentValue._type || $[1] !== schema ? (t0 = schema.get(documentValue._type), $[0] = documentValue._type, $[1] = schema, $[2] = t0) : t0 = $[2];
  const schemaType = t0, {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  let t1;
  $[3] !== documentValue || $[4] !== schemaType ? (t1 = {
    schemaType,
    value: documentValue
  }, $[3] = documentValue, $[4] = schemaType, $[5] = t1) : t1 = $[5];
  const {
    error,
    value
  } = useDocumentPreview(t1);
  if (!schemaType) {
    let t222;
    $[6] !== documentValue._type || $[7] !== t2 ? (t222 = t2("doc-title.unknown-schema-type.text", {
      schemaType: documentValue._type
    }), $[6] = documentValue._type, $[7] = t2, $[8] = t222) : t222 = $[8];
    let t32;
    return $[9] !== t222 ? (t32 = (0, import_jsx_runtime3.jsx)("code", { children: t222 }), $[9] = t222, $[10] = t32) : t32 = $[10], t32;
  }
  if (error) {
    let t222;
    $[11] !== error.message || $[12] !== t2 ? (t222 = t2("doc-title.error.text", {
      errorMessage: error.message
    }), $[11] = error.message, $[12] = t2, $[13] = t222) : t222 = $[13];
    let t32;
    return $[14] !== t222 ? (t32 = (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children: t222 }), $[14] = t222, $[15] = t32) : t32 = $[15], t32;
  }
  let t22;
  $[16] !== t2 || $[17] !== (value == null ? void 0 : value.title) ? (t22 = (value == null ? void 0 : value.title) || (0, import_jsx_runtime3.jsx)("span", { style: {
    color: "var(--card-muted-fg-color)"
  }, children: t2("doc-title.fallback.text") }), $[16] = t2, $[17] = value == null ? void 0 : value.title, $[18] = t22) : t22 = $[18];
  let t3;
  return $[19] !== t22 ? (t3 = (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children: t22 }), $[19] = t22, $[20] = t3) : t3 = $[20], t3;
}
var ChevronWrapper = dt(Box)`
  margin-left: auto;
`;
var CrossDatasetReferencesDetails = dt.details`
  flex: none;

  &[open] ${ChevronWrapper} {
    transform: rotate(180deg);
  }
`;
var CrossDatasetReferencesSummary = dt.summary`
  list-style: none;

  &::-webkit-details-marker {
    display: none;
  }
`;
var Table$1 = dt.table`
  width: 100%;
  text-align: left;
  padding: 0 ${({
  theme
}) => rem(theme.sanity.space[2])};
  border-collapse: collapse;

  th {
    padding: ${({
  theme
}) => rem(theme.sanity.space[1])};
  }

  td {
    padding: 0 ${({
  theme
}) => rem(theme.sanity.space[1])};
  }

  tr > *:last-child {
    text-align: right;
  }
`;
var DocumentIdFlex = dt(Flex)`
  min-height: 33px;
`;
var OtherReferenceCount = (props) => {
  const $ = (0, import_react_compiler_runtime.c)(13), {
    t: t2
  } = useTranslation(structureLocaleNamespace), difference = props.totalCount - props.references.length;
  if (!difference)
    return null;
  let t0;
  $[0] !== difference || $[1] !== t2 ? (t0 = t2("confirm-delete-dialog.other-reference-count.title", {
    count: difference
  }), $[0] = difference, $[1] = t2, $[2] = t0) : t0 = $[2];
  let t1;
  $[3] !== t0 ? (t1 = (0, import_jsx_runtime3.jsx)(Text, { size: 1, muted: true, children: t0 }), $[3] = t0, $[4] = t1) : t1 = $[4];
  let t22;
  $[5] !== t2 ? (t22 = t2("confirm-delete-dialog.other-reference-count.tooltip"), $[5] = t2, $[6] = t22) : t22 = $[6];
  let t3;
  $[7] === Symbol.for("react.memo_cache_sentinel") ? (t3 = (0, import_jsx_runtime3.jsx)(Text, { size: 1, muted: true, children: (0, import_jsx_runtime3.jsx)(InfoOutlineIcon, {}) }), $[7] = t3) : t3 = $[7];
  let t4;
  $[8] !== t22 ? (t4 = (0, import_jsx_runtime3.jsx)(Tooltip, { portal: true, placement: "top", content: t22, children: t3 }), $[8] = t22, $[9] = t4) : t4 = $[9];
  let t5;
  return $[10] !== t1 || $[11] !== t4 ? (t5 = (0, import_jsx_runtime3.jsx)(Box, { padding: 2, children: (0, import_jsx_runtime3.jsxs)(Inline, { space: 2, children: [
    t1,
    t4
  ] }) }), $[10] = t1, $[11] = t4, $[12] = t5) : t5 = $[12], t5;
};
function PaneItemPreview(props) {
  const $ = (0, import_react_compiler_runtime.c)(27), {
    icon,
    layout,
    presence,
    schemaType,
    value
  } = props, versionsInfo = useDocumentVersionInfo(value._id), {
    perspectiveStack
  } = usePerspective();
  let t0, t1;
  $[0] !== perspectiveStack || $[1] !== props.documentPreviewStore || $[2] !== schemaType || $[3] !== value._id ? (t1 = getPreviewStateObservable$1(props.documentPreviewStore, schemaType, value._id, perspectiveStack), $[0] = perspectiveStack, $[1] = props.documentPreviewStore, $[2] = schemaType, $[3] = value._id, $[4] = t1) : t1 = $[4], t0 = t1;
  const previewStateObservable = t0;
  let t2;
  $[5] === Symbol.for("react.memo_cache_sentinel") ? (t2 = {
    snapshot: null,
    isLoading: true,
    original: null
  }, $[5] = t2) : t2 = $[5];
  const {
    snapshot,
    original,
    isLoading: previewIsLoading
  } = useObservable(previewStateObservable, t2), isLoading = previewIsLoading;
  let t3;
  $[6] !== isLoading || $[7] !== presence || $[8] !== versionsInfo.draft || $[9] !== versionsInfo.published || $[10] !== versionsInfo.versions ? (t3 = isLoading ? null : (0, import_jsx_runtime3.jsx)(TooltipDelayGroupProvider, { children: (0, import_jsx_runtime3.jsxs)(Flex, { align: "center", gap: 3, children: [
    presence && presence.length > 0 && (0, import_jsx_runtime3.jsx)(DocumentPreviewPresence, { presence }),
    (0, import_jsx_runtime3.jsx)(DocumentStatusIndicator, { draft: versionsInfo.draft, published: versionsInfo.published, versions: versionsInfo.versions })
  ] }) }), $[6] = isLoading, $[7] = presence, $[8] = versionsInfo.draft, $[9] = versionsInfo.published, $[10] = versionsInfo.versions, $[11] = t3) : t3 = $[11];
  const status = t3;
  let t4;
  $[12] !== versionsInfo.draft || $[13] !== versionsInfo.published || $[14] !== versionsInfo.versions ? (t4 = (0, import_jsx_runtime3.jsx)(DocumentStatus, { draft: versionsInfo.draft, published: versionsInfo.published, versions: versionsInfo.versions }), $[12] = versionsInfo.draft, $[13] = versionsInfo.published, $[14] = versionsInfo.versions, $[15] = t4) : t4 = $[15];
  const tooltip = t4;
  let t5;
  $[16] !== original || $[17] !== snapshot || $[18] !== value ? (t5 = getPreviewValueWithFallback({
    snapshot,
    original,
    fallback: value
  }), $[16] = original, $[17] = snapshot, $[18] = value, $[19] = t5) : t5 = $[19];
  let t6;
  return $[20] !== icon || $[21] !== isLoading || $[22] !== layout || $[23] !== status || $[24] !== t5 || $[25] !== tooltip ? (t6 = (0, import_jsx_runtime3.jsx)(SanityDefaultPreview, { ...t5, isPlaceholder: isLoading, icon, layout, status, tooltip }), $[20] = icon, $[21] = isLoading, $[22] = layout, $[23] = status, $[24] = t5, $[25] = tooltip, $[26] = t6) : t6 = $[26], t6;
}
var BackLink = (0, import_react3.forwardRef)(function(props, ref) {
  const $ = (0, import_react_compiler_runtime.c)(9), {
    routerPanesState,
    groupIndex
  } = (0, import_react3.useContext)(PaneRouterContext);
  let t0, t1;
  $[0] !== groupIndex || $[1] !== routerPanesState ? (t1 = routerPanesState.slice(0, groupIndex), $[0] = groupIndex, $[1] = routerPanesState, $[2] = t1) : t1 = $[2], t0 = t1;
  const panes = t0;
  let t2, t3;
  $[3] !== panes ? (t3 = {
    panes
  }, $[3] = panes, $[4] = t3) : t3 = $[4], t2 = t3;
  const state2 = t2;
  let t4;
  return $[5] !== props || $[6] !== ref || $[7] !== state2 ? (t4 = (0, import_jsx_runtime3.jsx)(StateLink, { ...props, ref, state: state2 }), $[5] = props, $[6] = ref, $[7] = state2, $[8] = t4) : t4 = $[8], t4;
});
function usePaneLayout() {
  const pane2 = (0, import_react3.useContext)(PaneLayoutContext);
  if (!pane2)
    throw new Error("PaneLayout: missing context value");
  return pane2;
}
var ChildLink = (0, import_react3.forwardRef)(function(props, ref) {
  const $ = (0, import_react_compiler_runtime.c)(19);
  let childId, childParameters, childPayload, rest;
  $[0] !== props ? ({
    childId,
    childPayload,
    childParameters,
    ...rest
  } = props, $[0] = props, $[1] = childId, $[2] = childParameters, $[3] = childPayload, $[4] = rest) : (childId = $[1], childParameters = $[2], childPayload = $[3], rest = $[4]);
  const {
    routerPanesState,
    groupIndex
  } = (0, import_react3.useContext)(PaneRouterContext);
  let t0;
  if ($[5] !== childId || $[6] !== childParameters || $[7] !== childPayload || $[8] !== groupIndex || $[9] !== routerPanesState) {
    let t12;
    $[11] !== childId || $[12] !== childParameters || $[13] !== childPayload ? (t12 = [{
      id: childId,
      params: childParameters,
      payload: childPayload
    }], $[11] = childId, $[12] = childParameters, $[13] = childPayload, $[14] = t12) : t12 = $[14], t0 = {
      panes: [...routerPanesState.slice(0, groupIndex + 1), t12]
    }, $[5] = childId, $[6] = childParameters, $[7] = childPayload, $[8] = groupIndex, $[9] = routerPanesState, $[10] = t0;
  } else
    t0 = $[10];
  let t1;
  return $[15] !== ref || $[16] !== rest || $[17] !== t0 ? (t1 = (0, import_jsx_runtime3.jsx)(StateLink, { ...rest, ref, state: t0 }), $[15] = ref, $[16] = rest, $[17] = t0, $[18] = t1) : t1 = $[18], t1;
});
var ParameterizedLink = (0, import_react3.forwardRef)(function(props, ref) {
  const $ = (0, import_react_compiler_runtime.c)(22), {
    routerPanesState: currentPanes,
    groupIndex,
    siblingIndex
  } = (0, import_react3.useContext)(PaneRouterContext);
  let params, payload, rest;
  $[0] !== props ? ({
    params,
    payload,
    ...rest
  } = props, $[0] = props, $[1] = params, $[2] = payload, $[3] = rest) : (params = $[1], payload = $[2], rest = $[3]);
  const nextParams = useUnique(params), nextPayload = useUnique(payload);
  let t0;
  const currentGroup = currentPanes[groupIndex], currentSibling = currentGroup[siblingIndex], t1 = nextParams ?? currentSibling.params, t2 = nextPayload ?? currentSibling.payload;
  let t3;
  $[4] !== currentSibling || $[5] !== t1 || $[6] !== t2 ? (t3 = {
    ...currentSibling,
    params: t1,
    payload: t2
  }, $[4] = currentSibling, $[5] = t1, $[6] = t2, $[7] = t3) : t3 = $[7];
  const nextSibling = t3;
  let t4;
  $[8] !== currentGroup || $[9] !== nextSibling || $[10] !== siblingIndex ? (t4 = [...currentGroup.slice(0, siblingIndex), nextSibling, ...currentGroup.slice(siblingIndex + 1)], $[8] = currentGroup, $[9] = nextSibling, $[10] = siblingIndex, $[11] = t4) : t4 = $[11];
  const nextGroup = t4;
  let t5;
  $[12] !== currentPanes || $[13] !== groupIndex || $[14] !== nextGroup ? (t5 = [...currentPanes.slice(0, groupIndex), nextGroup, ...currentPanes.slice(groupIndex + 1)], $[12] = currentPanes, $[13] = groupIndex, $[14] = nextGroup, $[15] = t5) : t5 = $[15];
  const nextPanes = t5;
  let t6;
  $[16] !== nextPanes ? (t6 = {
    panes: nextPanes
  }, $[16] = nextPanes, $[17] = t6) : t6 = $[17], t0 = t6;
  const nextState = t0;
  let t7;
  return $[18] !== nextState || $[19] !== ref || $[20] !== rest ? (t7 = (0, import_jsx_runtime3.jsx)(StateLink, { ref, ...rest, state: nextState }), $[18] = nextState, $[19] = ref, $[20] = rest, $[21] = t7) : t7 = $[21], t7;
});
var ReferenceChildLink = (0, import_react3.forwardRef)(function(t0, ref) {
  const $ = (0, import_react_compiler_runtime.c)(22);
  let children, documentId, documentType, parentRefPath, rest, template;
  $[0] !== t0 ? ({
    documentId,
    documentType,
    parentRefPath,
    children,
    template,
    ...rest
  } = t0, $[0] = t0, $[1] = children, $[2] = documentId, $[3] = documentType, $[4] = parentRefPath, $[5] = rest, $[6] = template) : (children = $[1], documentId = $[2], documentType = $[3], parentRefPath = $[4], rest = $[5], template = $[6]);
  const t1 = template == null ? void 0 : template.params;
  let t2;
  $[7] !== parentRefPath ? (t2 = toString(parentRefPath), $[7] = parentRefPath, $[8] = t2) : t2 = $[8];
  let t3;
  $[9] !== template ? (t3 = template && {
    template: template == null ? void 0 : template.id
  }, $[9] = template, $[10] = t3) : t3 = $[10];
  let t4;
  $[11] !== documentType || $[12] !== t2 || $[13] !== t3 ? (t4 = {
    type: documentType,
    parentRefPath: t2,
    ...t3
  }, $[11] = documentType, $[12] = t2, $[13] = t3, $[14] = t4) : t4 = $[14];
  let t5;
  return $[15] !== children || $[16] !== documentId || $[17] !== ref || $[18] !== rest || $[19] !== t1 || $[20] !== t4 ? (t5 = (0, import_jsx_runtime3.jsx)(ChildLink, { ...rest, ref, childId: documentId, childPayload: t1, childParameters: t4, children }), $[15] = children, $[16] = documentId, $[17] = ref, $[18] = rest, $[19] = t1, $[20] = t4, $[21] = t5) : t5 = $[21], t5;
});
function usePaneRouter() {
  return (0, import_react3.useContext)(PaneRouterContext);
}
var EMPTY_ARRAY$1 = [];
function ReferencePreviewLink(props) {
  const $ = (0, import_react_compiler_runtime.c)(16), {
    onClick,
    type: type2,
    value
  } = props, t0 = value == null ? void 0 : value._id;
  let t1;
  $[0] !== t0 ? (t1 = getPublishedId(t0), $[0] = t0, $[1] = t1) : t1 = $[1];
  const documentPresence = useDocumentPresence(t1), documentPreviewStore = useDocumentPreviewStore(), {
    ReferenceChildLink: ReferenceChildLink2
  } = usePaneRouter();
  let t2;
  $[2] !== ReferenceChildLink2 || $[3] !== (type2 == null ? void 0 : type2.name) || $[4] !== (value == null ? void 0 : value._id) ? (t2 = function(linkProps) {
    return (0, import_jsx_runtime3.jsx)(ReferenceChildLink2, { documentId: value == null ? void 0 : value._id, documentType: type2 == null ? void 0 : type2.name, parentRefPath: EMPTY_ARRAY$1, ...linkProps });
  }, $[2] = ReferenceChildLink2, $[3] = type2 == null ? void 0 : type2.name, $[4] = value == null ? void 0 : value._id, $[5] = t2) : t2 = $[5];
  const t3 = t2, t4 = type2 == null ? void 0 : type2.icon, t5 = (documentPresence == null ? void 0 : documentPresence.length) > 0 ? documentPresence : EMPTY_ARRAY$1;
  let t6;
  $[6] !== documentPreviewStore || $[7] !== t4 || $[8] !== t5 || $[9] !== type2 || $[10] !== value ? (t6 = (0, import_jsx_runtime3.jsx)(PaneItemPreview, { documentPreviewStore, icon: t4, layout: "compact", presence: t5, schemaType: type2, value }), $[6] = documentPreviewStore, $[7] = t4, $[8] = t5, $[9] = type2, $[10] = value, $[11] = t6) : t6 = $[11];
  let t7;
  return $[12] !== onClick || $[13] !== t3 || $[14] !== t6 ? (t7 = (0, import_jsx_runtime3.jsx)(PreviewCard, { __unstable_focusRing: true, as: t3, "data-as": "a", onClick, radius: 2, children: t6 }), $[12] = onClick, $[13] = t3, $[14] = t6, $[15] = t7) : t7 = $[15], t7;
}
function ConfirmDeleteDialogBody(t0) {
  const $ = (0, import_react_compiler_runtime.c)(78), {
    crossDatasetReferences,
    internalReferences,
    documentTitle,
    totalCount,
    action,
    datasetNames,
    hasUnknownDatasetNames,
    onReferenceLinkClick
  } = t0, schema = useSchema(), toast = useToast(), {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  let t1;
  $[0] !== onReferenceLinkClick || $[1] !== schema || $[2] !== t2 ? (t1 = (item) => {
    const type2 = schema.get(item._type);
    return type2 ? (0, import_jsx_runtime3.jsx)(ReferencePreviewLink, { type: type2, value: item, onClick: onReferenceLinkClick }) : (0, import_jsx_runtime3.jsx)(Box, { padding: 2, children: (0, import_jsx_runtime3.jsx)(SanityDefaultPreview, { icon: UnknownIcon, title: t2("confirm-delete-dialog.preview-item.preview-unavailable.title"), subtitle: t2("confirm-delete-dialog.preview-item.preview-unavailable.subtitle", {
      documentId: item._id
    }), layout: "default" }) });
  }, $[0] = onReferenceLinkClick, $[1] = schema, $[2] = t2, $[3] = t1) : t1 = $[3];
  const renderPreviewItem = t1;
  if ((internalReferences == null ? void 0 : internalReferences.totalCount) === 0 && (crossDatasetReferences == null ? void 0 : crossDatasetReferences.totalCount) === 0) {
    let t222;
    $[4] !== documentTitle ? (t222 = {
      DocumentTitle: () => (0, import_jsx_runtime3.jsx)("strong", { children: documentTitle })
    }, $[4] = documentTitle, $[5] = t222) : t222 = $[5];
    let t32;
    return $[6] !== action || $[7] !== t2 || $[8] !== t222 ? (t32 = (0, import_jsx_runtime3.jsx)(Text, { as: "p", size: 1, children: (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey: "confirm-delete-dialog.confirmation.text", context: action, components: t222 }) }), $[6] = action, $[7] = t2, $[8] = t222, $[9] = t32) : t32 = $[9], t32;
  }
  let T0, T1, T2, t10, t11, t12, t22, t3, t4, t5, t6, t7, t8, t9;
  if ($[10] !== action || $[11] !== crossDatasetReferences || $[12] !== datasetNames || $[13] !== documentTitle || $[14] !== hasUnknownDatasetNames || $[15] !== internalReferences || $[16] !== renderPreviewItem || $[17] !== t2 || $[18] !== toast || $[19] !== totalCount) {
    const normalizedDatasetNames = [...datasetNames, ...hasUnknownDatasetNames ? ["unavailable"] : []], datasetSubtitle = t2("confirm-delete-dialog.cdr-summary.subtitle", {
      count: normalizedDatasetNames.length,
      datasets: normalizedDatasetNames.join(", "),
      context: hasUnknownDatasetNames && normalizedDatasetNames.length ? "unavailable" : ""
    });
    T2 = Flex, t9 = "column", t10 = 4;
    let t132;
    $[34] === Symbol.for("react.memo_cache_sentinel") ? (t132 = (0, import_jsx_runtime3.jsx)(Text, { "aria-hidden": "true", size: 1, children: (0, import_jsx_runtime3.jsx)(WarningOutlineIcon, {}) }), $[34] = t132) : t132 = $[34];
    let t142;
    $[35] !== documentTitle ? (t142 = {
      DocumentTitle: () => documentTitle
    }, $[35] = documentTitle, $[36] = t142) : t142 = $[36];
    let t152;
    $[37] !== totalCount ? (t152 = {
      count: totalCount
    }, $[37] = totalCount, $[38] = t152) : t152 = $[38], $[39] !== t2 || $[40] !== t142 || $[41] !== t152 ? (t11 = (0, import_jsx_runtime3.jsx)(Card, { padding: 3, radius: 2, tone: "caution", flex: "none", children: (0, import_jsx_runtime3.jsxs)(Flex, { children: [
      t132,
      (0, import_jsx_runtime3.jsx)(Box, { flex: 1, marginLeft: 3, children: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: (0, import_jsx_runtime3.jsx)(Translate, { i18nKey: "confirm-delete-dialog.referring-document-count.text", components: t142, t: t2, values: t152 }) }) })
    ] }) }), $[39] = t2, $[40] = t142, $[41] = t152, $[42] = t11) : t11 = $[42];
    let t162;
    $[43] !== documentTitle ? (t162 = {
      DocumentTitle: () => documentTitle
    }, $[43] = documentTitle, $[44] = t162) : t162 = $[44], $[45] !== action || $[46] !== t2 || $[47] !== t162 ? (t12 = (0, import_jsx_runtime3.jsx)(Box, { flex: "none", children: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: (0, import_jsx_runtime3.jsx)(Translate, { i18nKey: "confirm-delete-dialog.referring-documents-descriptor.text", t: t2, context: action, components: t162 }) }) }), $[45] = action, $[46] = t2, $[47] = t162, $[48] = t12) : t12 = $[48], T1 = Card, t5 = 2, t6 = 1, t7 = "auto", t8 = 2, T0 = Flex, t22 = "column", $[49] !== internalReferences || $[50] !== renderPreviewItem ? (t3 = internalReferences.totalCount > 0 && (0, import_jsx_runtime3.jsxs)(Stack, { as: "ul", marginBottom: 2, space: 2, "data-testid": "internal-references", children: [
      internalReferences == null ? void 0 : internalReferences.references.map((item_0) => (0, import_jsx_runtime3.jsx)(Box, { as: "li", children: renderPreviewItem(item_0) }, item_0._id)),
      internalReferences.totalCount > internalReferences.references.length && (0, import_jsx_runtime3.jsx)(Box, { as: "li", padding: 3, children: (0, import_jsx_runtime3.jsx)(OtherReferenceCount, { ...internalReferences }) })
    ] }), $[49] = internalReferences, $[50] = renderPreviewItem, $[51] = t3) : t3 = $[51], t4 = crossDatasetReferences.totalCount > 0 && (0, import_jsx_runtime3.jsxs)(CrossDatasetReferencesDetails, { "data-testid": "cross-dataset-references", style: {
      borderTop: internalReferences.totalCount > 0 ? "1px solid var(--card-shadow-outline-color)" : void 0
    }, children: [
      (0, import_jsx_runtime3.jsx)(CrossDatasetReferencesSummary, { children: (0, import_jsx_runtime3.jsx)(Card, { as: "a", marginTop: internalReferences.totalCount > 0 ? 2 : 0, radius: 2, shadow: 1, paddingY: 1, children: (0, import_jsx_runtime3.jsxs)(Flex, { align: "center", gap: 3, paddingX: 3, paddingY: 1, children: [
        (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: (0, import_jsx_runtime3.jsx)(DocumentsIcon, {}) }),
        (0, import_jsx_runtime3.jsxs)(Stack, { space: 2, children: [
          (0, import_jsx_runtime3.jsx)(Text, { textOverflow: "ellipsis", size: 1, children: t2("confirm-delete-dialog.cdr-summary.title", {
            count: normalizedDatasetNames.length,
            documentCount: t2("confirm-delete-dialog.cdr-summary.document-count", {
              count: crossDatasetReferences.totalCount
            })
          }) }),
          (0, import_jsx_runtime3.jsx)(Text, { title: datasetSubtitle, textOverflow: "ellipsis", size: 1, muted: true, children: datasetSubtitle })
        ] }),
        (0, import_jsx_runtime3.jsx)(ChevronWrapper, { children: (0, import_jsx_runtime3.jsx)(Text, { muted: true, size: 1, children: (0, import_jsx_runtime3.jsx)(ChevronDownIcon, {}) }) })
      ] }) }) }),
      (0, import_jsx_runtime3.jsxs)(Box, { overflow: "auto", paddingTop: 2, children: [
        (0, import_jsx_runtime3.jsxs)(Table$1, { children: [
          (0, import_jsx_runtime3.jsx)("thead", { children: (0, import_jsx_runtime3.jsxs)("tr", { children: [
            (0, import_jsx_runtime3.jsx)("th", { children: (0, import_jsx_runtime3.jsx)(Text, { muted: true, size: 1, style: {
              minWidth: "5rem"
            }, weight: "medium", children: t2("confirm-delete-dialog.cdr-table.project-id.label") }) }),
            (0, import_jsx_runtime3.jsx)("th", { children: (0, import_jsx_runtime3.jsx)(Text, { muted: true, size: 1, weight: "medium", children: t2("confirm-delete-dialog.cdr-table.dataset.label") }) }),
            (0, import_jsx_runtime3.jsx)("th", { children: (0, import_jsx_runtime3.jsx)(Text, { muted: true, size: 1, weight: "medium", children: t2("confirm-delete-dialog.cdr-table.document-id.label") }) })
          ] }) }),
          (0, import_jsx_runtime3.jsx)("tbody", { children: crossDatasetReferences.references.filter(_temp$q).map((t172, index) => {
            const {
              projectId,
              datasetName,
              documentId
            } = t172;
            return (0, import_jsx_runtime3.jsxs)("tr", { children: [
              (0, import_jsx_runtime3.jsx)("td", { children: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: projectId }) }),
              (0, import_jsx_runtime3.jsx)("td", { children: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: datasetName || "unavailable" }) }),
              (0, import_jsx_runtime3.jsx)("td", { children: (0, import_jsx_runtime3.jsxs)(DocumentIdFlex, { align: "center", gap: 2, justify: "flex-end", children: [
                (0, import_jsx_runtime3.jsx)(Text, { textOverflow: "ellipsis", size: 1, children: documentId || "unavailable" }),
                documentId && (0, import_jsx_runtime3.jsx)(Button2, { mode: "bleed", icon: CopyIcon, tooltipProps: {
                  content: t2("confirm-delete-dialog.cdr-table.copy-id-button.tooltip")
                }, onClick: () => {
                  navigator.clipboard.writeText(documentId).catch(() => {
                    toast.push({
                      status: "error",
                      title: t2("confirm-delete-dialog.cdr-table.id-copied-toast.title-failed")
                    });
                  });
                } })
              ] }) })
            ] }, `${documentId}-${index}`);
          }) })
        ] }),
        (0, import_jsx_runtime3.jsx)(OtherReferenceCount, { ...crossDatasetReferences })
      ] })
    ] }), $[10] = action, $[11] = crossDatasetReferences, $[12] = datasetNames, $[13] = documentTitle, $[14] = hasUnknownDatasetNames, $[15] = internalReferences, $[16] = renderPreviewItem, $[17] = t2, $[18] = toast, $[19] = totalCount, $[20] = T0, $[21] = T1, $[22] = T2, $[23] = t10, $[24] = t11, $[25] = t12, $[26] = t22, $[27] = t3, $[28] = t4, $[29] = t5, $[30] = t6, $[31] = t7, $[32] = t8, $[33] = t9;
  } else
    T0 = $[20], T1 = $[21], T2 = $[22], t10 = $[23], t11 = $[24], t12 = $[25], t22 = $[26], t3 = $[27], t4 = $[28], t5 = $[29], t6 = $[30], t7 = $[31], t8 = $[32], t9 = $[33];
  let t13;
  $[52] !== T0 || $[53] !== t22 || $[54] !== t3 || $[55] !== t4 ? (t13 = (0, import_jsx_runtime3.jsxs)(T0, { direction: t22, children: [
    t3,
    t4
  ] }), $[52] = T0, $[53] = t22, $[54] = t3, $[55] = t4, $[56] = t13) : t13 = $[56];
  let t14;
  $[57] !== T1 || $[58] !== t13 || $[59] !== t5 || $[60] !== t6 || $[61] !== t7 || $[62] !== t8 ? (t14 = (0, import_jsx_runtime3.jsx)(T1, { radius: t5, shadow: t6, flex: t7, padding: t8, children: t13 }), $[57] = T1, $[58] = t13, $[59] = t5, $[60] = t6, $[61] = t7, $[62] = t8, $[63] = t14) : t14 = $[63];
  let t15;
  $[64] !== documentTitle ? (t15 = {
    DocumentTitle: () => documentTitle
  }, $[64] = documentTitle, $[65] = t15) : t15 = $[65];
  let t16;
  $[66] !== action || $[67] !== t2 || $[68] !== t15 ? (t16 = (0, import_jsx_runtime3.jsx)(Box, { flex: "none", children: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: (0, import_jsx_runtime3.jsx)(Translate, { i18nKey: "confirm-delete-dialog.referential-integrity-disclaimer.text", t: t2, context: action, components: t15 }) }) }), $[66] = action, $[67] = t2, $[68] = t15, $[69] = t16) : t16 = $[69];
  let t17;
  return $[70] !== T2 || $[71] !== t10 || $[72] !== t11 || $[73] !== t12 || $[74] !== t14 || $[75] !== t16 || $[76] !== t9 ? (t17 = (0, import_jsx_runtime3.jsxs)(T2, { direction: t9, gap: t10, children: [
    t11,
    t12,
    t14,
    t16
  ] }), $[70] = T2, $[71] = t10, $[72] = t11, $[73] = t12, $[74] = t14, $[75] = t16, $[76] = t9, $[77] = t17) : t17 = $[77], t17;
}
function _temp$q(reference) {
  return "projectId" in reference;
}
function isClientError(e2) {
  return typeof e2 != "object" || !e2 ? false : "statusCode" in e2 && "response" in e2;
}
var POLL_INTERVAL = 5e3;
var visiblePoll$;
var getVisiblePoll$ = () => (visiblePoll$ || (visiblePoll$ = fromEvent(document, "visibilitychange").pipe(
  // add empty emission to have this fire on creation
  startWith(null),
  map(() => document.visibilityState === "visible"),
  distinctUntilChanged(),
  switchMap((visible) => visible ? (
    // using timer instead of interval since timer will emit on creation
    timer(0, POLL_INTERVAL)
  ) : EMPTY),
  shareReplay({
    refCount: true,
    bufferSize: 1
  })
)), visiblePoll$);
function getDocumentExistence(documentId, {
  versionedClient
}) {
  const draftId = getDraftId(documentId), publishedId = getPublishedId(documentId), requestOptions = {
    uri: versionedClient.getDataUrl("doc", `${draftId},${publishedId}`),
    json: true,
    query: {
      excludeContent: "true"
    },
    tag: "use-referring-documents.document-existence"
  };
  return versionedClient.observable.request(requestOptions).pipe(map(({
    omitted
  }) => {
    const nonExistant = omitted.filter((doc) => doc.reason === "existence");
    if (nonExistant.length !== 2)
      return nonExistant.length === 0 || nonExistant.some((doc) => doc.id === draftId) ? publishedId : draftId;
  }));
}
function fetchCrossDatasetReferences(documentId, context) {
  const {
    versionedClient
  } = context;
  return getVisiblePoll$().pipe(switchMap(() => getDocumentExistence(documentId, context)), switchMap((checkDocumentId) => {
    if (!checkDocumentId)
      return of({
        totalCount: 0,
        references: []
      });
    const currentDataset = versionedClient.config().dataset;
    return versionedClient.observable.request({
      url: `/data/references/${currentDataset}/documents/${checkDocumentId}/to?excludeInternalReferences=true&excludePaths=true`,
      tag: "use-referring-documents.external"
    }).pipe(catchError((e2) => {
      if (isClientError(e2) && e2.statusCode === 404)
        return of({
          totalCount: 0,
          references: []
        });
      throw e2;
    }));
  }));
}
var useInternalReferences = createHookFromObservableFactory(([documentId, documentStore]) => documentStore.listenQuery({
  fetch: '{"references":*[references($documentId)][0...100]{_id,_type},"totalCount":count(*[references($documentId)])}',
  listen: "*[references($documentId)]"
}, {
  documentId
}, {
  tag: "use-referring-documents",
  transitions: ["appear", "disappear"],
  throttleTime: 5e3
}));
var useCrossDatasetReferences = createHookFromObservableFactory(([documentId, versionedClient]) => getVisiblePoll$().pipe(switchMap(() => fetchCrossDatasetReferences(documentId, {
  versionedClient
}))));
function useReferringDocuments(documentId) {
  const $ = (0, import_react_compiler_runtime.c)(24), versionedClient = useClient(DEFAULT_STUDIO_CLIENT_OPTIONS), documentStore = useDocumentStore();
  let t0;
  $[0] !== documentId ? (t0 = getPublishedId(documentId), $[0] = documentId, $[1] = t0) : t0 = $[1];
  const publishedId = t0;
  let t1, t2;
  $[2] !== documentStore || $[3] !== publishedId ? (t2 = [publishedId, documentStore], $[2] = documentStore, $[3] = publishedId, $[4] = t2) : t2 = $[4], t1 = t2;
  const [internalReferences, isInternalReferencesLoading] = useInternalReferences(t1);
  let t3, t4;
  $[5] !== publishedId || $[6] !== versionedClient ? (t4 = [publishedId, versionedClient], $[5] = publishedId, $[6] = versionedClient, $[7] = t4) : t4 = $[7], t3 = t4;
  const [crossDatasetReferences, isCrossDatasetReferencesLoading] = useCrossDatasetReferences(t3);
  let t5;
  $[8] !== (crossDatasetReferences == null ? void 0 : crossDatasetReferences.references) ? (t5 = () => Array.from(new Set(crossDatasetReferences == null ? void 0 : crossDatasetReferences.references.map(_temp$p).filter(Boolean))).sort(), $[8] = crossDatasetReferences == null ? void 0 : crossDatasetReferences.references, $[9] = t5) : t5 = $[9];
  let t6;
  $[10] !== t5 ? (t6 = t5(), $[10] = t5, $[11] = t6) : t6 = $[11];
  const projectIds = t6;
  let t7, t8;
  $[12] !== (crossDatasetReferences == null ? void 0 : crossDatasetReferences.references) ? (t8 = Array.from(new Set(crossDatasetReferences == null ? void 0 : crossDatasetReferences.references.map(_temp2$9).filter(_temp3$2))).sort(), $[12] = crossDatasetReferences == null ? void 0 : crossDatasetReferences.references, $[13] = t8) : t8 = $[13], t7 = t8;
  const datasetNames = t7;
  let t9;
  $[14] !== (crossDatasetReferences == null ? void 0 : crossDatasetReferences.references) ? (t9 = () => !!(crossDatasetReferences == null ? void 0 : crossDatasetReferences.references.some(_temp4$2)), $[14] = crossDatasetReferences == null ? void 0 : crossDatasetReferences.references, $[15] = t9) : t9 = $[15];
  const hasUnknownDatasetNames = t9(), t10 = ((internalReferences == null ? void 0 : internalReferences.totalCount) || 0) + ((crossDatasetReferences == null ? void 0 : crossDatasetReferences.totalCount) || 0), t11 = isInternalReferencesLoading || isCrossDatasetReferencesLoading;
  let t12;
  return $[16] !== crossDatasetReferences || $[17] !== datasetNames || $[18] !== hasUnknownDatasetNames || $[19] !== internalReferences || $[20] !== projectIds || $[21] !== t10 || $[22] !== t11 ? (t12 = {
    totalCount: t10,
    projectIds,
    datasetNames,
    hasUnknownDatasetNames,
    internalReferences,
    crossDatasetReferences,
    isLoading: t11
  }, $[16] = crossDatasetReferences, $[17] = datasetNames, $[18] = hasUnknownDatasetNames, $[19] = internalReferences, $[20] = projectIds, $[21] = t10, $[22] = t11, $[23] = t12) : t12 = $[23], t12;
}
function _temp4$2(crossDatasetReference_1) {
  return typeof crossDatasetReference_1.datasetName != "string";
}
function _temp3$2(datasetName) {
  return !!datasetName && datasetName !== "";
}
function _temp2$9(crossDatasetReference_0) {
  return (crossDatasetReference_0 == null ? void 0 : crossDatasetReference_0.datasetName) || "";
}
function _temp$p(crossDatasetReference) {
  return crossDatasetReference.projectId;
}
var DialogBody$1 = dt(Box)`
  box-sizing: border-box;
`;
var LoadingContainer = dt(Flex).attrs({
  align: "center",
  direction: "column",
  justify: "center"
})`
  height: 110px;
`;
function ConfirmDeleteDialog(t0) {
  const $ = (0, import_react_compiler_runtime.c)(40), {
    id: id2,
    type: type2,
    action: t1,
    onCancel,
    onConfirm
  } = t0, action = t1 === void 0 ? "delete" : t1, {
    t: t2
  } = useTranslation(structureLocaleNamespace), dialogId = `deletion-confirmation-${(0, import_react3.useId)()}`, {
    internalReferences,
    crossDatasetReferences,
    isLoading,
    totalCount,
    projectIds,
    datasetNames,
    hasUnknownDatasetNames
  } = useReferringDocuments(id2);
  let t22, t3;
  $[0] !== id2 || $[1] !== type2 ? (t3 = {
    _id: id2,
    _type: type2
  }, $[0] = id2, $[1] = type2, $[2] = t3) : t3 = $[2], t22 = t3;
  let t4;
  $[3] !== t22 ? (t4 = (0, import_jsx_runtime3.jsx)(DocTitle, { document: t22 }), $[3] = t22, $[4] = t4) : t4 = $[4];
  const documentTitle = t4, showConfirmButton = !isLoading;
  let t5;
  $[5] !== action || $[6] !== t2 ? (t5 = t2("confirm-delete-dialog.header.text", {
    context: action
  }), $[5] = action, $[6] = t2, $[7] = t5) : t5 = $[7];
  let t6;
  $[8] !== t2 ? (t6 = t2("confirm-delete-dialog.cancel-button.text"), $[8] = t2, $[9] = t6) : t6 = $[9];
  let t7;
  $[10] !== onCancel || $[11] !== t6 ? (t7 = {
    onClick: onCancel,
    text: t6
  }, $[10] = onCancel, $[11] = t6, $[12] = t7) : t7 = $[12];
  let t8;
  $[13] !== action || $[14] !== onConfirm || $[15] !== showConfirmButton || $[16] !== t2 || $[17] !== totalCount ? (t8 = showConfirmButton ? {
    text: totalCount > 0 ? t2("confirm-delete-dialog.confirm-anyway-button.text", {
      context: action
    }) : t2("confirm-delete-dialog.confirm-button.text", {
      context: action
    }),
    onClick: onConfirm
  } : void 0, $[13] = action, $[14] = onConfirm, $[15] = showConfirmButton, $[16] = t2, $[17] = totalCount, $[18] = t8) : t8 = $[18];
  let t9;
  $[19] !== t7 || $[20] !== t8 ? (t9 = {
    cancelButton: t7,
    confirmButton: t8
  }, $[19] = t7, $[20] = t8, $[21] = t9) : t9 = $[21];
  let t10;
  $[22] !== action || $[23] !== crossDatasetReferences || $[24] !== datasetNames || $[25] !== documentTitle || $[26] !== hasUnknownDatasetNames || $[27] !== internalReferences || $[28] !== isLoading || $[29] !== onCancel || $[30] !== projectIds || $[31] !== t2 || $[32] !== totalCount ? (t10 = (0, import_jsx_runtime3.jsx)(DialogBody$1, { children: crossDatasetReferences && internalReferences && !isLoading ? (0, import_jsx_runtime3.jsx)(ConfirmDeleteDialogBody, { crossDatasetReferences, internalReferences, documentTitle, isLoading, totalCount, action, projectIds, datasetNames, hasUnknownDatasetNames, onReferenceLinkClick: onCancel }) : (0, import_jsx_runtime3.jsx)(LoadingContainer, { "data-testid": "loading-container", children: (0, import_jsx_runtime3.jsx)(LoadingBlock, { showText: true, title: t2("confirm-delete-dialog.loading.text") }) }) }), $[22] = action, $[23] = crossDatasetReferences, $[24] = datasetNames, $[25] = documentTitle, $[26] = hasUnknownDatasetNames, $[27] = internalReferences, $[28] = isLoading, $[29] = onCancel, $[30] = projectIds, $[31] = t2, $[32] = totalCount, $[33] = t10) : t10 = $[33];
  let t11;
  return $[34] !== dialogId || $[35] !== onCancel || $[36] !== t10 || $[37] !== t5 || $[38] !== t9 ? (t11 = (0, import_jsx_runtime3.jsx)(Dialog2, { width: 1, id: dialogId, header: t5, footer: t9, onClose: onCancel, onClickOutside: onCancel, children: t10 }), $[34] = dialogId, $[35] = onCancel, $[36] = t10, $[37] = t5, $[38] = t9, $[39] = t11) : t11 = $[39], t11;
}
function ConfirmDeleteDialogContainer(props) {
  const $ = (0, import_react_compiler_runtime.c)(6), {
    t: t2
  } = useTranslation(structureLocaleNamespace), id2 = (0, import_react3.useId)(), [error, setError] = (0, import_react3.useState)(null);
  let t0;
  $[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = () => setError(null), $[0] = t0) : t0 = $[0];
  const handleRetry = t0;
  let t1;
  return $[1] !== error || $[2] !== id2 || $[3] !== props || $[4] !== t2 ? (t1 = error ? (0, import_jsx_runtime3.jsx)(Dialog2, { id: `dialog-error-${id2}`, "data-testid": "confirm-delete-error-dialog", header: t2("confirm-delete-dialog.error.title.text"), footer: {
    confirmButton: {
      text: t2("confirm-delete-dialog.error.retry-button.text"),
      onClick: handleRetry,
      tone: "default"
    }
  }, onClose: props.onCancel, children: (0, import_jsx_runtime3.jsx)(Box, { padding: 4, children: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: t2("confirm-delete-dialog.error.message.text") }) }) }) : (0, import_jsx_runtime3.jsx)(ErrorBoundary, { onCatch: setError, children: (0, import_jsx_runtime3.jsx)(ConfirmDeleteDialog, { ...props }) }), $[1] = error, $[2] = id2, $[3] = props, $[4] = t2, $[5] = t1) : t1 = $[5], t1;
}
function Delay(t0) {
  const $ = (0, import_react_compiler_runtime.c)(6), {
    children,
    ms: t1
  } = t0, ms = t1 === void 0 ? 0 : t1, [ready, setReady] = (0, import_react3.useState)(ms <= 0);
  let t2, t3;
  if ($[0] !== ms ? (t2 = () => {
    if (ms <= 0)
      return;
    const timeoutId = setTimeout(() => setReady(true), ms);
    return () => {
      clearTimeout(timeoutId);
    };
  }, t3 = [ms], $[0] = ms, $[1] = t2, $[2] = t3) : (t2 = $[1], t3 = $[2]), (0, import_react3.useEffect)(t2, t3), !ready || !children) {
    let t42;
    return $[3] === Symbol.for("react.memo_cache_sentinel") ? (t42 = (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, {}), $[3] = t42) : t42 = $[3], t42;
  }
  let t4;
  return $[4] !== children ? (t4 = typeof children == "function" ? children() : children, $[4] = children, $[5] = t4) : t4 = $[5], t4;
}
var PANE_DEBUG = false;
var PANE_COLLAPSED_WIDTH = 51;
var PANE_DEFAULT_MIN_WIDTH = 204;
var Root$9 = dt(Layer)`
  position: relative;
  width: 1px;
  min-width: 1px;

  &:before {
    content: '';
    display: block;
    position: absolute;
    top: 0;
    left: 0;
    bottom: 0;
    width: 1px;
    background-color: var(--card-border-color);
  }

  &:not([data-disabled]) {
    cursor: ew-resize;
    width: 9px;
    min-width: 9px;
    margin: 0 -4px;

    &:before {
      left: 4px;
    }

    &:after {
      content: '';
      display: block;
      position: absolute;
      top: 0;
      left: 0;
      width: 9px;
      bottom: 0;
      background-color: var(--card-border-color);
      opacity: 0;
      transition: opacity 150ms;
    }

    &[data-dragging]:after,
    &:hover:after {
      opacity: 0.2;
    }
  }
`;
function PaneDivider(t0) {
  const $ = (0, import_react_compiler_runtime.c)(7), {
    disabled,
    element
  } = t0, {
    resize
  } = usePaneLayout(), [dragging, setDragging] = (0, import_react3.useState)(false);
  let t1;
  $[0] !== element || $[1] !== resize ? (t1 = (event) => {
    if (!element)
      return;
    setDragging(true), event.preventDefault();
    const startX = event.pageX;
    resize("start", element, 0);
    const handleMouseMove = (e2) => {
      e2.preventDefault();
      const deltaX = e2.pageX - startX;
      resize("move", element, deltaX);
    }, handleMouseUp = (e_0) => {
      e_0.preventDefault(), setDragging(false), window.removeEventListener("mousemove", handleMouseMove), window.removeEventListener("mouseup", handleMouseUp), resize("end", element, 0);
    };
    window.addEventListener("mousemove", handleMouseMove), window.addEventListener("mouseup", handleMouseUp);
  }, $[0] = element, $[1] = resize, $[2] = t1) : t1 = $[2];
  const handleMouseDown = t1, t2 = disabled ? "" : void 0, t3 = dragging ? "" : void 0;
  let t4;
  return $[3] !== handleMouseDown || $[4] !== t2 || $[5] !== t3 ? (t4 = (0, import_jsx_runtime3.jsx)(Root$9, { "data-disabled": t2, "data-dragging": t3, onMouseDown: handleMouseDown }), $[3] = handleMouseDown, $[4] = t2, $[5] = t3, $[6] = t4) : t4 = $[6], t4;
}
var Root$8 = dt(Card)`
  outline: none;

  // NOTE: This will render a border to the right side of each pane
  // without taking up physical space.
  box-shadow: 1px 0 0 var(--card-border-color);
`;
var Pane = (0, import_react3.forwardRef)(function(props, forwardedRef) {
  const $ = (0, import_react_compiler_runtime.c)(86);
  let children, currentMaxWidthProp, currentMinWidthProp, id2, maxWidthProp, minWidthProp, restProps, t0, t1;
  $[0] !== props ? ({
    children,
    currentMinWidth: currentMinWidthProp,
    currentMaxWidth: currentMaxWidthProp,
    flex: t0,
    id: id2,
    minWidth: minWidthProp,
    maxWidth: maxWidthProp,
    selected: t1,
    ...restProps
  } = props, $[0] = props, $[1] = children, $[2] = currentMaxWidthProp, $[3] = currentMinWidthProp, $[4] = id2, $[5] = maxWidthProp, $[6] = minWidthProp, $[7] = restProps, $[8] = t0, $[9] = t1) : (children = $[1], currentMaxWidthProp = $[2], currentMinWidthProp = $[3], id2 = $[4], maxWidthProp = $[5], minWidthProp = $[6], restProps = $[7], t0 = $[8], t1 = $[9]);
  const flexProp = t0 === void 0 ? 1 : t0, selected = t1 === void 0 ? false : t1, [rootElement, setRootElement] = (0, import_react3.useState)(null), {
    collapse,
    collapsed: layoutCollapsed,
    expand,
    expandedElement,
    mount,
    panes
  } = usePaneLayout();
  let pane2, t2;
  if ($[10] !== panes || $[11] !== rootElement) {
    let t32;
    $[14] !== rootElement ? (t32 = (p) => p.element === rootElement, $[14] = rootElement, $[15] = t32) : t32 = $[15], pane2 = panes.find(t32), t2 = pane2 && panes.indexOf(pane2), $[10] = panes, $[11] = rootElement, $[12] = pane2, $[13] = t2;
  } else
    pane2 = $[12], t2 = $[13];
  const paneIndex = t2, nextPane = typeof paneIndex == "number" ? panes[paneIndex + 1] : void 0, isLast = paneIndex === panes.length - 1, expanded = expandedElement === rootElement, collapsed = layoutCollapsed ? false : (pane2 == null ? void 0 : pane2.collapsed) || false, nextCollapsed = (nextPane == null ? void 0 : nextPane.collapsed) || false, ref = (0, import_react3.useRef)(null), flex = (pane2 == null ? void 0 : pane2.flex) ?? flexProp, currentMinWidth = (pane2 == null ? void 0 : pane2.currentMinWidth) ?? currentMinWidthProp, currentMaxWidth = (pane2 == null ? void 0 : pane2.currentMaxWidth) ?? currentMaxWidthProp;
  let t3;
  $[16] === Symbol.for("react.memo_cache_sentinel") ? (t3 = () => ref.current, $[16] = t3) : t3 = $[16], (0, import_react3.useImperativeHandle)(forwardedRef, t3);
  let t4;
  $[17] === Symbol.for("react.memo_cache_sentinel") ? (t4 = (refValue) => {
    setRootElement(refValue), ref.current = refValue;
  }, $[17] = t4) : t4 = $[17];
  const setRef = t4;
  let t5, t6;
  $[18] !== currentMaxWidthProp || $[19] !== currentMinWidthProp || $[20] !== flexProp || $[21] !== id2 || $[22] !== maxWidthProp || $[23] !== minWidthProp || $[24] !== mount || $[25] !== rootElement ? (t5 = () => {
    if (rootElement)
      return mount(rootElement, {
        currentMinWidth: currentMinWidthProp,
        currentMaxWidth: currentMaxWidthProp,
        flex: flexProp,
        id: id2,
        minWidth: minWidthProp,
        maxWidth: maxWidthProp
      });
  }, t6 = [currentMinWidthProp, currentMaxWidthProp, flexProp, id2, minWidthProp, maxWidthProp, mount, rootElement], $[18] = currentMaxWidthProp, $[19] = currentMinWidthProp, $[20] = flexProp, $[21] = id2, $[22] = maxWidthProp, $[23] = minWidthProp, $[24] = mount, $[25] = rootElement, $[26] = t5, $[27] = t6) : (t5 = $[26], t6 = $[27]), (0, import_react3.useLayoutEffect)(t5, t6);
  let t7;
  $[28] !== collapse || $[29] !== rootElement ? (t7 = () => {
    rootElement && collapse(rootElement);
  }, $[28] = collapse, $[29] = rootElement, $[30] = t7) : t7 = $[30];
  const handleCollapse = t7;
  let t8;
  $[31] !== expand || $[32] !== rootElement ? (t8 = () => {
    rootElement && expand(rootElement);
  }, $[31] = expand, $[32] = rootElement, $[33] = t8) : t8 = $[33];
  const handleExpand = t8;
  let t9;
  const t10 = layoutCollapsed ? false : collapsed;
  let t11;
  $[34] !== handleCollapse || $[35] !== handleExpand || $[36] !== isLast || $[37] !== paneIndex || $[38] !== rootElement || $[39] !== t10 ? (t11 = {
    collapse: handleCollapse,
    collapsed: t10,
    expand: handleExpand,
    index: paneIndex,
    isLast,
    rootElement
  }, $[34] = handleCollapse, $[35] = handleExpand, $[36] = isLast, $[37] = paneIndex, $[38] = rootElement, $[39] = t10, $[40] = t11) : t11 = $[40], t9 = t11;
  const contextValue = t9;
  let t12;
  bb0: {
    if (layoutCollapsed) {
      t12 = void 0;
      break bb0;
    }
    if (collapsed) {
      t12 = PANE_COLLAPSED_WIDTH;
      break bb0;
    }
    if (currentMinWidth === 0) {
      t12 = minWidthProp || PANE_DEFAULT_MIN_WIDTH;
      break bb0;
    }
    if (isLast) {
      t12 = minWidthProp || PANE_DEFAULT_MIN_WIDTH;
      break bb0;
    }
    t12 = currentMinWidth || minWidthProp || PANE_DEFAULT_MIN_WIDTH;
  }
  const minWidth = t12;
  let t13;
  bb1: {
    if (collapsed) {
      t13 = PANE_COLLAPSED_WIDTH;
      break bb1;
    }
    if (layoutCollapsed && isLast) {
      t13 = void 0;
      break bb1;
    }
    if (isLast) {
      if (maxWidthProp) {
        t13 = currentMaxWidth ?? maxWidthProp;
        break bb1;
      }
      t13 = void 0;
      break bb1;
    }
    t13 = currentMaxWidth ?? maxWidthProp;
  }
  const maxWidth = t13, hidden = layoutCollapsed && !isLast;
  let t14, t15;
  $[41] !== collapsed || $[42] !== isLast || $[43] !== layoutCollapsed || $[44] !== nextCollapsed || $[45] !== rootElement ? (t15 = !isLast && !layoutCollapsed && (0, import_jsx_runtime3.jsx)(LegacyLayerProvider, { zOffset: "paneResizer", children: (0, import_jsx_runtime3.jsx)(PaneDivider, { disabled: collapsed || nextCollapsed, element: rootElement }) }), $[41] = collapsed, $[42] = isLast, $[43] = layoutCollapsed, $[44] = nextCollapsed, $[45] = rootElement, $[46] = t15) : t15 = $[46], t14 = t15;
  const divider = t14;
  let t16;
  const t17 = maxWidth === 1 / 0 ? void 0 : maxWidth;
  let t18;
  $[47] !== flex || $[48] !== minWidth || $[49] !== t17 ? (t18 = {
    flex,
    minWidth,
    maxWidth: t17
  }, $[47] = flex, $[48] = minWidth, $[49] = t17, $[50] = t18) : t18 = $[50], t16 = t18;
  const style = t16, t19 = layoutCollapsed ? void 0 : "hidden", t20 = collapsed ? "" : void 0, t21 = selected ? "" : void 0;
  let t22;
  $[51] !== collapsed || $[52] !== currentMaxWidth || $[53] !== currentMinWidth || $[54] !== expanded || $[55] !== flex || $[56] !== maxWidth || $[57] !== minWidth || $[58] !== paneIndex ? (t22 = PANE_DEBUG, $[51] = collapsed, $[52] = currentMaxWidth, $[53] = currentMinWidth, $[54] = expanded, $[55] = flex, $[56] = maxWidth, $[57] = minWidth, $[58] = paneIndex, $[59] = t22) : t22 = $[59];
  let t23;
  $[60] !== children || $[61] !== hidden ? (t23 = !hidden && (0, import_jsx_runtime3.jsx)(Flex, { direction: "column", height: "fill", children }), $[60] = children, $[61] = hidden, $[62] = t23) : t23 = $[62];
  let t24;
  $[63] !== rootElement || $[64] !== t23 ? (t24 = (0, import_jsx_runtime3.jsx)(BoundaryElementProvider, { element: rootElement, children: t23 }), $[63] = rootElement, $[64] = t23, $[65] = t24) : t24 = $[65];
  let t25;
  $[66] !== hidden || $[67] !== id2 || $[68] !== paneIndex || $[69] !== restProps || $[70] !== style || $[71] !== t19 || $[72] !== t20 || $[73] !== t21 || $[74] !== t22 || $[75] !== t24 ? (t25 = (0, import_jsx_runtime3.jsxs)(Root$8, { "data-testid": "pane", "data-ui": "Pane", tone: "inherit", hidden, id: id2, overflow: t19, ...restProps, "data-pane-collapsed": t20, "data-pane-index": paneIndex, "data-pane-selected": t21, ref: setRef, style, children: [
    t22,
    t24
  ] }), $[66] = hidden, $[67] = id2, $[68] = paneIndex, $[69] = restProps, $[70] = style, $[71] = t19, $[72] = t20, $[73] = t21, $[74] = t22, $[75] = t24, $[76] = t25) : t25 = $[76];
  let t26;
  $[77] !== isLast || $[78] !== t25 ? (t26 = (0, import_jsx_runtime3.jsx)(IsLastPaneProvider, { isLastPane: isLast, children: t25 }), $[77] = isLast, $[78] = t25, $[79] = t26) : t26 = $[79];
  let t27;
  $[80] !== contextValue || $[81] !== t26 ? (t27 = (0, import_jsx_runtime3.jsx)(LegacyLayerProvider, { zOffset: "pane", children: (0, import_jsx_runtime3.jsx)(PaneContext.Provider, { value: contextValue, children: t26 }) }), $[80] = contextValue, $[81] = t26, $[82] = t27) : t27 = $[82];
  let t28;
  return $[83] !== divider || $[84] !== t27 ? (t28 = (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [
    t27,
    divider
  ] }), $[83] = divider, $[84] = t27, $[85] = t28) : t28 = $[85], t28;
});
var Root$7 = dt(Card)`
  position: relative;
  outline: none;
`;
Root$7.displayName = "PaneContent__root";
function usePane() {
  const pane2 = (0, import_react3.useContext)(PaneContext);
  if (!pane2)
    throw new Error("Pane: missing context value");
  return pane2;
}
var PaneContent = (0, import_react3.forwardRef)(function(props, ref) {
  const $ = (0, import_react_compiler_runtime.c)(14);
  let as, children, overflow, padding, restProps;
  $[0] !== props ? ({
    as,
    children,
    overflow,
    padding,
    ...restProps
  } = props, $[0] = props, $[1] = as, $[2] = children, $[3] = overflow, $[4] = padding, $[5] = restProps) : (as = $[1], children = $[2], overflow = $[3], padding = $[4], restProps = $[5]);
  const {
    collapsed
  } = usePane(), {
    collapsed: layoutCollapsed
  } = usePaneLayout(), t0 = layoutCollapsed ? void 0 : overflow;
  let t1;
  return $[6] !== as || $[7] !== children || $[8] !== collapsed || $[9] !== padding || $[10] !== ref || $[11] !== restProps || $[12] !== t0 ? (t1 = (0, import_jsx_runtime3.jsx)(Root$7, { "data-testid": "pane-content", forwardedAs: as, ...restProps, flex: 1, hidden: collapsed, overflow: t0, padding, ref, tone: "inherit", children }), $[6] = as, $[7] = children, $[8] = collapsed, $[9] = padding, $[10] = ref, $[11] = restProps, $[12] = t0, $[13] = t1) : t1 = $[13], t1;
});
function toLowerCaseNoSpaces(str) {
  return str ? str.toLocaleLowerCase().replaceAll(" ", "") : "";
}
var MENU_GROUP_POPOVER_PROPS = {
  constrainSize: true,
  placement: "left-start",
  portal: true
};
function PaneMenuButtonItem(props) {
  const $ = (0, import_react_compiler_runtime.c)(35), {
    disabled,
    isAfterGroup,
    node
  } = props, getI18nText = useGetI18nText("i18n" in node ? node : void 0);
  if (node.type === "divider") {
    let t02;
    return $[0] === Symbol.for("react.memo_cache_sentinel") ? (t02 = (0, import_jsx_runtime3.jsx)(MenuDivider, {}), $[0] = t02) : t02 = $[0], t02;
  }
  let t0;
  $[1] !== getI18nText || $[2] !== node ? (t0 = getI18nText(node), $[1] = getI18nText, $[2] = node, $[3] = t0) : t0 = $[3];
  const {
    title
  } = t0;
  if (node.type === "group") {
    if (node.children.length === 0)
      return null;
    if (node.expanded) {
      let t13;
      $[4] !== isAfterGroup ? (t13 = isAfterGroup && (0, import_jsx_runtime3.jsx)(MenuDivider, {}), $[4] = isAfterGroup, $[5] = t13) : t13 = $[5];
      let t23;
      $[6] !== disabled || $[7] !== node.children || $[8] !== node.disabled ? (t23 = node.children.map((child, childIndex) => {
        var _a;
        return (0, import_jsx_runtime3.jsx)(PaneMenuButtonItem, { disabled: disabled || !!node.disabled, isAfterGroup: ((_a = node.children[childIndex - 1]) == null ? void 0 : _a.type) === "group", node: child }, child.key);
      }), $[6] = disabled, $[7] = node.children, $[8] = node.disabled, $[9] = t23) : t23 = $[9];
      let t33;
      return $[10] !== t13 || $[11] !== t23 ? (t33 = (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [
        t13,
        t23
      ] }), $[10] = t13, $[11] = t23, $[12] = t33) : t33 = $[12], t33;
    }
    let t12;
    $[13] !== isAfterGroup ? (t12 = isAfterGroup && (0, import_jsx_runtime3.jsx)(MenuDivider, {}), $[13] = isAfterGroup, $[14] = t12) : t12 = $[14];
    const t22 = node.icon;
    let t32;
    $[15] !== disabled || $[16] !== node.children || $[17] !== node.disabled ? (t32 = node.children.map((child_0, childIndex_0) => {
      var _a;
      return (0, import_jsx_runtime3.jsx)(PaneMenuButtonItem, { disabled: disabled || !!node.disabled, isAfterGroup: ((_a = node.children[childIndex_0 - 1]) == null ? void 0 : _a.type) === "group", node: child_0 }, child_0.key);
    }), $[15] = disabled, $[16] = node.children, $[17] = node.disabled, $[18] = t32) : t32 = $[18];
    let t4;
    $[19] !== disabled || $[20] !== node.icon || $[21] !== t32 || $[22] !== title ? (t4 = (0, import_jsx_runtime3.jsx)(MenuGroup, { disabled, icon: t22, popover: MENU_GROUP_POPOVER_PROPS, text: title, children: t32 }), $[19] = disabled, $[20] = node.icon, $[21] = t32, $[22] = title, $[23] = t4) : t4 = $[23];
    let t5;
    return $[24] !== t12 || $[25] !== t4 ? (t5 = (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [
      t12,
      t4
    ] }), $[24] = t12, $[25] = t4, $[26] = t5) : t5 = $[26], t5;
  }
  let t1;
  $[27] !== isAfterGroup ? (t1 = isAfterGroup && (0, import_jsx_runtime3.jsx)(MenuDivider, {}), $[27] = isAfterGroup, $[28] = t1) : t1 = $[28];
  let t2;
  $[29] !== disabled || $[30] !== node ? (t2 = (0, import_jsx_runtime3.jsx)(PaneContextMenuItemResolver, { disabled, node }), $[29] = disabled, $[30] = node, $[31] = t2) : t2 = $[31];
  let t3;
  return $[32] !== t1 || $[33] !== t2 ? (t3 = (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [
    t1,
    t2
  ] }), $[32] = t1, $[33] = t2, $[34] = t3) : t3 = $[34], t3;
}
function PaneContextMenuItemResolver(props) {
  const $ = (0, import_react_compiler_runtime.c)(5), {
    node
  } = props;
  if (node.intent) {
    let t02;
    return $[0] !== node.intent || $[1] !== props ? (t02 = (0, import_jsx_runtime3.jsx)(PaneContextIntentMenuItem, { ...props, intent: node.intent }), $[0] = node.intent, $[1] = props, $[2] = t02) : t02 = $[2], t02;
  }
  let t0;
  return $[3] !== props ? (t0 = (0, import_jsx_runtime3.jsx)(PaneContextMenuItem, { ...props }), $[3] = props, $[4] = t0) : t0 = $[4], t0;
}
function PaneContextMenuItem(props) {
  var _a;
  const $ = (0, import_react_compiler_runtime.c)(17), {
    disabled,
    node
  } = props;
  let t0;
  $[0] !== node.disabled ? (t0 = typeof node.disabled == "object" && (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: node.disabled.reason }), $[0] = node.disabled, $[1] = t0) : t0 = $[1];
  const tooltipContent = t0, {
    title
  } = useI18nText(node), t1 = disabled || !!node.disabled;
  let t2;
  $[2] !== node.hotkey ? (t2 = (_a = node.hotkey) == null ? void 0 : _a.split("+"), $[2] = node.hotkey, $[3] = t2) : t2 = $[3];
  const t3 = node.iconRight || node.selected && CheckmarkIcon, t4 = `action-${toLowerCaseNoSpaces(node.title)}`;
  let t5;
  $[4] !== node.icon || $[5] !== node.onAction || $[6] !== node.selected || $[7] !== node.tone || $[8] !== t1 || $[9] !== t2 || $[10] !== t3 || $[11] !== t4 || $[12] !== title ? (t5 = (0, import_jsx_runtime3.jsx)(MenuItem2, { disabled: t1, hotkeys: t2, icon: node.icon, iconRight: t3, onClick: node.onAction, pressed: node.selected, text: title, tone: node.tone, "data-testid": t4 }), $[4] = node.icon, $[5] = node.onAction, $[6] = node.selected, $[7] = node.tone, $[8] = t1, $[9] = t2, $[10] = t3, $[11] = t4, $[12] = title, $[13] = t5) : t5 = $[13];
  let t6;
  return $[14] !== t5 || $[15] !== tooltipContent ? (t6 = (0, import_jsx_runtime3.jsx)(TooltipOfDisabled, { content: tooltipContent, placement: "left", children: t5 }), $[14] = t5, $[15] = tooltipContent, $[16] = t6) : t6 = $[16], t6;
}
function PaneContextIntentMenuItem(props) {
  var _a;
  const $ = (0, import_react_compiler_runtime.c)(23), {
    disabled,
    intent,
    node
  } = props;
  let t0;
  $[0] !== node.disabled ? (t0 = typeof node.disabled == "object" && (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: node.disabled.reason }), $[0] = node.disabled, $[1] = t0) : t0 = $[1];
  const tooltipContent = t0;
  let t1;
  $[2] !== intent.params || $[3] !== intent.type ? (t1 = {
    intent: intent.type,
    params: intent.params
  }, $[2] = intent.params, $[3] = intent.type, $[4] = t1) : t1 = $[4];
  const intentLink = useIntentLink(t1);
  let t2;
  $[5] !== intentLink || $[6] !== node ? (t2 = (event) => {
    intentLink.onClick(event), node.onAction();
  }, $[5] = intentLink, $[6] = node, $[7] = t2) : t2 = $[7];
  const handleClick = t2, {
    title
  } = useI18nText(node), t3 = disabled || !!node.disabled;
  let t4;
  $[8] !== node.hotkey ? (t4 = (_a = node.hotkey) == null ? void 0 : _a.split("+"), $[8] = node.hotkey, $[9] = t4) : t4 = $[9];
  const t5 = node.selected ? CheckmarkIcon : void 0;
  let t6;
  $[10] !== handleClick || $[11] !== intentLink.href || $[12] !== node.icon || $[13] !== node.selected || $[14] !== node.tone || $[15] !== t3 || $[16] !== t4 || $[17] !== t5 || $[18] !== title ? (t6 = (0, import_jsx_runtime3.jsx)(MenuItem2, { as: "a", disabled: t3, hotkeys: t4, href: intentLink.href, icon: node.icon, iconRight: t5, onClick: handleClick, pressed: node.selected, text: title, tone: node.tone }), $[10] = handleClick, $[11] = intentLink.href, $[12] = node.icon, $[13] = node.selected, $[14] = node.tone, $[15] = t3, $[16] = t4, $[17] = t5, $[18] = title, $[19] = t6) : t6 = $[19];
  let t7;
  return $[20] !== t6 || $[21] !== tooltipContent ? (t7 = (0, import_jsx_runtime3.jsx)(TooltipOfDisabled, { content: tooltipContent, placement: "left", children: t6 }), $[20] = t6, $[21] = tooltipContent, $[22] = t7) : t7 = $[22], t7;
}
var CONTEXT_MENU_POPOVER_PROPS = {
  constrainSize: true,
  placement: "bottom",
  portal: true
};
function nodesHasTone(nodes, tone) {
  return nodes.some((node) => node.type === "item" && node.tone === tone || node.type === "group" && nodesHasTone(node.children, tone));
}
function PaneContextMenuButton(props) {
  const $ = (0, import_react_compiler_runtime.c)(13), {
    nodes,
    actionsNodes
  } = props, id2 = (0, import_react3.useId)(), hasCritical = nodesHasTone(nodes, "critical"), hasCaution = nodesHasTone(nodes, "caution"), t0 = hasCritical ? "critical" : hasCaution ? "caution" : void 0;
  let t1;
  $[0] !== t0 ? (t1 = (0, import_jsx_runtime3.jsx)(ContextMenuButton, { tone: t0, "data-testid": "pane-context-menu-button" }), $[0] = t0, $[1] = t1) : t1 = $[1];
  let t2;
  $[2] !== actionsNodes ? (t2 = actionsNodes && (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [
    actionsNodes,
    (0, import_jsx_runtime3.jsx)(MenuDivider, {})
  ] }), $[2] = actionsNodes, $[3] = t2) : t2 = $[3];
  let t3;
  $[4] !== nodes ? (t3 = nodes.map((node, nodeIndex) => {
    var _a;
    const isAfterGroup = ((_a = nodes[nodeIndex - 1]) == null ? void 0 : _a.type) === "group";
    return (0, import_jsx_runtime3.jsx)(PaneMenuButtonItem, { isAfterGroup, node }, node.key);
  }), $[4] = nodes, $[5] = t3) : t3 = $[5];
  let t4;
  $[6] !== t2 || $[7] !== t3 ? (t4 = (0, import_jsx_runtime3.jsxs)(Menu, { children: [
    t2,
    t3
  ] }), $[6] = t2, $[7] = t3, $[8] = t4) : t4 = $[8];
  let t5;
  return $[9] !== id2 || $[10] !== t1 || $[11] !== t4 ? (t5 = (0, import_jsx_runtime3.jsx)(MenuButton, { button: t1, id: id2, menu: t4, popover: CONTEXT_MENU_POPOVER_PROPS }), $[9] = id2, $[10] = t1, $[11] = t4, $[12] = t5) : t5 = $[12], t5;
}
var Root$6 = dt(Layer)`
  position: sticky;
  bottom: 0;
`;
var RootCard = dt(Card)`
  padding-bottom: env(safe-area-inset-bottom);
`;
var PaneFooter = (0, import_react3.forwardRef)(function(props, ref) {
  const $ = (0, import_react_compiler_runtime.c)(7), {
    children,
    padding
  } = props, {
    collapsed
  } = usePane();
  let t0;
  $[0] !== children || $[1] !== padding ? (t0 = (0, import_jsx_runtime3.jsx)(RootCard, { tone: "inherit", children: (0, import_jsx_runtime3.jsx)(Box, { padding, children }) }), $[0] = children, $[1] = padding, $[2] = t0) : t0 = $[2];
  let t1;
  return $[3] !== collapsed || $[4] !== ref || $[5] !== t0 ? (t1 = (0, import_jsx_runtime3.jsx)(LegacyLayerProvider, { zOffset: "paneFooter", children: (0, import_jsx_runtime3.jsx)(Root$6, { "data-testid": "pane-footer", hidden: collapsed, ref, children: t0 }) }), $[3] = collapsed, $[4] = ref, $[5] = t0, $[6] = t1) : t1 = $[6], t1;
});
var Root$5 = dt(Layer)(({
  $border
}) => lt`
    line-height: 0;
    position: sticky;
    top: 0;

    &:not([data-collapsed]):after {
      content: '';
      display: block;
      position: absolute;
      left: 0;
      right: 0;
      bottom: -1px;
      border-bottom: 1px solid ${$border ? "var(--card-border-color)" : "transparent"};
      opacity: 1;
    }
  `);
var Layout = dt(Flex)`
  transform-origin: calc(51px / 2);

  [data-collapsed] > div > & {
    transform: rotate(90deg);
  }
`;
var TitleCard = dt(Card)(({
  theme
}) => {
  const {
    fg,
    bg
  } = theme.sanity.color.card.enabled;
  return lt`
    background-color: ${bg};

    [data-ui='Text'] {
      color: ${fg};
    }
  `;
});
var TitleTextSkeleton = dt(TextSkeleton)`
  width: 66%;
  max-width: 175px;
`;
var TitleText = dt(Text)`
  cursor: default;
  outline: none;
`;
var PaneHeader$1 = (0, import_react3.forwardRef)(function(props, ref) {
  const $ = (0, import_react_compiler_runtime.c)(48), {
    actions,
    backButton,
    border,
    contentAfter,
    loading,
    subActions,
    tabs,
    tabIndex,
    title,
    appendTitle
  } = props, {
    collapse,
    collapsed,
    expand,
    rootElement: paneElement
  } = usePane(), paneRect = useElementRect(paneElement || null);
  let t0;
  const t1 = collapsed ? (paneRect == null ? void 0 : paneRect.height) || window.innerHeight : void 0;
  let t2;
  $[0] !== t1 ? (t2 = {
    width: t1
  }, $[0] = t1, $[1] = t2) : t2 = $[1], t0 = t2;
  const layoutStyle = t0;
  let t3;
  $[2] !== collapse || $[3] !== collapsed ? (t3 = () => {
    collapsed || collapse();
  }, $[2] = collapse, $[3] = collapsed, $[4] = t3) : t3 = $[4];
  const handleTitleClick = t3;
  let t4;
  $[5] !== collapsed || $[6] !== expand ? (t4 = () => {
    collapsed && expand();
  }, $[5] = collapsed, $[6] = expand, $[7] = t4) : t4 = $[7];
  const handleLayoutClick = t4, showTabsOrSubActions = !!(!collapsed && (tabs || subActions)), t5 = collapsed ? "" : void 0, t6 = collapsed ? "" : void 0;
  let t7;
  $[8] !== backButton ? (t7 = backButton && (0, import_jsx_runtime3.jsx)(Box, { flex: "none", children: backButton }), $[8] = backButton, $[9] = t7) : t7 = $[9];
  const t8 = backButton ? 1 : 2;
  let t9;
  $[10] !== loading ? (t9 = loading && (0, import_jsx_runtime3.jsx)(Box, { children: (0, import_jsx_runtime3.jsx)(TitleTextSkeleton, { animated: true, radius: 1, size: 1 }) }), $[10] = loading, $[11] = t9) : t9 = $[11];
  let t10;
  $[12] !== appendTitle || $[13] !== loading || $[14] !== title ? (t10 = !loading && (0, import_jsx_runtime3.jsxs)(Flex, { align: "center", gap: 1, children: [
    (0, import_jsx_runtime3.jsx)(TitleText, { size: 1, textOverflow: "ellipsis", weight: "semibold", children: title }),
    appendTitle
  ] }), $[12] = appendTitle, $[13] = loading, $[14] = title, $[15] = t10) : t10 = $[15];
  let t11;
  $[16] !== handleTitleClick || $[17] !== t10 || $[18] !== t8 || $[19] !== t9 || $[20] !== tabIndex ? (t11 = (0, import_jsx_runtime3.jsxs)(TitleCard, { __unstable_focusRing: true, flex: 1, onClick: handleTitleClick, paddingLeft: t8, padding: 2, tabIndex, children: [
    t9,
    t10
  ] }), $[16] = handleTitleClick, $[17] = t10, $[18] = t8, $[19] = t9, $[20] = tabIndex, $[21] = t11) : t11 = $[21];
  let t12;
  $[22] !== actions || $[23] !== collapsed ? (t12 = actions && (0, import_jsx_runtime3.jsx)(Box, { hidden: collapsed, children: (0, import_jsx_runtime3.jsx)(LegacyLayerProvider, { zOffset: "paneHeader", children: actions }) }), $[22] = actions, $[23] = collapsed, $[24] = t12) : t12 = $[24];
  let t13;
  $[25] !== t11 || $[26] !== t12 || $[27] !== t7 ? (t13 = (0, import_jsx_runtime3.jsxs)(Flex, { align: "flex-start", gap: 3, children: [
    t7,
    t11,
    t12
  ] }), $[25] = t11, $[26] = t12, $[27] = t7, $[28] = t13) : t13 = $[28];
  let t14;
  $[29] !== collapsed || $[30] !== showTabsOrSubActions || $[31] !== subActions || $[32] !== tabs ? (t14 = showTabsOrSubActions && (0, import_jsx_runtime3.jsxs)(Flex, { align: "center", hidden: collapsed, overflow: "auto", children: [
    (0, import_jsx_runtime3.jsx)(Box, { flex: 1, marginRight: subActions ? 3 : 0, children: tabs }),
    subActions
  ] }), $[29] = collapsed, $[30] = showTabsOrSubActions, $[31] = subActions, $[32] = tabs, $[33] = t14) : t14 = $[33];
  let t15;
  $[34] !== handleLayoutClick || $[35] !== layoutStyle || $[36] !== t13 || $[37] !== t14 ? (t15 = (0, import_jsx_runtime3.jsxs)(Layout, { direction: "column", gap: 3, onClick: handleLayoutClick, padding: 3, sizing: "border", style: layoutStyle, children: [
    t13,
    t14
  ] }), $[34] = handleLayoutClick, $[35] = layoutStyle, $[36] = t13, $[37] = t14, $[38] = t15) : t15 = $[38];
  const t16 = !collapsed && contentAfter;
  let t17;
  $[39] !== t15 || $[40] !== t16 || $[41] !== t6 ? (t17 = (0, import_jsx_runtime3.jsx)(LegacyLayerProvider, { zOffset: "paneHeader", children: (0, import_jsx_runtime3.jsxs)(Card, { "data-collapsed": t6, tone: "inherit", children: [
    t15,
    t16
  ] }) }), $[39] = t15, $[40] = t16, $[41] = t6, $[42] = t17) : t17 = $[42];
  let t18;
  return $[43] !== border || $[44] !== ref || $[45] !== t17 || $[46] !== t5 ? (t18 = (0, import_jsx_runtime3.jsx)(LayerProvider, { zOffset: 100, children: (0, import_jsx_runtime3.jsx)(Root$5, { $border: border, "data-collapsed": t5, "data-testid": "pane-header", ref, children: t17 }) }), $[43] = border, $[44] = ref, $[45] = t17, $[46] = t5, $[47] = t18) : t18 = $[47], t18;
});
function getDisabledReason$1(node) {
  var _a;
  if (!node.disabled)
    return {
      disabledReason: void 0,
      ariaLabel: void 0,
      isDisabled: false
    };
  const disabledReason = typeof node.disabled == "object" ? node.disabled.reason : void 0, ariaLabel = typeof node.disabled == "object" && typeof ((_a = node.disabled) == null ? void 0 : _a.reason) == "string" ? node.disabled.reason : "This is disabled";
  return {
    disabledReason,
    ariaLabel,
    isDisabled: !!node.disabled
  };
}
function PaneHeaderActionButton(props) {
  const $ = (0, import_react_compiler_runtime.c)(4), {
    node
  } = props;
  if (node.type === "item") {
    let t0;
    return $[0] !== node ? (t0 = (0, import_jsx_runtime3.jsx)(PaneHeaderMenuItemActionButton, { node }), $[0] = node, $[1] = t0) : t0 = $[1], t0;
  }
  if (node.type === "group") {
    let t0;
    return $[2] !== node ? (t0 = (0, import_jsx_runtime3.jsx)(PaneHeaderMenuGroupActionButton, { node }), $[2] = node, $[3] = t0) : t0 = $[3], t0;
  }
  return console.warn('unknown menu node (expected `type: "item" | "group"`):', node), null;
}
function PaneHeaderMenuItemActionButton(props) {
  const $ = (0, import_react_compiler_runtime.c)(23), {
    node
  } = props, {
    title
  } = useI18nText(node), {
    t: t2
  } = useTranslation();
  if (node.intent) {
    let t02;
    return $[0] !== node.intent || $[1] !== props ? (t02 = (0, import_jsx_runtime3.jsx)(PaneHeaderActionIntentButton, { ...props, intent: node.intent }), $[0] = node.intent, $[1] = props, $[2] = t02) : t02 = $[2], t02;
  }
  let t0;
  $[3] !== node ? (t0 = getDisabledReason$1(node), $[3] = node, $[4] = t0) : t0 = $[4];
  const {
    disabledReason,
    ariaLabel,
    isDisabled
  } = t0;
  let t1;
  $[5] !== ariaLabel || $[6] !== t2 || $[7] !== title ? (t1 = ariaLabel || title || t2("status-button.aria-label"), $[5] = ariaLabel, $[6] = t2, $[7] = title, $[8] = t1) : t1 = $[8];
  let t22;
  $[9] !== isDisabled || $[10] !== node.hotkey ? (t22 = !isDisabled && node.hotkey ? node.hotkey.split("+") : void 0, $[9] = isDisabled, $[10] = node.hotkey, $[11] = t22) : t22 = $[11];
  const t3 = isDisabled ? disabledReason : title;
  let t4;
  $[12] !== t22 || $[13] !== t3 ? (t4 = {
    hotkeys: t22,
    content: t3
  }, $[12] = t22, $[13] = t3, $[14] = t4) : t4 = $[14];
  let t5;
  return $[15] !== isDisabled || $[16] !== node.icon || $[17] !== node.onAction || $[18] !== node.selected || $[19] !== node.tone || $[20] !== t1 || $[21] !== t4 ? (t5 = (0, import_jsx_runtime3.jsx)(StatusButton, { disabled: isDisabled, icon: node.icon, onClick: node.onAction, selected: node.selected, tone: node.tone, "aria-label": t1, tooltipProps: t4 }), $[15] = isDisabled, $[16] = node.icon, $[17] = node.onAction, $[18] = node.selected, $[19] = node.tone, $[20] = t1, $[21] = t4, $[22] = t5) : t5 = $[22], t5;
}
function PaneHeaderActionIntentButton(props) {
  const $ = (0, import_react_compiler_runtime.c)(27), {
    intent,
    node
  } = props;
  let t0;
  $[0] !== intent.params || $[1] !== intent.type ? (t0 = {
    intent: intent.type,
    params: intent.params
  }, $[0] = intent.params, $[1] = intent.type, $[2] = t0) : t0 = $[2];
  const intentLink = useIntentLink(t0), {
    t: t2
  } = useTranslation();
  let t1;
  $[3] !== intentLink || $[4] !== node ? (t1 = (event) => {
    intentLink.onClick(event), node.onAction();
  }, $[3] = intentLink, $[4] = node, $[5] = t1) : t1 = $[5];
  const handleClick = t1;
  let t22;
  $[6] !== node ? (t22 = getDisabledReason$1(node), $[6] = node, $[7] = t22) : t22 = $[7];
  const {
    disabledReason,
    ariaLabel,
    isDisabled
  } = t22;
  let t3;
  $[8] !== ariaLabel || $[9] !== node.title || $[10] !== t2 ? (t3 = ariaLabel || node.title || t2("status-button.aria-label"), $[8] = ariaLabel, $[9] = node.title, $[10] = t2, $[11] = t3) : t3 = $[11];
  let t4;
  $[12] !== isDisabled || $[13] !== node.hotkey ? (t4 = !isDisabled && node.hotkey ? node.hotkey.split("+") : void 0, $[12] = isDisabled, $[13] = node.hotkey, $[14] = t4) : t4 = $[14];
  const t5 = isDisabled ? disabledReason : node.title;
  let t6;
  $[15] !== t4 || $[16] !== t5 ? (t6 = {
    hotkeys: t4,
    content: t5,
    placement: "bottom",
    portal: true
  }, $[15] = t4, $[16] = t5, $[17] = t6) : t6 = $[17];
  let t7;
  return $[18] !== handleClick || $[19] !== intentLink.href || $[20] !== isDisabled || $[21] !== node.icon || $[22] !== node.selected || $[23] !== node.tone || $[24] !== t3 || $[25] !== t6 ? (t7 = (0, import_jsx_runtime3.jsx)(StatusButton, { forwardedAs: "a", disabled: isDisabled, href: intentLink.href, icon: node.icon, onClick: handleClick, selected: node.selected, tone: node.tone, "aria-label": t3, tooltipProps: t6 }), $[18] = handleClick, $[19] = intentLink.href, $[20] = isDisabled, $[21] = node.icon, $[22] = node.selected, $[23] = node.tone, $[24] = t3, $[25] = t6, $[26] = t7) : t7 = $[26], t7;
}
function PaneHeaderMenuGroupActionButton(props) {
  const $ = (0, import_react_compiler_runtime.c)(16), {
    node
  } = props, {
    title
  } = useI18nText(node), t0 = !!node.disabled, t1 = node.icon ?? UnknownIcon;
  let t2;
  $[0] !== node.title ? (t2 = {
    content: node.title,
    portal: true
  }, $[0] = node.title, $[1] = t2) : t2 = $[1];
  let t3;
  $[2] !== t0 || $[3] !== t1 || $[4] !== t2 || $[5] !== title ? (t3 = (0, import_jsx_runtime3.jsx)(Button2, { disabled: t0, icon: t1, label: title, mode: "bleed", tooltipProps: t2 }), $[2] = t0, $[3] = t1, $[4] = t2, $[5] = title, $[6] = t3) : t3 = $[6];
  const t4 = (0, import_react3.useId)();
  let t5;
  $[7] !== node.children || $[8] !== node.disabled ? (t5 = node.children.map((child, idx) => {
    var _a;
    return (0, import_jsx_runtime3.jsx)(PaneMenuButtonItem, { disabled: !!node.disabled, isAfterGroup: ((_a = node.children[idx - 1]) == null ? void 0 : _a.type) === "group", node: child }, child.key);
  }), $[7] = node.children, $[8] = node.disabled, $[9] = t5) : t5 = $[9];
  let t6;
  $[10] !== t5 ? (t6 = (0, import_jsx_runtime3.jsx)(Menu, { children: t5 }), $[10] = t5, $[11] = t6) : t6 = $[11];
  let t7;
  return $[12] !== t3 || $[13] !== t4 || $[14] !== t6 ? (t7 = (0, import_jsx_runtime3.jsx)(MenuButton, { button: t3, id: t4, menu: t6 }), $[12] = t3, $[13] = t4, $[14] = t6, $[15] = t7) : t7 = $[15], t7;
}
var Root$4 = dt(Card)`
  transition: opacity 200ms;
  position: relative;
  z-index: 1;
  padding-left: env(safe-area-inset-left);
  padding-right: env(safe-area-inset-right);
  opacity: 0;

  &:not([hidden]) {
    display: flex;
  }

  &:not([data-collapsed]) {
    overflow: auto;
  }

  &[data-mounted] {
    opacity: 1;
  }

  &[data-resizing] {
    pointer-events: none;
  }
`;
function _calcPaneResize(cache, left, right, deltaX) {
  const sum2 = {
    flex: cache.left.flex + cache.right.flex,
    width: cache.left.width + cache.right.width
  }, leftMinWidth = left.minWidth ?? PANE_DEFAULT_MIN_WIDTH, rightMinWidth = right.minWidth ?? PANE_DEFAULT_MIN_WIDTH, leftMaxWidth = Math.min(left.maxWidth || 1 / 0, sum2.width - rightMinWidth), rightMaxWidth = Math.min(right.maxWidth || 1 / 0, sum2.width - leftMinWidth);
  let minDeltaX = leftMinWidth - cache.left.width;
  const rightMinDeltaX = cache.right.width - rightMaxWidth;
  minDeltaX < rightMinDeltaX && (minDeltaX = rightMinDeltaX);
  let maxDeltaX = cache.right.width - rightMinWidth;
  const leftMaxDeltaX = leftMaxWidth - cache.left.width;
  maxDeltaX > leftMaxDeltaX && (maxDeltaX = leftMaxDeltaX);
  const _deltaX = Math.min(Math.max(deltaX, minDeltaX), maxDeltaX), leftW = cache.left.width + _deltaX, rightW = cache.right.width - _deltaX, leftFlex = leftW / sum2.width * sum2.flex, rightFlex = rightW / sum2.width * sum2.flex;
  return {
    leftFlex,
    leftW,
    rightFlex,
    rightW
  };
}
function _getDOMPath(rootElement, el) {
  const path = [];
  let e2 = el;
  for (; e2 !== rootElement; ) {
    const parentElement = e2.parentElement;
    if (!parentElement) return path;
    const index = Array.from(parentElement.childNodes).indexOf(e2);
    if (path.unshift(index), parentElement === rootElement)
      return path;
    e2 = parentElement;
  }
  return path;
}
var EMPTY_PATH = [];
function _sortElements(rootElement, elements) {
  const map2 = /* @__PURE__ */ new WeakMap();
  for (const element of elements)
    map2.set(element, _getDOMPath(rootElement, element));
  const _sortByElementPath = (a, b) => {
    const _a = map2.get(a) || EMPTY_PATH, _b = map2.get(b) || EMPTY_PATH, len = Math.max(_a.length, _b.length);
    for (let i = 0; i < len; i += 1) {
      const aIndex = _a[i] || -1, bIndex = _b[i] || -1;
      if (aIndex !== bIndex)
        return aIndex - bIndex;
    }
    return 0;
  };
  elements.sort(_sortByElementPath);
}
function createPaneLayoutController() {
  const observers = [], elements = [], optionsMap = /* @__PURE__ */ new WeakMap(), userCollapsedElementSet = /* @__PURE__ */ new Set(), cache = {};
  let rootElement = null, rootWidth = 0, expandedElement = null, resizeDataMap = /* @__PURE__ */ new Map(), resizing = false;
  function collapse(element) {
    userCollapsedElementSet.add(element), expandedElement === element && (expandedElement = null), _notifyObservers();
  }
  function expand(element) {
    userCollapsedElementSet.delete(element), expandedElement = element, _notifyObservers();
  }
  function mount(element, options) {
    return optionsMap.set(element, {
      ...options,
      original: options
    }), elements.push(element), rootElement && _sortElements(rootElement, elements), expand(element), () => {
      const idx = elements.indexOf(element);
      idx > -1 && elements.splice(idx, 1), optionsMap.delete(element), _notifyObservers();
    };
  }
  function resize(type2, leftElement, deltaX) {
    const leftIndex = elements.indexOf(leftElement), leftOptions = optionsMap.get(leftElement);
    if (!leftOptions) return;
    const rightElement = elements[leftIndex + 1], rightOptions = optionsMap.get(rightElement);
    if (rightOptions) {
      if (type2 === "start" && (resizing = true, cache.left = {
        element: leftElement,
        flex: leftOptions.flex || 1,
        width: leftElement.offsetWidth
      }, cache.right = {
        element: rightElement,
        flex: rightOptions.flex || 1,
        width: rightElement.offsetWidth
      }, _notifyObservers()), type2 === "move" && cache.left && cache.right) {
        resizeDataMap = /* @__PURE__ */ new Map();
        const {
          leftW,
          rightW,
          leftFlex,
          rightFlex
        } = _calcPaneResize(cache, leftOptions, rightOptions, deltaX);
        resizeDataMap.set(leftElement, {
          flex: leftFlex,
          width: leftW
        }), resizeDataMap.set(rightElement, {
          flex: rightFlex,
          width: rightW
        }), _notifyObservers();
      }
      if (type2 === "end") {
        resizing = false;
        const leftResizeData = resizeDataMap.get(leftElement), rightResizeData = resizeDataMap.get(rightElement);
        optionsMap.set(leftElement, {
          ...leftOptions,
          currentMinWidth: 0,
          currentMaxWidth: leftOptions.maxWidth ?? 1 / 0,
          flex: (leftResizeData == null ? void 0 : leftResizeData.flex) ?? leftOptions.flex
        }), optionsMap.set(rightElement, {
          ...rightOptions,
          currentMinWidth: 0,
          currentMaxWidth: leftOptions.maxWidth ?? 1 / 0,
          flex: (rightResizeData == null ? void 0 : rightResizeData.flex) ?? rightOptions.flex
        }), resizeDataMap = /* @__PURE__ */ new Map(), delete cache.left, delete cache.right, _notifyObservers();
      }
    }
  }
  function setRootElement(nextRootElement) {
    rootElement = nextRootElement;
  }
  function setRootWidth(nextRootWidth) {
    rootWidth = nextRootWidth, _notifyObservers();
  }
  function subscribe(observer) {
    return observers.push(observer), () => {
      const idx = observers.push(observer);
      idx > -1 && observers.splice(idx, 1);
    };
  }
  return {
    collapse,
    expand,
    mount,
    resize,
    setRootElement,
    setRootWidth,
    subscribe
  };
  function _notifyObservers() {
    if (!rootWidth) return;
    const _elements = [];
    for (const element of elements)
      element !== expandedElement && _elements.unshift(element);
    expandedElement && _elements.unshift(expandedElement);
    const dataMap = /* @__PURE__ */ new WeakMap(), len = _elements.length, lastElement = _elements[0], collapsedWidth = (len - 1) * PANE_COLLAPSED_WIDTH;
    let remaingWidth = rootWidth - collapsedWidth;
    for (const element of _elements) {
      const options = optionsMap.get(element);
      if (!options)
        continue;
      const minWidth = options.currentMinWidth || options.minWidth || PANE_DEFAULT_MIN_WIDTH, isLast = element === lastElement, userCollapsed = userCollapsedElementSet.has(element), sizeCollapsed = minWidth > remaingWidth, collapsed = isLast ? false : userCollapsed || sizeCollapsed, resizeData = resizeDataMap.get(element);
      dataMap.set(element, {
        element,
        collapsed,
        currentMinWidth: (resizeData == null ? void 0 : resizeData.width) ?? options.currentMinWidth,
        currentMaxWidth: (resizeData == null ? void 0 : resizeData.width) ?? options.currentMaxWidth,
        flex: (resizeData == null ? void 0 : resizeData.flex) ?? options.flex ?? 1
      }), collapsed ? remaingWidth -= PANE_COLLAPSED_WIDTH : remaingWidth -= minWidth - PANE_COLLAPSED_WIDTH;
    }
    const panes = [];
    for (const element of elements) {
      const data = dataMap.get(element);
      data && panes.push(data);
    }
    for (const observer of observers)
      observer({
        expandedElement: expandedElement || elements[elements.length - 1] || null,
        panes,
        resizing
      });
  }
}
function PaneLayout(props) {
  var _a;
  const $ = (0, import_react_compiler_runtime.c)(41);
  let children, minWidth, onCollapse, onExpand, restProps;
  $[0] !== props ? ({
    children,
    minWidth,
    onCollapse,
    onExpand,
    ...restProps
  } = props, $[0] = props, $[1] = children, $[2] = minWidth, $[3] = onCollapse, $[4] = onExpand, $[5] = restProps) : (children = $[1], minWidth = $[2], onCollapse = $[3], onExpand = $[4], restProps = $[5]);
  const [controller] = (0, import_react3.useState)(_temp$o), [rootElement, setRootElement] = (0, import_react3.useState)(null), width = ((_a = useElementRect(rootElement)) == null ? void 0 : _a.width) || 0, collapsed = width === void 0 || !minWidth ? void 0 : width < minWidth;
  let t0;
  $[6] === Symbol.for("react.memo_cache_sentinel") ? (t0 = {
    expandedElement: null,
    panes: [],
    resizing: false
  }, $[6] = t0) : t0 = $[6];
  const [state2, setState] = (0, import_react3.useState)(t0);
  let t1, t2;
  $[7] !== controller || $[8] !== rootElement ? (t1 = () => controller.setRootElement(rootElement), t2 = [controller, rootElement], $[7] = controller, $[8] = rootElement, $[9] = t1, $[10] = t2) : (t1 = $[9], t2 = $[10]), (0, import_react3.useEffect)(t1, t2);
  let t3, t4;
  $[11] !== controller || $[12] !== width ? (t3 = () => controller.setRootWidth(width), t4 = [controller, width], $[11] = controller, $[12] = width, $[13] = t3, $[14] = t4) : (t3 = $[13], t4 = $[14]), (0, import_react3.useEffect)(t3, t4);
  let t5, t6;
  $[15] !== controller ? (t5 = () => controller.subscribe(setState), t6 = [controller], $[15] = controller, $[16] = t5, $[17] = t6) : (t5 = $[16], t6 = $[17]), (0, import_react3.useEffect)(t5, t6);
  let t7, t8;
  $[18] !== collapsed || $[19] !== onCollapse || $[20] !== onExpand ? (t7 = () => {
    collapsed !== void 0 && (collapsed && onCollapse && onCollapse(), !collapsed && onExpand && onExpand());
  }, t8 = [collapsed, onCollapse, onExpand], $[18] = collapsed, $[19] = onCollapse, $[20] = onExpand, $[21] = t7, $[22] = t8) : (t7 = $[21], t8 = $[22]), (0, import_react3.useEffect)(t7, t8);
  let t9, t10;
  $[23] !== collapsed || $[24] !== controller.collapse || $[25] !== controller.expand || $[26] !== controller.mount || $[27] !== controller.resize || $[28] !== state2.expandedElement || $[29] !== state2.panes || $[30] !== state2.resizing ? (t10 = {
    collapse: controller.collapse,
    collapsed,
    expand: controller.expand,
    expandedElement: state2.expandedElement,
    mount: controller.mount,
    panes: state2.panes,
    resize: controller.resize,
    resizing: state2.resizing
  }, $[23] = collapsed, $[24] = controller.collapse, $[25] = controller.expand, $[26] = controller.mount, $[27] = controller.resize, $[28] = state2.expandedElement, $[29] = state2.panes, $[30] = state2.resizing, $[31] = t10) : t10 = $[31], t9 = t10;
  const paneLayout = t9, t11 = collapsed ? "" : void 0, t12 = state2.resizing ? "" : void 0, t13 = width ? "" : void 0;
  let t14;
  $[32] !== children || $[33] !== restProps || $[34] !== t11 || $[35] !== t12 || $[36] !== t13 ? (t14 = (0, import_jsx_runtime3.jsx)(Root$4, { "data-ui": "PaneLayout", ...restProps, "data-collapsed": t11, "data-resizing": t12, "data-mounted": t13, ref: setRootElement, children }), $[32] = children, $[33] = restProps, $[34] = t11, $[35] = t12, $[36] = t13, $[37] = t14) : t14 = $[37];
  let t15;
  return $[38] !== paneLayout || $[39] !== t14 ? (t15 = (0, import_jsx_runtime3.jsx)(PaneLayoutContext.Provider, { value: paneLayout, children: t14 }), $[38] = paneLayout, $[39] = t14, $[40] = t15) : t15 = $[40], t15;
}
function _temp$o() {
  return createPaneLayoutController();
}
function isMenuNodeButton(node) {
  return (node.type === "item" || node.type === "group") && node.renderAsButton;
}
var isNotMenuNodeButton = (0, import_negate.default)(isMenuNodeButton);
function resolveMenuNodes(params) {
  const {
    fieldActions = [],
    menuItems,
    menuItemGroups
  } = params, nodes = [];
  let keyOffset = 0;
  for (let i = 0; i < fieldActions.length; i += 1) {
    const a = fieldActions[i];
    nodes.push(mapFieldActionToPaneMenuNode(a, `${i}-${a.type}`));
  }
  keyOffset += nodes.length;
  const groups = [];
  for (const itemGroup of menuItemGroups)
    groups.find((g) => g.key === itemGroup.id) || groups.push({
      type: "group",
      key: itemGroup.id,
      children: [],
      expanded: true,
      renderAsButton: false,
      title: itemGroup.title,
      i18n: itemGroup.i18n
    });
  const ungroupedItems = [];
  for (let i = 0; i < menuItems.length; i += 1) {
    const item = menuItems[i];
    let group = item.group && groups.find((g) => g.key === item.group);
    const disabled = typeof item.disabled == "string" ? {
      reason: item.disabled
    } : item.disabled;
    item.group && !group && (group = {
      type: "group",
      key: item.group,
      disabled,
      expanded: true,
      icon: item.icon,
      title: item.group,
      children: [],
      renderAsButton: false
    }, groups.push(group)), group ? group.children.push({
      type: "item",
      key: `${keyOffset + i}-item`,
      hotkey: item.shortcut,
      icon: item.icon,
      intent: item.intent,
      disabled,
      onAction: () => params.actionHandler(item),
      renderAsButton: item.showAsAction ?? false,
      selected: item.selected,
      title: item.title,
      i18n: item.i18n,
      tone: item.tone
    }) : ungroupedItems.push({
      type: "item",
      key: `${keyOffset + i}-item`,
      hotkey: item.shortcut,
      icon: item.icon,
      intent: item.intent,
      disabled,
      onAction: () => params.actionHandler(item),
      renderAsButton: item.showAsAction ?? false,
      selected: item.selected,
      title: item.title,
      i18n: item.i18n,
      tone: item.tone
    });
  }
  return [...ungroupedItems, ...groups, ...nodes];
}
function mapFieldActionToPaneMenuNode(a, key) {
  return a.type === "divider" ? {
    type: "divider",
    key
  } : a.type === "group" ? {
    type: "group",
    key,
    children: a.children.map((child, childIdx) => mapFieldActionToPaneMenuNode(child, `${key}-${childIdx}-${child.type}`)),
    disabled: a.disabled,
    expanded: a.expanded ?? true,
    icon: a.icon,
    title: a.title,
    i18n: a.i18n,
    renderAsButton: a.renderAsButton ?? false
  } : {
    type: "item",
    key,
    intent: a.intent,
    disabled: a.disabled,
    icon: a.icon,
    iconRight: a.iconRight,
    onAction: a.onAction,
    renderAsButton: a.renderAsButton ?? false,
    selected: a.selected,
    title: a.title,
    i18n: a.i18n,
    tone: a.tone
  };
}
var IntentButton = (0, import_react3.forwardRef)(function(props, ref) {
  const $ = (0, import_react_compiler_runtime.c)(10);
  let intent, restProps;
  $[0] !== props ? ({
    intent,
    ...restProps
  } = props, $[0] = props, $[1] = intent, $[2] = restProps) : (intent = $[1], restProps = $[2]);
  let t0, t1;
  $[3] !== intent ? (t1 = (0, import_react3.forwardRef)(function(linkProps, linkRef) {
    return (0, import_jsx_runtime3.jsx)(IntentLink, { ...linkProps, intent: intent.type, params: intent.params, ref: linkRef, searchParams: intent.searchParams });
  }), $[3] = intent, $[4] = t1) : t1 = $[4], t0 = t1;
  const Link2 = t0;
  let t2;
  return $[5] !== Link2 || $[6] !== props.disabled || $[7] !== ref || $[8] !== restProps ? (t2 = props.disabled ? (0, import_jsx_runtime3.jsx)(Button2, { ...restProps, as: "a", role: "link", "aria-disabled": "true" }) : (0, import_jsx_runtime3.jsx)(Button2, { ...restProps, as: Link2, "data-as": "a", ref }), $[5] = Link2, $[6] = props.disabled, $[7] = ref, $[8] = restProps, $[9] = t2) : t2 = $[9], t2;
});
function InsufficientPermissionsMessageTooltip(t0) {
  const $ = (0, import_react_compiler_runtime.c)(12), {
    reveal,
    context,
    loading,
    children
  } = t0, currentUser = useCurrentUser(), {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  if (!reveal) {
    let t12;
    return $[0] !== children ? (t12 = (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children }), $[0] = children, $[1] = t12) : t12 = $[1], t12;
  }
  let t1;
  $[2] !== context || $[3] !== currentUser || $[4] !== loading || $[5] !== t2 ? (t1 = loading ? t2("insufficient-permissions-message-tooltip.loading-text") : (0, import_jsx_runtime3.jsx)(InsufficientPermissionsMessage, { context, currentUser }), $[2] = context, $[3] = currentUser, $[4] = loading, $[5] = t2, $[6] = t1) : t1 = $[6];
  let t22;
  $[7] !== children ? (t22 = (0, import_jsx_runtime3.jsx)("div", { children }), $[7] = children, $[8] = t22) : t22 = $[8];
  let t3;
  return $[9] !== t1 || $[10] !== t22 ? (t3 = (0, import_jsx_runtime3.jsx)(Tooltip, { content: t1, portal: true, children: t22 }), $[9] = t1, $[10] = t22, $[11] = t3) : t3 = $[11], t3;
}
var POPOVER_PROPS = {
  constrainSize: true,
  placement: "bottom",
  portal: true
};
var getIntent = (templates, item, version) => {
  var _a;
  const typeName = (_a = templates.find((t2) => t2.id === item.templateId)) == null ? void 0 : _a.schemaType;
  if (!typeName) return null;
  const baseParams = {
    template: item.templateId,
    type: typeName,
    version,
    id: item.initialDocumentId
  };
  return {
    type: "create",
    params: item.parameters ? [baseParams, item.parameters] : baseParams,
    searchParams: version ? [["perspective", version]] : void 0
  };
};
function PaneHeaderCreateButton(t0) {
  var _a;
  const $ = (0, import_react_compiler_runtime.c)(60), {
    templateItems
  } = t0, templates = useTemplates(), {
    selectedReleaseId
  } = usePerspective(), {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  let t1;
  $[0] !== templateItems || $[1] !== templates ? (t1 = [...templateItems, ...templates], $[0] = templateItems, $[1] = templates, $[2] = t1) : t1 = $[2];
  const getI18nText = useGetI18nText(t1);
  let t22;
  $[3] !== templateItems ? (t22 = {
    templateItems
  }, $[3] = templateItems, $[4] = t22) : t22 = $[4];
  const [templatePermissions, isTemplatePermissionsLoading] = useTemplatePermissions(t22);
  let t3;
  t3 = !isTemplatePermissionsLoading && (templatePermissions == null ? void 0 : templatePermissions.length) !== 0 && (templatePermissions == null ? void 0 : templatePermissions.every(_temp$n));
  const nothingGranted = t3;
  let t4;
  bb0: {
    if (!templatePermissions) {
      let t53;
      $[5] === Symbol.for("react.memo_cache_sentinel") ? (t53 = {}, $[5] = t53) : t53 = $[5], t4 = t53;
      break bb0;
    }
    let t52;
    $[6] !== templatePermissions ? (t52 = templatePermissions.reduce(_temp2$8, {}), $[6] = templatePermissions, $[7] = t52) : t52 = $[7], t4 = t52;
  }
  const permissionsById = t4;
  if (templateItems.length === 0)
    return null;
  if (nothingGranted) {
    let t52;
    $[8] !== t2 ? (t52 = t2("pane-header.disabled-created-button.aria-label"), $[8] = t2, $[9] = t52) : t52 = $[9];
    let t62;
    $[10] !== t52 ? (t62 = (0, import_jsx_runtime3.jsx)(Button2, { "aria-label": t52, icon: AddIcon, "data-testid": "action-intent-button", disabled: true, mode: "bleed", tooltipProps: null }), $[10] = t52, $[11] = t62) : t62 = $[11];
    let t72;
    return $[12] !== isTemplatePermissionsLoading || $[13] !== t62 ? (t72 = (0, import_jsx_runtime3.jsx)(InsufficientPermissionsMessageTooltip, { context: "create-document-type", reveal: true, loading: isTemplatePermissionsLoading, children: t62 }), $[12] = isTemplatePermissionsLoading, $[13] = t62, $[14] = t72) : t72 = $[14], t72;
  }
  if (templateItems.length === 1) {
    const firstItem = templateItems[0], disabled = !((_a = permissionsById[firstItem.id]) == null ? void 0 : _a.granted);
    let t52;
    $[15] !== firstItem || $[16] !== selectedReleaseId || $[17] !== templates ? (t52 = getIntent(templates, firstItem, selectedReleaseId), $[15] = firstItem, $[16] = selectedReleaseId, $[17] = templates, $[18] = t52) : t52 = $[18];
    const intent = t52;
    if (!intent)
      return null;
    let t62;
    $[19] !== firstItem || $[20] !== getI18nText ? (t62 = getI18nText(firstItem), $[19] = firstItem, $[20] = getI18nText, $[21] = t62) : t62 = $[21];
    const t72 = t62.title, t82 = firstItem.icon || AddIcon;
    let t92;
    $[22] !== t2 ? (t92 = t2("pane-header.create-new-button.tooltip"), $[22] = t2, $[23] = t92) : t92 = $[23];
    let t10;
    $[24] !== t92 ? (t10 = {
      content: t92
    }, $[24] = t92, $[25] = t10) : t10 = $[25];
    let t11;
    $[26] !== disabled || $[27] !== intent || $[28] !== t10 || $[29] !== t62.title || $[30] !== t82 ? (t11 = (0, import_jsx_runtime3.jsx)(IntentButton, { "aria-label": t72, icon: t82, intent, mode: "bleed", disabled, "data-testid": "action-intent-button", tooltipProps: t10 }), $[26] = disabled, $[27] = intent, $[28] = t10, $[29] = t62.title, $[30] = t82, $[31] = t11) : t11 = $[31];
    let t12;
    return $[32] !== disabled || $[33] !== isTemplatePermissionsLoading || $[34] !== t11 ? (t12 = (0, import_jsx_runtime3.jsx)(InsufficientPermissionsMessageTooltip, { reveal: disabled, loading: isTemplatePermissionsLoading, context: "create-document-type", children: t11 }), $[32] = disabled, $[33] = isTemplatePermissionsLoading, $[34] = t11, $[35] = t12) : t12 = $[35], t12;
  }
  let t5;
  $[36] !== t2 ? (t5 = t2("pane-header.create-new-button.tooltip"), $[36] = t2, $[37] = t5) : t5 = $[37];
  let t6;
  $[38] !== t5 ? (t6 = (0, import_jsx_runtime3.jsx)(Button2, { icon: AddIcon, mode: "bleed", "data-testid": "multi-action-intent-button", tooltipProps: {
    content: t5
  } }), $[38] = t5, $[39] = t6) : t6 = $[39];
  let t7;
  if ($[40] !== getI18nText || $[41] !== isTemplatePermissionsLoading || $[42] !== permissionsById || $[43] !== selectedReleaseId || $[44] !== t2 || $[45] !== templateItems || $[46] !== templates) {
    let t82;
    $[48] !== getI18nText || $[49] !== isTemplatePermissionsLoading || $[50] !== permissionsById || $[51] !== selectedReleaseId || $[52] !== t2 || $[53] !== templates ? (t82 = (item, itemIndex) => {
      var _a2;
      const disabled_0 = !((_a2 = permissionsById[item.id]) == null ? void 0 : _a2.granted), intent_0 = getIntent(templates, item, selectedReleaseId), template = templates.find((i) => i.id === item.templateId);
      if (!template || !intent_0)
        return null;
      const Link2 = (0, import_react3.forwardRef)((linkProps, linkRef) => disabled_0 ? (0, import_jsx_runtime3.jsx)("button", { type: "button", disabled: true, ...linkProps, ref: linkRef }) : (0, import_jsx_runtime3.jsx)(IntentLink, { ...linkProps, intent: intent_0.type, params: intent_0.params, searchParams: intent_0.searchParams, ref: linkRef }));
      Link2.displayName = "Link";
      const {
        title
      } = getI18nText({
        ...item,
        title: item.title || getI18nText(template).title
      });
      return (0, import_jsx_runtime3.jsx)(InsufficientPermissionsMessageTooltip, { context: "create-document-type", reveal: disabled_0, loading: isTemplatePermissionsLoading, children: (0, import_jsx_runtime3.jsx)(MenuItem2, { as: Link2, "data-as": disabled_0 ? "button" : "a", text: title, "aria-label": disabled_0 ? t2("pane-header.disabled-created-button.aria-label") : title, disabled: disabled_0, "data-testid": `action-intent-button-${itemIndex}` }) }, item.id);
    }, $[48] = getI18nText, $[49] = isTemplatePermissionsLoading, $[50] = permissionsById, $[51] = selectedReleaseId, $[52] = t2, $[53] = templates, $[54] = t82) : t82 = $[54], t7 = templateItems.map(t82), $[40] = getI18nText, $[41] = isTemplatePermissionsLoading, $[42] = permissionsById, $[43] = selectedReleaseId, $[44] = t2, $[45] = templateItems, $[46] = templates, $[47] = t7;
  } else
    t7 = $[47];
  let t8;
  $[55] !== t7 ? (t8 = (0, import_jsx_runtime3.jsx)(Menu, { children: t7 }), $[55] = t7, $[56] = t8) : t8 = $[56];
  let t9;
  return $[57] !== t6 || $[58] !== t8 ? (t9 = (0, import_jsx_runtime3.jsx)(MenuButton, { button: t6, id: "create-menu", menu: t8, popover: POPOVER_PROPS }), $[57] = t6, $[58] = t8, $[59] = t9) : t9 = $[59], t9;
}
function _temp2$8(acc, permission_0) {
  return acc[permission_0.id] = permission_0, acc;
}
function _temp$n(permission) {
  return !permission.granted;
}
function isNonNullable(value) {
  return value != null;
}
var hashObject = (value) => {
  const sortObject = (v) => typeof v != "object" || !v ? v : Array.isArray(v) ? v.map(sortObject) : Object.entries(v).sort(([keyA], [keyB]) => keyA.localeCompare(keyB, "en"));
  return JSON.stringify(sortObject(((v) => JSON.parse(JSON.stringify(v)))(value)));
};
var PaneHeaderActions = (0, import_react3.memo)(function(props) {
  const {
    initialValueTemplateItems: initialValueTemplateItemsFromStructure = EMPTY_ARRAY$B,
    menuItems = EMPTY_ARRAY$B,
    menuItemGroups = EMPTY_ARRAY$B,
    actionHandlers = EMPTY_OBJECT
  } = props, templates = useTemplates(), handleAction = (0, import_react3.useCallback)((item) => {
    if (typeof item.action == "string" && !(item.action in actionHandlers))
      return console.warn("No handler for action:", item.action), false;
    const handler = (
      // eslint-disable-next-line no-nested-ternary
      typeof item.action == "function" ? item.action : typeof item.action == "string" ? actionHandlers[item.action] : null
    );
    return handler ? (handler(item.params), true) : false;
  }, [actionHandlers]), menuNodes = (0, import_react3.useMemo)(() => resolveMenuNodes({
    actionHandler: handleAction,
    menuItemGroups,
    menuItems: menuItems.filter((item_0) => {
      var _a;
      return ((_a = item_0.intent) == null ? void 0 : _a.type) !== "create";
    })
  }), [handleAction, menuItemGroups, menuItems]), actionNodes = (0, import_react3.useMemo)(() => menuNodes.filter(isMenuNodeButton), [menuNodes]), contextMenuNodes = (0, import_react3.useMemo)(() => menuNodes.filter(isNotMenuNodeButton), [menuNodes]), initialValueTemplateItemFromMenuItems = (0, import_react3.useMemo)(() => menuItems.map((item_1, menuItemIndex) => {
    var _a;
    if (((_a = item_1.intent) == null ? void 0 : _a.type) !== "create") return null;
    const {
      params
    } = item_1.intent;
    if (!params) return null;
    const intentParams = Array.isArray(params) ? params[0] : params, templateParams = Array.isArray(params) ? params[1] : void 0, templateId = intentParams.template || intentParams.type;
    if (!templateId) return null;
    const template = templates.find((t2) => t2.id === templateId);
    if (!template) return null;
    const initialDocumentId = intentParams.id;
    return {
      item: item_1,
      template,
      templateParams,
      menuItemIndex,
      initialDocumentId
    };
  }).filter(isNonNullable).map(({
    initialDocumentId: initialDocumentId_0,
    item: item_2,
    template: template_0,
    menuItemIndex: menuItemIndex_0,
    templateParams: templateParams_0
  }) => ({
    id: `menuItem${menuItemIndex_0}`,
    initialDocumentId: initialDocumentId_0,
    templateId: template_0.id,
    type: "initialValueTemplateItem",
    title: item_2.title || template_0.title,
    i18n: item_2.i18n || template_0.i18n,
    icon: item_2.icon,
    description: template_0.description,
    parameters: templateParams_0,
    schemaType: template_0.schemaType
  })), [menuItems, templates]), combinedInitialValueTemplates = (0, import_react3.useMemo)(() => (0, import_uniqBy.default)([...initialValueTemplateItemFromMenuItems, ...initialValueTemplateItemsFromStructure], (item_3) => hashObject([item_3.initialDocumentId, item_3.templateId, item_3.parameters])), [initialValueTemplateItemFromMenuItems, initialValueTemplateItemsFromStructure]);
  return (0, import_jsx_runtime3.jsxs)(Flex, { gap: 1, children: [
    (0, import_jsx_runtime3.jsx)(PaneHeaderCreateButton, { templateItems: combinedInitialValueTemplates }),
    actionNodes.map((node) => (0, import_jsx_runtime3.jsx)(PaneHeaderActionButton, { node }, node.key)),
    contextMenuNodes.length > 0 && (0, import_jsx_runtime3.jsx)(PaneContextMenuButton, { nodes: contextMenuNodes })
  ] });
});
function MissingSchemaType(props) {
  const $ = (0, import_react_compiler_runtime.c)(16), {
    t: t2
  } = useTranslation(structureLocaleNamespace), {
    layout,
    value
  } = props;
  let t0;
  $[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = {
    Code: "code"
  }, $[0] = t0) : t0 = $[0];
  let t1;
  $[1] !== value._type ? (t1 = {
    documentType: value._type
  }, $[1] = value._type, $[2] = t1) : t1 = $[2];
  let t22;
  $[3] !== t2 || $[4] !== t1 ? (t22 = (0, import_jsx_runtime3.jsx)("em", { children: (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey: "pane-item.missing-schema-type.title", components: t0, values: t1 }) }), $[3] = t2, $[4] = t1, $[5] = t22) : t22 = $[5];
  let t3;
  $[6] === Symbol.for("react.memo_cache_sentinel") ? (t3 = {
    Code: "code"
  }, $[6] = t3) : t3 = $[6];
  let t4;
  $[7] !== value._id ? (t4 = {
    documentId: value._id
  }, $[7] = value._id, $[8] = t4) : t4 = $[8];
  let t5;
  $[9] !== t2 || $[10] !== t4 ? (t5 = (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey: "pane-item.missing-schema-type.subtitle", components: t3, values: t4 }), $[9] = t2, $[10] = t4, $[11] = t5) : t5 = $[11];
  let t6;
  return $[12] !== layout || $[13] !== t22 || $[14] !== t5 ? (t6 = (0, import_jsx_runtime3.jsx)(SanityDefaultPreview, { title: t22, subtitle: t5, media: _temp$m, layout }), $[12] = layout, $[13] = t22, $[14] = t5, $[15] = t6) : t6 = $[15], t6;
}
function _temp$m() {
  return (0, import_jsx_runtime3.jsx)(WarningOutlineIcon, {});
}
function getIconWithFallback(icon, schemaType, defaultIcon) {
  return icon === false ? false : icon || schemaType && schemaType.icon || defaultIcon || false;
}
function PaneItem(props) {
  const $ = (0, import_react_compiler_runtime.c)(42), {
    icon,
    id: id2,
    layout: t0,
    pressed,
    schemaType,
    selected,
    title,
    value,
    margin,
    marginBottom,
    marginTop
  } = props, layout = t0 === void 0 ? "default" : t0, schema = useSchema(), documentPreviewStore = useDocumentPreviewStore(), {
    ChildLink: ChildLink2
  } = usePaneRouter(), documentPresence = useDocumentPresence(id2), hasSchemaType = !!(schemaType && schemaType.name && schema.get(schemaType.name)), [clicked, setClicked] = (0, import_react3.useState)(false);
  let t1;
  bb0: {
    if (value && isSanityDocument(value)) {
      if (!schemaType || !hasSchemaType) {
        let t24;
        $[0] !== value ? (t24 = (0, import_jsx_runtime3.jsx)(MissingSchemaType, { value }), $[0] = value, $[1] = t24) : t24 = $[1], t1 = t24;
        break bb0;
      }
      let t23;
      $[2] !== icon || $[3] !== schemaType ? (t23 = getIconWithFallback(icon, schemaType, DocumentIcon), $[2] = icon, $[3] = schemaType, $[4] = t23) : t23 = $[4];
      let t33;
      $[5] !== documentPresence || $[6] !== documentPreviewStore || $[7] !== layout || $[8] !== schemaType || $[9] !== t23 || $[10] !== value ? (t33 = (0, import_jsx_runtime3.jsx)(PaneItemPreview, { documentPreviewStore, icon: t23, layout, schemaType, value, presence: documentPresence }), $[5] = documentPresence, $[6] = documentPreviewStore, $[7] = layout, $[8] = schemaType, $[9] = t23, $[10] = value, $[11] = t33) : t33 = $[11], t1 = t33;
      break bb0;
    }
    let t22;
    $[12] === Symbol.for("react.memo_cache_sentinel") ? (t22 = {
      opacity: 0.5
    }, $[12] = t22) : t22 = $[12];
    let t32;
    $[13] === Symbol.for("react.memo_cache_sentinel") ? (t32 = (0, import_jsx_runtime3.jsx)(Box, { style: t22, children: (0, import_jsx_runtime3.jsx)(Text, { muted: true, size: 1, children: (0, import_jsx_runtime3.jsx)(ChevronRightIcon, {}) }) }), $[13] = t32) : t32 = $[13];
    let t42;
    $[14] !== icon || $[15] !== schemaType ? (t42 = getIconWithFallback(icon, schemaType, FolderIcon), $[14] = icon, $[15] = schemaType, $[16] = t42) : t42 = $[16];
    let t52;
    $[17] !== t42 || $[18] !== title ? (t52 = (0, import_jsx_runtime3.jsx)(SanityDefaultPreview, { status: t32, icon: t42, layout: "compact", title }), $[17] = t42, $[18] = title, $[19] = t52) : t52 = $[19], t1 = t52;
  }
  const preview = t1;
  let t2;
  $[20] === Symbol.for("react.memo_cache_sentinel") ? (t2 = (e2) => {
    if (e2.metaKey) {
      setClicked(false);
      return;
    }
    setClicked(true);
  }, $[20] = t2) : t2 = $[20];
  const handleClick = t2;
  let t3;
  $[21] === Symbol.for("react.memo_cache_sentinel") ? (t3 = () => setClicked(false), $[21] = t3) : t3 = $[21];
  let t4;
  $[22] !== selected ? (t4 = [selected], $[22] = selected, $[23] = t4) : t4 = $[23], (0, import_react3.useEffect)(t3, t4);
  const [preloading, setPreload] = (0, import_react3.useState)(false), timeoutRef = (0, import_react3.useRef)(null);
  let t5;
  $[24] === Symbol.for("react.memo_cache_sentinel") ? (t5 = () => {
    timeoutRef.current = setTimeout(() => (0, import_react3.startTransition)(() => setPreload(true)), 400);
  }, $[24] = t5) : t5 = $[24];
  const handleMouseEnter = t5;
  let t6;
  $[25] === Symbol.for("react.memo_cache_sentinel") ? (t6 = () => {
    timeoutRef.current && clearTimeout(timeoutRef.current), (0, import_react3.startTransition)(() => setPreload(false));
  }, $[25] = t6) : t6 = $[25];
  const handleMouseLeave = t6, t7 = `pane-item-${title}`, t8 = ChildLink2, t9 = selected || clicked;
  let t10;
  $[26] !== id2 || $[27] !== preloading || $[28] !== schemaType || $[29] !== value ? (t10 = preloading && (schemaType == null ? void 0 : schemaType.name) && value && isSanityDocument(value) && (0, import_jsx_runtime3.jsx)(PreloadDocumentPane, { documentId: id2, documentType: schemaType.name }), $[26] = id2, $[27] = preloading, $[28] = schemaType, $[29] = value, $[30] = t10) : t10 = $[30];
  let t11;
  return $[31] !== id2 || $[32] !== margin || $[33] !== marginBottom || $[34] !== marginTop || $[35] !== pressed || $[36] !== preview || $[37] !== t10 || $[38] !== t7 || $[39] !== t8 || $[40] !== t9 ? (t11 = (0, import_jsx_runtime3.jsxs)(PreviewCard, { "data-testid": t7, __unstable_focusRing: true, as: t8, childId: id2, "data-as": "a", margin, marginBottom, marginTop, onClick: handleClick, onMouseEnter: handleMouseEnter, onMouseLeave: handleMouseLeave, pressed, radius: 2, selected: t9, sizing: "border", tabIndex: -1, tone: "inherit", children: [
    preview,
    t10
  ] }), $[31] = id2, $[32] = margin, $[33] = marginBottom, $[34] = marginTop, $[35] = pressed, $[36] = preview, $[37] = t10, $[38] = t7, $[39] = t8, $[40] = t9, $[41] = t11) : t11 = $[41], t11;
}
function PreloadDocumentPane(props) {
  const $ = (0, import_react_compiler_runtime.c)(2), {
    documentId,
    documentType
  } = props;
  let t0;
  return $[0] !== documentId ? (t0 = getPublishedId(documentId), $[0] = documentId, $[1] = t0) : t0 = $[1], useEditState(t0, documentType), null;
}
PreloadDocumentPane.displayName = "PreloadDocumentPane";
var RenderActionCollectionState = (0, import_react3.memo)((props) => {
  const $ = (0, import_react_compiler_runtime.c)(10), {
    actions,
    children,
    actionProps,
    onActionComplete,
    group
  } = props;
  let t0;
  $[0] !== actionProps.id || $[1] !== actionProps.liveEditSchemaType || $[2] !== children ? (t0 = (t12) => {
    const {
      states
    } = t12;
    return (0, import_jsx_runtime3.jsx)(ActionsGuardWrapper, { states, documentId: actionProps.liveEditSchemaType ? getPublishedId(actionProps.id) : getDraftId(actionProps.id), children });
  }, $[0] = actionProps.id, $[1] = actionProps.liveEditSchemaType, $[2] = children, $[3] = t0) : t0 = $[3];
  let t1;
  return $[4] !== actionProps || $[5] !== actions || $[6] !== group || $[7] !== onActionComplete || $[8] !== t0 ? (t1 = (0, import_jsx_runtime3.jsx)(GetHookCollectionState, { onReset: onActionComplete, hooks: actions, args: actionProps, group, children: t0 }), $[4] = actionProps, $[5] = actions, $[6] = group, $[7] = onActionComplete, $[8] = t0, $[9] = t1) : t1 = $[9], t1;
});
RenderActionCollectionState.displayName = "Memo(RenderActionCollectionState)";
var SUPPORTED_LINKED_TO_CANVAS_ACTIONS = ["delete", "duplicate", "publish", "unpublish", "unlinkFromCanvas", "editInCanvas", "linkToCanvas", "schedule"];
var ActionsGuardWrapper = (props) => {
  const $ = (0, import_react_compiler_runtime.c)(9), {
    states,
    children,
    documentId
  } = props, {
    t: t2
  } = useTranslation(structureLocaleNamespace), {
    isLinked
  } = useCanvasCompanionDoc(documentId);
  let t0;
  bb0: {
    if (isLinked) {
      let t12;
      if ($[0] !== children || $[1] !== states || $[2] !== t2) {
        let t22;
        $[4] !== t2 ? (t22 = (s2) => !s2.action || !SUPPORTED_LINKED_TO_CANVAS_ACTIONS.includes(s2.action) ? {
          ...s2,
          disabled: true,
          title: t2("action.disabled-by-canvas.tooltip")
        } : s2, $[4] = t2, $[5] = t22) : t22 = $[5], t12 = children({
          states: states.map(t22)
        }), $[0] = children, $[1] = states, $[2] = t2, $[3] = t12;
      } else
        t12 = $[3];
      t0 = t12;
      break bb0;
    }
    let t1;
    $[6] !== children || $[7] !== states ? (t1 = children({
      states
    }), $[6] = children, $[7] = states, $[8] = t1) : t1 = $[8], t0 = t1;
  }
  return t0;
};
var RenderBadgeCollectionState = (0, import_react3.memo)((props) => {
  const $ = (0, import_react_compiler_runtime.c)(4), {
    badges,
    children,
    badgeProps
  } = props;
  let t0;
  return $[0] !== badgeProps || $[1] !== badges || $[2] !== children ? (t0 = (0, import_jsx_runtime3.jsx)(GetHookCollectionState, { hooks: badges, args: badgeProps, children }), $[0] = badgeProps, $[1] = badges, $[2] = children, $[3] = t0) : t0 = $[3], t0;
});
RenderBadgeCollectionState.displayName = "Memo(RenderBadgeCollectionState)";
function useCreatePathSyncChannel() {
  const $ = (0, import_react_compiler_runtime.c)(1);
  let t0, t1;
  return $[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = new Subject(), $[0] = t1) : t1 = $[0], t0 = t1, t0;
}
var DIFF_VIEW_SEARCH_PARAMETER = "diffView";
var DIFF_VIEW_PREVIOUS_DOCUMENT_SEARCH_PARAMETER = "previousDocument";
var DIFF_VIEW_NEXT_DOCUMENT_SEARCH_PARAMETER = "nextDocument";
var DIFF_SEARCH_PARAM_DELIMITER = ",";
function useDiffViewRouter() {
  const $ = (0, import_react_compiler_runtime.c)(9), {
    navigate,
    state: routerState
  } = useRouter();
  let t0;
  $[0] !== navigate || $[1] !== routerState ? (t0 = (t12) => {
    const {
      mode,
      previousDocument,
      nextDocument
    } = t12, next = {
      ...(0, import_fromPairs.default)(routerState._searchParams),
      ...mode ? {
        [DIFF_VIEW_SEARCH_PARAMETER]: mode
      } : {},
      ...previousDocument ? {
        [DIFF_VIEW_PREVIOUS_DOCUMENT_SEARCH_PARAMETER]: [previousDocument.type, previousDocument.id].join(DIFF_SEARCH_PARAM_DELIMITER)
      } : {},
      ...nextDocument ? {
        [DIFF_VIEW_NEXT_DOCUMENT_SEARCH_PARAMETER]: [nextDocument.type, nextDocument.id].join(DIFF_SEARCH_PARAM_DELIMITER)
      } : {}
    };
    navigate({
      ...routerState,
      _searchParams: (0, import_toPairs.default)(next)
    });
  }, $[0] = navigate, $[1] = routerState, $[2] = t0) : t0 = $[2];
  const navigateDiffView = t0;
  let t1;
  $[3] !== navigate || $[4] !== routerState ? (t1 = () => {
    navigate({
      ...routerState,
      _searchParams: (routerState._searchParams ?? []).filter(_temp$l)
    });
  }, $[3] = navigate, $[4] = routerState, $[5] = t1) : t1 = $[5];
  const exitDiffView = t1;
  let t2;
  return $[6] !== exitDiffView || $[7] !== navigateDiffView ? (t2 = {
    navigateDiffView,
    exitDiffView
  }, $[6] = exitDiffView, $[7] = navigateDiffView, $[8] = t2) : t2 = $[8], t2;
}
function _temp$l(t0) {
  const [key] = t0;
  return ![DIFF_VIEW_SEARCH_PARAMETER, DIFF_VIEW_PREVIOUS_DOCUMENT_SEARCH_PARAMETER, DIFF_VIEW_NEXT_DOCUMENT_SEARCH_PARAMETER].includes(key);
}
var diffViewModes = ["version"];
function isDiffViewMode(maybeDiffViewMode) {
  return diffViewModes.includes(maybeDiffViewMode);
}
function useDiffViewState({
  onParamsError
} = {}) {
  const {
    state: routerState
  } = useRouter(), searchParams = new URLSearchParams(routerState._searchParams), previousDocument = searchParams.get(DIFF_VIEW_PREVIOUS_DOCUMENT_SEARCH_PARAMETER), nextDocument = searchParams.get(DIFF_VIEW_NEXT_DOCUMENT_SEARCH_PARAMETER), mode = searchParams.get(DIFF_VIEW_SEARCH_PARAMETER), anyParamSet = [previousDocument, nextDocument, mode].some((param) => param !== null), params = (0, import_react3.useMemo)(() => parseParams({
    previousDocument: previousDocument ?? "",
    nextDocument: nextDocument ?? "",
    mode: mode ?? ""
  }), [mode, nextDocument, previousDocument]);
  return (0, import_react3.useEffect)(() => {
    params.result === "error" && anyParamSet && (onParamsError == null ? void 0 : onParamsError(params.errors));
  }, [anyParamSet, onParamsError, params]), params.result === "error" ? {
    isActive: false
  } : {
    state: "ready",
    isActive: true,
    ...params.params
  };
}
function parseParams({
  previousDocument,
  nextDocument,
  mode
}) {
  const errors = [], [previousDocumentType, previousDocumentId] = previousDocument.split(DIFF_SEARCH_PARAM_DELIMITER).filter(Boolean), [nextDocumentType, nextDocumentId] = nextDocument.split(DIFF_SEARCH_PARAM_DELIMITER).filter(Boolean);
  return isDiffViewMode(mode) || errors.push(["invalidModeParam", mode]), (typeof previousDocumentType > "u" || typeof previousDocumentId > "u") && errors.push(["invalidPreviousDocumentParam", previousDocument]), (typeof nextDocumentType > "u" || typeof nextDocumentId > "u") && errors.push(["invalidNextDocumentParam", nextDocument]), errors.length !== 0 ? {
    result: "error",
    errors
  } : {
    result: "success",
    params: {
      mode,
      documents: {
        previous: {
          type: previousDocumentType,
          id: previousDocumentId
        },
        next: {
          type: nextDocumentType,
          id: nextDocumentId
        }
      }
    }
  };
}
function useScrollMirror(elements) {
  const $ = (0, import_react_compiler_runtime.c)(3);
  let t0, t1;
  $[0] !== elements ? (t0 = () => {
    const existentElements = elements.filter(_temp$k);
    if (existentElements.length === 0)
      return;
    const scrollMirror = new s(existentElements);
    return () => scrollMirror.destroy();
  }, t1 = [elements], $[0] = elements, $[1] = t0, $[2] = t1) : (t0 = $[1], t1 = $[2]), (0, import_react3.useEffect)(t0, t1);
}
function _temp$k(element) {
  return element !== null;
}
var VersionModeHeaderLayout = dt.header`
  display: grid;
  grid-area: header;
  grid-template-columns: 1fr min-content 1fr;
  border-block-end: 1px solid var(--card-border-color);
`;
var VersionModeHeaderLayoutSection = dt.div`
  display: flex;
  justify-content: space-between;
  align-items: center;
`;
var VersionModeHeader = ({
  documentId,
  state: state2
}) => {
  const {
    t: t2
  } = useTranslation(structureLocaleNamespace), {
    data: documentVersions
  } = useDocumentVersions({
    documentId
  }), {
    exitDiffView,
    navigateDiffView
  } = useDiffViewRouter(), {
    documents
  } = useDiffViewState(), activeReleases = useActiveReleases(), releasesIds = documentVersions.flatMap((id2) => getVersionFromId(id2) ?? []), releases = (0, import_react3.useMemo)(() => activeReleases.data.filter((release) => {
    const releaseId = getReleaseIdFromReleaseDocumentId(release._id);
    return typeof releaseId < "u" && releasesIds.includes(releaseId);
  }), [activeReleases.data, releasesIds]), onSelectPreviousRelease = (0, import_react3.useCallback)((selectedDocumentId) => {
    typeof (documents == null ? void 0 : documents.previous) < "u" && navigateDiffView({
      previousDocument: {
        ...documents.previous,
        id: selectedDocumentId
      }
    });
  }, [documents == null ? void 0 : documents.previous, navigateDiffView]), onSelectNextRelease = (0, import_react3.useCallback)((selectedDocumentId_0) => {
    typeof (documents == null ? void 0 : documents.next) < "u" && navigateDiffView({
      nextDocument: {
        ...documents.next,
        id: selectedDocumentId_0
      }
    });
  }, [documents == null ? void 0 : documents.next, navigateDiffView]);
  return (0, import_jsx_runtime3.jsxs)(VersionModeHeaderLayout, { children: [
    (0, import_jsx_runtime3.jsxs)(VersionModeHeaderLayoutSection, { children: [
      (0, import_jsx_runtime3.jsx)(Box, { padding: 4, children: (0, import_jsx_runtime3.jsx)(Text, { as: "h1", size: 1, muted: true, children: t2("compare-versions.title") }) }),
      typeof (documents == null ? void 0 : documents.previous) < "u" && (0, import_jsx_runtime3.jsx)(VersionMenu, { releases, onSelectRelease: onSelectPreviousRelease, role: "previous", documentId, state: state2, document: documents.previous })
    ] }),
    (0, import_jsx_runtime3.jsx)(Flex, { align: "center", paddingX: 3, children: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: (0, import_jsx_runtime3.jsx)(TransferIcon, {}) }) }),
    (0, import_jsx_runtime3.jsxs)(VersionModeHeaderLayoutSection, { children: [
      typeof (documents == null ? void 0 : documents.next) < "u" && (0, import_jsx_runtime3.jsx)(VersionMenu, { releases, onSelectRelease: onSelectNextRelease, role: "next", documentId, state: state2, document: documents.next }),
      (0, import_jsx_runtime3.jsx)(Box, { padding: 3, style: {
        justifySelf: "end"
      }, children: (0, import_jsx_runtime3.jsx)(Button, { icon: CloseIcon, mode: "bleed", onClick: exitDiffView, padding: 2 }) })
    ] })
  ] });
};
var VersionMenu = ({
  releases = [],
  onSelectRelease,
  role,
  documentId,
  document: document2
}) => {
  const {
    published,
    draft
  } = useEditState(getPublishedId(document2.id), document2.type), selected = (0, import_react3.useMemo)(() => findRelease(document2.id, releases), [document2.id, releases]), {
    t: tStructure
  } = useTranslation(structureLocaleNamespace), {
    t: tCore
  } = useTranslation(), {
    document: {
      drafts: {
        enabled: isDraftModelEnabled
      }
    }
  } = useWorkspace();
  return (0, import_jsx_runtime3.jsx)(MenuButton, { id: role, button: (0, import_jsx_runtime3.jsx)(Button, { type: "button", mode: "bleed", padding: 2, paddingRight: 3, radius: "full", selected: true, ...getMenuButtonProps({
    selected,
    tCore,
    tStructure
  }) }), menu: (0, import_jsx_runtime3.jsxs)(Menu, { children: [
    published && (0, import_jsx_runtime3.jsx)(VersionMenuItem, { type: "published", onSelect: onSelectRelease, isSelected: selected === "published", documentId }),
    isDraftModelEnabled && draft && (0, import_jsx_runtime3.jsx)(VersionMenuItem, { type: "draft", onSelect: onSelectRelease, isSelected: selected === "draft", documentId }),
    releases.map((release) => (0, import_jsx_runtime3.jsx)(VersionMenuItem, { release, onSelect: onSelectRelease, isSelected: typeof selected != "string" && (selected == null ? void 0 : selected._id) === release._id, documentId }, release._id))
  ] }) });
};
var VersionMenuItem = ({
  type: type2,
  release,
  onSelect,
  isSelected,
  documentId
}) => {
  const {
    t: tCore
  } = useTranslation(), {
    t: tStructure
  } = useTranslation(structureLocaleNamespace), onClick = (0, import_react3.useCallback)(() => {
    if (type2 === "draft") {
      onSelect(getDraftId(documentId));
      return;
    }
    if (type2 === "published") {
      onSelect(getPublishedId(documentId));
      return;
    }
    typeof (release == null ? void 0 : release._id) < "u" && onSelect(getVersionId(documentId, getReleaseIdFromReleaseDocumentId(release._id)));
  }, [type2, onSelect, documentId, release == null ? void 0 : release._id]);
  if (type2)
    return (0, import_jsx_runtime3.jsx)(MenuItem, { padding: 1, paddingRight: 3, onClick, pressed: isSelected, children: (0, import_jsx_runtime3.jsxs)(Flex, { gap: 1, children: [
      (0, import_jsx_runtime3.jsx)(ReleaseAvatar, { padding: 2, tone: type2 === "published" ? "positive" : "caution" }),
      (0, import_jsx_runtime3.jsx)(Box, { paddingY: 2, children: (0, import_jsx_runtime3.jsx)(Text, { size: 1, weight: "medium", children: tStructure(["compare-versions.status", type2].join(".")) }) })
    ] }) });
  const tone_0 = release ? getReleaseTone(release) : "neutral";
  return (0, import_jsx_runtime3.jsx)(MenuItem, { padding: 1, paddingRight: 3, onClick, pressed: isSelected, children: (0, import_jsx_runtime3.jsxs)(Flex, { gap: 1, children: [
    (0, import_jsx_runtime3.jsx)(ReleaseAvatar, { padding: 2, tone: tone_0 }),
    (0, import_jsx_runtime3.jsxs)(Stack, { flex: 1, paddingY: 2, paddingRight: 2, space: 2, children: [
      (0, import_jsx_runtime3.jsx)(Text, { size: 1, weight: "medium", children: release.metadata.title || tCore("release.placeholder-untitled-release") }),
      ["asap", "undecided"].includes(release.metadata.releaseType) && (0, import_jsx_runtime3.jsx)(Text, { muted: true, size: 1, children: tCore(`release.type.${release.metadata.releaseType}`) }),
      release.metadata.releaseType === "scheduled" && (0, import_jsx_runtime3.jsx)(Text, { muted: true, size: 1, children: formatRelativeLocalePublishDate(release) })
    ] }),
    (0, import_jsx_runtime3.jsx)(Flex, { flex: "none", children: isReleaseScheduledOrScheduling(release) && (0, import_jsx_runtime3.jsx)(Box, { padding: 2, children: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: (0, import_jsx_runtime3.jsx)(LockIcon, {}) }) }) })
  ] }) });
};
function getMenuButtonProps({
  selected,
  tCore,
  tStructure
}) {
  if (typeof selected > "u")
    return {
      text: tCore("common.loading"),
      tone: "neutral",
      disabled: true
    };
  if (isReleaseDocument(selected)) {
    const tone2 = selected ? getReleaseTone(selected) : "neutral";
    return {
      text: (selected == null ? void 0 : selected.metadata.title) || tCore("release.placeholder-untitled-release"),
      icon: (0, import_jsx_runtime3.jsx)(ReleaseAvatar, { padding: 1, tone: tone2 }),
      iconRight: selected && isReleaseScheduledOrScheduling(selected) ? (0, import_jsx_runtime3.jsx)(LockIcon, {}) : void 0,
      tone: tone2
    };
  }
  const tone = selected === "published" ? "positive" : "caution";
  return {
    text: tStructure(["compare-versions.status", selected].join(".")),
    icon: (0, import_jsx_runtime3.jsx)(ReleaseAvatar, { padding: 1, tone }),
    tone
  };
}
function findRelease(documentId, releases) {
  return isPublishedId(documentId) ? "published" : isDraftId(documentId) ? "draft" : releases.find(({
    _id
  }) => getReleaseIdFromReleaseDocumentId(_id) === getVersionFromId(documentId));
}
var DialogLayout = dt.div`
  --offset-block: 40px;
  display: grid;
  height: calc(100vh - var(--offset-block));
  min-height: 0;
  overflow: hidden;
  grid-template-areas:
    'header header'
    'previous-document next-document';
  grid-template-columns: 1fr 1fr;
  grid-template-rows: min-content minmax(0, 1fr);
`;
function pickDocumentLayoutComponent(plugin) {
  var _a, _b;
  return (_b = (_a = plugin.document) == null ? void 0 : _a.components) == null ? void 0 : _b.unstable_layout;
}
function usePathSyncChannel(t0) {
  const $ = (0, import_react_compiler_runtime.c)(11), {
    syncChannel,
    id: id2
  } = t0;
  let t1;
  $[0] !== id2 || $[1] !== syncChannel ? (t1 = (state2) => syncChannel.next({
    ...state2,
    source: id2
  }), $[0] = id2, $[1] = syncChannel, $[2] = t1) : t1 = $[2];
  const push = t1;
  let t2, t3;
  if ($[3] !== id2 || $[4] !== syncChannel) {
    let t42;
    $[6] !== id2 ? (t42 = (t5) => {
      const {
        source
      } = t5;
      return source !== id2;
    }, $[6] = id2, $[7] = t42) : t42 = $[7], t3 = syncChannel.pipe(distinctUntilChanged(_temp$j), filter(t42), map(_temp2$7)), $[3] = id2, $[4] = syncChannel, $[5] = t3;
  } else
    t3 = $[5];
  t2 = t3;
  const path = t2;
  let t4;
  return $[8] !== path || $[9] !== push ? (t4 = {
    path,
    push
  }, $[8] = path, $[9] = push, $[10] = t4) : t4 = $[10], t4;
}
function _temp2$7(state_0) {
  return state_0.path;
}
function _temp$j(previous, next) {
  return (0, import_react_fast_compare.default)(previous.path, next.path);
}
var Scroller$5 = dt.div`
  position: relative;
  height: 100%;
  overflow: auto;
  scroll-behavior: smooth;
  scrollbar-width: var(--scrollbar-width);
  overscroll-behavior: contain;
  will-change: scroll-position;
`;
var DiffViewPaneLayout = dt(Card)`
  position: relative;
  grid-area: var(--grid-area);
`;
var Container2 = dt(Container)`
  width: auto;
`;
var DiffViewPane = (0, import_react3.forwardRef)(function(t0, ref) {
  const $ = (0, import_react_compiler_runtime.c)(36), {
    role,
    documentType,
    documentId,
    scrollElement,
    syncChannel,
    compareDocument
  } = t0, containerElement = (0, import_react3.useRef)(null), [portalElement, setPortalElement] = (0, import_react3.useState)(null), [boundaryElement, setBoundaryElement] = (0, import_react3.useState)(null);
  let t1;
  $[0] !== compareDocument || $[1] !== documentId || $[2] !== documentType || $[3] !== role || $[4] !== scrollElement || $[5] !== syncChannel ? (t1 = () => (0, import_jsx_runtime3.jsx)(DiffViewDocument, { compareDocument, documentId, documentType, role, scrollElement, syncChannel }), $[0] = compareDocument, $[1] = documentId, $[2] = documentType, $[3] = role, $[4] = scrollElement, $[5] = syncChannel, $[6] = t1) : t1 = $[6];
  const t2 = t1;
  let t3;
  $[7] !== t2 ? (t3 = {
    pick: pickDocumentLayoutComponent,
    defaultComponent: t2
  }, $[7] = t2, $[8] = t3) : t3 = $[8];
  const DocumentLayout2 = useMiddlewareComponents(t3);
  let t4;
  $[9] === Symbol.for("react.memo_cache_sentinel") ? (t4 = {
    isInteractive: false,
    onOpenReviewChanges: import_noop.default,
    onSetFocus: import_noop.default,
    isReviewChangesOpen: false
  }, $[9] = t4) : t4 = $[9];
  const t5 = `${role}-document`;
  let t6;
  $[10] !== t5 ? (t6 = {
    "--grid-area": t5
  }, $[10] = t5, $[11] = t6) : t6 = $[11];
  const t7 = t6, t8 = role === "next", t9 = role !== "next" && "none";
  let t10;
  $[12] !== t9 ? (t10 = {
    "--scrollbar-width": t9
  }, $[12] = t9, $[13] = t10) : t10 = $[13];
  const t11 = t10;
  let t12;
  $[14] !== DocumentLayout2 || $[15] !== documentId || $[16] !== documentType ? (t12 = (0, import_jsx_runtime3.jsx)(DialogProvider, { position: "absolute", children: (0, import_jsx_runtime3.jsx)(Container2, { ref: containerElement, padding: 4, width: 1, children: (0, import_jsx_runtime3.jsx)(DocumentLayout2, { documentId, documentType }) }) }), $[14] = DocumentLayout2, $[15] = documentId, $[16] = documentType, $[17] = t12) : t12 = $[17];
  let t13;
  $[18] !== portalElement || $[19] !== t12 ? (t13 = (0, import_jsx_runtime3.jsx)(PortalProvider, { element: portalElement, children: t12 }), $[18] = portalElement, $[19] = t12, $[20] = t13) : t13 = $[20];
  let t14;
  $[21] !== ref || $[22] !== t11 || $[23] !== t13 ? (t14 = (0, import_jsx_runtime3.jsx)(Scroller$5, { ref, style: t11, children: t13 }), $[21] = ref, $[22] = t11, $[23] = t13, $[24] = t14) : t14 = $[24];
  let t15;
  $[25] === Symbol.for("react.memo_cache_sentinel") ? (t15 = (0, import_jsx_runtime3.jsx)("div", { "data-testid": "diffView-document-panel-portal", ref: setPortalElement }), $[25] = t15) : t15 = $[25];
  let t16;
  $[26] !== t14 || $[27] !== t7 || $[28] !== t8 ? (t16 = (0, import_jsx_runtime3.jsxs)(DiffViewPaneLayout, { ref: setBoundaryElement, style: t7, borderLeft: t8, children: [
    t14,
    t15
  ] }), $[26] = t14, $[27] = t7, $[28] = t8, $[29] = t16) : t16 = $[29];
  let t17;
  $[30] !== boundaryElement || $[31] !== t16 ? (t17 = (0, import_jsx_runtime3.jsx)(BoundaryElementProvider, { element: boundaryElement, children: t16 }), $[30] = boundaryElement, $[31] = t16, $[32] = t17) : t17 = $[32];
  let t18;
  return $[33] !== scrollElement || $[34] !== t17 ? (t18 = (0, import_jsx_runtime3.jsx)(ReviewChangesContext.Provider, { value: t4, children: (0, import_jsx_runtime3.jsx)(ChangeIndicatorsTracker, { children: (0, import_jsx_runtime3.jsx)(VirtualizerScrollInstanceProvider, { scrollElement, containerElement, children: t17 }) }) }), $[33] = scrollElement, $[34] = t17, $[35] = t18) : t18 = $[35], t18;
});
var DiffViewDocument = (t0) => {
  const $ = (0, import_react_compiler_runtime.c)(56), {
    role,
    documentType,
    documentId,
    syncChannel,
    compareDocument
  } = t0;
  let t1;
  $[0] !== compareDocument ? (t1 = {
    compareDocument
  }, $[0] = compareDocument, $[1] = t1) : t1 = $[1];
  const compareValue = useCompareValue(t1), [patchChannel] = (0, import_react3.useState)(_temp$i);
  let t2;
  $[2] !== documentId ? (t2 = getPublishedId(documentId), $[2] = documentId, $[3] = t2) : t2 = $[3];
  let t3;
  $[4] !== documentId ? (t3 = perspectiveName(documentId), $[4] = documentId, $[5] = t3) : t3 = $[5];
  let t4;
  $[6] !== documentId ? (t4 = getVersionFromId(documentId), $[6] = documentId, $[7] = t4) : t4 = $[7];
  let t5;
  $[8] !== compareValue || $[9] !== documentType || $[10] !== t2 || $[11] !== t3 || $[12] !== t4 ? (t5 = {
    documentId: t2,
    documentType,
    selectedPerspectiveName: t3,
    releaseId: t4,
    comparisonValue: compareValue
  }, $[8] = compareValue, $[9] = documentType, $[10] = t2, $[11] = t3, $[12] = t4, $[13] = t5) : t5 = $[13];
  const t6 = useDocumentForm(t5);
  let collapsedFieldSets, collapsedPaths, documentForm, formState, onBlur, onChange, onFocus, onProgrammaticFocus, onSetActiveFieldGroup, onSetCollapsedFieldSet, onSetCollapsedPath, ready, schemaType, value;
  $[14] !== t6 ? ({
    formState,
    onChange,
    onFocus,
    onBlur,
    onSetActiveFieldGroup,
    onSetCollapsedFieldSet,
    onSetCollapsedPath,
    collapsedFieldSets,
    ready,
    collapsedPaths,
    schemaType,
    value,
    onProgrammaticFocus,
    ...documentForm
  } = t6, $[14] = t6, $[15] = collapsedFieldSets, $[16] = collapsedPaths, $[17] = documentForm, $[18] = formState, $[19] = onBlur, $[20] = onChange, $[21] = onFocus, $[22] = onProgrammaticFocus, $[23] = onSetActiveFieldGroup, $[24] = onSetCollapsedFieldSet, $[25] = onSetCollapsedPath, $[26] = ready, $[27] = schemaType, $[28] = value) : (collapsedFieldSets = $[15], collapsedPaths = $[16], documentForm = $[17], formState = $[18], onBlur = $[19], onChange = $[20], onFocus = $[21], onProgrammaticFocus = $[22], onSetActiveFieldGroup = $[23], onSetCollapsedFieldSet = $[24], onSetCollapsedPath = $[25], ready = $[26], schemaType = $[27], value = $[28]);
  const isLoading = formState === null || !ready;
  let t7;
  $[29] !== role || $[30] !== syncChannel ? (t7 = {
    id: role,
    syncChannel
  }, $[29] = role, $[30] = syncChannel, $[31] = t7) : t7 = $[31];
  const pathSyncChannel = usePathSyncChannel(t7);
  let t8;
  $[32] !== documentForm || $[33] !== pathSyncChannel || $[34] !== role ? (t8 = (path) => {
    documentForm.onPathOpen(path), pathSyncChannel.push({
      source: role,
      path
    });
  }, $[32] = documentForm, $[33] = pathSyncChannel, $[34] = role, $[35] = t8) : t8 = $[35];
  const onPathOpen = t8;
  let t10, t9;
  $[36] !== onProgrammaticFocus || $[37] !== pathSyncChannel.path ? (t9 = () => {
    const subscription = pathSyncChannel.path.subscribe(onProgrammaticFocus);
    return () => subscription.unsubscribe();
  }, t10 = [onProgrammaticFocus, pathSyncChannel.path], $[36] = onProgrammaticFocus, $[37] = pathSyncChannel.path, $[38] = t10, $[39] = t9) : (t10 = $[38], t9 = $[39]), (0, import_react3.useEffect)(t9, t10);
  let t11;
  return $[40] !== collapsedFieldSets || $[41] !== collapsedPaths || $[42] !== formState || $[43] !== isLoading || $[44] !== onBlur || $[45] !== onChange || $[46] !== onFocus || $[47] !== onPathOpen || $[48] !== onSetActiveFieldGroup || $[49] !== onSetCollapsedFieldSet || $[50] !== onSetCollapsedPath || $[51] !== patchChannel || $[52] !== role || $[53] !== schemaType || $[54] !== value ? (t11 = isLoading ? (0, import_jsx_runtime3.jsx)(LoadingBlock, { showText: true }) : (0, import_jsx_runtime3.jsx)(CommentsEnabledContext.Provider, { value: {
    enabled: false,
    mode: null
  }, children: (0, import_jsx_runtime3.jsx)(FormBuilder, { __internal_patchChannel: patchChannel, id: `diffView-pane-${role}`, onChange, onPathFocus: onFocus, onPathOpen, onPathBlur: onBlur, onFieldGroupSelect: onSetActiveFieldGroup, onSetFieldSetCollapsed: onSetCollapsedFieldSet, onSetPathCollapsed: onSetCollapsedPath, collapsedPaths, collapsedFieldSets, focusPath: formState.focusPath, changed: formState.changed, focused: formState.focused, groups: formState.groups, validation: formState.validation, members: formState.members, presence: formState.presence, schemaType, value }) }), $[40] = collapsedFieldSets, $[41] = collapsedPaths, $[42] = formState, $[43] = isLoading, $[44] = onBlur, $[45] = onChange, $[46] = onFocus, $[47] = onPathOpen, $[48] = onSetActiveFieldGroup, $[49] = onSetCollapsedFieldSet, $[50] = onSetCollapsedPath, $[51] = patchChannel, $[52] = role, $[53] = schemaType, $[54] = value, $[55] = t11) : t11 = $[55], t11;
};
function perspectiveName(documentId) {
  if (isVersionId(documentId))
    return getVersionFromId(documentId);
  if (isPublishedId(documentId))
    return "published";
}
function useCompareValue(t0) {
  const $ = (0, import_react_compiler_runtime.c)(4), {
    compareDocument
  } = t0;
  let t1;
  $[0] !== compareDocument.id ? (t1 = getPublishedId(compareDocument.id), $[0] = compareDocument.id, $[1] = t1) : t1 = $[1];
  const t2 = compareDocument.type;
  let t3;
  $[2] !== compareDocument.id ? (t3 = getVersionFromId(compareDocument.id), $[2] = compareDocument.id, $[3] = t3) : t3 = $[3];
  const compareDocumentEditState = useEditState(t1, t2, "low", t3);
  let t4;
  bb0: {
    if (isVersionId(compareDocument.id)) {
      t4 = compareDocumentEditState.version ?? void 0;
      break bb0;
    }
    if (isDraftId(compareDocument.id)) {
      t4 = compareDocumentEditState.draft ?? void 0;
      break bb0;
    }
    if (isPublishedId(compareDocument.id)) {
      t4 = compareDocumentEditState.published ?? void 0;
      break bb0;
    }
    t4 = void 0;
  }
  return t4;
}
function _temp$i() {
  return createPatchChannel();
}
var Link = dt.a`
  flex: 1;
  text-decoration: none;
  color: inherit;
`;
var EditReferenceLinkComponent = (t0) => {
  const $ = (0, import_react_compiler_runtime.c)(6), {
    children,
    documentId: _documentId,
    documentType
  } = t0;
  let t1;
  $[0] !== _documentId || $[1] !== documentType ? (t1 = {
    intent: "edit",
    params: {
      id: _documentId,
      type: documentType
    }
  }, $[0] = _documentId, $[1] = documentType, $[2] = t1) : t1 = $[2];
  const {
    href
  } = useIntentLink(t1);
  let t2;
  return $[3] !== children || $[4] !== href ? (t2 = (0, import_jsx_runtime3.jsx)(Link, { href, target: "_blank", rel: "noopener noreferrer", children }), $[3] = children, $[4] = href, $[5] = t2) : t2 = $[5], t2;
};
var DiffView = (t0) => {
  const $ = (0, import_react_compiler_runtime.c)(24), {
    documentId
  } = t0, {
    documents,
    state: state2,
    mode
  } = useDiffViewState(), {
    exitDiffView
  } = useDiffViewRouter(), syncChannel = useCreatePathSyncChannel(), [previousPaneElement, setPreviousPaneElement] = (0, import_react3.useState)(null), [nextPaneElement, setNextPaneElement] = (0, import_react3.useState)(null), referenceInputOptionsContext = (0, import_react3.useContext)(ReferenceInputOptionsContext);
  let t1, t2;
  $[0] !== referenceInputOptionsContext ? (t2 = {
    ...referenceInputOptionsContext,
    disableNew: true,
    EditReferenceLinkComponent
  }, $[0] = referenceInputOptionsContext, $[1] = t2) : t2 = $[1], t1 = t2;
  const diffViewReferenceInputOptionsContext = t1;
  let t3;
  $[2] !== nextPaneElement || $[3] !== previousPaneElement ? (t3 = [previousPaneElement, nextPaneElement], $[2] = nextPaneElement, $[3] = previousPaneElement, $[4] = t3) : t3 = $[4], useScrollMirror(t3);
  let t4;
  $[5] !== documentId || $[6] !== mode || $[7] !== state2 ? (t4 = mode === "version" && (0, import_jsx_runtime3.jsx)(VersionModeHeader, { documentId, state: state2 }), $[5] = documentId, $[6] = mode, $[7] = state2, $[8] = t4) : t4 = $[8];
  let t5;
  $[9] !== documents || $[10] !== nextPaneElement || $[11] !== previousPaneElement || $[12] !== state2 || $[13] !== syncChannel ? (t5 = state2 === "ready" && (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [
    (0, import_jsx_runtime3.jsx)(DiffViewPane, { documentType: documents.previous.type, documentId: documents.previous.id, role: "previous", ref: setPreviousPaneElement, scrollElement: previousPaneElement, syncChannel, compareDocument: documents.previous }),
    (0, import_jsx_runtime3.jsx)(DiffViewPane, { documentType: documents.next.type, documentId: documents.next.id, role: "next", ref: setNextPaneElement, scrollElement: nextPaneElement, syncChannel, compareDocument: documents.previous })
  ] }), $[9] = documents, $[10] = nextPaneElement, $[11] = previousPaneElement, $[12] = state2, $[13] = syncChannel, $[14] = t5) : t5 = $[14];
  let t6;
  $[15] !== t4 || $[16] !== t5 ? (t6 = (0, import_jsx_runtime3.jsxs)(DialogLayout, { children: [
    t4,
    t5
  ] }), $[15] = t4, $[16] = t5, $[17] = t6) : t6 = $[17];
  let t7;
  $[18] !== exitDiffView || $[19] !== t6 ? (t7 = (0, import_jsx_runtime3.jsx)(Dialog2, { id: "diffView", width: "auto", onClose: exitDiffView, padding: false, __unstable_hideCloseButton: true, children: t6 }), $[18] = exitDiffView, $[19] = t6, $[20] = t7) : t7 = $[20];
  let t8;
  return $[21] !== diffViewReferenceInputOptionsContext || $[22] !== t7 ? (t8 = (0, import_jsx_runtime3.jsx)(ReferenceInputOptionsContext.Provider, { value: diffViewReferenceInputOptionsContext, children: t7 }), $[21] = diffViewReferenceInputOptionsContext, $[22] = t7, $[23] = t8) : t8 = $[23], t8;
};
var DiffViewDocumentLayout = (t0) => {
  const $ = (0, import_react_compiler_runtime.c)(9), {
    children,
    documentId
  } = t0, toast = useToast(), {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  let t1;
  $[0] !== t2 || $[1] !== toast ? (t1 = {
    onParamsError: (errors) => {
      toast.push({
        id: "diffViewParamsParsingError",
        status: "error",
        title: t2("compare-version.error.invalidParams.title"),
        description: (0, import_jsx_runtime3.jsx)("ul", { children: errors.map((t222) => {
          const [error, input] = t222;
          return (0, import_jsx_runtime3.jsx)("li", { children: t2(`compare-version.error.${error}`, {
            input
          }) }, error);
        }) })
      });
    }
  }, $[0] = t2, $[1] = toast, $[2] = t1) : t1 = $[2];
  const {
    isActive
  } = useDiffViewState(t1);
  let t22;
  $[3] !== documentId || $[4] !== isActive ? (t22 = isActive && (0, import_jsx_runtime3.jsx)(DiffView, { documentId }), $[3] = documentId, $[4] = isActive, $[5] = t22) : t22 = $[5];
  let t3;
  return $[6] !== children || $[7] !== t22 ? (t3 = (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [
    children,
    t22
  ] }), $[6] = children, $[7] = t22, $[8] = t3) : t3 = $[8], t3;
};
function ErrorPane(props) {
  const $ = (0, import_react_compiler_runtime.c)(12), {
    children,
    currentMinWidth,
    flex,
    minWidth,
    paneKey,
    title: t0,
    tone: t1
  } = props, title = t0 === void 0 ? "Error" : t0, tone = t1 === void 0 ? "critical" : t1;
  let t2;
  $[0] !== title ? (t2 = (0, import_jsx_runtime3.jsx)(PaneHeader$1, { title }), $[0] = title, $[1] = t2) : t2 = $[1];
  let t3;
  $[2] !== children ? (t3 = (0, import_jsx_runtime3.jsx)(PaneContent, { overflow: "auto", children: (0, import_jsx_runtime3.jsx)(Box, { paddingX: 4, paddingY: 5, children }) }), $[2] = children, $[3] = t3) : t3 = $[3];
  let t4;
  return $[4] !== currentMinWidth || $[5] !== flex || $[6] !== minWidth || $[7] !== paneKey || $[8] !== t2 || $[9] !== t3 || $[10] !== tone ? (t4 = (0, import_jsx_runtime3.jsxs)(Pane, { currentMinWidth, flex, id: paneKey, minWidth, tone, children: [
    t2,
    t3
  ] }), $[4] = currentMinWidth, $[5] = flex, $[6] = minWidth, $[7] = paneKey, $[8] = t2, $[9] = t3, $[10] = tone, $[11] = t4) : t4 = $[11], t4;
}
function getWaitMessages(path) {
  const thresholds = [{
    ms: 300,
    messageKey: "panes.resolving.default-message"
  }, {
    ms: 5e3,
    messageKey: "panes.resolving.slow-resolve-message"
  }];
  if (isDev) {
    const message = ["Check console for errors?", "Is your observable/promise resolving?", path.length > 0 ? `Structure path: ${path.join(" ➝ ")}` : ""];
    thresholds.push({
      ms: 1e4,
      message: message.join(`
`)
    });
  }
  const src = of(null);
  return merge(...thresholds.map((threshold) => src.pipe(mapTo("messageKey" in threshold ? {
    messageKey: threshold.messageKey
  } : {
    message: threshold.message
  }), delay(threshold.ms))));
}
var DEFAULT_MESSAGE_KEY = "panes.resolving.default-message";
var Content$2 = dt(Flex)`
  opacity: 0;
  transition: opacity 200ms;

  &[data-mounted] {
    opacity: 1;
  }
`;
var LoadingPane = (0, import_react3.memo)((props) => {
  const $ = (0, import_react_compiler_runtime.c)(28), {
    delay: t0,
    flex,
    message: t1,
    minWidth,
    paneKey,
    path,
    selected,
    title,
    tone
  } = props, delay2 = t0 === void 0 ? 300 : t0, messageProp = t1 === void 0 ? getWaitMessages : t1, {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  let t22;
  bb0: {
    if (typeof messageProp == "function") {
      let t32;
      $[0] !== messageProp || $[1] !== path ? (t32 = messageProp(path ? path.split(";") : []), $[0] = messageProp, $[1] = path, $[2] = t32) : t32 = $[2], t22 = t32;
      break bb0;
    }
    t22 = messageProp;
  }
  const resolvedMessage = t22;
  let t3;
  $[3] !== resolvedMessage || $[4] !== t2 ? (t3 = () => typeof resolvedMessage == "string" ? resolvedMessage : t2(DEFAULT_MESSAGE_KEY), $[3] = resolvedMessage, $[4] = t2, $[5] = t3) : t3 = $[5];
  const [currentMessage, setCurrentMessage] = (0, import_react3.useState)(t3);
  let t4, t5;
  $[6] !== resolvedMessage || $[7] !== t2 ? (t4 = () => {
    if (typeof resolvedMessage != "object" || typeof resolvedMessage.subscribe != "function")
      return;
    const sub = resolvedMessage.subscribe((message) => {
      setCurrentMessage("messageKey" in message ? t2(message.messageKey) : message.message);
    });
    return () => sub.unsubscribe();
  }, t5 = [resolvedMessage, t2], $[6] = resolvedMessage, $[7] = t2, $[8] = t4, $[9] = t5) : (t4 = $[8], t5 = $[9]), (0, import_react3.useEffect)(t4, t5);
  const [contentElement, setContentElement] = (0, import_react3.useState)(null), [mounted, setMounted] = (0, import_react3.useState)(false);
  let t6, t7;
  $[10] !== contentElement ? (t6 = () => {
    if (contentElement)
      return _raf2(() => setMounted(true));
  }, t7 = [contentElement], $[10] = contentElement, $[11] = t6, $[12] = t7) : (t6 = $[11], t7 = $[12]), (0, import_react3.useEffect)(t6, t7);
  const t8 = mounted ? "" : void 0, t9 = title || currentMessage;
  let t10;
  $[13] !== t9 ? (t10 = (0, import_jsx_runtime3.jsx)(LoadingBlock, { showText: true, title: t9 }), $[13] = t9, $[14] = t10) : t10 = $[14];
  let t11;
  $[15] !== t10 || $[16] !== t8 ? (t11 = (0, import_jsx_runtime3.jsx)(Content$2, { align: "center", "data-mounted": t8, direction: "column", height: "fill", justify: "center", ref: setContentElement, children: t10 }), $[15] = t10, $[16] = t8, $[17] = t11) : t11 = $[17];
  const content = t11;
  let t12;
  $[18] !== content || $[19] !== delay2 ? (t12 = (0, import_jsx_runtime3.jsx)(PaneContent, { children: content }), $[18] = content, $[19] = delay2, $[20] = t12) : t12 = $[20];
  let t13;
  return $[21] !== flex || $[22] !== minWidth || $[23] !== paneKey || $[24] !== selected || $[25] !== t12 || $[26] !== tone ? (t13 = (0, import_jsx_runtime3.jsx)(Pane, { flex, id: paneKey, minWidth, selected, tone, children: t12 }), $[21] = flex, $[22] = minWidth, $[23] = paneKey, $[24] = selected, $[25] = t12, $[26] = tone, $[27] = t13) : t13 = $[27], t13;
});
LoadingPane.displayName = "Memo(LoadingPane)";
function useDocumentPane() {
  const documentPane = (0, import_react3.useContext)(DocumentPaneContext);
  if (!documentPane)
    throw new Error("DocumentPane: missing context value");
  return documentPane;
}
function CommentsWrapper(props) {
  const $ = (0, import_react_compiler_runtime.c)(8), {
    children,
    documentId,
    documentType
  } = props;
  let t0;
  $[0] !== children || $[1] !== documentId || $[2] !== documentType ? (t0 = (0, import_jsx_runtime3.jsx)(CommentsProviderWrapper, { documentId, documentType, children }), $[0] = children, $[1] = documentId, $[2] = documentType, $[3] = t0) : t0 = $[3];
  let t1;
  return $[4] !== documentId || $[5] !== documentType || $[6] !== t0 ? (t1 = (0, import_jsx_runtime3.jsx)(CommentsEnabledProvider, { documentId, documentType, children: t0 }), $[4] = documentId, $[5] = documentType, $[6] = t0, $[7] = t1) : t1 = $[7], t1;
}
function CommentsProviderWrapper(props) {
  const $ = (0, import_react_compiler_runtime.c)(25), {
    children,
    documentId,
    documentType
  } = props, {
    enabled
  } = useCommentsEnabled(), {
    connectionState,
    onPathOpen,
    inspector,
    openInspector
  } = useDocumentPane(), {
    selectedReleaseId
  } = usePerspective(), {
    params,
    setParams,
    createPathWithParams
  } = usePaneRouter(), {
    studioUrl
  } = useStudioUrl(window.location.origin), selectedCommentId = params == null ? void 0 : params.comment, paramsRef = (0, import_react3.useRef)(params);
  let t0, t1;
  $[0] !== params ? (t0 = () => {
    paramsRef.current = params;
  }, t1 = [params], $[0] = params, $[1] = t0, $[2] = t1) : (t0 = $[1], t1 = $[2]), (0, import_react3.useLayoutEffect)(t0, t1);
  let t2;
  $[3] !== createPathWithParams || $[4] !== studioUrl ? (t2 = (commentId) => {
    const path = createPathWithParams({
      ...paramsRef.current,
      comment: commentId,
      inspect: COMMENTS_INSPECTOR_NAME
    });
    return `${studioUrl}${path}`;
  }, $[3] = createPathWithParams, $[4] = studioUrl, $[5] = t2) : t2 = $[5];
  const getCommentLink = t2;
  let t3;
  $[6] !== setParams ? (t3 = () => {
    setParams({
      ...paramsRef.current,
      comment: void 0
    });
  }, $[6] = setParams, $[7] = t3) : t3 = $[7];
  const handleClearSelectedComment = t3;
  let t4;
  $[8] !== (inspector == null ? void 0 : inspector.name) || $[9] !== openInspector ? (t4 = () => {
    (inspector == null ? void 0 : inspector.name) !== COMMENTS_INSPECTOR_NAME && openInspector(COMMENTS_INSPECTOR_NAME);
  }, $[8] = inspector == null ? void 0 : inspector.name, $[9] = openInspector, $[10] = t4) : t4 = $[10];
  const handleOpenCommentsInspector = t4;
  if (!enabled) {
    let t52;
    return $[11] !== children ? (t52 = (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children }), $[11] = children, $[12] = t52) : t52 = $[12], t52;
  }
  const t5 = (inspector == null ? void 0 : inspector.name) === COMMENTS_INSPECTOR_NAME, t6 = connectionState === "connecting";
  let t7;
  return $[13] !== children || $[14] !== documentId || $[15] !== documentType || $[16] !== getCommentLink || $[17] !== handleClearSelectedComment || $[18] !== handleOpenCommentsInspector || $[19] !== onPathOpen || $[20] !== selectedCommentId || $[21] !== selectedReleaseId || $[22] !== t5 || $[23] !== t6 ? (t7 = (0, import_jsx_runtime3.jsx)(CommentsProvider, { documentId, documentType, getCommentLink, isCommentsOpen: t5, isConnecting: t6, onClearSelectedComment: handleClearSelectedComment, onCommentsOpen: handleOpenCommentsInspector, onPathOpen, selectedCommentId, sortOrder: "desc", type: "field", releaseId: selectedReleaseId, children }), $[13] = children, $[14] = documentId, $[15] = documentType, $[16] = getCommentLink, $[17] = handleClearSelectedComment, $[18] = handleOpenCommentsInspector, $[19] = onPathOpen, $[20] = selectedCommentId, $[21] = selectedReleaseId, $[22] = t5, $[23] = t6, $[24] = t7) : t7 = $[24], t7;
}
function useFilteredReleases({
  displayed,
  documentId
}) {
  const {
    selectedReleaseId
  } = usePerspective(), {
    data: releases
  } = useActiveReleases(), {
    data: archivedReleases
  } = useArchivedReleases(), {
    data: documentVersions
  } = useDocumentVersions({
    documentId
  }), isCreatingDocument = displayed && !displayed._createdAt, {
    params
  } = usePaneRouter();
  return (0, import_react3.useMemo)(() => {
    if (!documentVersions) return {
      notCurrentReleases: [],
      currentReleases: [],
      inCreation: null
    };
    const releasesIds = documentVersions.map((id2) => getVersionFromId(id2)), activeReleases = releases.reduce((acc, release) => {
      const versionDocExists = releasesIds.includes(getReleaseIdFromReleaseDocumentId(release._id)), releaseId = getReleaseIdFromReleaseDocumentId(release._id);
      return isCreatingDocument && releaseId === getVersionFromId(displayed._id || "") && releaseId === selectedReleaseId ? acc.inCreation = release : versionDocExists ? acc.currentReleases.push(release) : acc.notCurrentReleases.push(release), acc;
    }, {
      notCurrentReleases: [],
      currentReleases: [],
      inCreation: null
    });
    if (!(params == null ? void 0 : params.historyVersion)) return activeReleases;
    const archivedRelease = archivedReleases.find((r2) => getReleaseIdFromReleaseDocumentId(r2._id) === (params == null ? void 0 : params.historyVersion));
    return (archivedRelease == null ? void 0 : archivedRelease.state) === "archived" && activeReleases.currentReleases.push(archivedRelease), activeReleases;
  }, [archivedReleases, isCreatingDocument, displayed == null ? void 0 : displayed._id, documentVersions, params == null ? void 0 : params.historyVersion, releases, selectedReleaseId]);
}
function useDocumentIdStack({
  displayed,
  documentId,
  editState
}) {
  var _a, _b;
  const {
    document: {
      drafts: {
        enabled: isDraftModelEnabled
      }
    }
  } = useWorkspace(), filteredReleases = useFilteredReleases({
    displayed,
    documentId
  }), systemStack = [(_a = editState == null ? void 0 : editState.published) == null ? void 0 : _a._id, isDraftModelEnabled ? (_b = editState == null ? void 0 : editState.draft) == null ? void 0 : _b._id : []].flat(), releaseStack = filteredReleases.currentReleases.map((release) => (editState == null ? void 0 : editState.id) && getVersionId(editState.id, getReleaseIdFromReleaseDocumentId(release._id))), stack = systemStack.concat(releaseStack).filter((id2) => typeof id2 == "string"), position = (0, import_react3.useMemo)(() => stack.findIndex((id_0) => id_0 === (displayed == null ? void 0 : displayed._id)), [displayed == null ? void 0 : displayed._id, stack]), previousId = (0, import_react3.useMemo)(() => stack[position - 1] ?? void 0, [position, stack]), nextId = (0, import_react3.useMemo)(() => stack[position + 1] ?? void 0, [position, stack]);
  return {
    position,
    previousId,
    nextId,
    stack
  };
}
function useStructureTool() {
  const structureTool2 = (0, import_react3.useContext)(StructureToolContext);
  if (!structureTool2)
    throw new Error("StructureTool: missing context value");
  return structureTool2;
}
var DOCUMENT_PANEL_MIN_WIDTH = 320;
var DOCUMENT_PANEL_INITIAL_MIN_WIDTH = 600;
var DOCUMENT_INSPECTOR_MIN_WIDTH = 320;
var DOCUMENT_INSPECTOR_MAX_WIDTH = 540;
var EMPTY_PARAMS$2 = {};
var INSPECT_ACTION_PREFIX = "inspect:";
var DEFAULT_MENU_ITEM_GROUPS = [{
  id: "inspectors"
}, {
  id: "links"
}];
var HISTORY_INSPECTOR_NAME = "sanity/structure/history";
var VALIDATION_INSPECTOR_NAME = "sanity/structure/validation";
function DocumentInspectorMenuItemsResolver(props) {
  const {
    documentId,
    documentType,
    inspectors: inspectors2,
    onMenuItems
  } = props, len = inspectors2.length, [menuItems, setMenuItems] = (0, import_react3.useState)(() => Array.from(new Array(len)));
  (0, import_react3.useEffect)(() => {
    if (menuItems.length !== len) {
      const newFieldActions = Array.from(new Array(len));
      for (let i = 0; i < len; i++)
        newFieldActions[i] = menuItems[i];
      setMenuItems(newFieldActions);
    }
  }, [len, menuItems]);
  const setMenuItem = (0, import_react3.useCallback)((index, node) => {
    setMenuItems((prev) => {
      const next = [...prev];
      return next[index] = node, next;
    });
  }, []);
  (0, import_react3.useEffect)(() => {
    onMenuItems(menuItems.filter(Boolean));
  }, [menuItems, onMenuItems]);
  const InspectorMenuItems = (0, import_react3.useMemo)(() => inspectors2.map((inspector, index_0) => inspector.useMenuItem ? [defineInspectorMenuItemComponent({
    documentId,
    documentType,
    index: index_0,
    setMenuItem,
    useMenuItem: inspector.useMenuItem
  }), inspector.name] : [() => null, ""]), [documentId, documentType, inspectors2, setMenuItem]);
  return (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children: InspectorMenuItems.map(([InspectorMenuItem, key]) => key && (0, import_jsx_runtime3.jsx)(InspectorMenuItem, {}, key)) });
}
DocumentInspectorMenuItemsResolver.displayName = "DocumentInspectorMenuItemsResolver";
function defineInspectorMenuItemComponent({
  documentId,
  documentType,
  index,
  setMenuItem,
  useMenuItem: useMenuItem2
}) {
  return (0, import_react3.memo)(function() {
    const $ = (0, import_react_compiler_runtime.c)(4);
    let t0;
    $[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = {
      documentId,
      documentType
    }, $[0] = t0) : t0 = $[0];
    const menuItem = useMenuItem2(t0), node = useUnique(menuItem);
    let t1, t2;
    return $[1] !== node ? (t1 = () => {
      setMenuItem(index, node);
    }, t2 = [node], $[1] = node, $[2] = t1, $[3] = t2) : (t1 = $[2], t2 = $[3]), (0, import_react3.useEffect)(t1, t2), null;
  });
}
function useDocumentTitle() {
  const $ = (0, import_react_compiler_runtime.c)(19), {
    connectionState,
    schemaType,
    title,
    displayed
  } = useDocumentPane(), {
    t: t2
  } = useTranslation(structureLocaleNamespace), subscribed = !!displayed;
  let t0;
  $[0] !== displayed || $[1] !== schemaType || $[2] !== subscribed ? (t0 = {
    enabled: subscribed,
    schemaType,
    value: displayed
  }, $[0] = displayed, $[1] = schemaType, $[2] = subscribed, $[3] = t0) : t0 = $[3];
  const {
    error,
    value
  } = useDocumentPreview(t0);
  if (connectionState === "connecting" && !subscribed) {
    let t12;
    return $[4] === Symbol.for("react.memo_cache_sentinel") ? (t12 = {
      error: void 0,
      title: void 0
    }, $[4] = t12) : t12 = $[4], t12;
  }
  if (title) {
    let t12;
    return $[5] !== title ? (t12 = {
      error: void 0,
      title
    }, $[5] = title, $[6] = t12) : t12 = $[6], t12;
  }
  if (!displayed) {
    const t12 = (schemaType == null ? void 0 : schemaType.title) || (schemaType == null ? void 0 : schemaType.name);
    let t222;
    $[7] !== t2 || $[8] !== t12 ? (t222 = t2("panes.document-header-title.new.text", {
      schemaType: t12
    }), $[7] = t2, $[8] = t12, $[9] = t222) : t222 = $[9];
    let t3;
    return $[10] !== t222 ? (t3 = {
      error: void 0,
      title: t222
    }, $[10] = t222, $[11] = t3) : t3 = $[11], t3;
  }
  if (error) {
    let t12;
    $[12] !== error.message || $[13] !== t2 ? (t12 = t2("panes.document-list-pane.error.text", {
      error: error.message
    }), $[12] = error.message, $[13] = t2, $[14] = t12) : t12 = $[14];
    let t222;
    return $[15] !== t12 ? (t222 = {
      error: t12,
      title: void 0
    }, $[15] = t12, $[16] = t222) : t222 = $[16], t222;
  }
  const t1 = value == null ? void 0 : value.title;
  let t22;
  return $[17] !== t1 ? (t22 = {
    error: void 0,
    title: t1
  }, $[17] = t1, $[18] = t22) : t22 = $[18], t22;
}
var IGNORE_OPS = ["patch", "commit"];
var DocumentOperationResults = (0, import_react3.memo)(function() {
  const $ = (0, import_react_compiler_runtime.c)(12), {
    push: pushToast
  } = useToast(), {
    documentId,
    documentType,
    value: documentPaneValue
  } = useDocumentPane(), documentTitleInfo = useDocumentTitle(), titleError = documentTitleInfo.error, event = useDocumentOperationEvent(documentId, documentType), prevEvent = (0, import_react3.useRef)(event), paneRouter = usePaneRouter(), {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  let t0;
  bb0: {
    if (!documentTitleInfo.title && !titleError && !IGNORE_OPS.includes(event == null ? void 0 : event.op) && typeof documentPaneValue.title == "string" && (event == null ? void 0 : event.type) === "success") {
      t0 = documentPaneValue.title;
      break bb0;
    }
    t0 = documentTitleInfo.title;
  }
  const title = t0;
  let t1;
  $[0] !== t2 || $[1] !== title ? (t1 = title || t2("panes.document-operation-results.operation-undefined-title"), $[0] = t2, $[1] = title, $[2] = t1) : t1 = $[2];
  const documentTitleBase = t1;
  let t22;
  $[3] !== documentTitleBase ? (t22 = documentTitleBase.length > 25 ? `${documentTitleBase.slice(0, 25)}...` : documentTitleBase, $[3] = documentTitleBase, $[4] = t22) : t22 = $[4];
  const documentTitle = t22;
  let t3, t4;
  return $[5] !== documentTitle || $[6] !== event || $[7] !== paneRouter || $[8] !== pushToast || $[9] !== t2 ? (t3 = () => {
    if (!event || event === prevEvent.current)
      return;
    let cleanupId;
    return event.type === "error" && pushToast({
      closable: true,
      duration: 3e4,
      status: "error",
      title: t2("panes.document-operation-results.operation-error", {
        context: event.op
      }),
      description: (0, import_jsx_runtime3.jsxs)("details", { children: [
        (0, import_jsx_runtime3.jsx)("summary", { children: t2("panes.document-operation-results.error.summary.title") }),
        event.error.message
      ] })
    }), event.type === "success" && !IGNORE_OPS.includes(event.op) && pushToast({
      closable: true,
      status: "success",
      title: (0, import_jsx_runtime3.jsx)(Translate, { context: event.op, i18nKey: "panes.document-operation-results.operation-success", t: t2, values: {
        op: event.op,
        title: documentTitle
      }, components: {
        Strong: "strong"
      } })
    }), event.type === "success" && event.op === "delete" && (cleanupId = setTimeout(() => paneRouter.closeCurrentAndAfter(), 0)), prevEvent.current = event, () => clearTimeout(cleanupId);
  }, t4 = [event, paneRouter, pushToast, t2, documentTitle], $[5] = documentTitle, $[6] = event, $[7] = paneRouter, $[8] = pushToast, $[9] = t2, $[10] = t3, $[11] = t4) : (t3 = $[10], t4 = $[11]), (0, import_react3.useEffect)(t3, t4), null;
});
var isLiveEditEnabled = (schemaType) => schemaType.liveEdit === true;
function hasObsoleteDraft({
  editState,
  workspace,
  schemaType
}) {
  if (!(editState == null ? void 0 : editState.ready))
    return {
      result: void 0
    };
  const draftExists = editState.draft !== null, {
    document: {
      drafts: {
        enabled: isDraftModelEnabled
      }
    }
  } = workspace;
  return draftExists === false ? {
    result: false
  } : isDraftModelEnabled ? isLiveEditEnabled(schemaType) ? {
    result: true,
    reason: "LIVE_EDIT_ACTIVE"
  } : {
    result: false
  } : {
    result: true,
    reason: "DRAFT_MODEL_INACTIVE"
  };
}
function mustChooseNewDocumentDestination({
  isSelectedPerspectiveWriteable,
  editState
}) {
  return isNewDocument(editState) && !isSelectedPerspectiveWriteable.result;
}
var Root$3 = dt(Card)({
  position: "relative",
  zIndex: 1,
  lineHeight: 0
});
function DocumentInspectorHeader(props) {
  const $ = (0, import_react_compiler_runtime.c)(25);
  let children, closeButtonLabel, forwardedAs, onClose, restProps, title;
  $[0] !== props ? ({
    as: forwardedAs,
    children,
    closeButtonLabel,
    onClose,
    title,
    ...restProps
  } = props, $[0] = props, $[1] = children, $[2] = closeButtonLabel, $[3] = forwardedAs, $[4] = onClose, $[5] = restProps, $[6] = title) : (children = $[1], closeButtonLabel = $[2], forwardedAs = $[3], onClose = $[4], restProps = $[5], title = $[6]);
  const {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  let t0;
  $[7] !== title ? (t0 = (0, import_jsx_runtime3.jsx)(Box, { flex: 1, padding: 3, children: (0, import_jsx_runtime3.jsx)(Text, { as: "h1", size: 1, weight: "medium", children: title }) }), $[7] = title, $[8] = t0) : t0 = $[8];
  let t1;
  $[9] !== t2 ? (t1 = t2("document-inspector.close-button.tooltip"), $[9] = t2, $[10] = t1) : t1 = $[10];
  let t22;
  $[11] !== t1 ? (t22 = {
    content: t1
  }, $[11] = t1, $[12] = t22) : t22 = $[12];
  let t3;
  $[13] !== closeButtonLabel || $[14] !== onClose || $[15] !== t22 ? (t3 = (0, import_jsx_runtime3.jsx)(Box, { flex: "none", padding: 1, children: (0, import_jsx_runtime3.jsx)(Button2, { "aria-label": closeButtonLabel, icon: CloseIcon, mode: "bleed", onClick: onClose, tooltipProps: t22 }) }), $[13] = closeButtonLabel, $[14] = onClose, $[15] = t22, $[16] = t3) : t3 = $[16];
  let t4;
  $[17] !== t0 || $[18] !== t3 ? (t4 = (0, import_jsx_runtime3.jsxs)(Flex, { padding: 2, children: [
    t0,
    t3
  ] }), $[17] = t0, $[18] = t3, $[19] = t4) : t4 = $[19];
  let t5;
  return $[20] !== children || $[21] !== forwardedAs || $[22] !== restProps || $[23] !== t4 ? (t5 = (0, import_jsx_runtime3.jsxs)(Root$3, { ...restProps, as: forwardedAs, children: [
    t4,
    children
  ] }), $[20] = children, $[21] = forwardedAs, $[22] = restProps, $[23] = t4, $[24] = t5) : t5 = $[24], t5;
}
function DocumentInspectorPanel(props) {
  const $ = (0, import_react_compiler_runtime.c)(14), {
    documentId,
    documentType,
    flex
  } = props, {
    collapsed
  } = usePane(), {
    closeInspector,
    inspector
  } = useDocumentPane(), {
    features
  } = useStructureTool();
  let t0;
  $[0] !== closeInspector || $[1] !== inspector ? (t0 = () => {
    inspector && closeInspector(inspector.name);
  }, $[0] = closeInspector, $[1] = inspector, $[2] = t0) : t0 = $[2];
  const handleClose = t0;
  if (collapsed || !inspector)
    return null;
  const Component2 = inspector.component;
  let t1;
  $[3] !== Component2 || $[4] !== documentId || $[5] !== documentType || $[6] !== handleClose ? (t1 = (0, import_jsx_runtime3.jsx)(Component2, { onClose: handleClose, documentId, documentType }), $[3] = Component2, $[4] = documentId, $[5] = documentType, $[6] = handleClose, $[7] = t1) : t1 = $[7];
  const element = t1;
  if (features.resizablePanes) {
    let t22;
    return $[8] !== element || $[9] !== flex ? (t22 = (0, import_jsx_runtime3.jsx)(Resizable, { as: "aside", "data-ui": "DocumentInspectorPanel", flex, resizerPosition: "left", maxWidth: DOCUMENT_INSPECTOR_MAX_WIDTH, minWidth: DOCUMENT_INSPECTOR_MIN_WIDTH, children: element }), $[8] = element, $[9] = flex, $[10] = t22) : t22 = $[10], t22;
  }
  let t2;
  return $[11] !== element || $[12] !== flex ? (t2 = (0, import_jsx_runtime3.jsx)(Box, { as: "aside", "data-ui": "DocumentInspectorPanel", flex, children: element }), $[11] = element, $[12] = flex, $[13] = t2) : t2 = $[13], t2;
}
var STRUCTURE_TOOL_NAMESPACE = "studio.structure-tool";
function useStructureToolSetting(namespace, key, defaultValue) {
  const $ = (0, import_react_compiler_runtime.c)(16), keyValueStore = useKeyValueStore();
  let t0;
  $[0] !== key || $[1] !== namespace ? (t0 = [STRUCTURE_TOOL_NAMESPACE, namespace, key].filter(Boolean), $[0] = key, $[1] = namespace, $[2] = t0) : t0 = $[2];
  const keyValueStoreKey = t0.join(".");
  let t1, t2;
  if ($[3] !== defaultValue || $[4] !== keyValueStore || $[5] !== keyValueStoreKey) {
    let t32;
    $[7] !== defaultValue ? (t32 = (value) => value === null ? defaultValue : value, $[7] = defaultValue, $[8] = t32) : t32 = $[8], t2 = keyValueStore.getKey(keyValueStoreKey).pipe(map(t32)), $[3] = defaultValue, $[4] = keyValueStore, $[5] = keyValueStoreKey, $[6] = t2;
  } else
    t2 = $[6];
  t1 = t2;
  const value_0 = useObservable(t1, defaultValue);
  let t3;
  $[9] !== keyValueStore || $[10] !== keyValueStoreKey || $[11] !== value_0 ? (t3 = (newValue) => {
    newValue !== value_0 && keyValueStore.setKey(keyValueStoreKey, newValue);
  }, $[9] = keyValueStore, $[10] = keyValueStoreKey, $[11] = value_0, $[12] = t3) : t3 = $[12];
  const set = t3;
  let t4, t5;
  return $[13] !== set || $[14] !== value_0 ? (t5 = [value_0, set], $[13] = set, $[14] = value_0, $[15] = t5) : t5 = $[15], t4 = t5, t4;
}
var VIEW_MODE_PARSED = {
  id: "parsed",
  title: "document-inspector.view-mode.parsed"
};
var VIEW_MODE_RAW = {
  id: "raw",
  title: "document-inspector.view-mode.raw-json"
};
var VIEW_MODES = [VIEW_MODE_PARSED, VIEW_MODE_RAW];
var lru = new import_quick_lru.default({
  maxSize: 1e3
});
function isExpanded(keyPath, value) {
  const cached = lru.get(keyPath);
  return cached === void 0 ? (lru.set(keyPath, Array.isArray(value) || isRecord$4(value)), isExpanded(keyPath, value)) : cached;
}
function toggleExpanded(event) {
  const {
    path
  } = event, current = lru.get(path);
  current !== void 0 && lru.set(path, !current);
}
function selectElement(element) {
  const sel = window.getSelection();
  if (sel) {
    const range = document.createRange();
    sel.removeAllRanges(), range.selectNodeContents(element), sel.addRange(range);
  }
}
function select(event) {
  selectElement(event.currentTarget);
}
function maybeSelectAll(event) {
  event.keyCode === 65 && (event.metaKey || event.ctrlKey) && (event.preventDefault(), selectElement(event.currentTarget));
}
function isDocumentLike(value) {
  return isRecord$4(value) && isString(value._id) && isString(value._type);
}
var JSONInspectorWrapper = dt.div(({
  theme
}) => {
  const {
    color,
    fonts,
    space
  } = theme.sanity;
  return lt`
    & .json-inspector,
    & .json-inspector .json-inspector__selection {
      font-family: ${fonts.code.family};
      font-size: ${fonts.code.sizes[1].fontSize}px;
      line-height: ${fonts.code.sizes[1].lineHeight}px;
      color: var(--card-code-fg-color);
    }

    & .json-inspector .json-inspector__leaf {
      padding-left: ${rem(space[4])};
    }

    & .json-inspector .json-inspector__leaf.json-inspector__leaf_root {
      padding-top: ${rem(space[3])};
      padding-left: 0;
    }

    & .json-inspector > .json-inspector__leaf_root > .json-inspector__line > .json-inspector__key {
      display: none;
    }

    & .json-inspector .json-inspector__line {
      display: block;
      position: relative;
      cursor: default;
    }

    & .json-inspector .json-inspector__line::after {
      content: '';
      position: absolute;
      top: 0;
      left: -200px;
      right: -50px;
      bottom: 0;
      z-index: -1;
      pointer-events: none;
    }

    & .json-inspector .json-inspector__line:hover::after {
      background: var(--card-code-bg-color);
    }

    & .json-inspector .json-inspector__leaf_composite > .json-inspector__line {
      cursor: pointer;
    }

    & .json-inspector .json-inspector__leaf_composite > .json-inspector__line::before {
      content: '▸ ';
      margin-left: calc(0 - ${rem(space[4])} + 3px);
      font-size: ${fonts.code.sizes[1].fontSize}px;
      line-height: ${fonts.code.sizes[1].lineHeight}px;
    }

    &
      .json-inspector
      .json-inspector__leaf_expanded.json-inspector__leaf_composite
      > .json-inspector__line::before {
      content: '▾ ';
      font-size: ${fonts.code.sizes[1].fontSize}px;
      line-height: ${fonts.code.sizes[1].lineHeight}px;
    }

    & .json-inspector .json-inspector__radio,
    & .json-inspector .json-inspector__flatpath {
      display: none;
    }

    & .json-inspector .json-inspector__value {
      margin-left: ${rem(space[4] / 2)};
    }

    &
      .json-inspector
      > .json-inspector__leaf_root
      > .json-inspector__line
      > .json-inspector__key
      + .json-inspector__value {
      margin: 0;
    }

    & .json-inspector .json-inspector__key {
      color: ${color.syntax.property};
    }

    & .json-inspector .json-inspector__value_helper,
    & .json-inspector .json-inspector__value_null {
      color: ${color.syntax.constant};
    }

    & .json-inspector .json-inspector__not-found {
      padding-top: ${rem(space[3])};
    }

    & .json-inspector .json-inspector__value_string {
      color: ${color.syntax.string};
    }

    & .json-inspector .json-inspector__value_boolean {
      color: ${color.syntax.boolean};
    }

    & .json-inspector .json-inspector__value_number {
      color: ${color.syntax.number};
    }

    & .json-inspector .json-inspector__show-original {
      display: inline-block;
      padding: 0 6px;
      cursor: pointer;
    }

    & .json-inspector .json-inspector__show-original:hover {
      color: inherit;
    }

    & .json-inspector .json-inspector__show-original::before {
      content: '↔';
    }

    & .json-inspector .json-inspector__show-original:hover::after {
      content: ' expand';
    }
  `;
});
function Search(props) {
  const $ = (0, import_react_compiler_runtime.c)(8), {
    onChange,
    query
  } = props;
  let t0;
  $[0] !== onChange ? (t0 = (event) => onChange(event.target.value), $[0] = onChange, $[1] = t0) : t0 = $[1];
  const handleChange = t0, {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  let t1;
  $[2] !== t2 ? (t1 = t2("document-inspector.search.placeholder"), $[2] = t2, $[3] = t1) : t1 = $[3];
  const t22 = query || "";
  let t3;
  return $[4] !== handleChange || $[5] !== t1 || $[6] !== t22 ? (t3 = (0, import_jsx_runtime3.jsx)(TextInput, { icon: SearchIcon, onChange: handleChange, placeholder: t1, radius: 2, value: t22 }), $[4] = handleChange, $[5] = t1, $[6] = t22, $[7] = t3) : t3 = $[7], t3;
}
function InspectDialog(props) {
  const $ = (0, import_react_compiler_runtime.c)(49), {
    value
  } = props, {
    onInspectClose,
    paneKey
  } = useDocumentPane(), dialogIdPrefix = `${paneKey}_inspect_`, [viewModeId, onViewModeChange] = useStructureToolSetting("inspect-view-mode", null, "parsed");
  let t0;
  $[0] !== viewModeId ? (t0 = VIEW_MODES.find((mode) => mode.id === viewModeId), $[0] = viewModeId, $[1] = t0) : t0 = $[1];
  const viewMode = t0;
  let t1;
  $[2] !== onViewModeChange ? (t1 = () => {
    onViewModeChange(VIEW_MODE_PARSED.id);
  }, $[2] = onViewModeChange, $[3] = t1) : t1 = $[3];
  const setParsedViewMode = t1;
  let t2;
  $[4] !== onViewModeChange ? (t2 = () => {
    onViewModeChange(VIEW_MODE_RAW.id);
  }, $[4] = onViewModeChange, $[5] = t2) : t2 = $[5];
  const setRawViewMode = t2, {
    t: t3
  } = useTranslation(structureLocaleNamespace), t32 = `${dialogIdPrefix}dialog`;
  let t4;
  $[6] !== t3 || $[7] !== value ? (t4 = isDocumentLike(value) ? (0, import_jsx_runtime3.jsx)(Translate, { t: t3, i18nKey: "document-inspector.dialog.title", components: {
    DocumentTitle: () => (0, import_jsx_runtime3.jsx)("em", { children: (0, import_jsx_runtime3.jsx)(DocTitle, { document: value }) })
  } }) : (0, import_jsx_runtime3.jsx)("em", { children: t3("document-inspector.dialog.title-no-value") }), $[6] = t3, $[7] = value, $[8] = t4) : t4 = $[8];
  let t5;
  $[9] === Symbol.for("react.memo_cache_sentinel") ? (t5 = {
    position: "sticky",
    bottom: 0,
    zIndex: 3
  }, $[9] = t5) : t5 = $[9];
  const t6 = `${dialogIdPrefix}tabpanel`, t7 = `${dialogIdPrefix}tab-${VIEW_MODE_PARSED.id}`;
  let t8;
  $[10] !== t3 ? (t8 = t3(VIEW_MODE_PARSED.title), $[10] = t3, $[11] = t8) : t8 = $[11];
  const t9 = viewMode === VIEW_MODE_PARSED;
  let t10;
  $[12] !== setParsedViewMode || $[13] !== t6 || $[14] !== t7 || $[15] !== t8 || $[16] !== t9 ? (t10 = (0, import_jsx_runtime3.jsx)(Tab, { "aria-controls": t6, id: t7, label: t8, onClick: setParsedViewMode, selected: t9 }), $[12] = setParsedViewMode, $[13] = t6, $[14] = t7, $[15] = t8, $[16] = t9, $[17] = t10) : t10 = $[17];
  const t11 = `${dialogIdPrefix}tabpanel`, t12 = `${dialogIdPrefix}tab-${VIEW_MODE_RAW.id}`;
  let t13;
  $[18] !== t3 ? (t13 = t3(VIEW_MODE_RAW.title), $[18] = t3, $[19] = t13) : t13 = $[19];
  const t14 = viewMode === VIEW_MODE_RAW;
  let t15;
  $[20] !== setRawViewMode || $[21] !== t11 || $[22] !== t12 || $[23] !== t13 || $[24] !== t14 ? (t15 = (0, import_jsx_runtime3.jsx)(Tab, { "aria-controls": t11, id: t12, label: t13, onClick: setRawViewMode, selected: t14 }), $[20] = setRawViewMode, $[21] = t11, $[22] = t12, $[23] = t13, $[24] = t14, $[25] = t15) : t15 = $[25];
  let t16;
  $[26] !== t10 || $[27] !== t15 ? (t16 = (0, import_jsx_runtime3.jsx)(Card, { padding: 3, paddingTop: 0, shadow: 1, style: t5, children: (0, import_jsx_runtime3.jsxs)(TabList, { space: 1, children: [
    t10,
    t15
  ] }) }), $[26] = t10, $[27] = t15, $[28] = t16) : t16 = $[28];
  const t17 = `${dialogIdPrefix}tab-${viewModeId}`, t18 = `${dialogIdPrefix}tabpanel`;
  let t19;
  $[29] === Symbol.for("react.memo_cache_sentinel") ? (t19 = {
    outline: "none"
  }, $[29] = t19) : t19 = $[29];
  let t20;
  $[30] !== value || $[31] !== viewMode ? (t20 = viewMode === VIEW_MODE_PARSED && (0, import_jsx_runtime3.jsx)(JSONInspectorWrapper, { children: (0, import_jsx_runtime3.jsx)(JsonInspector, { data: value, isExpanded, onClick: toggleExpanded, search: Search, filterOptions: {
    ignoreCase: true
  } }) }), $[30] = value, $[31] = viewMode, $[32] = t20) : t20 = $[32];
  let t21;
  $[33] !== value || $[34] !== viewMode ? (t21 = viewMode === VIEW_MODE_RAW && (0, import_jsx_runtime3.jsx)(Code, { language: "json", tabIndex: 0, onKeyDown: maybeSelectAll, onDoubleClick: select, onFocus: select, size: 1, children: JSON.stringify(value, null, 2) }), $[33] = value, $[34] = viewMode, $[35] = t21) : t21 = $[35];
  let t22;
  $[36] !== t17 || $[37] !== t18 || $[38] !== t20 || $[39] !== t21 ? (t22 = (0, import_jsx_runtime3.jsxs)(TabPanel, { "aria-labelledby": t17, flex: 1, id: t18, overflow: "auto", padding: 4, style: t19, children: [
    t20,
    t21
  ] }), $[36] = t17, $[37] = t18, $[38] = t20, $[39] = t21, $[40] = t22) : t22 = $[40];
  let t23;
  $[41] !== t16 || $[42] !== t22 ? (t23 = (0, import_jsx_runtime3.jsxs)(Flex, { direction: "column", height: "fill", children: [
    t16,
    t22
  ] }), $[41] = t16, $[42] = t22, $[43] = t23) : t23 = $[43];
  let t24;
  return $[44] !== onInspectClose || $[45] !== t23 || $[46] !== t32 || $[47] !== t4 ? (t24 = (0, import_jsx_runtime3.jsx)(Dialog2, { bodyHeight: "fill", id: t32, header: t4, onClose: onInspectClose, onClickOutside: onInspectClose, padding: false, width: 2, children: t23 }), $[44] = onInspectClose, $[45] = t23, $[46] = t32, $[47] = t4, $[48] = t24) : t24 = $[48], t24;
}
function Banner(props) {
  const $ = (0, import_react_compiler_runtime.c)(22);
  let Icon, action, content, rest, t0, t1;
  $[0] !== props ? ({
    action,
    content,
    icon: Icon,
    tone: t0,
    paddingY: t1,
    ...rest
  } = props, $[0] = props, $[1] = Icon, $[2] = action, $[3] = content, $[4] = rest, $[5] = t0, $[6] = t1) : (Icon = $[1], action = $[2], content = $[3], rest = $[4], t0 = $[5], t1 = $[6]);
  const tone = t0 === void 0 ? "transparent" : t0, paddingY = t1 === void 0 ? 2 : t1;
  let t2;
  $[7] !== Icon ? (t2 = Icon && (0, import_jsx_runtime3.jsx)(Text, { size: 0, children: (0, import_jsx_runtime3.jsx)(Icon, {}) }), $[7] = Icon, $[8] = t2) : t2 = $[8];
  let t3;
  $[9] !== content ? (t3 = (0, import_jsx_runtime3.jsx)(Flex, { align: "center", flex: 1, gap: 2, paddingY: 2, children: content }), $[9] = content, $[10] = t3) : t3 = $[10];
  let t4;
  $[11] !== action ? (t4 = action && (0, import_jsx_runtime3.jsx)(Button2, { ...action, mode: action.mode || "ghost", tone: action.tone || "default" }), $[11] = action, $[12] = t4) : t4 = $[12];
  let t5;
  $[13] !== t2 || $[14] !== t3 || $[15] !== t4 ? (t5 = (0, import_jsx_runtime3.jsxs)(Flex, { align: "center", gap: 3, paddingX: 2, children: [
    t2,
    t3,
    t4
  ] }), $[13] = t2, $[14] = t3, $[15] = t4, $[16] = t5) : t5 = $[16];
  let t6;
  return $[17] !== paddingY || $[18] !== rest || $[19] !== t5 || $[20] !== tone ? (t6 = (0, import_jsx_runtime3.jsx)(Box, { padding: 1, children: (0, import_jsx_runtime3.jsx)(Card, { radius: 3, paddingX: 2, paddingY, tone, ...rest, children: t5 }) }), $[17] = paddingY, $[18] = rest, $[19] = t5, $[20] = tone, $[21] = t6) : t6 = $[21], t6;
}
function DeletedDocumentBanners() {
  const $ = (0, import_react_compiler_runtime.c)(3), {
    isDeleted,
    isDeleting,
    ready
  } = useDocumentPane(), {
    selectedPerspective
  } = usePerspective();
  if (!ready)
    return null;
  if (!isPublishedPerspective(selectedPerspective) && !isDraftPerspective(selectedPerspective) && selectedPerspective.state === "archived") {
    const t0 = selectedPerspective;
    let t1;
    return $[0] !== t0 ? (t1 = (0, import_jsx_runtime3.jsx)(ArchivedReleaseBanner, { release: t0 }), $[0] = t0, $[1] = t1) : t1 = $[1], t1;
  }
  if (isDeleted && !isDeleting) {
    let t0;
    return $[2] === Symbol.for("react.memo_cache_sentinel") ? (t0 = (0, import_jsx_runtime3.jsx)(DeletedDocumentBanner, {}), $[2] = t0) : t0 = $[2], t0;
  }
}
function DeletedDocumentBanner() {
  const $ = (0, import_react_compiler_runtime.c)(17), {
    documentId,
    documentType
  } = useDocumentPane(), {
    restore
  } = useDocumentOperation(documentId, documentType), {
    navigateIntent
  } = useRouter();
  let t0;
  $[0] !== documentId || $[1] !== documentType || $[2] !== navigateIntent || $[3] !== restore ? (t0 = () => {
    restore.execute("lastRevision"), navigateIntent("edit", {
      id: documentId,
      type: documentType
    });
  }, $[0] = documentId, $[1] = documentType, $[2] = navigateIntent, $[3] = restore, $[4] = t0) : t0 = $[4];
  const handleRestore = t0, {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  let t1;
  $[5] !== t2 ? (t1 = t2("banners.deleted-document-banner.restore-button.text"), $[5] = t2, $[6] = t1) : t1 = $[6];
  let t22;
  $[7] !== handleRestore || $[8] !== t1 ? (t22 = {
    onClick: handleRestore,
    text: t1
  }, $[7] = handleRestore, $[8] = t1, $[9] = t22) : t22 = $[9];
  let t3;
  $[10] !== t2 ? (t3 = t2("banners.deleted-document-banner.text"), $[10] = t2, $[11] = t3) : t3 = $[11];
  let t4;
  $[12] !== t3 ? (t4 = (0, import_jsx_runtime3.jsx)(Text, { size: 1, weight: "medium", children: t3 }), $[12] = t3, $[13] = t4) : t4 = $[13];
  let t5;
  return $[14] !== t22 || $[15] !== t4 ? (t5 = (0, import_jsx_runtime3.jsx)(Banner, { action: t22, content: t4, "data-testid": "deleted-document-banner", icon: ReadOnlyIcon }), $[14] = t22, $[15] = t4, $[16] = t5) : t5 = $[16], t5;
}
var ArchivedReleaseBanner = (t0) => {
  var _a, _b, _c;
  const $ = (0, import_react_compiler_runtime.c)(8), {
    release
  } = t0, {
    t: t2
  } = useTranslation();
  let t1;
  $[0] !== ((_a = release.metadata) == null ? void 0 : _a.title) || $[1] !== t2 ? (t1 = ((_b = release.metadata) == null ? void 0 : _b.title) || t2("release.placeholder-untitled-release"), $[0] = (_c = release.metadata) == null ? void 0 : _c.title, $[1] = t2, $[2] = t1) : t1 = $[2];
  let t22;
  $[3] !== t1 ? (t22 = {
    title: t1
  }, $[3] = t1, $[4] = t22) : t22 = $[4];
  let t3;
  return $[5] !== t2 || $[6] !== t22 ? (t3 = (0, import_jsx_runtime3.jsx)(Banner, { tone: "caution", content: (0, import_jsx_runtime3.jsx)(Text, { size: 1, weight: "medium", children: (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey: "banners.deleted-release-banner.text", values: t22 }) }), "data-testid": "deleted-release-banner", icon: DocumentRemoveIcon }), $[5] = t2, $[6] = t22, $[7] = t3) : t3 = $[7], t3;
};
function DeprecatedDocumentTypeBanner() {
  const $ = (0, import_react_compiler_runtime.c)(5), {
    schemaType
  } = useDocumentPane(), {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  if (!isDeprecatedSchemaType(schemaType))
    return null;
  let t0;
  $[0] !== t2 ? (t0 = (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey: "banners.deprecated-document-type-banner.text" }), $[0] = t2, $[1] = t0) : t0 = $[1];
  let t1;
  return $[2] !== schemaType.deprecated.reason || $[3] !== t0 ? (t1 = (0, import_jsx_runtime3.jsx)(Banner, { content: (0, import_jsx_runtime3.jsxs)(Text, { size: 1, weight: "medium", children: [
    t0,
    " ",
    schemaType.deprecated.reason
  ] }), "data-testid": "deprecated-document-type-banner", icon: ErrorOutlineIcon }), $[2] = schemaType.deprecated.reason, $[3] = t0, $[4] = t1) : t1 = $[4], t1;
}
var AskToEditDialogOpened = defineEvent({
  name: "Ask To Edit Dialog Opened",
  version: 1,
  description: 'User clicked the "Ask to edit" button in the document permissions banner'
});
var AskToEditRequestSent = defineEvent({
  name: "Ask To Edit Request Sent",
  version: 1,
  description: "User sent a role change request from the dialog"
});
var MAX_NOTE_LENGTH = 150;
var DialogBody = dt(Box)`
  box-sizing: border-box;
`;
dt(Flex).attrs({
  align: "center",
  direction: "column",
  justify: "center"
})`
  height: 110px;
`;
function RequestPermissionDialog(t0) {
  const $ = (0, import_react_compiler_runtime.c)(56), {
    onClose,
    onRequestSubmitted
  } = t0, {
    t: t2
  } = useTranslation(structureLocaleNamespace), telemtry = useTelemetry(), dialogId = `request-permissions-${(0, import_react3.useId)()}`, projectId = useProjectId$1();
  let t1;
  $[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = {
    apiVersion: "2024-09-26"
  }, $[0] = t1) : t1 = $[0];
  const client = useClient(t1), toast = useToast(), zOffset = useZIndex(), [isSubmitting, setIsSubmitting] = (0, import_react3.useState)(false), [note, setNote] = (0, import_react3.useState)(""), [msgError, setMsgError] = (0, import_react3.useState)(), [hasTooManyRequests, setHasTooManyRequests] = (0, import_react3.useState)(false), [hasBeenDenied, setHasBeenDenied] = (0, import_react3.useState)(false);
  let t22;
  bb0: {
    const adminRole = "administrator";
    if (!projectId || !client) {
      let t33;
      $[1] === Symbol.for("react.memo_cache_sentinel") ? (t33 = of(adminRole), $[1] = t33) : t33 = $[1], t22 = t33;
      break bb0;
    }
    let t32;
    if ($[2] !== client.observable || $[3] !== projectId) {
      let t42;
      $[5] === Symbol.for("react.memo_cache_sentinel") ? (t42 = (roles) => roles.filter(_temp$h).find(_temp2$6) ? "editor" : adminRole, $[5] = t42) : t42 = $[5];
      let t52;
      $[6] === Symbol.for("react.memo_cache_sentinel") ? (t52 = () => of(adminRole), $[6] = t52) : t52 = $[6], t32 = client.observable.request({
        url: `/projects/${projectId}/roles`
      }).pipe(map(t42), startWith(adminRole), catchError(t52)), $[2] = client.observable, $[3] = projectId, $[4] = t32;
    } else
      t32 = $[4];
    t22 = t32;
  }
  const requestedRole = useObservable(t22);
  let t3;
  $[7] !== client || $[8] !== note || $[9] !== onRequestSubmitted || $[10] !== projectId || $[11] !== requestedRole || $[12] !== telemtry || $[13] !== toast ? (t3 = () => {
    setIsSubmitting(true), client.request({
      url: `/access/project/${projectId}/requests`,
      method: "post",
      body: {
        note,
        requestUrl: window == null ? void 0 : window.location.href,
        requestedRole,
        type: "role"
      }
    }).then((request) => {
      request && (onRequestSubmitted && onRequestSubmitted(), telemtry.log(AskToEditRequestSent), toast.push({
        title: "Edit access requested"
      }));
    }).catch((err) => {
      var _a, _b, _c;
      const statusCode = (_a = err == null ? void 0 : err.response) == null ? void 0 : _a.statusCode, errMessage = (_c = (_b = err == null ? void 0 : err.response) == null ? void 0 : _b.body) == null ? void 0 : _c.message;
      statusCode === 429 && (setHasTooManyRequests(true), setMsgError(errMessage)), statusCode === 409 ? (setHasBeenDenied(true), setMsgError(errMessage)) : toast.push({
        title: "There was a problem submitting your request.",
        status: "error"
      });
    }).finally(() => {
      setIsSubmitting(false);
    });
  }, $[7] = client, $[8] = note, $[9] = onRequestSubmitted, $[10] = projectId, $[11] = requestedRole, $[12] = telemtry, $[13] = toast, $[14] = t3) : t3 = $[14];
  const onSubmit = t3, t4 = zOffset.fullscreen;
  let t5;
  $[15] !== t2 ? (t5 = t2("request-permission-dialog.header.text"), $[15] = t2, $[16] = t5) : t5 = $[16];
  let t6;
  $[17] !== t2 ? (t6 = t2("confirm-dialog.cancel-button.fallback-text"), $[17] = t2, $[18] = t6) : t6 = $[18];
  let t7;
  $[19] !== onClose || $[20] !== t6 ? (t7 = {
    onClick: onClose,
    text: t6
  }, $[19] = onClose, $[20] = t6, $[21] = t7) : t7 = $[21];
  const t8 = hasTooManyRequests || hasBeenDenied;
  let t9;
  $[22] !== t2 ? (t9 = t2("request-permission-dialog.confirm-button.text"), $[22] = t2, $[23] = t9) : t9 = $[23];
  let t10;
  $[24] !== isSubmitting || $[25] !== onSubmit || $[26] !== t8 || $[27] !== t9 ? (t10 = {
    onClick: onSubmit,
    loading: isSubmitting,
    disabled: t8,
    text: t9,
    tone: "primary",
    type: "submit"
  }, $[24] = isSubmitting, $[25] = onSubmit, $[26] = t8, $[27] = t9, $[28] = t10) : t10 = $[28];
  let t11;
  $[29] !== t10 || $[30] !== t7 ? (t11 = {
    cancelButton: t7,
    confirmButton: t10
  }, $[29] = t10, $[30] = t7, $[31] = t11) : t11 = $[31];
  let t12;
  $[32] !== t2 ? (t12 = t2("request-permission-dialog.description.text"), $[32] = t2, $[33] = t12) : t12 = $[33];
  let t13;
  $[34] !== t12 ? (t13 = (0, import_jsx_runtime3.jsx)(Text, { children: t12 }), $[34] = t12, $[35] = t13) : t13 = $[35];
  let t14;
  $[36] !== hasBeenDenied || $[37] !== hasTooManyRequests || $[38] !== isSubmitting || $[39] !== msgError || $[40] !== note || $[41] !== onSubmit || $[42] !== t2 ? (t14 = hasTooManyRequests || hasBeenDenied ? (0, import_jsx_runtime3.jsx)(Card, { tone: "caution", padding: 3, radius: 2, shadow: 1, children: (0, import_jsx_runtime3.jsxs)(Text, { size: 1, children: [
    hasTooManyRequests && (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children: msgError ?? t2("request-permission-dialog.warning.limit-reached.text") }),
    hasBeenDenied && (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children: msgError ?? t2("request-permission-dialog.warning.denied.text") })
  ] }) }) : (0, import_jsx_runtime3.jsxs)(Stack, { space: 3, paddingBottom: 0, children: [
    (0, import_jsx_runtime3.jsx)(TextInput, { placeholder: t2("request-permission-dialog.note-input.placeholder.text"), disabled: isSubmitting, onKeyDown: (e2) => {
      e2.key === "Enter" && onSubmit();
    }, maxLength: MAX_NOTE_LENGTH, value: note, onChange: (e_0) => {
      setNote(e_0.currentTarget.value);
    } }),
    (0, import_jsx_runtime3.jsx)(Text, { align: "right", muted: true, size: 1, children: `${note.length}/${MAX_NOTE_LENGTH}` })
  ] }), $[36] = hasBeenDenied, $[37] = hasTooManyRequests, $[38] = isSubmitting, $[39] = msgError, $[40] = note, $[41] = onSubmit, $[42] = t2, $[43] = t14) : t14 = $[43];
  let t15;
  $[44] !== t13 || $[45] !== t14 ? (t15 = (0, import_jsx_runtime3.jsx)(DialogBody, { children: (0, import_jsx_runtime3.jsxs)(Stack, { space: 4, children: [
    t13,
    t14
  ] }) }), $[44] = t13, $[45] = t14, $[46] = t15) : t15 = $[46];
  let t16;
  $[47] !== dialogId || $[48] !== onClose || $[49] !== t11 || $[50] !== t15 || $[51] !== t5 ? (t16 = (0, import_jsx_runtime3.jsx)(Dialog2, { width: 1, id: dialogId, header: t5, footer: t11, onClose, onClickOutside: onClose, children: t15 }), $[47] = dialogId, $[48] = onClose, $[49] = t11, $[50] = t15, $[51] = t5, $[52] = t16) : t16 = $[52];
  let t17;
  return $[53] !== t16 || $[54] !== zOffset.fullscreen ? (t17 = (0, import_jsx_runtime3.jsx)(DialogProvider, { position: "fixed", zOffset: t4, children: t16 }), $[53] = t16, $[54] = zOffset.fullscreen, $[55] = t17) : t17 = $[55], t17;
}
function _temp2$6(role_0) {
  return role_0.name === "editor";
}
function _temp$h(role) {
  return role == null ? void 0 : role.appliesToUsers;
}
var LOADING_STATE = {
  loading: true,
  error: false,
  status: void 0
};
var EMPTY_STATE = {
  loading: false,
  error: false,
  status: "none"
};
var DECLINED_STATE = {
  loading: false,
  error: false,
  status: "declined"
};
var PENDING_STATE = {
  loading: false,
  error: false,
  status: "pending"
};
var EXPIRED_STATE = {
  loading: false,
  error: false,
  status: "expired"
};
var useRoleRequestsStatus = () => {
  const $ = (0, import_react_compiler_runtime.c)(9);
  let t0;
  $[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = {
    apiVersion: "2024-07-01"
  }, $[0] = t0) : t0 = $[0];
  const client = useClient(t0), projectId = useProjectId$1();
  let t1;
  bb0: {
    if (!client || !projectId) {
      let t23;
      $[1] === Symbol.for("react.memo_cache_sentinel") ? (t23 = of(EMPTY_STATE), $[1] = t23) : t23 = $[1], t1 = t23;
      break bb0;
    }
    let t22;
    $[2] !== client || $[3] !== projectId ? (t22 = checkRoleRequests(client, projectId), $[2] = client, $[3] = projectId, $[4] = t22) : t22 = $[4], t1 = t22;
  }
  const checkRoleRequests$ = t1, {
    loading,
    error,
    status
  } = useObservable(checkRoleRequests$, LOADING_STATE);
  let t2;
  return $[5] !== error || $[6] !== loading || $[7] !== status ? (t2 = {
    data: status,
    loading,
    error
  }, $[5] = error, $[6] = loading, $[7] = status, $[8] = t2) : t2 = $[8], t2;
};
function checkRoleRequests(client, projectId) {
  return client.observable.request({
    url: "/access/requests/me",
    tag: "use-role-requests-status"
  }).pipe(
    map((requests) => {
      if (!requests || requests.length === 0)
        return EMPTY_STATE;
      const projectRequests = requests.filter((request) => request.resourceId === projectId && request.type === "role"), declinedRequest = projectRequests.find((request) => request.status === "declined");
      return declinedRequest && isAfter(addWeeks(new Date(declinedRequest.createdAt), 2), /* @__PURE__ */ new Date()) ? DECLINED_STATE : projectRequests.find((request) => request.status === "pending" && isAfter(addWeeks(new Date(request.createdAt), 2), /* @__PURE__ */ new Date())) ? PENDING_STATE : projectRequests.find((request) => request.status === "pending" && isBefore(addWeeks(new Date(request.createdAt), 2), /* @__PURE__ */ new Date())) ? EXPIRED_STATE : EMPTY_STATE;
    }),
    catchError((err) => (console.error("Failed to fetch access requests", err), of({
      loading: false,
      error: true,
      status: void 0
    }))),
    startWith(LOADING_STATE)
    // Start with loading state
  );
}
function InsufficientPermissionBanner(t0) {
  const $ = (0, import_react_compiler_runtime.c)(33), {
    requiredPermission
  } = t0, currentUser = useCurrentUser(), {
    data: roleRequestStatus,
    loading: requestStatusLoading,
    error: requestStatusError
  } = useRoleRequestsStatus(), [requestSent, setRequestSent] = (0, import_react3.useState)(false);
  let t1;
  t1 = roleRequestStatus === "pending" || roleRequestStatus === "declined" || requestSent;
  const requestPending = t1;
  let t2;
  $[0] !== (currentUser == null ? void 0 : currentUser.roles) ? (t2 = (currentUser == null ? void 0 : currentUser.roles) || [], $[0] = currentUser == null ? void 0 : currentUser.roles, $[1] = t2) : t2 = $[1];
  const currentUserRoles = t2, isOnlyViewer = currentUserRoles.length === 1 && currentUserRoles[0].name === "viewer", [showRequestPermissionDialog, setShowRequestPermissionDialog] = (0, import_react3.useState)(false);
  let t3;
  $[2] === Symbol.for("react.memo_cache_sentinel") ? (t3 = {
    style: "short"
  }, $[2] = t3) : t3 = $[2];
  const listFormat = useListFormat(t3), {
    t: t4
  } = useTranslation(structureLocaleNamespace), telemetry = useTelemetry();
  let roleTitles, t42;
  $[3] !== currentUserRoles || $[4] !== listFormat ? (roleTitles = currentUserRoles.map(_temp$g), t42 = listFormat.formatToParts(roleTitles).map(_temp2$5), $[3] = currentUserRoles, $[4] = listFormat, $[5] = roleTitles, $[6] = t42) : (roleTitles = $[5], t42 = $[6]);
  const roles = t42;
  let t5;
  $[7] !== roles ? (t5 = {
    Roles: () => (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children: roles })
  }, $[7] = roles, $[8] = t5) : t5 = $[8];
  let t6;
  $[9] !== roleTitles || $[10] !== roles.length ? (t6 = {
    count: roles.length,
    roles: roleTitles
  }, $[9] = roleTitles, $[10] = roles.length, $[11] = t6) : t6 = $[11];
  let t7;
  $[12] !== requiredPermission || $[13] !== t4 || $[14] !== t5 || $[15] !== t6 ? (t7 = (0, import_jsx_runtime3.jsx)(Text, { size: 1, weight: "medium", children: (0, import_jsx_runtime3.jsx)(Translate, { t: t4, i18nKey: "banners.permission-check-banner.missing-permission", components: t5, values: t6, context: requiredPermission }) }), $[12] = requiredPermission, $[13] = t4, $[14] = t5, $[15] = t6, $[16] = t7) : t7 = $[16];
  let t8;
  $[17] !== isOnlyViewer || $[18] !== requestPending || $[19] !== requestStatusError || $[20] !== requestStatusLoading || $[21] !== roleRequestStatus || $[22] !== t4 || $[23] !== telemetry ? (t8 = isOnlyViewer && roleRequestStatus && !requestStatusError && !requestStatusLoading ? {
    onClick: requestPending ? void 0 : () => {
      setShowRequestPermissionDialog(true), telemetry.log(AskToEditDialogOpened);
    },
    text: t4(requestPending ? "banners.permission-check-banner.request-permission-button.sent" : "banners.permission-check-banner.request-permission-button.text"),
    tone: requestPending ? "default" : "primary",
    disabled: requestPending,
    mode: requestPending ? "bleed" : void 0
  } : void 0, $[17] = isOnlyViewer, $[18] = requestPending, $[19] = requestStatusError, $[20] = requestStatusLoading, $[21] = roleRequestStatus, $[22] = t4, $[23] = telemetry, $[24] = t8) : t8 = $[24];
  let t9;
  $[25] !== t7 || $[26] !== t8 ? (t9 = (0, import_jsx_runtime3.jsx)(Banner, { content: t7, action: t8, "data-testid": "permission-check-banner", icon: ReadOnlyIcon }), $[25] = t7, $[26] = t8, $[27] = t9) : t9 = $[27];
  let t10;
  $[28] !== showRequestPermissionDialog ? (t10 = showRequestPermissionDialog && (0, import_jsx_runtime3.jsx)(RequestPermissionDialog, { onClose: () => setShowRequestPermissionDialog(false), onRequestSubmitted: () => {
    setRequestSent(true), setShowRequestPermissionDialog(false);
  } }), $[28] = showRequestPermissionDialog, $[29] = t10) : t10 = $[29];
  let t11;
  return $[30] !== t10 || $[31] !== t9 ? (t11 = (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [
    t9,
    t10
  ] }), $[30] = t10, $[31] = t9, $[32] = t11) : t11 = $[32], t11;
}
function _temp2$5(part) {
  return part.type === "element" ? (0, import_jsx_runtime3.jsx)("code", { children: part.value }, part.value) : part.value;
}
function _temp$g(role) {
  return role.title;
}
var ReferenceChangedBanner = (0, import_react3.memo)(() => {
  var _a, _b, _c, _d, _e, _f, _g, _h;
  const documentPreviewStore = useDocumentPreviewStore(), {
    selectedReleaseId
  } = usePerspective(), {
    params,
    groupIndex,
    routerPanesState,
    replaceCurrent,
    BackLink: BackLink2
  } = usePaneRouter(), routerReferenceId = (_a = routerPanesState[groupIndex]) == null ? void 0 : _a[0].id, parentSibling = (_b = routerPanesState[groupIndex - 1]) == null ? void 0 : _b[0], parentId = parentSibling == null ? void 0 : parentSibling.id, hasHistoryOpen = !!((_c = parentSibling == null ? void 0 : parentSibling.params) == null ? void 0 : _c.rev), parentRefPath = (0, import_react3.useMemo)(() => (params == null ? void 0 : params.parentRefPath) && fromString(params.parentRefPath) || null, [params == null ? void 0 : params.parentRefPath]), {
    t: t2
  } = useTranslation(structureLocaleNamespace), referenceInfoObservable = (0, import_react3.useMemo)(() => {
    const parentRefPathSegment = parentRefPath == null ? void 0 : parentRefPath[0];
    if (!parentId || !parentRefPathSegment || !parentRefPath)
      return of({
        loading: false
      });
    const publishedId = getPublishedId(parentId), path = fromString(parentRefPathSegment), keyedSegmentIndex = path.findIndex((p) => typeof p == "object" && "_key" in p);
    return concat(
      // emit a loading state instantly
      of({
        loading: true
      }),
      // then emit the values from watching the published ID's path
      documentPreviewStore.unstable_observePathsDocumentPair(publishedId, keyedSegmentIndex === -1 ? path : path.slice(0, keyedSegmentIndex), {
        version: selectedReleaseId
      }).pipe(
        // this debounce time is needed to prevent flashing banners due to
        // the router state updating faster than the content-lake state. we
        // debounce to wait for more emissions because the value pulled
        // initially could be stale.
        debounceTime(750),
        map(({
          draft,
          published,
          version
        }) => {
          var _a2;
          return {
            loading: false,
            result: {
              availability: {
                draft: draft.availability,
                published: published.availability,
                ...(version == null ? void 0 : version.availability) ? {
                  version: version.availability
                } : {}
              },
              refValue: (_a2 = get((version == null ? void 0 : version.snapshot) || draft.snapshot || published.snapshot, parentRefPath)) == null ? void 0 : _a2._ref
            }
          };
        })
      )
    );
  }, [selectedReleaseId, documentPreviewStore, parentId, parentRefPath]), referenceInfo = useObservable(referenceInfoObservable, {
    loading: true
  }), handleReloadReference = (0, import_react3.useCallback)(() => {
    var _a2;
    referenceInfo.loading || ((_a2 = referenceInfo.result) == null ? void 0 : _a2.refValue) && replaceCurrent({
      id: referenceInfo.result.refValue,
      params
    });
  }, [referenceInfo.loading, referenceInfo.result, replaceCurrent, params]);
  return (
    // if `parentId` or `parentRefPath` is not present then this banner is n/a
    !parentId || !parentRefPath || // if viewing this pane via history, then hide
    hasHistoryOpen || // if loading, hide
    referenceInfo.loading || // if the parent document is not available (e.g. due to permission denied or
    // not found) we don't want to display a warning here, but instead rely on the
    // parent view to display the appropriate message
    !((_d = referenceInfo.result) == null ? void 0 : _d.availability.draft.available) && !((_e = referenceInfo.result) == null ? void 0 : _e.availability.published.available) || // if the references are the same, then hide the reference changed banner
    ((_f = referenceInfo.result) == null ? void 0 : _f.refValue) === routerReferenceId ? null : (0, import_jsx_runtime3.jsx)(Banner, { action: ((_g = referenceInfo.result) == null ? void 0 : _g.refValue) ? {
      onClick: handleReloadReference,
      icon: SyncIcon,
      text: t2("banners.reference-changed-banner.reason-changed.reload-button.text")
    } : {
      as: BackLink2,
      icon: CloseIcon,
      text: t2("banners.reference-changed-banner.reason-removed.close-button.text")
    }, "data-testid": "reference-changed-banner", content: (0, import_jsx_runtime3.jsx)(Text, { size: 1, weight: "medium", children: ((_h = referenceInfo.result) == null ? void 0 : _h.refValue) ? t2("banners.reference-changed-banner.reason-changed.text") : t2("banners.reference-changed-banner.reason-removed.text") }), icon: WarningOutlineIcon, tone: "caution" })
  );
});
ReferenceChangedBanner.displayName = "Memo(ReferenceChangedBanner)";
function ArchivedReleaseDocumentBanner() {
  const {
    t: t2
  } = useTranslation(structureLocaleNamespace), {
    t: tCore
  } = useTranslation(), {
    data: archivedReleases
  } = useArchivedReleases(), {
    params,
    setParams
  } = usePaneRouter(), handleGoBack = () => {
    setParams({
      ...params,
      rev: (params == null ? void 0 : params.historyEvent) || void 0,
      since: void 0,
      historyVersion: void 0
    });
  }, release = (0, import_react3.useMemo)(() => archivedReleases.find((r2) => getReleaseIdFromReleaseDocumentId(r2._id) === (params == null ? void 0 : params.historyVersion)), [archivedReleases, params == null ? void 0 : params.historyVersion]), description = (release == null ? void 0 : release.state) === "published" ? "banners.published-release.description" : "banners.archived-release.description", title = (release == null ? void 0 : release.metadata.title) || tCore("release.placeholder-untitled-release");
  return (0, import_jsx_runtime3.jsx)(Banner, { tone: "caution", content: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey: description, values: {
    title
  }, components: {
    VersionBadge: getVersionInlineBadge(release)
  } }) }), action: (params == null ? void 0 : params.archivedRelease) ? void 0 : {
    text: "Go back to published version",
    onClick: handleGoBack
  } });
}
var Image = dt.img`
  object-fit: cover;
  width: 100%;
  height: 100%;
  height: 180px;
  display: flex;
`;
var CANVAS_IMAGE_URL = "https://cdn.sanity.io/images/pyrmmpch/production/b47224e2f3a7d1747e43b9da1ac31739250e628b-632x376.png";
var CANVAS_APP_NAME = "Canvas";
var CanvasPopoverContent = (t0) => {
  const $ = (0, import_react_compiler_runtime.c)(22), {
    onClose
  } = t0, {
    t: t2
  } = useTranslation(structureLocaleNamespace), ref = (0, import_react3.useRef)(null);
  let t1;
  $[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = () => [ref.current], $[0] = t1) : t1 = $[0], useClickOutsideEvent(onClose, t1);
  let t22;
  $[1] === Symbol.for("react.memo_cache_sentinel") ? (t22 = (0, import_jsx_runtime3.jsx)(Image, { src: CANVAS_IMAGE_URL, alt: "Canvas" }), $[1] = t22) : t22 = $[1];
  let t3;
  $[2] === Symbol.for("react.memo_cache_sentinel") ? (t3 = (0, import_jsx_runtime3.jsx)(Text, { size: 1, weight: "semibold", children: CANVAS_APP_NAME }), $[2] = t3) : t3 = $[2];
  let t4;
  $[3] === Symbol.for("react.memo_cache_sentinel") ? (t4 = (0, import_jsx_runtime3.jsxs)(Flex, { paddingY: 1, gap: 2, children: [
    t3,
    (0, import_jsx_runtime3.jsx)(Text, { size: 1, muted: true, children: (0, import_jsx_runtime3.jsx)(ComposeSparklesIcon, {}) })
  ] }), $[3] = t4) : t4 = $[3];
  let t5;
  $[4] !== t2 ? (t5 = t2("canvas.banner.popover-heading"), $[4] = t2, $[5] = t5) : t5 = $[5];
  let t6;
  $[6] !== t5 ? (t6 = (0, import_jsx_runtime3.jsx)(Box, { paddingTop: 3, children: (0, import_jsx_runtime3.jsx)(Heading, { size: 1, children: t5 }) }), $[6] = t5, $[7] = t6) : t6 = $[7];
  let t7;
  $[8] !== t2 ? (t7 = t2("canvas.banner.popover-description"), $[8] = t2, $[9] = t7) : t7 = $[9];
  let t8;
  $[10] !== t7 ? (t8 = (0, import_jsx_runtime3.jsx)(Box, { paddingTop: 4, children: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: t7 }) }), $[10] = t7, $[11] = t8) : t8 = $[11];
  let t9;
  $[12] !== t6 || $[13] !== t8 ? (t9 = (0, import_jsx_runtime3.jsxs)(Flex, { paddingX: 4, paddingBottom: 4, paddingTop: 3, direction: "column", children: [
    t4,
    t6,
    t8
  ] }), $[12] = t6, $[13] = t8, $[14] = t9) : t9 = $[14];
  let t10;
  $[15] !== t2 ? (t10 = t2("canvas.banner.popover-button-text"), $[15] = t2, $[16] = t10) : t10 = $[16];
  let t11;
  $[17] !== t10 ? (t11 = (0, import_jsx_runtime3.jsx)(Flex, { width: "full", gap: 3, justify: "flex-end", paddingX: 4, paddingBottom: 4, children: (0, import_jsx_runtime3.jsx)(Button2, { mode: "bleed", text: t10, autoFocus: true, tone: "primary", href: "https://snty.link/canvas-docs", target: "_blank", rel: "noopener noreferrer", as: "a", iconRight: LaunchIcon }) }), $[17] = t10, $[18] = t11) : t11 = $[18];
  let t12;
  return $[19] !== t11 || $[20] !== t9 ? (t12 = (0, import_jsx_runtime3.jsx)(Card, { radius: 3, overflow: "hidden", width: 0, ref, children: (0, import_jsx_runtime3.jsxs)(Container, { width: 0, children: [
    t22,
    t9,
    t11
  ] }) }), $[19] = t11, $[20] = t9, $[21] = t12) : t12 = $[21], t12;
};
var CanvasLinkedBannerContent = (t0) => {
  const $ = (0, import_react_compiler_runtime.c)(17), {
    documentId
  } = t0, {
    t: t2
  } = useTranslation(structureLocaleNamespace), [open, setOpen] = (0, import_react3.useState)(false), documentVariantType = getDocumentVariantType(documentId);
  let t1;
  bb0: {
    if (documentVariantType === "published") {
      let t23;
      $[0] !== t2 ? (t23 = t2("canvas.banner.linked-text.published"), $[0] = t2, $[1] = t23) : t23 = $[1], t1 = t23;
      break bb0;
    }
    if (documentVariantType === "draft") {
      let t23;
      $[2] !== t2 ? (t23 = t2("canvas.banner.linked-text.draft"), $[2] = t2, $[3] = t23) : t23 = $[3], t1 = t23;
      break bb0;
    }
    let t222;
    $[4] !== t2 ? (t222 = t2("canvas.banner.linked-text.version"), $[4] = t2, $[5] = t222) : t222 = $[5], t1 = t222;
  }
  const variantText = t1;
  let t22;
  $[6] === Symbol.for("react.memo_cache_sentinel") ? (t22 = () => setOpen(_temp$f), $[6] = t22) : t22 = $[6];
  const togglePopover = t22;
  let t3;
  $[7] === Symbol.for("react.memo_cache_sentinel") ? (t3 = () => setOpen(false), $[7] = t3) : t3 = $[7];
  const onClose = t3;
  let t4;
  $[8] !== variantText ? (t4 = (0, import_jsx_runtime3.jsx)(Text, { size: 1, weight: "medium", children: variantText }), $[8] = variantText, $[9] = t4) : t4 = $[9];
  let t5, t6;
  $[10] === Symbol.for("react.memo_cache_sentinel") ? (t5 = (0, import_jsx_runtime3.jsx)(CanvasPopoverContent, { onClose }), t6 = (0, import_jsx_runtime3.jsx)(Button2, { tooltipProps: null, mode: "bleed", tone: "default", icon: InfoOutlineIcon, onClick: togglePopover }), $[10] = t5, $[11] = t6) : (t5 = $[10], t6 = $[11]);
  let t7;
  $[12] !== open ? (t7 = (0, import_jsx_runtime3.jsx)(Popover2, { open, size: 0, tone: "default", portal: true, placement: "bottom-start", content: t5, children: t6 }), $[12] = open, $[13] = t7) : t7 = $[13];
  let t8;
  return $[14] !== t4 || $[15] !== t7 ? (t8 = (0, import_jsx_runtime3.jsxs)(Flex, { align: "center", gap: 2, children: [
    t4,
    t7
  ] }), $[14] = t4, $[15] = t7, $[16] = t8) : t8 = $[16], t8;
};
function CanvasLinkedBanner() {
  const $ = (0, import_react_compiler_runtime.c)(10), {
    documentId,
    displayed
  } = useDocumentPane(), {
    t: t2
  } = useTranslation(structureLocaleNamespace), id2 = (displayed == null ? void 0 : displayed._id) || documentId, {
    companionDoc
  } = useCanvasCompanionDoc(id2), navigateToCanvas = useNavigateToCanvasDoc(companionDoc == null ? void 0 : companionDoc.canvasDocumentId, "banner");
  if (!companionDoc)
    return null;
  let t0;
  $[0] !== id2 ? (t0 = (0, import_jsx_runtime3.jsx)(CanvasLinkedBannerContent, { documentId: id2 }), $[0] = id2, $[1] = t0) : t0 = $[1];
  let t1;
  $[2] !== t2 ? (t1 = t2("canvas.banner.edit-in-canvas-action"), $[2] = t2, $[3] = t1) : t1 = $[3];
  let t22;
  $[4] !== navigateToCanvas || $[5] !== t1 ? (t22 = {
    mode: "ghost",
    text: t1,
    onClick: navigateToCanvas
  }, $[4] = navigateToCanvas, $[5] = t1, $[6] = t22) : t22 = $[6];
  let t3;
  return $[7] !== t0 || $[8] !== t22 ? (t3 = (0, import_jsx_runtime3.jsx)(Banner, { tone: "neutral", "data-test-id": "canvas-linked-banner", paddingY: 0, content: t0, action: t22 }), $[7] = t0, $[8] = t22, $[9] = t3) : t3 = $[9], t3;
}
function _temp$f(prev) {
  return !prev;
}
var ChooseNewDocumentDestinationBanner = (t0) => {
  const $ = (0, import_react_compiler_runtime.c)(27), {
    schemaType,
    selectedPerspective,
    reason
  } = t0, {
    t: t2
  } = useTranslation(structureLocaleNamespace), {
    document: t1
  } = useWorkspace(), {
    drafts: t22
  } = t1, {
    enabled: isDraftModelEnabled
  } = t22;
  let t3;
  $[0] !== isDraftModelEnabled || $[1] !== schemaType ? (t3 = (t42) => {
    const {
      perspective
    } = t42;
    return {
      disabled: !isPerspectiveWriteable({
        selectedPerspective: perspective,
        isDraftModelEnabled,
        schemaType
      }).result
    };
  }, $[0] = isDraftModelEnabled, $[1] = schemaType, $[2] = t3) : t3 = $[2];
  const menuItemProps = t3;
  let t4;
  $[3] !== reason || $[4] !== t2 ? (t4 = reason === "PUBLISHED_NOT_WRITEABLE" && t2("banners.choose-new-document-destination.cannot-create-published-document"), $[3] = reason, $[4] = t2, $[5] = t4) : t4 = $[5];
  let t5;
  $[6] !== reason || $[7] !== t2 ? (t5 = reason === "DRAFTS_NOT_WRITEABLE" && t2("banners.choose-new-document-destination.cannot-create-draft-document"), $[6] = reason, $[7] = t2, $[8] = t5) : t5 = $[8];
  let t6;
  $[9] !== reason || $[10] !== selectedPerspective || $[11] !== t2 ? (t6 = reason === "RELEASE_NOT_ACTIVE" && isReleaseDocument(selectedPerspective) && (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey: "banners.choose-new-document-destination.release-inactive", values: {
    title: selectedPerspective.metadata.title
  }, components: {
    VersionBadge: getVersionInlineBadge(selectedPerspective)
  } }), $[9] = reason, $[10] = selectedPerspective, $[11] = t2, $[12] = t6) : t6 = $[12];
  let t7;
  $[13] !== t2 ? (t7 = t2("banners.choose-new-document-destination.choose-destination"), $[13] = t2, $[14] = t7) : t7 = $[14];
  let t8;
  $[15] !== t7 ? (t8 = (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [
    " ",
    t7
  ] }), $[15] = t7, $[16] = t8) : t8 = $[16];
  let t9;
  $[17] !== t4 || $[18] !== t5 || $[19] !== t6 || $[20] !== t8 ? (t9 = (0, import_jsx_runtime3.jsxs)(Text, { size: 1, children: [
    t4,
    t5,
    t6,
    t8
  ] }), $[17] = t4, $[18] = t5, $[19] = t6, $[20] = t8, $[21] = t9) : t9 = $[21];
  let t10;
  $[22] !== menuItemProps ? (t10 = (0, import_jsx_runtime3.jsx)(ReleasesNav, { menuItemProps }), $[22] = menuItemProps, $[23] = t10) : t10 = $[23];
  let t11;
  return $[24] !== t10 || $[25] !== t9 ? (t11 = (0, import_jsx_runtime3.jsx)(Banner, { tone: "caution", icon: WarningOutlineIcon, content: (0, import_jsx_runtime3.jsxs)(Flex, { align: "center", gap: 2, children: [
    t9,
    t10
  ] }) }), $[24] = t10, $[25] = t9, $[26] = t11) : t11 = $[26], t11;
};
function CreateLinkedBanner() {
  const $ = (0, import_react_compiler_runtime.c)(5), {
    value
  } = useDocumentPane(), {
    documentLinkedBannerContent: CreateLinkedBannerContent
  } = useSanityCreateConfig().components ?? {};
  let t0;
  $[0] !== value ? (t0 = getSanityCreateLinkMetadata(value), $[0] = value, $[1] = t0) : t0 = $[1];
  const createLinkMetadata = t0;
  if (!CreateLinkedBannerContent || !createLinkMetadata)
    return null;
  let t1;
  return $[2] !== CreateLinkedBannerContent || $[3] !== createLinkMetadata ? (t1 = (0, import_jsx_runtime3.jsx)(Banner, { tone: "transparent", "data-test-id": "sanity-create-read-only-banner", content: (0, import_jsx_runtime3.jsx)(CreateLinkedBannerContent, { metadata: createLinkMetadata }) }), $[2] = CreateLinkedBannerContent, $[3] = createLinkMetadata, $[4] = t1) : t1 = $[4], t1;
}
var TOAST_DELAY = 1e3;
function DocumentNotInReleaseBanner({
  documentId,
  currentRelease,
  value,
  isScheduledRelease
}) {
  var _a;
  const tone = getReleaseTone(currentRelease ?? LATEST), {
    t: t2
  } = useTranslation(structureLocaleNamespace), {
    t: tCore
  } = useTranslation(), {
    createVersion
  } = useVersionOperations(), [versionCreateState, setVersionCreateState] = (0, import_react3.useState)(), toast = useToast(), handleAddToRelease = (0, import_react3.useCallback)(async () => {
    if (currentRelease._id) {
      setVersionCreateState({
        status: "creating",
        lastUpdate: /* @__PURE__ */ new Date()
      });
      try {
        await createVersion(getReleaseIdFromReleaseDocumentId(currentRelease._id), documentId, value), setVersionCreateState({
          status: "created",
          lastUpdate: /* @__PURE__ */ new Date()
        });
      } catch (err) {
        toast.push({
          status: "error",
          closable: true,
          title: t2("banners.release.error.title"),
          description: t2("banners.release.error.description", {
            message: err.message
          })
        }), setVersionCreateState(void 0);
      }
    }
  }, [createVersion, currentRelease._id, documentId, t2, toast, value]), now = useCurrentTime(200);
  return useConditionalToast({
    status: "info",
    id: "add-document-to-release",
    enabled: (versionCreateState == null ? void 0 : versionCreateState.status) === "created" && now.getTime() - versionCreateState.lastUpdate.getTime() > TOAST_DELAY,
    closable: true,
    title: t2("banners.release.waiting.title"),
    description: t2("banners.release.waiting.description")
  }), (0, import_jsx_runtime3.jsx)(
    Banner,
    {
      tone,
      content: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: (0, import_jsx_runtime3.jsx)(Translate, { i18nKey: "banners.release.not-in-release", t: t2, values: {
        title: ((_a = currentRelease == null ? void 0 : currentRelease.metadata) == null ? void 0 : _a.title) || tCore("release.placeholder-untitled-release")
      }, components: {
        VersionBadge: getVersionInlineBadge(currentRelease)
      } }) }),
      action: isScheduledRelease ? void 0 : {
        text: t2("banners.release.action.add-to-release"),
        tone,
        disabled: !!versionCreateState,
        onClick: handleAddToRelease,
        mode: "default"
      }
    }
  );
}
function useCurrentTime(updateIntervalMs) {
  const $ = (0, import_react_compiler_runtime.c)(4);
  let t0;
  $[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = /* @__PURE__ */ new Date(), $[0] = t0) : t0 = $[0];
  const [currentTime, setCurrentTime] = (0, import_react3.useState)(t0);
  let t1, t2;
  return $[1] !== updateIntervalMs ? (t1 = () => {
    const intervalId = setInterval(() => {
      setCurrentTime(/* @__PURE__ */ new Date());
    }, updateIntervalMs);
    return () => clearInterval(intervalId);
  }, t2 = [updateIntervalMs], $[1] = updateIntervalMs, $[2] = t1, $[3] = t2) : (t1 = $[2], t2 = $[3]), (0, import_react3.useEffect)(t1, t2), currentTime;
}
var ResolvedLiveEdit = defineEvent({
  name: "LiveEdit Draft Resolved",
  version: 1,
  description: "User resolved a draft of a live edit document to continue editing"
});
var ObsoleteDraftBanner = (t0) => {
  const $ = (0, import_react_compiler_runtime.c)(47), {
    displayed,
    documentId,
    schemaType,
    i18nKey,
    isEditBlocking
  } = t0, {
    t: t2
  } = useTranslation(structureLocaleNamespace), [isPublishing, setPublishing] = (0, import_react3.useState)(false), [isDiscarding, setDiscarding] = (0, import_react3.useState)(false), telemetry = useTelemetry(), {
    publish,
    discardChanges
  } = useDocumentOperation(documentId, (displayed == null ? void 0 : displayed._type) || "");
  let t1;
  $[0] !== publish || $[1] !== telemetry ? (t1 = () => {
    publish.execute(), setPublishing(true), telemetry.log(ResolvedLiveEdit, {
      liveEditResolveType: "publish"
    });
  }, $[0] = publish, $[1] = telemetry, $[2] = t1) : t1 = $[2];
  const handlePublish = t1;
  let t22;
  $[3] !== discardChanges || $[4] !== telemetry ? (t22 = () => {
    discardChanges.execute(), setDiscarding(true), telemetry.log(ResolvedLiveEdit, {
      liveEditResolveType: "discard"
    });
  }, $[3] = discardChanges, $[4] = telemetry, $[5] = t22) : t22 = $[5];
  const handleDiscard = t22;
  let t3;
  $[6] === Symbol.for("react.memo_cache_sentinel") ? (t3 = () => () => {
    setPublishing(false), setDiscarding(false);
  }, $[6] = t3) : t3 = $[6], (0, import_react3.useEffect)(t3);
  const diffViewRouter = useDiffViewRouter();
  let t4;
  $[7] !== diffViewRouter || $[8] !== (displayed == null ? void 0 : displayed._id) || $[9] !== schemaType.name ? (t4 = () => {
    typeof (displayed == null ? void 0 : displayed._id) > "u" || diffViewRouter.navigateDiffView({
      mode: "version",
      previousDocument: {
        type: schemaType.name,
        id: getPublishedId(displayed == null ? void 0 : displayed._id)
      },
      nextDocument: {
        type: schemaType.name,
        id: getDraftId(displayed == null ? void 0 : displayed._id)
      }
    });
  }, $[7] = diffViewRouter, $[8] = displayed == null ? void 0 : displayed._id, $[9] = schemaType.name, $[10] = t4) : t4 = $[10];
  const compareDraft = t4;
  let t5;
  $[11] !== schemaType.title ? (t5 = {
    schemaType: schemaType.title
  }, $[11] = schemaType.title, $[12] = t5) : t5 = $[12];
  let t6;
  $[13] !== i18nKey || $[14] !== t2 || $[15] !== t5 ? (t6 = (0, import_jsx_runtime3.jsx)(Text, { size: 1, weight: "medium", children: (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey, values: t5 }) }), $[13] = i18nKey, $[14] = t2, $[15] = t5, $[16] = t6) : t6 = $[16];
  let t7;
  $[17] !== t2 ? (t7 = t2("banners.obsolete-draft.actions.compare-draft.text"), $[17] = t2, $[18] = t7) : t7 = $[18];
  let t8;
  $[19] !== compareDraft || $[20] !== t7 ? (t8 = (0, import_jsx_runtime3.jsx)(Button2, { text: t7, mode: "ghost", onClick: compareDraft }), $[19] = compareDraft, $[20] = t7, $[21] = t8) : t8 = $[21];
  let t9;
  $[22] !== t2 ? (t9 = t2("banners.obsolete-draft.actions.publish-draft.text"), $[22] = t2, $[23] = t9) : t9 = $[23];
  let t10;
  $[24] !== isEditBlocking || $[25] !== t2 ? (t10 = isEditBlocking ? {
    content: t2("banners.live-edit-draft-banner.publish.tooltip")
  } : {}, $[24] = isEditBlocking, $[25] = t2, $[26] = t10) : t10 = $[26];
  let t11;
  $[27] !== handlePublish || $[28] !== isPublishing || $[29] !== t10 || $[30] !== t9 ? (t11 = (0, import_jsx_runtime3.jsx)(Button2, { onClick: handlePublish, text: t9, tooltipProps: t10, loading: isPublishing, tone: "positive" }), $[27] = handlePublish, $[28] = isPublishing, $[29] = t10, $[30] = t9, $[31] = t11) : t11 = $[31];
  let t12;
  $[32] !== t2 ? (t12 = t2("banners.obsolete-draft.actions.discard-draft.text"), $[32] = t2, $[33] = t12) : t12 = $[33];
  let t13;
  $[34] !== isEditBlocking || $[35] !== t2 ? (t13 = isEditBlocking ? {
    content: t2("banners.live-edit-draft-banner.discard.tooltip")
  } : {}, $[34] = isEditBlocking, $[35] = t2, $[36] = t13) : t13 = $[36];
  let t14;
  $[37] !== handleDiscard || $[38] !== isDiscarding || $[39] !== t12 || $[40] !== t13 ? (t14 = (0, import_jsx_runtime3.jsx)(Button2, { onClick: handleDiscard, text: t12, tooltipProps: t13, loading: isDiscarding, tone: "caution" }), $[37] = handleDiscard, $[38] = isDiscarding, $[39] = t12, $[40] = t13, $[41] = t14) : t14 = $[41];
  let t15;
  return $[42] !== t11 || $[43] !== t14 || $[44] !== t6 || $[45] !== t8 ? (t15 = (0, import_jsx_runtime3.jsx)(Banner, { content: (0, import_jsx_runtime3.jsxs)(Flex, { align: "center", justify: "space-between", gap: 2, children: [
    t6,
    t8,
    t11,
    t14
  ] }), "data-testid": "live-edit-type-banner", icon: ErrorOutlineIcon }), $[42] = t11, $[43] = t14, $[44] = t6, $[45] = t8, $[46] = t15) : t15 = $[46], t15;
};
function OpenReleaseToEditBanner(t0) {
  const $ = (0, import_react_compiler_runtime.c)(4), {
    documentId,
    isPinnedDraftOrPublished
  } = t0;
  let t1;
  $[0] !== documentId ? (t1 = {
    documentId
  }, $[0] = documentId, $[1] = t1) : t1 = $[1];
  const onlyHasVersions = useOnlyHasVersions(t1);
  if (!isVersionId(documentId) || !onlyHasVersions || !isPinnedDraftOrPublished)
    return null;
  let t2;
  return $[2] !== documentId ? (t2 = (0, import_jsx_runtime3.jsx)(OpenReleaseToEditBannerInner, { documentId }), $[2] = documentId, $[3] = t2) : t2 = $[3], t2;
}
function OpenReleaseToEditBannerInner({
  documentId
}) {
  const {
    data: activeReleases
  } = useActiveReleases(), setPerspective = useSetPerspective(), releaseId = getVersionFromId(documentId) ?? "", currentVersion = (0, import_react3.useMemo)(() => activeReleases.find((version) => version._id.includes(releaseId)), [activeReleases, releaseId]), {
    t: tCore
  } = useTranslation(), {
    data: documentVersions
  } = useDocumentVersions({
    documentId
  }), documentVersionsTitleList = (0, import_react3.useMemo)(() => activeReleases.filter((version_0) => documentVersions.find((release) => {
    const r2 = getVersionFromId(release) ?? "";
    return getReleaseIdFromReleaseDocumentId(version_0._id) === r2;
  })).map((version_1) => version_1.metadata.title || tCore("release.placeholder-untitled-release")), [activeReleases, documentVersions, tCore]), tone = currentVersion && getReleaseTone(currentVersion), {
    t: t2
  } = useTranslation(structureLocaleNamespace), handleGoToEdit = (0, import_react3.useCallback)(async () => {
    setPerspective(releaseId);
  }, [releaseId, setPerspective]);
  return (0, import_jsx_runtime3.jsx)(Banner, { tone, paddingY: 0, "data-testid": "open-release-to-edit-banner", content: (0, import_jsx_runtime3.jsxs)(Flex, { direction: "row", align: "center", justify: "space-between", flex: 1, children: [
    (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: (0, import_jsx_runtime3.jsx)(Flex, { direction: "row", gap: 1, children: documentVersionsTitleList.length > 1 ? (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey: "banners.release.navigate-to-edit-description-multiple", components: {
      VersionBadge: () => (0, import_jsx_runtime3.jsxs)(VersionInlineBadge, { children: [
        " ",
        documentVersionsTitleList[0]
      ] })
    }, values: {
      count: documentVersionsTitleList.length - 1
    } }) : (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey: "banners.release.navigate-to-edit-description-single", components: {
      VersionBadge: () => (0, import_jsx_runtime3.jsxs)(VersionInlineBadge, { children: [
        " ",
        documentVersionsTitleList[0]
      ] })
    } }) }) }),
    (0, import_jsx_runtime3.jsx)(Button2, { text: t2("banners.release.action.open-to-edit"), tone, onClick: handleGoToEdit })
  ] }) });
}
function RevisionNotFoundBanner() {
  const $ = (0, import_react_compiler_runtime.c)(4), {
    revisionNotFound
  } = useDocumentPane(), {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  if (!revisionNotFound)
    return null;
  let t0;
  $[0] !== t2 ? (t0 = t2("banners.revision-not-found.description"), $[0] = t2, $[1] = t0) : t0 = $[1];
  let t1;
  return $[2] !== t0 ? (t1 = (0, import_jsx_runtime3.jsx)(Banner, { tone: "caution", "data-test-id": "revision-not-found-banner", icon: WarningOutlineIcon, content: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: t0 }) }), $[2] = t0, $[3] = t1) : t1 = $[3], t1;
}
function ScheduledReleaseBanner(t0) {
  const $ = (0, import_react_compiler_runtime.c)(12), {
    currentRelease
  } = t0, t1 = currentRelease ?? LATEST;
  let t2;
  $[0] !== t1 ? (t2 = getReleaseTone(t1), $[0] = t1, $[1] = t2) : t2 = $[1];
  const tone = t2, {
    t: tCore
  } = useTranslation();
  let t3;
  $[2] !== currentRelease ? (t3 = formatRelativeLocalePublishDate(currentRelease), $[2] = currentRelease, $[3] = t3) : t3 = $[3];
  let t4;
  $[4] !== t3 ? (t4 = {
    date: t3
  }, $[4] = t3, $[5] = t4) : t4 = $[5];
  let t5;
  $[6] !== t4 || $[7] !== tCore ? (t5 = (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: (0, import_jsx_runtime3.jsx)(Translate, { t: tCore, i18nKey: "release.banner.scheduled-for-publishing-on", values: t4 }) }), $[6] = t4, $[7] = tCore, $[8] = t5) : t5 = $[8];
  let t6;
  return $[9] !== t5 || $[10] !== tone ? (t6 = (0, import_jsx_runtime3.jsx)(Banner, { tone, icon: LockIcon, content: t5 }), $[9] = t5, $[10] = tone, $[11] = t6) : t6 = $[11], t6;
}
function UnpublishedDocumentBanner() {
  var _a, _b, _c;
  const $ = (0, import_react_compiler_runtime.c)(13), {
    value
  } = useDocumentPane(), {
    selectedPerspective
  } = usePerspective(), willBeUnpublished = isGoingToUnpublish(value), {
    t: t2
  } = useTranslation(structureLocaleNamespace), {
    t: tCore
  } = useTranslation();
  if (isReleaseDocument(selectedPerspective) && willBeUnpublished) {
    let t0;
    $[0] !== ((_a = selectedPerspective.metadata) == null ? void 0 : _a.title) || $[1] !== tCore ? (t0 = ((_b = selectedPerspective.metadata) == null ? void 0 : _b.title) || tCore("release.placeholder-untitled-release"), $[0] = (_c = selectedPerspective.metadata) == null ? void 0 : _c.title, $[1] = tCore, $[2] = t0) : t0 = $[2];
    const title = t0;
    let t1;
    $[3] !== title ? (t1 = {
      title
    }, $[3] = title, $[4] = t1) : t1 = $[4];
    let t22;
    $[5] !== selectedPerspective ? (t22 = getVersionInlineBadge(selectedPerspective), $[5] = selectedPerspective, $[6] = t22) : t22 = $[6];
    let t3;
    $[7] !== t22 ? (t3 = {
      VersionBadge: t22
    }, $[7] = t22, $[8] = t3) : t3 = $[8];
    let t4;
    return $[9] !== t2 || $[10] !== t1 || $[11] !== t3 ? (t4 = (0, import_jsx_runtime3.jsx)(Banner, { tone: "critical", content: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey: "banners.unpublished-release-banner.text", values: t1, components: t3 }) }), icon: UnpublishIcon }), $[9] = t2, $[10] = t1, $[11] = t3, $[12] = t4) : t4 = $[12], t4;
  }
  return null;
}
var TitleContainer = dt(Stack)`
  ${({
  theme
}) => lt`
      @supports not (container-type: inline-size) {
        display: none !important;
      }

      container-type: inline-size;

      [data-heading] {
        font-size: ${theme.sanity.fonts.heading.sizes[4].fontSize}px;
        line-height: ${theme.sanity.fonts.heading.sizes[4].lineHeight}px;
        overflow-wrap: break-word;
        text-wrap: pretty;
      }

      @container (max-width: 560px) {
        [data-heading] {
          font-size: ${theme.sanity.fonts.heading.sizes[3].fontSize}px;
          line-height: ${theme.sanity.fonts.heading.sizes[3].lineHeight}px;
        }
      }

      @container (max-width: 420px) {
        [data-heading] {
          font-size: ${theme.sanity.fonts.heading.sizes[2].fontSize}px;
          line-height: ${theme.sanity.fonts.heading.sizes[2].lineHeight}px;
        }
      }
    `}
`;
var FormHeader = (t0) => {
  const $ = (0, import_react_compiler_runtime.c)(14), {
    documentId,
    schemaType,
    title
  } = t0, isSingleton = documentId === schemaType.name, description = schemaType.description, {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  if (schemaType.__experimental_formPreviewTitle === false)
    return null;
  let t1;
  $[0] !== description || $[1] !== isSingleton || $[2] !== schemaType.name || $[3] !== schemaType.title ? (t1 = !isSingleton && (0, import_jsx_runtime3.jsxs)(Inline, { space: 1, children: [
    (0, import_jsx_runtime3.jsx)(Text, { muted: true, size: 1, children: schemaType.title ?? schemaType.name }),
    description && (0, import_jsx_runtime3.jsx)(Tooltip, { content: description, placement: "right", children: (0, import_jsx_runtime3.jsx)(InfoOutlineIcon, { "data-testid": "schema-description-icon" }) })
  ] }), $[0] = description, $[1] = isSingleton, $[2] = schemaType.name, $[3] = schemaType.title, $[4] = t1) : t1 = $[4];
  const t22 = !title;
  let t3;
  $[5] !== t2 || $[6] !== title ? (t3 = title ?? t2("document-view.form-view.form-title-fallback"), $[5] = t2, $[6] = title, $[7] = t3) : t3 = $[7];
  let t4;
  $[8] !== t22 || $[9] !== t3 ? (t4 = (0, import_jsx_runtime3.jsx)(Heading, { as: "h2", "data-heading": true, muted: t22, "data-testid": "document-panel-document-title", children: t3 }), $[8] = t22, $[9] = t3, $[10] = t4) : t4 = $[10];
  let t5;
  return $[11] !== t1 || $[12] !== t4 ? (t5 = (0, import_jsx_runtime3.jsxs)(TitleContainer, { marginBottom: 6, space: 4, children: [
    t1,
    t4
  ] }), $[11] = t1, $[12] = t4, $[13] = t5) : t5 = $[13], t5;
};
var preventDefault = (ev) => ev.preventDefault();
var FormView = (0, import_react3.forwardRef)(function(props, ref) {
  var _a;
  const $ = (0, import_react_compiler_runtime.c)(74), {
    hidden,
    margins
  } = props, {
    collapsedFieldSets,
    collapsedPaths,
    displayed: value,
    editState,
    documentId,
    documentType,
    fieldActions,
    onChange,
    validation,
    ready,
    formState,
    onFocus,
    connectionState,
    onBlur,
    onSetCollapsedPath,
    onPathOpen,
    onSetCollapsedFieldSet,
    onSetActiveFieldGroup,
    openPath
  } = useDocumentPane(), {
    selectedReleaseId
  } = usePerspective(), documentStore = useDocumentStore(), presence = useDocumentPresence(documentId), {
    title
  } = useDocumentTitle(), [patchChannel] = (0, import_react3.useState)(_temp$e), isLocked = (_a = editState == null ? void 0 : editState.transactionSyncLock) == null ? void 0 : _a.enabled, {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  let t0, t1;
  $[0] !== t2 ? (t1 = t2("document-view.form-view.sync-lock-toast.title"), $[0] = t2, $[1] = t1) : t1 = $[1];
  let t22;
  $[2] !== t2 ? (t22 = t2("document-view.form-view.sync-lock-toast.description"), $[2] = t2, $[3] = t22) : t22 = $[3];
  let t3;
  $[4] !== isLocked || $[5] !== t1 || $[6] !== t22 ? (t3 = {
    id: "sync-lock",
    status: "warning",
    enabled: isLocked,
    title: t1,
    description: t22,
    closable: true
  }, $[4] = isLocked, $[5] = t1, $[6] = t22, $[7] = t3) : t3 = $[7], t0 = t3, useConditionalToast(t0);
  let t4;
  $[8] !== documentId || $[9] !== documentStore.pair || $[10] !== documentType || $[11] !== patchChannel || $[12] !== selectedReleaseId ? (t4 = () => {
    const sub = documentStore.pair.documentEvents(documentId, documentType, selectedReleaseId).pipe(tap((event) => {
      event.type === "mutation" && patchChannel.publish(prepareMutationEvent(event)), event.type === "rebase" && patchChannel.publish(prepareRebaseEvent(event));
    })).subscribe();
    return () => {
      sub.unsubscribe();
    };
  }, $[8] = documentId, $[9] = documentStore.pair, $[10] = documentType, $[11] = patchChannel, $[12] = selectedReleaseId, $[13] = t4) : t4 = $[13];
  let t5;
  $[14] !== documentId || $[15] !== documentStore || $[16] !== documentType || $[17] !== patchChannel || $[18] !== selectedReleaseId ? (t5 = [documentId, documentStore, documentType, patchChannel, selectedReleaseId], $[14] = documentId, $[15] = documentStore, $[16] = documentType, $[17] = patchChannel, $[18] = selectedReleaseId, $[19] = t5) : t5 = $[19], (0, import_react3.useEffect)(t4, t5);
  const hasRev = !!(value == null ? void 0 : value._rev);
  let t6;
  $[20] !== patchChannel || $[21] !== value ? (t6 = () => {
    patchChannel.publish({
      type: "mutation",
      patches: [],
      snapshot: value
    });
  }, $[20] = patchChannel, $[21] = value, $[22] = t6) : t6 = $[22];
  const handleInitialValue = useEffectEvent(t6);
  let t7;
  $[23] !== handleInitialValue || $[24] !== hasRev ? (t7 = () => {
    hasRev && handleInitialValue();
  }, $[23] = handleInitialValue, $[24] = hasRev, $[25] = t7) : t7 = $[25];
  let t8;
  $[26] !== hasRev ? (t8 = [hasRev], $[26] = hasRev, $[27] = t8) : t8 = $[27], (0, import_react3.useEffect)(t7, t8);
  const [formRef, setFormRef] = (0, import_react3.useState)(null), [focusedFirstDescendant, setFocusedFirstDescendant] = (0, import_react3.useState)(false);
  let t9;
  $[28] !== focusedFirstDescendant || $[29] !== formRef || $[30] !== (formState == null ? void 0 : formState.focusPath.length) || $[31] !== ready ? (t9 = () => {
    !focusedFirstDescendant && ready && !(formState == null ? void 0 : formState.focusPath.length) && formRef && (setFocusedFirstDescendant(true), focusFirstDescendant(formRef));
  }, $[28] = focusedFirstDescendant, $[29] = formRef, $[30] = formState == null ? void 0 : formState.focusPath.length, $[31] = ready, $[32] = t9) : t9 = $[32];
  const t10 = formState == null ? void 0 : formState.focusPath.length;
  let t11;
  $[33] !== focusedFirstDescendant || $[34] !== formRef || $[35] !== ready || $[36] !== t10 ? (t11 = [focusedFirstDescendant, formRef, t10, ready], $[33] = focusedFirstDescendant, $[34] = formRef, $[35] = ready, $[36] = t10, $[37] = t11) : t11 = $[37], (0, import_react3.useEffect)(t9, t11);
  let t12;
  $[38] !== ref ? (t12 = (node) => {
    setFormRef(node), typeof ref == "function" ? ref(node) : ref && (ref.current = node);
  }, $[38] = ref, $[39] = t12) : t12 = $[39];
  const setRef = t12, isReadOnly = connectionState === "reconnecting" || (formState == null ? void 0 : formState.readOnly) || !(editState == null ? void 0 : editState.ready), t13 = isReadOnly ? "true" : void 0;
  let t14;
  $[40] !== collapsedFieldSets || $[41] !== collapsedPaths || $[42] !== connectionState || $[43] !== documentId || $[44] !== (editState == null ? void 0 : editState.draft) || $[45] !== (editState == null ? void 0 : editState.published) || $[46] !== fieldActions || $[47] !== formState || $[48] !== hidden || $[49] !== isReadOnly || $[50] !== onBlur || $[51] !== onChange || $[52] !== onFocus || $[53] !== onPathOpen || $[54] !== onSetActiveFieldGroup || $[55] !== onSetCollapsedFieldSet || $[56] !== onSetCollapsedPath || $[57] !== openPath || $[58] !== patchChannel || $[59] !== presence || $[60] !== t2 || $[61] !== title || $[62] !== validation ? (t14 = connectionState === "connecting" && !(editState == null ? void 0 : editState.draft) && !(editState == null ? void 0 : editState.published) ? (0, import_jsx_runtime3.jsx)(Delay, { ms: 300, children: (0, import_jsx_runtime3.jsxs)(Flex, { align: "center", direction: "column", height: "fill", justify: "center", children: [
    (0, import_jsx_runtime3.jsx)(Spinner, { muted: true }),
    (0, import_jsx_runtime3.jsx)(Box, { marginTop: 3, children: (0, import_jsx_runtime3.jsx)(Text, { align: "center", muted: true, size: 1, children: t2("document-view.form-view.loading") }) })
  ] }) }) : formState === null || hidden ? (0, import_jsx_runtime3.jsx)(Box, { padding: 2, children: (0, import_jsx_runtime3.jsx)(Text, { children: t2("document-view.form-view.form-hidden") }) }) : (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [
    (0, import_jsx_runtime3.jsx)(FormHeader, { documentId, schemaType: formState.schemaType, title }),
    (0, import_jsx_runtime3.jsx)(FormBuilder, { __internal_fieldActions: fieldActions, __internal_patchChannel: patchChannel, changed: formState.changed, collapsedFieldSets, collapsedPaths, focused: formState.focused, focusPath: formState.focusPath, groups: formState.groups, id: "root", members: formState.members, onChange, onFieldGroupSelect: onSetActiveFieldGroup, onPathBlur: onBlur, onPathFocus: onFocus, onPathOpen, onSetFieldSetCollapsed: onSetCollapsedFieldSet, onSetPathCollapsed: onSetCollapsedPath, openPath, presence, readOnly: isReadOnly, schemaType: formState.schemaType, validation, value: formState.value })
  ] }), $[40] = collapsedFieldSets, $[41] = collapsedPaths, $[42] = connectionState, $[43] = documentId, $[44] = editState == null ? void 0 : editState.draft, $[45] = editState == null ? void 0 : editState.published, $[46] = fieldActions, $[47] = formState, $[48] = hidden, $[49] = isReadOnly, $[50] = onBlur, $[51] = onChange, $[52] = onFocus, $[53] = onPathOpen, $[54] = onSetActiveFieldGroup, $[55] = onSetCollapsedFieldSet, $[56] = onSetCollapsedPath, $[57] = openPath, $[58] = patchChannel, $[59] = presence, $[60] = t2, $[61] = title, $[62] = validation, $[63] = t14) : t14 = $[63];
  let t15;
  $[64] !== setRef || $[65] !== t13 || $[66] !== t14 ? (t15 = (0, import_jsx_runtime3.jsx)(Box, { as: "form", onSubmit: preventDefault, ref: setRef, "data-testid": "form-view", "data-read-only": t13, children: t14 }), $[64] = setRef, $[65] = t13, $[66] = t14, $[67] = t15) : t15 = $[67];
  let t16;
  $[68] !== margins || $[69] !== t15 ? (t16 = (0, import_jsx_runtime3.jsx)(PresenceOverlay, { margins, children: t15 }), $[68] = margins, $[69] = t15, $[70] = t16) : t16 = $[70];
  let t17;
  return $[71] !== hidden || $[72] !== t16 ? (t17 = (0, import_jsx_runtime3.jsx)(Container, { hidden, paddingX: 4, paddingTop: 5, paddingBottom: 9, sizing: "border", width: 1, children: t16 }), $[71] = hidden, $[72] = t16, $[73] = t17) : t17 = $[73], t17;
});
function prepareMutationEvent(event) {
  const patches = event.mutations.map((mut) => mut.patch).filter(Boolean);
  return {
    type: "mutation",
    snapshot: event.document,
    patches: fromMutationPatches(event.origin, patches)
  };
}
function prepareRebaseEvent(event) {
  const remotePatches = event.remoteMutations.map((mut) => mut.patch).filter(Boolean), localPatches = event.localMutations.map((mut) => mut.patch).filter(Boolean);
  return {
    type: "rebase",
    snapshot: event.document,
    patches: fromMutationPatches("remote", remotePatches).concat(fromMutationPatches("local", localPatches))
  };
}
function _temp$e() {
  return createPatchChannel();
}
function DocumentHeaderTabs() {
  const $ = (0, import_react_compiler_runtime.c)(11), {
    activeViewId,
    paneKey,
    views: views2
  } = useDocumentPane(), tabPanelId = `${paneKey}tabpanel`;
  let t0;
  if ($[0] !== activeViewId || $[1] !== paneKey || $[2] !== tabPanelId || $[3] !== views2) {
    let t12;
    $[5] !== activeViewId || $[6] !== paneKey || $[7] !== tabPanelId ? (t12 = (view, index) => (0, import_jsx_runtime3.jsx)(DocumentHeaderTab, { icon: view.icon, id: `${paneKey}tab-${view.id}`, isActive: activeViewId === view.id, label: view.title, tabPanelId, viewId: index === 0 ? null : view.id ?? null }, view.id), $[5] = activeViewId, $[6] = paneKey, $[7] = tabPanelId, $[8] = t12) : t12 = $[8], t0 = views2.map(t12), $[0] = activeViewId, $[1] = paneKey, $[2] = tabPanelId, $[3] = views2, $[4] = t0;
  } else
    t0 = $[4];
  let t1;
  return $[9] !== t0 ? (t1 = (0, import_jsx_runtime3.jsx)(TabList, { space: 1, children: t0 }), $[9] = t0, $[10] = t1) : t1 = $[10], t1;
}
function DocumentHeaderTab(props) {
  const $ = (0, import_react_compiler_runtime.c)(20);
  let icon, id2, isActive, label, rest, tabPanelId, viewId;
  $[0] !== props ? ({
    icon,
    id: id2,
    isActive,
    label,
    tabPanelId,
    viewId,
    ...rest
  } = props, $[0] = props, $[1] = icon, $[2] = id2, $[3] = isActive, $[4] = label, $[5] = rest, $[6] = tabPanelId, $[7] = viewId) : (icon = $[1], id2 = $[2], isActive = $[3], label = $[4], rest = $[5], tabPanelId = $[6], viewId = $[7]);
  const {
    ready,
    editState
  } = useDocumentPane(), {
    setView
  } = usePaneRouter();
  let t0;
  $[8] !== setView || $[9] !== viewId ? (t0 = () => setView(viewId), $[8] = setView, $[9] = viewId, $[10] = t0) : t0 = $[10];
  const handleClick = t0, t1 = !ready && !(editState == null ? void 0 : editState.draft) && !(editState == null ? void 0 : editState.published);
  let t2;
  return $[11] !== handleClick || $[12] !== icon || $[13] !== id2 || $[14] !== isActive || $[15] !== label || $[16] !== rest || $[17] !== t1 || $[18] !== tabPanelId ? (t2 = (0, import_jsx_runtime3.jsx)(Tab, { ...rest, "aria-controls": tabPanelId, disabled: t1, icon, id: id2, label, onClick: handleClick, selected: isActive }), $[11] = handleClick, $[12] = icon, $[13] = id2, $[14] = isActive, $[15] = label, $[16] = rest, $[17] = t1, $[18] = tabPanelId, $[19] = t2) : t2 = $[19], t2;
}
function DocumentHeaderTitle() {
  const $ = (0, import_react_compiler_runtime.c)(22), {
    connectionState,
    schemaType,
    title,
    value: documentValue
  } = useDocumentPane(), subscribed = !!documentValue;
  let t0;
  $[0] !== documentValue || $[1] !== schemaType || $[2] !== subscribed ? (t0 = {
    enabled: subscribed,
    schemaType,
    value: documentValue
  }, $[0] = documentValue, $[1] = schemaType, $[2] = subscribed, $[3] = t0) : t0 = $[3];
  const {
    error,
    value
  } = useDocumentPreview(t0), {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  if (connectionState === "connecting" && !subscribed) {
    let t12;
    return $[4] === Symbol.for("react.memo_cache_sentinel") ? (t12 = (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, {}), $[4] = t12) : t12 = $[4], t12;
  }
  if (title) {
    let t12;
    return $[5] !== title ? (t12 = (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children: title }), $[5] = title, $[6] = t12) : t12 = $[6], t12;
  }
  if (!documentValue) {
    const t12 = (schemaType == null ? void 0 : schemaType.title) || (schemaType == null ? void 0 : schemaType.name);
    let t222;
    $[7] !== t2 || $[8] !== t12 ? (t222 = t2("panes.document-header-title.new.text", {
      schemaType: t12
    }), $[7] = t2, $[8] = t12, $[9] = t222) : t222 = $[9];
    let t3;
    return $[10] !== t222 ? (t3 = (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children: t222 }), $[10] = t222, $[11] = t3) : t3 = $[11], t3;
  }
  if (error) {
    let t12;
    $[12] !== error.message || $[13] !== t2 ? (t12 = t2("panes.document-header-title.error.text", {
      error: error.message
    }), $[12] = error.message, $[13] = t2, $[14] = t12) : t12 = $[14];
    let t222;
    return $[15] !== t12 ? (t222 = (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children: t12 }), $[15] = t12, $[16] = t222) : t222 = $[16], t222;
  }
  let t1;
  $[17] !== t2 || $[18] !== (value == null ? void 0 : value.title) ? (t1 = (value == null ? void 0 : value.title) || (0, import_jsx_runtime3.jsx)("span", { style: {
    color: "var(--card-muted-fg-color)"
  }, children: t2("panes.document-header-title.untitled.text") }), $[17] = t2, $[18] = value == null ? void 0 : value.title, $[19] = t1) : t1 = $[19];
  let t22;
  return $[20] !== t1 ? (t22 = (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children: t1 }), $[20] = t1, $[21] = t22) : t22 = $[21], t22;
}
var FavoriteToggle = (t0) => {
  const $ = (0, import_react_compiler_runtime.c)(16);
  let documentExists, props;
  $[0] !== t0 ? ({
    documentExists,
    ...props
  } = t0, $[0] = t0, $[1] = documentExists, $[2] = props) : (documentExists = $[1], props = $[2]);
  const {
    isFavorited,
    isReady,
    favorite,
    unfavorite
  } = useManageFavorite(props), {
    t: t2
  } = useTranslation(structureLocaleNamespace), t1 = isFavorited ? "document.favorites.remove-from-favorites" : "document.favorites.add-to-favorites";
  let t22;
  $[3] !== t2 || $[4] !== t1 ? (t22 = t2(t1), $[3] = t2, $[4] = t1, $[5] = t22) : t22 = $[5];
  const description = t22, t3 = isFavorited ? unfavorite : favorite, t4 = !isReady || !documentExists;
  let t5;
  $[6] !== description ? (t5 = {
    content: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: description }),
    placement: "right"
  }, $[6] = description, $[7] = t5) : t5 = $[7];
  let t6;
  $[8] !== isFavorited ? (t6 = (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: isFavorited ? (0, import_jsx_runtime3.jsx)(StarFilledIcon, {}) : (0, import_jsx_runtime3.jsx)(StarIcon, {}) }), $[8] = isFavorited, $[9] = t6) : t6 = $[9];
  let t7;
  return $[10] !== description || $[11] !== t3 || $[12] !== t4 || $[13] !== t5 || $[14] !== t6 ? (t7 = (0, import_jsx_runtime3.jsx)(Button2, { mode: "bleed", onClick: t3, disabled: t4, "aria-label": description, "aria-live": "assertive", tooltipProps: t5, children: t6 }), $[10] = description, $[11] = t3, $[12] = t4, $[13] = t5, $[14] = t6, $[15] = t7) : t7 = $[15], t7;
};
var DocumentPanelSubHeader = (0, import_react3.memo)(function() {
  const $ = (0, import_react_compiler_runtime.c)(26), {
    editState,
    connectionState,
    views: views2,
    documentId,
    displayed
  } = useDocumentPane(), {
    features
  } = useStructureTool(), {
    index,
    BackLink: BackLink2
  } = usePaneRouter(), {
    activeWorkspace
  } = useActiveWorkspace();
  let t0;
  $[0] !== documentId ? (t0 = getPublishedId(documentId), $[0] = documentId, $[1] = t0) : t0 = $[1];
  const publishedDocId = t0, showTabs = views2.length > 1, {
    collapsed,
    isLast
  } = usePane(), tabIndex = isLast && !collapsed ? -1 : 0, showBackButton = features.backButton && index > 0, {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  let t1, t22;
  $[2] === Symbol.for("react.memo_cache_sentinel") ? (t22 = (0, import_jsx_runtime3.jsx)(DocumentHeaderTitle, {}), $[2] = t22) : t22 = $[2], t1 = t22;
  const title = t1;
  let t3, t4;
  $[3] !== showTabs ? (t4 = showTabs && (0, import_jsx_runtime3.jsx)(DocumentHeaderTabs, {}), $[3] = showTabs, $[4] = t4) : t4 = $[4], t3 = t4;
  const tabs = t3;
  let t5, t6;
  $[5] !== BackLink2 || $[6] !== showBackButton || $[7] !== t2 ? (t6 = showBackButton && (0, import_jsx_runtime3.jsx)(Button2, { as: BackLink2, "data-as": "a", icon: ArrowLeftIcon, mode: "bleed", tooltipProps: {
    content: t2("pane-header.back-button.text")
  } }), $[5] = BackLink2, $[6] = showBackButton, $[7] = t2, $[8] = t6) : t6 = $[8], t5 = t6;
  const backButton = t5, t7 = (displayed == null ? void 0 : displayed._type) ?? "";
  let t8;
  $[9] !== activeWorkspace.dataset || $[10] !== activeWorkspace.projectId ? (t8 = [activeWorkspace.projectId, activeWorkspace.dataset], $[9] = activeWorkspace.dataset, $[10] = activeWorkspace.projectId, $[11] = t8) : t8 = $[11];
  const t9 = t8.join("."), t10 = ((editState == null ? void 0 : editState.ready) && (editState.version ?? editState.draft ?? editState.published) !== null) ?? false;
  let t11;
  $[12] !== activeWorkspace.dataset || $[13] !== activeWorkspace.name || $[14] !== activeWorkspace.projectId || $[15] !== publishedDocId || $[16] !== t10 || $[17] !== t7 || $[18] !== t9 ? (t11 = (0, import_jsx_runtime3.jsx)(CapabilityGate, { capability: "comlink", condition: "available", children: (0, import_jsx_runtime3.jsx)(FavoriteToggle, { resourceType: "studio", documentId: publishedDocId, documentType: t7, resourceId: t9, schemaName: activeWorkspace.name, projectId: activeWorkspace.projectId, dataset: activeWorkspace.dataset, documentExists: t10 }) }), $[12] = activeWorkspace.dataset, $[13] = activeWorkspace.name, $[14] = activeWorkspace.projectId, $[15] = publishedDocId, $[16] = t10, $[17] = t7, $[18] = t9, $[19] = t11) : t11 = $[19];
  const favoriteToggle = t11, t12 = connectionState === "connecting" && !(editState == null ? void 0 : editState.draft) && !(editState == null ? void 0 : editState.published);
  let t13;
  return $[20] !== backButton || $[21] !== favoriteToggle || $[22] !== t12 || $[23] !== tabIndex || $[24] !== tabs ? (t13 = (0, import_jsx_runtime3.jsx)(PaneHeader$1, { loading: t12, title, tabs, tabIndex, backButton, appendTitle: favoriteToggle }), $[20] = backButton, $[21] = favoriteToggle, $[22] = t12, $[23] = tabIndex, $[24] = tabs, $[25] = t13) : t13 = $[25], t13;
});
var DocumentBox = dt(Box)({
  position: "relative"
});
var Scroller$4 = dt(ScrollContainer)(({
  $disabled
}) => $disabled ? {
  height: "100%"
} : lt`
    height: 100%;
    overflow: auto;
    position: relative;
    scroll-behavior: smooth;
    outline: none;
  `);
var DocumentPanel = function(props) {
  const $ = (0, import_react_compiler_runtime.c)(102), {
    footerHeight,
    headerHeight,
    isInspectOpen,
    rootElement,
    setDocumentPanelPortalElement,
    footer
  } = props, {
    activeViewId,
    displayed,
    documentId,
    editState,
    inspector,
    value,
    views: views2,
    ready,
    schemaType,
    permissions,
    isPermissionsLoading
  } = useDocumentPane();
  let t0;
  if ($[0] !== value) {
    const createLinkMetadata = getSanityCreateLinkMetadata(value);
    t0 = isSanityCreateLinked(createLinkMetadata), $[0] = value, $[1] = t0;
  } else
    t0 = $[1];
  const showCreateBanner = t0, {
    params
  } = usePaneRouter(), {
    collapsed: layoutCollapsed
  } = usePaneLayout(), {
    collapsed
  } = usePane(), parentPortal = usePortal(), {
    features
  } = useStructureTool(), [_portalElement, setPortalElement] = (0, import_react3.useState)(null), [documentScrollElement, setDocumentScrollElement] = (0, import_react3.useState)(null), formContainerElement = (0, import_react3.useRef)(null), workspace = useWorkspace(), requiredPermission = value._createdAt ? "update" : "create";
  let t1, t2;
  $[2] !== activeViewId || $[3] !== views2 ? (t2 = views2.find((view) => view.id === activeViewId) || views2[0] || {
    type: "form"
  }, $[2] = activeViewId, $[3] = views2, $[4] = t2) : t2 = $[4], t1 = t2;
  const activeView = t1, portalElement = features.splitPanes && _portalElement || parentPortal.element;
  let t3;
  bb0: {
    if (layoutCollapsed) {
      const t43 = headerHeight || 0, t52 = footerHeight ? footerHeight + 2 : 2;
      let t62;
      $[5] !== t43 || $[6] !== t52 ? (t62 = [t43, 0, t52, 0], $[5] = t43, $[6] = t52, $[7] = t62) : t62 = $[7], t3 = t62;
      break bb0;
    }
    let t42;
    $[8] === Symbol.for("react.memo_cache_sentinel") ? (t42 = [0, 0, 2, 0], $[8] = t42) : t42 = $[8], t3 = t42;
  }
  const margins = t3, formViewHidden = activeView.type !== "form";
  let t4;
  $[9] !== activeView.component || $[10] !== activeView.options || $[11] !== activeView.type || $[12] !== displayed || $[13] !== documentId || $[14] !== (editState == null ? void 0 : editState.draft) || $[15] !== (editState == null ? void 0 : editState.published) || $[16] !== schemaType || $[17] !== value ? (t4 = () => {
    if (activeView.type === "component" && activeView.component) {
      const ActiveViewComponent = activeView.component;
      return (0, import_jsx_runtime3.jsx)(ActiveViewComponent, { document: {
        draft: (editState == null ? void 0 : editState.draft) || null,
        displayed: displayed || value,
        historical: displayed,
        published: (editState == null ? void 0 : editState.published) || null
      }, documentId, options: activeView.options, schemaType });
    }
    return false;
  }, $[9] = activeView.component, $[10] = activeView.options, $[11] = activeView.type, $[12] = displayed, $[13] = documentId, $[14] = editState == null ? void 0 : editState.draft, $[15] = editState == null ? void 0 : editState.published, $[16] = schemaType, $[17] = value, $[18] = t4) : t4 = $[18];
  let t5;
  $[19] !== t4 ? (t5 = t4(), $[19] = t4, $[20] = t5) : t5 = $[20];
  const activeViewNode = t5;
  let t6;
  $[21] !== documentScrollElement ? (t6 = () => {
    (documentScrollElement == null ? void 0 : documentScrollElement.scrollTo) && documentScrollElement.scrollTo(0, 0);
  }, $[21] = documentScrollElement, $[22] = t6) : t6 = $[22];
  let t7;
  $[23] !== documentId || $[24] !== documentScrollElement ? (t7 = [documentId, documentScrollElement], $[23] = documentId, $[24] = documentScrollElement, $[25] = t7) : t7 = $[25], (0, import_react3.useEffect)(t6, t7);
  let t8, t9;
  $[26] !== portalElement || $[27] !== setDocumentPanelPortalElement ? (t8 = () => {
    portalElement && setDocumentPanelPortalElement(portalElement);
  }, t9 = [portalElement, setDocumentPanelPortalElement], $[26] = portalElement, $[27] = setDocumentPanelPortalElement, $[28] = t8, $[29] = t9) : (t8 = $[28], t9 = $[29]), (0, import_react3.useEffect)(t8, t9);
  let t10, t11;
  $[30] !== displayed || $[31] !== isInspectOpen || $[32] !== value ? (t11 = isInspectOpen ? (0, import_jsx_runtime3.jsx)(InspectDialog, { value: displayed || value }) : null, $[30] = displayed, $[31] = isInspectOpen, $[32] = value, $[33] = t11) : t11 = $[33], t10 = t11;
  const inspectDialog = t10, showInspector = !!(!collapsed && inspector), {
    selectedPerspective,
    selectedReleaseId
  } = usePerspective();
  let t12;
  bb1: {
    if (params == null ? void 0 : params.historyVersion) {
      let t133;
      $[34] === Symbol.for("react.memo_cache_sentinel") ? (t133 = (0, import_jsx_runtime3.jsx)(ArchivedReleaseDocumentBanner, {}), $[34] = t133) : t133 = $[34], t12 = t133;
      break bb1;
    }
    let t132;
    $[35] !== selectedPerspective ? (t132 = isReleaseDocument(selectedPerspective) && isReleaseScheduledOrScheduling(selectedPerspective), $[35] = selectedPerspective, $[36] = t132) : t132 = $[36];
    const isScheduledRelease = t132, documentInScheduledRelease = !!(isScheduledRelease && (displayed == null ? void 0 : displayed._id) && getVersionFromId(displayed == null ? void 0 : displayed._id) === selectedReleaseId);
    let isSelectedPerspectiveWriteable, t142;
    if ($[37] !== editState || $[38] !== schemaType || $[39] !== selectedPerspective || $[40] !== workspace.document.drafts.enabled ? (isSelectedPerspectiveWriteable = isPerspectiveWriteable({
      selectedPerspective,
      isDraftModelEnabled: workspace.document.drafts.enabled,
      schemaType
    }), t142 = mustChooseNewDocumentDestination({
      isSelectedPerspectiveWriteable,
      editState
    }), $[37] = editState, $[38] = schemaType, $[39] = selectedPerspective, $[40] = workspace.document.drafts.enabled, $[41] = isSelectedPerspectiveWriteable, $[42] = t142) : (isSelectedPerspectiveWriteable = $[41], t142 = $[42]), t142) {
      let t153;
      $[43] !== isSelectedPerspectiveWriteable.reason || $[44] !== isSelectedPerspectiveWriteable.result || $[45] !== schemaType || $[46] !== selectedPerspective ? (t153 = !isSelectedPerspectiveWriteable.result && (0, import_jsx_runtime3.jsx)(ChooseNewDocumentDestinationBanner, { schemaType, selectedPerspective, reason: isSelectedPerspectiveWriteable.reason }), $[43] = isSelectedPerspectiveWriteable.reason, $[44] = isSelectedPerspectiveWriteable.result, $[45] = schemaType, $[46] = selectedPerspective, $[47] = t153) : t153 = $[47], t12 = t153;
      break bb1;
    }
    if (documentInScheduledRelease) {
      const t153 = selectedPerspective;
      let t162;
      $[48] !== t153 ? (t162 = (0, import_jsx_runtime3.jsx)(ScheduledReleaseBanner, { currentRelease: t153 }), $[48] = t153, $[49] = t162) : t162 = $[49], t12 = t162;
      break bb1;
    }
    let t152;
    $[50] !== selectedPerspective ? (t152 = isSystemBundle(selectedPerspective), $[50] = selectedPerspective, $[51] = t152) : t152 = $[51];
    const isPinnedDraftOrPublish = t152;
    if ((displayed == null ? void 0 : displayed._id) && getVersionFromId(displayed._id) !== selectedReleaseId && ready && !isPinnedDraftOrPublish && isNewDocument(editState) === false) {
      const t162 = selectedPerspective, t172 = displayed || void 0;
      let t182;
      $[52] !== isScheduledRelease || $[53] !== t162 || $[54] !== t172 || $[55] !== value._id ? (t182 = (0, import_jsx_runtime3.jsx)(DocumentNotInReleaseBanner, { documentId: value._id, currentRelease: t162, value: t172, isScheduledRelease }), $[52] = isScheduledRelease, $[53] = t162, $[54] = t172, $[55] = value._id, $[56] = t182) : t182 = $[56], t12 = t182;
      break bb1;
    }
    const displayedHasObsoleteDraft = hasObsoleteDraft({
      editState,
      workspace,
      schemaType
    });
    if (activeView.type === "form" && !selectedReleaseId && displayedHasObsoleteDraft.result) {
      if (displayedHasObsoleteDraft.reason === "DRAFT_MODEL_INACTIVE") {
        let t162;
        $[57] !== displayed || $[58] !== documentId || $[59] !== schemaType ? (t162 = (0, import_jsx_runtime3.jsx)(ObsoleteDraftBanner, { displayed, documentId, schemaType, i18nKey: "banners.obsolete-draft.draft-model-inactive.text" }), $[57] = displayed, $[58] = documentId, $[59] = schemaType, $[60] = t162) : t162 = $[60], t12 = t162;
        break bb1;
      }
      if (displayedHasObsoleteDraft.reason === "LIVE_EDIT_ACTIVE") {
        let t162;
        $[61] !== displayed || $[62] !== documentId || $[63] !== schemaType ? (t162 = (0, import_jsx_runtime3.jsx)(ObsoleteDraftBanner, { displayed, documentId, schemaType, i18nKey: "banners.live-edit-draft-banner.text", isEditBlocking: true }), $[61] = displayed, $[62] = documentId, $[63] = schemaType, $[64] = t162) : t162 = $[64], t12 = t162;
        break bb1;
      }
    }
    if (activeView.type !== "form" || isPermissionsLoading) {
      t12 = null;
      break bb1;
    }
    let t16;
    $[65] !== showCreateBanner ? (t16 = showCreateBanner && (0, import_jsx_runtime3.jsx)(CreateLinkedBanner, {}), $[65] = showCreateBanner, $[66] = t16) : t16 = $[66];
    let t17;
    $[67] !== (permissions == null ? void 0 : permissions.granted) || $[68] !== requiredPermission ? (t17 = !(permissions == null ? void 0 : permissions.granted) && (0, import_jsx_runtime3.jsx)(InsufficientPermissionBanner, { requiredPermission }), $[67] = permissions == null ? void 0 : permissions.granted, $[68] = requiredPermission, $[69] = t17) : t17 = $[69];
    let t18, t19, t20, t21, t22, t23;
    $[70] === Symbol.for("react.memo_cache_sentinel") ? (t18 = (0, import_jsx_runtime3.jsx)(RevisionNotFoundBanner, {}), t19 = (0, import_jsx_runtime3.jsx)(ReferenceChangedBanner, {}), t20 = (0, import_jsx_runtime3.jsx)(DeprecatedDocumentTypeBanner, {}), t21 = (0, import_jsx_runtime3.jsx)(CanvasLinkedBanner, {}), t22 = (0, import_jsx_runtime3.jsx)(DeletedDocumentBanners, {}), t23 = (0, import_jsx_runtime3.jsx)(UnpublishedDocumentBanner, {}), $[70] = t18, $[71] = t19, $[72] = t20, $[73] = t21, $[74] = t22, $[75] = t23) : (t18 = $[70], t19 = $[71], t20 = $[72], t21 = $[73], t22 = $[74], t23 = $[75]);
    const t24 = (displayed == null ? void 0 : displayed._id) ?? documentId;
    let t25;
    $[76] !== isPinnedDraftOrPublish || $[77] !== t24 ? (t25 = (0, import_jsx_runtime3.jsx)(OpenReleaseToEditBanner, { documentId: t24, isPinnedDraftOrPublished: isPinnedDraftOrPublish }), $[76] = isPinnedDraftOrPublish, $[77] = t24, $[78] = t25) : t25 = $[78];
    let t26;
    $[79] !== t16 || $[80] !== t17 || $[81] !== t25 ? (t26 = (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [
      t16,
      t17,
      t18,
      t19,
      t20,
      t21,
      t22,
      t23,
      t25
    ] }), $[79] = t16, $[80] = t17, $[81] = t25, $[82] = t26) : t26 = $[82], t12 = t26;
  }
  const banners = t12, showFormView = features.resizablePanes || !showInspector;
  let t13;
  $[83] !== activeViewNode || $[84] !== banners || $[85] !== documentScrollElement || $[86] !== footer || $[87] !== formViewHidden || $[88] !== inspectDialog || $[89] !== layoutCollapsed || $[90] !== margins || $[91] !== portalElement || $[92] !== showFormView ? (t13 = showFormView && (0, import_jsx_runtime3.jsxs)(Flex, { height: "fill", direction: "column", width: "fill", flex: 2, children: [
    (0, import_jsx_runtime3.jsxs)(LegacyLayerProvider, { zOffset: "paneHeader", children: [
      banners,
      (0, import_jsx_runtime3.jsx)(DocumentPanelSubHeader, {})
    ] }),
    (0, import_jsx_runtime3.jsx)(DocumentBox, { flex: 2, overflow: "hidden", children: (0, import_jsx_runtime3.jsx)(PortalProvider, { element: portalElement, __unstable_elements: {
      documentScrollElement
    }, children: (0, import_jsx_runtime3.jsx)(BoundaryElementProvider, { element: documentScrollElement, children: (0, import_jsx_runtime3.jsxs)(VirtualizerScrollInstanceProvider, { scrollElement: documentScrollElement, containerElement: formContainerElement, children: [
      (0, import_jsx_runtime3.jsxs)(Scroller$4, { $disabled: layoutCollapsed || false, "data-testid": "document-panel-scroller", ref: setDocumentScrollElement, children: [
        (0, import_jsx_runtime3.jsx)(FormView, { hidden: formViewHidden, margins, ref: formContainerElement }),
        activeViewNode
      ] }),
      inspectDialog,
      (0, import_jsx_runtime3.jsx)("div", { "data-testid": "document-panel-portal", ref: setPortalElement })
    ] }) }) }) }),
    footer
  ] }), $[83] = activeViewNode, $[84] = banners, $[85] = documentScrollElement, $[86] = footer, $[87] = formViewHidden, $[88] = inspectDialog, $[89] = layoutCollapsed, $[90] = margins, $[91] = portalElement, $[92] = showFormView, $[93] = t13) : t13 = $[93];
  let t14;
  $[94] !== documentId || $[95] !== rootElement || $[96] !== schemaType || $[97] !== showInspector ? (t14 = showInspector && (0, import_jsx_runtime3.jsx)(BoundaryElementProvider, { element: rootElement, children: (0, import_jsx_runtime3.jsx)(DocumentInspectorPanel, { documentId, documentType: schemaType.name, flex: 1 }) }), $[94] = documentId, $[95] = rootElement, $[96] = schemaType, $[97] = showInspector, $[98] = t14) : t14 = $[98];
  let t15;
  return $[99] !== t13 || $[100] !== t14 ? (t15 = (0, import_jsx_runtime3.jsx)(PaneContent, { children: (0, import_jsx_runtime3.jsxs)(Flex, { height: "fill", children: [
    t13,
    t14
  ] }) }), $[99] = t13, $[100] = t14, $[101] = t15) : t15 = $[101], t15;
};
DocumentPanel.displayName = "DocumentPanel";
var _DEBUG = false;
var EMPTY_PARAMS$1 = Object.freeze({});
var LOADING_PANE = Symbol("LOADING_PANE");
var DOCUMENT_PANEL_PORTAL_ELEMENT = "documentPanelPortalElement";
var POPOVER_FALLBACK_PLACEMENTS = ["left", "bottom"];
var DIALOG_WIDTH_TO_UI_WIDTH = {
  small: 0,
  medium: 1,
  large: 2,
  full: "auto"
};
function ConfirmDialog(props) {
  const $ = (0, import_react_compiler_runtime.c)(5), {
    dialog,
    referenceElement
  } = props;
  let t0;
  $[0] !== dialog ? (t0 = (0, import_jsx_runtime3.jsx)(ConfirmDialogContent, { dialog }), $[0] = dialog, $[1] = t0) : t0 = $[1];
  let t1;
  return $[2] !== referenceElement || $[3] !== t0 ? (t1 = (0, import_jsx_runtime3.jsx)(Popover, { content: t0, fallbackPlacements: POPOVER_FALLBACK_PLACEMENTS, open: true, placement: "top", portal: true, preventOverflow: true, referenceElement }), $[2] = referenceElement, $[3] = t0, $[4] = t1) : t1 = $[4], t1;
}
function ConfirmDialogContent(props) {
  const $ = (0, import_react_compiler_runtime.c)(29), {
    dialog
  } = props, {
    cancelButtonIcon,
    cancelButtonText,
    confirmButtonIcon,
    confirmButtonText,
    message,
    onCancel,
    onConfirm,
    tone
  } = dialog, {
    t: t2
  } = useTranslation(structureLocaleNamespace), {
    isTopLayer
  } = useLayer(), ref = (0, import_react3.useRef)(null);
  let t0;
  $[0] !== isTopLayer || $[1] !== onCancel ? (t0 = (event) => {
    event.key === "Escape" && isTopLayer && onCancel();
  }, $[0] = isTopLayer, $[1] = onCancel, $[2] = t0) : t0 = $[2], useGlobalKeyDown(t0);
  let t1;
  $[3] === Symbol.for("react.memo_cache_sentinel") ? (t1 = () => [ref.current], $[3] = t1) : t1 = $[3], useClickOutsideEvent(isTopLayer && onCancel, t1);
  let t22;
  $[4] === Symbol.for("react.memo_cache_sentinel") ? (t22 = {
    minWidth: 304,
    maxWidth: 400
  }, $[4] = t22) : t22 = $[4];
  let t3;
  $[5] !== message ? (t3 = (0, import_jsx_runtime3.jsx)(Box, { flex: 1, overflow: "auto", padding: 4, children: (0, import_jsx_runtime3.jsx)(Text, { children: message }) }), $[5] = message, $[6] = t3) : t3 = $[6];
  let t4;
  $[7] === Symbol.for("react.memo_cache_sentinel") ? (t4 = {
    borderTop: "1px solid var(--card-border-color)"
  }, $[7] = t4) : t4 = $[7];
  let t5;
  $[8] !== cancelButtonText || $[9] !== t2 ? (t5 = cancelButtonText || t2("confirm-dialog.cancel-button.fallback-text"), $[8] = cancelButtonText, $[9] = t2, $[10] = t5) : t5 = $[10];
  let t6;
  $[11] !== cancelButtonIcon || $[12] !== onCancel || $[13] !== t5 ? (t6 = (0, import_jsx_runtime3.jsx)(Button, { "data-testid": "confirm-dialog-cancel-button", icon: cancelButtonIcon, onClick: onCancel, mode: "ghost", text: t5 }), $[11] = cancelButtonIcon, $[12] = onCancel, $[13] = t5, $[14] = t6) : t6 = $[14];
  let t7;
  $[15] !== confirmButtonText || $[16] !== t2 ? (t7 = confirmButtonText || t2("confirm-dialog.confirm-button.fallback-text"), $[15] = confirmButtonText, $[16] = t2, $[17] = t7) : t7 = $[17];
  let t8;
  $[18] !== confirmButtonIcon || $[19] !== onConfirm || $[20] !== t7 || $[21] !== tone ? (t8 = (0, import_jsx_runtime3.jsx)(Button, { "data-testid": "confirm-dialog-confirm-button", icon: confirmButtonIcon, onClick: onConfirm, text: t7, tone }), $[18] = confirmButtonIcon, $[19] = onConfirm, $[20] = t7, $[21] = tone, $[22] = t8) : t8 = $[22];
  let t9;
  $[23] !== t6 || $[24] !== t8 ? (t9 = (0, import_jsx_runtime3.jsx)(Box, { paddingX: 4, paddingY: 3, style: t4, children: (0, import_jsx_runtime3.jsxs)(Grid, { columns: 2, gap: 2, children: [
    t6,
    t8
  ] }) }), $[23] = t6, $[24] = t8, $[25] = t9) : t9 = $[25];
  let t10;
  return $[26] !== t3 || $[27] !== t9 ? (t10 = (0, import_jsx_runtime3.jsxs)(Flex, { direction: "column", ref, style: t22, children: [
    t3,
    t9
  ] }), $[26] = t3, $[27] = t9, $[28] = t10) : t10 = $[28], t10;
}
function ModalDialog(props) {
  const $ = (0, import_react_compiler_runtime.c)(12), {
    dialog
  } = props, dialogId = (0, import_react3.useId)();
  let t0;
  $[0] !== dialog.footer ? (t0 = dialog.footer && (0, import_jsx_runtime3.jsx)(Box, { paddingX: 4, paddingY: 3, children: dialog.footer }), $[0] = dialog.footer, $[1] = t0) : t0 = $[1];
  const footer = t0, t1 = dialog.showCloseButton === false, t2 = dialog.width === void 0 ? 1 : DIALOG_WIDTH_TO_UI_WIDTH[dialog.width];
  let t3;
  $[2] !== dialog.content ? (t3 = (0, import_jsx_runtime3.jsx)(Box, { padding: 4, children: dialog.content }), $[2] = dialog.content, $[3] = t3) : t3 = $[3];
  let t4;
  return $[4] !== dialog.header || $[5] !== dialog.onClose || $[6] !== dialogId || $[7] !== footer || $[8] !== t1 || $[9] !== t2 || $[10] !== t3 ? (t4 = (0, import_jsx_runtime3.jsx)(LegacyLayerProvider, { zOffset: "fullscreen", children: (0, import_jsx_runtime3.jsx)(Dialog, { animate: true, __unstable_hideCloseButton: t1, footer, header: dialog.header, id: dialogId, onClose: dialog.onClose, onClickOutside: dialog.onClose, width: t2, children: t3 }) }), $[4] = dialog.header, $[5] = dialog.onClose, $[6] = dialogId, $[7] = footer, $[8] = t1, $[9] = t2, $[10] = t3, $[11] = t4) : t4 = $[11], t4;
}
function PopoverDialog(props) {
  const $ = (0, import_react_compiler_runtime.c)(5), {
    dialog,
    referenceElement
  } = props;
  let t0;
  $[0] !== dialog ? (t0 = (0, import_jsx_runtime3.jsx)(PopoverDialogContent, { dialog }), $[0] = dialog, $[1] = t0) : t0 = $[1];
  let t1;
  return $[2] !== referenceElement || $[3] !== t0 ? (t1 = (0, import_jsx_runtime3.jsx)(Popover, { content: t0, fallbackPlacements: POPOVER_FALLBACK_PLACEMENTS, open: true, placement: "top", portal: true, preventOverflow: true, referenceElement }), $[2] = referenceElement, $[3] = t0, $[4] = t1) : t1 = $[4], t1;
}
function PopoverDialogContent(props) {
  const $ = (0, import_react_compiler_runtime.c)(6), {
    dialog
  } = props, {
    content,
    onClose
  } = dialog, {
    isTopLayer
  } = useLayer(), ref = (0, import_react3.useRef)(null);
  let t0;
  $[0] !== isTopLayer || $[1] !== onClose ? (t0 = (event) => {
    event.key === "Escape" && isTopLayer && onClose();
  }, $[0] = isTopLayer, $[1] = onClose, $[2] = t0) : t0 = $[2], useGlobalKeyDown(t0);
  let t1;
  $[3] === Symbol.for("react.memo_cache_sentinel") ? (t1 = () => [ref.current], $[3] = t1) : t1 = $[3], useClickOutsideEvent(isTopLayer && onClose, t1);
  let t2;
  return $[4] !== content ? (t2 = (0, import_jsx_runtime3.jsx)("div", { ref, children: content }), $[4] = content, $[5] = t2) : t2 = $[5], t2;
}
function DocumentActionPortalProvider(props) {
  const $ = (0, import_react_compiler_runtime.c)(3), {
    children
  } = props, {
    element,
    elements
  } = usePortal(), portalElement = (elements == null ? void 0 : elements[DOCUMENT_PANEL_PORTAL_ELEMENT]) || element;
  let t0;
  return $[0] !== children || $[1] !== portalElement ? (t0 = (0, import_jsx_runtime3.jsx)(PortalProvider, { element: portalElement, children }), $[0] = children, $[1] = portalElement, $[2] = t0) : t0 = $[2], t0;
}
function ActionStateDialog(props) {
  const $ = (0, import_react_compiler_runtime.c)(17), {
    dialog,
    referenceElement: t0
  } = props, referenceElement = t0 === void 0 ? null : t0, modalId = (0, import_react3.useId)();
  if (dialog.type === "confirm") {
    let t12;
    return $[0] !== dialog || $[1] !== referenceElement ? (t12 = (0, import_jsx_runtime3.jsx)(ConfirmDialog, { dialog, referenceElement }), $[0] = dialog, $[1] = referenceElement, $[2] = t12) : t12 = $[2], t12;
  }
  if (dialog.type === "popover") {
    let t12;
    return $[3] !== dialog || $[4] !== referenceElement ? (t12 = (0, import_jsx_runtime3.jsx)(PopoverDialog, { dialog, referenceElement }), $[3] = dialog, $[4] = referenceElement, $[5] = t12) : t12 = $[5], t12;
  }
  if (dialog.type === "dialog" || !dialog.type) {
    let t12;
    return $[6] !== dialog ? (t12 = (0, import_jsx_runtime3.jsx)(DocumentActionPortalProvider, { children: (0, import_jsx_runtime3.jsx)(ModalDialog, { dialog }) }), $[6] = dialog, $[7] = t12) : t12 = $[7], t12;
  }
  if (dialog.type === "custom") {
    const t12 = dialog == null ? void 0 : dialog.component;
    let t22;
    return $[8] !== t12 ? (t22 = (0, import_jsx_runtime3.jsx)(DocumentActionPortalProvider, { children: t12 }), $[8] = t12, $[9] = t22) : t22 = $[9], t22;
  }
  const unknownModal = dialog;
  console.warn(`Unsupported modal type ${unknownModal.type}`);
  let t1;
  $[10] !== unknownModal.content || $[11] !== unknownModal.type ? (t1 = unknownModal.content || (0, import_jsx_runtime3.jsxs)(Text, { size: 1, children: [
    "Unexpected modal type (",
    (0, import_jsx_runtime3.jsx)("code", { children: unknownModal.type }),
    ")"
  ] }), $[10] = unknownModal.content, $[11] = unknownModal.type, $[12] = t1) : t1 = $[12];
  let t2;
  return $[13] !== modalId || $[14] !== t1 || $[15] !== unknownModal.onClose ? (t2 = (0, import_jsx_runtime3.jsx)(Dialog2, { id: modalId, onClose: unknownModal.onClose, onClickOutside: unknownModal.onClose, width: 1, children: t1 }), $[13] = modalId, $[14] = t1, $[15] = unknownModal.onClose, $[16] = t2) : t2 = $[16], t2;
}
var ActionDialogWrapper = (0, import_react3.memo)(function(t0) {
  const $ = (0, import_react_compiler_runtime.c)(9), {
    actionStates,
    children,
    referenceElement
  } = t0, [actionIndex, setActionIndex] = (0, import_react3.useState)(-1);
  let t1;
  t1 = actionStates[actionIndex];
  const currentAction = t1;
  let t2;
  $[0] === Symbol.for("react.memo_cache_sentinel") ? (t2 = (idx) => {
    setActionIndex(idx);
  }, $[0] = t2) : t2 = $[0];
  const handleAction = t2;
  let t3, t4;
  $[1] !== children ? (t4 = children({
    handleAction
  }), $[1] = children, $[2] = t4) : t4 = $[2], t3 = t4;
  const result = t3;
  let t5;
  $[3] !== currentAction || $[4] !== referenceElement ? (t5 = currentAction && currentAction.dialog && (0, import_jsx_runtime3.jsx)(LegacyLayerProvider, { zOffset: "paneFooter", children: (0, import_jsx_runtime3.jsx)(ActionStateDialog, { dialog: currentAction.dialog, referenceElement }) }), $[3] = currentAction, $[4] = referenceElement, $[5] = t5) : t5 = $[5];
  let t6;
  return $[6] !== result || $[7] !== t5 ? (t6 = (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [
    t5,
    result
  ] }), $[6] = result, $[7] = t5, $[8] = t6) : t6 = $[8], t6;
});
function ActionMenuButton(props) {
  const $ = (0, import_react_compiler_runtime.c)(10), {
    actionStates,
    disabled
  } = props, idPrefix = (0, import_react3.useId)(), [referenceElement, setReferenceElement] = (0, import_react3.useState)(null);
  let t0, t1;
  $[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = {
    placement: "top-end",
    portal: true,
    preventOverflow: true
  }, $[0] = t1) : t1 = $[0], t0 = t1;
  const popoverProps = t0, {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  let t22;
  $[1] !== actionStates || $[2] !== disabled || $[3] !== idPrefix || $[4] !== t2 ? (t22 = (t32) => {
    const {
      handleAction
    } = t32;
    return (0, import_jsx_runtime3.jsx)(MenuButton, { id: `${idPrefix}-action-menu`, button: (0, import_jsx_runtime3.jsx)(ContextMenuButton, { "aria-label": t2("buttons.action-menu-button.aria-label"), disabled, "data-testid": "action-menu-button", tooltipProps: {
      content: t2("buttons.action-menu-button.tooltip")
    } }), menu: (0, import_jsx_runtime3.jsx)(Menu, { padding: 1, children: actionStates.map((actionState, idx) => (0, import_jsx_runtime3.jsx)(ActionMenuListItem, { actionState, disabled, index: idx, onAction: handleAction }, idx)) }), popover: popoverProps, ref: setReferenceElement });
  }, $[1] = actionStates, $[2] = disabled, $[3] = idPrefix, $[4] = t2, $[5] = t22) : t22 = $[5];
  const renderActionDialog = t22;
  let t3;
  return $[6] !== actionStates || $[7] !== referenceElement || $[8] !== renderActionDialog ? (t3 = (0, import_jsx_runtime3.jsx)(ActionDialogWrapper, { actionStates, referenceElement, children: renderActionDialog }), $[6] = actionStates, $[7] = referenceElement, $[8] = renderActionDialog, $[9] = t3) : t3 = $[9], t3;
}
function ActionMenuListItem(props) {
  const $ = (0, import_react_compiler_runtime.c)(18), {
    actionState,
    disabled,
    index,
    onAction
  } = props, {
    onHandle
  } = actionState;
  let t0;
  $[0] !== index || $[1] !== onAction || $[2] !== onHandle ? (t0 = () => {
    onAction(index), onHandle && onHandle();
  }, $[0] = index, $[1] = onAction, $[2] = onHandle, $[3] = t0) : t0 = $[3];
  const handleClick = t0;
  let t1, t2;
  $[4] !== actionState.shortcut ? (t2 = actionState.shortcut ? String(actionState.shortcut).split("+").map(_temp$d) : void 0, $[4] = actionState.shortcut, $[5] = t2) : t2 = $[5], t1 = t2;
  const hotkeys = t1, t3 = `action-${actionState.label.replace(" ", "")}`, t4 = disabled || !!actionState.disabled;
  let t5;
  $[6] !== actionState.disabled || $[7] !== actionState.title ? (t5 = actionState.disabled && {
    tooltipProps: {
      content: actionState.title
    }
  }, $[6] = actionState.disabled, $[7] = actionState.title, $[8] = t5) : t5 = $[8];
  let t6;
  return $[9] !== actionState.icon || $[10] !== actionState.label || $[11] !== actionState.tone || $[12] !== handleClick || $[13] !== hotkeys || $[14] !== t3 || $[15] !== t4 || $[16] !== t5 ? (t6 = (0, import_jsx_runtime3.jsx)(MenuItem2, { "data-testid": t3, disabled: t4, hotkeys, icon: actionState.icon, onClick: handleClick, text: actionState.label, tone: actionState.tone, ...t5 }), $[9] = actionState.icon, $[10] = actionState.label, $[11] = actionState.tone, $[12] = handleClick, $[13] = hotkeys, $[14] = t3, $[15] = t4, $[16] = t5, $[17] = t6) : t6 = $[17], t6;
}
function _temp$d(s2) {
  return s2.slice(0, 1).toUpperCase() + s2.slice(1);
}
var DISABLED_REASON_TITLE_KEY$1 = {
  NOTHING_TO_DELETE: "action.delete.disabled.nothing-to-delete",
  NOT_READY: "action.delete.disabled.not-ready"
};
var DeleteAction = (t0) => {
  const $ = (0, import_react_compiler_runtime.c)(36), {
    id: id2,
    type: type2,
    draft,
    onComplete,
    release
  } = t0, {
    setIsDeleting: paneSetIsDeleting
  } = useDocumentPane(), {
    delete: deleteOp
  } = useDocumentOperation(id2, type2, release), [isDeleting, setIsDeleting] = (0, import_react3.useState)(false), [isConfirmDialogOpen, setConfirmDialogOpen] = (0, import_react3.useState)(false), {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  let t1;
  $[0] !== onComplete ? (t1 = () => {
    setConfirmDialogOpen(false), onComplete();
  }, $[0] = onComplete, $[1] = t1) : t1 = $[1];
  const handleCancel = t1;
  let t22;
  $[2] !== deleteOp || $[3] !== onComplete || $[4] !== paneSetIsDeleting ? (t22 = () => {
    setIsDeleting(true), setConfirmDialogOpen(false), paneSetIsDeleting(true), deleteOp.execute(), onComplete();
  }, $[2] = deleteOp, $[3] = onComplete, $[4] = paneSetIsDeleting, $[5] = t22) : t22 = $[5];
  const handleConfirm = t22;
  let t3;
  $[6] === Symbol.for("react.memo_cache_sentinel") ? (t3 = () => {
    setConfirmDialogOpen(true);
  }, $[6] = t3) : t3 = $[6];
  const handle = t3;
  let t4;
  $[7] !== id2 || $[8] !== release || $[9] !== type2 ? (t4 = {
    id: id2,
    type: type2,
    version: release,
    permission: "delete"
  }, $[7] = id2, $[8] = release, $[9] = type2, $[10] = t4) : t4 = $[10];
  const [permissions, isPermissionsLoading] = useDocumentPairPermissions(t4), currentUser = useCurrentUser();
  let t5;
  bb0: {
    if (!isPermissionsLoading && !(permissions == null ? void 0 : permissions.granted)) {
      let t62;
      $[11] !== t2 ? (t62 = t2("action.delete.label"), $[11] = t2, $[12] = t62) : t62 = $[12];
      let t72;
      $[13] !== currentUser ? (t72 = (0, import_jsx_runtime3.jsx)(InsufficientPermissionsMessage, { context: "delete-document", currentUser }), $[13] = currentUser, $[14] = t72) : t72 = $[14];
      let t82;
      $[15] !== t62 || $[16] !== t72 ? (t82 = {
        tone: "critical",
        icon: TrashIcon,
        disabled: true,
        label: t62,
        title: t72
      }, $[15] = t62, $[16] = t72, $[17] = t82) : t82 = $[17], t5 = t82;
      break bb0;
    }
    const t6 = isDeleting || !!deleteOp.disabled || isPermissionsLoading;
    let t7;
    $[18] !== deleteOp.disabled || $[19] !== t2 ? (t7 = deleteOp.disabled && t2(DISABLED_REASON_TITLE_KEY$1[deleteOp.disabled]) || "", $[18] = deleteOp.disabled, $[19] = t2, $[20] = t7) : t7 = $[20];
    let t8;
    $[21] !== isDeleting || $[22] !== t2 ? (t8 = t2(isDeleting ? "action.delete.running.label" : "action.delete.label"), $[21] = isDeleting, $[22] = t2, $[23] = t8) : t8 = $[23];
    let t9;
    $[24] !== (draft == null ? void 0 : draft._id) || $[25] !== handleCancel || $[26] !== handleConfirm || $[27] !== id2 || $[28] !== isConfirmDialogOpen || $[29] !== type2 ? (t9 = isConfirmDialogOpen && {
      type: "custom",
      component: (0, import_jsx_runtime3.jsx)(ConfirmDeleteDialogContainer, { action: "delete", id: (draft == null ? void 0 : draft._id) || id2, type: type2, onCancel: handleCancel, onConfirm: handleConfirm })
    }, $[24] = draft == null ? void 0 : draft._id, $[25] = handleCancel, $[26] = handleConfirm, $[27] = id2, $[28] = isConfirmDialogOpen, $[29] = type2, $[30] = t9) : t9 = $[30];
    let t10;
    $[31] !== t6 || $[32] !== t7 || $[33] !== t8 || $[34] !== t9 ? (t10 = {
      tone: "critical",
      icon: TrashIcon,
      disabled: t6,
      title: t7,
      label: t8,
      shortcut: "Ctrl+Alt+D",
      onHandle: handle,
      dialog: t9
    }, $[31] = t6, $[32] = t7, $[33] = t8, $[34] = t9, $[35] = t10) : t10 = $[35], t5 = t10;
  }
  return t5;
};
DeleteAction.action = "delete";
DeleteAction.displayName = "DeleteAction";
var DISABLED_REASON_KEY$2 = {
  NO_CHANGES: "action.discard-changes.disabled.no-change",
  NOT_PUBLISHED: "action.discard-changes.disabled.not-published",
  NOT_READY: "action.discard-changes.disabled.not-ready"
};
var DiscardChangesAction = ({
  id: id2,
  type: type2,
  published,
  liveEdit,
  onComplete,
  release
}) => {
  const {
    discardChanges
  } = useDocumentOperation(id2, type2, release), [isConfirmDialogOpen, setConfirmDialogOpen] = (0, import_react3.useState)(false), [permissions, isPermissionsLoading] = useDocumentPairPermissions({
    id: id2,
    type: type2,
    version: release,
    permission: "discardDraft"
  }), currentUser = useCurrentUser(), {
    displayed
  } = useDocumentPane(), {
    t: t2
  } = useTranslation(structureLocaleNamespace), isPublished = (displayed == null ? void 0 : displayed._id) && isPublishedId(displayed == null ? void 0 : displayed._id), handleConfirm = (0, import_react3.useCallback)(() => {
    discardChanges.execute(), onComplete();
  }, [discardChanges, onComplete]), handle = (0, import_react3.useCallback)(() => {
    setConfirmDialogOpen(true);
  }, []), dialog = (0, import_react3.useMemo)(() => isConfirmDialogOpen && {
    type: "confirm",
    tone: "critical",
    onCancel: onComplete,
    onConfirm: handleConfirm,
    message: t2("action.discard-changes.confirm-dialog.confirm-discard-changes")
  }, [handleConfirm, isConfirmDialogOpen, onComplete, t2]);
  return (0, import_react3.useMemo)(() => !published || liveEdit || isPublished ? null : !isPermissionsLoading && !(permissions == null ? void 0 : permissions.granted) ? {
    tone: "critical",
    icon: ResetIcon,
    disabled: true,
    label: t2("action.discard-changes.label"),
    title: (0, import_jsx_runtime3.jsx)(InsufficientPermissionsMessage, { context: "discard-changes", currentUser })
  } : {
    tone: "critical",
    icon: ResetIcon,
    disabled: !!discardChanges.disabled || isPermissionsLoading,
    title: t2(discardChanges.disabled && DISABLED_REASON_KEY$2[discardChanges.disabled] || ""),
    label: t2("action.discard-changes.label"),
    onHandle: handle,
    dialog
  }, [currentUser, dialog, discardChanges.disabled, handle, isPermissionsLoading, isPublished, liveEdit, permissions == null ? void 0 : permissions.granted, published, t2]);
};
DiscardChangesAction.action = "discardChanges";
DiscardChangesAction.displayName = "DiscardChangesAction";
var DISABLED_REASON_KEY$1 = {
  NOTHING_TO_DUPLICATE: "action.duplicate.disabled.nothing-to-duplicate",
  NOT_READY: "action.duplicate.disabled.not-ready"
};
var DuplicateAction = ({
  id: id2,
  type: type2,
  onComplete,
  release,
  mapDocument
}) => {
  const documentStore = useDocumentStore(), {
    duplicate
  } = useDocumentOperation(id2, type2, release), {
    navigateIntent
  } = useRouter(), [isDuplicating, setDuplicating] = (0, import_react3.useState)(false), [permissions, isPermissionsLoading] = useDocumentPairPermissions({
    id: id2,
    type: type2,
    version: release,
    permission: "duplicate"
  }), {
    t: t2
  } = useTranslation(structureLocaleNamespace), currentUser = useCurrentUser(), handle = (0, import_react3.useCallback)(async () => {
    const dupeId = v4_default();
    setDuplicating(true);
    const duplicateSuccess = firstValueFrom(documentStore.pair.operationEvents(id2, type2).pipe(filter((e2) => e2.op === "duplicate" && e2.type === "success")));
    duplicate.execute(dupeId, {
      mapDocument
    }), await duplicateSuccess, navigateIntent("edit", {
      id: dupeId,
      type: type2
    }), onComplete();
  }, [documentStore.pair, duplicate, id2, mapDocument, navigateIntent, onComplete, type2]);
  return (0, import_react3.useMemo)(() => !isPermissionsLoading && !(permissions == null ? void 0 : permissions.granted) ? {
    icon: CopyIcon,
    disabled: true,
    label: t2("action.duplicate.label"),
    title: (0, import_jsx_runtime3.jsx)(InsufficientPermissionsMessage, { context: "duplicate-document", currentUser })
  } : {
    icon: CopyIcon,
    disabled: isDuplicating || !!duplicate.disabled || isPermissionsLoading,
    label: t2(isDuplicating ? "action.duplicate.running.label" : "action.duplicate.label"),
    title: duplicate.disabled ? t2(DISABLED_REASON_KEY$1[duplicate.disabled]) : "",
    onHandle: handle
  }, [currentUser, duplicate.disabled, handle, isDuplicating, isPermissionsLoading, permissions == null ? void 0 : permissions.granted, t2]);
};
DuplicateAction.action = "duplicate";
DuplicateAction.displayName = "DuplicateAction";
var HistoryRestoreAction = (t0) => {
  const $ = (0, import_react_compiler_runtime.c)(27), {
    id: id2,
    type: type2,
    revision,
    onComplete,
    release
  } = t0, {
    restore
  } = useDocumentOperation(id2, type2, release), {
    revisionNotFound
  } = useDocumentPane(), event = useDocumentOperationEvent(id2, type2), {
    navigateIntent
  } = useRouter(), prevEvent = (0, import_react3.useRef)(event), [isConfirmDialogOpen, setConfirmDialogOpen] = (0, import_react3.useState)(false), {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  let t1;
  $[0] !== onComplete || $[1] !== restore || $[2] !== revision ? (t1 = () => {
    restore.execute(revision), onComplete();
  }, $[0] = onComplete, $[1] = restore, $[2] = revision, $[3] = t1) : t1 = $[3];
  const handleConfirm = t1;
  let t22, t3;
  $[4] !== event || $[5] !== id2 || $[6] !== navigateIntent || $[7] !== type2 ? (t22 = () => {
    !event || event === prevEvent.current || (event.type === "success" && event.op === "restore" && navigateIntent("edit", {
      id: id2,
      type: type2
    }), prevEvent.current = event);
  }, t3 = [event, id2, navigateIntent, type2], $[4] = event, $[5] = id2, $[6] = navigateIntent, $[7] = type2, $[8] = t22, $[9] = t3) : (t22 = $[8], t3 = $[9]), (0, import_react3.useEffect)(t22, t3);
  let t4;
  $[10] === Symbol.for("react.memo_cache_sentinel") ? (t4 = () => {
    setConfirmDialogOpen(true);
  }, $[10] = t4) : t4 = $[10];
  const handle = t4;
  let t5;
  bb0: {
    if (isConfirmDialogOpen) {
      let t62;
      $[11] !== t2 ? (t62 = t2("action.restore.confirm.message"), $[11] = t2, $[12] = t62) : t62 = $[12];
      let t7;
      $[13] !== handleConfirm || $[14] !== onComplete || $[15] !== t62 ? (t7 = {
        type: "confirm",
        tone: "critical",
        onCancel: onComplete,
        onConfirm: handleConfirm,
        message: t62
      }, $[13] = handleConfirm, $[14] = onComplete, $[15] = t62, $[16] = t7) : t7 = $[16], t5 = t7;
      break bb0;
    }
    t5 = null;
  }
  const dialog = t5, isRevisionInitial = revision === "@initial", isRevisionLatest = revision === void 0;
  let t6;
  bb1: {
    if (isRevisionLatest || revisionNotFound) {
      t6 = null;
      break bb1;
    }
    let t7;
    $[17] !== t2 ? (t7 = t2("action.restore.label"), $[17] = t2, $[18] = t7) : t7 = $[18];
    const t8 = isRevisionInitial ? "action.restore.disabled.cannot-restore-initial" : "action.restore.tooltip";
    let t9;
    $[19] !== t2 || $[20] !== t8 ? (t9 = t2(t8), $[19] = t2, $[20] = t8, $[21] = t9) : t9 = $[21];
    let t10;
    $[22] !== dialog || $[23] !== isRevisionInitial || $[24] !== t7 || $[25] !== t9 ? (t10 = {
      label: t7,
      tone: "caution",
      onHandle: handle,
      title: t9,
      icon: RevertIcon,
      dialog,
      disabled: isRevisionInitial
    }, $[22] = dialog, $[23] = isRevisionInitial, $[24] = t7, $[25] = t9, $[26] = t10) : t10 = $[26], t6 = t10;
  }
  return t6;
};
HistoryRestoreAction.action = "restore";
HistoryRestoreAction.displayName = "HistoryRestoreAction";
var DocumentPublished = defineEvent({
  name: "Document Published",
  version: 1,
  description: 'User clicked the "Publish" button in the document pane'
});
var DISABLED_REASON_TITLE_KEY = {
  LIVE_EDIT_ENABLED: "action.publish.live-edit.publish-disabled",
  ALREADY_PUBLISHED: "action.publish.already-published.no-time-ago.tooltip",
  NO_CHANGES: "action.publish.no-changes.tooltip",
  NOT_READY: "action.publish.disabled.not-ready"
};
function getDisabledReason(reason, publishedAt, t2) {
  return reason === "ALREADY_PUBLISHED" && publishedAt ? (0, import_jsx_runtime3.jsx)(AlreadyPublished, { publishedAt }) : t2(DISABLED_REASON_TITLE_KEY[reason]);
}
function AlreadyPublished(t0) {
  const $ = (0, import_react_compiler_runtime.c)(6), {
    publishedAt
  } = t0, {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  let t1;
  $[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = {
    useTemporalPhrase: true
  }, $[0] = t1) : t1 = $[0];
  const timeSincePublished = useRelativeTime(publishedAt, t1);
  let t22;
  $[1] !== t2 || $[2] !== timeSincePublished ? (t22 = t2("action.publish.already-published.tooltip", {
    timeSincePublished
  }), $[1] = t2, $[2] = timeSincePublished, $[3] = t22) : t22 = $[3];
  let t3;
  return $[4] !== t22 ? (t3 = (0, import_jsx_runtime3.jsx)("span", { children: t22 }), $[4] = t22, $[5] = t3) : t3 = $[5], t3;
}
var PublishAction = (props) => {
  var _a;
  const $ = (0, import_react_compiler_runtime.c)(78), {
    id: id2,
    type: type2,
    liveEdit,
    draft,
    published,
    release
  } = props, [publishState, setPublishState] = (0, import_react3.useState)(null), {
    publish
  } = useDocumentOperation(id2, type2), validationStatus = useValidationStatus(id2, type2), syncState = useSyncState(id2, type2), {
    changesOpen,
    documentId,
    documentType,
    value
  } = useDocumentPane(), editState = useEditState(documentId, documentType), {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  let t0;
  $[0] !== (editState == null ? void 0 : editState.draft) || $[1] !== (editState == null ? void 0 : editState.published) ? (t0 = (editState == null ? void 0 : editState.draft) || (editState == null ? void 0 : editState.published) || {}, $[0] = editState == null ? void 0 : editState.draft, $[1] = editState == null ? void 0 : editState.published, $[2] = t0) : t0 = $[2];
  const revision = t0._rev;
  let t1;
  $[3] !== validationStatus.validation ? (t1 = validationStatus.validation.some(isValidationErrorMarker), $[3] = validationStatus.validation, $[4] = t1) : t1 = $[4];
  const hasValidationErrors = t1, [publishScheduled, setPublishScheduled] = (0, import_react3.useState)(false), isSyncing = syncState.isSyncing, isValidating = validationStatus.isValidating;
  let t22;
  $[5] !== id2 || $[6] !== type2 ? (t22 = {
    id: id2,
    type: type2,
    permission: "publish"
  }, $[5] = id2, $[6] = type2, $[7] = t22) : t22 = $[7];
  const [permissions, isPermissionsLoading] = useDocumentPairPermissions(t22), currentUser = useCurrentUser();
  let t3;
  $[8] !== hasValidationErrors || $[9] !== publish.disabled || $[10] !== published || $[11] !== t2 ? (t3 = publish.disabled ? getDisabledReason(publish.disabled, (published || {})._updatedAt, t2) || "" : hasValidationErrors ? t2("action.publish.validation-issues.tooltip") : "", $[8] = hasValidationErrors, $[9] = publish.disabled, $[10] = published, $[11] = t2, $[12] = t3) : t3 = $[12];
  const title = t3, hasDraft = !!draft;
  let t4;
  $[13] !== publish ? (t4 = () => {
    publish.execute(), setPublishState("publishing");
  }, $[13] = publish, $[14] = t4) : t4 = $[14];
  const doPublish = t4;
  let t5;
  $[15] !== doPublish || $[16] !== hasValidationErrors || $[17] !== isSyncing || $[18] !== publishScheduled || $[19] !== revision || $[20] !== validationStatus.isValidating || $[21] !== validationStatus.revision ? (t5 = () => {
    const validationComplete = validationStatus.isValidating === false && validationStatus.revision !== revision;
    !publishScheduled || isSyncing || !validationComplete || (hasValidationErrors || doPublish(), setPublishScheduled(false));
  }, $[15] = doPublish, $[16] = hasValidationErrors, $[17] = isSyncing, $[18] = publishScheduled, $[19] = revision, $[20] = validationStatus.isValidating, $[21] = validationStatus.revision, $[22] = t5) : t5 = $[22];
  let t6;
  $[23] !== doPublish || $[24] !== hasValidationErrors || $[25] !== isSyncing || $[26] !== isValidating || $[27] !== publishScheduled || $[28] !== revision || $[29] !== validationStatus.isValidating || $[30] !== validationStatus.revision ? (t6 = [isSyncing, doPublish, hasValidationErrors, publishScheduled, validationStatus.revision, revision, isValidating, validationStatus.isValidating], $[23] = doPublish, $[24] = hasValidationErrors, $[25] = isSyncing, $[26] = isValidating, $[27] = publishScheduled, $[28] = revision, $[29] = validationStatus.isValidating, $[30] = validationStatus.revision, $[31] = t6) : t6 = $[31], (0, import_react3.useEffect)(t5, t6);
  let t7;
  $[32] !== hasDraft || $[33] !== publishState ? (t7 = () => {
    const didPublish = publishState === "publishing" && !hasDraft, nextState = didPublish ? "published" : null, timer2 = setTimeout(() => {
      setPublishState(nextState);
    }, didPublish ? 200 : 4e3);
    return () => clearTimeout(timer2);
  }, $[32] = hasDraft, $[33] = publishState, $[34] = t7) : t7 = $[34];
  let t8;
  $[35] !== changesOpen || $[36] !== hasDraft || $[37] !== publishState ? (t8 = [changesOpen, publishState, hasDraft], $[35] = changesOpen, $[36] = hasDraft, $[37] = publishState, $[38] = t8) : t8 = $[38], (0, import_react3.useEffect)(t7, t8);
  const telemetry = useTelemetry();
  let t9;
  $[39] !== doPublish || $[40] !== (draft == null ? void 0 : draft._createdAt) || $[41] !== published || $[42] !== revision || $[43] !== syncState.isSyncing || $[44] !== telemetry || $[45] !== validationStatus.isValidating || $[46] !== validationStatus.revision ? (t9 = () => {
    telemetry.log(DocumentPublished, {
      publishedImmediately: !(draft == null ? void 0 : draft._createdAt),
      previouslyPublished: !!published
    }), syncState.isSyncing || validationStatus.isValidating || validationStatus.revision !== revision ? setPublishScheduled(true) : doPublish();
  }, $[39] = doPublish, $[40] = draft == null ? void 0 : draft._createdAt, $[41] = published, $[42] = revision, $[43] = syncState.isSyncing, $[44] = telemetry, $[45] = validationStatus.isValidating, $[46] = validationStatus.revision, $[47] = t9) : t9 = $[47];
  const handle = t9;
  let t10;
  bb0: {
    if (release) {
      t10 = null;
      break bb0;
    }
    if (liveEdit) {
      t10 = null;
      break bb0;
    }
    if (isPublishedId(value._id) && draft !== null) {
      let t112;
      $[48] !== t2 ? (t112 = t2("action.publish.label"), $[48] = t2, $[49] = t112) : t112 = $[49];
      const t122 = published == null ? void 0 : published._updatedAt;
      let t132;
      $[50] !== t2 || $[51] !== t122 ? (t132 = getDisabledReason("ALREADY_PUBLISHED", t122, t2), $[50] = t2, $[51] = t122, $[52] = t132) : t132 = $[52];
      let t142;
      $[53] !== t112 || $[54] !== t132 ? (t142 = {
        tone: "default",
        icon: PublishIcon,
        label: t112,
        title: t132,
        disabled: true
      }, $[53] = t112, $[54] = t132, $[55] = t142) : t142 = $[55], t10 = t142;
      break bb0;
    }
    if (!isPermissionsLoading && !(permissions == null ? void 0 : permissions.granted)) {
      let t112;
      $[56] !== t2 ? (t112 = t2("action.publish.label"), $[56] = t2, $[57] = t112) : t112 = $[57];
      let t122;
      $[58] !== currentUser ? (t122 = (0, import_jsx_runtime3.jsx)(InsufficientPermissionsMessage, { context: "publish-document", currentUser }), $[58] = currentUser, $[59] = t122) : t122 = $[59];
      let t132;
      $[60] !== t112 || $[61] !== t122 ? (t132 = {
        tone: "default",
        icon: PublishIcon,
        label: t112,
        title: t122,
        disabled: true
      }, $[60] = t112, $[61] = t122, $[62] = t132) : t132 = $[62], t10 = t132;
      break bb0;
    }
    const disabled = !!(publishScheduled || ((_a = editState == null ? void 0 : editState.transactionSyncLock) == null ? void 0 : _a.enabled) || publishState === "publishing" || publishState === "published" || hasValidationErrors || publish.disabled), t11 = disabled || isPermissionsLoading;
    let t12;
    $[63] !== publishScheduled || $[64] !== publishState || $[65] !== t2 ? (t12 = t2(publishState === "published" ? "action.publish.published.label" : publishScheduled || publishState === "publishing" ? "action.publish.running.label" : "action.publish.draft.label"), $[63] = publishScheduled, $[64] = publishState, $[65] = t2, $[66] = t12) : t12 = $[66];
    let t13;
    $[67] !== publishScheduled || $[68] !== publishState || $[69] !== t2 || $[70] !== title ? (t13 = publishScheduled ? t2("action.publish.waiting") : publishState === "published" || publishState === "publishing" ? null : title, $[67] = publishScheduled, $[68] = publishState, $[69] = t2, $[70] = title, $[71] = t13) : t13 = $[71];
    const t14 = disabled || publishScheduled ? null : "Ctrl+Alt+P";
    let t15;
    $[72] !== handle || $[73] !== t11 || $[74] !== t12 || $[75] !== t13 || $[76] !== t14 ? (t15 = {
      disabled: t11,
      tone: "default",
      label: t12,
      icon: PublishIcon,
      title: t13,
      shortcut: t14,
      onHandle: handle
    }, $[72] = handle, $[73] = t11, $[74] = t12, $[75] = t13, $[76] = t14, $[77] = t15) : t15 = $[77], t10 = t15;
  }
  return t10;
};
PublishAction.action = "publish";
PublishAction.displayName = "PublishAction";
var DISABLED_REASON_KEY = {
  NOT_PUBLISHED: "action.unpublish.disabled.not-published",
  NOT_READY: "action.unpublish.disabled.not-ready",
  LIVE_EDIT_ENABLED: "action.unpublish.disabled.live-edit-enabled"
};
var UnpublishAction = ({
  id: id2,
  type: type2,
  draft,
  onComplete,
  liveEdit,
  release
}) => {
  const {
    unpublish
  } = useDocumentOperation(id2, type2), [isConfirmDialogOpen, setConfirmDialogOpen] = (0, import_react3.useState)(false), [permissions, isPermissionsLoading] = useDocumentPairPermissions({
    id: id2,
    type: type2,
    permission: "unpublish"
  }), currentUser = useCurrentUser(), {
    displayed
  } = useDocumentPane(), {
    t: t2
  } = useTranslation(structureLocaleNamespace), isDraft = (displayed == null ? void 0 : displayed._id) && isDraftId(displayed == null ? void 0 : displayed._id), handleCancel = (0, import_react3.useCallback)(() => {
    setConfirmDialogOpen(false), onComplete();
  }, [onComplete]), handleConfirm = (0, import_react3.useCallback)(() => {
    setConfirmDialogOpen(false), unpublish.execute(), onComplete();
  }, [onComplete, unpublish]), dialog = (0, import_react3.useMemo)(() => isConfirmDialogOpen ? {
    type: "dialog",
    onClose: onComplete,
    content: (0, import_jsx_runtime3.jsx)(
      ConfirmDeleteDialogContainer,
      {
        id: (draft == null ? void 0 : draft._id) || id2,
        type: type2,
        action: "unpublish",
        onCancel: handleCancel,
        onConfirm: handleConfirm
      }
    )
  } : null, [draft, id2, handleCancel, handleConfirm, isConfirmDialogOpen, onComplete, type2]);
  return (0, import_react3.useMemo)(() => release || isDraft || liveEdit ? null : !isPermissionsLoading && !(permissions == null ? void 0 : permissions.granted) ? {
    tone: "critical",
    icon: UnpublishIcon,
    label: "Unpublish",
    title: (0, import_jsx_runtime3.jsx)(InsufficientPermissionsMessage, { context: "unpublish-document", currentUser }),
    disabled: true
  } : {
    tone: "critical",
    icon: UnpublishIcon,
    disabled: !!unpublish.disabled || isPermissionsLoading,
    label: t2("action.unpublish.label"),
    title: unpublish.disabled ? t2(DISABLED_REASON_KEY[unpublish.disabled]) : "",
    onHandle: () => setConfirmDialogOpen(true),
    dialog
  }, [release, isDraft, liveEdit, isPermissionsLoading, permissions == null ? void 0 : permissions.granted, unpublish.disabled, t2, dialog, currentUser]);
};
UnpublishAction.action = "unpublish";
UnpublishAction.displayName = "UnpublishAction";
var DocumentStatusBarActionsInner = (0, import_react3.memo)(function(props) {
  const $ = (0, import_react_compiler_runtime.c)(33), {
    disabled,
    showMenu,
    states
  } = props, {
    __internal_tasks
  } = useSource(), {
    editState
  } = useDocumentPane(), {
    selectedReleaseId
  } = usePerspective();
  let firstActionState, menuActionStates;
  $[0] !== states ? ([firstActionState, ...menuActionStates] = states, $[0] = states, $[1] = firstActionState, $[2] = menuActionStates) : (firstActionState = $[1], menuActionStates = $[2]);
  const [buttonElement, setButtonElement] = (0, import_react3.useState)(null);
  let t0;
  bb0: {
    if (!firstActionState || !firstActionState.title && !firstActionState.shortcut) {
      t0 = null;
      break bb0;
    }
    let t12;
    $[3] === Symbol.for("react.memo_cache_sentinel") ? (t12 = {
      maxWidth: 300
    }, $[3] = t12) : t12 = $[3];
    let t22;
    $[4] !== firstActionState.title ? (t22 = firstActionState.title && (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: firstActionState.title }), $[4] = firstActionState.title, $[5] = t22) : t22 = $[5];
    let t32;
    $[6] !== firstActionState.shortcut ? (t32 = firstActionState.shortcut && (0, import_jsx_runtime3.jsx)(Hotkeys, { "data-testid": "document-status-bar-hotkeys", fontSize: 1, style: {
      marginTop: -4,
      marginBottom: -4
    }, keys: String(firstActionState.shortcut).split("+").map(_temp$c) }), $[6] = firstActionState.shortcut, $[7] = t32) : t32 = $[7];
    let t42;
    $[8] !== t22 || $[9] !== t32 ? (t42 = (0, import_jsx_runtime3.jsxs)(Flex, { style: t12, align: "center", gap: 3, children: [
      t22,
      t32
    ] }), $[8] = t22, $[9] = t32, $[10] = t42) : t42 = $[10], t0 = t42;
  }
  const tooltipContent = t0, showFirstActionButton = selectedReleaseId ? firstActionState && !isSanityDefinedAction(firstActionState) : firstActionState && !(editState == null ? void 0 : editState.liveEdit);
  let t1, t2;
  $[11] !== firstActionState || $[12] !== menuActionStates || $[13] !== showFirstActionButton ? (t2 = showFirstActionButton ? menuActionStates : [firstActionState, ...menuActionStates], $[11] = firstActionState, $[12] = menuActionStates, $[13] = showFirstActionButton, $[14] = t2) : t2 = $[14], t1 = t2;
  const sideMenuItems = t1, t3 = __internal_tasks && __internal_tasks.footerAction;
  let t4;
  $[15] !== disabled || $[16] !== firstActionState || $[17] !== showFirstActionButton || $[18] !== tooltipContent ? (t4 = showFirstActionButton && (0, import_jsx_runtime3.jsx)(LayerProvider, { zOffset: 200, children: (0, import_jsx_runtime3.jsx)(Tooltip, { disabled: !tooltipContent, content: tooltipContent, placement: "top", children: (0, import_jsx_runtime3.jsx)(Stack, { children: (0, import_jsx_runtime3.jsx)(Button2, { "data-testid": `action-${toLowerCaseNoSpaces(firstActionState.label)}`, disabled: disabled || !!firstActionState.disabled, icon: firstActionState.icon, onClick: firstActionState.onHandle, ref: setButtonElement, text: firstActionState.label, tone: firstActionState.tone || "primary" }) }) }) }), $[15] = disabled, $[16] = firstActionState, $[17] = showFirstActionButton, $[18] = tooltipContent, $[19] = t4) : t4 = $[19];
  let t5;
  $[20] !== disabled || $[21] !== menuActionStates || $[22] !== showMenu || $[23] !== sideMenuItems ? (t5 = showMenu && menuActionStates.length > 0 && (0, import_jsx_runtime3.jsx)(ActionMenuButton, { actionStates: sideMenuItems, disabled }), $[20] = disabled, $[21] = menuActionStates, $[22] = showMenu, $[23] = sideMenuItems, $[24] = t5) : t5 = $[24];
  let t6;
  $[25] !== buttonElement || $[26] !== firstActionState ? (t6 = firstActionState && firstActionState.dialog && (0, import_jsx_runtime3.jsx)(ActionStateDialog, { dialog: firstActionState.dialog, referenceElement: buttonElement }), $[25] = buttonElement, $[26] = firstActionState, $[27] = t6) : t6 = $[27];
  let t7;
  return $[28] !== t3 || $[29] !== t4 || $[30] !== t5 || $[31] !== t6 ? (t7 = (0, import_jsx_runtime3.jsxs)(Flex, { align: "center", gap: 1, children: [
    t3,
    t4,
    t5,
    t6
  ] }), $[28] = t3, $[29] = t4, $[30] = t5, $[31] = t6, $[32] = t7) : t7 = $[32], t7;
});
var DocumentStatusBarActions = (0, import_react3.memo)(function() {
  const $ = (0, import_react_compiler_runtime.c)(15), {
    actions: allActions,
    connectionState,
    documentId,
    editState,
    isInitialValueLoading
  } = useDocumentPane();
  let t0, t1;
  $[0] !== allActions ? (t1 = allActions ?? [], $[0] = allActions, $[1] = t1) : t1 = $[1];
  let t2;
  $[2] !== t1 ? (t2 = t1.filter(_temp2$4), $[2] = t1, $[3] = t2) : t2 = $[3], t0 = t2;
  const actions = t0;
  let t3, t4;
  $[4] !== editState || $[5] !== isInitialValueLoading ? (t4 = editState ? {
    ...editState,
    initialValueResolved: !isInitialValueLoading
  } : null, $[4] = editState, $[5] = isInitialValueLoading, $[6] = t4) : t4 = $[6], t3 = t4;
  const actionProps = t3;
  let t5;
  $[7] !== actions.length || $[8] !== connectionState || $[9] !== documentId ? (t5 = (t62) => {
    const {
      states
    } = t62;
    return (0, import_jsx_runtime3.jsx)(DocumentStatusBarActionsInner, { disabled: connectionState !== "connected", showMenu: actions.length > 1, states }, documentId);
  }, $[7] = actions.length, $[8] = connectionState, $[9] = documentId, $[10] = t5) : t5 = $[10];
  const renderDocumentStatusBarActions = t5;
  if (actions.length === 0 || !actionProps)
    return null;
  let t6;
  return $[11] !== actionProps || $[12] !== actions || $[13] !== renderDocumentStatusBarActions ? (t6 = (0, import_jsx_runtime3.jsx)(RenderActionCollectionState, { actions, actionProps, group: "default", children: renderDocumentStatusBarActions }), $[11] = actionProps, $[12] = actions, $[13] = renderDocumentStatusBarActions, $[14] = t6) : t6 = $[14], t6;
});
var HistoryStatusBarActions = (0, import_react3.memo)(function() {
  const $ = (0, import_react_compiler_runtime.c)(15), {
    actions,
    connectionState,
    editState,
    revisionId: revision,
    isInitialValueLoading
  } = useDocumentPane(), disabled = ((editState == null ? void 0 : editState.draft) || (editState == null ? void 0 : editState.published) || {})._rev === revision;
  let t0, t1;
  $[0] !== editState || $[1] !== isInitialValueLoading || $[2] !== revision ? (t1 = editState ? {
    ...editState,
    revision: revision || void 0,
    initialValueResolved: !isInitialValueLoading
  } : null, $[0] = editState, $[1] = isInitialValueLoading, $[2] = revision, $[3] = t1) : t1 = $[3], t0 = t1;
  const actionProps = t0;
  let t2, t3;
  $[4] !== actions ? (t3 = actions ?? [], $[4] = actions, $[5] = t3) : t3 = $[5];
  let t4;
  $[6] !== t3 ? (t4 = t3.filter(isRestoreAction).slice(-1), $[6] = t3, $[7] = t4) : t4 = $[7], t2 = t4;
  const historyActions = t2;
  let t5;
  $[8] !== connectionState || $[9] !== disabled ? (t5 = (t62) => {
    const {
      states
    } = t62;
    return (0, import_jsx_runtime3.jsx)(DocumentStatusBarActionsInner, { disabled: connectionState !== "connected" || !!disabled, showMenu: false, states });
  }, $[8] = connectionState, $[9] = disabled, $[10] = t5) : t5 = $[10];
  const renderDocumentStatusBarActions = t5;
  if (!actionProps)
    return null;
  let t6;
  return $[11] !== actionProps || $[12] !== historyActions || $[13] !== renderDocumentStatusBarActions ? (t6 = (0, import_jsx_runtime3.jsx)(RenderActionCollectionState, { actions: historyActions, actionProps, group: "default", children: renderDocumentStatusBarActions }), $[11] = actionProps, $[12] = historyActions, $[13] = renderDocumentStatusBarActions, $[14] = t6) : t6 = $[14], t6;
});
function isRestoreAction(action) {
  return action.action === HistoryRestoreAction.action;
}
function _temp$c(s2) {
  return s2.slice(0, 1).toUpperCase() + s2.slice(1).toLowerCase();
}
function _temp2$4(action) {
  return !isRestoreAction(action);
}
function useChipScrollPosition(containerRef) {
  const $ = (0, import_react_compiler_runtime.c)(4), [showGradient, setShowGradient] = (0, import_react3.useState)(false);
  let t0;
  $[0] !== containerRef.current ? (t0 = () => {
    const checkOverflow = () => {
      const container = containerRef.current;
      if (!container)
        return;
      const {
        scrollWidth,
        clientWidth
      } = container;
      if (!(scrollWidth > clientWidth)) {
        setShowGradient(false);
        return;
      }
      const {
        scrollLeft
      } = container, isAtEnd = scrollLeft + clientWidth >= scrollWidth;
      setShowGradient(!isAtEnd);
    }, setupObservers = function() {
      checkOverflow();
      const container_0 = containerRef.current;
      if (!container_0)
        return {
          intersectionObserver: null,
          mutationObserver: null
        };
      const intersectionObserver = new IntersectionObserver((entries) => {
        const entry = entries[0];
        entry && setShowGradient(!entry.isIntersecting);
      }), updateLastChipObserver = () => {
        intersectionObserver.disconnect();
        const lastChip = container_0.children[container_0.children.length - 1];
        intersectionObserver.observe(lastChip);
      };
      updateLastChipObserver();
      const mutationObserver = new MutationObserver(() => {
        updateLastChipObserver(), checkOverflow();
      });
      return mutationObserver.observe(container_0, {
        childList: true,
        subtree: false
      }), {
        intersectionObserver,
        mutationObserver
      };
    }, {
      intersectionObserver: intersectionObserver_0,
      mutationObserver: mutationObserver_0
    } = setupObservers();
    return () => {
      intersectionObserver_0 == null ? void 0 : intersectionObserver_0.disconnect(), mutationObserver_0 == null ? void 0 : mutationObserver_0.disconnect();
    };
  }, $[0] = containerRef.current, $[1] = t0) : t0 = $[1];
  let t1;
  return $[2] !== containerRef ? (t1 = [containerRef], $[2] = containerRef, $[3] = t1) : t1 = $[3], (0, import_react3.useEffect)(t0, t1), showGradient;
}
var TooltipContent$1 = (t0) => {
  const $ = (0, import_react_compiler_runtime.c)(16), {
    release
  } = t0, {
    t: t2
  } = useTranslation();
  if (release.state === "archived") {
    let t1;
    $[0] !== t2 ? (t1 = t2("release.chip.tooltip.archived"), $[0] = t2, $[1] = t1) : t1 = $[1];
    let t22;
    return $[2] !== t1 ? (t22 = (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: t1 }), $[2] = t1, $[3] = t22) : t22 = $[3], t22;
  }
  if (release.metadata.releaseType === "asap") {
    let t1;
    $[4] !== t2 ? (t1 = t2("release.type.asap"), $[4] = t2, $[5] = t1) : t1 = $[5];
    let t22;
    return $[6] !== t1 ? (t22 = (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: t1 }), $[6] = t1, $[7] = t22) : t22 = $[7], t22;
  }
  if (release.metadata.releaseType === "scheduled") {
    const isActive = release.state === "active";
    let t1;
    return $[8] !== isActive || $[9] !== release || $[10] !== t2 ? (t1 = release.metadata.intendedPublishAt && (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: isActive ? (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey: "release.chip.tooltip.intended-for-date", values: {
      date: formatRelativeLocalePublishDate(release)
    } }) : (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey: "release.chip.tooltip.scheduled-for-date", values: {
      date: formatRelativeLocalePublishDate(release)
    } }) }), $[8] = isActive, $[9] = release, $[10] = t2, $[11] = t1) : t1 = $[11], t1;
  }
  if (release.metadata.releaseType === "undecided") {
    let t1;
    $[12] !== t2 ? (t1 = t2("release.type.undecided"), $[12] = t2, $[13] = t1) : t1 = $[13];
    let t22;
    return $[14] !== t1 ? (t22 = (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: t1 }), $[14] = t1, $[15] = t22) : t22 = $[15], t22;
  }
  return null;
};
var DATE_TIME_FORMAT = {
  dateStyle: "medium",
  timeStyle: "short"
};
var DocumentPerspectiveList = (0, import_react3.memo)(function() {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j;
  const {
    selectedReleaseId,
    selectedPerspectiveName
  } = usePerspective(), {
    t: t2
  } = useTranslation(), setPerspective = useSetPerspective(), {
    params,
    setParams
  } = usePaneRouter(), dateTimeFormat = useDateTimeFormat(DATE_TIME_FORMAT), {
    loading
  } = useActiveReleases(), schema = useSchema(), {
    editState,
    displayed,
    documentType,
    documentId
  } = useDocumentPane(), isCreatingDocument = displayed && !displayed._createdAt, filteredReleases = useFilteredReleases({
    displayed,
    documentId
  }), onlyHasVersions = useOnlyHasVersions({
    documentId
  }), workspace = useWorkspace(), handlePerspectiveChange = (0, import_react3.useCallback)((perspective) => () => {
    perspective === "published" && (params == null ? void 0 : params.historyVersion) && setParams({
      ...params,
      rev: (params == null ? void 0 : params.historyEvent) || void 0,
      since: void 0,
      historyVersion: void 0
    }), setPerspective(perspective);
  }, [setPerspective, setParams, params]), schemaType = schema.get(documentType), isLiveEdit = schemaType ? isLiveEditEnabled(schemaType) : false, isPublishedChipDisabled = (0, import_react3.useMemo)(() => isLiveEdit && !selectedReleaseId ? false : !(editState == null ? void 0 : editState.published), [isLiveEdit, selectedReleaseId, editState == null ? void 0 : editState.published]), getReleaseChipState = (0, import_react3.useCallback)((release) => {
    if (!(params == null ? void 0 : params.historyVersion)) return {
      selected: getReleaseIdFromReleaseDocumentId(release._id) === getVersionFromId((displayed == null ? void 0 : displayed._id) || "")
    };
    const isReleaseHistoryMatch = getReleaseIdFromReleaseDocumentId(release._id) === params.historyVersion;
    return {
      selected: isReleaseHistoryMatch,
      disabled: isReleaseHistoryMatch
    };
  }, [displayed == null ? void 0 : displayed._id, params == null ? void 0 : params.historyVersion]), isPublishSelected = (0, import_react3.useMemo)(() => {
    var _a2;
    return !!(isLiveEdit && !((_a2 = editState == null ? void 0 : editState.draft) == null ? void 0 : _a2._id) && !selectedReleaseId || isPublishedId((displayed == null ? void 0 : displayed._id) || "") && isPublishedPerspective(selectedPerspectiveName || ""));
  }, [displayed == null ? void 0 : displayed._id, (_a = editState == null ? void 0 : editState.draft) == null ? void 0 : _a._id, isLiveEdit, selectedPerspectiveName, selectedReleaseId]), isDraftSelected = (0, import_react3.useMemo)(() => {
    const displayedId = (displayed == null ? void 0 : displayed._id) || "";
    return isPublishSelected || (params == null ? void 0 : params.historyVersion) || selectedPerspectiveName || isVersionId(displayedId) ? false : isDraftId(displayedId) ? true : !(isPublishedId(displayedId) && (editState == null ? void 0 : editState.published) && isPublishedPerspective(selectedPerspectiveName || ""));
  }, [displayed == null ? void 0 : displayed._id, editState == null ? void 0 : editState.published, isPublishSelected, params == null ? void 0 : params.historyVersion, selectedPerspectiveName]), isDraftDisabled = (0, import_react3.useMemo)(() => onlyHasVersions || isCreatingDocument && selectedReleaseId ? true : !(editState == null ? void 0 : editState.draft) && !isLiveEdit ? false : !!(isCreatingDocument && selectedReleaseId || isLiveEdit), [editState == null ? void 0 : editState.draft, isCreatingDocument, isLiveEdit, onlyHasVersions, selectedReleaseId]), isDraftModelEnabled = (_b = workspace.document.drafts) == null ? void 0 : _b.enabled;
  return (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [
    (0, import_jsx_runtime3.jsx)(VersionChip$1, { tooltipContent: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: (editState == null ? void 0 : editState.published) && ((_c = editState == null ? void 0 : editState.published) == null ? void 0 : _c._updatedAt) ? (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey: "release.chip.tooltip.published-date", values: {
      date: dateTimeFormat.format(new Date(editState == null ? void 0 : editState.published._updatedAt))
    } }) : (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children: t2("release.chip.tooltip.not-published") }) }), disabled: isPublishedChipDisabled, onClick: handlePerspectiveChange("published"), selected: isPublishSelected, text: t2("release.chip.published"), tone: "positive", contextValues: {
      documentId: ((_d = editState == null ? void 0 : editState.published) == null ? void 0 : _d._id) || (editState == null ? void 0 : editState.id) || "",
      menuReleaseId: ((_e = editState == null ? void 0 : editState.published) == null ? void 0 : _e._id) || (editState == null ? void 0 : editState.id) || "",
      releases: filteredReleases.notCurrentReleases,
      releasesLoading: loading,
      documentType,
      fromRelease: "published",
      isVersion: false,
      disabled: !(editState == null ? void 0 : editState.published)
    } }),
    isDraftModelEnabled && (0, import_jsx_runtime3.jsx)(VersionChip$1, { tooltipContent: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: (editState == null ? void 0 : editState.draft) ? (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children: (editState == null ? void 0 : editState.draft._updatedAt) ? (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey: "release.chip.tooltip.edited-date", values: {
      date: dateTimeFormat.format(new Date(editState == null ? void 0 : editState.draft._updatedAt))
    } }) : (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey: "release.chip.tooltip.created-date", values: {
      date: dateTimeFormat.format(new Date(editState == null ? void 0 : editState.draft._createdAt))
    } }) }) : (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children: t2(isLiveEdit ? "release.chip.tooltip.draft-disabled.live-edit" : "release.chip.tooltip.no-edits") }) }), selected: isDraftSelected, disabled: isDraftDisabled, text: t2("release.chip.draft"), tone: (editState == null ? void 0 : editState.draft) ? "caution" : "neutral", onClick: handlePerspectiveChange("drafts"), contextValues: {
      documentId: ((_f = editState == null ? void 0 : editState.draft) == null ? void 0 : _f._id) || ((_g = editState == null ? void 0 : editState.published) == null ? void 0 : _g._id) || (editState == null ? void 0 : editState.id) || "",
      menuReleaseId: ((_h = editState == null ? void 0 : editState.draft) == null ? void 0 : _h._id) || ((_i = editState == null ? void 0 : editState.published) == null ? void 0 : _i._id) || (editState == null ? void 0 : editState.id) || "",
      releases: filteredReleases.notCurrentReleases,
      releasesLoading: loading,
      documentType,
      fromRelease: "draft",
      isVersion: false,
      disabled: !(editState == null ? void 0 : editState.draft)
    } }),
    filteredReleases.inCreation && (0, import_jsx_runtime3.jsx)(VersionChip$1, { tooltipContent: (0, import_jsx_runtime3.jsx)(TooltipContent$1, { release: filteredReleases.inCreation }), selected: true, onClick: () => {
    }, locked: false, tone: getReleaseTone(filteredReleases.inCreation), text: filteredReleases.inCreation.metadata.title || t2("release.placeholder-untitled-release"), contextValues: {
      disabled: true,
      // disable the chip context menu, this one is in creation
      documentId: (displayed == null ? void 0 : displayed._id) || "",
      menuReleaseId: filteredReleases.inCreation._id,
      releases: filteredReleases.notCurrentReleases,
      releasesLoading: loading,
      documentType,
      fromRelease: getReleaseIdFromReleaseDocumentId(filteredReleases.inCreation._id),
      releaseState: filteredReleases.inCreation.state,
      isVersion: true
    } }),
    displayed && ((_j = filteredReleases.currentReleases) == null ? void 0 : _j.map((release_0) => (0, import_jsx_runtime3.jsx)(VersionChip$1, { tooltipContent: (0, import_jsx_runtime3.jsx)(TooltipContent$1, { release: release_0 }), ...getReleaseChipState(release_0), onClick: handlePerspectiveChange(getReleaseIdFromReleaseDocumentId(release_0._id)), text: release_0.metadata.title || t2("release.placeholder-untitled-release"), tone: getReleaseTone(release_0), locked: isReleaseScheduledOrScheduling(release_0), contextValues: {
      documentId: (displayed == null ? void 0 : displayed._id) || "",
      menuReleaseId: release_0._id,
      releases: filteredReleases.notCurrentReleases,
      releasesLoading: loading,
      documentType,
      fromRelease: getReleaseIdFromReleaseDocumentId(release_0._id),
      releaseState: release_0.state,
      isVersion: true
    } }, release_0._id)))
  ] });
});
var HorizontalScroller = dt(Card)((props) => {
  const theme = getTheme_v2(props.theme);
  return lt`
    scrollbar-width: none;
    z-index: 1;
    flex: 1;
    position: relative;
    > div {
      &::-webkit-scrollbar {
        width: 0;
        height: 0;
      }
    }

    ${props.$showGradient && lt`
      &::after {
        content: '';
        display: block;
        position: absolute;
        top: 0;
        right: 0;
        bottom: 0;
        width: 150px;
        background: linear-gradient(to right, ${rgba(theme.color.bg, 0)}, var(--card-bg-color));
        transition: 'opacity 300ms ease-out';
        pointer-events: none;
      }
    `}
  `;
});
var DocumentPanelHeader = (0, import_react3.memo)((0, import_react3.forwardRef)(function(_props, ref) {
  const {
    menuItems
  } = _props, {
    actions: allActions,
    editState,
    onMenuAction,
    isInitialValueLoading,
    onPaneClose,
    onPaneSplit,
    menuItemGroups,
    schemaType,
    connectionState,
    views: views2,
    unstable_languageFilter
  } = useDocumentPane(), {
    features
  } = useStructureTool(), {
    index,
    BackLink: BackLink2,
    hasGroupSiblings
  } = usePaneRouter(), {
    actions: fieldActions
  } = useFieldActions(), [referenceElement, setReferenceElement] = (0, import_react3.useState)(null), scrollContainerRef = (0, import_react3.useRef)(null), showGradient = useChipScrollPosition(scrollContainerRef), actions = (0, import_react3.useMemo)(() => (allActions ?? []).filter((action) => !isRestoreAction(action)), [allActions]), menuNodes = (0, import_react3.useMemo)(() => resolveMenuNodes({
    actionHandler: onMenuAction,
    fieldActions,
    menuItems,
    menuItemGroups
  }), [onMenuAction, fieldActions, menuItemGroups, menuItems]), menuButtonNodes = (0, import_react3.useMemo)(() => menuNodes.filter(isMenuNodeButton), [menuNodes]), contextMenuNodes = (0, import_react3.useMemo)(() => menuNodes.filter(isNotMenuNodeButton), [menuNodes]), {
    collapsed,
    isLast
  } = usePane(), tabIndex = isLast && !collapsed ? -1 : 0, showSplitPaneButton = features.splitViews && onPaneSplit && views2.length > 1, showSplitPaneCloseButton = showSplitPaneButton && hasGroupSiblings, showBackButton = features.backButton && index > 0, showPaneGroupCloseButton = !showSplitPaneCloseButton && !showBackButton && !!BackLink2, {
    t: t2
  } = useTranslation(structureLocaleNamespace), renderPaneActions = (0, import_react3.useCallback)(({
    states
  }) => (0, import_jsx_runtime3.jsx)(DocumentPanelHeaderActionDialogDeferred, { contextMenuNodes, setReferenceElement, referenceElement, states }), [contextMenuNodes, referenceElement]), title = (0, import_react3.useMemo)(() => (0, import_jsx_runtime3.jsx)(DocumentHeaderTitle, {}), []), backButton = (0, import_react3.useMemo)(() => showBackButton && (0, import_jsx_runtime3.jsx)(Button2, { as: BackLink2, "data-as": "a", icon: ArrowLeftIcon, mode: "bleed", tooltipProps: {
    content: t2("pane-header.back-button.text")
  } }), [BackLink2, showBackButton, t2]), renderedActions = (0, import_react3.useMemo)(() => (0, import_jsx_runtime3.jsxs)(Flex, { align: "center", gap: 1, children: [
    unstable_languageFilter.length > 0 && (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children: unstable_languageFilter.map((LanguageFilterComponent, idx) => (0, import_jsx_runtime3.jsx)(
      LanguageFilterComponent,
      {
        schemaType
      },
      `language-filter-${idx}`
    )) }),
    menuButtonNodes.map((item) => (0, import_jsx_runtime3.jsx)(PaneHeaderActionButton, { node: item }, item.key)),
    editState && (0, import_jsx_runtime3.jsx)(RenderActionCollectionState, { actions, actionProps: {
      ...editState,
      initialValueResolved: !isInitialValueLoading
    }, group: "paneActions", children: renderPaneActions }),
    showSplitPaneButton && (0, import_jsx_runtime3.jsx)(Button2, { "aria-label": t2("buttons.split-pane-button.aria-label"), icon: SplitVerticalIcon, mode: "bleed", onClick: onPaneSplit, tooltipProps: {
      content: t2("buttons.split-pane-button.tooltip")
    } }, "split-pane-button"),
    showSplitPaneCloseButton && (0, import_jsx_runtime3.jsx)(Button2, { icon: CloseIcon, mode: "bleed", onClick: onPaneClose, tooltipProps: {
      content: t2("buttons.split-pane-close-button.title")
    } }, "close-view-button"),
    showPaneGroupCloseButton && (0, import_jsx_runtime3.jsx)(Button2, { icon: CloseIcon, mode: "bleed", tooltipProps: {
      content: t2("buttons.split-pane-close-group-button.title")
    }, as: BackLink2 }, "close-view-button")
  ] }), [BackLink2, actions, editState, isInitialValueLoading, menuButtonNodes, onPaneClose, onPaneSplit, renderPaneActions, schemaType, showPaneGroupCloseButton, showSplitPaneButton, showSplitPaneCloseButton, t2, unstable_languageFilter]);
  return (0, import_jsx_runtime3.jsx)(TooltipDelayGroupProvider, { children: collapsed ? (0, import_jsx_runtime3.jsx)(PaneHeader$1, { border: true, ref, loading: connectionState === "connecting" && !(editState == null ? void 0 : editState.draft) && !(editState == null ? void 0 : editState.published), title, tabIndex, backButton }) : (0, import_jsx_runtime3.jsx)(Card, { hidden: collapsed, style: {
    lineHeight: 0
  }, borderBottom: true, children: (0, import_jsx_runtime3.jsxs)(Flex, { gap: 3, paddingY: 3, children: [
    (0, import_jsx_runtime3.jsx)(HorizontalScroller, { $showGradient: showGradient, children: (0, import_jsx_runtime3.jsx)(Flex, { flex: 1, gap: 1, overflow: "auto", paddingX: 3, "data-testid": "document-perspective-list", ref: scrollContainerRef, children: (0, import_jsx_runtime3.jsx)(DocumentPerspectiveList, {}) }) }),
    (0, import_jsx_runtime3.jsx)(Box, { flex: "none", paddingRight: 3, children: renderedActions })
  ] }) }) });
}));
var DocumentPanelHeaderActionDialogDeferred = (0, import_react3.memo)(function(props) {
  const $ = (0, import_react_compiler_runtime.c)(5), {
    setReferenceElement,
    referenceElement,
    contextMenuNodes
  } = props, states = (0, import_react3.useDeferredValue)(props.states);
  let t0;
  return $[0] !== contextMenuNodes || $[1] !== referenceElement || $[2] !== setReferenceElement || $[3] !== states ? (t0 = (0, import_jsx_runtime3.jsx)(DocumentPanelHeaderActionDialog, { setReferenceElement, referenceElement, contextMenuNodes, states }), $[0] = contextMenuNodes, $[1] = referenceElement, $[2] = setReferenceElement, $[3] = states, $[4] = t0) : t0 = $[4], t0;
});
var DocumentPanelHeaderActionDialog = (0, import_react3.memo)(function(props) {
  const $ = (0, import_react_compiler_runtime.c)(8), {
    states,
    setReferenceElement,
    contextMenuNodes,
    referenceElement
  } = props;
  let t0;
  $[0] !== contextMenuNodes || $[1] !== setReferenceElement || $[2] !== states ? (t0 = (t12) => {
    const {
      handleAction
    } = t12;
    return (0, import_jsx_runtime3.jsx)("div", { ref: setReferenceElement, children: (0, import_jsx_runtime3.jsx)(PaneContextMenuButton, { nodes: contextMenuNodes, actionsNodes: states.length > 0 ? states.map((actionState, actionIndex) => (0, import_jsx_runtime3.jsx)(ActionMenuListItem, { actionState, disabled: !!actionState.disabled, index: actionIndex, onAction: handleAction }, actionState.label)) : void 0 }, "context-menu") });
  }, $[0] = contextMenuNodes, $[1] = setReferenceElement, $[2] = states, $[3] = t0) : t0 = $[3];
  const renderActionDialog = t0;
  let t1;
  return $[4] !== referenceElement || $[5] !== renderActionDialog || $[6] !== states ? (t1 = (0, import_jsx_runtime3.jsx)(ActionDialogWrapper, { actionStates: states, referenceElement, children: renderActionDialog }), $[4] = referenceElement, $[5] = renderActionDialog, $[6] = states, $[7] = t1) : t1 = $[7], t1;
});
function SpacerButton(t0) {
  const $ = (0, import_react_compiler_runtime.c)(3), {
    size
  } = t0;
  let t1;
  $[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = {
    pointerEvents: "none",
    visibility: "hidden",
    width: 0
  }, $[0] = t1) : t1 = $[0];
  let t2;
  return $[1] !== size ? (t2 = (0, import_jsx_runtime3.jsx)(Button2, { "aria-hidden": true, disabled: true, size, style: t1, text: "-" }), $[1] = size, $[2] = t2) : t2 = $[2], t2;
}
var BADGE_TONES = {
  primary: "primary",
  success: "positive",
  warning: "caution",
  danger: "critical"
};
var DocumentBadgesInner = (0, import_react3.memo)(function(t0) {
  const $ = (0, import_react_compiler_runtime.c)(4), {
    states
  } = t0;
  if (states.length === 0)
    return null;
  let t1;
  $[0] !== states ? (t1 = states.map(_temp$b), $[0] = states, $[1] = t1) : t1 = $[1];
  let t2;
  return $[2] !== t1 ? (t2 = (0, import_jsx_runtime3.jsx)(Inline, { space: 1, children: t1 }), $[2] = t1, $[3] = t2) : t2 = $[3], t2;
});
var DocumentBadgesDeferred = (0, import_react3.memo)(function(props) {
  const $ = (0, import_react_compiler_runtime.c)(2), states = (0, import_react3.useDeferredValue)(props.states);
  let t0;
  return $[0] !== states ? (t0 = (0, import_jsx_runtime3.jsx)(DocumentBadgesInner, { states }), $[0] = states, $[1] = t0) : t0 = $[1], t0;
});
function DocumentBadges() {
  const $ = (0, import_react_compiler_runtime.c)(3), {
    badges,
    editState
  } = useDocumentPane(), renderDocumentBadges = _temp2$3;
  if (!editState || !badges)
    return null;
  let t0;
  return $[0] !== badges || $[1] !== editState ? (t0 = (0, import_jsx_runtime3.jsx)(RenderBadgeCollectionState, { badges, badgeProps: editState, children: renderDocumentBadges }), $[0] = badges, $[1] = editState, $[2] = t0) : t0 = $[2], t0;
}
function _temp2$3(t0) {
  const {
    states
  } = t0;
  return (0, import_jsx_runtime3.jsx)(DocumentBadgesDeferred, { states });
}
function _temp$b(badge, index) {
  return (0, import_jsx_runtime3.jsx)(Tooltip, { content: badge.title, disabled: !badge.title, placement: "top", portal: true, children: (0, import_jsx_runtime3.jsx)(Badge, { fontSize: 1, mode: "outline", paddingX: 2, paddingY: 1, radius: 4, tone: badge.color ? BADGE_TONES[badge.color] : void 0, style: {
    whiteSpace: "nowrap"
  }, children: badge.label }) }, `${badge.label}-${index}`);
}
var TIMELINE_ICON_COMPONENTS = {
  create: AddIcon,
  delete: TrashIcon,
  discardDraft: CloseIcon,
  initial: AddIcon,
  editDraft: EditIcon,
  editLive: EditIcon,
  publish: PublishIcon,
  unpublish: UnpublishIcon
};
function getTimelineEventIconComponent(type2) {
  return TIMELINE_ICON_COMPONENTS[type2];
}
function hideScrollbarOnExpand(isExpanded2) {
  if (isExpanded2) return;
  const listWrapper = document.getElementById(TIMELINE_LIST_WRAPPER_ID$1);
  if (listWrapper) {
    const firstChildren = listWrapper.children[0];
    if (!(firstChildren.scrollHeight > firstChildren.clientHeight)) {
      const currentStyle = getComputedStyle(firstChildren).overflowY;
      firstChildren.style.overflowY = "hidden", setTimeout(() => {
        firstChildren.style.overflowY = currentStyle;
      }, 0);
    }
  }
}
var FlipIcon = dt(ChevronLeftIcon)`
  transition: transform 200ms;
  &[data-expanded='true'] {
    transform: rotate(-90deg);
  }
`;
function ExpandableTimelineItemButton(t0) {
  const $ = (0, import_react_compiler_runtime.c)(14), {
    isExpanded: isExpanded2,
    onExpand
  } = t0, {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  let t1;
  $[0] !== isExpanded2 || $[1] !== onExpand ? (t1 = (e2) => {
    e2.stopPropagation(), hideScrollbarOnExpand(isExpanded2), onExpand();
  }, $[0] = isExpanded2, $[1] = onExpand, $[2] = t1) : t1 = $[2];
  const handleExpandClick = t1;
  let t22;
  $[3] !== isExpanded2 ? (t22 = (0, import_jsx_runtime3.jsx)(FlipIcon, { "data-expanded": isExpanded2 }), $[3] = isExpanded2, $[4] = t22) : t22 = $[4];
  let t3;
  $[5] !== isExpanded2 || $[6] !== t2 ? (t3 = t2(isExpanded2 ? "timeline-item.menu.action-collapse" : "timeline-item.menu.action-expand"), $[5] = isExpanded2, $[6] = t2, $[7] = t3) : t3 = $[7];
  let t4;
  $[8] !== t3 ? (t4 = {
    content: t3
  }, $[8] = t3, $[9] = t4) : t4 = $[9];
  let t5;
  return $[10] !== handleExpandClick || $[11] !== t22 || $[12] !== t4 ? (t5 = (0, import_jsx_runtime3.jsx)(Button2, { mode: "bleed", icon: t22, tooltipProps: t4, onClick: handleExpandClick }), $[10] = handleExpandClick, $[11] = t22, $[12] = t4, $[13] = t5) : t5 = $[13], t5;
}
var StackWrapper = dt(Stack)`
  max-width: 200px;
`;
var ListWrapper = dt(Flex)`
  max-height: ${(props) => props.$maxHeight};
  min-width: 244px;
`;
var Root$2 = dt(Box)(({
  $visible
}) => lt`
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.2s;

    ${$visible && lt`
      opacity: 1;
      pointer-events: auto;
    `}
  `);
var TIMELINE_ITEM_I18N_KEY_MAPPING2 = {
  initial: "timeline.operation.created-initial",
  create: "timeline.operation.created",
  publish: "timeline.operation.published",
  editLive: "timeline.operation.edited-live",
  editDraft: "timeline.operation.edited-draft",
  unpublish: "timeline.operation.unpublished",
  discardDraft: "timeline.operation.draft-discarded",
  delete: "timeline.operation.deleted"
};
function UserAvatarStack(t0) {
  const $ = (0, import_react_compiler_runtime.c)(9), {
    maxLength,
    userIds,
    size,
    withTooltip: t1
  } = t0, withTooltip = t1 === void 0 ? true : t1;
  let t2;
  if ($[0] !== userIds || $[1] !== withTooltip) {
    let t32;
    $[3] !== withTooltip ? (t32 = (userId) => (0, import_jsx_runtime3.jsx)(UserAvatar, { user: userId, withTooltip }, userId), $[3] = withTooltip, $[4] = t32) : t32 = $[4], t2 = userIds.map(t32), $[0] = userIds, $[1] = withTooltip, $[2] = t2;
  } else
    t2 = $[2];
  let t3;
  return $[5] !== maxLength || $[6] !== size || $[7] !== t2 ? (t3 = (0, import_jsx_runtime3.jsx)(AvatarStack, { maxLength, size, children: t2 }), $[5] = maxLength, $[6] = size, $[7] = t2, $[8] = t3) : t3 = $[8], t3;
}
var IconBox = dt(Flex)((props) => {
  const theme = getTheme_v2(props.theme), color = props.$color;
  return lt`
    --card-icon-color: ${theme.color.avatar[color].fg};
    background-color: ${theme.color.avatar[color].bg};
    box-shadow: 0 0 0 1px var(--card-bg-color);

    position: absolute;
    width: ${theme.avatar.sizes[0].size}px;
    height: ${theme.avatar.sizes[0].size}px;
    right: -3px;
    bottom: -3px;
    border-radius: 50%;
  `;
});
var TIMELINE_ITEM_EVENT_TONE = {
  initial: "blue",
  create: "blue",
  publish: "green",
  editLive: "green",
  editDraft: "yellow",
  unpublish: "orange",
  discardDraft: "orange",
  delete: "red",
  withinSelection: "magenta"
};
var RELATIVE_TIME_OPTIONS$1 = {
  minimal: true,
  useTemporalPhrase: true
};
var NameSkeleton = dt(Skeleton)((props) => {
  const theme = getTheme_v2(props.theme);
  return lt`
    width: 6ch;
    height: ${theme.font.text.sizes[0].lineHeight}px;
  `;
});
var UserLine = (t0) => {
  const $ = (0, import_react_compiler_runtime.c)(10), {
    userId
  } = t0, [user, loading] = useUser(userId);
  let t1;
  $[0] !== loading || $[1] !== user ? (t1 = (0, import_jsx_runtime3.jsx)(Box, { children: loading || !user ? (0, import_jsx_runtime3.jsx)(AvatarSkeleton$1, { animated: true }) : (0, import_jsx_runtime3.jsx)(UserAvatar, { user }) }), $[0] = loading, $[1] = user, $[2] = t1) : t1 = $[2];
  let t2;
  $[3] !== loading || $[4] !== user ? (t2 = (0, import_jsx_runtime3.jsx)(Box, { children: loading || !(user == null ? void 0 : user.displayName) ? (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: (0, import_jsx_runtime3.jsx)(NameSkeleton, { animated: true }) }) : (0, import_jsx_runtime3.jsx)(Text, { muted: true, size: 1, children: user.displayName }) }), $[3] = loading, $[4] = user, $[5] = t2) : t2 = $[5];
  let t3;
  return $[6] !== t1 || $[7] !== t2 || $[8] !== userId ? (t3 = (0, import_jsx_runtime3.jsxs)(Flex, { align: "center", gap: 2, padding: 1, children: [
    t1,
    t2
  ] }, userId), $[6] = t1, $[7] = t2, $[8] = userId, $[9] = t3) : t3 = $[9], t3;
};
var TooltipContent = (t0) => {
  const $ = (0, import_react_compiler_runtime.c)(9), {
    collaborators
  } = t0, {
    t: t2
  } = useTranslation("studio");
  let t1;
  $[0] !== t2 ? (t1 = t2("timeline.changes.title"), $[0] = t2, $[1] = t1) : t1 = $[1];
  let t22;
  $[2] !== t1 ? (t22 = (0, import_jsx_runtime3.jsx)(Box, { padding: 1, paddingBottom: 2, children: (0, import_jsx_runtime3.jsx)(Text, { size: 1, weight: "medium", children: t1 }) }), $[2] = t1, $[3] = t22) : t22 = $[3];
  let t3;
  $[4] !== collaborators ? (t3 = collaborators.map(_temp$a), $[4] = collaborators, $[5] = t3) : t3 = $[5];
  let t4;
  return $[6] !== t22 || $[7] !== t3 ? (t4 = (0, import_jsx_runtime3.jsxs)(Stack, { paddingBottom: 1, children: [
    t22,
    t3
  ] }), $[6] = t22, $[7] = t3, $[8] = t4) : t4 = $[8], t4;
};
function TimelineItem(t0) {
  const $ = (0, import_react_compiler_runtime.c)(55), {
    chunk,
    isSelected,
    onSelect,
    collaborators,
    optionsMenu
  } = t0, {
    t: t2
  } = useTranslation("studio"), {
    type: type2,
    endTimestamp: timestamp
  } = chunk;
  let t1;
  $[0] !== type2 ? (t1 = getTimelineEventIconComponent(type2), $[0] = type2, $[1] = t1) : t1 = $[1];
  const IconComponent = t1;
  let t22;
  $[2] !== chunk.authors ? (t22 = Array.from(chunk.authors), $[2] = chunk.authors, $[3] = t22) : t22 = $[3];
  const authorUserIds = t22;
  let t3;
  $[4] !== collaborators ? (t3 = collaborators ? Array.from(collaborators) : [], $[4] = collaborators, $[5] = t3) : t3 = $[5];
  const collaboratorsUsersIds = t3, isSelectable = type2 !== "delete";
  let t4;
  $[6] === Symbol.for("react.memo_cache_sentinel") ? (t4 = {
    dateStyle: "medium",
    timeStyle: "short"
  }, $[6] = t4) : t4 = $[6];
  const dateFormat = useDateTimeFormat(t4);
  let t5;
  $[7] !== timestamp ? (t5 = new Date(timestamp), $[7] = timestamp, $[8] = t5) : t5 = $[8];
  const updatedTimeAgo = useRelativeTime(t5 || "", RELATIVE_TIME_OPTIONS$1);
  let t6, t7;
  if ($[9] !== dateFormat || $[10] !== timestamp) {
    const parsedDate = new Date(timestamp);
    t7 = dateFormat.format(parsedDate), $[9] = dateFormat, $[10] = timestamp, $[11] = t7;
  } else
    t7 = $[11];
  t6 = t7;
  const formattedTimestamp = t6;
  let t8;
  $[12] !== chunk || $[13] !== isSelectable || $[14] !== onSelect ? (t8 = (evt) => {
    evt.preventDefault(), evt.stopPropagation(), isSelectable && onSelect(chunk);
  }, $[12] = chunk, $[13] = isSelectable, $[14] = onSelect, $[15] = t8) : t8 = $[15];
  const handleClick = t8;
  let t9;
  $[16] === Symbol.for("react.memo_cache_sentinel") ? (t9 = {
    position: "relative"
  }, $[16] = t9) : t9 = $[16];
  let t10;
  $[17] !== authorUserIds ? (t10 = (0, import_jsx_runtime3.jsx)(UserAvatarStack, { maxLength: 3, userIds: authorUserIds, size: 2 }), $[17] = authorUserIds, $[18] = t10) : t10 = $[18];
  const t11 = TIMELINE_ITEM_EVENT_TONE[type2];
  let t12;
  $[19] !== IconComponent ? (t12 = IconComponent && (0, import_jsx_runtime3.jsx)(IconComponent, {}), $[19] = IconComponent, $[20] = t12) : t12 = $[20];
  let t13;
  $[21] !== t12 ? (t13 = (0, import_jsx_runtime3.jsx)(Text, { size: 0, children: t12 }), $[21] = t12, $[22] = t13) : t13 = $[22];
  let t14;
  $[23] !== t11 || $[24] !== t13 ? (t14 = (0, import_jsx_runtime3.jsx)(IconBox, { align: "center", justify: "center", $color: t11, children: t13 }), $[23] = t11, $[24] = t13, $[25] = t14) : t14 = $[25];
  let t15;
  $[26] !== t10 || $[27] !== t14 ? (t15 = (0, import_jsx_runtime3.jsxs)("div", { style: t9, children: [
    t10,
    t14
  ] }), $[26] = t10, $[27] = t14, $[28] = t15) : t15 = $[28];
  let t16;
  $[29] !== t2 || $[30] !== type2 ? (t16 = t2(TIMELINE_ITEM_I18N_KEY_MAPPING2[type2]) || (0, import_jsx_runtime3.jsx)("code", { children: type2 }), $[29] = t2, $[30] = type2, $[31] = t16) : t16 = $[31];
  let t17;
  $[32] !== t16 ? (t17 = (0, import_jsx_runtime3.jsx)(Text, { size: 1, weight: "medium", children: t16 }), $[32] = t16, $[33] = t17) : t17 = $[33];
  let t18;
  $[34] !== formattedTimestamp || $[35] !== timestamp || $[36] !== updatedTimeAgo ? (t18 = (0, import_jsx_runtime3.jsx)(Text, { as: "time", size: 1, muted: true, dateTime: timestamp, title: formattedTimestamp, children: updatedTimeAgo }), $[34] = formattedTimestamp, $[35] = timestamp, $[36] = updatedTimeAgo, $[37] = t18) : t18 = $[37];
  let t19;
  $[38] !== t17 || $[39] !== t18 ? (t19 = (0, import_jsx_runtime3.jsxs)(Stack, { space: 2, children: [
    t17,
    t18
  ] }), $[38] = t17, $[39] = t18, $[40] = t19) : t19 = $[40];
  let t20;
  $[41] !== collaboratorsUsersIds ? (t20 = collaboratorsUsersIds.length > 0 && (0, import_jsx_runtime3.jsx)(Flex, { flex: 1, justify: "flex-end", align: "center", children: (0, import_jsx_runtime3.jsx)(Tooltip, { placement: "top", content: (0, import_jsx_runtime3.jsx)(TooltipContent, { collaborators: collaboratorsUsersIds }), portal: true, children: (0, import_jsx_runtime3.jsx)(Box, { paddingLeft: 2, paddingY: 2, children: (0, import_jsx_runtime3.jsx)(UserAvatarStack, { maxLength: 3, userIds: collaboratorsUsersIds, size: 0, withTooltip: false }) }) }) }), $[41] = collaboratorsUsersIds, $[42] = t20) : t20 = $[42];
  let t21;
  $[43] !== t15 || $[44] !== t19 || $[45] !== t20 ? (t21 = (0, import_jsx_runtime3.jsxs)(Flex, { align: "center", gap: 3, children: [
    t15,
    t19,
    t20
  ] }), $[43] = t15, $[44] = t19, $[45] = t20, $[46] = t21) : t21 = $[46];
  let t222;
  $[47] !== chunk.id || $[48] !== handleClick || $[49] !== isSelected || $[50] !== t21 ? (t222 = (0, import_jsx_runtime3.jsx)(Card, { as: "button", onClick: handleClick, padding: 2, pressed: isSelected, radius: 2, "data-ui": "timelineItem", "data-testid": "timeline-item-button", "data-chunk-id": chunk.id, children: t21 }), $[47] = chunk.id, $[48] = handleClick, $[49] = isSelected, $[50] = t21, $[51] = t222) : t222 = $[51];
  let t23;
  return $[52] !== optionsMenu || $[53] !== t222 ? (t23 = (0, import_jsx_runtime3.jsxs)(Flex, { align: "center", gap: 1, children: [
    t222,
    optionsMenu
  ] }), $[52] = optionsMenu, $[53] = t222, $[54] = t23) : t23 = $[54], t23;
}
function _temp$a(userId) {
  return (0, import_jsx_runtime3.jsx)(UserLine, { userId }, userId);
}
var isNonPublishChunk = (chunk) => chunk.type !== "publish";
var isPublishChunk = (chunk) => chunk.type === "publish";
function getPreviousPublishAction(chunks) {
  let previousPublish = null;
  for (let index = chunks.length - 1; index >= 0; index--) {
    const chunk = chunks[index];
    if (isPublishChunk(chunk)) {
      previousPublish = chunk;
      break;
    }
    if (chunk.type !== "editDraft")
      break;
  }
  return previousPublish;
}
function addChunksMetadata(chunks) {
  const result = [];
  for (const chunk of chunks) {
    if (isPublishChunk(chunk)) {
      result.push({
        ...chunk,
        type: "publish",
        children: [],
        collaborators: /* @__PURE__ */ new Set()
        // Initialize the collaborators array
      });
      continue;
    }
    if (isNonPublishChunk(chunk)) {
      const previousPublish = getPreviousPublishAction(result);
      if (chunk.type === "editDraft" && (previousPublish == null ? void 0 : previousPublish.type) === "publish") {
        Array.from(chunk.authors).forEach((id2) => {
          previousPublish.collaborators.add(id2);
        }), previousPublish.children.push(chunk.id), result.push({
          ...chunk,
          parentId: previousPublish.id
        });
        continue;
      }
    }
    isNonPublishChunk(chunk) && result.push(chunk);
  }
  return result;
}
var TIMELINE_LIST_WRAPPER_ID$1 = "timeline-list-wrapper";
var Timeline = ({
  chunks,
  hasMoreChunks,
  lastChunk: selectedChunk,
  onLoadMore,
  onSelect,
  listMaxHeight = "calc(100vh - 280px)"
}) => {
  const [mounted, setMounted] = (0, import_react3.useState)(false), {
    t: t2
  } = useTranslation("studio"), selectedChunkId = selectedChunk == null ? void 0 : selectedChunk.id, chunksWithMetadata = (0, import_react3.useMemo)(() => addChunksMetadata(chunks), [chunks]), [expandedParents, setExpandedParents] = (0, import_react3.useState)(() => {
    if (selectedChunkId) {
      const selected = chunksWithMetadata.find((chunk) => chunk.id === selectedChunkId);
      if (selected && isNonPublishChunk(selected) && selected.parentId)
        return /* @__PURE__ */ new Set([selected.parentId]);
    }
    return /* @__PURE__ */ new Set();
  });
  (0, import_react3.useEffect)(() => {
    if (selectedChunkId) {
      const selected_0 = chunksWithMetadata.find((chunk_0) => chunk_0.id === selectedChunkId);
      if (selected_0 && isNonPublishChunk(selected_0) && selected_0.parentId) {
        const parentId = selected_0.parentId;
        setExpandedParents((prev) => {
          if (prev.has(parentId)) return prev;
          const next = new Set(prev);
          return next.add(parentId), next;
        });
      }
    }
  }, [chunksWithMetadata, selectedChunkId]);
  const filteredChunks = (0, import_react3.useMemo)(() => chunksWithMetadata.filter((chunk_1) => isPublishChunk(chunk_1) || !chunk_1.parentId ? true : expandedParents.has(chunk_1.parentId)), [chunksWithMetadata, expandedParents]);
  (0, import_react3.useEffect)(() => {
    filteredChunks.length < 16 && hasMoreChunks && onLoadMore();
  }, [filteredChunks, hasMoreChunks, onLoadMore]);
  const handleExpandParent = (0, import_react3.useCallback)((parentId_0) => () => setExpandedParents((prev_0) => {
    const next_0 = new Set(prev_0);
    return prev_0.has(parentId_0) ? next_0.delete(parentId_0) : next_0.add(parentId_0), next_0;
  }), []), selectedIndex = (0, import_react3.useMemo)(() => selectedChunkId ? filteredChunks.findIndex((chunk_2) => chunk_2.id === selectedChunkId) : -1, [selectedChunkId, filteredChunks]), handleSelectChunk = (0, import_react3.useCallback)((chunk_3) => {
    const timelineChunk = chunks.find((c2) => c2.id === chunk_3.id);
    timelineChunk ? onSelect(timelineChunk) : (console.error("TimelineItem: chunk not found"), onSelect(chunk_3));
  }, [chunks, onSelect]), renderItem = (0, import_react3.useCallback)((chunk_4, {
    activeIndex
  }) => (0, import_jsx_runtime3.jsxs)(Box, { paddingBottom: 1, paddingTop: activeIndex === 0 ? 1 : 0, paddingRight: 1, paddingLeft: isNonPublishChunk(chunk_4) && chunk_4.parentId ? 4 : 1, children: [
    (0, import_jsx_runtime3.jsx)(TimelineItem, { chunk: chunk_4, isSelected: selectedChunkId === chunk_4.id, onSelect: handleSelectChunk, collaborators: isPublishChunk(chunk_4) ? chunk_4.collaborators : void 0, optionsMenu: isPublishChunk(chunk_4) && chunk_4.children.length > 0 ? (0, import_jsx_runtime3.jsx)(ExpandableTimelineItemButton, { isExpanded: expandedParents.has(chunk_4.id), onExpand: handleExpandParent(chunk_4.id) }) : null }),
    activeIndex === filteredChunks.length - 1 && hasMoreChunks && (0, import_jsx_runtime3.jsx)(LoadingBlock, {})
  ] }), [expandedParents, filteredChunks.length, handleExpandParent, handleSelectChunk, hasMoreChunks, selectedChunkId]);
  return (0, import_react3.useEffect)(() => setMounted(true), []), (0, import_jsx_runtime3.jsxs)(
    Root$2,
    {
      $visible: !selectedIndex || mounted,
      "data-ui": "timeline",
      children: [
        filteredChunks.length === 0 && (0, import_jsx_runtime3.jsxs)(StackWrapper, { padding: 3, space: 3, children: [
          (0, import_jsx_runtime3.jsx)(Text, { size: 1, weight: "medium", children: t2("timeline.error.no-document-history-title") }),
          (0, import_jsx_runtime3.jsx)(Text, { muted: true, size: 1, children: t2("timeline.error.no-document-history-description") })
        ] }),
        filteredChunks.length > 0 && (0, import_jsx_runtime3.jsx)(ListWrapper, { direction: "column", $maxHeight: listMaxHeight, id: TIMELINE_LIST_WRAPPER_ID$1, children: (0, import_jsx_runtime3.jsx)(CommandList, { activeItemDataAttr: "data-hovered", ariaLabel: t2("timeline.list.aria-label"), autoFocus: "list", initialIndex: selectedIndex, initialScrollAlign: "center", itemHeight: 57, items: filteredChunks, onEndReached: onLoadMore, onEndReachedIndexOffset: 20, overscan: 5, renderItem, wrapAround: false }) })
      ]
    }
  );
};
Timeline.displayName = "Timeline";
function TimelineError(t0) {
  const $ = (0, import_react_compiler_runtime.c)(14), {
    versionError
  } = t0, {
    t: t2
  } = useTranslation("studio");
  let t1;
  $[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = (0, import_jsx_runtime3.jsx)(TextWithTone, { tone: "critical", children: (0, import_jsx_runtime3.jsx)(ErrorOutlineIcon, {}) }), $[0] = t1) : t1 = $[0];
  let t22;
  $[1] !== t2 || $[2] !== versionError ? (t22 = t2(versionError ? "timeline.error.load-document-changes-version-title" : "timeline.error.load-document-changes-title"), $[1] = t2, $[2] = versionError, $[3] = t22) : t22 = $[3];
  let t3;
  $[4] !== t22 ? (t3 = (0, import_jsx_runtime3.jsx)(TextWithTone, { size: 1, tone: "critical", weight: "medium", children: t22 }), $[4] = t22, $[5] = t3) : t3 = $[5];
  let t4;
  $[6] !== t2 || $[7] !== versionError ? (t4 = t2(versionError ? "timeline.error.load-document-changes-version-description" : "timeline.error.load-document-changes-description"), $[6] = t2, $[7] = versionError, $[8] = t4) : t4 = $[8];
  let t5;
  $[9] !== t4 ? (t5 = (0, import_jsx_runtime3.jsx)(TextWithTone, { size: 1, tone: "critical", children: t4 }), $[9] = t4, $[10] = t5) : t5 = $[10];
  let t6;
  return $[11] !== t3 || $[12] !== t5 ? (t6 = (0, import_jsx_runtime3.jsxs)(Flex, { align: "flex-start", gap: 3, padding: 4, children: [
    t1,
    (0, import_jsx_runtime3.jsxs)(Stack, { space: 4, children: [
      t3,
      t5
    ] })
  ] }), $[11] = t3, $[12] = t5, $[13] = t6) : t6 = $[13], t6;
}
var Root$1 = dt(Popover2)`
  overflow: hidden;
  overflow: clip;
`;
var TIMELINE_MENU_PORTAL$1 = "timeline-menu";
function TimelineMenu({
  chunk,
  mode,
  placement
}) {
  const {
    setTimelineRange,
    timelineError,
    ready,
    timelineStore
  } = useDocumentPane(), [open, setOpen] = (0, import_react3.useState)(false), [button, setButton] = (0, import_react3.useState)(null), [popoverRef, setPopoverRef] = (0, import_react3.useState)(null), toast = useToast(), chunks = useTimelineSelector(timelineStore, (state2) => state2.chunks), loading = useTimelineSelector(timelineStore, (state_0) => state_0.isLoading), hasMoreChunks = useTimelineSelector(timelineStore, (state_1) => state_1.hasMoreChunks), realRevChunk = useTimelineSelector(timelineStore, (state_2) => state_2.realRevChunk), sinceTime = useTimelineSelector(timelineStore, (state_3) => state_3.sinceTime), {
    t: t2
  } = useTranslation("studio"), handleOpen = (0, import_react3.useCallback)(() => {
    setOpen(true);
  }, []), handleClose = (0, import_react3.useCallback)(() => {
    setOpen(false);
  }, []), handleGlobalKeyDown = (0, import_react3.useCallback)((event) => {
    open && (event.key === "Escape" || event.key === "Tab") && (handleClose(), button == null ? void 0 : button.focus());
  }, [button, handleClose, open]);
  useGlobalKeyDown(handleGlobalKeyDown), useClickOutsideEvent(open && handleClose, () => [button, popoverRef]);
  const selectRev = (0, import_react3.useCallback)((revChunk) => {
    try {
      const [sinceId, revId] = (timelineStore == null ? void 0 : timelineStore.findRangeForRev(revChunk)) || [null, null];
      setTimelineRange(sinceId, revId);
    } catch (err) {
      toast.push({
        closable: true,
        description: err.message,
        status: "error",
        title: t2("timeline.error.unable-to-load-revision")
      });
    }
  }, [setTimelineRange, t2, timelineStore, toast]), selectSince = (0, import_react3.useCallback)((sinceChunk) => {
    try {
      const [sinceId_0, revId_0] = (timelineStore == null ? void 0 : timelineStore.findRangeForSince(sinceChunk)) || [null, null];
      setTimelineRange(sinceId_0, revId_0);
    } catch (err_0) {
      toast.push({
        closable: true,
        description: err_0.message,
        status: "error",
        title: t2("timeline.error.unable-to-load-revision")
      });
    }
  }, [setTimelineRange, t2, timelineStore, toast]), handleLoadMore = (0, import_react3.useCallback)(() => {
    loading || (timelineStore == null ? void 0 : timelineStore.loadMore());
  }, [loading, timelineStore]), content = (0, import_react3.useMemo)(() => {
    if (timelineError) return (0, import_jsx_runtime3.jsx)(TimelineError, {});
    if (mode === "rev")
      return (0, import_jsx_runtime3.jsx)(Timeline, { chunks, hasMoreChunks, lastChunk: realRevChunk, onLoadMore: handleLoadMore, onSelect: selectRev });
    const filteredChunks = realRevChunk ? chunks.filter((c2) => c2.index < realRevChunk.index) : chunks;
    return (0, import_jsx_runtime3.jsx)(Timeline, { chunks: filteredChunks, hasMoreChunks, lastChunk: sinceTime, onLoadMore: handleLoadMore, onSelect: selectSince });
  }, [chunks, handleLoadMore, hasMoreChunks, mode, realRevChunk, selectRev, selectSince, sinceTime, timelineError]), formatParams2 = {
    timestamp: {
      dateStyle: "medium",
      timeStyle: "short"
    }
  }, revLabel = chunk ? t2(TIMELINE_ITEM_I18N_KEY_MAPPING2[chunk.type], {
    context: "timestamp",
    timestamp: new Date(chunk == null ? void 0 : chunk.endTimestamp),
    formatParams: formatParams2
  }) : t2("timeline.latest-revision"), sinceLabel = chunk ? t2("timeline.since", {
    timestamp: new Date(chunk == null ? void 0 : chunk.endTimestamp),
    formatParams: formatParams2
  }) : t2("timeline.since-version-missing"), buttonLabel = mode === "rev" ? revLabel : sinceLabel;
  return (0, import_jsx_runtime3.jsx)(PortalProvider, { __unstable_elements: {
    [TIMELINE_MENU_PORTAL$1]: popoverRef
  }, children: (0, import_jsx_runtime3.jsx)(Root$1, { "data-testid": "timeline-menu", constrainSize: true, content: open && content, "data-ui": "versionMenu", open, placement, matchReferenceWidth: true, portal: true, ref: setPopoverRef, children: (0, import_jsx_runtime3.jsx)(Flex, { width: "fill", children: (0, import_jsx_runtime3.jsx)(Button2, { "data-testid": open ? "timeline-menu-close-button" : "timeline-menu-open-button", disabled: !ready, mode: "ghost", onClick: open ? handleClose : handleOpen, ref: setButton, selected: open, width: "fill", tooltipProps: null, justify: "space-between", style: {
    maxWidth: "100%"
  }, iconRight: ChevronDownIcon, text: ready ? buttonLabel : t2("timeline.loading-history") }) }) }) });
}
var StyledMotionPath = dt(motion.path)`
  transform-origin: center;
`;
var Circle = (props) => {
  const $ = (0, import_react_compiler_runtime.c)(2);
  let t0;
  return $[0] !== props ? (t0 = (0, import_jsx_runtime3.jsx)(motion.circle, { fill: "none", r: "8", cx: "12.5", cy: "12.5", strokeWidth: "1.2", ...props }), $[0] = props, $[1] = t0) : t0 = $[1], t0;
};
var Arrows = (props) => {
  const $ = (0, import_react_compiler_runtime.c)(2);
  let t0;
  return $[0] !== props ? (t0 = (0, import_jsx_runtime3.jsx)(StyledMotionPath, { fill: "none", d: "M14 17.5619L11.5 20.5L14.5 23.0619M11 7.43811L13.5 4.50001L10.5 1.93811", ...props }), $[0] = props, $[1] = t0) : t0 = $[1], t0;
};
var Checkmark = (props) => {
  const $ = (0, import_react_compiler_runtime.c)(2);
  let t0;
  return $[0] !== props ? (t0 = (0, import_jsx_runtime3.jsx)(motion.path, { d: "M9.5 12.1316L11.7414 14.5L16 10", ...props }), $[0] = props, $[1] = t0) : t0 = $[1], t0;
};
var rotateAnimation = mt`
  0% {
    transform: rotate(0);
  }
  100% {
    transform: rotate(360deg);
  }
`;
var RotateGroup = dt.g`
  transform-origin: center;

  &[data-rotate] {
    animation: ${rotateAnimation} 1s ease-in-out infinite;
  }
`;
var root = {
  syncing: {
    scale: 1,
    transition: {
      duration: 0
    }
  },
  saved: {
    scale: [1, 0.8, 1.2, 0.9, 1.1, 0.95, 1.05, 0.99, 1],
    transition: {
      duration: 0.5,
      delay: 0.2
    }
  },
  changes: {
    transition: {
      duration: 0
    }
  }
};
var circle = {
  syncing: {
    strokeDasharray: "0, 0, 23, 3, 23, 3",
    strokeDashoffset: 10,
    opacity: 1,
    transition: {
      duration: 0
    }
  },
  saved: {
    strokeDasharray: "0, 0, 23, 0, 23, 0",
    strokeDashoffset: 10,
    opacity: 1,
    transition: {
      duration: 0.2
    }
  },
  changes: {
    strokeDasharray: "0, 60, 23, 0, 23, 0",
    strokeDashoffset: 0,
    opacity: 0,
    transition: {
      duration: 0.5
    }
  }
};
var arrows = {
  syncing: {
    opacity: 1,
    transition: {
      duration: 0
    }
  },
  saved: {
    opacity: 0,
    transition: {
      duration: 0.2
    }
  },
  changes: {
    opacity: 0
  }
};
var checkmark = {
  syncing: {
    pathLength: 0,
    transition: {
      duration: 0
    }
  },
  saved: {
    pathLength: 1,
    transition: {
      delay: 0.4,
      duration: 0.3
    }
  },
  changes: {
    pathLength: 0,
    transition: {
      duration: 0.2
    }
  }
};
function AnimatedStatusIcon(props) {
  const $ = (0, import_react_compiler_runtime.c)(13), {
    status
  } = props;
  if (!status)
    return null;
  const t0 = status === "changes" ? void 0 : "";
  let t1, t2;
  $[0] !== status ? (t1 = (0, import_jsx_runtime3.jsx)(Arrows, { variants: arrows, initial: status, animate: status }), t2 = (0, import_jsx_runtime3.jsx)(Circle, { variants: circle, initial: status, animate: status }), $[0] = status, $[1] = t1, $[2] = t2) : (t1 = $[1], t2 = $[2]);
  let t3;
  $[3] !== t0 || $[4] !== t1 || $[5] !== t2 ? (t3 = (0, import_jsx_runtime3.jsxs)(RotateGroup, { "data-rotate": t0, children: [
    t1,
    t2
  ] }), $[3] = t0, $[4] = t1, $[5] = t2, $[6] = t3) : t3 = $[6];
  let t4;
  $[7] !== status ? (t4 = (0, import_jsx_runtime3.jsx)(Checkmark, { variants: checkmark, initial: status, animate: status }), $[7] = status, $[8] = t4) : t4 = $[8];
  let t5;
  return $[9] !== status || $[10] !== t3 || $[11] !== t4 ? (t5 = (0, import_jsx_runtime3.jsx)("svg", { width: "1em", height: "1em", viewBox: "0 0 25 25", fill: "none", stroke: "currentColor", strokeWidth: "1.2", "data-sanity-icon": "animated-status-icon", children: (0, import_jsx_runtime3.jsxs)(motion.g, { variants: root, initial: status, animate: status, children: [
    t3,
    t4
  ] }) }), $[9] = status, $[10] = t3, $[11] = t4, $[12] = t5) : t5 = $[12], t5;
}
var STATUS_DICTIONARY = {
  saved: {
    i18nKey: "status-bar.document-status-pulse.status.saved.text",
    tone: "positive"
  },
  syncing: {
    i18nKey: "status-bar.document-status-pulse.status.syncing.text",
    tone: "default"
  }
};
var DocumentStatusPulse = (props) => {
  const $ = (0, import_react_compiler_runtime.c)(13), {
    status
  } = props, {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  if (status !== "saved" && status !== "syncing")
    return null;
  const currentStatus = STATUS_DICTIONARY[status];
  let t0;
  $[0] !== status ? (t0 = (0, import_jsx_runtime3.jsx)(AnimatedStatusIcon, { status }), $[0] = status, $[1] = t0) : t0 = $[1];
  let t1;
  $[2] !== currentStatus.tone || $[3] !== t0 ? (t1 = (0, import_jsx_runtime3.jsx)(TextWithTone, { size: 1, tone: currentStatus.tone, children: t0 }), $[2] = currentStatus.tone, $[3] = t0, $[4] = t1) : t1 = $[4];
  let t22;
  $[5] !== currentStatus.i18nKey || $[6] !== t2 ? (t22 = t2(currentStatus.i18nKey), $[5] = currentStatus.i18nKey, $[6] = t2, $[7] = t22) : t22 = $[7];
  let t3;
  $[8] !== t22 ? (t3 = (0, import_jsx_runtime3.jsx)(Text, { muted: true, size: 1, children: t22 }), $[8] = t22, $[9] = t3) : t3 = $[9];
  let t4;
  return $[10] !== t1 || $[11] !== t3 ? (t4 = (0, import_jsx_runtime3.jsxs)(Flex, { align: "center", gap: 2, "data-testid": "pane-footer-document-status-pulse", children: [
    t1,
    t3
  ] }), $[10] = t1, $[11] = t3, $[12] = t4) : t4 = $[12], t4;
};
var RELATIVE_TIME_OPTIONS = {
  minimal: true,
  useTemporalPhrase: true
};
var MotionButton = motion.create(Button);
var MotionBox = motion.create(Box);
var ButtonSkeleton = () => {
  const $ = (0, import_react_compiler_runtime.c)(2);
  let t0;
  $[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = (0, import_jsx_runtime3.jsx)("div", { style: {
    margin: -5
  }, children: (0, import_jsx_runtime3.jsx)(AvatarSkeleton$1, { $size: 0, animated: true }) }), $[0] = t0) : t0 = $[0];
  let t1;
  return $[1] === Symbol.for("react.memo_cache_sentinel") ? (t1 = (0, import_jsx_runtime3.jsxs)(Flex, { align: "center", gap: 3, paddingLeft: 1, paddingRight: 2, paddingY: 2, children: [
    t0,
    (0, import_jsx_runtime3.jsx)(Skeleton, { animated: true, style: {
      width: "80px",
      height: "15px"
    }, radius: 2 })
  ] }), $[1] = t1) : t1 = $[1], t1;
};
var DocumentStatusButton = (t0) => {
  const $ = (0, import_react_compiler_runtime.c)(17), {
    author,
    translationKey,
    timestamp: t1
  } = t0, timestamp = t1 === void 0 ? "" : t1, {
    onHistoryOpen,
    inspector,
    onHistoryClose
  } = useDocumentPane(), {
    t: t2
  } = useTranslation(), relativeTime = useRelativeTime(timestamp, RELATIVE_TIME_OPTIONS);
  let t22, t3, t4;
  $[0] === Symbol.for("react.memo_cache_sentinel") ? (t22 = {
    opacity: 1
  }, t3 = {
    opacity: 0
  }, t4 = {
    opacity: 0
  }, $[0] = t22, $[1] = t3, $[2] = t4) : (t22 = $[0], t3 = $[1], t4 = $[2]);
  const t5 = (inspector == null ? void 0 : inspector.name) === HISTORY_INSPECTOR_NAME ? onHistoryClose : onHistoryOpen;
  let t6;
  $[3] !== author ? (t6 = author && (0, import_jsx_runtime3.jsx)("div", { style: {
    margin: -5
  }, children: (0, import_jsx_runtime3.jsx)(UserAvatar, { user: author, size: 0 }) }), $[3] = author, $[4] = t6) : t6 = $[4];
  let t7;
  $[5] !== t2 || $[6] !== translationKey ? (t7 = t2(translationKey), $[5] = t2, $[6] = translationKey, $[7] = t7) : t7 = $[7];
  let t8;
  $[8] !== relativeTime || $[9] !== t7 ? (t8 = (0, import_jsx_runtime3.jsxs)(Text, { muted: true, size: 1, children: [
    t7,
    " ",
    relativeTime
  ] }), $[8] = relativeTime, $[9] = t7, $[10] = t8) : t8 = $[10];
  let t9;
  $[11] !== t6 || $[12] !== t8 ? (t9 = (0, import_jsx_runtime3.jsxs)(Flex, { align: "center", flex: "none", gap: 3, children: [
    t6,
    t8
  ] }), $[11] = t6, $[12] = t8, $[13] = t9) : t9 = $[13];
  let t10;
  return $[14] !== t5 || $[15] !== t9 ? (t10 = (0, import_jsx_runtime3.jsx)(MotionButton, { "data-testid": "pane-footer-document-status", animate: t22, initial: t3, exit: t4, mode: "bleed", onClick: t5, padding: 2, muted: true, children: t9 }), $[14] = t5, $[15] = t9, $[16] = t10) : t10 = $[16], t10;
};
var FallbackStatus = () => {
  var _a, _b, _c, _d, _e;
  const {
    editState
  } = useDocumentPane(), {
    selectedPerspective
  } = usePerspective(), status = (0, import_react3.useMemo)(() => {
    var _a2, _b2, _c2, _d2, _e2, _f, _g;
    return isPublishedPerspective(selectedPerspective) && ((_a2 = editState == null ? void 0 : editState.published) == null ? void 0 : _a2._updatedAt) ? {
      translationKey: TIMELINE_ITEM_I18N_KEY_MAPPING.published.createDocumentVersion,
      timestamp: editState.published._updatedAt
    } : ((_b2 = editState == null ? void 0 : editState.version) == null ? void 0 : _b2._updatedAt) ? {
      translationKey: ((_c2 = editState == null ? void 0 : editState.version) == null ? void 0 : _c2._updatedAt) === ((_d2 = editState == null ? void 0 : editState.version) == null ? void 0 : _d2._createdAt) ? TIMELINE_ITEM_I18N_KEY_MAPPING.version.createDocumentVersion : TIMELINE_ITEM_I18N_KEY_MAPPING.version.editDocumentVersion,
      timestamp: editState.version._updatedAt
    } : ((_e2 = editState == null ? void 0 : editState.draft) == null ? void 0 : _e2._updatedAt) ? {
      translationKey: ((_f = editState == null ? void 0 : editState.draft) == null ? void 0 : _f._updatedAt) === ((_g = editState == null ? void 0 : editState.draft) == null ? void 0 : _g._createdAt) ? TIMELINE_ITEM_I18N_KEY_MAPPING.draft.createDocumentVersion : TIMELINE_ITEM_I18N_KEY_MAPPING.draft.editDocumentVersion,
      timestamp: editState.draft._updatedAt
    } : null;
  }, [selectedPerspective, (_a = editState == null ? void 0 : editState.published) == null ? void 0 : _a._updatedAt, (_b = editState == null ? void 0 : editState.version) == null ? void 0 : _b._updatedAt, (_c = editState == null ? void 0 : editState.version) == null ? void 0 : _c._createdAt, (_d = editState == null ? void 0 : editState.draft) == null ? void 0 : _d._updatedAt, (_e = editState == null ? void 0 : editState.draft) == null ? void 0 : _e._createdAt]);
  return status ? (0, import_jsx_runtime3.jsx)(DocumentStatusButton, { author: "", translationKey: status.translationKey, timestamp: status.timestamp }) : null;
};
var EventsStatus = () => {
  const $ = (0, import_react_compiler_runtime.c)(6), {
    events,
    loading
  } = useEvents(), event = events == null ? void 0 : events[0];
  if (!event && loading) {
    let t02;
    return $[0] === Symbol.for("react.memo_cache_sentinel") ? (t02 = (0, import_jsx_runtime3.jsx)(ButtonSkeleton, {}), $[0] = t02) : t02 = $[0], t02;
  }
  if (!event) {
    let t02;
    return $[1] === Symbol.for("react.memo_cache_sentinel") ? (t02 = (0, import_jsx_runtime3.jsx)(FallbackStatus, {}), $[1] = t02) : t02 = $[1], t02;
  }
  const t0 = TIMELINE_ITEM_I18N_KEY_MAPPING[event.documentVariantType][event.type];
  let t1;
  return $[2] !== event.author || $[3] !== event.timestamp || $[4] !== t0 ? (t1 = (0, import_jsx_runtime3.jsx)(DocumentStatusButton, { author: event.author, translationKey: t0, timestamp: event.timestamp }), $[2] = event.author, $[3] = event.timestamp, $[4] = t0, $[5] = t1) : t1 = $[5], t1;
};
var TimelineStatus = () => {
  const $ = (0, import_react_compiler_runtime.c)(8), {
    timelineStore
  } = useDocumentPane(), chunks = useTimelineSelector(timelineStore, _temp$9), loading = useTimelineSelector(timelineStore, _temp2$2), event = chunks == null ? void 0 : chunks[0];
  if (!event && loading) {
    let t02;
    return $[0] === Symbol.for("react.memo_cache_sentinel") ? (t02 = (0, import_jsx_runtime3.jsx)(ButtonSkeleton, {}), $[0] = t02) : t02 = $[0], t02;
  }
  if (!event) {
    let t02;
    return $[1] === Symbol.for("react.memo_cache_sentinel") ? (t02 = (0, import_jsx_runtime3.jsx)(FallbackStatus, {}), $[1] = t02) : t02 = $[1], t02;
  }
  let t0;
  $[2] !== event.authors ? (t0 = Array.from(event.authors), $[2] = event.authors, $[3] = t0) : t0 = $[3];
  const author = t0[0], t1 = TIMELINE_ITEM_I18N_KEY_MAPPING2[event.type];
  let t2;
  return $[4] !== author || $[5] !== event.endTimestamp || $[6] !== t1 ? (t2 = (0, import_jsx_runtime3.jsx)(DocumentStatusButton, { author, translationKey: t1, timestamp: event.endTimestamp }), $[4] = author, $[5] = event.endTimestamp, $[6] = t1, $[7] = t2) : t2 = $[7], t2;
};
var SYNCING_TIMEOUT = 1e3;
var SAVED_TIMEOUT = 3e3;
function DocumentStatusLine() {
  var _a, _b;
  const $ = (0, import_react_compiler_runtime.c)(18), {
    documentId,
    documentType,
    editState,
    value
  } = useDocumentPane(), [status, setStatus] = (0, import_react3.useState)(null), eventsEnabled = (_b = (_a = useSource().beta) == null ? void 0 : _a.eventsAPI) == null ? void 0 : _b.documents, syncState = useSyncState(documentId, documentType, editState == null ? void 0 : editState.release), lastUpdated = value == null ? void 0 : value._updatedAt;
  let t0;
  $[0] !== status || $[1] !== syncState.isSyncing ? (t0 = () => {
    if (status === "syncing" && !syncState.isSyncing) {
      const timerId = setTimeout(() => setStatus("saved"), SYNCING_TIMEOUT);
      return () => clearTimeout(timerId);
    }
    if (status === "saved") {
      const timerId_0 = setTimeout(() => setStatus(null), SAVED_TIMEOUT);
      return () => clearTimeout(timerId_0);
    }
  }, $[0] = status, $[1] = syncState.isSyncing, $[2] = t0) : t0 = $[2];
  let t1;
  $[3] !== lastUpdated || $[4] !== status || $[5] !== syncState.isSyncing ? (t1 = [status, lastUpdated, syncState.isSyncing], $[3] = lastUpdated, $[4] = status, $[5] = syncState.isSyncing, $[6] = t1) : t1 = $[6], (0, import_react3.useEffect)(t0, t1);
  let t2;
  $[7] === Symbol.for("react.memo_cache_sentinel") ? (t2 = () => {
    setStatus(null);
  }, $[7] = t2) : t2 = $[7];
  let t3;
  $[8] !== documentId ? (t3 = [documentId], $[8] = documentId, $[9] = t3) : t3 = $[9], (0, import_react3.useLayoutEffect)(t2, t3);
  let t4;
  $[10] !== syncState.isSyncing ? (t4 = () => {
    syncState.isSyncing && setStatus("syncing");
  }, $[10] = syncState.isSyncing, $[11] = t4) : t4 = $[11];
  let t5;
  $[12] !== lastUpdated || $[13] !== syncState.isSyncing ? (t5 = [syncState.isSyncing, lastUpdated], $[12] = lastUpdated, $[13] = syncState.isSyncing, $[14] = t5) : t5 = $[14], (0, import_react3.useLayoutEffect)(t4, t5);
  let t6;
  return $[15] !== eventsEnabled || $[16] !== status ? (t6 = (0, import_jsx_runtime3.jsx)(AnimatePresence, { children: status ? (0, import_jsx_runtime3.jsx)(MotionBox, { paddingLeft: 2, animate: {
    opacity: 1
  }, initial: {
    opacity: 0
  }, exit: {
    opacity: 0
  }, children: (0, import_jsx_runtime3.jsx)(DocumentStatusPulse, { status: status || void 0 }) }) : (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children: eventsEnabled ? (0, import_jsx_runtime3.jsx)(EventsStatus, {}) : (0, import_jsx_runtime3.jsx)(TimelineStatus, {}) }) }), $[15] = eventsEnabled, $[16] = status, $[17] = t6) : t6 = $[17], t6;
}
function _temp$9(state2) {
  return state2.chunks;
}
function _temp2$2(state_0) {
  return state_0.isLoading;
}
var StatusText = dt(Text)`
  color: var(--card-muted-fg-color);

  em {
    color: var(--card-fg-color);
    font-weight: 500;
    font-style: normal;
  }
`;
function RevisionStatusLine() {
  const $ = (0, import_react_compiler_runtime.c)(12), {
    displayed,
    revisionNotFound
  } = useDocumentPane(), {
    t: t2
  } = useTranslation(), date = (displayed == null ? void 0 : displayed._updatedAt) || (displayed == null ? void 0 : displayed._createdAt);
  let t0;
  $[0] !== date || $[1] !== t2 ? (t0 = date ? (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey: "document-status.revision-from", values: {
    date: format(new Date(date), "MMM d, yyyy '@' pp")
  } }) : null, $[0] = date, $[1] = t2, $[2] = t0) : t0 = $[2];
  let t1;
  $[3] !== t0 ? (t1 = {
    name: "revision",
    text: t0,
    tone: "caution"
  }, $[3] = t0, $[4] = t1) : t1 = $[4];
  const message = t1;
  let t22;
  $[5] === Symbol.for("react.memo_cache_sentinel") ? (t22 = (0, import_jsx_runtime3.jsx)(Box, { flex: "none", children: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: (0, import_jsx_runtime3.jsx)(RestoreIcon, {}) }) }), $[5] = t22) : t22 = $[5];
  let t3;
  $[6] !== message || $[7] !== revisionNotFound || $[8] !== t2 ? (t3 = revisionNotFound ? (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey: "document-status.revision-not-found" }) : message.text, $[6] = message, $[7] = revisionNotFound, $[8] = t2, $[9] = t3) : t3 = $[9];
  let t4;
  return $[10] !== t3 ? (t4 = (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children: (0, import_jsx_runtime3.jsxs)(Flex, { flex: 1, gap: 3, padding: 2, children: [
    t22,
    (0, import_jsx_runtime3.jsx)(Box, { flex: 1, children: (0, import_jsx_runtime3.jsx)(StatusText, { size: 1, textOverflow: "ellipsis", children: t3 }) })
  ] }) }), $[10] = t3, $[11] = t4) : t4 = $[11], t4;
}
function useResizeObserver(t0) {
  const $ = (0, import_react_compiler_runtime.c)(4), {
    element,
    onResize
  } = t0;
  let t1, t2;
  $[0] !== element || $[1] !== onResize ? (t1 = () => (element && resizeObserver.observe(element, onResize), () => {
    element && resizeObserver.unobserve(element);
  }), t2 = [element, onResize], $[0] = element, $[1] = onResize, $[2] = t1, $[3] = t2) : (t1 = $[2], t2 = $[3]), (0, import_react3.useLayoutEffect)(t1, t2);
}
var CONTAINER_BREAKPOINT = 480;
var AnimatedCard = motion.create(Card);
function DocumentStatusBar(props) {
  var _a;
  const $ = (0, import_react_compiler_runtime.c)(22), {
    actionsBoxRef,
    createLinkMetadata
  } = props, {
    editState,
    onChange: onDocumentChange,
    revisionNotFound
  } = useDocumentPane(), {
    params: t0
  } = usePaneRouter(), params = t0 === void 0 ? EMPTY_PARAMS$2 : t0, {
    selectedPerspective
  } = usePerspective(), {
    title
  } = useDocumentTitle(), CreateLinkedActions = (_a = useSanityCreateConfig().components) == null ? void 0 : _a.documentLinkedActions, showingRevision = !!params.rev, [collapsed, setCollapsed] = (0, import_react3.useState)(null), [rootElement, setRootElement] = (0, import_react3.useState)(null);
  let t1;
  $[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = (event) => {
    setCollapsed(event.contentRect.width < CONTAINER_BREAKPOINT);
  }, $[0] = t1) : t1 = $[0];
  const handleResize = t1;
  let t2;
  $[1] !== rootElement ? (t2 = {
    element: rootElement,
    onResize: handleResize
  }, $[1] = rootElement, $[2] = t2) : t2 = $[2], useResizeObserver(t2);
  let t3;
  bb0: {
    const isReady = !!((editState == null ? void 0 : editState.ready) && typeof collapsed == "boolean");
    if (selectedPerspective) {
      if (isPublishedPerspective(selectedPerspective)) {
        t3 = isReady && !!(editState == null ? void 0 : editState.published);
        break bb0;
      }
      if (isReleaseDocument(selectedPerspective)) {
        t3 = isReady && !!(editState == null ? void 0 : editState.version);
        break bb0;
      }
    }
    t3 = isReady;
  }
  const shouldRender = t3;
  let actions;
  if (createLinkMetadata && isSanityCreateLinked(createLinkMetadata) && CreateLinkedActions) {
    let t42;
    $[3] !== CreateLinkedActions || $[4] !== createLinkMetadata || $[5] !== onDocumentChange || $[6] !== title ? (t42 = (0, import_jsx_runtime3.jsx)(CreateLinkedActions, { metadata: createLinkMetadata, panelPortalElementId: DOCUMENT_PANEL_PORTAL_ELEMENT, onDocumentChange, documentTitle: title }), $[3] = CreateLinkedActions, $[4] = createLinkMetadata, $[5] = onDocumentChange, $[6] = title, $[7] = t42) : t42 = $[7], actions = t42;
  } else if (showingRevision) {
    let t42;
    $[8] === Symbol.for("react.memo_cache_sentinel") ? (t42 = (0, import_jsx_runtime3.jsx)(HistoryStatusBarActions, {}), $[8] = t42) : t42 = $[8], actions = t42;
  } else {
    let t42;
    $[9] === Symbol.for("react.memo_cache_sentinel") ? (t42 = (0, import_jsx_runtime3.jsx)(DocumentStatusBarActions, {}), $[9] = t42) : t42 = $[9], actions = t42;
  }
  if (showingRevision && revisionNotFound)
    return null;
  const t4 = showingRevision ? "revision" : "published";
  let t5;
  $[10] === Symbol.for("react.memo_cache_sentinel") ? (t5 = {
    opacity: 0.2
  }, $[10] = t5) : t5 = $[10];
  let t6;
  $[11] === Symbol.for("react.memo_cache_sentinel") ? (t6 = {
    opacity: 1,
    transition: {
      duration: 0.3
    }
  }, $[11] = t6) : t6 = $[11];
  const t7 = showingRevision ? "caution" : void 0;
  let t8;
  $[12] !== actions || $[13] !== actionsBoxRef || $[14] !== collapsed || $[15] !== shouldRender || $[16] !== showingRevision ? (t8 = shouldRender && (0, import_jsx_runtime3.jsxs)(Flex, { align: "stretch", gap: 1, justify: "space-between", paddingLeft: showingRevision ? 0 : 1, paddingRight: showingRevision ? 0 : 1, children: [
    (0, import_jsx_runtime3.jsxs)(Flex, { align: "center", flex: 1, gap: collapsed ? 2 : 3, wrap: "wrap", paddingRight: 3, children: [
      (0, import_jsx_runtime3.jsxs)(Flex, { align: "center", children: [
        showingRevision ? (0, import_jsx_runtime3.jsx)(RevisionStatusLine, {}) : (0, import_jsx_runtime3.jsx)(DocumentStatusLine, {}),
        (0, import_jsx_runtime3.jsx)(SpacerButton, {})
      ] }),
      (0, import_jsx_runtime3.jsx)(DocumentBadges, {})
    ] }),
    (0, import_jsx_runtime3.jsxs)(Flex, { align: "flex-start", justify: "flex-end", ref: actionsBoxRef, style: {
      flexShrink: 0,
      marginLeft: "auto"
    }, children: [
      (0, import_jsx_runtime3.jsx)(SpacerButton, {}),
      actions
    ] })
  ] }), $[12] = actions, $[13] = actionsBoxRef, $[14] = collapsed, $[15] = shouldRender, $[16] = showingRevision, $[17] = t8) : t8 = $[17];
  let t9;
  return $[18] !== t4 || $[19] !== t7 || $[20] !== t8 ? (t9 = (0, import_jsx_runtime3.jsx)(AnimatedCard, { initial: t5, animate: t6, tone: t7, radius: 3, ref: setRootElement, sizing: "border", padding: 2, children: t8 }, t4), $[18] = t4, $[19] = t7, $[20] = t8, $[21] = t9) : t9 = $[21], t9;
}
var KeyboardShortcutResponder = (0, import_react3.memo)(function(props) {
  const $ = (0, import_react_compiler_runtime.c)(26);
  let actionsBoxElement, activeIndex, children, id2, onActionStart, onKeyDown, rest, rootRef, states, t0;
  $[0] !== props ? ({
    actionsBoxElement,
    activeIndex,
    as: t0,
    children,
    id: id2,
    onActionStart,
    onKeyDown,
    rootRef,
    states,
    ...rest
  } = props, $[0] = props, $[1] = actionsBoxElement, $[2] = activeIndex, $[3] = children, $[4] = id2, $[5] = onActionStart, $[6] = onKeyDown, $[7] = rest, $[8] = rootRef, $[9] = states, $[10] = t0) : (actionsBoxElement = $[1], activeIndex = $[2], children = $[3], id2 = $[4], onActionStart = $[5], onKeyDown = $[6], rest = $[7], rootRef = $[8], states = $[9], t0 = $[10]);
  const As = t0 === void 0 ? "div" : t0, activeAction = states[activeIndex];
  let t1;
  $[11] !== onActionStart || $[12] !== onKeyDown || $[13] !== states ? (t1 = (event) => {
    const matchingStates = states.filter((state2) => state2.shortcut && isHotkey(state2.shortcut, event)), matchingState = matchingStates[0];
    if (matchingStates.length > 1 && console.warn(`Keyboard shortcut conflict: More than one document action matches the shortcut "${matchingState.shortcut}"`), matchingState && !matchingState.disabled && matchingState.onHandle) {
      event.preventDefault(), matchingState.onHandle(), onActionStart(states.indexOf(matchingState));
      return;
    }
    onKeyDown && onKeyDown(event);
  }, $[11] = onActionStart, $[12] = onKeyDown, $[13] = states, $[14] = t1) : t1 = $[14];
  const handleKeyDown = t1;
  let t2;
  $[15] !== actionsBoxElement || $[16] !== activeAction ? (t2 = activeAction && activeAction.dialog && (0, import_jsx_runtime3.jsx)(LegacyLayerProvider, { zOffset: "paneFooter", children: (0, import_jsx_runtime3.jsx)(ActionStateDialog, { dialog: activeAction.dialog, referenceElement: actionsBoxElement }) }), $[15] = actionsBoxElement, $[16] = activeAction, $[17] = t2) : t2 = $[17];
  let t3;
  return $[18] !== As || $[19] !== children || $[20] !== handleKeyDown || $[21] !== id2 || $[22] !== rest || $[23] !== rootRef || $[24] !== t2 ? (t3 = (0, import_jsx_runtime3.jsxs)(As, { id: id2, onKeyDown: handleKeyDown, tabIndex: -1, ...rest, ref: rootRef, children: [
    children,
    t2
  ] }), $[18] = As, $[19] = children, $[20] = handleKeyDown, $[21] = id2, $[22] = rest, $[23] = rootRef, $[24] = t2, $[25] = t3) : t3 = $[25], t3;
});
var DocumentActionShortcuts = (0, import_react3.memo)((props) => {
  const $ = (0, import_react_compiler_runtime.c)(20);
  let actionsBoxElement, children, rest, t0;
  $[0] !== props ? ({
    actionsBoxElement,
    as: t0,
    children,
    ...rest
  } = props, $[0] = props, $[1] = actionsBoxElement, $[2] = children, $[3] = rest, $[4] = t0) : (actionsBoxElement = $[1], children = $[2], rest = $[3], t0 = $[4]);
  const as = t0 === void 0 ? "div" : t0, {
    actions,
    editState,
    isInitialValueLoading,
    revisionId
  } = useDocumentPane(), [activeIndex, setActiveIndex] = (0, import_react3.useState)(-1);
  let t1;
  $[5] === Symbol.for("react.memo_cache_sentinel") ? (t1 = (idx) => {
    setActiveIndex(idx);
  }, $[5] = t1) : t1 = $[5];
  const onActionStart = t1;
  let t2, t3;
  $[6] !== editState || $[7] !== isInitialValueLoading || $[8] !== revisionId ? (t3 = editState && {
    ...editState,
    onComplete: _temp$8,
    revision: revisionId || void 0,
    initialValueResolved: !isInitialValueLoading
  }, $[6] = editState, $[7] = isInitialValueLoading, $[8] = revisionId, $[9] = t3) : t3 = $[9], t2 = t3;
  const actionProps = t2;
  let t4;
  $[10] !== actionsBoxElement || $[11] !== activeIndex || $[12] !== as || $[13] !== children || $[14] !== rest ? (t4 = (t52) => {
    const {
      states
    } = t52;
    return (0, import_jsx_runtime3.jsx)(KeyboardShortcutResponder, { ...rest, activeIndex, actionsBoxElement, as, onActionStart, states, children });
  }, $[10] = actionsBoxElement, $[11] = activeIndex, $[12] = as, $[13] = children, $[14] = rest, $[15] = t4) : t4 = $[15];
  const renderDocumentActionShortcuts = t4;
  if (!actionProps || !actions)
    return null;
  let t5;
  return $[16] !== actionProps || $[17] !== actions || $[18] !== renderDocumentActionShortcuts ? (t5 = (0, import_jsx_runtime3.jsx)(RenderActionCollectionState, { actionProps, actions, children: renderDocumentActionShortcuts }), $[16] = actionProps, $[17] = actions, $[18] = renderDocumentActionShortcuts, $[19] = t5) : t5 = $[19], t5;
});
DocumentActionShortcuts.displayName = "Memo(DocumentActionShortcuts)";
function _temp$8() {
}
function getInspectorItems({
  currentInspector,
  hasValue,
  inspectors: inspectors2,
  inspectorMenuItems
}) {
  return inspectors2.map((inspector, index) => {
    var _a;
    const menuItem = inspectorMenuItems[index];
    return !menuItem || menuItem.hidden ? null : {
      action: `${INSPECT_ACTION_PREFIX}${inspector.name}`,
      group: menuItem.showAsAction ? void 0 : "inspectors",
      icon: menuItem.icon,
      disabled: !hasValue,
      selected: (currentInspector == null ? void 0 : currentInspector.name) === inspector.name,
      shortcut: (_a = menuItem.hotkeys) == null ? void 0 : _a.join("+"),
      showAsAction: menuItem.showAsAction,
      title: menuItem.title,
      tone: menuItem.tone
    };
  }).filter(Boolean);
}
function getInspectItem({
  hasValue,
  t: t2
}) {
  return {
    action: "inspect",
    group: "inspectors",
    title: t2("document-inspector.menu-item.title"),
    icon: JsonIcon,
    disabled: !hasValue,
    shortcut: "Ctrl+Alt+I"
  };
}
function getCompareVersionsItem({
  documentIdStack,
  t: t2
}) {
  const disabled = typeof (documentIdStack == null ? void 0 : documentIdStack.previousId) > "u" && {
    reason: t2("compare-versions.menu-item.disabled-reason")
  };
  return {
    action: "compareVersions",
    group: "inspectors",
    title: t2("compare-versions.menu-item.title"),
    icon: TransferIcon,
    disabled
  };
}
function getProductionPreviewItem({
  previewUrl,
  t: t2
}) {
  return previewUrl ? {
    action: "production-preview",
    group: "links",
    title: t2("production-preview.menu-item.title"),
    icon: EarthAmericasIcon,
    shortcut: "Ctrl+Alt+O"
  } : null;
}
function getMenuItems(params) {
  const inspectorItems = getInspectorItems(params), items2 = [
    // Get production preview item
    getProductionPreviewItem(params),
    getCompareVersionsItem(params)
  ].filter(Boolean);
  return [
    // Always present document menu item to copy current url to clipboard
    {
      action: "copy-document-url",
      showAsAction: true,
      title: params.t("action.copy-document-url.label"),
      icon: LinkIcon
    },
    ...inspectorItems,
    // TODO: convert to inspector or document view?
    getInspectItem(params),
    ...items2
  ];
}
function DocumentLayoutError(props) {
  const $ = (0, import_react_compiler_runtime.c)(23), {
    documentType,
    value,
    currentMinWidth,
    paneKey,
    minWidth
  } = props, {
    t: t2
  } = useTranslation();
  let t0;
  $[0] !== documentType ? (t0 = {
    documentType
  }, $[0] = documentType, $[1] = t0) : t0 = $[1];
  let t1;
  $[2] !== t2 || $[3] !== t0 ? (t1 = (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey: "panes.document-pane.document-unknown-type.title", values: t0 }), $[2] = t2, $[3] = t0, $[4] = t1) : t1 = $[4];
  let t22;
  $[5] !== documentType || $[6] !== t2 ? (t22 = documentType && (0, import_jsx_runtime3.jsx)(Text, { as: "p", children: (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey: "panes.document-pane.document-unknown-type.text", values: {
    documentType
  } }) }), $[5] = documentType, $[6] = t2, $[7] = t22) : t22 = $[7];
  let t3;
  $[8] !== documentType || $[9] !== t2 ? (t3 = !documentType && (0, import_jsx_runtime3.jsx)(Text, { as: "p", children: t2("panes.document-pane.document-unknown-type.without-schema.text") }), $[8] = documentType, $[9] = t2, $[10] = t3) : t3 = $[10];
  let t4;
  $[11] !== value ? (t4 = isDev && value && (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [
    (0, import_jsx_runtime3.jsx)(Text, { as: "p", children: "Here is the JSON representation of the document:" }),
    (0, import_jsx_runtime3.jsx)(Card, { padding: 3, overflow: "auto", radius: 2, shadow: 1, tone: "inherit", children: (0, import_jsx_runtime3.jsx)(Code, { language: "json", size: [1, 1, 2], children: JSON.stringify(value, null, 2) }) })
  ] }), $[11] = value, $[12] = t4) : t4 = $[12];
  let t5;
  $[13] !== t22 || $[14] !== t3 || $[15] !== t4 ? (t5 = (0, import_jsx_runtime3.jsxs)(Stack, { space: 4, children: [
    t22,
    t3,
    t4
  ] }), $[13] = t22, $[14] = t3, $[15] = t4, $[16] = t5) : t5 = $[16];
  let t6;
  return $[17] !== currentMinWidth || $[18] !== minWidth || $[19] !== paneKey || $[20] !== t1 || $[21] !== t5 ? (t6 = (0, import_jsx_runtime3.jsx)(ErrorPane, { currentMinWidth, flex: 2.5, minWidth, paneKey, title: t1, tone: "caution", children: t5 }), $[17] = currentMinWidth, $[18] = minWidth, $[19] = paneKey, $[20] = t1, $[21] = t5, $[22] = t6) : t6 = $[22], t6;
}
var DIALOG_PROVIDER_POSITION$1 = [
  // We use the `position: fixed` for dialogs on narrower screens (first two media breakpoints).
  "fixed",
  "fixed",
  // And we use the `position: absolute` strategy (within panes) on wide screens.
  "absolute"
];
function DocumentLayoutFooter(t0) {
  const $ = (0, import_react_compiler_runtime.c)(16), {
    documentPanelPortalElement,
    setFooterElement,
    setActionsBoxElement
  } = t0, zOffsets = useZIndex(), {
    value
  } = useDocumentPane();
  let t1, t2;
  $[0] !== documentPanelPortalElement ? (t2 = {
    [DOCUMENT_PANEL_PORTAL_ELEMENT]: documentPanelPortalElement
  }, $[0] = documentPanelPortalElement, $[1] = t2) : t2 = $[1], t1 = t2;
  const portalElements = t1;
  let t3;
  $[2] !== value ? (t3 = getSanityCreateLinkMetadata(value), $[2] = value, $[3] = t3) : t3 = $[3];
  const createLinkMetadata = t3;
  let t4;
  $[4] !== createLinkMetadata || $[5] !== setActionsBoxElement ? (t4 = (0, import_jsx_runtime3.jsx)(TooltipDelayGroupProvider, { children: (0, import_jsx_runtime3.jsx)(DocumentStatusBar, { actionsBoxRef: setActionsBoxElement, createLinkMetadata }) }), $[4] = createLinkMetadata, $[5] = setActionsBoxElement, $[6] = t4) : t4 = $[6];
  let t5;
  $[7] !== setFooterElement || $[8] !== t4 ? (t5 = (0, import_jsx_runtime3.jsx)(PaneFooter, { ref: setFooterElement, padding: 1, children: t4 }), $[7] = setFooterElement, $[8] = t4, $[9] = t5) : t5 = $[9];
  let t6;
  $[10] !== t5 || $[11] !== zOffsets.portal ? (t6 = (0, import_jsx_runtime3.jsx)(DialogProvider, { position: DIALOG_PROVIDER_POSITION$1, zOffset: zOffsets.portal, children: t5 }), $[10] = t5, $[11] = zOffsets.portal, $[12] = t6) : t6 = $[12];
  let t7;
  return $[13] !== portalElements || $[14] !== t6 ? (t7 = (0, import_jsx_runtime3.jsx)(PortalProvider, { __unstable_elements: portalElements, children: t6 }), $[13] = portalElements, $[14] = t6, $[15] = t7) : t7 = $[15], t7;
}
var EMPTY_ARRAY = [];
var DIALOG_PROVIDER_POSITION = [
  // We use the `position: fixed` for dialogs on narrower screens (first two media breakpoints).
  "fixed",
  "fixed",
  // And we use the `position: absolute` strategy (within panes) on wide screens.
  "absolute"
];
var StyledChangeConnectorRoot = dt(ChangeConnectorRoot)`
  flex: 1;
  display: flex;
  flex-direction: column;
  min-height: 0;
  min-width: 0;
`;
function DocumentLayout() {
  const {
    changesOpen,
    displayed,
    documentId,
    documentType,
    editState,
    fieldActions,
    focusPath,
    inspectOpen,
    inspector,
    inspectors: inspectors2,
    onFocus,
    onHistoryOpen,
    onMenuAction,
    onPathOpen,
    paneKey,
    schemaType,
    value,
    previewUrl
  } = useDocumentPane(), {
    params: paneParams
  } = usePaneRouter(), {
    features
  } = useStructureTool(), {
    t: t2
  } = useTranslation(structureLocaleNamespace), {
    collapsed: layoutCollapsed
  } = usePaneLayout(), zOffsets = useZIndex(), [rootElement, setRootElement] = (0, import_react3.useState)(null), [footerElement, setFooterElement] = (0, import_react3.useState)(null), [headerElement, setHeaderElement] = (0, import_react3.useState)(null), [actionsBoxElement, setActionsBoxElement] = (0, import_react3.useState)(null), [documentPanelPortalElement, setDocumentPanelPortalElement] = (0, import_react3.useState)(null);
  useGlobalCopyPasteElementHandler({
    element: rootElement,
    focusPath,
    value
  });
  const [inspectorMenuItems, setInspectorMenuItems] = (0, import_react3.useState)([]), [rootFieldActionNodes, setRootFieldActionNodes] = (0, import_react3.useState)([]), footerRect = useElementRect(footerElement), headerRect = useElementRect(headerElement), footerHeight = footerRect == null ? void 0 : footerRect.height, headerHeight = headerRect == null ? void 0 : headerRect.height, currentMinWidth = DOCUMENT_PANEL_INITIAL_MIN_WIDTH + (inspector ? DOCUMENT_INSPECTOR_MIN_WIDTH : 0), minWidth = DOCUMENT_PANEL_MIN_WIDTH + (inspector ? DOCUMENT_INSPECTOR_MIN_WIDTH : 0), currentInspector = (0, import_react3.useMemo)(() => inspectors2 == null ? void 0 : inspectors2.find((i) => i.name === (inspector == null ? void 0 : inspector.name)), [inspectors2, inspector == null ? void 0 : inspector.name]), documentIdStack = useDocumentIdStack({
    displayed,
    documentId,
    editState
  }), hasValue = !!value, menuItems = (0, import_react3.useMemo)(() => getMenuItems({
    currentInspector,
    features,
    hasValue,
    inspectorMenuItems,
    inspectors: inspectors2,
    previewUrl,
    documentIdStack,
    t: t2
  }), [currentInspector, documentIdStack, features, hasValue, inspectorMenuItems, inspectors2, previewUrl, t2]), handleKeyUp = (0, import_react3.useCallback)((event) => {
    for (const item of menuItems)
      if (item.shortcut && isHotkey(item.shortcut, event)) {
        event.preventDefault(), event.stopPropagation(), onMenuAction(item);
        return;
      }
  }, [onMenuAction, menuItems]), onConnectorSetFocus = (0, import_react3.useCallback)((path) => {
    onPathOpen(path), onFocus(path);
  }, [onPathOpen, onFocus]);
  return schemaType ? (0, import_jsx_runtime3.jsxs)(GetFormValueProvider, { value, children: [
    inspectors2.length > 0 && (0, import_jsx_runtime3.jsx)(DocumentInspectorMenuItemsResolver, { documentId, documentType, inspectors: inspectors2, onMenuItems: setInspectorMenuItems }),
    fieldActions.length > 0 && schemaType && (0, import_jsx_runtime3.jsx)(FieldActionsResolver, { actions: fieldActions, documentId, documentType, onActions: setRootFieldActionNodes, path: EMPTY_ARRAY, schemaType }),
    (0, import_jsx_runtime3.jsx)(FieldActionsProvider, { actions: rootFieldActionNodes, path: EMPTY_ARRAY, children: (0, import_jsx_runtime3.jsxs)(DocumentActionShortcuts, { actionsBoxElement, as: Pane, currentMinWidth, "data-testid": "document-pane", flex: 2.5, id: paneKey, minWidth, onKeyUp: handleKeyUp, rootRef: setRootElement, children: [
      (0, import_jsx_runtime3.jsx)(DocumentPanelHeader, { ref: setHeaderElement, menuItems }),
      (0, import_jsx_runtime3.jsx)(DialogProvider, { position: DIALOG_PROVIDER_POSITION, zOffset: zOffsets.paneDialog, children: (0, import_jsx_runtime3.jsx)(Flex, { direction: "column", flex: 1, height: layoutCollapsed ? void 0 : "fill", children: (0, import_jsx_runtime3.jsx)(StyledChangeConnectorRoot, { "data-testid": "change-connector-root", isReviewChangesOpen: changesOpen && (paneParams == null ? void 0 : paneParams.changesInspectorTab) === "review", onOpenReviewChanges: onHistoryOpen, onSetFocus: onConnectorSetFocus, children: (0, import_jsx_runtime3.jsx)(DocumentPanel, { footerHeight: footerHeight || null, headerHeight: headerHeight || null, isInspectOpen: inspectOpen, rootElement, setDocumentPanelPortalElement, footer: (0, import_jsx_runtime3.jsx)(DocumentLayoutFooter, { documentPanelPortalElement, setFooterElement, setActionsBoxElement }) }) }) }) }),
      (0, import_jsx_runtime3.jsx)(DocumentOperationResults, {})
    ] }) })
  ] }) : (0, import_jsx_runtime3.jsx)(DocumentLayoutError, { currentMinWidth, documentType, minWidth, paneKey, value });
}
function useDocumentLayoutComponent() {
  const $ = (0, import_react_compiler_runtime.c)(1);
  let t0;
  return $[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = {
    pick: pickDocumentLayoutComponent,
    defaultComponent: DocumentLayout
  }, $[0] = t0) : t0 = $[0], useMiddlewareComponents(t0);
}
var DocumentURLCopied = defineEvent({
  name: "DocumentURLCopied",
  version: 1,
  description: "User copied document URL to clipboard"
});
function getInitialValueTemplateOpts(templates, opts) {
  const payload = opts.panePayload || {}, structureNodeTemplate = opts.templateName;
  opts.urlTemplate && structureNodeTemplate && structureNodeTemplate !== opts.urlTemplate && console.warn(`Conflicting templates: URL says "${opts.urlTemplate}", structure node says "${structureNodeTemplate}". Using "${structureNodeTemplate}".`);
  const template = structureNodeTemplate || opts.urlTemplate, typeTemplates = templates.filter((t2) => t2.schemaType === opts.documentType), templateParams = {
    ...opts.templateParams,
    ...typeof payload == "object" ? payload || {} : {}
  };
  let templateName = template;
  return !template && typeTemplates.length === 1 && (templateName = typeTemplates[0].id), {
    templateName,
    templateParams
  };
}
var useDocumentPaneInitialValue = (t0) => {
  const $ = (0, import_react_compiler_runtime.c)(13), {
    paneOptions,
    documentType,
    documentId,
    params
  } = t0, templates = useTemplates(), paneRouter = usePaneRouter(), panePayload = useUnique(paneRouter.payload);
  let t1, t2;
  $[0] !== documentType || $[1] !== paneOptions.template || $[2] !== paneOptions.templateParameters || $[3] !== panePayload || $[4] !== params.template || $[5] !== templates ? (t2 = getInitialValueTemplateOpts(templates, {
    documentType,
    templateName: paneOptions.template,
    templateParams: paneOptions.templateParameters,
    panePayload,
    urlTemplate: params.template
  }), $[0] = documentType, $[1] = paneOptions.template, $[2] = paneOptions.templateParameters, $[3] = panePayload, $[4] = params.template, $[5] = templates, $[6] = t2) : t2 = $[6], t1 = t2;
  const {
    templateName,
    templateParams
  } = t1;
  let t3;
  $[7] !== documentId || $[8] !== documentType || $[9] !== params.version || $[10] !== templateName || $[11] !== templateParams ? (t3 = {
    documentId,
    documentType,
    templateName,
    templateParams,
    version: params.version
  }, $[7] = documentId, $[8] = documentType, $[9] = params.version, $[10] = templateName, $[11] = templateParams, $[12] = t3) : t3 = $[12];
  const initialValueRaw = useInitialValue(t3);
  return useUnique(initialValueRaw);
};
function useDocumentPaneInspector({
  documentId,
  documentType,
  params,
  setParams
}) {
  const {
    features
  } = useStructureTool(), inspectorsResolver = useSource().document.inspectors, inspectors2 = (0, import_react3.useMemo)(() => inspectorsResolver({
    documentId,
    documentType
  }), [documentId, documentType, inspectorsResolver]), [inspectorName, setInspectorName] = (0, import_react3.useState)(() => params.inspect || null), inspectParamRef = (0, import_react3.useRef)(params.inspect);
  (0, import_react3.useEffect)(() => {
    inspectParamRef.current !== params.inspect && (inspectParamRef.current = params.inspect, setInspectorName(params.inspect || null));
  }, [params.inspect]);
  const currentInspector = (0, import_react3.useMemo)(() => inspectors2 == null ? void 0 : inspectors2.find((i) => i.name === inspectorName), [inspectors2, inspectorName]), historyInspector = (0, import_react3.useMemo)(() => inspectors2.find((i_0) => i_0.name === HISTORY_INSPECTOR_NAME), [inspectors2]), changesOpen = (currentInspector == null ? void 0 : currentInspector.name) === HISTORY_INSPECTOR_NAME, closeInspector = (0, import_react3.useCallback)((closeInspectorName) => {
    var _a, _b;
    const inspector = closeInspectorName && inspectors2.find((i_1) => i_1.name === closeInspectorName);
    if (closeInspectorName && !inspector) {
      console.warn(`No inspector named "${closeInspectorName}"`);
      return;
    }
    if (currentInspector) {
      if (inspector) {
        const result = ((_a = inspector.onClose) == null ? void 0 : _a.call(inspector, {
          params
        })) ?? {
          params
        };
        setInspectorName(null), inspectParamRef.current = void 0, setParams({
          ...result.params,
          inspect: void 0
        });
        return;
      }
      if (currentInspector) {
        const result_0 = ((_b = currentInspector.onClose) == null ? void 0 : _b.call(currentInspector, {
          params
        })) ?? {
          params
        };
        setInspectorName(null), inspectParamRef.current = void 0, setParams({
          ...result_0.params,
          inspect: void 0
        });
      }
    }
  }, [currentInspector, inspectors2, params, setParams]), openInspector = (0, import_react3.useCallback)((nextInspectorName, paneParams) => {
    var _a, _b;
    const nextInspector = inspectors2.find((i_2) => i_2.name === nextInspectorName);
    if (!nextInspector) {
      console.warn(`No inspector named "${nextInspectorName}"`);
      return;
    }
    if ((currentInspector == null ? void 0 : currentInspector.name) === nextInspector.name) {
      setParams({
        ...params,
        ...paneParams,
        inspect: nextInspector.name
      });
      return;
    }
    let currentParams = params;
    currentInspector && (currentParams = (((_a = nextInspector.onClose) == null ? void 0 : _a.call(nextInspector, {
      params: currentParams
    })) ?? {
      params: currentParams
    }).params);
    const result_1 = ((_b = nextInspector.onOpen) == null ? void 0 : _b.call(nextInspector, {
      params: currentParams
    })) ?? {
      params: currentParams
    };
    setInspectorName(nextInspector.name), inspectParamRef.current = nextInspector.name, setParams({
      ...result_1.params,
      ...paneParams,
      inspect: nextInspector.name
    });
  }, [currentInspector, inspectors2, params, setParams]), handleHistoryClose = (0, import_react3.useCallback)(() => {
    historyInspector && closeInspector(historyInspector.name);
  }, [closeInspector, historyInspector]), handleHistoryOpen = (0, import_react3.useCallback)(() => {
    features.reviewChanges && historyInspector && openInspector(historyInspector.name, {
      changesInspectorTab: "review"
    });
  }, [features.reviewChanges, openInspector, historyInspector]), inspectOpen = params.inspect === "on", toggleLegacyInspect = (0, import_react3.useCallback)((toggle = !inspectOpen) => {
    setParams(toggle ? {
      ...params,
      inspect: "on"
    } : (0, import_omit.default)(params, "inspect"));
  }, [inspectOpen, params, setParams]), handleLegacyInspectClose = (0, import_react3.useCallback)(() => toggleLegacyInspect(false), [toggleLegacyInspect]), handleInspectorAction = (0, import_react3.useCallback)((item) => {
    if (item.action === "inspect")
      return toggleLegacyInspect(true), true;
    if (typeof item.action != "string") return false;
    const nextInspectorName_0 = item.action.slice(INSPECT_ACTION_PREFIX.length), nextInspector_0 = inspectors2.find((i_3) => i_3.name === nextInspectorName_0);
    return nextInspector_0 ? (nextInspector_0.name === inspectorName ? closeInspector(nextInspector_0.name) : openInspector(nextInspector_0.name), true) : false;
  }, [closeInspector, inspectorName, inspectors2, openInspector, toggleLegacyInspect]);
  return {
    changesOpen,
    currentInspector,
    inspectors: inspectors2,
    closeInspector,
    openInspector,
    handleHistoryClose,
    handleHistoryOpen,
    handleInspectorAction,
    // TODO: Deprecate this legacy inspect toggle it's used to render the <InspectDialog /> component
    handleLegacyInspectClose,
    inspectOpen
  };
}
var isSanityDocument2 = (value) => isRecord$4(value) && typeof value._id == "string" && typeof value._type == "string";
function usePreviewUrl(value) {
  const $ = (0, import_react_compiler_runtime.c)(6), {
    resolveProductionUrl
  } = useSource().document;
  let t0, t1;
  $[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = new BehaviorSubject(void 0), $[0] = t1) : t1 = $[0], t0 = t1;
  const subject = t0;
  let t2, t3;
  $[1] !== value ? (t2 = () => {
    subject.next(value);
  }, t3 = [subject, value], $[1] = value, $[2] = t2, $[3] = t3) : (t2 = $[2], t3 = $[3]), (0, import_react3.useEffect)(t2, t3);
  let t4, t5;
  return $[4] !== resolveProductionUrl ? (t5 = subject.asObservable().pipe(distinctUntilChanged(), debounceTime(500), switchMap((document2) => isSanityDocument2(document2) ? from(resolveProductionUrl({
    document: document2
  })) : of(void 0)), distinctUntilChanged(), catchError(_temp$7)), $[4] = resolveProductionUrl, $[5] = t5) : t5 = $[5], t4 = t5, useObservable(t4);
}
function _temp$7(err) {
  const message = isRecord$4(err) && typeof err.message == "string" ? err.message : "Unknown error";
  throw new Error(`An error was thrown while trying to get your preview url: ${message}`);
}
var DocumentPaneProvider = (0, import_react3.memo)((props) => {
  const {
    children,
    index,
    pane: pane2,
    paneKey,
    onFocusPath,
    forcedVersion,
    historyStore
  } = props, {
    store: timelineStore,
    error: timelineError,
    ready: timelineReady,
    revisionDocument,
    onOlderRevision,
    sinceDocument,
    isPristine,
    revisionId,
    lastNonDeletedRevId
  } = historyStore, schema = useSchema(), {
    setDocumentMeta
  } = useCopyPaste(), {
    document: {
      actions: documentActions2,
      badges: documentBadges2,
      unstable_fieldActions: fieldActionsResolver,
      unstable_languageFilter: languageFilterResolver,
      drafts: {
        enabled: draftsEnabled
      }
    }
  } = useSource(), telemetry = useTelemetry(), paneRouter = usePaneRouter(), setPaneParams = paneRouter.setParams, {
    push: pushToast
  } = useToast(), {
    options,
    menuItemGroups = DEFAULT_MENU_ITEM_GROUPS,
    title = null,
    views: viewsProp = []
  } = pane2, paneOptions = useUnique(options), documentIdRaw = paneOptions.id, documentId = getPublishedId(documentIdRaw), documentType = options.type, params = useUnique(paneRouter.params) || EMPTY_PARAMS$2, {
    buildStudioUrl
  } = useStudioUrl(), perspective = usePerspective(), {
    document: {
      drafts: {
        enabled: isDraftModelEnabled
      }
    }
  } = useWorkspace(), {
    selectedReleaseId,
    selectedPerspectiveName
  } = (0, import_react3.useMemo)(() => forcedVersion || {
    selectedPerspectiveName: perspective.selectedPerspectiveName,
    selectedReleaseId: perspective.selectedReleaseId
  }, [forcedVersion, perspective.selectedPerspectiveName, perspective.selectedReleaseId]), diffViewRouter = useDiffViewRouter(), initialValue = useDocumentPaneInitialValue({
    paneOptions,
    documentId,
    documentType,
    params
  }), isInitialValueLoading = initialValue.loading, {
    changesOpen,
    currentInspector,
    inspectors: inspectors2,
    closeInspector,
    openInspector,
    handleHistoryClose,
    handleHistoryOpen,
    handleInspectorAction,
    inspectOpen,
    handleLegacyInspectClose
  } = useDocumentPaneInspector({
    documentId,
    documentType,
    params,
    setParams: setPaneParams
  }), [isDeleting, setIsDeleting] = (0, import_react3.useState)(false), getIsDeleted = (0, import_react3.useCallback)((editState) => timelineReady ? !(editState == null ? void 0 : editState.draft) && !(editState == null ? void 0 : editState.published) && !(editState == null ? void 0 : editState.version) && !isPristine : false, [timelineReady, isPristine]), getComparisonValue = (0, import_react3.useCallback)((editState_0) => changesOpen ? sinceDocument || (editState_0 == null ? void 0 : editState_0.published) : (editState_0 == null ? void 0 : editState_0.published) || null, [changesOpen, sinceDocument]), schemaType = schema.get(documentType), getIsReadOnly = (0, import_react3.useCallback)((editState_1) => {
    const isDeleted = getIsDeleted(editState_1);
    return !!params.rev || isDeleting || isDeleted || !isPerspectiveWriteable({
      selectedPerspective: perspective.selectedPerspective,
      isDraftModelEnabled,
      schemaType
    }).result;
  }, [getIsDeleted, isDeleting, isDraftModelEnabled, params.rev, perspective.selectedPerspective, schemaType]), getDisplayed = (0, import_react3.useCallback)((value) => onOlderRevision ? revisionDocument || {
    _id: value._id,
    _type: value._type
  } : value, [onOlderRevision, revisionDocument]), {
    editState: editState_2,
    connectionState,
    focusPath,
    onChange,
    validation,
    ready: formReady,
    value: value_0,
    formState,
    permissions,
    onPathOpen,
    isPermissionsLoading,
    formStateRef,
    onProgrammaticFocus,
    collapsedFieldSets,
    collapsedPaths,
    onBlur,
    onFocus,
    onSetActiveFieldGroup,
    onSetCollapsedPath,
    onSetCollapsedFieldSet,
    openPath
  } = useDocumentForm({
    documentType,
    documentId,
    initialValue,
    comparisonValue: getComparisonValue,
    releaseId: selectedReleaseId,
    selectedPerspectiveName,
    initialFocusPath: params.path ? fromString(params.path) : EMPTY_ARRAY$B,
    readOnly: getIsReadOnly,
    onFocusPath,
    getFormDocumentValue: getDisplayed
  }), getDocumentVersionType = (0, import_react3.useCallback)(() => {
    let version;
    switch (true) {
      case !!params.rev:
        version = "revision";
        break;
      case (selectedReleaseId && isVersionId(value_0._id)):
        version = "version";
        break;
      case selectedPerspectiveName === "published":
        version = "published";
        break;
      case draftsEnabled:
        version = "draft";
        break;
      default:
        version = "published";
    }
    return version;
  }, [params.rev, selectedReleaseId, value_0._id, selectedPerspectiveName, draftsEnabled]), actionsPerspective = (0, import_react3.useMemo)(() => getDocumentVersionType(), [getDocumentVersionType]), documentActionsProps = (0, import_react3.useMemo)(() => ({
    schemaType: documentType,
    documentId,
    versionType: actionsPerspective,
    releaseId: selectedReleaseId
  }), [documentType, documentId, actionsPerspective, selectedReleaseId]), actions = (0, import_react3.useMemo)(() => documentActions2(documentActionsProps), [documentActions2, documentActionsProps]), badges = (0, import_react3.useMemo)(() => documentBadges2({
    schemaType: documentType,
    documentId
  }), [documentBadges2, documentId, documentType]), languageFilter = (0, import_react3.useMemo)(() => languageFilterResolver({
    schemaType: documentType,
    documentId
  }), [documentId, documentType, languageFilterResolver]), views2 = useUnique(viewsProp), activeViewId = params.view || views2[0] && views2[0].id || null, previewUrl = usePreviewUrl(value_0), {
    t: t2
  } = useTranslation(structureLocaleNamespace), fieldActions = (0, import_react3.useMemo)(() => schemaType ? fieldActionsResolver({
    documentId,
    documentType,
    schemaType
  }) : [], [documentId, documentType, fieldActionsResolver, schemaType]), ready = formReady && (!params.rev || timelineReady || !!timelineError), displayed = (0, import_react3.useMemo)(() => getDisplayed(value_0), [getDisplayed, value_0]), {
    previousId
  } = useDocumentIdStack({
    displayed,
    documentId,
    editState: editState_2
  }), setTimelineRange = (0, import_react3.useCallback)((newSince, newRev) => {
    setPaneParams({
      ...params,
      since: newSince,
      rev: newRev || void 0
    });
  }, [params, setPaneParams]), handlePaneClose = (0, import_react3.useCallback)(() => paneRouter.closeCurrent(), [paneRouter]), handlePaneSplit = (0, import_react3.useCallback)(() => paneRouter.duplicateCurrent(), [paneRouter]), handleMenuAction = (0, import_react3.useCallback)(async (item) => {
    if (item.action === "production-preview" && previewUrl)
      return window.open(previewUrl), true;
    if (item.action === "copy-document-url" && navigator) {
      telemetry.log(DocumentURLCopied);
      const copyUrl = buildStudioUrl({
        coreUi: (url) => `${url}/intent/edit/id=${documentId};type=${documentType}`
      });
      return await navigator.clipboard.writeText(copyUrl), pushToast({
        id: "copy-document-url",
        status: "info",
        title: t2("panes.document-operation-results.operation-success_copy-url")
      }), true;
    }
    return item.action === "reviewChanges" ? (handleHistoryOpen(), true) : ((item.action === "inspect" || typeof item.action == "string" && item.action.startsWith(INSPECT_ACTION_PREFIX)) && handleInspectorAction(item), item.action === "compareVersions" && typeof previousId < "u" ? (diffViewRouter.navigateDiffView({
      mode: "version",
      previousDocument: {
        type: documentType,
        id: previousId
      },
      nextDocument: {
        type: documentType,
        id: value_0._id
      }
    }), true) : false);
  }, [previewUrl, previousId, telemetry, buildStudioUrl, pushToast, t2, documentId, documentType, handleHistoryOpen, handleInspectorAction, diffViewRouter, value_0._id]);
  (0, import_react3.useEffect)(() => {
    setDocumentMeta({
      documentId,
      documentType,
      schemaType,
      onChange
    });
  }, [documentId, documentType, schemaType, onChange, setDocumentMeta]);
  const compareValue = (0, import_react3.useMemo)(() => getComparisonValue(editState_2), [editState_2, getComparisonValue]), isDeleted_0 = (0, import_react3.useMemo)(() => getIsDeleted(editState_2), [editState_2, getIsDeleted]), revisionNotFound = onOlderRevision && !revisionDocument, documentPane = (0, import_react3.useMemo)(() => ({
    actions,
    activeViewId,
    badges,
    changesOpen,
    closeInspector,
    collapsedFieldSets,
    collapsedPaths,
    compareValue,
    connectionState,
    displayed,
    documentId,
    documentIdRaw,
    documentType,
    editState: editState_2,
    fieldActions,
    focusPath,
    inspector: currentInspector || null,
    inspectors: inspectors2,
    onBlur,
    onChange,
    onFocus,
    onPathOpen,
    onHistoryClose: handleHistoryClose,
    onHistoryOpen: handleHistoryOpen,
    onInspectClose: handleLegacyInspectClose,
    onMenuAction: handleMenuAction,
    onPaneClose: handlePaneClose,
    onPaneSplit: handlePaneSplit,
    onSetActiveFieldGroup,
    onSetCollapsedPath,
    onSetCollapsedFieldSet,
    openInspector,
    openPath,
    index,
    inspectOpen,
    validation,
    menuItemGroups: menuItemGroups || [],
    paneKey,
    previewUrl,
    ready,
    schemaType,
    isPermissionsLoading,
    isInitialValueLoading,
    permissions,
    setTimelineRange,
    setIsDeleting,
    isDeleting,
    isDeleted: isDeleted_0,
    timelineError,
    timelineStore,
    title,
    value: value_0,
    selectedReleaseId,
    views: views2,
    formState,
    unstable_languageFilter: languageFilter,
    revisionId,
    revisionNotFound,
    lastNonDeletedRevId
  }), [actions, activeViewId, badges, changesOpen, closeInspector, collapsedFieldSets, collapsedPaths, compareValue, connectionState, displayed, documentId, documentIdRaw, documentType, editState_2, fieldActions, focusPath, currentInspector, inspectors2, onBlur, onChange, onFocus, onPathOpen, handleHistoryClose, handleHistoryOpen, handleLegacyInspectClose, handleMenuAction, handlePaneClose, handlePaneSplit, onSetActiveFieldGroup, onSetCollapsedPath, onSetCollapsedFieldSet, openInspector, openPath, index, inspectOpen, validation, menuItemGroups, paneKey, previewUrl, ready, schemaType, isPermissionsLoading, isInitialValueLoading, permissions, setTimelineRange, isDeleting, isDeleted_0, timelineError, timelineStore, title, value_0, selectedReleaseId, views2, formState, languageFilter, revisionId, revisionNotFound, lastNonDeletedRevId]);
  return (0, import_react3.useEffect)(() => {
    var _a;
    if (ready && params.path) {
      const {
        path,
        ...restParams
      } = params, pathFromUrl = resolveKeyedPath((_a = formStateRef.current) == null ? void 0 : _a.value, fromString(path));
      onProgrammaticFocus(pathFromUrl), paneRouter.setParams(restParams);
    }
  }, [formStateRef, onProgrammaticFocus, paneRouter, params, ready]), (0, import_jsx_runtime3.jsx)(DocumentPaneContext.Provider, { value: documentPane, children });
});
DocumentPaneProvider.displayName = "Memo(DocumentPaneProvider)";
var DocumentEventsPane = (props) => {
  var _a, _b, _c, _d, _e, _f;
  const $ = (0, import_react_compiler_runtime.c)(37), {
    params: t0
  } = usePaneRouter(), params = t0 === void 0 ? EMPTY_PARAMS$2 : t0, options = usePaneOptions(props.pane.options, params), schema = useSchema(), documentType = options.type, liveEdit = !!((_a = schema.get(options.type)) == null ? void 0 : _a.liveEdit), {
    selectedPerspectiveName,
    selectedReleaseId,
    selectedPerspective
  } = usePerspective(), {
    data: archivedReleases
  } = useArchivedReleases();
  let t1;
  $[0] !== options.id ? (t1 = getPublishedId(options.id), $[0] = options.id, $[1] = t1) : t1 = $[1];
  const editState = useEditState(t1, documentType, "default", selectedReleaseId), showingPublishedOnDraft = liveEdit && selectedPerspective === "drafts" && !(editState == null ? void 0 : editState.draft), {
    rev,
    since
  } = params, historyVersion = params.historyVersion;
  let t2;
  bb0: {
    if (showingPublishedOnDraft) {
      let t32;
      $[2] !== options.id ? (t32 = getPublishedId(options.id), $[2] = options.id, $[3] = t32) : t32 = $[3], t2 = t32;
      break bb0;
    }
    if (historyVersion && archivedReleases.some((release) => getReleaseIdFromReleaseDocumentId(release._id) === historyVersion)) {
      let t32;
      $[4] !== historyVersion || $[5] !== options.id ? (t32 = getVersionId(options.id, historyVersion), $[4] = historyVersion, $[5] = options.id, $[6] = t32) : t32 = $[6], t2 = t32;
      break bb0;
    }
    if (typeof selectedPerspectiveName > "u") {
      let t32;
      $[7] !== options.id ? (t32 = getDraftId(options.id), $[7] = options.id, $[8] = t32) : t32 = $[8], t2 = t32;
      break bb0;
    }
    if (selectedPerspectiveName === "published") {
      let t32;
      $[9] !== options.id ? (t32 = getPublishedId(options.id), $[9] = options.id, $[10] = t32) : t32 = $[10], t2 = t32;
      break bb0;
    }
    if (selectedReleaseId) {
      let t32;
      $[11] !== options.id || $[12] !== selectedPerspectiveName ? (t32 = getVersionId(options.id, selectedPerspectiveName), $[11] = options.id, $[12] = selectedPerspectiveName, $[13] = t32) : t32 = $[13], t2 = t32;
      break bb0;
    }
    t2 = options.id;
  }
  const documentId = t2;
  let t3;
  $[14] !== documentId || $[15] !== options.type || $[16] !== rev || $[17] !== since ? (t3 = {
    documentId,
    documentType: options.type,
    rev,
    since
  }, $[14] = documentId, $[15] = options.type, $[16] = rev, $[17] = since, $[18] = t3) : t3 = $[18];
  const eventsStore = useEventsStore(t3);
  let t4;
  const t5 = eventsStore.error, t6 = ((_b = eventsStore.revision) == null ? void 0 : _b.revisionId) || null, t7 = !!(rev && !((_c = eventsStore.revision) == null ? void 0 : _c.loading)), t8 = ((_d = eventsStore.revision) == null ? void 0 : _d.document) || null, t9 = ((_e = eventsStore.sinceRevision) == null ? void 0 : _e.document) || null, t10 = !eventsStore.loading, t11 = eventsStore.events.length === 0;
  let t12;
  $[19] !== eventsStore.events ? (t12 = ((_f = eventsStore.events.find(_temp$6)) == null ? void 0 : _f.id) || null, $[19] = eventsStore.events, $[20] = t12) : t12 = $[20];
  let t13;
  $[21] !== eventsStore.error || $[22] !== t10 || $[23] !== t11 || $[24] !== t12 || $[25] !== t6 || $[26] !== t7 || $[27] !== t8 || $[28] !== t9 ? (t13 = {
    error: t5,
    revisionId: t6,
    onOlderRevision: t7,
    revisionDocument: t8,
    sinceDocument: t9,
    ready: t10,
    isPristine: t11,
    lastNonDeletedRevId: t12
  }, $[21] = eventsStore.error, $[22] = t10, $[23] = t11, $[24] = t12, $[25] = t6, $[26] = t7, $[27] = t8, $[28] = t9, $[29] = t13) : t13 = $[29], t4 = t13;
  const historyStoreProps = t4;
  let t14;
  t14 = eventsStore;
  const value = t14;
  let t15;
  $[30] !== historyStoreProps || $[31] !== historyVersion || $[32] !== props ? (t15 = historyVersion ? (0, import_jsx_runtime3.jsx)(PerspectiveProvider, { selectedPerspectiveName: historyVersion, excludedPerspectives: EMPTY_ARRAY$B, children: (0, import_jsx_runtime3.jsx)(DocumentPaneProvider, { ...props, historyStore: historyStoreProps }) }) : (0, import_jsx_runtime3.jsx)(DocumentPaneProvider, { ...props, historyStore: historyStoreProps }), $[30] = historyStoreProps, $[31] = historyVersion, $[32] = props, $[33] = t15) : t15 = $[33];
  let t16;
  return $[34] !== t15 || $[35] !== value ? (t16 = (0, import_jsx_runtime3.jsx)(EventsProvider, { value, children: t15 }), $[34] = t15, $[35] = value, $[36] = t16) : t16 = $[36], t16;
};
function _temp$6(e2) {
  return !isDeleteDocumentGroupEvent(e2) && !isDeleteDocumentVersionEvent(e2);
}
var DocumentPaneWithLegacyTimelineStore = (props) => {
  const $ = (0, import_react_compiler_runtime.c)(20), {
    pane: pane2
  } = props, paneRouter = usePaneRouter(), options = usePaneOptions(pane2.options, paneRouter.params), params = paneRouter.params || EMPTY_PARAMS$2, [timelineError, setTimelineError] = (0, import_react3.useState)(null);
  let t0;
  $[0] !== options.id ? (t0 = getPublishedId(options.id), $[0] = options.id, $[1] = t0) : t0 = $[1];
  let t1;
  $[2] !== options.type || $[3] !== params.rev || $[4] !== params.since || $[5] !== t0 ? (t1 = {
    documentId: t0,
    documentType: options.type,
    onError: setTimelineError,
    rev: params.rev,
    since: params.since
  }, $[2] = options.type, $[3] = params.rev, $[4] = params.since, $[5] = t0, $[6] = t1) : t1 = $[6];
  const store = useTimelineStore(t1), onOlderRevision = useTimelineSelector(store, _temp$5), revTime = useTimelineSelector(store, _temp2$1), sinceAttributes = useTimelineSelector(store, _temp3$1), timelineDisplayed = useTimelineSelector(store, _temp4$1), timelineReady = useTimelineSelector(store, _temp5$1), isPristine = useTimelineSelector(store, _temp6$1), lastNonDeletedRevId = useTimelineSelector(store, _temp7);
  let t2;
  const t3 = (revTime == null ? void 0 : revTime.id) || null, t4 = timelineDisplayed, t5 = sinceAttributes, t6 = !!isPristine;
  let t7;
  $[7] !== lastNonDeletedRevId || $[8] !== onOlderRevision || $[9] !== store || $[10] !== t3 || $[11] !== t4 || $[12] !== t5 || $[13] !== t6 || $[14] !== timelineError || $[15] !== timelineReady ? (t7 = {
    store,
    error: timelineError,
    revisionId: t3,
    onOlderRevision,
    revisionDocument: t4,
    sinceDocument: t5,
    ready: timelineReady,
    isPristine: t6,
    lastNonDeletedRevId
  }, $[7] = lastNonDeletedRevId, $[8] = onOlderRevision, $[9] = store, $[10] = t3, $[11] = t4, $[12] = t5, $[13] = t6, $[14] = timelineError, $[15] = timelineReady, $[16] = t7) : t7 = $[16], t2 = t7;
  const historyStoreProps = t2;
  let t8;
  return $[17] !== historyStoreProps || $[18] !== props ? (t8 = (0, import_jsx_runtime3.jsx)(DocumentPaneProvider, { ...props, historyStore: historyStoreProps }), $[17] = historyStoreProps, $[18] = props, $[19] = t8) : t8 = $[19], t8;
};
function _temp$5(state2) {
  return state2.onOlderRevision;
}
function _temp2$1(state_0) {
  return state_0.revTime;
}
function _temp3$1(state_1) {
  return state_1.sinceAttributes;
}
function _temp4$1(state_2) {
  return state_2.timelineDisplayed;
}
function _temp5$1(state_3) {
  return state_3.timelineReady;
}
function _temp6$1(state_4) {
  return state_4.isPristine;
}
function _temp7(state_5) {
  return state_5.lastNonDeletedRevId;
}
var DocumentPaneProviderWrapper = (0, import_react3.memo)((props) => {
  var _a, _b;
  const $ = (0, import_react_compiler_runtime.c)(4);
  if ((_b = (_a = useSource().beta) == null ? void 0 : _a.eventsAPI) == null ? void 0 : _b.documents) {
    let t02;
    return $[0] !== props ? (t02 = (0, import_jsx_runtime3.jsx)(DocumentEventsPane, { ...props }), $[0] = props, $[1] = t02) : t02 = $[1], t02;
  }
  let t0;
  return $[2] !== props ? (t0 = (0, import_jsx_runtime3.jsx)(DocumentPaneWithLegacyTimelineStore, { ...props }), $[2] = props, $[3] = t0) : t0 = $[3], t0;
});
DocumentPaneProviderWrapper.displayName = "Memo(DocumentPaneProviderWrapper)";
function useResetHistoryParams() {
  const $ = (0, import_react_compiler_runtime.c)(10), {
    params: t0,
    setParams
  } = usePaneRouter(), params = t0 === void 0 ? EMPTY_PARAMS$2 : t0, {
    selectedPerspectiveName
  } = usePerspective(), isMounted = (0, import_react3.useRef)(false);
  let t1;
  $[0] !== params || $[1] !== setParams ? (t1 = (_perspective) => {
    ["since", "historyVersion", "rev", "preserveRev"].some((param) => params[param]) && setParams({
      ...params,
      rev: params.preserveRev === "true" ? params.rev : void 0,
      preserveRev: void 0,
      since: void 0,
      historyVersion: void 0
    });
  }, $[0] = params, $[1] = setParams, $[2] = t1) : t1 = $[2];
  const updateHistoryParams = useEffectEvent(t1);
  let t2;
  $[3] !== selectedPerspectiveName || $[4] !== updateHistoryParams ? (t2 = () => {
    isMounted.current && updateHistoryParams(selectedPerspectiveName);
  }, $[3] = selectedPerspectiveName, $[4] = updateHistoryParams, $[5] = t2) : t2 = $[5];
  let t3;
  $[6] !== selectedPerspectiveName ? (t3 = [selectedPerspectiveName], $[6] = selectedPerspectiveName, $[7] = t3) : t3 = $[7], (0, import_react3.useEffect)(t2, t3);
  let t4, t5;
  $[8] === Symbol.for("react.memo_cache_sentinel") ? (t4 = () => (isMounted.current = true, () => {
    isMounted.current = false;
  }), t5 = [], $[8] = t4, $[9] = t5) : (t4 = $[8], t5 = $[9]), (0, import_react3.useEffect)(t4, t5);
}
var DocumentPane = (0, import_react3.memo)(function(props) {
  const $ = (0, import_react_compiler_runtime.c)(5), {
    name: parentSourceName
  } = useSource(), t0 = props.pane.source || parentSourceName;
  let t1;
  $[0] !== props ? (t1 = (0, import_jsx_runtime3.jsx)(CopyPasteProvider, { children: (0, import_jsx_runtime3.jsx)(DocumentPaneInner, { ...props }) }), $[0] = props, $[1] = t1) : t1 = $[1];
  let t2;
  return $[2] !== t0 || $[3] !== t1 ? (t2 = (0, import_jsx_runtime3.jsx)(SourceProvider, { name: t0, children: t1 }), $[2] = t0, $[3] = t1, $[4] = t2) : t2 = $[4], t2;
});
function DocumentPaneInner(props) {
  var _a;
  const $ = (0, import_react_compiler_runtime.c)(57), {
    pane: pane2,
    paneKey
  } = props, {
    resolveNewDocumentOptions
  } = useSource().document, {
    selectedPerspectiveName
  } = usePerspective(), paneRouter = usePaneRouter(), options = usePaneOptions(pane2.options, paneRouter.params), {
    documentType,
    isLoaded: isDocumentLoaded
  } = useDocumentType(options.id, options.type);
  useResetHistoryParams();
  const DocumentLayout2 = useDocumentLayoutComponent();
  let t0, t1;
  $[0] !== options.id || $[1] !== options.type || $[2] !== resolveNewDocumentOptions ? (t1 = resolveNewDocumentOptions({
    type: "document",
    documentId: options.id,
    schemaType: options.type
  }), $[0] = options.id, $[1] = options.type, $[2] = resolveNewDocumentOptions, $[3] = t1) : t1 = $[3], t0 = t1;
  const templateItems = t0;
  let t2;
  $[4] !== templateItems ? (t2 = {
    templateItems
  }, $[4] = templateItems, $[5] = t2) : t2 = $[5];
  const [templatePermissions, isTemplatePermissionsLoading] = useTemplatePermissions(t2), isLoaded = isDocumentLoaded && !isTemplatePermissionsLoading;
  let t3, t4;
  $[6] !== documentType || $[7] !== isLoaded || $[8] !== options || $[9] !== props ? (t4 = isLoaded && documentType && options.type !== documentType ? mergeDocumentType(props, options, documentType) : props, $[6] = documentType, $[7] = isLoaded, $[8] = options, $[9] = props, $[10] = t4) : t4 = $[10], t3 = t4;
  const providerProps = t3, {
    ReferenceChildLink: ReferenceChildLink2,
    handleEditReference,
    groupIndex,
    routerPanesState
  } = paneRouter;
  let t5;
  $[11] !== groupIndex || $[12] !== routerPanesState ? (t5 = ((_a = routerPanesState[groupIndex + 1]) == null ? void 0 : _a[0].params) || {}, $[11] = groupIndex, $[12] = routerPanesState, $[13] = t5) : t5 = $[13];
  const childParams = t5, routerPanesStateLength = routerPanesState.length, {
    parentRefPath
  } = childParams;
  let t6, t7;
  $[14] !== groupIndex || $[15] !== parentRefPath || $[16] !== routerPanesStateLength ? (t7 = parentRefPath ? {
    path: fromString(parentRefPath),
    state: groupIndex >= routerPanesStateLength - 1 ? "none" : groupIndex >= routerPanesStateLength - 2 ? "selected" : "pressed"
  } : {
    path: [],
    state: "none"
  }, $[14] = groupIndex, $[15] = parentRefPath, $[16] = routerPanesStateLength, $[17] = t7) : t7 = $[17], t6 = t7;
  const activePath = t6, {
    t: t8
  } = useTranslation(structureLocaleNamespace);
  if (options.type === "*" && !isLoaded) {
    let t822;
    $[18] !== t8 ? (t822 = t8("panes.document-pane.document-not-found.loading"), $[18] = t8, $[19] = t822) : t822 = $[19];
    let t92;
    return $[20] !== paneKey || $[21] !== t822 ? (t92 = (0, import_jsx_runtime3.jsx)(LoadingPane, { flex: 2.5, minWidth: 320, paneKey, title: t822 }), $[20] = paneKey, $[21] = t822, $[22] = t92) : t92 = $[22], t92;
  }
  if (!documentType) {
    let t822;
    $[23] !== t8 ? (t822 = t8("panes.document-pane.document-not-found.title"), $[23] = t8, $[24] = t822) : t822 = $[24];
    let t92;
    $[25] !== options.id ? (t92 = {
      id: options.id
    }, $[25] = options.id, $[26] = t92) : t92 = $[26];
    let t102;
    $[27] === Symbol.for("react.memo_cache_sentinel") ? (t102 = {
      Code: _temp$4
    }, $[27] = t102) : t102 = $[27];
    let t112;
    $[28] !== t8 || $[29] !== t92 ? (t112 = (0, import_jsx_runtime3.jsx)(Stack, { space: 4, children: (0, import_jsx_runtime3.jsx)(Text, { as: "p", children: (0, import_jsx_runtime3.jsx)(Translate, { t: t8, i18nKey: "panes.document-pane.document-not-found.text", values: t92, components: t102 }) }) }), $[28] = t8, $[29] = t92, $[30] = t112) : t112 = $[30];
    let t122;
    return $[31] !== paneKey || $[32] !== t112 || $[33] !== t822 ? (t122 = (0, import_jsx_runtime3.jsx)(ErrorPane, { flex: 2.5, minWidth: 320, paneKey, title: t822, children: t112 }), $[31] = paneKey, $[32] = t112, $[33] = t822, $[34] = t122) : t122 = $[34], t122;
  }
  const t82 = `${documentType}-${options.id}-${selectedPerspectiveName || ""}`;
  let t9;
  $[35] !== DocumentLayout2 || $[36] !== options.id || $[37] !== options.type ? (t9 = (0, import_jsx_runtime3.jsx)(DocumentLayout2, { documentId: options.id, documentType: options.type }), $[35] = DocumentLayout2, $[36] = options.id, $[37] = options.type, $[38] = t9) : t9 = $[38];
  let t10;
  $[39] !== options.id || $[40] !== options.type || $[41] !== t9 ? (t10 = (0, import_jsx_runtime3.jsx)(CommentsWrapper, { documentId: options.id, documentType: options.type, children: t9 }), $[39] = options.id, $[40] = options.type, $[41] = t9, $[42] = t10) : t10 = $[42];
  let t11;
  $[43] !== options.id || $[44] !== options.type || $[45] !== t10 ? (t11 = (0, import_jsx_runtime3.jsx)(DiffViewDocumentLayout, { documentId: options.id, documentType: options.type, children: t10 }), $[43] = options.id, $[44] = options.type, $[45] = t10, $[46] = t11) : t11 = $[46];
  let t12;
  $[47] !== ReferenceChildLink2 || $[48] !== activePath || $[49] !== handleEditReference || $[50] !== t11 || $[51] !== templatePermissions ? (t12 = (0, import_jsx_runtime3.jsx)(ReferenceInputOptionsProvider, { EditReferenceLinkComponent: ReferenceChildLink2, onEditReference: handleEditReference, initialValueTemplateItems: templatePermissions, activePath, children: t11 }), $[47] = ReferenceChildLink2, $[48] = activePath, $[49] = handleEditReference, $[50] = t11, $[51] = templatePermissions, $[52] = t12) : t12 = $[52];
  let t13;
  return $[53] !== providerProps || $[54] !== t12 || $[55] !== t82 ? (t13 = (0, import_jsx_runtime3.jsx)(DocumentPaneProviderWrapper, { ...providerProps, children: t12 }, t82), $[53] = providerProps, $[54] = t12, $[55] = t82, $[56] = t13) : t13 = $[56], t13;
}
function _temp$4(t0) {
  const {
    children
  } = t0;
  return (0, import_jsx_runtime3.jsx)("code", { children });
}
function usePaneOptions(options, t0) {
  const $ = (0, import_react_compiler_runtime.c)(8);
  let t1;
  $[0] !== t0 ? (t1 = t0 === void 0 ? {} : t0, $[0] = t0, $[1] = t1) : t1 = $[1];
  const params = t1, templates = useTemplates();
  let t2;
  bb0: {
    if (options.type && options.type !== "*") {
      t2 = options;
      break bb0;
    }
    const templateName = options.template || params.template;
    let t3;
    $[2] !== templateName || $[3] !== templates ? (t3 = templateName ? templates.find((t5) => t5.id === templateName) : void 0, $[2] = templateName, $[3] = templates, $[4] = t3) : t3 = $[4];
    const documentType = t3 == null ? void 0 : t3.schemaType;
    if (!documentType) {
      t2 = options;
      break bb0;
    }
    let t4;
    $[5] !== documentType || $[6] !== options ? (t4 = {
      ...options,
      type: documentType
    }, $[5] = documentType, $[6] = options, $[7] = t4) : t4 = $[7], t2 = t4;
  }
  return t2;
}
function mergeDocumentType(props, options, documentType) {
  return {
    ...props,
    pane: {
      ...props.pane,
      options: {
        ...options,
        type: documentType
      }
    }
  };
}
var PARTIAL_PAGE_LIMIT = 100;
var FULL_LIST_LIMIT = 2e3;
var DEFAULT_ORDERING = {
  by: [{
    field: "_updatedAt",
    direction: "desc"
  }]
};
var EMPTY_RECORD = {};
var RootBox = dt(Box)`
  position: relative;
  opacity: ${(props) => props.$opacity || 1};
  transition: opacity 0.4s;
`;
var CommandListBox = dt(Box)`
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
`;
var SKELETON_ITEMS = [...Array(30).keys()];
function LoadingView(props) {
  const $ = (0, import_react_compiler_runtime.c)(4), {
    layout
  } = props;
  let t0;
  $[0] !== layout ? (t0 = SKELETON_ITEMS.map((num) => (0, import_jsx_runtime3.jsx)(SanityDefaultPreview, { isPlaceholder: true, layout }, num)), $[0] = layout, $[1] = t0) : t0 = $[1];
  let t1;
  return $[2] !== t0 ? (t1 = (0, import_jsx_runtime3.jsx)(Stack, { paddingX: 3, paddingY: 2, paddingTop: 0, space: 1, children: t0 }), $[2] = t0, $[3] = t1) : t1 = $[3], t1;
}
function DocumentListPaneContent(props) {
  const $ = (0, import_react_compiler_runtime.c)(66), {
    childItemId,
    error,
    isRetrying,
    autoRetry,
    filterIsSimpleTypeConstraint,
    hasMaxItems,
    hasSearchQuery,
    isActive,
    isLazyLoading,
    muted,
    isLoading,
    isConnected,
    retryCount,
    canRetry,
    items: items2,
    layout,
    loadingVariant,
    onEndReached,
    onRetry,
    paneTitle,
    searchInputElement,
    showIcons
  } = props, schema = useSchema(), {
    collapsed: layoutCollapsed
  } = usePaneLayout(), {
    collapsed,
    index
  } = usePane(), [shouldRender, setShouldRender] = (0, import_react3.useState)(!collapsed), {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  let t0;
  $[0] !== onEndReached || $[1] !== shouldRender ? (t0 = () => {
    shouldRender && onEndReached();
  }, $[0] = onEndReached, $[1] = shouldRender, $[2] = t0) : t0 = $[2];
  const handleEndReached = t0;
  let t1;
  $[3] !== collapsed ? (t1 = () => {
    if (collapsed)
      return;
    const timer2 = setTimeout(() => {
      setShouldRender(true);
    }, 0);
    return () => {
      clearTimeout(timer2);
    };
  }, $[3] = collapsed, $[4] = t1) : t1 = $[4];
  let t22;
  $[5] !== collapsed || $[6] !== items2 ? (t22 = [collapsed, items2], $[5] = collapsed, $[6] = items2, $[7] = t22) : t22 = $[7], (0, import_react3.useEffect)(t1, t22);
  let t3;
  $[8] !== childItemId || $[9] !== hasMaxItems || $[10] !== isActive || $[11] !== isLazyLoading || $[12] !== items2.length || $[13] !== layout || $[14] !== schema || $[15] !== showIcons || $[16] !== t2 ? (t3 = (item, t42) => {
    const {
      activeIndex
    } = t42, publishedId = getPublishedId(item._id), isSelected = childItemId === publishedId, pressed = !isActive && isSelected, selected = isActive && isSelected, isLastItem = activeIndex === items2.length - 1, showSpinner = isLastItem && isLazyLoading, showMaxItemsMessage = isLastItem && hasMaxItems;
    return (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [
      (0, import_jsx_runtime3.jsx)(PaneItem, { icon: showIcons === false ? false : void 0, id: publishedId, layout, marginBottom: 1, pressed, schemaType: schema.get(item._type), selected, value: item }),
      showSpinner && (0, import_jsx_runtime3.jsx)(LoadingBlock, {}),
      showMaxItemsMessage && (0, import_jsx_runtime3.jsx)(Box, { marginY: 1, paddingX: 3, paddingY: 4, children: (0, import_jsx_runtime3.jsx)(Text, { align: "center", muted: true, size: 1, children: t2("panes.document-list-pane.max-items.text", {
        limit: FULL_LIST_LIMIT
      }) }) })
    ] });
  }, $[8] = childItemId, $[9] = hasMaxItems, $[10] = isActive, $[11] = isLazyLoading, $[12] = items2.length, $[13] = layout, $[14] = schema, $[15] = showIcons, $[16] = t2, $[17] = t3) : t3 = $[17];
  const renderItem = t3;
  let t4;
  bb0: {
    if (hasSearchQuery) {
      let t53;
      $[18] !== t2 ? (t53 = t2("panes.document-list-pane.no-documents.text"), $[18] = t2, $[19] = t53) : t53 = $[19];
      let t63;
      $[20] !== t53 ? (t63 = (0, import_jsx_runtime3.jsx)(Flex, { align: "center", direction: "column", height: "fill", justify: "center", children: (0, import_jsx_runtime3.jsx)(Container, { width: 1, children: (0, import_jsx_runtime3.jsx)(Box, { paddingX: 4, paddingY: 5, children: (0, import_jsx_runtime3.jsx)(Text, { align: "center", muted: true, children: t53 }) }) }) }), $[20] = t53, $[21] = t63) : t63 = $[21], t4 = t63;
      break bb0;
    }
    let t52;
    $[22] !== filterIsSimpleTypeConstraint || $[23] !== t2 ? (t52 = t2(filterIsSimpleTypeConstraint ? "panes.document-list-pane.no-documents-of-type.text" : "panes.document-list-pane.no-matching-documents.text"), $[22] = filterIsSimpleTypeConstraint, $[23] = t2, $[24] = t52) : t52 = $[24];
    let t62;
    $[25] !== t52 ? (t62 = (0, import_jsx_runtime3.jsx)(Flex, { align: "center", direction: "column", height: "fill", justify: "center", children: (0, import_jsx_runtime3.jsx)(Container, { width: 1, children: (0, import_jsx_runtime3.jsx)(Box, { paddingX: 4, paddingY: 5, children: (0, import_jsx_runtime3.jsx)(Text, { align: "center", muted: true, children: t52 }) }) }) }), $[25] = t52, $[26] = t62) : t62 = $[26], t4 = t62;
  }
  const noDocumentsContent = t4;
  let t5;
  bb1: {
    if (!shouldRender) {
      t5 = null;
      break bb1;
    }
    const isOnline = window.navigator.onLine;
    if (error) {
      let t63;
      $[27] !== t2 ? (t63 = t2("panes.document-list-pane.error.title"), $[27] = t2, $[28] = t63) : t63 = $[28];
      let t73;
      $[29] !== t63 ? (t73 = (0, import_jsx_runtime3.jsx)(Heading, { as: "h3", children: t63 }), $[29] = t63, $[30] = t73) : t73 = $[30];
      let t82;
      $[31] !== error || $[32] !== t2 ? (t82 = isDev ? (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey: "panes.document-list-pane.error.text.dev", values: {
        error: error.message
      }, components: {
        Code: _temp$3
      } }) : t2(isOnline ? "panes.document-list-pane.error.text" : "panes.document-list-pane.error.text.offline"), $[31] = error, $[32] = t2, $[33] = t82) : t82 = $[33];
      let t9;
      $[34] !== t82 ? (t9 = (0, import_jsx_runtime3.jsx)(Text, { as: "p", children: t82 }), $[34] = t82, $[35] = t9) : t9 = $[35];
      const t10 = isOnline && canRetry ? onRetry : void 0;
      let t11;
      $[36] !== error || $[37] !== isRetrying || $[38] !== t10 ? (t11 = (0, import_jsx_runtime3.jsx)(ErrorActions, { error, eventId: null, onRetry: t10, isRetrying }), $[36] = error, $[37] = isRetrying, $[38] = t10, $[39] = t11) : t11 = $[39];
      let t12;
      $[40] !== autoRetry || $[41] !== canRetry || $[42] !== isRetrying || $[43] !== retryCount || $[44] !== t2 ? (t12 = canRetry ? (0, import_jsx_runtime3.jsx)(Text, { as: "p", muted: true, size: 1, children: isRetrying ? t2("panes.document-list-pane.error.retrying", {
        count: retryCount
      }) : autoRetry ? t2("panes.document-list-pane.error.will-retry-automatically", {
        count: retryCount
      }) : t2("panes.document-list-pane.error.max-retries-attempted", {
        count: retryCount
      }) }) : null, $[40] = autoRetry, $[41] = canRetry, $[42] = isRetrying, $[43] = retryCount, $[44] = t2, $[45] = t12) : t12 = $[45];
      let t13;
      $[46] !== t11 || $[47] !== t12 || $[48] !== t73 || $[49] !== t9 ? (t13 = (0, import_jsx_runtime3.jsx)(Flex, { align: "center", direction: "column", height: "fill", justify: "center", children: (0, import_jsx_runtime3.jsx)(Container, { width: 1, children: (0, import_jsx_runtime3.jsxs)(Stack, { paddingX: 4, paddingY: 5, space: 4, children: [
        t73,
        t9,
        t11,
        t12
      ] }) }) }), $[46] = t11, $[47] = t12, $[48] = t73, $[49] = t9, $[50] = t13) : t13 = $[50], t5 = t13;
      break bb1;
    }
    if (isConnected && !isLoading && items2.length === 0) {
      t5 = noDocumentsContent;
      break bb1;
    }
    if (loadingVariant === "initial" && isLoading) {
      let t63;
      $[51] !== layout ? (t63 = (0, import_jsx_runtime3.jsx)(Delay, { ms: 300, children: (0, import_jsx_runtime3.jsx)(LoadingView, { layout }) }), $[51] = layout, $[52] = t63) : t63 = $[52], t5 = t63;
      break bb1;
    }
    if (loadingVariant === "spinner" && isLoading) {
      t5 = null;
      break bb1;
    }
    const key = `${index}-${collapsed}`, t62 = muted ? 0.8 : 1;
    let t72;
    $[53] !== handleEndReached || $[54] !== items2 || $[55] !== key || $[56] !== paneTitle || $[57] !== renderItem || $[58] !== searchInputElement ? (t72 = (0, import_jsx_runtime3.jsx)(CommandListBox, { children: (0, import_jsx_runtime3.jsx)(CommandList, { activeItemDataAttr: "data-hovered", ariaLabel: paneTitle, canReceiveFocus: true, inputElement: searchInputElement, itemHeight: 51, items: items2, onEndReached: handleEndReached, onlyShowSelectionWhenActive: true, overscan: 10, paddingBottom: 1, paddingX: 3, renderItem, wrapAround: false }, key) }), $[53] = handleEndReached, $[54] = items2, $[55] = key, $[56] = paneTitle, $[57] = renderItem, $[58] = searchInputElement, $[59] = t72) : t72 = $[59];
    let t8;
    $[60] !== t62 || $[61] !== t72 ? (t8 = (0, import_jsx_runtime3.jsx)(RootBox, { overflow: "hidden", height: "fill", $opacity: t62, children: t72 }), $[60] = t62, $[61] = t72, $[62] = t8) : t8 = $[62], t5 = t8;
  }
  const mainContent = t5, t6 = layoutCollapsed || loadingVariant === "initial" ? "hidden" : "auto";
  let t7;
  return $[63] !== mainContent || $[64] !== t6 ? (t7 = (0, import_jsx_runtime3.jsx)(PaneContent, { "data-testid": "document-list-pane", overflow: t6, children: mainContent }), $[63] = mainContent, $[64] = t6, $[65] = t7) : t7 = $[65], t7;
}
function _temp$3(t0) {
  const {
    children
  } = t0;
  return (0, import_jsx_runtime3.jsx)("code", { children });
}
function removePublishedWithDrafts(documents) {
  return collate(documents).map((entry) => {
    const doc = entry.draft || entry.published || entry.versions[0], hasDraft = !!entry.draft;
    return {
      ...doc,
      hasPublished: !!entry.published,
      hasDraft
    };
  });
}
function applyOrderingFunctions(order, schemaType) {
  const orderBy = order.by.map((by) => {
    if (by.mapWith)
      return by;
    const fieldType = tryResolveSchemaTypeForPath(schemaType, by.field);
    return fieldType ? fieldExtendsType(fieldType, "datetime") ? {
      ...by,
      mapWith: "dateTime"
    } : fieldType.jsonType === "string" ? {
      ...by,
      mapWith: "lower"
    } : by : by;
  });
  return orderBy.every((item, index) => item === order.by[index]) ? order : {
    ...order,
    by: orderBy
  };
}
function tryResolveSchemaTypeForPath(baseType, path) {
  const pathSegments = fromString(path);
  let current = baseType;
  for (const segment of pathSegments) {
    if (!current)
      return;
    if (typeof segment == "string") {
      current = getFieldTypeByName(current, segment);
      continue;
    }
    if (!(isKeySegment(segment) || isIndexSegment(segment)) || current.jsonType !== "array")
      return;
    const [memberType, otherType] = current.of || [];
    if (otherType || !memberType)
      return;
    if (!isReferenceSchemaType(memberType)) {
      current = memberType;
      continue;
    }
    const [refType, otherRefType] = memberType.to || [];
    if (otherRefType || !refType)
      return;
    current = refType;
  }
  return current;
}
function getFieldTypeByName(type2, fieldName) {
  if (!("fields" in type2))
    return;
  const fieldType = type2.fields.find((field) => field.name === fieldName);
  return fieldType ? fieldType.type : void 0;
}
function fieldExtendsType(field, ofType) {
  let current = field.type;
  for (; current; ) {
    if (current.name === ofType || !current.type && current.jsonType === ofType)
      return true;
    current = current.type;
  }
  return false;
}
function findTypes(node) {
  switch (node.type) {
    case "OpCall": {
      const {
        left,
        right
      } = node;
      switch (node.op) {
        // e.g. `a == b`
        case "==":
          return left.type === "AccessAttribute" && left.name === "_type" && !left.base ? right.type !== "Value" || typeof right.value != "string" ? null : /* @__PURE__ */ new Set([right.value]) : right.type === "AccessAttribute" && right.name === "_type" && !right.base ? left.type !== "Value" || typeof left.value != "string" ? null : /* @__PURE__ */ new Set([left.value]) : null;
        // e.g. `a in b`
        case "in": {
          if (left.type !== "AccessAttribute" || left.name !== "_type" || left.base || right.type !== "Array") return null;
          const types = /* @__PURE__ */ new Set();
          for (const element of right.elements) {
            if (element.isSplat || element.value.type !== "Value" || typeof element.value.value != "string") return null;
            types.add(element.value.value);
          }
          return types.size ? types : null;
        }
        default:
          return null;
      }
    }
    // groups can just be unwrapped, the AST preserves the order
    case "Group":
      return findTypes(node.base);
    // e.g. `_type == 'a' || _type == 'b'`
    // with Or nodes, if we can't determine the types for either the left or
    // right hand side then we can't determine the types for any
    // e.g. `_type == 'a' || isActive`
    // — can't determine types because `isActive` could be true on another types
    case "Or": {
      const left = findTypes(node.left);
      if (!left) return null;
      const right = findTypes(node.right);
      return right ? /* @__PURE__ */ new Set([...left, ...right]) : null;
    }
    // e.g. `_type == 'a' && isActive`
    // with And nodes, we can determine the types as long as we can determine
    // the types for one side. We can't determine the types if both are `null`.
    case "And": {
      const left = findTypes(node.left), right = findTypes(node.right);
      return !left && !right ? null : /* @__PURE__ */ new Set([...left || [], ...right || []]);
    }
    default:
      return null;
  }
}
function findStaticTypesInFilter(filter2, params = {}) {
  try {
    const types = findTypes(parse(filter2, {
      params
    }));
    return types ? Array.from(types).sort() : null;
  } catch {
    return null;
  }
}
var swr = createSWR({
  maxSize: 100
});
function listenSearchQuery(options) {
  const {
    client,
    schema,
    sort,
    perspective,
    limit,
    params,
    filter: groqFilter,
    searchQuery,
    staticTypeNames,
    maxFieldDepth,
    searchStrategy
  } = options, sortBy = sort.by, extendedProjection = sort == null ? void 0 : sort.extendedProjection, events$ = defer(() => client.listen(`*[${groqFilter}]`, params, {
    events: ["welcome", "mutation", "reconnect"],
    includeAllVersions: true,
    includeResult: false,
    visibility: "query",
    tag: "listen-search-query"
  })).pipe(mergeMap((ev, i) => {
    const isFirst = i === 0;
    return isFirst && ev.type === "reconnect" ? throwError(() => new Error("Failed to establish EventSource connection")) : isFirst && ev.type !== "welcome" ? throwError(() => new Error(`Received unexpected type of first event "${ev.type}"`)) : of(ev);
  }), share()), [welcome$, mutationAndReconnect$] = partition(events$, (ev) => ev.type === "welcome"), swrKey = JSON.stringify({
    fiilter: groqFilter,
    limit,
    params,
    searchQuery,
    perspective,
    sort,
    staticTypeNames
  });
  return merge(welcome$, mutationAndReconnect$.pipe(throttleTime(1e3, asyncScheduler, {
    leading: true,
    trailing: true
  }))).pipe(exhaustMapWithTrailing((event) => (staticTypeNames ? of(staticTypeNames) : client.observable.fetch(`array::unique(*[${groqFilter}][]._type)`, params)).pipe(mergeMap((typeNames) => {
    const types = getSearchableTypes(schema, staticTypeNames || []).filter((type2) => typeNames.includes(type2.name) ? (getExtendedProjection(type2, sort.by, true), true) : false), search = createSearch(types, client, {
      filter: groqFilter,
      params,
      strategy: searchStrategy,
      maxDepth: maxFieldDepth
    }), doFetch = () => search({
      query: searchQuery || "",
      types
    }, {
      __unstable_extendedProjection: extendedProjection,
      comments: [`findability-source: ${searchQuery ? "list-query" : "list"}`],
      limit,
      skipSortByScore: true,
      sort: sortBy,
      perspective
    }).pipe(map((result) => (
      // eslint-disable-next-line max-nested-callbacks
      result.hits.map(({
        hit
      }) => hit)
    )), map((hits) => ({
      type: "result",
      documents: hits
    })));
    return event.type === "mutation" && event.visibility !== "query" ? timer(1200).pipe(mergeMap(doFetch)) : event.type === "reconnect" ? of(event) : doFetch();
  }))), scan((acc, event) => ({
    connected: event.type !== "reconnect",
    documents: event.type === "result" ? event.documents : (acc == null ? void 0 : acc.documents) || []
  }), null), filter((v) => v !== null), swr(swrKey), map(({
    fromCache,
    value
  }) => ({
    fromCache,
    ...value
  })));
}
var INITIAL_QUERY_STATE = {
  error: null,
  isRetrying: false,
  retryCount: 0,
  autoRetry: false,
  canRetry: false,
  isLoading: true,
  isLoadingFullList: false,
  fromCache: false,
  items: []
};
function isRetriableError(error) {
  return error instanceof ChannelError ? false : error instanceof ServerError ? true : error instanceof ClientError ? error.statusCode === 403 : true;
}
function useDocumentList(opts) {
  const {
    filter: searchFilter,
    params: paramsProp,
    sortOrder,
    searchQuery,
    perspective,
    apiVersion
  } = opts, client = useClient({
    ...DEFAULT_STUDIO_CLIENT_OPTIONS,
    apiVersion: apiVersion || DEFAULT_STUDIO_CLIENT_OPTIONS.apiVersion
  }), {
    strategy: searchStrategy
  } = useWorkspace().search, schema = useSchema(), maxFieldDepth = useSearchMaxFieldDepth(), typeNameFromFilter = (0, import_react3.useMemo)(() => findStaticTypesInFilter(searchFilter, paramsProp), [searchFilter, paramsProp]), [[onRetry$, onRetry]] = (0, import_react3.useState)(() => observableCallback()), [[onFetchFullList$, onLoadFullList]] = (0, import_react3.useState)(() => observableCallback()), queryResults$ = (0, import_react3.useMemo)(() => {
    const listenSearchQueryArgs = {
      client,
      filter: searchFilter,
      limit: PARTIAL_PAGE_LIMIT,
      params: paramsProp,
      schema,
      perspective,
      searchQuery: searchQuery || "",
      sort: sortOrder || DEFAULT_ORDERING,
      staticTypeNames: typeNameFromFilter,
      maxFieldDepth,
      searchStrategy
    }, partialList$ = listenSearchQuery(listenSearchQueryArgs).pipe(shareReplay({
      refCount: true,
      bufferSize: 1
    })), fullList$ = onFetchFullList$.pipe(
      withLatestFrom(partialList$),
      filter(([, result]) => (result == null ? void 0 : result.documents.length) === PARTIAL_PAGE_LIMIT),
      // we want to set up the full list listener only once
      take(1),
      mergeMap(() => concat(of({
        type: "loadFullList"
      }), listenSearchQuery({
        ...listenSearchQueryArgs,
        limit: FULL_LIST_LIMIT
      }).pipe(map((result_0) => ({
        type: "result",
        result: result_0
      }))))),
      share()
    );
    return merge(partialList$.pipe(
      map((result_1) => ({
        type: "result",
        result: result_1
      })),
      // when the full list listener kicks off, we want to stop the partial list listener
      takeUntil(fullList$)
    ), fullList$).pipe(catchWithCount((lastError, retryCount, caught$) => {
      const error = safeError(lastError), isOnline = window.navigator.onLine, canRetry = isOnline && isRetriableError(lastError), autoRetry = retryCount < 10, retries = merge(isOnline ? onRetry$ : fromEvent(window, "online"), isOnline && autoRetry ? timer(retryCount * 1e3) : NEVER).pipe(take(1), switchMap(() => merge(of({
        type: "error",
        error,
        retrying: true,
        autoRetry,
        canRetry,
        retryCount
      }), caught$)));
      return concat(of({
        type: "error",
        error,
        retrying: false,
        autoRetry,
        canRetry,
        retryCount
      }), retries);
    }), scan((prev, event) => {
      if (event.type === "error")
        return {
          ...prev,
          error: event.error,
          retryCount: event.retryCount,
          isRetrying: event.retrying,
          autoRetry: event.autoRetry,
          canRetry: event.canRetry
        };
      if (event.type === "result")
        return {
          ...prev,
          error: null,
          isRetrying: false,
          fromCache: event.result.fromCache,
          connected: event.result.connected,
          isLoading: false,
          items: removePublishedWithDrafts(event.result.documents),
          isLoadingFullList: false
        };
      if (event.type === "loadFullList")
        return {
          ...prev,
          error: null,
          isLoadingFullList: true
        };
      throw new Error(`Unexpected event type: ${event.type}`);
    }, INITIAL_QUERY_STATE));
  }, [client, searchFilter, paramsProp, schema, perspective, searchQuery, sortOrder, typeNameFromFilter, maxFieldDepth, searchStrategy, onFetchFullList$, onRetry$]), {
    error: error_0,
    items: items2,
    isLoading,
    fromCache,
    connected,
    canRetry: canRetry_0,
    isLoadingFullList,
    isRetrying,
    autoRetry: autoRetry_0,
    retryCount: retryCount_0
  } = useObservable(queryResults$, INITIAL_QUERY_STATE);
  return {
    error: error_0,
    onRetry,
    isLoading,
    items: items2,
    isRetrying,
    canRetry: canRetry_0,
    retryCount: retryCount_0,
    autoRetry: autoRetry_0,
    connected,
    fromCache,
    onLoadFullList,
    isLoadingFullList
  };
}
var nonErrorThrownWarning = "[WARNING: This was thrown as a non-error. Only Error instances should be thrown]";
function safeError(thrown) {
  return thrown instanceof Error ? thrown : typeof thrown == "object" && thrown !== null ? "message" in thrown && typeof thrown.message == "string" ? new Error(`${thrown.message} ${nonErrorThrownWarning}`) : new Error(`${String(thrown)} ${nonErrorThrownWarning}`) : new Error(`${String(thrown)} ${nonErrorThrownWarning}`);
}
var rotate = mt`
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
`;
var fadeIn = mt`
  0% {
    opacity: 0;
  }
  50% {
    opacity: 0.1;
  }
  100% {
    opacity: 0.4;
  }
`;
var AnimatedSpinnerIcon = dt(SpinnerIcon)`
  animation: ${rotate} 500ms linear infinite;
`;
var SubtleSpinnerIcon = dt(SpinnerIcon)`
  animation: ${rotate} 1500ms linear infinite;
  opacity: 0.4;
`;
var DelayedSubtleSpinnerIcon = dt(SpinnerIcon)`
  animation:
    ${rotate} 1500ms linear infinite,
    ${fadeIn} 1000ms linear;
  opacity: 0.4;
`;
var DocumentListPane = (0, import_react3.memo)(function(props) {
  const {
    childItemId,
    isActive,
    pane: pane2,
    paneKey,
    sortOrder: sortOrderRaw,
    layout
  } = props, schema = useSchema(), releases = useActiveReleases(), {
    perspectiveStack
  } = usePerspective(), {
    displayOptions,
    options
  } = pane2, {
    apiVersion,
    filter: filter2
  } = options, params = useShallowUnique(options.params || EMPTY_RECORD), typeName = (0, import_react3.useMemo)(() => {
    const staticTypes = findStaticTypesInFilter(filter2, params);
    return (staticTypes == null ? void 0 : staticTypes.length) === 1 ? staticTypes[0] : null;
  }, [filter2, params]), showIcons = (displayOptions == null ? void 0 : displayOptions.showIcons) !== false, {
    t: t2
  } = useTranslation(structureLocaleNamespace), {
    title
  } = useI18nText(pane2), [searchQuery, setSearchQuery] = (0, import_react3.useState)(""), [searchInputValue, setSearchInputValue] = (0, import_react3.useState)(""), [searchInputElement, setSearchInputElement] = (0, import_react3.useState)(null), sortWithOrderingFn = typeName && sortOrderRaw ? applyOrderingFunctions(sortOrderRaw, schema.get(typeName)) : sortOrderRaw, sortOrder = useUnique(sortWithOrderingFn), {
    error,
    isLoadingFullList,
    isLoading: documentListIsLoading,
    items: items2,
    fromCache,
    isRetrying,
    autoRetry,
    canRetry,
    retryCount,
    connected,
    onLoadFullList,
    onRetry
  } = useDocumentList({
    apiVersion,
    filter: filter2,
    perspective: perspectiveStack,
    params,
    searchQuery: searchQuery == null ? void 0 : searchQuery.trim(),
    sortOrder
  }), isLoading = documentListIsLoading || releases.loading, handleQueryChange = useObservableEvent((event$) => event$.pipe(map((event) => event.target.value), tap(setSearchInputValue), debounce((value) => value === "" ? of("") : timer(300)), tap(setSearchQuery))), handleClearSearch = (0, import_react3.useCallback)(() => {
    setSearchQuery(""), setSearchInputValue("");
  }, []), handleSearchKeyDown = (0, import_react3.useCallback)((event_0) => {
    event_0.key === "Escape" && handleClearSearch();
  }, [handleClearSearch]), [enableSearchSpinner, setEnableSearchSpinner] = (0, import_react3.useState)();
  (0, import_react3.useEffect)(() => {
    !enableSearchSpinner && !isLoading && setEnableSearchSpinner(paneKey);
  }, [enableSearchSpinner, isLoading, paneKey]), (0, import_react3.useEffect)(() => {
    handleClearSearch(), setEnableSearchSpinner();
  }, [paneKey, handleClearSearch]);
  const loadingVariant = (0, import_react3.useMemo)(() => connected && isLoading && enableSearchSpinner === paneKey ? "spinner" : connected && fromCache ? "subtle" : "initial", [connected, enableSearchSpinner, fromCache, isLoading, paneKey]), textInputIcon = (0, import_react3.useMemo)(() => loadingVariant === "spinner" ? AnimatedSpinnerIcon : searchInputValue && loadingVariant === "subtle" ? SubtleSpinnerIcon : SearchIcon, [loadingVariant, searchInputValue]);
  return useReconnectingToast(!connected), (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [
    (0, import_jsx_runtime3.jsx)(Box, { paddingX: 3, paddingBottom: 3, children: (0, import_jsx_runtime3.jsx)(TextInput, { "aria-label": t2("panes.document-list-pane.search-input.aria-label"), autoComplete: "off", border: false, clearButton: !!searchQuery, fontSize: [2, 2, 1], icon: textInputIcon, iconRight: !connected || loadingVariant === "subtle" && !searchInputValue ? DelayedSubtleSpinnerIcon : null, onChange: handleQueryChange, onClear: handleClearSearch, onKeyDown: handleSearchKeyDown, padding: 2, placeholder: t2("panes.document-list-pane.search-input.placeholder"), radius: 2, ref: setSearchInputElement, spellCheck: false, value: searchInputValue }) }),
    (0, import_jsx_runtime3.jsx)(DocumentListPaneContent, { childItemId, error, filterIsSimpleTypeConstraint: !!typeName, hasMaxItems: items2.length === FULL_LIST_LIMIT, hasSearchQuery: !!searchQuery, isActive, isLazyLoading: isLoadingFullList, isLoading, autoRetry, canRetry, retryCount, isRetrying, isConnected: connected, items: items2, layout, muted: loadingVariant === "subtle", loadingVariant, onEndReached: onLoadFullList, onRetry, paneTitle: title, searchInputElement, showIcons }, paneKey)
  ] });
});
var PaneHeader = (0, import_react3.memo)((t0) => {
  const $ = (0, import_react_compiler_runtime.c)(27), {
    contentAfter,
    index,
    initialValueTemplates: t1,
    menuItemGroups: t2,
    menuItems: t3,
    setLayout,
    setSortOrder,
    title
  } = t0;
  let t4;
  $[0] !== t1 ? (t4 = t1 === void 0 ? [] : t1, $[0] = t1, $[1] = t4) : t4 = $[1];
  const initialValueTemplates = t4;
  let t5;
  $[2] !== t2 ? (t5 = t2 === void 0 ? [] : t2, $[2] = t2, $[3] = t5) : t5 = $[3];
  const menuItemGroups = t5;
  let t6;
  $[4] !== t3 ? (t6 = t3 === void 0 ? [] : t3, $[4] = t3, $[5] = t6) : t6 = $[5];
  const menuItems = t6, {
    features
  } = useStructureTool(), {
    collapsed,
    isLast
  } = usePane(), tabIndex = isLast && !collapsed ? -1 : 0;
  let t7, t8;
  $[6] !== setLayout ? (t8 = (t92) => {
    const {
      layout: value
    } = t92;
    setLayout(value);
  }, $[6] = setLayout, $[7] = t8) : t8 = $[7];
  let t9;
  $[8] !== setSortOrder ? (t9 = (sort) => {
    setSortOrder(sort);
  }, $[8] = setSortOrder, $[9] = t9) : t9 = $[9];
  let t10;
  $[10] !== t8 || $[11] !== t9 ? (t10 = {
    setLayout: t8,
    setSortOrder: t9
  }, $[10] = t8, $[11] = t9, $[12] = t10) : t10 = $[12], t7 = t10;
  const actionHandlers = t7;
  let t11;
  $[13] !== actionHandlers || $[14] !== initialValueTemplates || $[15] !== menuItemGroups || $[16] !== menuItems ? (t11 = (0, import_jsx_runtime3.jsx)(PaneHeaderActions, { initialValueTemplateItems: initialValueTemplates, actionHandlers, menuItemGroups, menuItems }), $[13] = actionHandlers, $[14] = initialValueTemplates, $[15] = menuItemGroups, $[16] = menuItems, $[17] = t11) : t11 = $[17];
  let t12;
  $[18] !== features.backButton || $[19] !== index ? (t12 = features.backButton && index > 0 && (0, import_jsx_runtime3.jsx)(Button2, { as: BackLink, "data-as": "a", icon: ArrowLeftIcon, mode: "bleed", tooltipProps: {
    content: "Back"
  } }), $[18] = features.backButton, $[19] = index, $[20] = t12) : t12 = $[20];
  let t13;
  return $[21] !== contentAfter || $[22] !== t11 || $[23] !== t12 || $[24] !== tabIndex || $[25] !== title ? (t13 = (0, import_jsx_runtime3.jsx)(TooltipDelayGroupProvider, { children: (0, import_jsx_runtime3.jsx)(PaneHeader$1, { actions: t11, backButton: t12, contentAfter, tabIndex, title }) }), $[21] = contentAfter, $[22] = t11, $[23] = t12, $[24] = tabIndex, $[25] = title, $[26] = t13) : t13 = $[26], t13;
});
PaneHeader.displayName = "Memo(PaneHeader)";
function DocumentSheetListSelect(props) {
  const {
    row,
    table
  } = props, {
    selectedAnchor,
    setSelectedAnchor
  } = table.options.meta || {}, handleOnClick = (0, import_react3.useCallback)((e2) => {
    if (e2.shiftKey && selectedAnchor !== null && selectedAnchor !== void 0) {
      const shiftClickIndex = row.index, lowerIndex = shiftClickIndex < selectedAnchor ? shiftClickIndex : selectedAnchor, upperIndex = shiftClickIndex < selectedAnchor ? selectedAnchor : shiftClickIndex, additionalSelectedRows = table.getRowModel().flatRows.slice(lowerIndex, upperIndex + 1).map(({
        id: id2
      }) => id2), currentSelectedRows = table.getSelectedRowModel().rows.map(({
        id: id_0
      }) => id_0);
      table.setRowSelection(() => [...additionalSelectedRows, ...currentSelectedRows].reduce((nextSelectedRows, rowId) => ({
        ...nextSelectedRows,
        [rowId]: true
      }), {}));
    } else if (setSelectedAnchor) {
      const isRowCurrentlySelected = row.getIsSelected();
      setSelectedAnchor(isRowCurrentlySelected ? null : row.index), row.toggleSelected();
    }
  }, [selectedAnchor, row, setSelectedAnchor, table]);
  return (0, import_jsx_runtime3.jsx)(
    Checkbox,
    {
      style: {
        paddingLeft: 4
      },
      checked: props.row.getIsSelected(),
      disabled: !props.row.getCanSelect(),
      onChange: () => null,
      onClick: handleOnClick
    }
  );
}
var useDocumentSheetListContext = () => {
  const context = (0, import_react3.useContext)(DocumentSheetListContext);
  if (context === void 0)
    throw new Error("useDocumentSheetListContext must be used within an DocumentSheetListProvider");
  return context;
};
function DocumentSheetListProvider({
  children,
  table
}) {
  const [selectedAnchorCellDetails, setSelectedAnchorCellDetails] = (0, import_react3.useState)(null), [selectedRangeCellIndexes, setSelectedRangeCellIndexes] = (0, import_react3.useState)([]), clearAndSetFocusSelection = (0, import_react3.useCallback)((nextAnchorDetails = null) => {
    (selectedAnchorCellDetails == null ? void 0 : selectedAnchorCellDetails.state) === "focused" && document.activeElement instanceof HTMLElement && document.activeElement.blur(), setSelectedAnchorCellDetails(nextAnchorDetails), setSelectedRangeCellIndexes([]);
  }, [selectedAnchorCellDetails]), resetFocusSelection = (0, import_react3.useCallback)(() => clearAndSetFocusSelection(), [clearAndSetFocusSelection]), changeSelectionColumn = (0, import_react3.useCallback)((direction) => {
    if (!selectedAnchorCellDetails) return;
    const visibleColumns = table.getVisibleLeafColumns(), columnIndexAfterMove = visibleColumns.findIndex((col) => col.id === selectedAnchorCellDetails.colId) + (direction === "left" ? -1 : 1);
    columnIndexAfterMove < 0 || columnIndexAfterMove >= visibleColumns.length || clearAndSetFocusSelection({
      colId: visibleColumns[columnIndexAfterMove].id,
      rowIndex: selectedAnchorCellDetails.rowIndex,
      state: "selected"
    });
  }, [clearAndSetFocusSelection, selectedAnchorCellDetails, table]), changeSelectionRange = (0, import_react3.useCallback)((direction_0) => {
    selectedAnchorCellDetails && setSelectedRangeCellIndexes((previousSelection) => {
      const {
        rowIndex: anchorIndex
      } = selectedAnchorCellDetails, getNextIndex = (startingIndex) => startingIndex + (direction_0 === "down" ? 1 : -1);
      if (!previousSelection.length) {
        const firstSelectedIndex = getNextIndex(anchorIndex);
        return firstSelectedIndex < 0 ? [] : [firstSelectedIndex];
      }
      const lastIndexSelected = previousSelection[previousSelection.length - 1], nextIndex = getNextIndex(lastIndexSelected);
      return nextIndex < 0 ? previousSelection : nextIndex === anchorIndex ? [] : previousSelection.includes(nextIndex) ? previousSelection.slice(0, -1) : [...previousSelection, nextIndex];
    });
  }, [selectedAnchorCellDetails]), setSelectedAnchorCell = (0, import_react3.useCallback)((colId, rowIndex) => {
    clearAndSetFocusSelection({
      colId,
      rowIndex,
      state: "selected"
    });
  }, [clearAndSetFocusSelection]), handleEscapePress = (0, import_react3.useCallback)(() => {
    if (selectedAnchorCellDetails)
      if (selectedRangeCellIndexes.length)
        setSelectedRangeCellIndexes([]);
      else {
        const nextAnchorCellDetails = selectedAnchorCellDetails.state === "selected" ? null : {
          ...selectedAnchorCellDetails,
          state: "selected"
        };
        clearAndSetFocusSelection(nextAnchorCellDetails);
      }
  }, [clearAndSetFocusSelection, selectedAnchorCellDetails, selectedRangeCellIndexes.length]), handleUpDownKey = (0, import_react3.useCallback)((isShiftKey, key) => {
    if (!selectedAnchorCellDetails) return;
    const direction_1 = key === "ArrowDown" ? "down" : "up", offset = direction_1 === "down" ? 1 : -1;
    if (isShiftKey)
      changeSelectionRange(direction_1);
    else {
      const newSelectedCellRowIndex = selectedAnchorCellDetails.rowIndex + offset;
      if (newSelectedCellRowIndex < 0) return;
      setSelectedAnchorCell(selectedAnchorCellDetails.colId, newSelectedCellRowIndex);
    }
  }, [changeSelectionRange, selectedAnchorCellDetails, setSelectedAnchorCell]), handleAnchorKeydown = (0, import_react3.useCallback)((event) => {
    if (!selectedAnchorCellDetails) return;
    const {
      key: key_0,
      shiftKey
    } = event;
    switch (key_0) {
      case "Shift":
        break;
      // shift allow should do nothing
      case "Escape":
        handleEscapePress();
        break;
      case "ArrowDown":
      case "ArrowUp":
        event.preventDefault(), handleUpDownKey(shiftKey, key_0);
        break;
      case "ArrowLeft":
      case "ArrowRight":
        selectedAnchorCellDetails.state === "selected" && (event.preventDefault(), changeSelectionColumn(key_0 === "ArrowLeft" ? "left" : "right"));
        break;
    }
  }, [selectedAnchorCellDetails, handleEscapePress, handleUpDownKey, changeSelectionColumn]), handleAnchorClick = (0, import_react3.useCallback)((event_0) => {
    var _a;
    selectedAnchorCellDetails && (((_a = document.getElementById(`cell-${selectedAnchorCellDetails.colId}-${selectedAnchorCellDetails.rowIndex}`)) == null ? void 0 : _a.contains(event_0.target)) || resetFocusSelection());
  }, [resetFocusSelection, selectedAnchorCellDetails]);
  (0, import_react3.useEffect)(() => (selectedAnchorCellDetails && (document.addEventListener("keydown", handleAnchorKeydown), document.addEventListener("click", handleAnchorClick)), () => {
    selectedAnchorCellDetails && (document.removeEventListener("keydown", handleAnchorKeydown), document.removeEventListener("click", handleAnchorClick));
  }), [handleAnchorClick, handleAnchorKeydown, selectedAnchorCellDetails]);
  const focusAnchorCell = (0, import_react3.useCallback)(() => setSelectedAnchorCellDetails((anchorCellDetails) => anchorCellDetails ? {
    ...anchorCellDetails,
    state: "focused"
  } : null), []), getStateByCellId = (0, import_react3.useCallback)((colId_0, rowIndex_0) => (selectedAnchorCellDetails == null ? void 0 : selectedAnchorCellDetails.colId) !== colId_0 ? null : selectedAnchorCellDetails.rowIndex === rowIndex_0 ? selectedAnchorCellDetails.state === "focused" ? "focused" : "selectedAnchor" : selectedRangeCellIndexes.includes(rowIndex_0) ? "selectedRange" : null, [selectedAnchorCellDetails, selectedRangeCellIndexes]), submitFocusedCell = (0, import_react3.useCallback)(() => {
    selectedAnchorCellDetails && clearAndSetFocusSelection({
      colId: selectedAnchorCellDetails.colId,
      rowIndex: selectedAnchorCellDetails.rowIndex + 1,
      state: "selected"
    });
  }, [clearAndSetFocusSelection, selectedAnchorCellDetails]), value = (0, import_react3.useMemo)(() => ({
    focusAnchorCell,
    resetFocusSelection,
    setSelectedAnchorCell,
    getStateByCellId,
    submitFocusedCell
  }), [focusAnchorCell, resetFocusSelection, setSelectedAnchorCell, getStateByCellId, submitFocusedCell]);
  return (0, import_jsx_runtime3.jsx)(DocumentSheetListContext.Provider, { value, children });
}
var DataCell = dt.td`
  display: flex;
  overflow: hidden;
  box-sizing: border-box;
  width: ${({
  width
}) => width}px;
  border-top: 1px solid var(--card-border-color);
  background-color: var(--card-bg-color);
`;
var PinnedDataCell = dt(DataCell)`
  position: sticky;
  z-index: 2;
`;
function SheetListCellInner(props) {
  const {
    getValue,
    column,
    row,
    fieldType
  } = props, cellId = `cell-${column.id}-${row.index}`, [renderValue, setRenderValue] = (0, import_react3.useState)(getValue()), [isDirty, setIsDirty] = (0, import_react3.useState)(false), inputRef = (0, import_react3.useRef)(null), {
    focusAnchorCell,
    resetFocusSelection,
    setSelectedAnchorCell,
    getStateByCellId,
    submitFocusedCell
  } = useDocumentSheetListContext(), cellState = getStateByCellId(column.id, row.index), handleOnFocus = (0, import_react3.useCallback)(() => {
    setSelectedAnchorCell(column.id, row.index), focusAnchorCell();
  }, [column.id, focusAnchorCell, row.index, setSelectedAnchorCell]), {
    patchDocument
  } = props.table.options.meta || {}, handleProgrammaticFocus = () => {
    var _a;
    (_a = inputRef.current) == null ? void 0 : _a.focus(), inputRef.current instanceof HTMLInputElement && inputRef.current.select();
  }, handleOnMouseDown = (event) => {
    event.detail === 2 ? handleProgrammaticFocus() : (event.preventDefault(), setSelectedAnchorCell(column.id, row.index));
  }, handleOnEnterDown = (0, import_react3.useCallback)((event_0) => {
    const {
      key
    } = event_0;
    key === "Enter" && (cellState === "selectedAnchor" && handleProgrammaticFocus(), cellState === "focused" && submitFocusedCell());
  }, [cellState, submitFocusedCell]), handleOnChange = (event_1) => {
    setIsDirty(true), setRenderValue(event_1.target.value);
  }, handleOnBlur = () => {
    isDirty && (patchDocument == null ? void 0 : patchDocument(row.id, column.id, renderValue), setIsDirty(false)), resetFocusSelection();
  }, handlePaste = (0, import_react3.useCallback)((event_2) => {
    var _a;
    const clipboardData = (_a = event_2.clipboardData) == null ? void 0 : _a.getData("Text");
    (typeof clipboardData == "string" || typeof clipboardData == "number") && (setRenderValue(clipboardData), patchDocument == null ? void 0 : patchDocument(row.id, column.id, clipboardData));
  }, [column.id, patchDocument, row.id]), handleCopy = (0, import_react3.useCallback)(() => {
    navigator.clipboard.writeText(renderValue.toString());
  }, [renderValue]);
  (0, import_react3.useEffect)(() => ((cellState === "selectedAnchor" || cellState === "focused") && document.addEventListener("keydown", handleOnEnterDown), (cellState === "selectedAnchor" || cellState === "selectedRange") && document.addEventListener("paste", handlePaste), cellState === "selectedAnchor" && document.addEventListener("copy", handleCopy), () => {
    (cellState === "selectedAnchor" || cellState === "focused") && document.removeEventListener("keydown", handleOnEnterDown), (cellState === "selectedAnchor" || cellState === "selectedRange") && document.removeEventListener("paste", handlePaste), cellState === "selectedAnchor" && document.removeEventListener("copy", handleCopy);
  }), [cellId, cellState, column.id, getStateByCellId, handleCopy, handleOnEnterDown, handlePaste, row.index]);
  const getBorderStyle = () => cellState === "focused" ? "2px solid blue" : cellState === "selectedRange" ? "1px solid green" : cellState === "selectedAnchor" ? "1px solid blue" : "1px solid transparent", inputProps = {
    onFocus: handleOnFocus,
    onBlur: handleOnBlur,
    onMouseDown: handleOnMouseDown,
    "aria-selected": !!cellState,
    "data-testid": cellId,
    id: cellId,
    ref: (ref) => {
      inputRef.current = ref;
    }
  };
  return fieldType.name === "boolean" ? (0, import_jsx_runtime3.jsxs)(Select, { ...inputProps, onChange: () => null, radius: 0, style: {
    boxShadow: "none",
    border: getBorderStyle(),
    padding: 0
  }, value: JSON.stringify(renderValue), children: [
    (0, import_jsx_runtime3.jsx)("option", { value: "True", children: "True" }),
    (0, import_jsx_runtime3.jsx)("option", { value: "False", children: "False" })
  ] }) : (0, import_jsx_runtime3.jsx)(TextInput, { ...inputProps, size: 0, radius: 0, border: false, style: {
    border: getBorderStyle(),
    padding: "22px 16px"
  }, value: typeof renderValue == "string" || typeof renderValue == "number" ? renderValue : JSON.stringify(renderValue), onChange: handleOnChange });
}
function SheetListCell(cell) {
  var _a;
  const isPinned = cell.column.getIsPinned(), Cell = isPinned ? PinnedDataCell : DataCell, borderWidth = isPinned && cell.column.getIsLastColumn("left") ? 2 : 1;
  return (0, import_jsx_runtime3.jsx)(Cell, { style: {
    left: cell.column.getStart("left") ?? void 0,
    borderRight: `${borderWidth}px solid var(--card-border-color)`
  }, width: cell.column.getSize(), children: flexRender(cell.column.columnDef.cell, (_a = cell.getContext) == null ? void 0 : _a.call(cell)) }, cell.row.original._id + cell.id);
}
var VISIBLE_COLUMN_LIMIT = 5;
var PreviewCell = (props) => {
  const {
    documentPreviewStore,
    row,
    schemaType
  } = props, previewStateObservable = (0, import_react3.useMemo)(() => getPreviewStateObservable$1(documentPreviewStore, schemaType, row.original._id), [documentPreviewStore, row.original._id, schemaType]), versionsInfo = useDocumentVersionInfo(row.original._id), {
    snapshot,
    isLoading
  } = useObservable(previewStateObservable, {
    snapshot: null,
    isLoading: true
  });
  if (isLoading)
    return (0, import_jsx_runtime3.jsx)(Text, { size: 1, muted: true, children: "Loading..." });
  const displayValue = (snapshot == null ? void 0 : snapshot.title) ?? "Untitled";
  return (0, import_jsx_runtime3.jsxs)(Flex, { align: "center", gap: 3, children: [
    (0, import_jsx_runtime3.jsx)(DocumentStatusIndicator, { draft: versionsInfo.draft, published: versionsInfo.published, versions: void 0 }),
    (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: displayValue })
  ] });
};
var columnHelper = createColumnHelper();
var SUPPORTED_FIELDS = ["string", "number", "boolean"];
var getColsFromSchemaType = (schemaType, parentalField) => schemaType.fields.reduce((tableColumns, field) => {
  const {
    type: type2,
    name
  } = field;
  if (SUPPORTED_FIELDS.includes(type2.name)) {
    const nextCol = columnHelper.accessor(
      // accessor must use dot notation for internal tanstack method of reading cell data
      parentalField ? `${parentalField}.${field.name}` : field.name,
      {
        id: parentalField ? `${parentalField}_${field.name}` : field.name,
        header: field.type.title,
        enableHiding: true,
        cell: (info) => (0, import_jsx_runtime3.jsx)(SheetListCellInner, { ...info, fieldType: type2 })
      }
    );
    return [...tableColumns, nextCol];
  }
  return type2.name === "object" && isObjectSchemaType(type2) && !parentalField ? [...tableColumns, columnHelper.group({
    header: name,
    columns: getColsFromSchemaType(type2, field.name)
  })] : tableColumns;
}, []);
function isAccessorKeyColumnDef(column) {
  return "accessorKey" in column;
}
function isGroupColumnDef(column) {
  return "columns" in column;
}
var flatColumns = (cols) => cols.flatMap((col) => isAccessorKeyColumnDef(col) ? col : isGroupColumnDef(col) ? col.columns ? flatColumns(col.columns) : [] : []);
function useDocumentSheetColumns(documentSchemaType) {
  const documentPreviewStore = useDocumentPreviewStore(), columns = (0, import_react3.useMemo)(() => documentSchemaType ? [columnHelper.display({
    id: "selected",
    enableHiding: false,
    header: (info) => (0, import_jsx_runtime3.jsxs)(Box, { children: [
      (0, import_jsx_runtime3.jsx)(Checkbox, { style: {
        paddingLeft: 4
      }, indeterminate: info.table.getIsSomeRowsSelected(), onChange: info.table.getToggleAllPageRowsSelectedHandler() }),
      info.table.getSelectedRowModel().rows.length,
      " selected"
    ] }),
    cell: DocumentSheetListSelect
  }), columnHelper.accessor("Preview", {
    enableHiding: false,
    id: "Preview",
    cell: (info_0) => (0, import_jsx_runtime3.jsx)(PreviewCell, { ...info_0, documentPreviewStore, schemaType: documentSchemaType })
  }), ...getColsFromSchemaType(documentSchemaType)] : [], [documentPreviewStore, documentSchemaType]), [initialColumnsVisibility] = (0, import_react3.useMemo)(() => flatColumns(columns).reduce(([accCols, countAllowedVisible], column) => {
    if (!column.id) throw new Error("Column must have an id");
    const visibilityKey = column.id;
    return column.enableHiding ? countAllowedVisible === VISIBLE_COLUMN_LIMIT ? [{
      ...accCols,
      [visibilityKey]: false
    }, countAllowedVisible] : [{
      ...accCols,
      [visibilityKey]: true
    }, countAllowedVisible + 1] : [{
      ...accCols,
      [visibilityKey]: true
    }, countAllowedVisible];
  }, [{}, 0]), [columns]);
  return {
    columns,
    initialColumnsVisibility
  };
}
function ColumnsControl({
  table
}) {
  const {
    t: t2
  } = useTranslation(), isVisibleLimitReached = table.getVisibleLeafColumns().filter((col) => col.getCanHide()).length >= VISIBLE_COLUMN_LIMIT, setInitialColumns = (0, import_react3.useCallback)(() => {
    table.resetColumnVisibility();
  }, [table]), handleColumnOnChange = (column) => () => {
    column.toggleVisibility();
  }, getColumnVisibilityDisabled = (column_0) => {
    const isColumnVisible = column_0.getIsVisible(), isSingleColumnVisible = table.getVisibleLeafColumns().filter((col_0) => col_0.getCanHide()).length === 1;
    return isVisibleLimitReached && !isColumnVisible || isSingleColumnVisible && isColumnVisible;
  };
  return (0, import_jsx_runtime3.jsx)(MenuButton, { button: (0, import_jsx_runtime3.jsx)(Button2, { mode: "bleed", text: t2("sheet-list.edit-columns") }), id: "columns-control", menu: (0, import_jsx_runtime3.jsx)(Menu, { padding: 3, paddingTop: 4, style: {
    width: 240
  }, children: (0, import_jsx_runtime3.jsxs)(Flex, { direction: "column", height: "fill", gap: 3, children: [
    (0, import_jsx_runtime3.jsx)(Text, { weight: "semibold", size: 1, children: t2("sheet-list.select-fields") }),
    (0, import_jsx_runtime3.jsx)(Flex, { style: {
      flex: "1 1 auto",
      maxHeight: 320,
      overflowY: "scroll"
    }, children: (0, import_jsx_runtime3.jsx)(Stack, { children: table.getAllLeafColumns().filter((column_1) => column_1.getCanHide()).map((column_2) => {
      var _a;
      return (0, import_jsx_runtime3.jsxs)(Flex, { marginY: 2, align: "center", children: [
        (0, import_jsx_runtime3.jsx)(Checkbox, { readOnly: getColumnVisibilityDisabled(column_2), checked: column_2.getIsVisible(), onChange: handleColumnOnChange(column_2), id: `col-visibility-${column_2.id}`, style: {
          display: "block"
        } }),
        (0, import_jsx_runtime3.jsx)(Box, { flex: 1, paddingLeft: 3, children: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: (0, import_jsx_runtime3.jsx)("label", { htmlFor: `col-visibility-${column_2.id}`, children: (_a = column_2.columnDef.header) == null ? void 0 : _a.toString() }) }) })
      ] }, column_2.id);
    }) }) }),
    (0, import_jsx_runtime3.jsx)(Button2, { width: "fill", mode: "ghost", text: t2("sheet-list.reset-columns"), onClick: setInitialColumns })
  ] }) }), placement: "bottom", popover: {
    portal: true
  } });
}
var SearchContainer = dt(Flex)`
  flex-shrink: 0;
`;
function DocumentSheetListFilter() {
  const $ = (0, import_react_compiler_runtime.c)(5), {
    state: t0
  } = useSearchState(), {
    filtersVisible
  } = t0;
  let t1;
  $[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = (0, import_jsx_runtime3.jsx)(SearchHeader, {}), $[0] = t1) : t1 = $[0];
  let t2;
  $[1] !== filtersVisible ? (t2 = filtersVisible && (0, import_jsx_runtime3.jsx)(Filters, { showTypeFilter: false }), $[1] = filtersVisible, $[2] = t2) : t2 = $[2];
  let t3;
  return $[3] !== t2 ? (t3 = (0, import_jsx_runtime3.jsxs)(SearchContainer, { children: [
    t1,
    t2
  ] }), $[3] = t2, $[4] = t3) : t3 = $[4], t3;
}
var Header = dt.th`
  margin: 16px;
  z-index: 1;
  padding: 22px 0px;
  border-top: 1px solid var(--card-border-color);
  background-color: var(--card-badge-default-bg-color);
  box-sizing: border-box;
  text-align: left;
  width: ${({
  width
}) => width}px;
  max-width: ${({
  width
}) => width}px;
`;
var PinnedHeader = dt(Header)`
  position: sticky;
  z-index: 2;
`;
var HoverMenu = dt.div`
  visibility: hidden;

  ${Header}:hover & {
    visibility: visible;
  }
`;
function DocumentSheetListHeader(props) {
  const {
    header,
    headerGroup
  } = props, {
    t: t2
  } = useTranslation(), isPinned = header.column.getIsPinned(), headerTitle = headerGroup.depth > 0 && !header.column.parent ? null : (0, import_jsx_runtime3.jsx)(Text, { size: 1, weight: "semibold", textOverflow: "ellipsis", children: flexRender(header.column.columnDef.header, header.getContext()) }), HeaderTag = isPinned ? PinnedHeader : Header, canShowHeaderMenu = header.column.getCanHide() && (headerGroup.depth === 0 ? !header.column.columns.length : header.column.parent), borderWidth = isPinned && header.column.getIsLastColumn("left") ? 2 : 1;
  return (0, import_jsx_runtime3.jsx)(HeaderTag, { style: {
    left: header.column.getStart("left") ?? void 0,
    borderRight: `${borderWidth}px solid var(--card-border-color)`
  }, "data-testid": `header-${header.id}`, width: header.getSize(), children: (0, import_jsx_runtime3.jsxs)(Flex, { justify: "space-between", marginX: 2, align: "baseline", children: [
    (0, import_jsx_runtime3.jsx)(Tooltip, { delay: 500, content: headerTitle, children: (0, import_jsx_runtime3.jsx)(Box, { style: {
      boxSizing: "border-box"
    }, marginLeft: 3, marginRight: 3, children: headerTitle }) }),
    canShowHeaderMenu && (0, import_jsx_runtime3.jsx)(HoverMenu, { children: (0, import_jsx_runtime3.jsx)(MenuButton, { button: (0, import_jsx_runtime3.jsx)(Button2, { tooltipProps: {
      content: "Open field menu"
    }, mode: "bleed", icon: EllipsisHorizontalIcon, "data-testid": "field-menu-button" }), id: "field menu", popover: {
      placement: "bottom-end"
    }, menu: (0, import_jsx_runtime3.jsx)(Menu, { children: (0, import_jsx_runtime3.jsx)(MenuItem2, { text: t2("sheet-list.hide-field"), icon: CloseIcon, onClick: () => header.column.toggleVisibility() }) }) }) })
  ] }) }, header.id);
}
function DocumentSheetListPaginator({
  table
}) {
  return (0, import_jsx_runtime3.jsx)(TooltipDelayGroupProvider, { children: (0, import_jsx_runtime3.jsxs)(Flex, { gap: 3, align: "center", children: [
    (0, import_jsx_runtime3.jsx)(Button2, { onClick: () => table.setPageIndex(0), disabled: !table.getCanPreviousPage(), icon: DoubleChevronLeftIcon, tooltipProps: {
      content: "Go to first page"
    } }),
    (0, import_jsx_runtime3.jsx)(Button2, { onClick: () => table.previousPage(), disabled: !table.getCanPreviousPage(), icon: ChevronLeftIcon, tooltipProps: {
      content: "Go to previous page"
    } }),
    (0, import_jsx_runtime3.jsxs)(Text, { style: {
      whiteSpace: "nowrap"
    }, children: [
      table.getState().pagination.pageIndex + 1,
      " of ",
      table.getPageCount()
    ] }),
    (0, import_jsx_runtime3.jsx)(Button2, { onClick: () => table.nextPage(), disabled: !table.getCanNextPage(), icon: ChevronRightIcon, tooltipProps: {
      content: "Go to next page"
    } }),
    (0, import_jsx_runtime3.jsx)(Button2, { onClick: () => table.setPageIndex(table.getPageCount() - 1), disabled: !table.getCanNextPage(), icon: DoubleChevronRightIcon, tooltipProps: {
      content: "Go to last page"
    } }),
    (0, import_jsx_runtime3.jsx)("select", { value: table.getState().pagination.pageSize, onChange: (e2) => {
      table.setPageSize(Number(e2.target.value));
    }, children: [25, 50, 100].map((pageSize) => (0, import_jsx_runtime3.jsx)("option", { value: pageSize, children: pageSize }, pageSize)) })
  ] }) });
}
function createDocumentsSet(documents) {
  return documents.reduce((acc, doc) => ({
    ...acc,
    [doc._id]: doc
  }), {});
}
function documentsReducer(state2, action) {
  switch (action.type) {
    case "DOCUMENTS_SET": {
      const documents = createDocumentsSet(action.documents);
      return {
        ...state2,
        documents
      };
    }
    case "DOCUMENT_RECEIVED": {
      const nextDocumentResult = action.payload;
      return {
        ...state2,
        documents: {
          ...state2.documents,
          [nextDocumentResult._id]: nextDocumentResult
        }
      };
    }
    case "DOCUMENT_DELETED": {
      const {
        [action.id]: _,
        ...restDocuments
      } = state2.documents;
      return {
        ...state2,
        documents: restDocuments
      };
    }
    case "DOCUMENT_UPDATED": {
      const updatedDocument = action.payload, id2 = updatedDocument._id, nextDocument = {
        // Add existing document data
        ...state2.documents[id2],
        // Add incoming document data
        ...updatedDocument
      };
      return {
        ...state2,
        documents: {
          ...state2.documents,
          [id2]: nextDocument
        }
      };
    }
    default:
      return state2;
  }
}
var LISTEN_OPTIONS = {
  events: ["welcome", "mutation", "reconnect"],
  includeResult: true,
  visibility: "query",
  includeAllVersions: true,
  tag: "document-sheet-list-store"
};
function useDocumentSheetListStore(t0) {
  const $ = (0, import_react_compiler_runtime.c)(25), {
    filter: filter2,
    params,
    apiVersion
  } = t0, QUERY = `*[${filter2}][0...2000]`, t1 = apiVersion || DEFAULT_STUDIO_CLIENT_OPTIONS.apiVersion;
  let t2;
  $[0] !== t1 ? (t2 = {
    ...DEFAULT_STUDIO_CLIENT_OPTIONS,
    apiVersion: t1
  }, $[0] = t1, $[1] = t2) : t2 = $[1];
  const client = useClient(t2);
  let t3;
  $[2] === Symbol.for("react.memo_cache_sentinel") ? (t3 = {
    documents: {}
  }, $[2] = t3) : t3 = $[2];
  const [state2, dispatch] = (0, import_react3.useReducer)(documentsReducer, t3), [isLoading, setIsLoading] = (0, import_react3.useState)(client !== null), [error, setError] = (0, import_react3.useState)(null);
  let t4;
  $[3] !== QUERY || $[4] !== client || $[5] !== params ? (t4 = async () => {
    if (!client) {
      setIsLoading(false);
      return;
    }
    try {
      const res = await client.fetch(QUERY, params);
      dispatch({
        type: "DOCUMENTS_SET",
        documents: res
      }), setIsLoading(false);
    } catch (t52) {
      setError(t52);
    }
  }, $[3] = QUERY, $[4] = client, $[5] = params, $[6] = t4) : t4 = $[6];
  const initialFetch = t4;
  let t5;
  $[7] !== initialFetch ? (t5 = async (event) => {
    if (event.type === "welcome" && (setIsLoading(true), await initialFetch(), setIsLoading(false)), event.type === "reconnect" && setIsLoading(true), event.type === "mutation") {
      if (event.transition === "appear") {
        const nextDocument = event.result;
        nextDocument && dispatch({
          type: "DOCUMENT_RECEIVED",
          payload: nextDocument
        });
      }
      if (event.transition === "disappear" && dispatch({
        type: "DOCUMENT_DELETED",
        id: event.documentId
      }), event.transition === "update") {
        const updatedDocument = event.result;
        updatedDocument && dispatch({
          type: "DOCUMENT_UPDATED",
          payload: updatedDocument
        });
      }
    }
  }, $[7] = initialFetch, $[8] = t5) : t5 = $[8];
  const handleListenerEvent = t5;
  let t6;
  bb0: {
    if (!client) {
      let t73;
      $[9] === Symbol.for("react.memo_cache_sentinel") ? (t73 = of(), $[9] = t73) : t73 = $[9], t6 = t73;
      break bb0;
    }
    let t72;
    if ($[10] !== QUERY || $[11] !== client.observable || $[12] !== params) {
      let t82;
      $[14] === Symbol.for("react.memo_cache_sentinel") ? (t82 = (err_0) => (setError(err_0), of(err_0)), $[14] = t82) : t82 = $[14], t72 = client.observable.listen(QUERY, params, LISTEN_OPTIONS).pipe(catchError(t82)), $[10] = QUERY, $[11] = client.observable, $[12] = params, $[13] = t72;
    } else
      t72 = $[13];
    t6 = t72;
  }
  const listener$ = t6;
  let t7, t8;
  $[15] !== handleListenerEvent || $[16] !== listener$ ? (t7 = () => {
    const sub = listener$.subscribe(handleListenerEvent);
    return () => {
      sub == null ? void 0 : sub.unsubscribe();
    };
  }, t8 = [handleListenerEvent, listener$], $[15] = handleListenerEvent, $[16] = listener$, $[17] = t7, $[18] = t8) : (t7 = $[17], t8 = $[18]), (0, import_react3.useEffect)(t7, t8);
  let t9, t10;
  if ($[19] !== state2.documents) {
    const uniques = Object.keys(state2.documents).reduce((acc, key) => {
      const document2 = state2.documents[key], isDraft = document2._id === getDraftId(document2._id), id2 = isDraft ? document2._id : getDraftId(document2._id);
      return acc[id2] && !isDraft || (acc[id2] = document2), acc;
    }, {});
    t10 = Object.values(uniques), $[19] = state2.documents, $[20] = t10;
  } else
    t10 = $[20];
  t9 = t10;
  const dataAsArray = t9;
  let t11;
  return $[21] !== dataAsArray || $[22] !== error || $[23] !== isLoading ? (t11 = {
    data: dataAsArray,
    isLoading,
    error
  }, $[21] = dataAsArray, $[22] = error, $[23] = isLoading, $[24] = t11) : t11 = $[24], t11;
}
function useDocumentSheetList(t0) {
  const $ = (0, import_react_compiler_runtime.c)(12), {
    typeName
  } = t0, {
    state: state2
  } = useSearchState();
  let t1, map2;
  $[0] !== state2.result.hits ? (map2 = /* @__PURE__ */ new Map(), state2.result.hits.forEach((h) => map2.set(getPublishedId(h.hit._id), h.hit)), $[0] = state2.result.hits, $[1] = map2) : map2 = $[1], t1 = map2;
  const items2 = t1, t2 = `_type == "${typeName}"`;
  let t3;
  $[2] !== t2 ? (t3 = {
    filter: t2
  }, $[2] = t2, $[3] = t3) : t3 = $[3];
  const {
    data,
    isLoading
  } = useDocumentSheetListStore(t3);
  let t4, t5;
  if ($[4] !== data || $[5] !== items2) {
    let t62;
    $[7] !== items2 ? (t62 = (doc) => items2.has(getPublishedId(doc._id)), $[7] = items2, $[8] = t62) : t62 = $[8], t5 = data.filter(t62), $[4] = data, $[5] = items2, $[6] = t5;
  } else
    t5 = $[6];
  t4 = t5;
  const documents = t4;
  let t6;
  return $[9] !== documents || $[10] !== isLoading ? (t6 = {
    data: documents,
    isLoading
  }, $[9] = documents, $[10] = isLoading, $[11] = t6) : t6 = $[11], t6;
}
var PaneContainer$1 = dt(Flex)`
  height: 100%;
`;
var TableContainer = dt.div`
  overflow: auto; //our scrollable table container
  position: relative; //needed for sticky header
`;
var Table = dt.table`
  border-collapse: separate;
  border-spacing: 0;
  font-family: arial, sans-serif;
  white-space: nowrap;
  width: 100%;
  border: 1px solid lightgray;

  thead {
    display: grid;
    position: sticky;
    top: 0;
    z-index: 10;
  }
  tr {
    padding: 0;
  }
  tr:last-child {
    border-bottom: none;
  }
`;
function DocumentSheetListPaneInner({
  documentSchemaType
}) {
  const {
    dispatch,
    state: state2
  } = useSearchState(), {
    columns,
    initialColumnsVisibility
  } = useDocumentSheetColumns(documentSchemaType), {
    data
  } = useDocumentSheetList({
    typeName: documentSchemaType.name
  }), [selectedAnchor, setSelectedAnchor] = (0, import_react3.useState)(null), totalRows = state2.result.hits.length, table = useReactTable({
    data,
    columns,
    getCoreRowModel: getCoreRowModel(),
    getFilteredRowModel: getFilteredRowModel(),
    getPaginationRowModel: getPaginationRowModel(),
    // Avoids resetting the page index when the data changes, e.g. a mutation is received
    autoResetPageIndex: false,
    initialState: {
      columnPinning: {
        left: ["selected", "Preview"]
      },
      pagination: {
        pageSize: 25
      },
      columnVisibility: initialColumnsVisibility
    },
    getRowId: (row) => row._id,
    meta: {
      selectedAnchor,
      setSelectedAnchor,
      patchDocument: (documentId, fieldId, value) => null
    }
  }), {
    rows
  } = table.getRowModel();
  (0, import_react3.useEffect)(() => (dispatch({
    type: "TERMS_TYPE_ADD",
    schemaType: documentSchemaType
  }), () => {
    dispatch({
      type: "TERMS_TYPE_REMOVE",
      schemaType: documentSchemaType
    });
  }), [documentSchemaType, dispatch]);
  const renderRow = (0, import_react3.useCallback)((row_0) => (0, import_jsx_runtime3.jsx)(Box, { as: "tr", paddingY: 2, style: {
    display: "flex",
    width: "100%"
  }, children: row_0.getVisibleCells().map((cell) => (0, import_react3.createElement)(SheetListCell, { ...cell, key: row_0.original._id + cell.id })) }, row_0.original._id + row_0.id), []), rowsCount = `Total: ${totalRows} rows, showing ${rows.length} rows`;
  return (0, import_jsx_runtime3.jsxs)(PaneContainer$1, { direction: "column", paddingX: 3, "data-testid": "document-sheet-list-pane", children: [
    (0, import_jsx_runtime3.jsxs)(Flex, { direction: "row", align: "center", paddingY: 3, paddingX: 1, justify: "space-between", children: [
      (0, import_jsx_runtime3.jsxs)(Flex, { direction: "row", align: "center", children: [
        (0, import_jsx_runtime3.jsx)(DocumentSheetListFilter, {}),
        (0, import_jsx_runtime3.jsx)(Text, { size: 0, muted: true, children: rowsCount })
      ] }),
      (0, import_jsx_runtime3.jsx)(ColumnsControl, { table })
    ] }),
    (0, import_jsx_runtime3.jsx)(TableContainer, { children: (0, import_jsx_runtime3.jsx)(DocumentSheetListProvider, { table, children: (0, import_jsx_runtime3.jsxs)(Table, { children: [
      (0, import_jsx_runtime3.jsx)("thead", { children: table.getHeaderGroups().map((headerGroup) => (0, import_jsx_runtime3.jsx)(Box, { as: "tr", children: headerGroup.headers.map((header) => (0, import_jsx_runtime3.jsx)(DocumentSheetListHeader, { header, headerGroup }, header.id)) }, headerGroup.id)) }),
      (0, import_jsx_runtime3.jsx)("tbody", { children: table.getRowModel().rows.map(renderRow) })
    ] }) }) }),
    (0, import_jsx_runtime3.jsx)(Flex, { justify: "flex-end", padding: 3, gap: 4, paddingY: 5, children: (0, import_jsx_runtime3.jsx)(DocumentSheetListPaginator, { table }) })
  ] });
}
function DocumentSheetListPane(props) {
  const schema = useSchema(), typeName = props.pane.schemaTypeName, schemaType = schema.get(typeName);
  if (!schemaType || !isDocumentSchemaType(schemaType))
    throw new Error(`Schema type "${typeName}" not found or not a document schema`);
  return (0, import_jsx_runtime3.jsx)(SearchProvider, { children: (0, import_jsx_runtime3.jsx)(DocumentSheetListPaneInner, { ...props, documentSchemaType: schemaType }) });
}
var addSelectedStateToMenuItems = (options) => {
  const {
    menuItems,
    sortOrderRaw,
    layout
  } = options;
  return menuItems == null ? void 0 : menuItems.map((item) => {
    var _a, _b, _c, _d;
    return ((_a = item.params) == null ? void 0 : _a.layout) ? {
      ...item,
      selected: layout === ((_b = item.params) == null ? void 0 : _b.layout)
    } : ((_c = item == null ? void 0 : item.params) == null ? void 0 : _c.by) ? {
      ...item,
      selected: (0, import_isEqual.default)(sortOrderRaw == null ? void 0 : sortOrderRaw.by, ((_d = item == null ? void 0 : item.params) == null ? void 0 : _d.by) || EMPTY_ARRAY$B)
    } : {
      ...item,
      selected: false
    };
  });
};
function useShallowUnique(value) {
  const [previous, setPrevious] = (0, import_react3.useState)(value);
  return (0, import_shallow_equals.default)(previous, value) ? previous : (setPrevious(value), value);
}
var PaneContainer = (0, import_react3.memo)(function(props) {
  const $ = (0, import_react_compiler_runtime.c)(36), {
    index,
    isSelected,
    pane: pane2,
    paneKey
  } = props, {
    name: parentSourceName
  } = useSource(), {
    defaultLayout: t0,
    initialValueTemplates: t1,
    menuItemGroups,
    menuItems,
    options
  } = pane2, defaultLayout = t0 === void 0 ? "default" : t0, initialValueTemplates = t1 === void 0 ? EMPTY_ARRAY$B : t1, {
    defaultOrdering: t2,
    filter: filter2
  } = options, defaultOrdering = t2 === void 0 ? EMPTY_ARRAY$B : t2, params = useShallowUnique(options.params || EMPTY_RECORD), sourceName = pane2.source;
  let t3;
  bb0: {
    let t42;
    $[0] !== filter2 || $[1] !== params ? (t42 = findStaticTypesInFilter(filter2, params), $[0] = filter2, $[1] = params, $[2] = t42) : t42 = $[2];
    const staticTypes = t42;
    if ((staticTypes == null ? void 0 : staticTypes.length) === 1) {
      t3 = staticTypes[0];
      break bb0;
    }
    t3 = null;
  }
  const typeName = t3, [layout, setLayout] = useStructureToolSetting("layout", typeName ?? pane2.id, defaultLayout), {
    title
  } = useI18nText(pane2);
  let t4, t5;
  $[3] !== defaultOrdering ? (t5 = (defaultOrdering == null ? void 0 : defaultOrdering.length) > 0 ? {
    by: defaultOrdering
  } : DEFAULT_ORDERING, $[3] = defaultOrdering, $[4] = t5) : t5 = $[4], t4 = t5;
  const defaultSortOrder = t4, [sortOrderRaw, setSortOrder] = useStructureToolSetting("sort-order", typeName ?? pane2.id, defaultSortOrder);
  let t6, t7;
  $[5] !== layout || $[6] !== menuItems || $[7] !== sortOrderRaw ? (t7 = addSelectedStateToMenuItems({
    menuItems,
    sortOrderRaw,
    layout
  }), $[5] = layout, $[6] = menuItems, $[7] = sortOrderRaw, $[8] = t7) : t7 = $[8], t6 = t7;
  const menuItemsWithSelectedState = t6, isSheetListLayout = layout === "sheetList";
  let t8;
  $[9] !== isSheetListLayout || $[10] !== layout || $[11] !== props || $[12] !== sortOrderRaw ? (t8 = isSheetListLayout ? (0, import_react3.createElement)(DocumentSheetListPane, { ...props, key: props.pane.id }) : (0, import_jsx_runtime3.jsx)(DocumentListPane, { ...props, sortOrder: sortOrderRaw, layout }), $[9] = isSheetListLayout, $[10] = layout, $[11] = props, $[12] = sortOrderRaw, $[13] = t8) : t8 = $[13];
  const paneLayout = t8, t9 = sourceName || parentSourceName;
  let t10;
  $[14] !== isSheetListLayout ? (t10 = isSheetListLayout ? {} : {
    currentMaxWidth: 350,
    maxWidth: 640
  }, $[14] = isSheetListLayout, $[15] = t10) : t10 = $[15];
  let t11;
  $[16] !== pane2.source ? (t11 = _DEBUG, $[16] = pane2.source, $[17] = t11) : t11 = $[17];
  let t12;
  $[18] !== index || $[19] !== initialValueTemplates || $[20] !== menuItemGroups || $[21] !== menuItemsWithSelectedState || $[22] !== setLayout || $[23] !== setSortOrder || $[24] !== title ? (t12 = (0, import_jsx_runtime3.jsx)(PaneHeader, { index, initialValueTemplates, menuItemGroups, menuItems: menuItemsWithSelectedState, setLayout, setSortOrder, title }), $[18] = index, $[19] = initialValueTemplates, $[20] = menuItemGroups, $[21] = menuItemsWithSelectedState, $[22] = setLayout, $[23] = setSortOrder, $[24] = title, $[25] = t12) : t12 = $[25];
  let t13;
  $[26] !== isSelected || $[27] !== paneKey || $[28] !== paneLayout || $[29] !== t10 || $[30] !== t11 || $[31] !== t12 ? (t13 = (0, import_jsx_runtime3.jsxs)(Pane, { "data-ui": "DocumentListPane", id: paneKey, minWidth: 320, ...t10, selected: isSelected, children: [
    t11,
    t12,
    paneLayout
  ] }), $[26] = isSelected, $[27] = paneKey, $[28] = paneLayout, $[29] = t10, $[30] = t11, $[31] = t12, $[32] = t13) : t13 = $[32];
  let t14;
  return $[33] !== t13 || $[34] !== t9 ? (t14 = (0, import_jsx_runtime3.jsx)(SourceProvider, { name: t9, children: t13 }), $[33] = t13, $[34] = t9, $[35] = t14) : t14 = $[35], t14;
});
PaneContainer.displayName = "Memo(PaneContainer)";
var LiveEditBadge = (props) => {
  const {
    liveEditSchemaType,
    version
  } = props;
  return liveEditSchemaType && !version ? {
    label: "Live",
    color: "danger"
  } : null;
};
LiveEditBadge.displayName = "LiveEditBadge";
var state = {
  activePanes: []
};
function setActivePanes(panes) {
  state.activePanes = panes;
}
function getIntentState(intent, params, routerState, payload) {
  var _a;
  const panes = (routerState == null ? void 0 : routerState.panes) || [], activePanes = state.activePanes || [], editDocumentId = params.id || v4_default();
  for (let i = activePanes.length - 1; i >= 0; i--) {
    const pane2 = activePanes[i];
    if (typeof pane2 == "object" && (((_a = pane2.canHandleIntent) == null ? void 0 : _a.call(pane2, intent, params, {
      pane: pane2,
      index: i
    })) || // see `resolveIntent.ts` for more info
    pane2.type === "documentList" && pane2.schemaTypeName === params.type && pane2.options.filter === "_type == $type")) {
      const paneParams = getPaneParams(intent, params);
      return {
        panes: panes.slice(0, i).concat([[{
          id: editDocumentId,
          params: paneParams,
          payload
        }]])
      };
    }
  }
  return {
    intent,
    params,
    payload
  };
}
function getPaneParams(intent, {
  template,
  version
}) {
  return intent !== "create" ? EMPTY_PARAMS$1 : template && version ? {
    template,
    version
  } : template ? {
    template
  } : version ? {
    version
  } : EMPTY_PARAMS$1;
}
var Scroller$3 = dt(ScrollContainer)`
  height: 100%;
  overflow: auto;
  position: relative;
  scroll-behavior: smooth;
`;
var Grid$1 = dt(Box)`
  &:not([hidden]) {
    display: grid;
  }
  grid-template-columns: 48px 1fr;
  align-items: center;
  gap: 0.25em;
`;
function ChangesInspector(t0) {
  const $ = (0, import_react_compiler_runtime.c)(39), {
    showChanges
  } = t0, {
    documentId,
    schemaType,
    timelineError,
    timelineStore,
    value
  } = useDocumentPane(), {
    selectedReleaseId
  } = usePerspective(), [scrollRef, setScrollRef] = (0, import_react3.useState)(null), rev = useTimelineSelector(timelineStore, _temp$2), diff = useTimelineSelector(timelineStore, _temp2), onOlderRevision = useTimelineSelector(timelineStore, _temp3), selectionState = useTimelineSelector(timelineStore, _temp4), sinceTime = useTimelineSelector(timelineStore, _temp5), loading = selectionState === "loading", isComparingCurrent = !onOlderRevision, {
    t: structureT
  } = useTranslation(structureLocaleNamespace);
  let t1, t2;
  $[0] !== diff || $[1] !== documentId || $[2] !== isComparingCurrent || $[3] !== schemaType || $[4] !== value ? (t2 = {
    documentId,
    schemaType,
    FieldWrapper: _temp6,
    rootDiff: diff,
    isComparingCurrent,
    value,
    showFromValue: true
  }, $[0] = diff, $[1] = documentId, $[2] = isComparingCurrent, $[3] = schemaType, $[4] = value, $[5] = t2) : t2 = $[5], t1 = t2;
  const documentContext = t1;
  if (selectedReleaseId) {
    let t32;
    return $[6] === Symbol.for("react.memo_cache_sentinel") ? (t32 = (0, import_jsx_runtime3.jsx)(Flex, { "data-testid": "review-changes-pane", direction: "column", height: "fill", children: (0, import_jsx_runtime3.jsx)(Card, { flex: 1, padding: 2, paddingTop: 0, children: (0, import_jsx_runtime3.jsx)(TimelineError, { versionError: true }) }) }), $[6] = t32) : t32 = $[6], t32;
  }
  let t3;
  $[7] !== structureT ? (t3 = structureT("changes.from.label"), $[7] = structureT, $[8] = t3) : t3 = $[8];
  let t4;
  $[9] !== t3 ? (t4 = (0, import_jsx_runtime3.jsx)(Text, { size: 1, muted: true, children: t3 }), $[9] = t3, $[10] = t4) : t4 = $[10];
  let t5;
  $[11] !== sinceTime ? (t5 = (0, import_jsx_runtime3.jsx)(TimelineMenu, { mode: "since", chunk: sinceTime, placement: "bottom-start" }), $[11] = sinceTime, $[12] = t5) : t5 = $[12];
  let t6;
  $[13] !== structureT ? (t6 = structureT("changes.to.label"), $[13] = structureT, $[14] = t6) : t6 = $[14];
  let t7;
  $[15] !== t6 ? (t7 = (0, import_jsx_runtime3.jsx)(Text, { size: 1, muted: true, children: t6 }), $[15] = t6, $[16] = t7) : t7 = $[16];
  let t8;
  $[17] !== rev ? (t8 = (0, import_jsx_runtime3.jsx)(TimelineMenu, { chunk: rev, mode: "rev", placement: "bottom-end" }), $[17] = rev, $[18] = t8) : t8 = $[18];
  let t9;
  $[19] !== t4 || $[20] !== t5 || $[21] !== t7 || $[22] !== t8 ? (t9 = (0, import_jsx_runtime3.jsx)(Box, { padding: 3, children: (0, import_jsx_runtime3.jsxs)(Grid$1, { paddingX: 2, paddingBottom: 2, children: [
    t4,
    t5,
    t7,
    t8
  ] }) }), $[19] = t4, $[20] = t5, $[21] = t7, $[22] = t8, $[23] = t9) : t9 = $[23];
  let t10;
  $[24] !== diff || $[25] !== documentContext || $[26] !== loading || $[27] !== schemaType || $[28] !== showChanges || $[29] !== timelineError ? (t10 = showChanges && (0, import_jsx_runtime3.jsx)(Content$1, { diff, documentContext, error: timelineError, loading, schemaType }), $[24] = diff, $[25] = documentContext, $[26] = loading, $[27] = schemaType, $[28] = showChanges, $[29] = timelineError, $[30] = t10) : t10 = $[30];
  let t11;
  $[31] !== t10 ? (t11 = (0, import_jsx_runtime3.jsx)(Scroller$3, { "data-ui": "Scroller", ref: setScrollRef, children: (0, import_jsx_runtime3.jsx)(Box, { flex: 1, paddingX: 3, height: "fill", children: t10 }) }), $[31] = t10, $[32] = t11) : t11 = $[32];
  let t12;
  $[33] !== scrollRef || $[34] !== t11 ? (t12 = (0, import_jsx_runtime3.jsx)(Card, { flex: 1, paddingX: 2, paddingY: 2, children: (0, import_jsx_runtime3.jsx)(BoundaryElementProvider, { element: scrollRef, children: t11 }) }), $[33] = scrollRef, $[34] = t11, $[35] = t12) : t12 = $[35];
  let t13;
  return $[36] !== t12 || $[37] !== t9 ? (t13 = (0, import_jsx_runtime3.jsxs)(Flex, { "data-testid": "review-changes-pane", direction: "column", height: "fill", overflow: "hidden", children: [
    t9,
    t12
  ] }), $[36] = t12, $[37] = t9, $[38] = t13) : t13 = $[38], t13;
}
function _temp6(props) {
  return (0, import_jsx_runtime3.jsx)(ChangeFieldWrapper, { ...props, hasRevertHover: false });
}
function _temp5(state_3) {
  return state_3.sinceTime;
}
function _temp4(state_2) {
  return state_2.selectionState;
}
function _temp3(state_1) {
  return state_1.onOlderRevision;
}
function _temp2(state_0) {
  return state_0.diff;
}
function _temp$2(state2) {
  return state2.revTime;
}
function Content$1(t0) {
  const $ = (0, import_react_compiler_runtime.c)(9), {
    error,
    diff,
    documentContext,
    loading,
    schemaType
  } = t0;
  if (error) {
    let t12;
    return $[0] === Symbol.for("react.memo_cache_sentinel") ? (t12 = (0, import_jsx_runtime3.jsx)(NoChanges, {}), $[0] = t12) : t12 = $[0], t12;
  }
  if (loading) {
    let t12;
    return $[1] === Symbol.for("react.memo_cache_sentinel") ? (t12 = (0, import_jsx_runtime3.jsx)(LoadingBlock, { showText: true }), $[1] = t12) : t12 = $[1], t12;
  }
  if (!diff) {
    let t12;
    return $[2] === Symbol.for("react.memo_cache_sentinel") ? (t12 = (0, import_jsx_runtime3.jsx)(NoChanges, {}), $[2] = t12) : t12 = $[2], t12;
  }
  let t1;
  $[3] !== diff || $[4] !== schemaType ? (t1 = (0, import_jsx_runtime3.jsx)(ChangeList, { diff, schemaType }), $[3] = diff, $[4] = schemaType, $[5] = t1) : t1 = $[5];
  let t2;
  return $[6] !== documentContext || $[7] !== t1 ? (t2 = (0, import_jsx_runtime3.jsx)(DocumentChangeContext.Provider, { value: documentContext, children: t1 }), $[6] = documentContext, $[7] = t1, $[8] = t2) : t2 = $[8], t2;
}
var getIsSelectable = (event) => !isDeleteDocumentVersionEvent(event) && !isDeleteDocumentGroupEvent(event) && !isUnpublishDocumentEvent(event) && !isScheduleDocumentVersionEvent(event) && !isUnscheduleDocumentVersionEvent(event);
function EventTimelineItem(t0) {
  const $ = (0, import_react_compiler_runtime.c)(27), {
    event,
    isSelected,
    onSelect,
    optionsMenu
  } = t0, {
    t: t2
  } = useTranslation("studio");
  let t1;
  $[0] !== event ? (t1 = getIsSelectable(event), $[0] = event, $[1] = t1) : t1 = $[1];
  const isSelectable = t1;
  let t22;
  $[2] !== event || $[3] !== isSelectable || $[4] !== onSelect ? (t22 = (evt) => {
    evt.preventDefault(), evt.stopPropagation(), isSelectable && onSelect(event);
  }, $[2] = event, $[3] = isSelectable, $[4] = onSelect, $[5] = t22) : t22 = $[5];
  const handleClick = t22;
  let t3;
  $[6] !== isSelectable || $[7] !== t2 ? (t3 = isSelectable ? "" : t2("changes.not-selectable"), $[6] = isSelectable, $[7] = t2, $[8] = t3) : t3 = $[8];
  const t4 = isSelectable ? "button" : "div", t5 = isSelectable ? "pointer" : "default";
  let t6;
  $[9] !== t5 ? (t6 = {
    cursor: t5,
    width: "100%"
  }, $[9] = t5, $[10] = t6) : t6 = $[10];
  let t7;
  $[11] !== event ? (t7 = (0, import_jsx_runtime3.jsx)(Event$1, { event, showChangesBy: "tooltip" }), $[11] = event, $[12] = t7) : t7 = $[12];
  let t8;
  $[13] !== event.timestamp || $[14] !== handleClick || $[15] !== isSelected || $[16] !== t4 || $[17] !== t6 || $[18] !== t7 ? (t8 = (0, import_jsx_runtime3.jsx)(Card, { as: t4, onClick: handleClick, padding: 2, pressed: isSelected, radius: 2, "data-ui": "timelineItem", "data-testid": "timeline-item-button", "data-chunk-timestamp": event.timestamp, style: t6, children: t7 }), $[13] = event.timestamp, $[14] = handleClick, $[15] = isSelected, $[16] = t4, $[17] = t6, $[18] = t7, $[19] = t8) : t8 = $[19];
  let t9;
  $[20] !== isSelectable || $[21] !== t3 || $[22] !== t8 ? (t9 = (0, import_jsx_runtime3.jsx)(Tooltip, { content: t3, disabled: isSelectable, children: t8 }), $[20] = isSelectable, $[21] = t3, $[22] = t8, $[23] = t9) : t9 = $[23];
  let t10;
  return $[24] !== optionsMenu || $[25] !== t9 ? (t10 = (0, import_jsx_runtime3.jsxs)(Flex, { gap: 1, children: [
    t9,
    optionsMenu
  ] }), $[24] = optionsMenu, $[25] = t9, $[26] = t10) : t10 = $[26], t10;
}
function PublishedEventMenu(t0) {
  var _a, _b, _c, _d;
  const $ = (0, import_react_compiler_runtime.c)(41), {
    event
  } = t0, {
    t: t2
  } = useTranslation(structureLocaleNamespace), {
    t: tCore
  } = useTranslation(), portalContext = usePortal(), {
    params,
    setParams
  } = usePaneRouter(), setPerspective = useSetPerspective();
  let t1;
  $[0] !== event.id || $[1] !== event.versionId || $[2] !== event.versionRevisionId || $[3] !== params || $[4] !== setParams ? (t1 = () => {
    setParams({
      ...params,
      rev: event.versionRevisionId || "@lastPublished",
      since: void 0,
      historyEvent: event.id,
      historyVersion: getVersionFromId(event.versionId)
    });
  }, $[0] = event.id, $[1] = event.versionId, $[2] = event.versionRevisionId, $[3] = params, $[4] = setParams, $[5] = t1) : t1 = $[5];
  const handleOpenReleaseDocument = t1;
  let t22;
  $[6] !== event.versionRevisionId || $[7] !== params || $[8] !== setParams || $[9] !== setPerspective ? (t22 = () => {
    setParams({
      ...params,
      rev: event.versionRevisionId,
      preserveRev: "true",
      since: void 0
    }), setTimeout(() => {
      setPerspective("drafts");
    }, 100);
  }, $[6] = event.versionRevisionId, $[7] = params, $[8] = setParams, $[9] = setPerspective, $[10] = t22) : t22 = $[10];
  const handleOpenDraftDocument = t22;
  let t3;
  $[11] !== event.release ? (t3 = (t42) => {
    const {
      children
    } = t42;
    return (0, import_jsx_runtime3.jsx)(VersionInlineBadge, { $tone: event.release ? isReleaseDocument(event.release) ? getReleaseTone(event.release) : "default" : void 0, children });
  }, $[11] = event.release, $[12] = t3) : t3 = $[12];
  const VersionBadge = t3, isMenuDisabled = event.release && !isReleaseDocument(event.release), t4 = `timeline-item-menu-button-${event.versionId}`;
  let t5;
  $[13] !== t2 ? (t5 = t2("timeline-item.menu-button.aria-label"), $[13] = t2, $[14] = t5) : t5 = $[14];
  let t6;
  $[15] !== event.release || $[16] !== isMenuDisabled || $[17] !== t2 ? (t6 = isMenuDisabled ? t2("timeline-item.not-found-release.tooltip", {
    releaseId: ((_a = event.release) == null ? void 0 : _a._id) ? getReleaseIdFromReleaseDocumentId(event.release._id) : void 0
  }) : t2("timeline-item.menu-button.tooltip"), $[15] = event.release, $[16] = isMenuDisabled, $[17] = t2, $[18] = t6) : t6 = $[18];
  let t7;
  $[19] !== t6 ? (t7 = {
    content: t6
  }, $[19] = t6, $[20] = t7) : t7 = $[20];
  let t8;
  $[21] !== isMenuDisabled || $[22] !== t5 || $[23] !== t7 ? (t8 = (0, import_jsx_runtime3.jsx)(ContextMenuButton, { "aria-label": t5, tooltipProps: t7, disabled: isMenuDisabled }), $[21] = isMenuDisabled, $[22] = t5, $[23] = t7, $[24] = t8) : t8 = $[24];
  let t9;
  $[25] !== VersionBadge || $[26] !== event.release || $[27] !== event.versionRevisionId || $[28] !== handleOpenDraftDocument || $[29] !== handleOpenReleaseDocument || $[30] !== t2 || $[31] !== tCore ? (t9 = (0, import_jsx_runtime3.jsx)(Menu, { padding: 1, children: event.release ? (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [
    (0, import_jsx_runtime3.jsx)(IntentLink, { intent: RELEASES_INTENT, params: {
      id: getReleaseIdFromReleaseDocumentId(event.release._id)
    }, style: {
      textDecoration: "none"
    }, children: (0, import_jsx_runtime3.jsx)(MenuItem, { padding: 3, children: (0, import_jsx_runtime3.jsx)(Flex, { align: "center", justify: "flex-start", children: (0, import_jsx_runtime3.jsx)(Text, { size: 1, style: {
      textDecoration: "none"
    }, children: (0, import_jsx_runtime3.jsx)(Translate, { components: {
      VersionBadge: (t102) => {
        const {
          children: children_0
        } = t102;
        return (0, import_jsx_runtime3.jsx)(VersionBadge, { children: children_0 });
      }
    }, i18nKey: "events.open.release", values: {
      releaseTitle: ((_b = event.release.metadata) == null ? void 0 : _b.title) || tCore("release.placeholder-untitled-release")
    }, t: t2 }) }) }) }) }),
    (0, import_jsx_runtime3.jsx)(MenuItem, { onClick: handleOpenReleaseDocument, children: (0, import_jsx_runtime3.jsx)(Flex, { align: "center", justify: "flex-start", children: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: (0, import_jsx_runtime3.jsx)(Translate, { components: {
      VersionBadge: (t112) => {
        const {
          children: children_1
        } = t112;
        return (0, import_jsx_runtime3.jsx)(VersionBadge, { children: children_1 });
      }
    }, i18nKey: "events.inspect.release", values: {
      releaseTitle: ((_c = event.release.metadata) == null ? void 0 : _c.title) || tCore("release.placeholder-untitled-release")
    }, t: t2 }) }) }) })
  ] }) : (0, import_jsx_runtime3.jsx)(MenuItem, { onClick: handleOpenDraftDocument, disabled: !event.versionRevisionId, children: (0, import_jsx_runtime3.jsx)(Flex, { align: "center", children: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: (0, import_jsx_runtime3.jsx)(Translate, { components: {
    VersionBadge: _temp$1
  }, i18nKey: "events.open.draft", t: t2 }) }) }) }) }), $[25] = VersionBadge, $[26] = event.release, $[27] = event.versionRevisionId, $[28] = handleOpenDraftDocument, $[29] = handleOpenReleaseDocument, $[30] = t2, $[31] = tCore, $[32] = t9) : t9 = $[32];
  const t10 = ((_d = portalContext.elements) == null ? void 0 : _d[TIMELINE_MENU_PORTAL$1]) ? TIMELINE_MENU_PORTAL$1 : true;
  let t11;
  $[33] === Symbol.for("react.memo_cache_sentinel") ? (t11 = ["bottom-end", "bottom-start"], $[33] = t11) : t11 = $[33];
  let t12;
  $[34] !== t10 ? (t12 = {
    portal: t10,
    placement: "bottom",
    fallbackPlacements: t11
  }, $[34] = t10, $[35] = t12) : t12 = $[35];
  let t13;
  return $[36] !== t12 || $[37] !== t4 || $[38] !== t8 || $[39] !== t9 ? (t13 = (0, import_jsx_runtime3.jsx)(MenuButton, { id: t4, button: t8, menu: t9, popover: t12 }), $[36] = t12, $[37] = t4, $[38] = t8, $[39] = t9, $[40] = t13) : t13 = $[40], t13;
}
function _temp$1(t0) {
  const {
    children: children_2
  } = t0;
  return (0, import_jsx_runtime3.jsx)(VersionInlineBadge, { $tone: "caution", children: children_2 });
}
var TimelineItemWrapper = motion.create(Box);
var CHILDREN_ITEMS_VARIANTS = {
  initial: {
    opacity: 0
  },
  animate: {
    opacity: 1
  },
  exit: {
    opacity: 0
  }
};
var ITEM_HEIGHT = 57;
var TIMELINE_LIST_WRAPPER_ID = "timeline-list-wrapper";
var EventsTimeline = ({
  events: allEvents,
  hasMoreEvents,
  selectedEventId,
  onLoadMore,
  onSelect,
  listMaxHeight = "calc(100vh - 280px)",
  fetchEventChildren
}) => {
  const [mounted, setMounted] = (0, import_react3.useState)(false), {
    t: t2
  } = useTranslation("studio"), [expandedParents, setExpandedParents] = (0, import_react3.useState)(() => {
    if (selectedEventId) {
      const selectedEvent = allEvents.find((event) => event.id === selectedEventId);
      if (selectedEvent && "parentId" in selectedEvent && selectedEvent.parentId)
        return /* @__PURE__ */ new Set([selectedEvent.parentId]);
    }
    return /* @__PURE__ */ new Set();
  }), [expandingParents, setExpandingParents] = (0, import_react3.useState)(/* @__PURE__ */ new Set()), events = (0, import_react3.useMemo)(() => allEvents.filter((event_0) => isCreateDocumentVersionEvent(event_0) && event_0.parentId ? allEvents[allEvents.length - 1].id === event_0.id ? true : expandedParents.has(event_0.parentId) : isEditDocumentVersionEvent(event_0) && event_0.parentId ? expandedParents.has(event_0.parentId) : true), [expandedParents, allEvents]), handleExpandParent = (0, import_react3.useCallback)((event_1) => async () => {
    const parentId = event_1.id;
    let isExpanding = false;
    setExpandedParents((prev) => {
      const next = new Set(prev);
      return prev.has(parentId) ? next.delete(parentId) : (isExpanding = true, next.add(parentId)), next;
    }), isExpanding && (setExpandingParents((prev_0) => {
      const next_0 = new Set(prev_0);
      return next_0.add(parentId), next_0;
    }), await fetchEventChildren(event_1), setExpandingParents((prev_1) => {
      const next_1 = new Set(prev_1);
      return next_1.delete(parentId), next_1;
    }));
  }, [fetchEventChildren, setExpandingParents]), handleSelectChunk = (0, import_react3.useCallback)((event_2) => {
    onSelect(event_2);
  }, [onSelect]), renderOptionsMenu = (0, import_react3.useCallback)((event_3) => {
    const documentVariantType = getDocumentVariantType(event_3.documentId);
    return isPublishDocumentVersionEvent(event_3) && documentVariantType === "published" ? (0, import_jsx_runtime3.jsx)(PublishedEventMenu, { event: event_3 }) : isPublishDocumentVersionEvent(event_3) && documentVariantType === "draft" && event_3.creationEvent || isDeleteDocumentVersionEvent(event_3) && event_3.creationEvent ? (0, import_jsx_runtime3.jsx)(ExpandableTimelineItemButton, { isExpanded: expandedParents.has(event_3.id), onExpand: handleExpandParent(event_3) }) : null;
  }, [expandedParents, handleExpandParent]), renderItem = (0, import_react3.useCallback)((event_4, {
    activeIndex
  }) => {
    const showExpandingLoader = isCreateDocumentVersionEvent(event_4) && event_4.parentId && expandingParents.has(event_4.parentId), isLastEvent_0 = activeIndex === events.length - 1;
    return showExpandingLoader ? (0, import_jsx_runtime3.jsx)(TimelineItemWrapper, { animate: {
      opacity: 1
    }, initial: {
      opacity: 0
    }, transition: {
      duration: 0.2,
      delay: 0.2
    }, children: (0, import_jsx_runtime3.jsx)(Flex, { align: "center", justify: "center", style: {
      height: ITEM_HEIGHT
    }, children: (0, import_jsx_runtime3.jsx)(Spinner, {}) }) }) : (0, import_jsx_runtime3.jsxs)(TimelineItemWrapper, { paddingBottom: 1, paddingRight: 1, animate: "animate", exit: "exit", initial: "initial", variants: "parentId" in event_4 ? CHILDREN_ITEMS_VARIANTS : void 0, paddingLeft: (isEditDocumentVersionEvent(event_4) || isCreateDocumentVersionEvent(event_4)) && event_4.parentId && !isLastEvent_0 ? 4 : 1, children: [
      (0, import_jsx_runtime3.jsx)(EventTimelineItem, { event: event_4, isSelected: event_4.id === selectedEventId, onSelect: handleSelectChunk, optionsMenu: renderOptionsMenu(event_4) }),
      isLastEvent_0 && hasMoreEvents && (0, import_jsx_runtime3.jsx)(LoadingBlock, {})
    ] }, event_4.timestamp);
  }, [expandingParents, selectedEventId, handleSelectChunk, renderOptionsMenu, events.length, hasMoreEvents]);
  (0, import_react3.useEffect)(() => setMounted(true), []);
  const selectedIndex = events.findIndex((event_5) => event_5.id === selectedEventId);
  return (0, import_jsx_runtime3.jsx)(
    Root$2,
    {
      $visible: !selectedIndex || mounted,
      "data-ui": "timeline",
      children: events.length > 0 ? (0, import_jsx_runtime3.jsx)(ListWrapper, { direction: "column", $maxHeight: listMaxHeight, id: TIMELINE_LIST_WRAPPER_ID, children: (0, import_jsx_runtime3.jsx)(CommandList, { activeItemDataAttr: "data-hovered", ariaLabel: t2("timeline.list.aria-label"), autoFocus: "list", initialIndex: selectedIndex, initialScrollAlign: "center", itemHeight: ITEM_HEIGHT, items: events, onEndReached: onLoadMore, onEndReachedIndexOffset: 20, overscan: 5, renderItem, wrapAround: false }) }) : (0, import_jsx_runtime3.jsxs)(Stack, { padding: 3, space: 3, children: [
        (0, import_jsx_runtime3.jsx)(Text, { size: 1, weight: "medium", children: t2("timeline.error.no-document-history-title") }),
        (0, import_jsx_runtime3.jsx)(Text, { muted: true, size: 1, children: t2("timeline.error.no-document-history-description") })
      ] })
    }
  );
};
EventsTimeline.displayName = "EventsTimeline";
var Root = dt(Popover2)`
  overflow: hidden;
  overflow: clip;
`;
var TIMELINE_MENU_PORTAL = "timeline-menu";
var formatParams = {
  timestamp: {
    dateStyle: "medium",
    timeStyle: "short"
  }
};
function EventsTimelineMenu({
  event,
  events,
  mode,
  placement
}) {
  const {
    setTimelineRange
  } = useDocumentPane(), [open, setOpen] = (0, import_react3.useState)(false), [button, setButton] = (0, import_react3.useState)(null), [popoverRef, setPopoverRef] = (0, import_react3.useState)(null), toast = useToast(), {
    nextCursor,
    loading,
    error: eventsError,
    findRangeForRevision,
    findRangeForSince,
    loadMoreEvents,
    expandEvent
  } = useEvents(), {
    t: t2
  } = useTranslation("studio"), handleOpen = (0, import_react3.useCallback)(() => {
    setOpen(true);
  }, []), handleClose = (0, import_react3.useCallback)(() => {
    setOpen(false);
  }, []), handleGlobalKeyDown = (0, import_react3.useCallback)((e2) => {
    open && (e2.key === "Escape" || e2.key === "Tab") && (handleClose(), button == null ? void 0 : button.focus());
  }, [button, handleClose, open]);
  useGlobalKeyDown(handleGlobalKeyDown), useClickOutsideEvent(open && handleClose, () => [button, popoverRef]);
  const selectRev = (0, import_react3.useCallback)((revEvent) => {
    try {
      if (isDeleteDocumentVersionEvent(revEvent) || isDeleteDocumentGroupEvent(revEvent) || isUnpublishDocumentEvent(revEvent) || isScheduleDocumentVersionEvent(revEvent) || isUnscheduleDocumentVersionEvent(revEvent)) {
        console.error("Event is not selectable");
        return;
      }
      const [since, rev] = findRangeForRevision(revEvent == null ? void 0 : revEvent.id);
      setTimelineRange(since, rev), handleClose();
    } catch (err) {
      toast.push({
        closable: true,
        description: err.message,
        status: "error",
        title: t2("timeline.error.unable-to-load-revision")
      });
    }
  }, [t2, toast, setTimelineRange, findRangeForRevision, handleClose]), selectSince = (0, import_react3.useCallback)((sinceEvent) => {
    try {
      const [since_0, rev_0] = findRangeForSince(sinceEvent.id);
      setTimelineRange(since_0, rev_0), handleClose();
    } catch (err_0) {
      toast.push({
        closable: true,
        description: err_0.message,
        status: "error",
        title: t2("timeline.error.unable-to-load-revision")
      });
    }
  }, [findRangeForSince, setTimelineRange, toast, t2, handleClose]), handleLoadMore = (0, import_react3.useCallback)(() => {
    !loading && nextCursor && loadMoreEvents();
  }, [loading, loadMoreEvents, nextCursor]), content = (0, import_react3.useMemo)(() => eventsError ? (0, import_jsx_runtime3.jsx)(TimelineError, {}) : (0, import_jsx_runtime3.jsx)(EventsTimeline, { events, fetchEventChildren: expandEvent, hasMoreEvents: !!nextCursor, selectedEventId: event == null ? void 0 : event.id, onLoadMore: handleLoadMore, onSelect: mode === "rev" ? selectRev : selectSince }), [eventsError, mode, expandEvent, events, nextCursor, event == null ? void 0 : event.id, handleLoadMore, selectSince, selectRev]), revLabel = event ? t2(TIMELINE_ITEM_I18N_KEY_MAPPING[event.documentVariantType][event.type], {
    context: "timestamp",
    timestamp: new Date(event.timestamp),
    formatParams
  }) : t2("timeline.latest-revision"), sinceLabel = event ? t2(TIMELINE_ITEM_I18N_KEY_MAPPING[event.documentVariantType][event.type], {
    context: "timestamp",
    timestamp: new Date(event.timestamp),
    formatParams
  }) : events.length > 0 ? t2("timeline.since-version-missing") : t2("timeline.no-previous-events"), buttonLabel = mode === "rev" ? revLabel : sinceLabel;
  return (0, import_jsx_runtime3.jsx)(PortalProvider, { __unstable_elements: {
    [TIMELINE_MENU_PORTAL]: popoverRef
  }, children: (0, import_jsx_runtime3.jsx)(Root, { "data-testid": "timeline-menu", constrainSize: true, content: open && content, "data-ui": "versionMenu", open, placement, matchReferenceWidth: true, portal: true, ref: setPopoverRef, children: (0, import_jsx_runtime3.jsx)(Flex, { width: "fill", children: (0, import_jsx_runtime3.jsx)(Button2, { "data-testid": open ? "timeline-menu-close-button" : "timeline-menu-open-button", disabled: loading || !events.length, mode: "ghost", onClick: open ? handleClose : handleOpen, ref: setButton, selected: open, width: "fill", tooltipProps: null, justify: "space-between", style: {
    maxWidth: "100%"
  }, iconRight: ChevronDownIcon, text: loading ? t2("timeline.loading-history") : buttonLabel }) }) }) });
}
var Scroller$2 = dt(ScrollContainer)`
  height: 100%;
  overflow: auto;
  position: relative;
  scroll-behavior: smooth;
`;
var Grid2 = dt(Box)`
  &:not([hidden]) {
    display: grid;
  }
  grid-template-columns: 48px 1fr;
  align-items: center;
  gap: 0.25em;
`;
var SpinnerContainer = dt(Flex)`
  width: 100%;
  position: absolute;
  bottom: -4px;
`;
var DIFF_INITIAL_VALUE = {
  diff: null,
  loading: true,
  error: null
};
var CompareWithPublishedView = () => {
  var _a;
  const $ = (0, import_react_compiler_runtime.c)(35), {
    documentId,
    schemaType,
    editState,
    displayed
  } = useDocumentPane(), {
    selectedPerspective,
    selectedPerspectiveName,
    selectedReleaseId
  } = usePerspective(), {
    t: t2
  } = useTranslation(structureLocaleNamespace);
  let t0, t1;
  $[0] !== (editState == null ? void 0 : editState.published) ? (t1 = (editState == null ? void 0 : editState.published) ?? {}, $[0] = editState == null ? void 0 : editState.published, $[1] = t1) : t1 = $[1];
  let t22;
  if ($[2] !== displayed || $[3] !== t1) {
    let t32;
    $[5] !== displayed ? (t32 = displayed ?? {}, $[5] = displayed, $[6] = t32) : t32 = $[6], t22 = diffInput(wrap(t1, {
      author: ""
    }), wrap(t32, {
      author: ""
    })), $[2] = displayed, $[3] = t1, $[4] = t22;
  } else
    t22 = $[4];
  t0 = t22;
  const rootDiff = t0;
  if (selectedReleaseId && !(editState == null ? void 0 : editState.version) || selectedPerspective === "drafts" && !(editState == null ? void 0 : editState.draft) || selectedPerspectiveName === "published" || !(displayed == null ? void 0 : displayed._rev))
    return null;
  let t3;
  $[7] !== t2 ? (t3 = t2("events.compare-with-published.title"), $[7] = t2, $[8] = t3) : t3 = $[8];
  let t4;
  $[9] !== t3 ? (t4 = (0, import_jsx_runtime3.jsx)(Text, { size: 1, weight: "medium", children: t3 }), $[9] = t3, $[10] = t4) : t4 = $[10];
  let t5;
  $[11] !== selectedPerspective ? (t5 = isReleaseDocument(selectedPerspective) ? (_a = selectedPerspective.metadata) == null ? void 0 : _a.title : "draft", $[11] = selectedPerspective, $[12] = t5) : t5 = $[12];
  let t6;
  $[13] !== t5 ? (t6 = {
    version: t5
  }, $[13] = t5, $[14] = t6) : t6 = $[14];
  let t7;
  $[15] !== t2 || $[16] !== t6 ? (t7 = (0, import_jsx_runtime3.jsx)(Text, { size: 1, muted: true, children: (0, import_jsx_runtime3.jsx)(Translate, { i18nKey: "events.compare-with-published.description", t: t2, values: t6 }) }), $[15] = t2, $[16] = t6, $[17] = t7) : t7 = $[17];
  let t8;
  $[18] !== t4 || $[19] !== t7 ? (t8 = (0, import_jsx_runtime3.jsx)(Card, { borderBottom: true, paddingBottom: 3, children: (0, import_jsx_runtime3.jsxs)(Stack, { space: 3, paddingTop: 1, children: [
    t4,
    t7
  ] }) }), $[18] = t4, $[19] = t7, $[20] = t8) : t8 = $[20];
  let t9;
  $[21] !== displayed || $[22] !== documentId || $[23] !== rootDiff || $[24] !== schemaType ? (t9 = {
    documentId,
    schemaType,
    rootDiff,
    isComparingCurrent: true,
    FieldWrapper: _temp,
    value: displayed,
    showFromValue: true
  }, $[21] = displayed, $[22] = documentId, $[23] = rootDiff, $[24] = schemaType, $[25] = t9) : t9 = $[25];
  let t10;
  $[26] !== rootDiff || $[27] !== schemaType ? (t10 = (0, import_jsx_runtime3.jsx)(Box, { paddingY: 1, children: (0, import_jsx_runtime3.jsx)(ChangeList, { diff: rootDiff, schemaType }) }), $[26] = rootDiff, $[27] = schemaType, $[28] = t10) : t10 = $[28];
  let t11;
  $[29] !== t10 || $[30] !== t9 ? (t11 = (0, import_jsx_runtime3.jsx)(DocumentChangeContext.Provider, { value: t9, children: t10 }), $[29] = t10, $[30] = t9, $[31] = t11) : t11 = $[31];
  let t12;
  return $[32] !== t11 || $[33] !== t8 ? (t12 = (0, import_jsx_runtime3.jsxs)(Stack, { space: 2, marginBottom: 3, children: [
    t8,
    t11
  ] }), $[32] = t11, $[33] = t8, $[34] = t12) : t12 = $[34], t12;
};
function EventsInspector({
  showChanges
}) {
  const {
    documentId,
    schemaType,
    timelineError,
    value,
    formState
  } = useDocumentPane(), [scrollRef, setScrollRef] = (0, import_react3.useState)(null), {
    events,
    revision,
    sinceRevision,
    getChangesList
  } = useEvents(), isComparingCurrent = !(revision == null ? void 0 : revision.revisionId), changesList$ = (0, import_react3.useMemo)(() => getChangesList(), [getChangesList]), {
    diff,
    loading: diffLoading,
    error: diffError
  } = useObservable(changesList$, DIFF_INITIAL_VALUE), {
    t: t2
  } = useTranslation("studio"), documentContext = (0, import_react3.useMemo)(() => ({
    documentId,
    schemaType,
    FieldWrapper: (props) => props.path.length > 0 ? (0, import_jsx_runtime3.jsx)(ChangeFieldWrapper, { ...props }) : props.children,
    rootDiff: diff,
    isComparingCurrent: isComparingCurrent && !(formState == null ? void 0 : formState.readOnly),
    value,
    showFromValue: true
  }), [diff, documentId, isComparingCurrent, formState == null ? void 0 : formState.readOnly, schemaType, value]), [sinceEvent, toEvent] = (0, import_react3.useMemo)(() => events ? [events.find((e2) => e2.id === (sinceRevision == null ? void 0 : sinceRevision.revisionId)) || null, events.find((e_0) => e_0.id === (revision == null ? void 0 : revision.revisionId)) || events[0]] : [null, null], [events, revision == null ? void 0 : revision.revisionId, sinceRevision == null ? void 0 : sinceRevision.revisionId]), sinceEvents = (0, import_react3.useMemo)(() => toEvent ? events.slice(events.indexOf(toEvent) + 1).map((event) => "parentId" in toEvent && "parentId" in event && event.parentId === toEvent.parentId || "parentId" in event && toEvent.id === event.parentId ? {
    ...event,
    parentId: void 0
  } : event) : events.slice(1), [events, toEvent]);
  return events.length ? (0, import_jsx_runtime3.jsxs)(Flex, { "data-testid": "review-changes-pane", direction: "column", height: "fill", overflow: "hidden", children: [
    (0, import_jsx_runtime3.jsxs)(Box, { padding: 3, style: {
      position: "relative"
    }, children: [
      (0, import_jsx_runtime3.jsxs)(Grid2, { paddingX: 2, paddingBottom: 2, children: [
        (0, import_jsx_runtime3.jsx)(Text, { size: 1, muted: true, children: t2("changes.inspector.from-label") }),
        (0, import_jsx_runtime3.jsx)(EventsTimelineMenu, { event: sinceEvent || null, events: sinceEvents, mode: "since", placement: "bottom-start" }),
        (0, import_jsx_runtime3.jsx)(Text, { size: 1, muted: true, children: t2("changes.inspector.to-label") }),
        (0, import_jsx_runtime3.jsx)(EventsTimelineMenu, { event: toEvent || null, events, mode: "rev", placement: "bottom-end" })
      ] }),
      diffLoading && (0, import_jsx_runtime3.jsx)(motion.div, { animate: {
        opacity: 1
      }, initial: {
        opacity: 0
      }, transition: {
        delay: 0.2,
        duration: 0.2
      }, children: (0, import_jsx_runtime3.jsxs)(SpinnerContainer, { justify: "center", align: "center", gap: 2, children: [
        (0, import_jsx_runtime3.jsx)(Text, { muted: true, size: 0, children: t2("changes.loading-changes") }),
        (0, import_jsx_runtime3.jsx)(Spinner, { size: 0 })
      ] }) })
    ] }),
    (0, import_jsx_runtime3.jsx)(Card, { flex: 1, paddingX: 2, paddingY: 2, children: (0, import_jsx_runtime3.jsx)(BoundaryElementProvider, { element: scrollRef, children: (0, import_jsx_runtime3.jsx)(Scroller$2, { "data-ui": "Scroller", ref: setScrollRef, children: (0, import_jsx_runtime3.jsx)(Box, { flex: 1, paddingX: 3, height: "fill", children: showChanges && (0, import_jsx_runtime3.jsx)(Content, { documentContext, error: timelineError || diffError, loading: (revision == null ? void 0 : revision.loading) || (sinceRevision == null ? void 0 : sinceRevision.loading) || false, schemaType, sameRevisionSelected: (sinceEvent == null ? void 0 : sinceEvent.id) === (toEvent == null ? void 0 : toEvent.id), sinceEvent }) }) }) }) })
  ] }) : (0, import_jsx_runtime3.jsx)(Box, { paddingX: 2, children: (0, import_jsx_runtime3.jsxs)(Stack, { padding: 3, space: 3, children: [
    (0, import_jsx_runtime3.jsx)(Text, { size: 1, weight: "medium", children: t2("timeline.error.no-document-history-title") }),
    (0, import_jsx_runtime3.jsx)(Text, { muted: true, size: 1, children: t2("timeline.error.no-document-history-description") })
  ] }) });
}
function Content(t0) {
  const $ = (0, import_react_compiler_runtime.c)(15), {
    error,
    documentContext,
    loading,
    schemaType,
    sameRevisionSelected,
    sinceEvent
  } = t0;
  if (error) {
    let t12;
    $[0] === Symbol.for("react.memo_cache_sentinel") ? (t12 = (0, import_jsx_runtime3.jsx)(CompareWithPublishedView, {}), $[0] = t12) : t12 = $[0];
    let t22;
    $[1] !== error || $[2] !== (sinceEvent == null ? void 0 : sinceEvent.type) ? (t22 = (sinceEvent == null ? void 0 : sinceEvent.type) !== "historyCleared" && (0, import_jsx_runtime3.jsx)(ChangesError, { error }), $[1] = error, $[2] = sinceEvent == null ? void 0 : sinceEvent.type, $[3] = t22) : t22 = $[3];
    let t3;
    return $[4] !== t22 ? (t3 = (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [
      t12,
      t22
    ] }), $[4] = t22, $[5] = t3) : t3 = $[5], t3;
  }
  if (loading) {
    let t12;
    return $[6] === Symbol.for("react.memo_cache_sentinel") ? (t12 = (0, import_jsx_runtime3.jsx)(LoadingBlock, { showText: true }), $[6] = t12) : t12 = $[6], t12;
  }
  if (sameRevisionSelected) {
    let t12;
    return $[7] === Symbol.for("react.memo_cache_sentinel") ? (t12 = (0, import_jsx_runtime3.jsx)(SameRevisionSelected, {}), $[7] = t12) : t12 = $[7], t12;
  }
  if (!documentContext.rootDiff) {
    let t12;
    return $[8] === Symbol.for("react.memo_cache_sentinel") ? (t12 = (0, import_jsx_runtime3.jsx)(motion.div, { animate: {
      opacity: 1
    }, initial: {
      opacity: 0
    }, transition: {
      delay: 0.2,
      duration: 0.2
    }, children: (0, import_jsx_runtime3.jsx)(NoChanges, {}) }), $[8] = t12) : t12 = $[8], t12;
  }
  let t1;
  $[9] !== documentContext.rootDiff || $[10] !== schemaType ? (t1 = (0, import_jsx_runtime3.jsx)(Box, { paddingY: 1, children: (0, import_jsx_runtime3.jsx)(ChangeList, { diff: documentContext.rootDiff, schemaType }) }), $[9] = documentContext.rootDiff, $[10] = schemaType, $[11] = t1) : t1 = $[11];
  let t2;
  return $[12] !== documentContext || $[13] !== t1 ? (t2 = (0, import_jsx_runtime3.jsx)(DocumentChangeContext.Provider, { value: documentContext, children: t1 }), $[12] = documentContext, $[13] = t1, $[14] = t2) : t2 = $[14], t2;
}
function SameRevisionSelected() {
  const $ = (0, import_react_compiler_runtime.c)(12), {
    t: t2
  } = useTranslation("");
  let t0, t1, t22;
  $[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = {
    opacity: 1
  }, t1 = {
    opacity: 0
  }, t22 = {
    delay: 0.2,
    duration: 0.2
  }, $[0] = t0, $[1] = t1, $[2] = t22) : (t0 = $[0], t1 = $[1], t22 = $[2]);
  let t3;
  $[3] !== t2 ? (t3 = t2("changes.same-revision-selected-title"), $[3] = t2, $[4] = t3) : t3 = $[4];
  let t4;
  $[5] !== t3 ? (t4 = (0, import_jsx_runtime3.jsx)(Text, { size: 1, weight: "medium", as: "h3", children: t3 }), $[5] = t3, $[6] = t4) : t4 = $[6];
  let t5;
  $[7] !== t2 ? (t5 = (0, import_jsx_runtime3.jsx)(Text, { as: "p", size: 1, muted: true, children: (0, import_jsx_runtime3.jsx)(Translate, { i18nKey: "changes.same-revision-selected-description", t: t2 }) }), $[7] = t2, $[8] = t5) : t5 = $[8];
  let t6;
  return $[9] !== t4 || $[10] !== t5 ? (t6 = (0, import_jsx_runtime3.jsx)(motion.div, { animate: t0, initial: t1, transition: t22, children: (0, import_jsx_runtime3.jsxs)(Stack, { space: 3, paddingTop: 2, children: [
    t4,
    t5
  ] }) }), $[9] = t4, $[10] = t5, $[11] = t6) : t6 = $[11], t6;
}
function _temp(props) {
  return props.children;
}
var Scroller$1 = dt(ScrollContainer)`
  height: 100%;
  overflow: auto;
  position: relative;
  scroll-behavior: smooth;
`;
function EventsSelector({
  showList
}) {
  var _a;
  const [scrollRef, setScrollRef] = (0, import_react3.useState)(null), [listHeight, setListHeight] = (0, import_react3.useState)(0), {
    setTimelineRange
  } = useDocumentPane(), getScrollerRef = (0, import_react3.useCallback)((el) => {
    !listHeight && el && (setListHeight(el.clientHeight ? el.clientHeight - 1 : 0), setScrollRef(el));
  }, [listHeight]), {
    events,
    nextCursor,
    loading,
    error,
    revision,
    loadMoreEvents,
    findRangeForRevision,
    expandEvent
  } = useEvents(), {
    t: t2
  } = useTranslation("studio"), toast = useToast(), selectRev = (0, import_react3.useCallback)((event) => {
    try {
      if (isDeleteDocumentVersionEvent(event) || isDeleteDocumentGroupEvent(event) || isUnpublishDocumentEvent(event) || isScheduleDocumentVersionEvent(event) || isUnscheduleDocumentVersionEvent(event)) {
        console.error("Event is not selectable");
        return;
      }
      const [since, rev] = findRangeForRevision(event.id);
      setTimelineRange(since, rev);
    } catch (err) {
      toast.push({
        closable: true,
        description: err.message,
        status: "error",
        title: t2("timeline.error.unable-to-load-revision")
      });
    }
  }, [t2, toast, setTimelineRange, findRangeForRevision]), initialLoad = loading && !events.length;
  return (0, import_jsx_runtime3.jsx)(Flex, { "data-testid": "review-changes-pane", direction: "column", height: "fill", children: (0, import_jsx_runtime3.jsx)(Card, { flex: 1, padding: 2, paddingTop: 0, children: error ? (0, import_jsx_runtime3.jsx)(TimelineError, {}) : (0, import_jsx_runtime3.jsx)(BoundaryElementProvider, { element: scrollRef, children: (0, import_jsx_runtime3.jsxs)(Scroller$1, { "data-ui": "Scroller", ref: getScrollerRef, children: [
    listHeight && // This forces the list to unmount and remount, which is needed to reset the scroll position
    showList && !initialLoad ? (0, import_jsx_runtime3.jsx)(
      EventsTimeline,
      {
        events,
        fetchEventChildren: expandEvent,
        hasMoreEvents: !!nextCursor,
        selectedEventId: (revision == null ? void 0 : revision.revisionId) || ((_a = events[0]) == null ? void 0 : _a.id),
        onLoadMore: loadMoreEvents,
        onSelect: selectRev,
        listMaxHeight: `${listHeight}px`
      }
    ) : null,
    loading && (0, import_jsx_runtime3.jsx)(LoadingBlock, {})
  ] }) }) }) });
}
var Scroller = dt(ScrollContainer)`
  height: 100%;
  overflow: auto;
  position: relative;
  scroll-behavior: smooth;
`;
function HistorySelector({
  showList
}) {
  const {
    timelineError,
    setTimelineRange,
    timelineStore
  } = useDocumentPane(), {
    selectedReleaseId
  } = usePerspective(), [scrollRef, setScrollRef] = (0, import_react3.useState)(null), [listHeight, setListHeight] = (0, import_react3.useState)(0), getScrollerRef = (0, import_react3.useCallback)((el) => {
    (el == null ? void 0 : el.clientHeight) && (setListHeight(el.clientHeight - 1), setScrollRef(el));
  }, []), chunks = useTimelineSelector(timelineStore, (state2) => state2.chunks), realRevChunk = useTimelineSelector(timelineStore, (state_0) => state_0.realRevChunk), hasMoreChunks = useTimelineSelector(timelineStore, (state_1) => state_1.hasMoreChunks), loading = useTimelineSelector(timelineStore, (state_2) => state_2.isLoading), {
    t: t2
  } = useTranslation("studio"), toast = useToast(), selectRev = (0, import_react3.useCallback)((revChunk) => {
    try {
      const [sinceId, revId] = (timelineStore == null ? void 0 : timelineStore.findRangeForRev(revChunk)) || [null, null];
      setTimelineRange(sinceId, revId);
    } catch (err) {
      toast.push({
        closable: true,
        description: err.message,
        status: "error",
        title: t2("timeline.error.unable-to-load-revision")
      });
    }
  }, [setTimelineRange, t2, timelineStore, toast]), handleLoadMore = (0, import_react3.useCallback)(() => {
    loading || (timelineStore == null ? void 0 : timelineStore.loadMore());
  }, [loading, timelineStore]);
  return (0, import_jsx_runtime3.jsx)(Flex, { "data-testid": "review-changes-pane", direction: "column", height: "fill", children: (0, import_jsx_runtime3.jsx)(Card, { flex: 1, padding: 2, paddingTop: 0, children: timelineError || selectedReleaseId ? (0, import_jsx_runtime3.jsx)(TimelineError, { versionError: !!selectedReleaseId }) : (0, import_jsx_runtime3.jsx)(BoundaryElementProvider, { element: scrollRef, children: (0, import_jsx_runtime3.jsx)(Scroller, { "data-ui": "Scroller", ref: getScrollerRef, children: listHeight && // This forces the list to unmount and remount, which is needed to reset the scroll position
  showList ? (0, import_jsx_runtime3.jsx)(Timeline, { chunks, hasMoreChunks, lastChunk: realRevChunk, onLoadMore: handleLoadMore, onSelect: selectRev, listMaxHeight: `${listHeight}px` }) : null }) }) }) });
}
var FadeInFlex = dt(Flex)`
  opacity: 0;
  transition: opacity 200ms;
  &[data-ready] {
    opacity: 1;
  }
`;
var TABS = ["history", "review"];
var isValidTab = (tab) => (
  // @ts-expect-error TS doesn't understand the type guard
  tab && TABS.includes(tab)
);
function ChangesTabs(props) {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l;
  const $ = (0, import_react_compiler_runtime.c)(72), {
    params,
    setParams
  } = usePaneRouter(), source = useSource(), [parentRef, setParentRef] = (0, import_react3.useState)(null), {
    t: t2
  } = useTranslation(structureLocaleNamespace), isReady = (params == null ? void 0 : params.inspect) === HISTORY_INSPECTOR_NAME, {
    selectedPerspective
  } = usePerspective();
  let t0;
  $[0] !== params ? (t0 = isValidTab(params == null ? void 0 : params.changesInspectorTab) ? params.changesInspectorTab : TABS[0], $[0] = params, $[1] = t0) : t0 = $[1];
  const paneRouterTab = t0;
  let t1;
  $[2] !== params || $[3] !== setParams ? (t1 = (tab) => setParams({
    ...params,
    changesInspectorTab: tab,
    since: tab === "history" ? void 0 : params == null ? void 0 : params.since
  }), $[2] = params, $[3] = setParams, $[4] = t1) : t1 = $[4];
  const setPaneRouterTab = t1;
  let t22;
  $[5] !== selectedPerspective || $[6] !== t2 ? (t22 = isReleaseDocument(selectedPerspective) ? selectedPerspective.metadata.title : t2(selectedPerspective === "drafts" ? "compare-versions.status.draft" : "compare-versions.status.published"), $[5] = selectedPerspective, $[6] = t2, $[7] = t22) : t22 = $[7];
  const perspectiveName2 = t22, t3 = isReady ? "" : void 0;
  let t4;
  $[8] !== t2 ? (t4 = t2("changes.tab.history"), $[8] = t2, $[9] = t4) : t4 = $[9];
  let t5;
  $[10] !== setPaneRouterTab ? (t5 = () => setPaneRouterTab("history"), $[10] = setPaneRouterTab, $[11] = t5) : t5 = $[11];
  const t6 = paneRouterTab === "history";
  let t7;
  $[12] !== t4 || $[13] !== t5 || $[14] !== t6 ? (t7 = (0, import_jsx_runtime3.jsx)(Tab, { "aria-controls": "history-panel", id: "history-tab", label: t4, onClick: t5, selected: t6 }), $[12] = t4, $[13] = t5, $[14] = t6, $[15] = t7) : t7 = $[15];
  let t8;
  $[16] !== t2 ? (t8 = t2("changes.tab.review-changes"), $[16] = t2, $[17] = t8) : t8 = $[17];
  let t9;
  $[18] !== setPaneRouterTab ? (t9 = () => setPaneRouterTab("review"), $[18] = setPaneRouterTab, $[19] = t9) : t9 = $[19];
  const t10 = paneRouterTab === "review";
  let t11;
  $[20] !== t10 || $[21] !== t8 || $[22] !== t9 ? (t11 = (0, import_jsx_runtime3.jsx)(Tab, { "aria-controls": "review-changes-panel", id: "changes-tab", label: t8, onClick: t9, selected: t10 }), $[20] = t10, $[21] = t8, $[22] = t9, $[23] = t11) : t11 = $[23];
  let t12;
  $[24] !== t11 || $[25] !== t7 ? (t12 = (0, import_jsx_runtime3.jsxs)(TabList, { space: 1, flex: 1, children: [
    t7,
    t11
  ] }), $[24] = t11, $[25] = t7, $[26] = t12) : t12 = $[26];
  let t13;
  $[27] !== t2 ? (t13 = t2("changes.action.close-label"), $[27] = t2, $[28] = t13) : t13 = $[28];
  const t14 = props.onClose;
  let t15;
  $[29] !== t2 ? (t15 = t2("document-inspector.close-button.tooltip"), $[29] = t2, $[30] = t15) : t15 = $[30];
  let t16;
  $[31] !== t15 ? (t16 = {
    content: t15
  }, $[31] = t15, $[32] = t16) : t16 = $[32];
  let t17;
  $[33] !== props.onClose || $[34] !== t13 || $[35] !== t16 ? (t17 = (0, import_jsx_runtime3.jsx)(Button2, { "aria-label": t13, icon: CloseIcon, mode: "bleed", onClick: t14, tooltipProps: t16 }), $[33] = props.onClose, $[34] = t13, $[35] = t16, $[36] = t17) : t17 = $[36];
  let t18;
  $[37] !== t12 || $[38] !== t17 ? (t18 = (0, import_jsx_runtime3.jsx)(Card, { paddingBottom: 1, children: (0, import_jsx_runtime3.jsxs)(Flex, { align: "center", padding: 3, gap: 2, children: [
    t12,
    t17
  ] }) }), $[37] = t12, $[38] = t17, $[39] = t18) : t18 = $[39];
  let t19;
  $[40] !== t2 ? (t19 = (0, import_jsx_runtime3.jsx)(Box, { flex: 1, padding: 1, children: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: (0, import_jsx_runtime3.jsx)(Translate, { t: t2, i18nKey: "changes.banner.tooltip" }) }) }), $[40] = t2, $[41] = t19) : t19 = $[41];
  let t20;
  $[42] === Symbol.for("react.memo_cache_sentinel") ? (t20 = (0, import_jsx_runtime3.jsx)(Text, { size: 0, muted: true, children: (0, import_jsx_runtime3.jsx)(InfoOutlineIcon, { fontSize: 1 }) }), $[42] = t20) : t20 = $[42];
  let t21;
  $[43] !== parentRef || $[44] !== t19 ? (t21 = (0, import_jsx_runtime3.jsx)(Tooltip, { portal: true, placement: "bottom-end", boundaryElement: parentRef, content: t19, children: t20 }), $[43] = parentRef, $[44] = t19, $[45] = t21) : t21 = $[45];
  let t222;
  $[46] !== perspectiveName2 ? (t222 = {
    perspective: perspectiveName2
  }, $[46] = perspectiveName2, $[47] = t222) : t222 = $[47];
  let t23;
  $[48] !== t2 || $[49] !== t222 ? (t23 = (0, import_jsx_runtime3.jsx)(Text, { size: 0, muted: true, children: (0, import_jsx_runtime3.jsx)(Translate, { t: t2, values: t222, i18nKey: "changes.banner.description" }) }), $[48] = t2, $[49] = t222, $[50] = t23) : t23 = $[50];
  let t24;
  $[51] !== t21 || $[52] !== t23 ? (t24 = (0, import_jsx_runtime3.jsx)(Card, { padding: 2, marginBottom: 3, marginX: 3, tone: "neutral", border: true, radius: 3, children: (0, import_jsx_runtime3.jsxs)(Flex, { gap: 2, align: "flex-start", children: [
    t21,
    t23
  ] }) }), $[51] = t21, $[52] = t23, $[53] = t24) : t24 = $[53];
  const t25 = paneRouterTab !== "history";
  let t26;
  $[54] !== paneRouterTab || $[55] !== ((_b = (_a = source.beta) == null ? void 0 : _a.eventsAPI) == null ? void 0 : _b.documents) ? (t26 = ((_d = (_c = source.beta) == null ? void 0 : _c.eventsAPI) == null ? void 0 : _d.documents) ? (0, import_jsx_runtime3.jsx)(EventsSelector, { showList: paneRouterTab === "history" }) : (0, import_jsx_runtime3.jsx)(HistorySelector, { showList: paneRouterTab === "history" }), $[54] = paneRouterTab, $[55] = (_f = (_e = source.beta) == null ? void 0 : _e.eventsAPI) == null ? void 0 : _f.documents, $[56] = t26) : t26 = $[56];
  let t27;
  $[57] !== t25 || $[58] !== t26 ? (t27 = (0, import_jsx_runtime3.jsx)(TabPanel, { "aria-labelledby": "history-tab", height: "fill", hidden: t25, id: "history-panel", children: t26 }), $[57] = t25, $[58] = t26, $[59] = t27) : t27 = $[59];
  const t28 = paneRouterTab !== "review";
  let t29;
  $[60] !== paneRouterTab || $[61] !== ((_h = (_g = source.beta) == null ? void 0 : _g.eventsAPI) == null ? void 0 : _h.documents) ? (t29 = ((_j = (_i = source.beta) == null ? void 0 : _i.eventsAPI) == null ? void 0 : _j.documents) ? (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children: paneRouterTab === "review" ? (0, import_jsx_runtime3.jsx)(EventsInspector, { showChanges: paneRouterTab === "review" }) : null }) : (0, import_jsx_runtime3.jsx)(ChangesInspector, { showChanges: paneRouterTab === "review" }), $[60] = paneRouterTab, $[61] = (_l = (_k = source.beta) == null ? void 0 : _k.eventsAPI) == null ? void 0 : _l.documents, $[62] = t29) : t29 = $[62];
  let t30;
  $[63] !== t28 || $[64] !== t29 ? (t30 = (0, import_jsx_runtime3.jsx)(TabPanel, { "aria-labelledby": "review-tab", hidden: t28, id: "review-panel", height: "fill", children: t29 }), $[63] = t28, $[64] = t29, $[65] = t30) : t30 = $[65];
  let t31;
  return $[66] !== t18 || $[67] !== t24 || $[68] !== t27 || $[69] !== t3 || $[70] !== t30 ? (t31 = (0, import_jsx_runtime3.jsxs)(FadeInFlex, { direction: "column", padding: 0, height: "fill", "data-ready": t3, ref: setParentRef, children: [
    t18,
    t24,
    t27,
    t30
  ] }), $[66] = t18, $[67] = t24, $[68] = t27, $[69] = t3, $[70] = t30, $[71] = t31) : t31 = $[71], t31;
}
var changesInspector = {
  name: HISTORY_INSPECTOR_NAME,
  useMenuItem: () => {
    const {
      features
    } = useStructureTool(), {
      t: t2
    } = useTranslation();
    return {
      hidden: !features.reviewChanges,
      icon: RestoreIcon,
      title: t2("changes.title")
    };
  },
  component: ChangesTabs,
  onClose: ({
    params
  }) => ({
    params: {
      ...params,
      since: void 0,
      rev: void 0,
      changesInspectorTab: void 0,
      historyVersion: void 0
    }
  }),
  onOpen: ({
    params
  }) => ({
    params: {
      ...params,
      since: "@lastPublished"
    }
  })
};
function getPathTitles(options) {
  const {
    path,
    schemaType,
    value
  } = options, result = [];
  let s2 = schemaType, v = value;
  for (const segment of path) {
    if (typeof segment == "string") {
      if (!isRecord$4(v) && v !== void 0)
        throw new Error(`Parent value is not an object, cannot get path segment: .${segment}`);
      if (s2.jsonType !== "object")
        throw new Error(`Parent type is not an object schema type, cannot get path segment: .${segment}`);
      v = v == null ? void 0 : v[segment];
      const field = s2.fields.find((f) => f.name === segment);
      if (!field)
        return result.push({
          name: segment
        }), result;
      s2 = field.type, result.push(s2);
      continue;
    }
    if (typeof segment == "number") {
      if (!isArray(v) && v !== void 0)
        throw new Error(`Parent value is not an array, cannot get path segment: [${segment}]`);
      if (s2.jsonType !== "array")
        throw new Error(`Parent type is not an array schema type, cannot get path segment: [${segment}]`);
      v = v == null ? void 0 : v[segment];
      const itemType = s2.of.find((ofType) => typeof v == "string" ? ofType.jsonType === "string" : typeof v == "number" ? ofType.jsonType === "number" : typeof v == "boolean" ? ofType.jsonType === "boolean" : isRecord$4(v) ? ofType.name === (v == null ? void 0 : v._type) : false);
      if (!itemType)
        throw new Error(`Item type not found: [${segment}]`);
      s2 = itemType, result.push(s2);
      continue;
    }
    if (isRecord$4(segment) && segment._key) {
      if (!isArray(v))
        throw new Error(`Parent value is not an array, cannot get path segment: [_key == ${segment}]`);
      if (s2.jsonType !== "array")
        throw new Error(`Parent type is not an array schema type, cannot get path segment: .${segment}`);
      if (v = (v ?? []).find((i) => isRecord$4(i) && i._key === segment._key), !isRecord$4(v))
        throw new Error(`Array item not found: [_key == ${segment._key}]`);
      const ofType = s2.of.find((i) => isRecord$4(v) && i.name === (v == null ? void 0 : v._type));
      if (!ofType)
        throw new Error(`Array item type not found: .${v == null ? void 0 : v._type}`);
      s2 = ofType, result.push(s2);
      continue;
    }
    throw new Error(`Invalid path segment: ${JSON.stringify(segment)}`);
  }
  return result;
}
var MARKER_ICON = {
  error: ErrorOutlineIcon,
  warning: WarningOutlineIcon,
  info: InfoOutlineIcon
};
var MARKER_TONE = {
  error: "critical",
  warning: "caution",
  info: "primary"
};
function ValidationInspector(props) {
  const $ = (0, import_react_compiler_runtime.c)(23), {
    onClose
  } = props, {
    onFocus,
    onPathOpen,
    schemaType,
    validation,
    value,
    editState
  } = useDocumentPane(), {
    t: t2
  } = useTranslation("validation");
  let t0;
  $[0] !== onFocus || $[1] !== onPathOpen ? (t0 = (path) => {
    onPathOpen(path), onFocus(path);
  }, $[0] = onFocus, $[1] = onPathOpen, $[2] = t0) : t0 = $[2];
  const handleOpen = t0;
  let t1;
  $[3] !== editState ? (t1 = editState && editState.version && isGoingToUnpublish(editState.version), $[3] = editState, $[4] = t1) : t1 = $[4];
  const isVersionGoingToUnpublish = t1;
  let t22;
  $[5] !== t2 ? (t22 = t2("panel.close-button-aria-label"), $[5] = t2, $[6] = t22) : t22 = $[6];
  let t3;
  $[7] !== t2 ? (t3 = t2("panel.title"), $[7] = t2, $[8] = t3) : t3 = $[8];
  let t4;
  $[9] !== onClose || $[10] !== t22 || $[11] !== t3 ? (t4 = (0, import_jsx_runtime3.jsx)(DocumentInspectorHeader, { as: "header", closeButtonLabel: t22, flex: "none", onClose, title: t3 }), $[9] = onClose, $[10] = t22, $[11] = t3, $[12] = t4) : t4 = $[12];
  let t5;
  $[13] !== handleOpen || $[14] !== isVersionGoingToUnpublish || $[15] !== schemaType || $[16] !== t2 || $[17] !== validation || $[18] !== value ? (t5 = (0, import_jsx_runtime3.jsx)(Card, { flex: 1, overflow: "auto", padding: 3, children: isVersionGoingToUnpublish ? (0, import_jsx_runtime3.jsx)(Box, { padding: 2, children: (0, import_jsx_runtime3.jsx)(Text, { muted: true, size: 1, children: t2("panel.unpublish-message") }) }) : (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [
    validation.length === 0 && (0, import_jsx_runtime3.jsx)(Box, { padding: 2, children: (0, import_jsx_runtime3.jsx)(Text, { muted: true, size: 1, children: t2("panel.no-errors-message") }) }),
    validation.length > 0 && (0, import_jsx_runtime3.jsx)(Stack, { space: 2, children: validation.map((marker, i) => (0, import_jsx_runtime3.jsx)(ValidationCard, { marker, onOpen: handleOpen, schemaType, value }, i)) })
  ] }) }), $[13] = handleOpen, $[14] = isVersionGoingToUnpublish, $[15] = schemaType, $[16] = t2, $[17] = validation, $[18] = value, $[19] = t5) : t5 = $[19];
  let t6;
  return $[20] !== t4 || $[21] !== t5 ? (t6 = (0, import_jsx_runtime3.jsxs)(Flex, { direction: "column", height: "fill", overflow: "hidden", children: [
    t4,
    t5
  ] }), $[20] = t4, $[21] = t5, $[22] = t6) : t6 = $[22], t6;
}
function ValidationCard(props) {
  const $ = (0, import_react_compiler_runtime.c)(17), {
    marker,
    onOpen,
    schemaType,
    value
  } = props;
  let t0;
  $[0] !== marker.path || $[1] !== onOpen ? (t0 = () => onOpen(marker.path), $[0] = marker.path, $[1] = onOpen, $[2] = t0) : t0 = $[2];
  const handleOpen = t0, [errorInfo, setErrorInfo] = (0, import_react3.useState)(null), Icon = MARKER_ICON[marker.level];
  let t1;
  $[3] !== errorInfo ? (t1 = errorInfo && (0, import_jsx_runtime3.jsx)(Card, { padding: 3, radius: 2, tone: "critical", children: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: errorInfo.error.message }) }), $[3] = errorInfo, $[4] = t1) : t1 = $[4];
  let t2;
  $[5] !== Icon || $[6] !== errorInfo || $[7] !== handleOpen || $[8] !== marker.level || $[9] !== marker.message || $[10] !== marker.path || $[11] !== schemaType || $[12] !== value ? (t2 = !errorInfo && (0, import_jsx_runtime3.jsx)(Card, { __unstable_focusRing: true, as: "button", onClick: handleOpen, padding: 3, radius: 2, tone: MARKER_TONE[marker.level], children: (0, import_jsx_runtime3.jsxs)(Flex, { align: "flex-start", gap: 3, children: [
    (0, import_jsx_runtime3.jsx)(Box, { flex: "none", children: (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: (0, import_jsx_runtime3.jsx)(Icon, {}) }) }),
    (0, import_jsx_runtime3.jsxs)(Stack, { flex: 1, space: 2, children: [
      (0, import_jsx_runtime3.jsx)(DocumentNodePathBreadcrumbs, { path: marker.path, schemaType, value }),
      (0, import_jsx_runtime3.jsx)(Text, { muted: true, size: 1, children: marker.message })
    ] })
  ] }) }), $[5] = Icon, $[6] = errorInfo, $[7] = handleOpen, $[8] = marker.level, $[9] = marker.message, $[10] = marker.path, $[11] = schemaType, $[12] = value, $[13] = t2) : t2 = $[13];
  let t3;
  return $[14] !== t1 || $[15] !== t2 ? (t3 = (0, import_jsx_runtime3.jsxs)(ErrorBoundary, { onCatch: setErrorInfo, children: [
    t1,
    t2
  ] }), $[14] = t1, $[15] = t2, $[16] = t3) : t3 = $[16], t3;
}
function DocumentNodePathBreadcrumbs(props) {
  const {
    path,
    schemaType,
    value
  } = props, pathTitles = (0, import_react3.useMemo)(() => {
    try {
      return getPathTitles({
        path,
        schemaType,
        value
      });
    } catch (e2) {
      console.error(e2);
    }
    return null;
  }, [path, schemaType, value]);
  return (pathTitles == null ? void 0 : pathTitles.length) ? (0, import_jsx_runtime3.jsx)(Text, { size: 1, children: pathTitles.map((t2, i) => (0, import_jsx_runtime3.jsxs)(import_react3.Fragment, { children: [
    i > 0 && (0, import_jsx_runtime3.jsx)("span", { style: {
      color: "var(--card-muted-fg-color)",
      opacity: 0.5
    }, children: " / " }),
    (0, import_jsx_runtime3.jsx)("span", { style: {
      fontWeight: 500
    }, children: t2.title || t2.name })
  ] }, i)) }) : null;
}
function useMenuItem(props) {
  const {
    documentId,
    documentType
  } = props, {
    t: t2
  } = useTranslation("validation"), {
    selectedReleaseId
  } = usePerspective(), {
    validation: validationMarkers
  } = useValidationStatus(documentId, documentType, selectedReleaseId), {
    value
  } = useDocumentPane(), validation = (0, import_react3.useMemo)(() => validationMarkers.map((item) => ({
    level: item.level,
    message: item.message,
    path: item.path
  })), [validationMarkers]), hasErrors = validation.some(isValidationError), hasWarnings = validation.some(isValidationWarning), isDocumentGoingToUnpublish = isGoingToUnpublish(value), icon = (0, import_react3.useMemo)(() => hasErrors ? ErrorOutlineIcon : hasWarnings ? WarningOutlineIcon : CheckmarkCircleIcon, [hasErrors, hasWarnings]), tone = (0, import_react3.useMemo)(() => hasErrors ? "critical" : hasWarnings ? "caution" : "positive", [hasErrors, hasWarnings]);
  return {
    hidden: validation.length === 0 || isDocumentGoingToUnpublish,
    icon,
    title: t2("panel.title"),
    tone,
    showAsAction: true
  };
}
var validationInspector = {
  name: VALIDATION_INSPECTOR_NAME,
  component: ValidationInspector,
  useMenuItem
};
var EMPTY_PARAMS = {};
function legacyEditParamsToState(params) {
  try {
    return JSON.parse(decodeURIComponent(params));
  } catch {
    return console.warn("Failed to parse JSON parameters"), {};
  }
}
function encodePanesSegment(panes) {
  return (panes || []).map((group) => group.map(encodeChunks).join("|")).map(encodeURIComponent).join(";");
}
function legacyEditParamsToPath(params) {
  return JSON.stringify(params);
}
function toState(pathSegment) {
  return parsePanesSegment(decodeURIComponent(pathSegment));
}
function toPath(panes) {
  return encodePanesSegment(panes);
}
var router = route.create("/", [
  // "Asynchronous intent resolving" route
  route.intents("/intent"),
  // Legacy fallback route, will be redirected to new format
  route.create("/edit/:type/:editDocumentId", [route.create({
    path: "/:params",
    transform: {
      params: {
        toState: legacyEditParamsToState,
        toPath: legacyEditParamsToPath
      }
    }
  })]),
  // The regular path - when the intent can be resolved to a specific pane
  route.create({
    path: "/:panes",
    // Legacy URLs, used to handle redirects
    children: [route.create("/:action", route.create("/:legacyEditDocumentId"))],
    transform: {
      panes: {
        toState,
        toPath
      }
    }
  })
]);
var panePattern = /^([.a-z0-9_-]+),?({.*?})?(?:(;|$))/i;
var isParam = (str) => /^[a-z0-9]+=[^=]+/i.test(str);
var isPayloadLike = (str) => /^[A-Za-z0-9\-_]+(?:={0,2})$/.test(str);
var exclusiveParams = ["view", "since", "rev", "inspect", "comment"];
var isTruthy = Boolean;
function parseChunks(chunks, initial) {
  const sibling = {
    ...initial,
    params: EMPTY_PARAMS,
    payload: void 0
  };
  return chunks.reduce((pane2, chunk) => {
    if (isParam(chunk)) {
      const key = chunk.slice(0, chunk.indexOf("=")), value = chunk.slice(key.length + 1);
      pane2.params = {
        ...pane2.params,
        [decodeURIComponent(key)]: decodeURIComponent(value)
      };
    } else isPayloadLike(chunk) ? pane2.payload = tryParseBase64Payload(chunk) : console.warn("Unknown pane segment: %s - skipping", chunk);
    return pane2;
  }, sibling);
}
function encodeChunks(pane2, index, group) {
  const {
    payload,
    params = {},
    id: id2
  } = pane2, [firstSibling] = group, paneIsFirstSibling = pane2 === firstSibling, sameAsFirst = index !== 0 && id2 === firstSibling.id, encodedPayload = typeof payload > "u" ? void 0 : encodeJsonParams(payload), encodedParams = Object.entries(params).filter((entry) => {
    var _a;
    const [key, value] = entry;
    if (!value) return false;
    if (paneIsFirstSibling) return true;
    const valueFromFirstSibling = (_a = firstSibling.params) == null ? void 0 : _a[key];
    return !(value === valueFromFirstSibling && !exclusiveParams.includes(key));
  }).map(([key, value]) => `${encodeURIComponent(key)}=${encodeURIComponent(value)}`);
  return [sameAsFirst ? "" : id2].concat([encodedParams.length > 0 && encodedParams, encodedPayload].filter(isTruthy).flat()).join(",") || ",";
}
function parsePanesSegment(str) {
  return str.indexOf(",{") !== -1 ? parseOldPanesSegment(str) : str.split(";").map((group) => {
    const [firstSibling, ...restOfSiblings] = group.split("|").map((segment) => {
      const [id2, ...chunks] = segment.split(",");
      return parseChunks(chunks, {
        id: id2
      });
    });
    return [firstSibling, ...restOfSiblings.map((sibling) => ({
      ...firstSibling,
      ...sibling,
      id: sibling.id || firstSibling.id,
      params: {
        ...(0, import_omit.default)(firstSibling.params, exclusiveParams),
        ...sibling.params
      },
      payload: sibling.payload || firstSibling.payload
    }))];
  }).filter((group) => group.length > 0);
}
function parseOldPanesSegment(str) {
  const chunks = [];
  let buffer = str;
  for (; buffer.length; ) {
    const [match, id2, payloadChunk] = buffer.match(panePattern) || [];
    if (!match) {
      buffer = buffer.slice(1);
      continue;
    }
    const payload = payloadChunk && tryParsePayload(payloadChunk);
    chunks.push({
      id: id2,
      payload
    }), buffer = buffer.slice(match.length);
  }
  return [chunks];
}
function tryParsePayload(json) {
  try {
    return JSON.parse(json);
  } catch (err) {
    console.warn(`Failed to parse parameters: ${err.message}`);
    return;
  }
}
function tryParseBase64Payload(data) {
  try {
    return data ? decodeJsonParams(data) : void 0;
  } catch (err) {
    console.warn(`Failed to parse parameters: ${err.message}`);
    return;
  }
}
var documentActions = [PublishAction, UnpublishAction, DiscardChangesAction, DuplicateAction, DeleteAction, HistoryRestoreAction];
var documentBadges = [LiveEditBadge];
var inspectors = [validationInspector, changesInspector];
var structureTool = definePlugin((options) => {
  const icon = (options == null ? void 0 : options.icon) || MasterDetailIcon;
  return {
    name: "sanity/structure",
    document: {
      actions: (prevActions) => Array.from(/* @__PURE__ */ new Set([...prevActions, ...documentActions])),
      badges: (prevBadges) => Array.from(/* @__PURE__ */ new Set([...prevBadges, ...documentBadges])),
      inspectors: (prevInspectors) => Array.from(/* @__PURE__ */ new Set([...prevInspectors, ...inspectors]))
    },
    tools: [{
      name: (options == null ? void 0 : options.name) || "structure",
      title: (options == null ? void 0 : options.title) || "Structure",
      icon,
      component: (0, import_react3.lazy)(() => import("./index3-5DG6U7O3.js")),
      canHandleIntent: (intent, params) => intent === "create" ? canHandleCreateIntent(params) : intent === "edit" ? canHandleEditIntent(params) : false,
      getIntentState,
      // Controlled by sanity/src/structure/components/structureTool/StructureTitle.tsx
      controlsDocumentTitle: true,
      options,
      router,
      __internalApplicationType: "sanity/structure"
    }],
    i18n: {
      bundles: [structureUsEnglishLocaleBundle]
    }
  };
});
function canHandleCreateIntent(params) {
  return "type" in params ? "template" in params ? {
    template: true
  } : true : false;
}
function canHandleEditIntent(params) {
  return "id" in params ? "mode" in params ? {
    mode: params.mode === "structure"
  } : true : false;
}
var pane$1 = Object.freeze({
  __proto__: null,
  default: DocumentPane
});
var pane = Object.freeze({
  __proto__: null,
  default: PaneContainer
});

export {
  structureLocaleNamespace,
  SerializeError,
  HELP_URL,
  maybeSerializeMenuItem,
  MenuItemBuilder,
  getOrderingMenuItem,
  getOrderingMenuItemsForSchemaType,
  maybeSerializeMenuItemGroup,
  MenuItemGroupBuilder,
  ComponentBuilder,
  GenericViewBuilder,
  maybeSerializeView,
  ComponentViewBuilder,
  FormViewBuilder,
  form,
  component,
  DocumentBuilder,
  documentFromEditor,
  documentFromEditorWithInitialValue,
  InitialValueTemplateItemBuilder,
  defaultInitialValueTemplateItems,
  maybeSerializeInitialValueTemplateItem,
  menuItemsFromInitialValueTemplateItems,
  DEFAULT_INTENT_HANDLER,
  defaultIntentChecker,
  shallowIntentChecker,
  GenericListBuilder,
  DocumentListBuilder,
  getTypeNamesFromFilter,
  ListBuilder,
  ListItemBuilder,
  DocumentListItemBuilder,
  isDocumentListItem,
  DocumentTypeListBuilder,
  createStructureBuilder,
  StructureToolProvider,
  BackLink,
  usePaneLayout,
  ChildLink,
  ParameterizedLink,
  ReferenceChildLink,
  usePaneRouter,
  ConfirmDeleteDialogContainer,
  Pane,
  usePane,
  PaneContent,
  PaneHeader$1,
  PaneLayout,
  PaneHeaderActions,
  PaneItem,
  LoadingPane,
  useDocumentPane,
  useStructureTool,
  useDocumentTitle,
  DocumentInspectorHeader,
  _DEBUG,
  LOADING_PANE,
  DocumentPaneProviderWrapper,
  DocumentPane,
  usePaneOptions,
  PaneContainer,
  setActivePanes,
  structureTool,
  pane$1,
  pane
};
/*! Bundled license information:

@tanstack/table-core/build/lib/index.mjs:
  (**
     * table-core
     *
     * Copyright (c) TanStack
     *
     * This source code is licensed under the MIT license found in the
     * LICENSE.md file in the root directory of this source tree.
     *
     * @license MIT
     *)

@tanstack/react-table/build/lib/index.mjs:
  (**
     * react-table
     *
     * Copyright (c) TanStack
     *
     * This source code is licensed under the MIT license found in the
     * LICENSE.md file in the root directory of this source tree.
     *
     * @license MIT
     *)
*/
//# sourceMappingURL=chunk-26YSZXNB.js.map
