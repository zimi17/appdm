---
interface Props {
  maxWidth?: string;
}

const { maxWidth = '1200px' } = Astro.props;
---

<div class="container" style={{ maxWidth }}>
  <slot />
</div>

<style>
  .container {
    margin: 0 auto;
    padding: 0 var(--spacing-lg);
  }
</style>
