---
import Card from '../ui/Card.astro';
import Heading from '../ui/Heading.astro';
import Text from '../ui/Text.astro';
import Button from '../ui/Button.astro';

interface Props {
  title: string;
  description: string;
  image: string;
  slug: string;
}

const { title, description, image, slug } = Astro.props;
---

<Card shadow={true}>
  <img src={image} alt={title} class="w-full h-48 object-cover rounded-t-lg mb-4" />
  <Heading variant="subtitle" className="mb-2">{title}</Heading>
  <Text variant="body" className="mb-4">{description}</Text>
  <Button href={slug} variant="outline">Lihat Detail</Button>
</Card>
