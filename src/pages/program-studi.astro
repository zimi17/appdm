---
import MainLayout from '../components/layout/MainLayout.astro';
import { studyPrograms } from '../data/studyPrograms.ts';
---

<MainLayout title="Program Studi - STIE Dwimulya">
  <section class="py-10 lg:py-20">
    <div class="container mx-auto px-4">
      <h1 class="text-4xl font-bold text-default-950 mb-8">Program Studi</h1>
      <p class="text-lg text-gray-700 mb-8">STIE Dwimulya menawarkan program studi yang relevan dengan kebutuhan pasar kerja dan didukung oleh kurikulum yang komprehensif serta dosen-dosen berpengalaman.</p>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {studyPrograms.map(program => (
          <div class="bg-white rounded-lg shadow-md p-6">
            <h2 class="text-2xl font-bold text-default-950 mb-4">{program.title}</h2>
            <p class="text-gray-700 mb-4">{program.description}</p>
            <a href={`/program-studi/${program.title.toLowerCase().replace(/ /g, '-')}`} class="text-primary hover:underline">Selengkapnya</a>
          </div>
        ))}
      </div>
    </div>
  </section>
</MainLayout>