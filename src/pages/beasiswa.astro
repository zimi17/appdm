---
import MainLayout from '../components/layout/MainLayout.astro';
import { scholarships, generalRequirements, requiredDocuments, applicationProcedure, scholarshipTermination, recipientObligations } from '../data/scholarships.ts';
---

<MainLayout title="Beasiswa - STIE Dwimulya">
  <section class="py-10 lg:py-20">
    <div class="container mx-auto px-4">
      <h1 class="text-4xl font-bold text-default-950 mb-8">Program Beasiswa STIE Dwimulya</h1>
      <p class="text-lg text-gray-700 mb-8">STIE Dwimulya menyediakan berbagai program beasiswa untuk mendukung mahasiswa berprestasi dan mereka yang membutuhkan bantuan finansial. Komitmen ini sejalan dengan upaya kami untuk meningkatkan akses pendidikan tinggi bagi seluruh lapisan masyarakat.</p>

      <h2 class="text-3xl font-bold text-default-950 mb-6 mt-12">Jenis-Jenis Beasiswa di STIE Dwimulya</h2>
      {scholarships.map(category => (
        <div class="mb-8">
          <h3 class="text-2xl font-bold text-default-950 mb-4">{category.category}</h3>
          {category.programs.map(program => (
            <div class="bg-white rounded-lg shadow-md p-6 mb-6">
              <h4 class="text-xl font-bold text-default-950 mb-2">{program.title}</h4>
              <p class="text-gray-700 mb-4">{program.description}</p>
              
              <h5 class="text-lg font-bold text-default-950 mb-2">Persyaratan:</h5>
              <ul class="list-disc list-inside text-gray-700 mb-4">
                {program.eligibility.map(item => (
                  <li>{item}</li>
                ))}
              </ul>

              <h5 class="text-lg font-bold text-default-950 mb-2">Prosedur Pendaftaran:</h5>
              <p class="text-gray-700">{program.application}</p>
              {program.duration && <p class="text-gray-700 mt-2"><strong>Jangka Waktu:</strong> {program.duration}</p>}
            </div>
          ))}
        </div>
      ))}

      <h2 class="text-3xl font-bold text-default-950 mb-6 mt-12">Persyaratan Umum dan Prosedur Pendaftaran</h2>
      <h3 class="text-xl font-bold text-default-950 mb-2">Persyaratan Umum:</h3>
      <ul class="list-disc list-inside text-lg text-gray-700 mb-4">
        {generalRequirements.map(item => (
          <li>{item}</li>
        ))}
      </ul>

      <h3 class="text-xl font-bold text-default-950 mb-2">Dokumen Pendukung:</h3>
      <ul class="list-disc list-inside text-lg text-gray-700 mb-4">
        {requiredDocuments.map(item => (
          <li>{item}</li>
        ))}
      </ul>

      <h3 class="text-xl font-bold text-default-950 mb-2">Prosedur:</h3>
      <ol class="list-decimal list-inside text-lg text-gray-700 mb-4">
        {applicationProcedure.map(item => (
          <li>{item}</li>
        ))}
      </ol>

      <h2 class="text-3xl font-bold text-default-950 mb-6 mt-12">Ketentuan Penghentian dan Kewajiban Penerima</h2>
      <h3 class="text-xl font-bold text-default-950 mb-2">Penghentian Beasiswa apabila:</h3>
      <ul class="list-disc list-inside text-lg text-gray-700 mb-4">
        {scholarshipTermination.map(item => (
          <li>{item}</li>
        ))}
      </ul>

      <h3 class="text-xl font-bold text-default-950 mb-2">Kewajiban Penerima:</h3>
      <ul class="list-disc list-inside text-lg text-gray-700 mb-4">
        {recipientObligations.map(item => (
          <li>{item}</li>
        ))}
      </ul>

      <div class="mt-12 text-center">
        <p class="text-lg text-gray-700">Untuk informasi lebih lanjut mengenai prosedur pendaftaran, persyaratan detail setiap jenis beasiswa, dan jadwal pendaftaran, calon mahasiswa disarankan untuk menghubungi langsung bagian kemahasiswaan STIE Dwimulya.</p>
      </div>
    </div>
  </section>
</MainLayout>